=== VERIFY: Overview hero "70,000 people borrow against crypto to spend 
  checks 24, confirmed 22
  - REFUTED | A Go comment records 'live census 2026-07-31: 7,503 of ~9,700 DM accounts hold USDC on BOTH sides' (assemble.go:955-956), so the real strip may print ~9,700, 'still two orders short of 70,000'. | CORR: Say 'about 7× short' for ~9,700 and 'about 50× short' for 1,412. Also missed: spec §8 :318-320 says the live book is '~10,000 positions across both engines with 6 refused (riskd log, batch 18,251)' — if the DM alone is ~9,700, the legacy engine would be ~300, not the ~8,500 the mockups assume (:321-322). The two in-repo figures are in tension; neither is a served number.
  - UNVERIFIABLE | P1's 138-char H1 stays at 3 lines (pixelPinMoves says the fallback is needed only 'if it wraps'). | CORR: Treat the 4-line outcome as likely, not marginal: make the 129-char tail (' This is how close each borrower is to liquidation — right now.') the primary P1 wording, or measure before re-baselining.
  ! PROBLEM: P1 keeps '70,000 people' — a figure that cannot be verified against code, contract or Go source (the task's TRUE-of-the-system law); the only sources are external and say 'active cards', not people (a person may hold several cards). It is also the one number on the front door with no evidence behind it, under a dek that promises 'every number opens its evidence' (copy.ts:7) — exactly the B2 objection in .superpowers/sdd/2026-09-16-ui-convergence/task-12-user-readthrough.md:104. P1 removes the co
  ! PROBLEM: P1 '70,000 people spend crypto on a Visa card. Some borrow against it.' — in Borrow Mode cardholders spend a BORROWED stablecoin, not their crypto (recon/v4-proposal-arfc-post1.html:12 'they borrow a stablecoin to settle a card transaction while preserving their underlying asset exposure'; help 326983), and Direct Pay spends USDC/LiquidUSD. 'spend crypto' is loose, and 'it' in 'Some borrow against it' has an ambiguous antecedent (the card or the crypto). Not a law break, but not cleanly TRUE eit
  ! PROBLEM: P1 at 138 characters very likely wraps the H1 to four lines (the pin shows line 2 at 49 chars already at the 900px edge; P1's third line would be 48 chars of wider glyphs). A fourth line adds ≈48px, moves everything below the H1 in BOTH overview PNGs beyond glyph changes, and pushes the Scenarios card's bottom from ≈885 to ≈933, failing overview.spec.ts:200-209. The reader listed the fallback as conditional; it should be primary or measured first.
  ! PROBLEM: P1/P2 tail 'each borrowing account' and the companion label 'Borrowing accounts' sit above a census (Aggregate.positions) that counts collateral-only, zero-debt accounts (assemble.go:989-997 set nonzero on swept collateral alone; no later zero-debt skip). 'Borrowing accounts 1,412' would then over-claim for any zero-debt row on the live book — an untrue label, the same over-inclusion the Book's 'N borrowing accounts' (cash-view.ts:256) already carries. The demo has no such rows (grep '"debt": "0
  ! PROBLEM: Companion relabel 'Accounts' → 'Borrowing accounts': the pin shows only ~40px between the strip dek's longest line (x≈948) and the stats column (x≈988); the extra label width narrows the 1fr column, so the two-line dek very likely becomes three lines, the strip grows, and the entries' bottom (≈885) crosses 900 — failing overview.spec.ts:200-209 and moving both overview pins below the strip. Editing the literal in place also adds a fourth copy-in-component string rather than moving the labels to 
  ! PROBLEM: P3 loaded text '1,412 accounts borrow against crypto…' carries the same zero-debt over-inclusion as above, now in the H1; and a served positions === 0 on a non-withheld engine would print '0 accounts borrow against crypto to spend on a Visa card. This is how close each of them is…' — a knowable zero, so not a law break, but nonsense copy the proposal leaves undecided. P3 also makes the H1 reflow between fallback and loaded text on first paint.
  ! PROBLEM: No proposal sums or compares Cash with the legacy Aave market — confirmed clean. No proposal renders a refused/withheld/unread value as zero or 'No' — confirmed clean (P3's null branch is figure-free).
  + MISSED: This finding is already adjudicated on record: .superpowers/sdd/2026-09-16-ui-convergence/task-12-user-readthrough.md:100-105 (B2, BLOCKER: '"Each of them" promises 70,000 positions; the product shows 1,412 (9,964 with legacy)'), task-12-readthrough-triage.md:19 (B2 → **OWNER (rank 2)**, suggested clause '…; N of them borrow against it today' — the owner's w
  + MISSED: A THIRD rendering of the same census on the page: the pipeline's Compute step prints 'batch 18,251 · 1,412 Cash accounts' (web/lib/verification-view.ts:399 `sub: \`batch · ${n(census.accounts)} Cash accounts\``, :403; CENSUS_REFUSED :284 'Cash accounts withheld'), pinned by overview.spec.ts:40, :103, :127-129. It is below y=900 (not in the pin) but gives the
  + MISSED: In-repo ether.fi source the reader said did not exist: recon/v4-proposal-arfc-post1.html:7 'our Visa card product used by tens of thousands of cardholders' and :12 'Cash lets cardholders spend against … collateral …: they borrow a stablecoin to settle a card transaction while preserving their underlying asset exposure … roughly $25M in active borrows'. Suppo
  + MISSED: ether.fi's help center already shows borrowers their own liquidation risk: https://help.ether.fi/en/articles/558503-monitoring-your-borrow-position-and-liquidation-risk — 'you'll see a visual bar that represents how much you've borrowed relative to your collateral', 'Clicking Details opens the Borrow Position view, which shows: … Your current loan-to-value (
  + MISSED: Pins the Book's 'borrowing accounts' unit carries, relevant to openQuestion 3 (relabelling the Book/strip unit): web/tests/e2e/book.spec.ts:76 ('Debt Manager engine · OP Mainnet · 2 borrowing accounts'), :138, :146; web/tests/unit/cash-view.spec.ts:288, :297, :305, :311, :336; and web/tests/e2e/screenshots.spec.ts-snapshots/book-dark.png / book-light.png (th
  + MISSED: Spec §8 :318-320 states the live book is '~10,000 positions across both engines with 6 refused (riskd log, batch 18,251)', while assemble.go:954-955 says '~9,700 DM accounts' alone — an in-repo tension the reader's openQuestion 4 does not name; if the Go comment is right, the legacy engine is ~300 positions, not ~8,500, and book.demo.json's 8,552 legacy figu
  + MISSED: Demo fixtures cannot expose the collateral-only over-inclusion: positions-dm-demo-page-1.json and -2.json have zero rows with debt "0"; book.demo.json debt_manager hf_histogram infinite_count is 0 (:241). Any 'borrowing' wording is only testable against live data.
  + MISSED: Line-number drift in the reader's citations: generate-demo.mjs comment is :29-31 (not 27-30); the census comment is assemble.go:954-955 (not 955-956); `if !nonzero { return nil, nil, nil }` is :995-997; aggregate() starts :1196; the spec's 'no public REST API' line is :45; the mockup's Accounts line is front-door.html:133 (not 132); Aggregate.positions is op
=== VERIFY: Kit gaps: pending headline + disabled
  checks 27, confirmed 25
  - REFUTED | Proposal (b): retiring lab.module.css:4-7 and re-aiming lab.spec.ts:518-522 is the whole test surface; size trivial, 2 test lines. | CORR: Two tests pin the opacity look, not one: lab.spec.ts:1494-1498 (data-state listing-unreadable) must be re-aimed alongside :518-522, or (b) fails that test. The other toBeDisabled-only sites (lab.spec.ts:266, 519, 529, 531, 837, 840, 963; keyboard.spec.ts:407, 415, 429) are unaffected.
  - REFUTED | notFeasibleNow: an animated in-flight indicator would need a reduced-motion contract that does not exist yet; no motion tokens exist. | CORR: A reduced-motion contract already exists and is used by three stylesheets; only the "no motion tokens in tokens.css" half is true (grep -i 'motion|--dur|--ease' web/app/tokens.css -> no token).
  ! PROBLEM: (b) as specified fails an uncited test: web/tests/e2e/lab.spec.ts:1497-1498 (listing-unreadable) also asserts `toHaveCSS("cursor", "not-allowed")` and `toHaveCSS("opacity", "0.45")` on lab-run and lab-compare. Retiring lab.module.css:4-7 without re-aiming that test breaks it; the "2 test lines" size is really 4 across two tests.
  ! PROBLEM: (a) reverses a written lib doctrine the reader did not surface: web/lib/lab-headline.ts:2-4 `// Every state the workspace can be in has its own sentence; a definition, a // run in flight and every refusal use the dashed tone, because none of them is // a verdict.` (traced to the owner-approved plan docs/plans/2026-09-15-ui-scenarios.md:1360-1363, spec §3.5). The change is therefore a policy change, not only mechanical wiring; the comment must be rewritten and the direction likely captured under 
  ! PROBLEM: (a) newWords leaves a public styleguide sentence false: web/app/styleguide/page.tsx:299 `refused is every non-answer, in ink-2 with a dashed chip.` Once a sixth tone exists, a read in flight is a non-answer that is not refused, so that sentence must be amended, not just extended with the proposed clause.
  ! PROBLEM: (a)'s proposed styleguide clause `pending is a read in flight: ink-2, no dashed chip, the header busy` is TRUE only if the companion chip change ships in the same commit: without it the Book (cash-view.ts:244) and Inspector (inspector-view.ts:165) loading headers still print a dashed `Identity pending` chip under a pending header, and the Scenarios listing-loading header still prints the dashed `Identity missing`. The two proposals are presented as separable; they are not if that sentence is pub
  ! PROBLEM: notFeasibleNow's claim that a pulsing indicator 'would need a reduced-motion contract that does not exist yet' is false — globals.css:114 already kills animation under prefers-reduced-motion and activity.module.css:18 / states.module.css:17 already gate their own animations. Only the absence of motion tokens is true.
  ! PROBLEM: Minor exactness slips in cited facts: HistorySurface's aria-busy is at :128 (reader: :127); the Area Y primary figure 1.87 is over --bg while the library's ground is --panel (1.85 light / 2.59 dark) — conclusion unchanged.
  + MISSED: A twelfth in-flight arm: web/lib/lab-address.ts:226 `if (stress === null) return empty("loading", address, refused(`Running the committed scenarios for ${short}…`, ...));` — same sentence as :223, must also move to pending().
  + MISSED: Second opacity/cursor pin: web/tests/e2e/lab.spec.ts:1494-1498 (listing-unreadable test) asserts `opacity 0.45` and `cursor not-allowed` on lab-run and lab-compare.
  + MISSED: Documented doctrine that in-flight states deliberately wear the dashed tone: web/lib/lab-headline.ts:2-4 and docs/plans/2026-09-15-ui-scenarios.md:1360-1363; also web/lib/activity-view.ts:477 `// Before the first page answers there is no answer to tone;` — the proposal must rewrite these comments.
  + MISSED: Styleguide note sentence at web/app/styleguide/page.tsx:299 `refused is every non-answer, in ink-2 with a dashed chip` becomes false under a pending tone and needs editing beyond the added clause.
  + MISSED: No e2e exercises the Scenarios listing-loading state (grep 'listing-loading' over web/tests/e2e returns nothing), so today's dashed `Identity missing` on that header is code-derived and unasserted; the companion chip proposal has no failing test to re-aim there, only lab-view.spec.ts:37-41 to extend.
  + MISSED: Book pin gate nuance: screenshots.spec.ts:62 waits on `book-kpi-near` not aria-busy, but that tile's aria-busy is `pending={walking}` (BookSurface.tsx:149) with `walking = summary !== null && ...` (cash-view.ts:258), which is false during the initial /v1/book read; the pin is settled by `waitUntil: "networkidle"` (:130), not by the marker alone. The reader's
  + MISSED: Reduced-motion contract exists: web/app/globals.css:112-120, web/app/feed/activity.module.css:18-24 (livePulse), web/components/states/states.module.css:17-24 (shimmer), web/components/drawer.module.css:37-40.
  + MISSED: Additional in-flight unit assertions that pin emphasis/variant but NOT tone, so they are no-ops for (a) but worth listing as checked: history-view.spec.ts:436-438 (Aave loading emphasis), activity-view.spec.ts:448-451, lab-view.spec.ts:84-88, lab-address.spec.ts:62-68, cash-view.spec.ts:93-95 (`variant` "pending"), cash-summary.spec.ts:62-66 (`variant` "pend
=== VERIFY: Activity amounts + small honest wins (Activity /feed and Ins
  checks 51, confirmed 43
  - REFUTED | currentStrings: the Cash liquidation repaid figure prints as "358.12" on Activity (and Inspector detail "repaid {358.12} USD; seized {0.0000940065 weETH}") | CORR: Activity extract: "debt repaid 0.35812" (row 19) and "0.409762" (row 37); before_debt_usd "716240" → "0.71624". Inspector detail (if such a row existed) would read "repaid 0.3581 USD; seized <0.0001 weETH". The demo Cash liquidations are sub-dollar positions ($0.72 / $0.82 total debt), not $358/$409
  - REFUTED | fact: /v1/book carries Aggregate.value_decimals (Cash 6, legacy 8), the demo mocks it in the pin, and three pages already fetch it client-side with getSolventClient().book(signal) (cash-book.tsx:133-1 | CORR: Two pages (Book via cash-book.tsx, Verification) fetch /v1/book client-side; the API page only prints a code sample containing `client.book()`
  - REFUTED | fact: the Activity extract prints "2,500", "0.65625 weETH", "358.12" while the Amount column shows the raw deltas "-2380952380" / "-341066" / "-390249"; DEMO rows 5, 19, 37 with debt_repaid "250000000 | CORR: Cash extracts read "debt repaid 0.35812" (row 19) and "debt repaid 0.409762" (row 37)
  - REFUTED | fact (layout): Activity stack = body padding 28 + page padding 28, header, two tiles (~100), strip, SectionHead, controls (two rows), tail notice, 12px gaps → table head near y≈560-620, rows ~35px, so | CORR: Table head ≈ y 700-720, rows ≈ 41px: rows 0-3 are surely in view, row 4 straddles the fold, row 5 (Aave liquidation) is probably just below it, rows 7-12 are certainly below it. Exact positions need the pin run
  - UNVERIFIABLE | Proposal A: activity.spec.ts / r1-fixes.spec.ts must mock /v1/book where the raw state is asserted | CORR: 
  - UNVERIFIABLE | Proposal B pixelPinMoves: row 5 (the Aave liquidation) is inside the first viewport, so its extract's "0xA0b86991…" → "USDC" moves the pin; Cash liquidations (rows 19, 37) are below the fold | CORR: State it as 'may or may not move the Activity pins; the pin run decides' rather than 'Yes'
  - REFUTED | Proposal E pixelPinMoves: rows 2, 7, 8, 9, 10, 11, 12 (record-only rows inside the first viewport) change | CORR: The pins move because of row 2 alone; rows 7-12 are below the fold
  - REFUTED | Proposal F assertsFacts and example: "Cash 2 · 767.88 USD repaid · legacy 1 · 2,500 USDC repaid" computed from debt_repaid at debt_decimals with BigInt; Cash debt_repaid USD-6 (p5_events.go:393-396),  | CORR: Example should read "Cash 2 · 0.767882 USD repaid · legacy 1 · 2,500 USDC repaid" (fixture-derived), and the sub must keep the 'loaded rows' scope
  ! PROBLEM: Proposal B / currentStrings / Proposal F quote Cash repaid figures 1000× too large: DEMO row 19 debt_repaid "358120" at debt_decimals 6 renders "0.35812" (packages/client-ts/src/decimal.ts:171-188 formatUnits; groupDecimalString leaves it), row 37 "0.409762", before_debt_usd "0.71624", and F's Cash sum is "0.767882 USD", not "767.88". The proposed wording "debt repaid 358.12 USD" / "of 716.24 USD before" / "767.88 USD repaid" would be FALSE of the fixture (and of the wire's scale). The Inspector
  ! PROBLEM: Proposal B says the Activity pins move because row 5 (Aave liquidation) is inside the first viewport. By the CSS (56px header kit.module.css:8, `.shell` padding 0 24px globals.css:52, `.page` padding 28px activity.module.css:6, body line-height 1.55 globals.css:23, `.sec` margin-top 26px kit.module.css:60, td 9px padding kit.module.css:106) the table head lands ≈ y 700-720 and rows are ≈ 41px, so row 5 begins ≈ y 919 — at or below the 900px edge. Whether B moves a pinned first viewport is undete
  ! PROBLEM: Proposal E's pin reasoning names rows 7-12 as inside the first viewport; they begin ≈ y 1000+ and are outside the 1440×900 capture. Only row 2 is in view. (The pins still move, via row 2.)
  ! PROBLEM: Fact and Proposal A rationale claim "three pages already fetch [/v1/book] client-side ... ApiSurface.tsx:27". ApiSurface.tsx:27 `const book = await client.book();` is inside the `quickstart()` template-literal string (ApiSurface.tsx:20-21) — printed documentation, not a fetch. Only the Book (cash-book.tsx:133-134) and Verification (VerificationSurface.tsx:86-87) pages fetch /v1/book.
  ! PROBLEM: Proposal D corrects only the two `supply`/`opaque` rows but leaves the fixture's DM `collateral_enabled` row (events-demo-near.json index 2, raw_type "collateral_enabled", engine debt_manager) — also a shape the server cannot emit: dmEventDisplay (internal/store/p5_events.go:152-177) has no collateral-toggle type and the contract maps collateral_enabled/disabled to "the Aave usage-as-collateral toggles" only (api/openapi.yaml:4057-4058). A fixture labelled 'the wire's real shape' that keeps that
  ! PROBLEM: Proposal F's per-engine Cash sum adds USD-6 `debt_repaid` payload facts from liquidations at different blocks/times into one figure; it does not cross engines (allowed), but the tile sub must keep the 'among the loaded rows' scope (activity-view.ts:484) or it overclaims a window as a total; the reader's example omits that scope.
  ! PROBLEM: Proposal A's claim that activity.spec.ts / r1-fixes.spec.ts 'assert the raw state' and so need a /v1/book mock is directionally right, but the outcome without a mock is environment-dependent: unrouted requests go to NEXT_PUBLIC_SOLVENT_API_URL or http://localhost:8080 (web/lib/api.ts:11-19), and the tree's api-server.log shows a local API is sometimes running — the raw assertions at activity.spec.ts:166/:169 and r1-fixes.spec.ts:80/:93 could pass or fail depending on that. Route /v1/book explici
  + MISSED: The Inspector demo fixture's third impossible row: events-demo-near.json index 2 is engine debt_manager, type/raw_type "collateral_enabled", unit none — the Debt Manager deriver emits no collateral-usage toggle (internal/store/p5_events.go:152-177 dmEventDisplay: borrow, repay, liquidation, supplied, withdraw_borrow_token, migration_genesis, five config type
  + MISSED: The demo Cash liquidations are sub-dollar: generate-demo-secondary.mjs:394-395 takes `before = position.total_debt` and `repaid = before / 2n` from liquidatable demo positions whose total_debt is "716240" / "819525" at value_decimals 6 (positions-dm-demo-page-1.json), i.e. $0.72 / $0.82. Any proposal that prints these ('0.35812 USD', '0.71624 USD before') wi
  + MISSED: "(opaque units)" is never visible on the Inspector: inspector/[addr]/ActivityTable.tsx:45 renders the unit chip only when `!r.rawUnits`; a raw row shows the value, the symbol and the refused pill "raw units", with the opaque sentence (feed-view.ts:148) as the hover title only.
  + MISSED: Proposal A's test impact list omits web/tests/unit/activity-view.spec.ts:138-139, which builds the raw arm explicitly via `base({ valueDecimals: {} })` — unaffected by a surface-level book fetch, so the lib pin survives; only the e2e pins (activity.spec.ts:166,:169; r1-fixes.spec.ts:80,:93) depend on the page's scale source.
  + MISSED: activity.spec.ts:225-245 (listed as a file B must change) does not assert the repaid asset or unit: it checks "seized 0.65625 weETH", the bonus dashes, the liquidator href and the note. The only pin of the "0xA0b86991…" prefix is the unit spec activity-view.spec.ts:99-108 (exact `toEqual` on the whole detail object) and :110 (`repaidAsset: null`).
  + MISSED: openapi.yaml line citations drift by a few lines in several places: LiquidationDetail `properties:` is :4121 (before_debt_usd USD-6 text is :4128-4130), realized_bonus_bps is :4142-4144 (not :4139-4141, which is deficit_paired), RateIndex is :3308-3334, debtmanager.go arithmetic is :28-30.
  + MISSED: The wire's own served note (cmd/api/p5_events.go:425, carried as `liquidation.note` and rendered as page text by feed/ActivityTable.tsx:24-28) already states on every Cash liquidation row that "`debt_repaid`/`before_debt_usd` are the Debt Manager's USD-6 figures" — so the 'before_debt_usd scale' open question is answered by a served sentence, not only by ser
  + MISSED: The Activity pin's vertical budget: the AppShell header (56px, kit.module.css:8) and `main.shell` (globals.css:49-53) sit above `.page`; DegradationBanner renders nothing with the stream aborted (DegradationBanner.tsx:81-94). Row height uses body line-height 1.55 (globals.css:23). With those, rows 0-3 are in the pinned viewport, row 4 straddles, and rows ≥5 
=== VERIFY: A — numbers a visitor cannot reconcile (Book 2+47 vs History
  checks 43, confirmed 35
  - REFUTED | Verification restates the 87 fact 'seven times in the first viewport'. | CORR: Six numeric restatements; seven only if the numberless status row is counted.
  - UNVERIFIABLE | (a) pixelPinMoves: '· 49 in all' likely adds a third line, growing the KPI row ~18-20px and shifting the cards below. | CORR: Most likely the sub stays at two lines and nothing below shifts; the Book PNGs still re-baseline because the pixels change. 'Yes, Book pins move' stands; the third-line/row-growth reasoning does not.
  - REFUTED | (d) pinsAffected and pixelPinMoves: materiality.spec.ts:48-59 (three strings), book-headline.spec.ts:33, 69, book.spec.ts:750 breaks, :97 still passes; only the Book PNGs re-baseline. | CORR: (d) also re-baselines overview-dark.png and overview-light.png (the Overview live card carries the Book dek). No e2e/unit Overview spec pins that sentence (grep 'technically' in web/tests → book.spec only).
  - REFUTED | (e) pinsAffected: lab.spec.ts:256, lab-movers.spec.ts:79; lab.spec.ts:1411 unaffected; pixel: none, caption below y=900. | CORR: Add lab-movers.spec.ts:112 to pinsAffected; the pixel claim (no Lab PNG change) holds.
  - UNVERIFIABLE | (f) same pins as (c); still ≤ 2 lines, no layout shift. | CORR: 
  - REFUTED | (b, data) pinsAffected includes web/tests/unit/instant-split.spec.ts:44. | CORR: Not a pin on the evidence fixture; drop it.
  - REFUTED | (b, data) pixelPinMoves: Verification, Overview (pipeline line '87/87 gated rows exact · drift 0') and Inspector PNGs re-baseline; Book/History/Lab/Activity/API untouched. | CORR: Overview PNGs do NOT move under (b, data); only Verification and Inspector PNGs re-baseline (four files, not six).
  - REFUTED | (b, data) files: web/tests/fixtures/evidence-manifest.json, a new generate-demo-evidence.mjs, and proof-contract.gen.ts 'if it embeds the example'. | CORR: The sanctioned path is to change the contract EXAMPLE at api/openapi.yaml:2049-2065 and rerun generate-proof.mjs (which regenerates evidence-manifest.json, evidence-proof-failed.json and proof-contract.gen.ts); a separate generator reading drift-report.json would violate the fixture rule and fail the fidelity spec. Files list must add api/openapi.yaml, web/tests/fixtures/generate-proof.mjs, web/te
  ! PROBLEM: (d) pixelPinMoves says 'Book pins' only — FALSE by omission: the Overview live card renders the identical dek (web/app/overview/OverviewSurface.tsx:143 `{headline.dek}`; overview-dark.png y≈578-600), so overview-dark.png and overview-light.png also re-baseline.
  ! PROBLEM: (b, data) pixelPinMoves claims Overview PNGs re-baseline for the pipeline line — the Pipeline (OverviewSurface.tsx:199) is below the three cards and outside overview-dark.png's 900px viewport; only Verification and Inspector PNGs move. It also lists web/tests/unit/instant-split.spec.ts:44 as a pin, but that line is a hardcoded literal in a splitInstants test, not fixture-derived.
  ! PROBLEM: (b, data) method contradicts the repo's fixture rule: evidence-manifest.json is a VERBATIM extract of api/openapi.yaml's GET /v1/evidence example (web/tests/fixtures/generate-proof.mjs:11-12, 122-124), proof-contract-fidelity.spec.ts:150 asserts the Developers extract equals EVIDENCE_MANIFEST, and generate-proof.mjs:129-135 hard-fails if the example loses 87/87 or 29/29 before deriving evidence-proof-failed.json (84/87, 26/29; 18 test hits). 'Regenerate from drift-report.json with a new generato
  ! PROBLEM: (e) shown == total wording 'all 5 accounts moved' breaks web/tests/unit/lab-movers.spec.ts:112 ('showing 1 of 1 account moved'), which is not in pinsAffected, and yields the awkward singular 'all 1 account moved'.
  ! PROBLEM: (e) moves MoversTable.tsx:35's literals into lib but leaves web/app/lab/TransitionCard.tsx:72 `label: "Most affected accounts →"` composed in a component — the same noun, same law.
  ! PROBLEM: (e, adjacent) relabels the tile but leaves the Lab dek's '425 accounts move to a worse band; none improve.' (web/lib/lab-headline.ts:74) using 'move' for the web's band count, so 'move/moved' still spans two populations (bandChanged 425 vs movers_total 118) in the pinned viewport.
  ! PROBLEM: (b, disclosure) wording 'the two per-engine weld tallies are not a breakdown of the gated rows' is TRUE of the deployed loader (p5_evidence.go:332-348: welds are len(aave_rows)/len(dm_rows) by verdict, gate ignored; 10 of the 14 Aave rows are advisory) but is stated nowhere in the contract (ReconcileWeld, openapi.yaml:5811-5818, has no description). It is a web sentence about server internals with no contract sentence to quote; if the loader ever made welds gate-aware the row would go silently f
  ! PROBLEM: (a) pixel reasoning ('likely adds a third line, growing the KPI row ~18-20px and shifting the Distance/Needs-attention cards down') is unsupported: at the tile's ~164px content width '$100 · 49 in all' fits on the existing second line. The conclusion (Book PNGs re-baseline) still holds; the layout-shift claim should be dropped or marked unverified.
  ! PROBLEM: (a) caveat: '49 in all' is true of the walk's own partition, but on live data the Book's whole-walk total (material + belowLine over /v1/positions rows) and History's engine aggregate (sweep-cut liquidatable_positions) can differ, so the proposal does not by itself make the two pages agree — the reader's own openQuestion 2; the summary's 'so the Book states the 49 History prints' is true only on the welded demo.
  + MISSED: The fixture's 87/21 is not an arbitrary example: over the committed drift-report.json the pre-P3 row families tally to exactly 87 gated (dm_rows 29 + dm_weld 8 + dm_index_check 1 + aave_rows 4 + aave_weld 4 + golden 9 + collateral_replay 3 + freshness.sampled 29) and 21 advisory (dm_index_check 7 + aave_rows 10 + aave_weld 4); p3_task6.rows add 30,751 gated 
  + MISSED: Overview carries the Book dek (OverviewSurface.tsx:143) — a second pinned occurrence of '47 more positions … total $109.45 — below the $100 line' (overview-dark.png y≈578-600) that (d) rewrites; overview-*.png re-baseline.
  + MISSED: Lab tile 'Newly liquidatable 118 · accounts · was 49, now 167' (web/app/lab/LabTiles.tsx:50; lab-dark.png y≈400) — a third surface printing the 49 the Book never states (49 + 118 = 167).
  + MISSED: Lab dek 'Bad debt would rise by $40K if all 167 were liquidated at the shocked prices. 425 accounts move to a worse band; none improve.' (web/lib/lab-headline.ts:74) — 'move' for bandChanged, adjacent to 'Accounts moved 941' and 'across 118 accounts'; also the library row chip '+$1.2M liquidatable · 118 accounts' (web/lib/lab-library.ts:112; lab-dark.png y≈2
  + MISSED: web/app/lab/TransitionCard.tsx:72 `label: "Most affected accounts →"` — a component-composed literal of the section noun, alongside MoversTable.tsx:35.
  + MISSED: web/tests/unit/lab-movers.spec.ts:112 (`showing 1 of 1 account moved`) — the shown == total branch (e) rewrites; :132 (total null) unchanged.
  + MISSED: Provenance chain for the evidence fixture: web/tests/fixtures/generate-proof.mjs:11-12, 122-140 (verbatim contract example; derived evidence-proof-failed.json 84/87 & 26/29), web/tests/unit/proof-contract-fidelity.spec.ts:150, web/lib/proof-contract.gen.ts:3754-3770 (embeds the example) — all bound to the 87.
  + MISSED: Loader weld order is Aave-first (cmd/api/p5_evidence.go:346-348) while the fixture/contract example is debt_manager-first — a regeneration 'by the loader's own rules' reorders proof-card rows (verification-view.spec.ts:663-664).
=== VERIFY: A — the repo's front door: verified fact sheet for a rewritt
  checks 38, confirmed 37
  - UNVERIFIABLE | The ci.yml badge will render green for a stranger. | CORR: 
  ! PROBLEM: Proposal 1 sentence 'it ingests OP Mainnet (chain 10) and Ethereum mainnet (chain 1) logs into Postgres, derives every borrower position' implies positions are log-derived; Debt Manager collateral is explicitly NOT event-derivable and comes from periodic on-chain view sweeps (internal/snapshot/snapshot.go:2-5). Tighten to '…derives positions from those logs and periodic collateral sweeps'.
  ! PROBLEM: Proposal 1 sentence 'the web re-extracts it in CI' overstates: web/tests/unit/proof-contract-fidelity.spec.ts:1-4 re-extracts 'the load-bearing fields' behind the Developers page, not the contract, on every unit run (which CI's `npm run test:e2e` includes). Say 'the web re-extracts its load-bearing fields on every test run'.
  ! PROBLEM: Proposal 1's run block 'cp .env.example .env; make db-up; make run-indexer; …' fails as written on a fresh clone: .env.example ships SOLVENT_RPC_OP= / SOLVENT_RPC_ETH= empty and config.Load refuses without them (internal/config/config_test.go:52 TestLoadFailsWhenRPCEnvMissing; Makefile:42). The README must add 'fill in SOLVENT_RPC_OP / SOLVENT_RPC_ETH' before run-indexer, and note that riskd/api refuse to start until the indexer has migrated the schema (cmd/api/main.go:32-38).
  ! PROBLEM: Proposal 1's 'What is proven' list (rewind, reconcile, risk math, fidelity gates) cites complex components in the README; D-006 clause 5 (roadmap/decisions/D-006-codex-approval-gate.md:30-31) defines README/portfolio citation as a 'trusted' use permitted only after Codex approval. The reader raised this as an open question but did not compile which components carry a Codex-approved closing verdict — that list is a precondition for the sentences, not an open question.
  ! PROBLEM: Proposal 1 hardcodes '1807 Go test funcs, 1010 web unit + 237 e2e, 295 client-ts' — true at f28d89d, false after the next test commit; either omit or stamp the commit.
  ! PROBLEM: Proposal 1 'Status: built and tested locally' undersells and slightly misstates: ci.yml runs the Go, race and web suites remotely and passes (STATUS.md:44); only the pixel pins are local-only (screenshots.spec.ts:25). Say 'built; CI green on ci.yml; pixel pins are a local gate; not yet publicly deployed'.
  ! PROBLEM: Proposal 2's 'unmasked' README screenshots will show the header pill reading 'Reconnecting' (or 'Not connected') because the script aborts /v1/stream (screenshot-pages.mjs:58) and livePillWords prints 'Live' only for an open stream with a base frame (web/lib/live-pill.ts:26-29; sse.ts:576). On the Overview that pill sits beside the hero 'A LIVE RISK SURFACE FOR ETHER.FI CASH … right now.' Any caption calling the image 'live' would be false; the caption must say 'demo fixtures, stream not connect
  ! PROBLEM: Proposal 2's run instruction 'node scripts/screenshot-pages.mjs …' silently depends on Node type stripping (the script imports tests/fixtures/*.ts at :20-24); web/package.json engines '>=20' does not guarantee it. The README (or the script's usage line) should state Node ≥22.18/23.6 or the --experimental-strip-types flag.
  ! PROBLEM: Open question 1's fix-path 'regenerate web/tests/fixtures/evidence-manifest.json to 30838' is not a fixture edit: generate-proof.mjs:11-12 makes that file a VERBATIM extract of api/openapi.yaml's /v1/evidence example, so matching the committed artifact means changing the contract's example (openapi.yaml:2053-2056) and the derived negatives, and proof.ts (unit fixtures) consumes it too — and it WOULD move both verification pins (the 87 is in the headline at y≈130).
  + MISSED: cmd/riskd/main.go:17 carries the same comment drift as cmd/api ('`TestRiskdMakesNoChainCalls`' vs actual TestRiskdLinksNoChainClient at cmd/riskd/purity_test.go:49).
  + MISSED: internal/risk/types.go:10 cites 'TestNoFloatInNonTestSources'; the function is TestNoFloatAnywhereInNonTestSources (floatcheck_test.go:178).
  + MISSED: The W3 work object (roadmap/work/W3-phase5-public-web.md:25) lists README.md in its declared scope, and its acceptance item 5 (:91-92) is 'README landing per spec §8 (live link above the fold, architecture diagram, screenshots, demo path)' — so the README rewrite is in-scope but a README without a live link is a partial delivery of W3 item 5, and the work ob
  + MISSED: The pinned Verification screenshot also shows the fixture's 'finished Jul 29, 02:14 UTC' and 'Proof pin 5f0b3e2a…' — both differ from the committed artifact (finished 2026-08-02T02:42:31Z; comparison_sha256 a34d7a53…), not just the 87 vs 30838 count.
  + MISSED: 'legacy market' wording has precedent in web copy (web/lib/activity-view.ts:222 'Cash and the legacy market run on different chains'; web/lib/cash-book.tsx:59 `const LEGACY = "aave_v3_etherfi"`), so the proposed 'legacy Aave v3 ether.fi market' matches the product's own vocabulary; the contract itself (openapi.yaml:24-25) calls it simply 'the Aave v3 ether.f
  + MISSED: VISION.md:3 is '> Draft until explicitly ratified by the project owner.' — the reader quotes it as the settled thesis.
  + MISSED: The design spec's deployment row (Fly.io + Vercel, docs/specs/2026-07-20-solvent-design.md:114-115) and web/README.md:69-73 'Vercel' section coexist with STATUS.md:36 'VPS account provisioning + domain purchase' — the reader's open question is real, and web/README.md is a second place that would need reconciling if the README states the VPS path.
  + MISSED: No banner renders in the screenshot-pages.mjs demo state (DegradationBanner.tsx:81 requires posture.batch from a stream snapshot), so the only visible artefact of the aborted stream is the pill word — worth knowing before deciding to crop.
=== VERIFY: Book: show all + legacy + not computed
  checks 33, confirmed 33
  ! PROBLEM: (a) alternative label "Show all 27 near-cap accounts ($830K)": "all N" states a total, but while the walk is running or stopped the tile beside it wears " · lower bound, walking" / " · lower bound, walk stopped" (cash-summary.ts:152-160 tileBoundNote) — the label would claim a total the tile itself refuses. The "more" form (hidden count + hidden Σ) does not claim a total and should be the only wording.
  ! PROBLEM: (d) "1 of 3 swept accounts failed their last sweep": `failed` counts every snapshot_sweeps row whose last status is not 'success' (store/risk.go:646), including rows with last_success_block 0 — accounts that were ATTEMPTED and never swept (migration 00003:2-3, :14) and are therefore SWEEP_NEVER-refused, not computed-and-stale. "swept accounts" is false for such a row, and the demo's one failed row may be exactly that kind. Use "attempted accounts" or the contract's own partition words (SweepCoun
  ! PROBLEM: (d) "not this one" is sound only because the served stamp is the batch's persisted compute-time snapshot (pass.go:97-127; store/risk.go:1262-1280; openapi.yaml:2467-2478). The detail should not be reused against any live sweep read (RiskSweepStateFor at cmd/api/read.go:383 is a live read used for supersession, not the wire stamp) — worth stating in the lib comment so a future refactor does not swap the source.
  ! PROBLEM: (b) edge case: on a served (not withheld) engine with computed_positions 0 and refused_positions > 0 the new line reads "0 of 0 computed positions are liquidatable · debt withheld · N refused" — a negative said over nothing computed. Today's line has the same "0 liquidatable" defect, so it is not a regression, but the new form should guard computed === 0 (omit the clause) rather than inherit it.
  ! PROBLEM: (b) drops the fold's total population ("8,552 positions") from the collapsed line whenever refused > 0 makes positions ≠ computed; the closed fold then states no total. Not false — the Positions tile inside carries it — but the reader did not note the loss.
  ! PROBLEM: (c-ii) is framed as "demo-only", but the COMMITTED fixture web/tests/fixtures/positions-dm-page-1.json refused row also carries `"total_debt": "1500000000"` (it is the demo generator's refusedTemplate, generate-demo.mjs:108-109) and mirrors the Go DB fixture (fixture_test.go:425-431). Nulling only the demo leaves the committed e2e's "Not computed" row (book.spec.ts:57-60) printing a debt the engine never writes; the generator comment at :142 ("borrowings are known") would also become false.
  + MISSED: meta.demo.json:211-220 carries a SweepCounts census for debt_manager — rows 1412, never_swept 6, failed_since_success 1, success 1405 — generated from the rows (generate-demo.mjs:268-274), which contradicts the book watermark stamp rows 3 / failed 1 (book.demo.json:44-52, :101-109). The demo disagrees with itself across /v1/meta and /v1/book; no page renders
  + MISSED: Fixture-authored refusal `detail` "collateral sweep never ran for this account" survives the phrasebook change: address-demo-refused.json:162, history-demo-near.json:1506, generate-demo-inspector.mjs:161 and :236. It is never rendered raw (InspectorDrawer.tsx:50 goes through plainCause; no raw `refusal.detail` render in web/app), but it contradicts both the 
  + MISSED: The committed positions-dm-page-1.json refused row carries total_debt "1500000000" plus the API's own SWEEP_NEVER note — the reader's 'production would serve null' argument applies to the committed fixture as much as to the demo, and cash-rows.spec.ts:86 / book.spec.ts:57-60 pin that display.
  + MISSED: `failed` in the stamp includes never-succeeded rows (last_success_block 0), i.e. SWEEP_NEVER-refused accounts — so the engine-wide 'failed row' need not be a computed stale account at all; this bears on (d)'s wording and on what an evaluator should infer from '1 of 3 rows failed'.
  + MISSED: The contract already states the stamp's provenance — openapi.yaml:2467-2478 Aggregate.sweep 'read from the batch's own persisted watermark vector' — which is the fact that makes (d)'s flag inference valid; the reader did not cite it.
  + MISSED: Why the Inspector pin survives (c-i) despite history-demo-near.json:1506 carrying a SWEEP_NEVER point: room-history.ts:68 phrases it with plainCause, but Sparkline.tsx:244/:307 emit it only as SVG <title> (hover), and historyFinding prints KIND_WORD 'not computed' without the cause (inspector-view.ts:327-333).
  + MISSED: Trust detail column CSS is `.checkSmall { max-width: 58%; ... }` (kit.module.css:158), not a fixed ~150px column.
  + MISSED: Line-reference drift in the findings: human-usd.ts is 48 lines (rules :37-47, not :349-369); assemble.go 874-876 → 896-897, 940-946 → 934-940, 1000-1008 → 1001-1008, 1009-1011 → 1010-1011, 1012 → 1013. The facts hold; the citations do not.
