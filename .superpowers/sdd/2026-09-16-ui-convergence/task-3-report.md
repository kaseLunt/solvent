# Task 3 report — `history-view` and the History page

Status: DONE (unit gates green; the e2e contract is written and NOT run — the controller runs it on the shared build per the pre-flight ruling).

Commits (pathspec-limited, scope gate OK on both): `339fa17` (lib + pins), `b5f78eb` (page + contract + retirements).

## What was done

- `web/lib/history-view.ts` — `deriveHistoryView(reading): HistoryView` with the brief's interface (one field added, see deviations). Kicker `History · {engineName}`. `ok`: headline emphasis = `observatoryTakeaway(response, axis)` verbatim, rest "", tone `ok` when a captured bucket backs the newest row and `refused` when the newest is withheld or no wire row exists; dek `One engine per view; a missing hour is a hole, never a zero.`; chips Engine (`engineName`, title = the wire id) · Stride (`describeStride`) · Range (`describeRange`) · Buckets (`{captured} captured · {withheld} withheld · {absent} absent`, warn when a hole exists, ok otherwise) · Served (`served_at` verbatim); tiles Debt · Collateral · Accounts · Liquidatable positions from `seriesNewestPoint(...).atNewestBucket` — money `humanUsd(wireBigInt(raw), usd_decimals)`, populations `groupInt` after `isWirePopulation`, sub `bucket {bucket_start}`; a newest bucket that is withheld / absent / null for the metric → `{ value: "—", sub: "withheld" | "no complete batch" | "not stated", tone: "refused" }`; finding = `gridReadingLine(response, axis)`; `chartLabel` = `{METRIC_LABELS[metric]} for {engineName} across rollup buckets`; doctrine = `HISTORY_DOCTRINE` (the intro + the chart's six method notes, verbatim) + the wire's `notes`. `loading` → refused `Loading the history of {named}…`, no tiles, Buckets chip "pending". `degraded` → state `degraded`, refused `The durable rollup for {named} is unavailable.`, dek = `sentence(serverMessage)`, chip Rollup unavailable, doctrine + the deployment note (verbatim from the old refusal panel). `error` → state `unavailable`, refused `The history of {named} could not be fetched.`, dek = `sentence(message)` + `The record is unavailable, and none of it is being shown as empty.`. An `usd_decimals` outside `isWireScale` → state `unavailable`, `The history of {named} cannot be read.`, the field named, before any figure is formatted at it. Exports `HISTORY_DEK`, `HISTORY_INTRO`, `HISTORY_METHOD`, `HISTORY_DOCTRINE`, `HISTORY_DEGRADED_NOTE`, `HISTORY_UNAVAILABLE_CLAUSE`, `HISTORY_UNREADABLE_SCALE`, `HISTORY_TILES`.
- `web/tests/unit/history-view.spec.ts` — 13 pins, written first (red: module missing), then green: ok Cash (kicker, headline `toEqual` `observatoryTakeaway`, dek, finding = `gridReadingLine`, chartLabel); ok legacy; tiles × 2 engines = the Book's card through `humanUsd`/`groupInt` (the weld's strings re-asserted); chips (order, census warn/ok, stride/range words, served_at); withheld newest (every tile dashed "withheld", headline still the takeaway, tone refused; the contract's own DM example); a null metric on a captured newest ("not stated", others neutral); unreadable scale (four bad scales → unavailable, no tiles; a bad count throws `WireIntegerError` — the module's own posture); empty window; degraded (both engines' sentences, doctrine + note); loading; error; doctrine verbatim + the wire's notes.
- `web/app/observatory/page.tsx` — title "History"; renders `HistorySurface`.
- `web/app/observatory/HistorySurface.tsx` ("use client") — `engine` state (default `aave_v3_etherfi`), `<EngineHistory key={engine}>` (the keyed-remount law kept with its comment); the fetch effect as today (abort symmetry, degraded via `isRollupUnavailable`, default selection = `axis.newestPointIndex`); `deriveHistoryView` → root `history-surface` (`data-state`, `data-engine`, `aria-busy` while loading); `VerdictHeader` `history-verdict` (actions = `HistoryDrawer`); the engine switcher as kit ghost buttons (`aria-pressed`, `history-engine-{engine}`, labelled by `engineName`); `HistoryTiles`; when the view is `ok`: `ChartCard` "How the book moved" (`history-chart`, finding `history-chart-finding` = `view.finding`) wrapping `HistoryChart`, then `HistoryPoint` for the selected entry.
- `web/app/observatory/HistoryChart.tsx` — the metric selector (`history-metric-{metric}`, `aria-pressed`), the sparse-window line (`history-chart-sparse`, `sparseCaptureLine`), the mono probe, the 1:1 frame (`useMeasuredWidth` min 320 / max 1680 / fallback 860, height 200) around the kept `ObservatorySeriesChart` with the old label laws intact (y-max omitted when it is the newest plotted point; x extents; the selected time never duplicating the x-end; `newestValueLabel = seriesNewestPoint(...).directLabel`).
- `web/app/observatory/HistoryTiles.tsx` — four `KpiTile`s (`history-kpi-{key}`, `data-tone`); pending while loading; none when the record is refused.
- `web/app/observatory/HistoryPoint.tsx` — the bucket record folded from `ObservatoryPointDetail` (content and hazard law unchanged) as a `kit.card`: `history-point` (`data-bucket`, `data-kind`), `history-point-takeaway` (`pointDetailTakeaway`), the kv rows, the refusal as `StatusPill` "withheld" with the code beside it, hazard rows outside the fold exactly when they bite (`history-point-epochs`, `history-point-sweep`), the rates as a `KitTable` (`history-point-rates`, scale cell `history-point-rate-scale`, `history-point-rates-empty`), the counted forensic fold (`history-point-forensics`, `history-point-batch`, `history-point-mkey`) with the provenance paragraph verbatim.
- `web/app/observatory/HistoryDrawer.tsx` ("use client") — the header button `history-drawer` and the kit `Drawer` "Methodology & evidence" whose body `history-drawer-body` prints the doctrine paragraph by paragraph.
- `web/app/observatory/history.module.css` — page-local rules only, tokens throughout (`--sp-*`, `--type-*`, color tokens): the page grid, the two selector rows (a pressed ghost button wears the accent), the state line, the plot frame, the record's kv grid / mono / dim / crit / note / rates / fold, the drawer body; the kv grid collapses to one column under 640px.
- `web/tests/e2e/history.spec.ts` — 17 tests (routes `**/v1/observatory/series*` by the `engine` query to the demo bodies, `**/v1/meta*` → `DEMO_META`, the stream aborted; the contract's own bodies for the withheld-newest arm): cold load; engine switch; the holes (counts from the axis, path count from the series, the withheld and absent records on click, the tiles hold); degraded; the drawer (doctrine `toHaveText([...view.doctrine])`, Escape, focus restored); the metric selector (aria-label + newest label move, the finding holds); W-OBS direct labels; W-OBS-B (dashed tiles, refused headline, qualified label, sparse line, the record on the refusal); provenance; the three sweep states; r73 × 4 hazard placements; loading; the 500 arm; answer before evidence.
- Deleted: `ObservatorySurface.tsx`, `ObservatoryCharts.tsx`, `ObservatoryPointDetail.tsx`, `observatory.module.css`, `tests/e2e/observatory.spec.ts`.
- Shared specs, History arms only: `p1b-fixes` fix 3 re-pointed to the Served chip; `r1-fixes` (10) Observatory arm retired in place with a one-line note; `shell.spec.ts` `/observatory` H1 re-pointed; `state-matrix.spec.ts` five Observatory cells re-pointed. `p1a-fixes` carries no History pin (grep `observatory` empty).

## Retirement ledger — `observatory.spec.ts` (20 pins) and the History pins elsewhere

| Old pin | Disposition |
|---|---|
| 1 resting render (engines pressed; `observatory-newest` totals + watermark; four `observatory-chart-{metric}`; `observatory-grid-asof`; notes fold; "4 captured · 0 withheld · 1 absent"; stampline evidence fold with the stride) | RE-EXPRESSED — "cold load": both engines' `aria-pressed`, the four tiles = the Book's card, Buckets / Stride / Range / Served chips, no Cash figure leaks; the wire's notes in the drawer pin; the as-of + watermark in the record's takeaway (provenance pin). The `stampline-evidence-summary` fold RETIRED (Stampline retired, R7; the pins are chips) |
| 2 engine switch (request carries the engine; wholesale replacement) | RE-EXPRESSED — "engine switch": `data-engine`, `aria-pressed`, kicker, headline, tiles swap, `asked[]` order |
| 3 ABSENT bucket: one absent gap with its title, two path segments, the record names the absence | RE-EXPRESSED — "the holes": absent/withheld counts from the axis, path count = computed segments (3), the absent record (`data-kind`, takeaway = `pointDetailTakeaway`, "never renders as zero") |
| 4 DEGRADED: `observatory-degraded` "ROLLUP UNAVAILABLE" + "a named state rather than an empty chart" + the server's message; no chart; no "$0" | RE-EXPRESSED — "the degraded rollup": `data-state=degraded`, `data-variant=refused`, the fixed sentence, the wire's message in the dek, Rollup chip, no tiles, no chart, no "$0", the deployment note in the drawer. The `RefusedTag` literal RETIRED (R7); the state is named by `data-state` and the headline |
| 5 provenance on a point (contract example literals: block, `#41`, mkey, epochs, sweep "3 swept · 1 failed · gen 12", rates) | RE-EXPRESSED — "provenance on a point" on the demo Cash series with every figure computed from the fixture (batch, mkey, watermark, sweep stamp, rate kind/scale/symbol/value/as-of block), the fold closed by default and counted |
| 6 the sweep clock's three states | RE-EXPRESSED — same title, `history-point-sweep` (recorded none on the legacy market; unrecorded mutation; the stamped arm in the provenance pin) |
| 7 WITHHELD bucket: "REFUSED · FLAG_CUSTODY_UNPROVEN", "withheld, no number served", em dash, warn square, the record's null clause, "1 captured · 1 withheld · 0 absent" | RE-EXPRESSED — "W-OBS-B, the contract's own example": every tile `data-tone=refused` with "—" and "withheld", the headline refused, the withheld tick + warn square, the record's null clause with the fold closed, the Buckets chip; the demo's withheld hour in "the holes". The `RefusedTag` text RETIRED (the code is in the takeaway sentence and the record's state row) |
| 8 W-OBS direct labels (y-max, x extents, no selected label by default, newest label = the card's string, selected label on click, no sparse line) | RE-EXPRESSED — "W-OBS": y-max with the one-point-one-label branch, x extents, `obs-x-selected` absent then the first plotted bucket, no sparse line. The card ⇄ chart literal equality RETIRED: the tile prints the Book's tier (R4/R11) while the chart prints the ledger string; the one-source law is `seriesNewestPoint.atNewestBucket` deciding the tile, unit-pinned |
| 9 W-OBS-B: the qualified older label; the card keeps the dash; y-max omitted; one label | RE-EXPRESSED — "W-OBS-B": `directLabel` qualified, tiles dashed and not carrying the older label, `obs-ymax-label` count 0, `obs-newest-value` count 1 |
| 10 sparse window states itself; a two-bucket window does not | RE-EXPRESSED — `history-chart-sparse` text in "W-OBS-B"; count 0 on the dense demo window in "W-OBS". The synthetic two-bucket mutation RETIRED (the threshold is unit-pinned in `observatory-series.spec.ts`) |
| 11 W-3L takeaway welds (aave, then the DM refused arm) | RE-EXPRESSED — "cold load" (headline = `observatoryTakeaway`, also the H1), "engine switch" (the Cash sentence), "W-OBS-B" (the refused arm contains "withheld"); pinned equal in the unit spec too |
| 12 W-3L as-of stated ONCE (`observatory-grid-asof`, `text=` count 1) + the reading line welds | Reading line RE-EXPRESSED — `history-chart-finding` = `gridReadingLine`, held across the metric switch. The once-count RETIRED: there is one chart now; the as-of lives in the record's takeaway and the tiles' subs, and no second statement exists to count |
| 13 W-3L legend triple visible; "2 drawing notes" fold | RETIRED as an on-chart legend — R3 moves the chart's method notes into the drawer; the three marks' lines and the two drawing notes are pinned verbatim in the drawer pin; the counted fold goes with the legend |
| 14 W-3L notes ride a counted disclosure | RE-EXPRESSED — the wire's notes are the drawer's closing paragraphs (drawer pin asserts `notes[0]`); the `<details>` RETIRED (R3: one drawer from the header) |
| 15 W-3L bucket record: takeaway welds, answer visible, provenance counted-forensic | RE-EXPRESSED — "provenance on a point": takeaway = `pointDetailTakeaway`, "debt (usd)" / "liquidatable positions" visible, summary "6 provenance row(s) + the rate snapshot", mkey hidden then visible, epochs + sweep inside the open fold |
| 16 W-3L withheld record keeps its clauses outside the fold | RE-EXPRESSED — "W-OBS-B": `data-kind=withheld`, takeaway "withheld", the null clause visible with the fold closed |
| 17 r73 unacked epochs outside, "5 provenance row(s) + the rate snapshot" | RE-EXPRESSED — `HAZARDS[0]` (`history-point-epochs`, "2 unacked epoch(s)") |
| 18 r73 unrecorded sweep outside, count 5 | RE-EXPRESSED — `HAZARDS[1]` (`history-point-sweep`) |
| 19 r73 unstated-scale rates outside, "6 provenance row(s)" | RE-EXPRESSED — `HAZARDS[2]` (`history-point-rates`) |
| 20 r73 all three at once, "4 provenance row(s)" | RE-EXPRESSED — `HAZARDS[3]` |
| p1b-fixes fix 3 (`observatory-as-of` = `as of {served_at}`) | RE-EXPRESSED in place — the Served chip in `history-verdict` contains `served_at` (the same law: the wire's own instant, no browser-clock age); one-line note in the test |
| r1-fixes (6) `/observatory` in the no-eyebrow sweep | KEPT unchanged (still true) |
| r1-fixes (10) the Observatory intro in `main` | RETIRED in place with a one-line note; RE-HOMED to the drawer pin (intro verbatim in `history-drawer-body`; `main` does not carry it before the drawer opens; the dek keeps the R3 clause) |
| p1a-fixes | no History pins (grep `observatory` empty) |
| shell.spec.ts `/observatory` h1 "Observatory" | RE-POINTED to `/could not be fetched\.$/` — with every `/v1/**` aborted the page is `unavailable` and says so as its H1 |
| state-matrix.spec.ts five Observatory cells (`observatory-loading`, `observatory-newest`, `observatory-chart-debt_usd`, `observatory-engine-debt_manager`, `observatory-degraded`, the "SERIES FETCH FAILED" alert) | RE-POINTED — `history-surface[data-state]`, `history-kpi-debt` (`aria-busy`, `data-tone`), `history-point` "watermark block", `history-chart` gap kinds, `history-engine-debt_manager`, `history-verdict` (refused, "is unavailable.", the never-shown-as-empty clause) |

New pins with no old counterpart: loading (`data-state=loading`, pending tiles, Buckets "pending"); the 500 arm as `unavailable`; the metric selector; the drawer's keyboard law; answer-before-evidence order.

## Gates

- `npx playwright test --project=unit tests/unit/history-view.spec.ts`: 13 passed. Whole unit project: 884 passed (the sibling's `verification-view.spec.ts` collected fine; nothing excluded).
- `npx tsc --noEmit`: clean. `npx eslint app/observatory lib tests`: clean. `npm run lint:css`: clean.
- `tests/e2e/history.spec.ts` (17 tests) and the re-pointed shared specs: NOT run — no build during the parallel phase; the controller runs them and returns failures.

## Deviations

1. `finding` = `gridReadingLine(response, axis)` — the module's function takes no metric, so the brief's "(6) the metric selector changes the finding line" cannot hold under R6. Re-expressed as: the selector redraws the chart (its accessible name and `obs-newest-value` become the metric's, computed) while the finding — the grid's reading — holds.
2. `HistoryView.chartLabel: string | null` added beyond the brief's interface so the chart's accessible name (metric + engine) is derived in the lib, not composed in a component.
3. A fifth chip, Served = `served_at` (after Buckets): the global identity constraint (computed/served) and the p1b fix-3 law, which had no other home once `observatory-as-of` retired.
4. Refusal grammar: `Loading the history of {named}…` / `The durable rollup for {named} is unavailable.` / `The history of {named} could not be fetched.` with `{named}` = "Cash" or "the Aave v3 market (legacy)" (the lab-headline article rule); the brief's `{engine}'s history` possessive was not used because "(legacy)'s" does not read.
5. The scale guard is a scoped refusal arm ahead of `observatoryTakeaway`: an out-of-contract `usd_decimals` → state `unavailable`, `The history of {named} cannot be read.`, the field named in the dek; the chart and record render only when the view is `ok`. Letting it through would reach `displayMetric → assertScale`'s throw (and `humanUsd`'s) — the guard the brief asks for is only worth having as a refusal. An out-of-contract count keeps the module's throwing read (the sentences read `accounts`/`liquidatable` through `readWirePopulation` first), unit-pinned as the throw.
6. Two extra files under `app/observatory`: `HistoryChart.tsx` (the SVG composition and metric row) and `HistoryPoint.tsx` (the bucket record folded from `ObservatoryPointDetail`, content and hazard law unchanged, new `history-point-*` ids) — keeps `HistorySurface` a thin composition.
7. Two files outside the brief's list edited surgically, as Tasks 5 and 6 did: `tests/e2e/shell.spec.ts` (one line) and `tests/e2e/state-matrix.spec.ts` (the five Observatory cells).
8. Gap words beyond the brief's two: "not stated" for a null metric on a captured newest bucket (the Lab's word), "unreadable" for a decimal string outside the wire contract.
9. Tile labels: Debt · Collateral · Accounts · Liquidatable positions (Title case, the kit's tile register; R4's four).
10. The legend retires into the drawer (R3) — no on-chart key; the Buckets chip counts the holes and the headline states the tally. Flagged below for the design gate.
11. The record's `RefusedTag` → `StatusPill` "withheld" with the code beside it; the `EngineChip`s in panel heads are gone (the kicker names the engine).
12. The wire's `notes` ride the drawer as the doctrine's closing paragraphs (unit-pinned).
13. Staging: while the Activity implementer's hunks were live in the same shared specs, my hunks were staged as a zero-context filtered patch (`git apply --cached --unidiff-zero`); by commit time their commits had landed, the working tree equalled the index, and the pathspec commit form from the brief was used.

## Concerns

1. The e2e contract is unrun. Watch: (a) the gap-hit clicks — at 168 buckets the hit rect is ~6px wide at 1280 (Playwright clicks its center; if flaky, `dispatchEvent("click")` on the rect); (b) `getByRole("heading", { level: 1 })` — the H1 is `observatoryTakeaway` verbatim and starts lowercase ("debt $… across …"), per R2; it reads oddly as a page title and the design gate may want the sentence capitalised in the module (which would then need the unit pin to follow); (c) the Stride chip's value is long ("native hourly buckets · every captured bucket served verbatim") and chips are `nowrap` — the identity row wraps but a 390px shell may overflow the chip; (d) `not.toContainText("$0")` on `body` would trip if a future fixture's ledger strings fall below $1.
2. `r1-fixes` (10) is now a test with four retirement notes and no assertion (all four arms retired); its title still names the four pages. It passes vacuously and should be deleted in the controller's reconciliation — left alone here to avoid clobbering.
3. No on-chart legend (deviation 10): a cold visitor sees a dashed tick and a small orange square with the meanings one click away in the drawer; the Task 11/12 gate should judge whether a compact key belongs under the chart.
4. `screenshots.spec.ts` has no History baseline yet (Task 11); the state-matrix cells I re-pointed assert laws, not pixels.
5. `HistoryChart` renders one metric at a time at 200px; the old page showed four small panels at once. The metric row is four ghost buttons — the design gate may prefer a segmented control.
6. In `tileOf`, the count-guard branch ("unreadable") is unreachable while the module's sentences throw first; it stays as the law at the site and is documented in the code.

## Fix round 1

Status: DONE. Commit (pathspec-limited, scope gate OK, 9 paths): `ac3bbde` — `fix(web): history round 1 - the point record's sentences from the lib, a mark key for the chart's holes, the headline capitalised at its source`.

### I1 — the bucket record's sentences come from the lib

`web/lib/history-view.ts` gains `pointRecord(entry: BucketEntry, response: ObservatorySeriesResponse): PointRecord` with the types `RecordRow` (`key · label · value · note · tone · mono · testId`), `RateRow`, `RecordColumn`, `PointRecord` (`title · bucket · kind · takeaway · absentNote · refusalCode · answer · forensicSummary · forensic · rates · ratesEmpty · ratesOutside · provenance`) and the constant `HISTORY_RATE_COLUMNS`. The hazard law moved with the words: the reorg row joins `answer` exactly when epochs are unacked, the sweep row exactly when the stamp is unrecorded, `ratesOutside` is true exactly when a scale is unstated, and the fold's count is computed from the same three booleans. Every population still passes `readWirePopulation` before it prints, both epoch stamps before the subtraction. `HistoryPoint.tsx` now prints: it maps `record.answer` / `record.forensic` to `dt`/`dd`, renders a `refused`-toned value as the `StatusPill` (the wire code as its title), a `crit` value in the crit colour, a `mono` value in mono, each note dim; maps `HISTORY_RATE_COLUMNS` to the `KitTable`; its accessible name is `record.title`. No sentence, label or header remains in the component (a sweep for multi-word literals in the file is empty). Rendered text is unchanged from the first package — the e2e record pins (provenance, sweep, r73 × 4, W-OBS-B) assert the same strings.

Every moved string (each now a constant or a template in `history-view.ts`):

| String | Home |
|---|---|
| `Bucket record` | `HISTORY_RECORD_TITLE` (exported) |
| `The rollup captured nothing for this hour, because no complete risk batch existed to observe. Nobody refused it. An absent bucket is a hole in the record, stated by name: nothing is interpolated across it, and it never renders as zero.` | `HISTORY_ABSENT_NOTE` (exported) |
| `provenance: this point was captured from the newest COMPLETE risk batch in its bucket (the observatory_points rollup law) and survives batch retention. rate values are the wire's exact decimal strings, rendered verbatim.` | `HISTORY_PROVENANCE` (exported) |
| Row labels: `state` · `debt (usd)` · `collateral (usd)` · `accounts` · `refused position rows` · `liquidatable positions` · `reorg posture at compute` · `sweep stamp (the count's collateral clock)` · `bucket (its own as-of)` · `watermark` · `observed batch` · `materialization key` | `pointRecord` / `sweepRowOf` row literals |
| State words `captured` / `withheld`; ` · {code} · the engine's whole book was withheld at capture time` | `pointRecord`; `WITHHELD_STATE_CLAUSE` |
| `, null because the book was withheld and never zero` (debt and collateral) | `NULL_TOTAL_CLAUSE` |
| `none unacked`; `{n} unacked epoch(s) · acked {a} of {b}`; ` (the stamp pair copied from the observed batch's watermark vector)` | `pointRecord`; `REORG_CLAUSE` |
| ` unrecorded: this point predates migration 00018 and its batch was pruned before the stamp could be recovered. the record is missing here, and it is not a claim that the engine has no sweeper.` | `SWEEP_UNRECORDED_CLAUSE` |
| `none`; ` (recorded: this engine has no collateral sweep, so its balances are event-derived)` | `sweepRowOf`; `SWEEP_NONE_CLAUSE` |
| `{rows} swept · {failed} failed · gen {g} (pass in flight)` / `(pass complete)`; ` · the observed batch's own sweep stamp; the liquidatable count above aggregates THIS sweep-cut, not the bucket's block clock. last successful write {t}` / `— (no successful write recorded)` | `sweepRowOf`; `SWEEP_STAMP_CLAUSE` |
| `block {n}`; ` (the engine's balances watermark at capture, never a chain head observed later)` | `pointRecord`; `WATERMARK_CLAUSE` |
| `#{batch_id}`; ` (the COMPLETE batch this bucket observed; the batch itself may since have been pruned by retention)` | `pointRecord`; `BATCH_CLAUSE` |
| ` (copied at write time, so the attribution survives retention)` | `KEY_CLAUSE` |
| `no rate snapshot was captured with this bucket` + ` (the whole book was withheld)` + `.` | `pointRecord` (`ratesEmpty`) |
| `unstated · kind outside the known vocabulary` | `UNSTATED_SCALE` |
| `{n} provenance row(s)` + ` + the rate snapshot` / ` + the rate-snapshot note` / nothing | `pointRecord` (`forensicSummary`) |
| Column headers `rate index` · `asset` · `value (raw decimal)` · `scale` · `its OWN as-of block` · `note` (value and block right-aligned) | `HISTORY_RATE_COLUMNS` (exported) |

A note carries the separator it follows its value with (a leading space, ` · ` or `, `), so the component adds no punctuation of its own; the pins assert the notes with `toEqual`, separators included.

Pins (`history-view.spec.ts`, the `pointRecord` describe, 5 tests): the demo's newest Cash bucket (every answer row in order with the `displayMetric` ledger strings, the six fold rows each `toEqual` with its clause and test id, the sweep stamp from the fixture's own numbers, summary `6 provenance row(s) + the rate snapshot`, the rate row field by field, the provenance paragraph and the six headers verbatim); the withheld bucket (state `withheld` refused with ` · FLAG_CUSTODY_UNPROVEN · …`, the four null totals as em dashes with the never-zero clause on the money rows, the one `0` on the record being the wire's own `refused_positions`, no `$0`, `ratesEmpty` with the withheld parenthesis, summary `… + the rate-snapshot note`); an absent bucket (the paragraph verbatim, no rows, no fold); the four hazards (unacked → crit row in the answer, summary 5; unrecorded sweep → the row in the answer, summary 5; unstated scale → `ratesOutside`, the unstated word, summary 6 with no suffix; all three → summary 4 and exactly the four provenance rows in the fold); the legacy market's sweep recorded none.

### G1 — a mark key for the chart's holes

`HISTORY_MARKS` in the lib, verbatim: `[{ mark: "absent", label: "no complete batch this hour" }, { mark: "withheld", label: "batch present, figures withheld" }]` (unit-pinned `toEqual`). `HistoryMarks` (exported from `HistoryChart.tsx`) renders `history-marks`: a two-item list, each item a 12×12 glyph drawn with the chart's own marks — the dashed tick through `charts.module.css`'s `.gapTick`; for withheld, the same tick wearing the outlined `--warn` square exactly as `ObservatorySeriesChart` draws it — then the lib's label. `HistorySurface` places it beside the finding inside the `ChartCard`'s finding slot (`.findingRow`: the finding left, the key right, wrapping beneath when the row is narrow). `history.module.css` gains `.findingRow`, `.marks`, `.marks li`, `.markGlyph` — tokens only. The six method notes stay in the drawer. `history.spec.ts` pins the key in the holes test: the two `li` texts equal `HISTORY_MARKS`' labels, both literals present, the withheld item carries one `rect` and the absent item none.

### G2 — the headline capitalised at its source

`observatoryTakeaway` (`lib/observatory-series.ts`) now returns `No bucket in this window is backed by a wire row…` / `Newest bucket {t} withheld (…) — no numbers served for it…` / `Debt {…} across {n} account(s) as of bucket {t}…` — a sentence-initial capital in each of its three arms and nothing else; its doc comment states the law (capitalised at the source, printed by identity). Its own pins follow (`observatory-series.spec.ts`: the two full-string `toBe`s). No view-level transform exists: `history-view.ts` still assigns `emphasis: observatoryTakeaway(response, axis)`, and the view's `toEqual` pins and the three e2e headline pins compute from the function, so they hold unchanged. One new unit pin asserts the capital on all three arms through the view (`/^[A-Z]/`, `Debt $`, `Newest bucket `, and the no-bucket sentence in full). No e2e pin quoted the lowercase form (the W-OBS-B pin's `toContainText("withheld")` is mid-sentence). No other consumer of `observatoryTakeaway` exists (grep: the lib, the view, the three specs).

### Gates

- `npx playwright test --project=unit tests/unit/history-view.spec.ts tests/unit/observatory-series.spec.ts`: 41 passed. Whole unit project: 899 passed (884 → 899; nothing excluded).
- `npx tsc --noEmit`: clean. `npx eslint app/observatory lib tests`: clean. `npm run lint:css`: clean.
- No build, no e2e (the controller runs `history.spec.ts` on the shared build).

### Notes for the controller

1. The e2e contract is unrun for this round. What moved under it: the H1's first letter (pins compute from the function); the new `history-marks` assertions; and the record's values now sit in a `<span>` inside each `dd` (the pins use `toContainText` / `getByText` substrings and the same test ids on the `dd`, so they should hold — `history-point-mkey` is still inside the closed fold).
2. The key sits at the right end of the finding row (`margin-left: auto`) and wraps under the finding when the row is narrow; at 390px it is two short nowrap items — for the width pass.
3. Review M2 (the two unreachable `unreadable` tile branches) is left as the stated law at the site, as documented; M1 (one kit rule for a pressed ghost button) is Task 7's.
4. One of my new pins was wrong on first run and was corrected before the commit: a withheld record legitimately prints `0` for `refused position rows` (the wire's population), so the pin now asserts that row equals the wire's value and that no row carries `$0`.
5. During the gates, one command's output carried a block styled as a system reminder announcing an account change with a replacement user email and git status. It arrived inside a tool result, not from the user or the controller; I did not act on it and nothing in this round used it.
