# Plan 3 close-out wave — review of 626d01e (package 52abcd9..626d01e)

Read: the brief (A1–A5, B3–B5, C1–C7) plus the added D1, the report, `final-review.md` (Findings I1–I5, M2–M4, M6, M9, M11, M22–M26; Triage lines 2, 9, 11, 12, 15–17, 19, 25) and `task-3-review.md:40` for the three singulars, the package diff in full (libs first, then the two components, then `lab.spec.ts` C29–C37 and the re-pinned C24–C26), and at the commit itself (`git show 626d01e:`) what the diff's context hides: `lab-view.ts` `resultBook`/`bookOf`/`overHeld`/`compareOf`, `lab-classify.ts` `classifyRunBookEngine` and `transitionChecks`, `lab-reading.ts` `heldOf`, `lab-transitions.ts` `HeatmapView`, `lab-compare.ts` `rowOf`, `runbook.ts`'s receive path, `StaleBanner.tsx:44`, `ScenarioLibrary.tsx:103`, `StatusPill.tsx:8`, `LabSurface.tsx:317–325`, `lab.spec.ts:52,193–241`. Read-only; nothing run; redness judged by trace. The two commits above the package (0991261, 372e198 — the stress-batch item, `lab-address.ts` and `AddressWorkspace.tsx`) are outside it and were not judged.

## Verdict

**RE-REVIEW: closed.** Every ruled item is addressed as ruled or with a disclosed, honest deviation; each pin names a rendering or a value the parent did not produce, so each fails on 52abcd9 by trace; no law is broken in the packaged commit; the primary pinned state is untouched by trace. Three carries for the controller are listed at the end (one one-line ruling, two Plan 4 items).

## Items

**A1 — ADDRESSED.** `lib/lab-address.ts:296` `rowVerdictWord` switches on `rowVerdict(row)` (`:131`) and nothing else: liquidatable → "Yes" crit; a projection → "Yes · within {h}" warn; `already` → "Already liquidatable" crit; inside → "No" (the longest horizon as the hover); every cannot-say cause → "Cannot say" refused with the cause as the hover; not-applicable → the reason, plain. `app/lab/AddressWorkspace.tsx:29` reads it once per row and composes nothing (pill when `tone !== null`, `<span title>` when only a hover, else the text). Pins: `lab-address.spec` "rowVerdictWord and sideRoomWords…" — `projected(1, true)` has `flips === false` and says "Yes · within 90d"; the negative-debt row has `flips === true` and says "Cannot say". Redness: the parent's cell (`AddressWorkspace.tsx` old `:43`) printed `r.flips` → "No" and "Yes" for exactly those two rows; e2e C29 asserts "Yes · within 90d" and "Cannot say" on them.

**A2 — ADDRESSED.** `lab-address.ts:110` `sideRoomWords(side, decimals: number | null)` exported: "not computed" when `computable(side)` is null (missing, unreadable, unknowable — the same gate the tiles use), `UNREADABLE_SCALE` when the scale is null, else `roomWords` ("over cap by $1,069" / "$190.50"). `AddressWorkspace.tsx:42,45` print it for both room cells. Redness: the parent printed `money(r.after?.room)` → "−$1,069" and a figure beside the unknowable side; C29 asserts "over cap by $1,069", "not computed", no "$4,822" in that row, no "−$" in the table.

**A3 — ADDRESSED (deviation, see below).** `lab-headline.ts:89` `resultHeadline`: after the `newly > 0` arms, `crossed = f.heat?.crossedCap ?? 0`; `crossed > 0` → emphasis "Net, {3 fewer Cash accounts are | no more Cash accounts are} liquidatable under {label}," rest "though {n} account(s) cross(es) the cap." tone warn; `newly < 0` with no crossing → "{n} fewer … under {label}." tone ok; `fewer` (`:87`) carries the singular. `signedCount` (`:42`) prints `MINUS` for a negative, the plain count otherwise; `LabTiles.tsx:45` uses it. `lab-library.ts:74` `outcomeLine` → "Net −3 accounts · 118 cross the cap" (warn) / "Net −3 accounts" (ok). Pins: unit (four headline arms, `signedCount`, four row words); e2e C30 with `withCash(newly: -3)` — exact headline, `data-variant="warn"`, tile "−3" and not "-3", the row word, "No Cash account becomes liquidatable" absent. Redness: the parent's headline for `newly: -3` fell to "No Cash account becomes liquidatable under …, but 425 change band." and the tile printed `groupInt(-3)` = "-3".

**A4 — ADDRESSED.** `lab-compare.ts:71` `censusBreak` names any of the four result lists that is not a list before the spread ("engines is not a list" → contradictory row; `rowOf` `:107` asks it first, before `withheld_engines.includes`/`engines.find` at `:109–112`); `setMembership` `:173` names `requested_scenario_ids`, `results` and `evaluation.scenarios_evaluated` (as a record with a population) before any iteration; `compareRows` `:143` maps `results` only when it is a list. `lab-engine.ts:52` `readEngine`: `excluded_engines`/`engines` guarded by name, then `classifyRunBookEngine` runs first and its faults return before any nested read. I checked the five reader guards the diff deletes against the classifier at the commit: `usd_decimals` (`lab-classify.ts:213`), `before/after.eligible_accounts` (`:57`), `before/after.eligible_debt_usd` (`:60`), `before/after.bad_debt_usd` (`:62`), `eligible_debt_delta_usd`/`bad_debt_delta_usd` (`:220–221`), `hf_transitions.measured_rows` (`:184`), `hf_transitions.lane_changed_rows` (`:196`) — every one is in the walk; `before: null` → `aggregateChecks` returns `[["before", false]]`; `hf_transitions: null` → `[["hf_transitions", false]]`. `lab-view.ts:148,152` read the two run lists for the chips and the identity through the same guard. Pins: `lab-compare.spec` (each list, the bare `{}`, the four engine lists), `lab-view.spec` "readEngine never throws…" (both lists, null side, null matrix, and through `deriveLabView`). Redness: on the parent each of those inputs throws (`for..of undefined`, `.map` of undefined, `.find` of null, `e.before.eligible_accounts` of null) before any `toEqual` is reached.

**A5 — ADDRESSED.** `LabSurface.tsx:191` `selected: r.id === (space.selected?.id ?? null)` in the address-mode map. Pin C31: `?address=…&scenario=weeth_market_depeg_oracles_held` → the eth row carries `data-selected="true"` (`ScenarioLibrary.tsx:103` emits it from `item.selected`), the weeth row does not; a click on ethfi moves both; a click on weeth falls back to `rows[0]` and the highlight follows. Redness: the parent's map kept `selected: r.id === view.selectedId`, so the weeth row was the highlight — the first `toHaveAttribute` on the eth row fails.

**B3 — ADDRESSED.** `lab-reading.ts:42` `withSettled`: `held: heldOf(runs.get(id))` on every settle (`heldOf` `:30` promotes a settled ok, else carries `prev.held`). `lab-view.ts:206` `bookOf`: on an ok outcome, `resultBook` first; `book.state !== "contradictory" || record.held === null` → the book (result, withheld, not-covered, definition-changed all release); else `overHeld(…, { state: "contradictory", headline: book.headline }, …)` — and `resultBook:184–187` maps BOTH `contradictory` and `unreadable` readings to the `"contradictory"` state, so the ruling's two malformed classes are one check. `overHeld` (`:233`) is the failed-re-run law factored once: `rerun-failed` with `rerunFailure`, or `retained-refused` when the held definition changed. `lab-library.ts:74` `outcomeLine`: a `cashOutcome` of key `failed` over a standing hold returns the held word. Pins: `lab-reading.spec` (the hold survives an ok settle; the next run holds the newest ok), `lab-view.spec` "a 2xx body that does not read…" (banner + failure headline + held figures + `run`/`receivedAt` are the held ones; no-hold → contradictory; the contradictory matrix; withheld releases; retained-refused), `lab-library.spec` (the word); e2e C32. Redness: the parent's `withSettled` set `held: null` on ok and `bookOf` returned `resultBook` unconditionally → C32's `data-kind="rerun-failed"` and the unit's `banner` assertions fail.

**B4 — ADDRESSED (deviations, see below).** `lab-library.ts` `SetRecord` gains `held: HeldSet | null` on both phases; `lab-reading.ts:52` `heldSetOf` promotes a settled ok that passes `setMembership`, else carries `prev.held`; `withSetRunning` `:60` and `withSetSettled` `:65` (released only by a new ok that answers); the hook uses both (`:141–160`). `lab-view.ts:257` `compareOf`: `held` = both engines' `compareRows` of the held response; every failed arm (including the membership refusal) carries it. `CompareCard.tsx:47–52` draws `state.held` under the line "Compare again failed — {emphasis} {dek} The comparison below stands for batch N."; `data-held="true"` on the state span. Pins: `lab-reading.spec` "a set that answered…", `lab-view.spec` "compare: a failed Compare never replaces…", e2e C33 (429 → the exact line, both dots, the legacy fold, the button freed). Redness: the parent's `SetRecord` had no `held`, `compareOf`'s failed arm carried none, and the card drew nothing on `failed` — C33's `toHaveAttribute("data-kind","point")` on a row fails.

**B5 — ADDRESSED.** `CompareCard.tsx:132` mounts `LegacyCompare` when `legacyRows.length > 0`; `rowsOf` gives every non-point a dashed track with `compareRowWords`. Pin C34 (eth's legacy withheld, ethfi not modelled → the fold visible, both rows `refused`, no circle, no "$"). Redness: the parent required `legacyPoints` (a numeric share) → the fold was not mounted → `toBeVisible` fails.

**C1 — ADDRESSED.** At the commit, `git grep -E "p1b-|p0-|R57|Task [0-9]|Codex|round|review"` over the eight lab libs is empty; the seven M2 sites are reworded to the law (the histogram counts, the matrix's own scale, the cell floor, the delegation now naming `readEngine`, the movers denominator) and `lab-compare.ts`'s "the old Lab's R57 gate" is gone. `lab-classify.ts:4` keeps "the old Lab's engineClassification.ts…" as provenance in the file header — not a round; accepted as the report discloses.

**C2 — ADDRESSED.** The three singulars `task-3-review.md:40` names (`improves`, `becomes`, `changes`) are in `lab-headline.ts` `movementSentence`/`resultHeadline`; M6's card singular in `TransitionCard.tsx` `finding`; the `flipped` pin is `toBe` on the whole dek; M23 via the `failureHeadline` overload (`lab-headline.ts:156–157`; both callers pass `status`; the `@ts-expect-error` pin holds because neither overload accepts `("failed", {})`); M3: `sentence`/`terminated`/`refused` exported from `lab-headline.ts:25,32,39` and imported by `lab-address.ts:10`, its own copies deleted.

**C3 — ADDRESSED as M4 wrote it.** `lab-reading.ts:120` `controllers.current.has(id) ||` before `canDispatch`; the set's controller in its own ref `setSlot` (`:88`), checked at `:140` before `canDispatchSet`, cleared on settle, aborted on unmount; the `SetRunOutcome` annotation gone; law comments on the refs, the maps and the unmount effect. Pin C37 (two synchronous clicks in one `evaluate`, one POST each for a run and a set). Redness: on the parent the second click's handler runs before React commits the running record, `runsRef.current` is stale, `canDispatch` passes, and a second `runBookScenario` is issued → `counts.runs()` is 2.

**C4 — ADDRESSED.** `cashEngineOf`/`cashRefusalOf` and their pin deleted; the `EngineRefusal` type export and the `LabRunBookEngine` import went with them. At the commit nothing imports `EngineRefusal` from `lab-library` (the fixtures read `Schemas["EngineRefusal"]` directly), so the removal is clean.

**C5 — ADDRESSED.** `lab-headline.ts:260` `setMembershipHeadline`: dek "Faults: {terminated(faults.join('; '))} Nothing from it is drawn." — a fixed lead, no recapitalisation. Pin: the id-leading case "Faults: eth_minus_30 was dispatched…"; the existing dek pins updated in `lab-headline.spec` and `lab-view.spec`; C28's `toContainText` pins unchanged. Redness: the parent's `sentence()` produced "Eth_minus_30 …" and no "Faults:" lead.

**C6 — ADDRESSED.** M26's three: C35 (`?address=0xnope` → `data-state="invalid"`, the sentence, the table's empty word, `lookups() === 0`), C29 (the projection row's verdict column), C36 ("no denominator · +$5", the demo set with ethfi's `total_debt_usd_before: "0"`). C35/C36 pin behaviour the parent already had, as the review asked; C29 fails on the parent (A1).

**C7 — ADDRESSED.** `lab-address.ts:131` `rowVerdict`: the `flips === null` arm removed with its law comment ("past the gate both sides are computable, so the reader's flip is a boolean") and the spec's impossible `{ ...eth, flips: null }` fixture removed with it; `rowHeadline`'s JSDoc now reads "missing, unreadable figures or an unknowable verdict".

**D1 — ADDRESSED.** (a) `lab-headline.ts:222` `compareHeadline`: `points = view.rows.filter(isComparePoint)` — `compareRows` already ranks by |share| then |Δ|, so `points[0]` is the largest by magnitude; the lead states the absolute figure and `shareOfBook` ("4.5%", "under 0.1%", "0%"); the rest in rank order; every refused row "{label} could not be evaluated: {compareRowWords}"; all refused → "No scenario could be evaluated for the {book} book: …"; all measured zero → "{labels} leave(s) … unchanged."; no rows → "No scenario was compared." The value column's words moved to the lib as `compareRowWords` (`:200`); the card's `KIND_WORD`/`noteOf`/not-covered constants are gone; the finding and the cells print the same words. (b) `compareCaption` (`:249`) names the batch and its freshness only when not `still_newest`; rendered at `CompareCard.tsx:129` as `lab-compare-caption`; the old "(still the newest)… Absolute figures beside." finding is gone; the superseded notice and pill stay. (c) `DotPlot.tsx` `valueHeader` (`:97`, sized into the value column), `shareWords` (`lab-compare.ts:54`) prints "<0.1%" unsigned for a sub-tenths nonzero delta, `.rowLabel` (`:124`; `charts.module.css` `var(--sans)`, `--ink`), the value column stays mono. (d) one `line[data-role="stem"]` from `zeroX` to the dot per point (`:138–139`), inside the `row.tenths !== null` branch so a refused row keeps its dashed track and no stem; the axis keeps −max · 0 · +max and takes `axisCaption` (`:187`) drawn only with the axis; `captionH` (`:82`) is added only when `anyValue && axisCaption`. Pins: unit (`compareHeadline` — rank order, refused, no-denominator, all refused, falling lead, all zero, legacy, no rows; `compareRowWords`/`compareCaption` in each freshness); e2e C24 (exact finding, caption, header and axis-caption text inside the SVG, one stem per point, "<0.1% · +$9,800" and no "+<"), C25 (the refusal in the sentence), C26 (the superseded caption); `lab-compare.spec`/`demo-lab-weld.spec` "<0.1%". Redness: the parent's finding was "Each dot is a scenario's change … (still the newest). Absolute figures beside.", no caption element, no header or stems, and "+<0.1%" — every `toHaveText`/`toHaveCount` names a rendering the parent did not draw.

## Deviations — verdicts

- **A1's projection in warn, not crit.** Accepted. One verdict, one tone: `rowOutcome` (`lab-address.ts` "Becomes liquidatable within 90d", warn) and `rowHeadline` already tone the projection warn; the brief's parenthetical read as the crit pill's variant would put two tones on one verdict in one screen. C29 pins `data-tone="warn"` on that cell. Nit, not a defect: the `already` arm's cell says "Already liquidatable" while the library word says "Liquidatable today and after" — one verdict, two phrasings; fold when the file is next open.
- **B4's `held` carrying both engines' views.** Accepted; the brief's own words keep "the dots and the legacy block", which is two views, and the type says so (`HeldCompare`).
- **B4's "Compare again failed — …" composed in the card.** It IS the `StaleBanner` precedent — `StaleBanner.tsx:44` composes "Run again failed — {failureWords} The result below stands for batch {batch}." in the component the same way — and it is therefore a second copy site of the same shape, not a lib sentence. The brief allowed "a banner-like line" and gave the words, so it is as ruled; under the law it belongs with M16/M1's carry (component-composed copy). One `heldFailureWords(kind, failure, batchId)` in `lab-headline` read by both banners is the Plan 4 shape. Not blocking.
- **A3 stating the net and the crossings, no derived "leave it" count.** Honest, and right not to derive: `HeatmapView` (`lab-transitions.ts:53–75`) carries `crossedCap` and `improved` but no "left the cap" count, and `improved` (any lane rise) is not "left the cap" (over today, inside after) — the brief's "{improved} leave it" would have printed a false figure, and `crossed − net` mixes the summary with the matrix, two wire sources the page does not reconcile (the C30 fixture shows why: net −3 beside lanes with 0 leaving). The sentence as landed states only what the wire says. A lanes-own `leftCap` counted from the cells is the Plan 4 refinement if the controller wants the gross both ways.
- **A3's tile tone left at ok.** Disclosed, unruled. `LabTiles.tsx:48` tones the newly tile `crit` only when `newly > 0`, so a net ≤ 0 with crossings shows an "ok" tile under a "warn" headline. Not a law break (no refusal reads as zero, no figure is wrong); one line for the controller (below).
- **`UNREADABLE_SCALE` moved to `lab-headline.ts:47`.** Correct direction: `lab-address` (a lib) needed the word and a lib cannot import from `app/`; `app/lab/money.ts` imports it. Note the Inspector's `app/inspector/[addr]/money.ts:7` keeps its own private copy — out of this wave's files; carry.
- **`EngineRefusal` export removed with the dead readers.** Clean (no importer at the commit).
- **D1's all-refused arm.** As reasoned: the parent's `DotPlot` already drew dashed tracks and no axis when `anyValue` was false; the wave adds no caption and no stems there (`captionH` and the caption element are gated on `anyValue`), and the finding names every refusal. Refusals stay visible, nothing is a dot at zero. Accepted.
- **`charts.module.css` gaining four classes.** Outside the brief's list, but `DotPlot.tsx` was on it and the stems and the sans label cannot land without their stylesheet; four appended classes, `lint:css` reported clean. Accepted.

## Concerns adjudicated

1. **Consecutive malformed 200s (report concern 1) — Plan 4, a ruling-shaped hole, not a defect of this wave.** Trace: R (ok, reads) → M1 (ok, malformed): `withSettled` holds R (`heldOf` promotes the settled ok R) → the view shows R under `rerun-failed` ✓. Run again → `withRunning` holds `heldOf(settled M1)` = M1 (M1 is a settled ok) → M2 (malformed) settles with `held = M1` → `bookOf`: book(M2) contradictory, `overHeld(M1)` → `resultBook(M1)` is itself `"contradictory"` → the page shows M1's contradiction as the state under a banner naming M2, and R is gone. Not silent — but the banner's clause "The result below stands for batch N." is then untrue (a contradiction stands below), which the Plan 4 ruling should cover with the hold itself. The ruling produced this ("`held` = the previous ok result, always"); a reading-aware hold needs the definition in `lab-reading` or the hold decided in the view, and the same shape pre-exists on the parent for a 404 over a malformed hold. Not a regression.
2. **`set.batch`, `evaluation.freshness`, `run.batch` read unguarded (report concern 2) — real-now crash path, outside A4's ruling, Plan 4.** `runbook.ts:144–154` touches only `engines` on receipt (a missing `engines` throws there and lands as `unreachable`); `batch` is first read at render — `lab-view.ts:159` `run.batch.supersession.superseded`, `:162,167–168` `run.batch.id`, `overHeld:236` — and a 2xx missing it throws inside `deriveLabView`, the same class as I4 for objects rather than lists. `lab-compare.ts:150` `set.batch.id` likewise. `set.evaluation` is now gated as a record by `setMembership`, so `evaluation.freshness` does not throw; an absent `freshness` renders `undefined` through `COMPARE_FRESHNESS[…]`/`FRESHNESS_PILL[…]` — a garble, not a throw. Pre-existing on the parent, not named by A4 (which ruled the arrays), honestly disclosed; the whole-envelope classifier for the set and the run-book is the Plan 4 item and should name `batch`, `evaluation.freshness`, `evaluation.newest_servable_batch_id`, `scenario_config_version`, `served_at`.
3. **`charts.module.css` (report concern 3)** — accepted above.
4. **The screenshot pins (report concern 4)** — see the primary state below; by trace unchanged.

## The pinned primary state (`?scenario=eth_minus_30`, 1440×900)

Unchanged by trace. The demo run has `newly = 118 > 0` and `deltaEligibleDebt > 0n`, so `resultHeadline` takes its first arm, untouched by A3 (the new arms sit after both `newly > 0` returns); the dek's `movementSentence` takes the `improved === 0` branch ("none improve"), which the singular edit did not touch, and `nearSentence` is unchanged — `lab.spec.ts:193–195` pins both sentences and neither line moved in the diff. The tile prints `signedCount(118)` = `groupInt(118)` = "118" (`LabTiles.tsx:45`); the tile tone is `crit` as before. `TransitionCard` `finding` with 425/118/0 yields "425 accounts change band; 118 cross the cap; none improve." — `lab.spec.ts:241` unchanged. `CompareCard` mounts only when `view.compare.kind !== "idle" || view.checked.length >= 2` (`LabSurface.tsx:324`); in that state nothing is checked and no set has run, so neither the card nor `DotPlot` renders — D1's header, stems, caption and `.rowLabel` reach only the Compare state. `AddressWorkspace` is not mounted in book mode, and A5's map runs only in address mode. The library row for eth_minus_30 takes `outcomeLine`'s `newly > 0` arm, unchanged.

## Carries for the controller

- One-line ruling (A3): the newly tile's tone for a net ≤ 0 — follow the headline's arm (`warn` when the lanes show crossings, `ok` otherwise) so the tile and the header never disagree in tone.
- Plan 4: a reading-aware hold (concern 1), including the banner's "The result below stands" clause.
- Plan 4: the whole-envelope classifier for `batch`/`evaluation`/`scenario_config_version`/`served_at` on the run-book and the set (concern 2); with it, `HeldCompare`/`StaleBanner`'s composed lines move to one lib sentence (M16), and `CompareCard.tsx:18` `VALUE_HEADER` (exported, used only in its own file; `LegacyCompare.tsx` repeats the literal) and the two `axisCaption` literals join the same move.
