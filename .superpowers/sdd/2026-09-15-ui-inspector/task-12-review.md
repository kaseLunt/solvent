# Task 12 review — d1032e4..a8d30a1

### Spec Compliance
- ✅ Spec compliant (the four controller rulings folded in as accepted). File-by-file against the brief's list:
  - `web/tests/e2e/inspector.spec.ts` — reconstructed the new file from the diff (review diff lines 1750–2535) and `diff`ed it against the brief's Step 1 block: the only deltas are the brief's own `// web/tests/e2e/inspector.spec.ts` first line and the ruled 503 pin plus its one-line comment. 15 tests, titles and bodies verbatim.
  - `shell.spec.ts` — `/inspector` row `h1: "Is this address at risk?"` (diff 3925).
  - `state-matrix.spec.ts` — all 7 `surface: "inspector"` cells re-expressed exactly as the brief's table says (diff 3991–4211), the `error:429` cell with the ruled `"Rate limited (429)"`; `mockInspector` gains `**/v1/evidence*` → `EVIDENCE_MANIFEST` (diff 3978) and `**/v1/address/*/stress*` → `stress-dm.json` via the existing `labFixture` reader, moved above the helper (diff 3966–3990); `loading` also stalls evidence/stress, `error:429` serves evidence and answers stress 429 (diff 4143–4153).
  - `runbook-bsplit.spec.ts` — click and URL assertion kept; the three DOM assertions replaced by the three the brief names (diff 3890–3901); `mockInspectorFor` gains `/evidence` and `/stress` (re-identified to the mover) (diff 3860–3875).
  - `p0-fixes.spec.ts` — 7 retired (p0-3 ×2, p0-4 ×1, p0-8 boundary ×3, p0-9 f3 ×1; diff 2582–2820, 2833–2855), `mockInspectorFound`/`mockInspectorBothHistories`/`DM_HISTORY_ENGINE` and the `../fixtures/inspector` import removed, header updated to name the ledger (diff 2555–2563).
  - `p1b-fixes.spec.ts` — 4 retired (p1b-4 ×2, p1b-6 fix 4 ×2; diff 2896–2960, 3040–3068), helpers and types removed, import narrowed to `EVENTS` (still used by fix 2), section comments updated.
  - `r1-fixes.spec.ts` — 8 retired (diff 3140–3343, 3372–3410), `mockInspector` and the inspector fixture import removed, header updated.
  - `r3-fixes.spec.ts` (3 tests), `r4-fixes.spec.ts` (1), `r6-fixes.spec.ts` (1) — each file's only tests; deletion ruled accepted.
  - Deleted: `AddressEntry.tsx`, `[addr]/InspectorPositionCard.tsx`, `[addr]/InspectorHistory.tsx`, `[addr]/InspectorActivity.tsx`, `lib/inspector-lines.ts`, `tests/unit/inspector-lines.spec.ts` (stat block, diff 8–24).
  - Ledger: `.superpowers/sdd/progress-ui-overhaul.md` gains `## 2026-09-15 · Plan 2 (Inspector) — retirements` with the brief's 11 lines verbatim plus the deletion parentheticals (diff 40–55).
- ⚠️ Cannot verify from diff: the whole-suite count (1429 / 10 / 0) was, by the report's own admission, not re-run after `d1032e4` landed; the contract's own 15/15 (run 3) was on HEAD. Not re-run here per instructions.

### Strengths
- The contract is the brief's spec verbatim, and its `mockInspector` answers every route the page actually issues: `lib/address-lookup.ts` fetches lookup, history (:128–129), stress (:130), params gated on `hasCash` (:131), evidence (:135–142), activity (:171–174); the shell's `useMetaConstants` (`lib/meta.tsx:184`) fetches `/v1/meta`; the stream is aborted. All eight are routed (diff 1806–1815).
- The invalid-path test is hermetic without the helper because the hook gates every address-keyed fetch on `valid` (`lib/address-lookup.ts:70,129–131`), params on a ready Cash lookup, and evidence is "Not fetched on the invalid-address page" (:135); the test's `**/v1/address/**` counter proves zero lookups (diff 2234–2243).
- R14 homes verified in the named unit specs: `tests/unit/inspector-position.spec.ts:129–162` pins absent (`liquidation_price: null`), breached-with-`never_liquidatable`, no-price-path with the wire reason in `title`, `[null]` entry → unreadable, deleted `price_decimals` → unreadable naming `prices[0].price_decimals`, null `lowest_healthy_price` → absent, `prices: null` → absent (covers p0-8 a/b, p0-9 f3, p1b-4 ×2, r1 (2) ×2); `:101` pins the uncertified-boundary arm (p0-8 c). `tests/unit/inspector-view.spec.ts:111,186` pins the Snapshot chip as the lookup's own age (r1 (3)). `tests/unit/params-format.spec.ts:34–39` pins DM 100e18 percents (r1 (12)); `:92–99,108` pins 10500 → 5%, 10600 → 6%, and the DM non-par premium (r3 ×3). `tests/unit/inspector-headline.spec.ts:96–101` pins the capitalised 503 dek; `:59,69` the no-position and cannot-compute headlines.
- `activityTakeaway`: the `r74` describe block in the retired `inspector-lines.spec.ts` and the one at `tests/unit/activity-rows.spec.ts:72–95` are byte-identical (`diff` exit 0).
- No stale imports of the deleted modules: grep over `web/app web/lib web/components web/tests` hits only the ruled comment at `lib/factorPriceGuard.ts:14` and a provenance comment at `tests/unit/activity-rows.spec.ts:66`.
- Every re-expressed test queries only ids from the contract vocabulary (`inspector-surface`, `-verdict`, `-verdict-headline`, `-verdict-dek`, `-address-input`, `-legacy`, `-history`, `-history-legacy`); every retirement leaves a header comment pointing at the ledger rather than a silent hole.
- Deviation 2 (staging by exact path) kept a concurrent agent's uncommitted page edits out of this commit; `git show --stat` lists exactly the 17 paths.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
1. **(plan-mandated) `web/tests/e2e/inspector.spec.ts` "resume: a failed background repair never replaces the rendered position" (diff 2489–2501) — the retired r4 law has no home that can fail.** r4 pinned that one resume produces exactly one own-envelope re-fetch (`expect(addressCalls).toBe(2)`, deleted at diff 3567–3573) and r6 pinned attempt → fail → bounded retry lands (diff 3760–3792). The contract test dispatches `pageshow`, sleeps `waitForTimeout(500)`, and asserts the state is still `near` — it never observes that a repair request fired, so it passes identically against a page that ignores `pageshow` (and, given the coalescing/blind-resume predicates in `tests/unit/freshness-resume.spec.ts:211–224`, a zero-gap `pageshow` may in fact never reconcile, making the pin vacuous in practice). No unit spec pins the hook either: grep `keepOnFailure|useAddressLookup|address-lookup` over `tests/unit` hits only a comment (`freshness-blind-resume.spec.ts:23`) and a type import (`inspector-view.spec.ts:4`); `tests/unit/address-lookup.spec.ts` does not exist. The law lives only in `lib/address-lookup.ts:82–100,156`. The brief's block is verbatim, so this is a plan defect, not the implementer's — but R14 says re-expressed, not dropped. Fix: count requests in the aborting route (`let addressRequests = 0; … addressRequests += 1; return route.abort();`), replace the sleep with `await expect.poll(() => addressRequests).toBe(1)` (proves the repair was attempted and failed), keep the two state pins. If the poll never reaches 1, the page's resume wiring is a Task 11 finding.
2. **(already ruled; listed so the fix round carries it)** r1 "(10) the adjudicated intros render, and the endpoint lines are demoted" (diff 3372–3410) also pinned the Lab, Observatory, Feed, Proof and Developers intro paragraphs; re-home as one test in `r1-fixes.spec.ts` without the `/inspector` arm and without the "fed by" placement check. Same round: the stale `InspectorPositionCard` comment at `web/lib/factorPriceGuard.ts:14`.

#### Minor (Nice to Have)
1. `web/tests/e2e/state-matrix.spec.ts` `mockInspector` (diff 3977–3990) and `runbook-bsplit.spec.ts` `mockInspectorFor` (diff 3860–3875) still route no `**/v1/meta*`, while the shell fetches it on every page (`lib/meta.tsx:184`); each Inspector cell and the deep-link test leaves one request to whatever listens on :8080 (falls back to tier constants when unreachable). Pre-existing, and the brief enumerated only stress + evidence — but the contract's helper shows the complete set. Add the route in the fix round.
2. Ledger line for `r1-fixes.spec.ts` (diff 47) says the "never-touched engine" law now lives in "inspector-position / inspector-view unit specs and the contract"; neither pins it. Its actual home is `tests/unit/history-copy.spec.ts:88–94` (`engineNeverPresent`), still consumed by the new page at `app/inspector/[addr]/HistoryCard.tsx:33`. Correct the ledger line (or add a one-line `inspector-history-legacy` count-0 pin on a never-present legacy engine).
3. The retired `tests/unit/inspector-lines.spec.ts` also carried `lookupTakeaway` (4 tests) and `positionTakeaway`/`positionMethodLine` (5 tests) pins (diff 4234–4360); the ledger line (diff 53) names only `activityTakeaway`. The lookup laws are re-expressed in `inspector-headline.spec.ts:59,69`; the position-takeaway laws died with the position card. One clause on the ledger line would close the gap.
4. `inspector.spec.ts` "a stale Cash price input…" pins `toHaveClass(/chipWarn/)` (diff 2117) — a CSS-module class, not a contract id or `data-*` attribute. Brief-verbatim; prefer a `data-tone` on the chip if the kit exposes one.
5. `inspector.spec.ts` "liquidatable and healthy" (diff 1859–1872) re-routes only the address and leaves `DEMO_HISTORY_NEAR`/`DEMO_STRESS_NEAR` (the near account's bodies) under the liquidatable and healthy addresses. The page tolerates the mismatch today (15/15), but the History and Stress cards on those arms are unasserted and fed another account's data. Brief-verbatim; note only.

### Assessment
**Task quality:** Needs fixes
**Reasoning:** The diff is the brief executed verbatim — every listed file has its hunk, every retirement is ledgered, and the R14 homes named in the brief exist and pin the laws they are credited with — but the resume contract test cannot fail against a page that never repairs, so r4's law is dropped rather than re-expressed; that one test change plus the two already-ruled re-homings make a single small fix round.

## Re-review 1 (6d403ad)

**RE-REVIEW: closed.** Scope: Important 1–2, Minor 1–3 only; read-only, no e2e run (the implementer's 78/78 on the four files is the run of record).

- **Important 1 — closed.** `web/tests/e2e/inspector.spec.ts:272–286` (6d403ad): the re-registered address route counts then aborts (`addressRequests += 1; return route.abort()`), the sleep is gone, `await expect.poll(() => addressRequests).toBe(1)` sits before the two kept state pins (`data-state` still `near`, debt tile still `$4,822`). Can now fail against a page that never repairs: `expect.poll` (5 s default) fails at 0. Trigger: `lib/freshness.ts:475` returns `"definitive"` for `pageshow` with `persisted === true` before any clock is consulted, and `ResumeTracker.lastReconcileProven` starts `false` (`:454`), so the echo guard at `:538` cannot swallow the first signal — the implementer's 560 ms pass is consistent with the code. Race against the bounded retry: `lib/freshness.ts:572–584` — the first attempt is immediate and not in `RESUME_RETRY_DELAYS_MS = [5_000, 15_000]`; retries follow a failed attempt at 5 s and 15 s. `expect.poll` samples at 100/250/500/1000 ms, so it observes 1 within ~100 ms of the abort, ≥4.9 s before the count can become 2; `toBe(1)` can only be overtaken if the immediate attempt fires ≥4.9 s late. Not a realistic race; no change needed. The counter is isolated to the bare lookup (`*` never crosses `/`; `repair = loadLookup({ keepOnFailure: true })` at `lib/address-lookup.ts:156` re-fetches only the lookup), so a history/stress refetch could not inflate it.
- **Important 2 — closed.** `web/tests/e2e/r1-fixes.spec.ts:212–254` (6d403ad): the new "(10) the adjudicated intros render — Lab, Observatory, Feed, Proof, Developers…" test. Every string literal from the `/lab` arm onward is identical to the retired test's (`git show d1032e4:web/tests/e2e/r1-fixes.spec.ts`, `diff` exit 0); the literals dropped are exactly the Inspector arm (`"/inspector"`, its paragraph, `"lab-address-input"`, `"form"`, `"fed by"`). The new body contains neither `inspector` nor `fed by`. Header comment (`:19–27`) lists (10) as surviving with its Inspector arm retired. `web/lib/factorPriceGuard.ts:14` now names `boundaryOf` (inspector-position.ts) and evidence.ts — matches the real consumers (`lib/inspector-position.ts:6,226`; `lib/evidence.ts:19,320`).
- **Minor 1 — closed as scoped.** `web/tests/e2e/state-matrix.spec.ts:104–106` `mockInspector` and `web/tests/e2e/runbook-bsplit.spec.ts:366–368` `mockInspectorFor` route `**/v1/meta*` → `DEMO_META` (imports added at `:28` and `:27`). Residual, outside this round's scoping and not reopening: the two state-matrix Inspector cells that route by hand rather than through `mockInspector` — `loading` (stalls six routes) and `error:429` (serves six) — still leave `/v1/meta` to :8080. One line each if the next round wants every Inspector cell hermetic.
- **Minor 2 — closed.** Ledger r1 line (`.superpowers/sdd/progress-ui-overhaul.md:4391`) names `tests/unit/history-copy.spec.ts` (`engineNeverPresent`, consumed by `app/inspector/[addr]/HistoryCard.tsx`) as the never-touched-engine home and names the re-homed (10) test.
- **Minor 3 — closed.** Ledger `inspector-lines.spec.ts` line (`:4398`) gains "lookupTakeaway re-expressed in tests/unit/inspector-headline.spec.ts; positionTakeaway / positionMethodLine retired with the position card."
- Minor 4 (class pin, no `data-tone` on the chip) accepted per the coordinator; Minor 5 deferred. Neither examined.
