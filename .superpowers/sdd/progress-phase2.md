# Solvent Phase 2 — SDD Progress Ledger (W1)

Plan: docs/plans/2026-07-22-solvent-phase2-positions-prices.md (staged: Tasks 0-3 coded, 4-10 authored after Task 0 gate)
Repo: C:\Users\kasel\source\repos\etherfi\Solvent, branch main, plan committed dccd84b.
Governance: W1 active with integrator claim; scope gate enforces W1 allowed_paths (amended dccd84b+1 to add recon/derivation-notes.md + recon/feeds.json).
Protocol: Phase 1 carry-over — per-task implementer+reviewer, review-package diffs, consolidated final fix wave. CODEX = STANDING SENIOR REVIEWER per D-005 (7cc91b3): adversarial pass on every correctness-critical component (Phase 2: Tasks 2-store-rework re-check optional, 5/6/7 mandatory + any schema/consistency change), whole-branch at phase close, fix-wave verification, all findings adjudicated explicitly, session IDs recorded here. Sandbox: all go/network commands need dangerouslyDisableSandbox:true + PATH export; db tests need TEST_DATABASE_URL export.
SHA anchors (new chain): v0.1.0-foundation tag; fix-wave HEAD 3ef9117; control plane a3b8a00; ratification 2ce285c; plan dccd84b.

- Task 0 (recon): COMPLETE (d779971 + claim rescope db4b926). GATE FIRED (contradictions non-empty) → USER APPROVED corrected architecture ("no shortcuts", 2026-07-22). Key findings: plan's debt identity unimplementable (UserInterestAdded never emitted; unit asymmetry) → normalized-index replay VALIDATED BIT-EXACT on 3 borrowers + liq vector; 7,337 migration-seeded positions event-invisible → genesis via calldata decode of 80 MigrationBorrowerPositionsSet txs; OP collateral + supplier shares not event-derivable → snapshot-based collateral, supplier descoped to record-only; Aave debt Pool-exact, collateral needs 4 aToken streams; oracles: OP poll-only PriceProviderV2 (engine-exact), ETH 4 raw aggregators w/ cap-adapter caveats; local db volume was recreated → Task 9 backfills from scratch. recon/derivation-notes.md is NORMATIVE for Tasks 4-10.
- Tasks 3b-10 AUTHORED into plan (0a4844b): 3b schema amendments (seq-in-PK + source column + rate_indexes + zero-row consistency + snapshot upsert) → 4 decode (all streams + migration calldata) → 5 DM deriver (golden = recon validation table) → 6 Aave deriver ∥ 5 → 7 runner+snapshotter+daemon deferrals → 8 prices → 9 backfill+reconcile (R-001 user gate on RPC throughput) → 10 anvil+phase gate. Codex-on-schema verdict (session 019f8cfd-b315-7443-a23f-1430e1ebe660): NO-SHIP, 2 critical + 3 high. ADJUDICATED into Task 3b (plan amended, committed): ACCEPTED — seq-in-PK (converged w/ standard); DURABLE REORG EPOCHS (the new critical: crash window between raw Rewind and RewindDerived leaves no marker → reorg_epochs written atomically in Rewind's tx, ApplyDerived refuses unacked epochs, chain-wide invalidation); zero-row HAVING drop (converged); derive-cursor chain binding w/ distinct mismatch error; expanded test list (NUMERIC extremes, intra-batch, crash-window two-engines-one-chain). REFUSED (recorded in plan): engine-in-PK (unreachable topology, both reviewers' analysis); concurrent-replay tests (single-writer contract D-004). Codex also cleared: NUMERIC ::text round-trip (with PG source citations), replay no-double-add, tx atomicity.
- Task 3b: commit 8abfd02, 32/32 live-db (22+10), full suite green, refusals as comments. Implementer concern: BalancesFor event/snapshot merge undefined if same (asset,side) has both sources — architecturally non-overlapping today (DM debt=event, DM collateral=snapshot, Aave=event) but unenforced; routed to reviewers + Task 7 reader-rule requirement. Standard closing review: Needs fixes — items 1-3,5-8 all verified correct (incl. chain-guard being a genuine bug fix over 3a's clause; NUMERIC extremes exact; refusals comment-only, no creep). TWO NEW IMPORTANTS, both live-reproduced: (a) new-engine implicit-ack hole — no-cursor path bypasses epoch gate, stale batch admitted (fix: unconditional gate; bootstrap RewindDerived ack when maxEpoch>0); (b) BalancesFor source-overlap order-dependence (snapshot won over event 3/3 by storage accident; the overlap fixture exists in tests but never reads through BalancesFor). ADJUDICATION: fix both — BalancesFor = ERROR on same-(asset,side) dual-source conflict (out-of-architecture, loud) + regression test through BalancesFor; genesis-ownership note: authored Task 5 uses calldata-decoded EVENTS for DM genesis (not UpsertSnapshotBalances) — restate in Task 5 dispatch to close the reviewer's ambiguity. Codex re-verify (session 019f8d29-9f97-7f81-a0e6-71781f2ade5b): still no-ship. F3 zero-rows RESOLVED; scope clean. NEW critical F2b: epoch ack not bound to deepest unacked rewound_to (stacked epochs bless stale mid-depth events — rewound_to written, never read); F4b: RewindDerived bypasses chain binding + rebuild unscoped by chain + no sentinel errors; BalancesFor collision (converged with standard). ADJUDICATED → consolidated fix wave dispatched (fable): deepest-target binding w/ caller-target lowering, chain-bound RewindDerived, sentinel errors (errors.Is), bootstrap-ack requirement for new engines, BalancesFor ErrBalanceSourceConflict, 6 new tests. DEFERRED WITH REASONS: seq allocator + fan-out bound + migration-replay determinism → Task 5 (producer-side by design; bound = len(seeds)>65536 error in DecodeMigrationCalldata); epoch-row pruning → Task 7 runner. D-005 fix-wave verification plan: standard re-review + documented adjudication (3rd Codex pass folds into phase-close whole-branch review — two rounds already on this surface).
- Task 3b: COMPLETE (commits 8abfd02 + fix 1ea4bad, pushed). Final review: Approved — all 4 fixes verified with the reviewer re-reproducing its own repros as closed; deepest-epoch test proven to distinguish old-vs-new behavior; no drift outside scope. Deferred (recorded): stale migration-header comment → next migration-touching wave; epoch pruning → Task 7; seq allocator + fan-out bound + migration replay determinism → Task 5.
- Task 4 (decode): commit 4d2f4ba, 40/40 decode + 12/12 config + full suite; 4 aTokens discovered+verified; ~50/54 real fixtures byte-checked vs receipts. RECON CORRECTION: migration payload = abi.encode(address[],uint256[]) (not (address,uint256)[]), 2 delivery selectors (commitAndExecute + execute302), validated vs recon's borrower (30,578,521) + golden liq vector. Routed forward: 80-tx selector sweep = Task 5 step 0 (mandatory); DMBorrowTokenAdded/Removed omission + ATokenBalanceTransfer scope judgment → reviewer. D-005 adjudication REVERSED BY OWNER QUESTION (correctly): golden-vector coverage is partial (untraversed events unprotected), the migration-layout recon correction is unvalidated by any independent eye, and fixtures are same-author-as-decoder (byte-checks pin data, not interpretation). Codex adversarial pass on decode DISPATCHED (attack list: field-offset fidelity on 5 subtle layouts, migration-layout independent verification from fixture bytes, skip-vs-error, fixture circularity, config streams, roster omissions). Standard review verdict: APPROVED — migration correction INDEPENDENTLY CONFIRMED (reviewer's own from-scratch Python ABI parser, byte-for-byte: 50 seeds, 0xac5f3ce9→30,578,521); DMLiquidated/AaveReserveDataUpdated/ATokenMint offsets manually word-verified (incl. cross-fixture liquidityIndex check); skip-vs-error clean; config exact; both scope judgments ratified (ATokenBalanceTransfer = required by Task 6 plan text; DMBorrowTokenAdded/Removed omission safe — silent-skip + recon says index reconstructible without them). 2 trivial Importants held for consolidation with Codex verdict: (a) one-line comment documenting why (engine,topic0) keying is safe (singleton-address streams); (b) report says 40/40, actual 45/45. Minor: imprecise truncation-test comment.
- Codex decode verdict (session 019f8d84-3de0-7cd3-8bd4-d68169259368): no-ship. HIGH: aToken events are OVERLAPPING VIEWS not independent deltas (fixture-proven: tx 0x7714…09d logs 236/237 = one transfer, 233/234 = one burn; Mint.Value includes BalanceIncrease; Value==BalanceIncrease = pure interest) — PLAN DEFECT in Task 6 semantics caught pre-implementation; plan amended+committed (record-only Transfer, BalanceTransfer-once, Mint/Burn formula derived from deployed source, tx-grouped pairing tests mandatory). MEDIUM: migration parser lenient (trailing garbage, dirty padding, post-allocation bound) → strict fail-closed parser + repack-bytecompare + adversarial fixtures. Task 5 plan gains Count==len(seeds) check + 80-tx sweep as step 0. VINDICATES the owner's D-005 question — golden vectors would NOT have caught the aToken fold defect until Task 6 was built wrong. Consolidated decode fix wave dispatched (sonnet, 4 fixes incl. both standard-review Importants).
- GOVERNANCE: D-006 accepted (51a8731, supersedes D-005 — status flipped via the gate-enforced supersession path): CODEX APPROVAL GATE — complex work untrusted until Codex approves; fix waves return to Codex; dependents don't EXECUTE against unapproved complex components (authoring ok; frozen-interface deps ok). RETROACTIVE QUEUE: derive fix 1ea4bad (Codex never re-saw F2/F4 fixes) + the pending decode fix wave → ONE Codex re-verification pass over both fix commits when decode fix lands. Task 5/6 EXECUTION gated on that approval; Task 5/6 authoring/dispatch prep may proceed (briefs extracted).
- Decode fix wave: b4bf737 (51/51, 6 adversarial parser tests, fixtures byte-identical), pushed. D-006 approval pass RUNNING (Codex, both waves: 1ea4bad derive + b4bf737 decode) in worktree wt-d006-b4bf737.
- Serena: .serena/project.yml created (go; ignored recon clones/prose; ts noted for P5); queue CLEARED — claim rescoped gen-3 (741b19c, isolated commit — claim mutations must not share a commit with product paths), .gitignore committed (30b4aa7), pushed.
- D-006 APPROVAL PASS verdicts (session 019f8da7-829e-7173-8f31-dfbd70c81132): Wave A derive 1ea4bad = APPROVED (stacked-epoch traced sound; bootstrap safe; sentinels errors.Is-able; NOTE for Task 7 dispatch: runner must resume from DeriveCursor, never assume its supplied rewind target). Wave B decode = needs-attention round 2: [medium] OUTER calldata layer still permissive (inner money parser fully PASSED its attack list) — fix: outer repack-bytecompare canonicality both selectors; [low] my Burn doc line was FALSE (Burn.Value = action − BalanceIncrease; equality ⇒ action = 2×increase — the pure-interest reading is Mint-only). Round-2 fix 0e670da (55/55, outer repack backstop). Codex round 3: outer canonicality CLOSED (repack determinism affirmed for these shapes); Burn doc CLOSED; NEW [low] — Mint comment not universal (_burnScaled-origin Mint: Value = BalanceIncrease − action; transfer interest-only Mints). Fixed by controller with Codex's own wording (47000ab + gofmt d8c462b; controller note: gofmt -l doesn't exit nonzero — a flagged file slipped past one chained commit, corrected immediately). Codex confirmation: **APPROVED** (session 019f8dc3-8ac9-7d40-8ec8-34afa49cac2c, "approve", no material findings). DECODE LAYER CODEX-APPROVED at d8c462b. D-006 gate satisfied for both dependency layers (derive @1ea4bad, decode @d8c462b).
- Tasks 5+6 EXECUTING in parallel (both fable): controller pre-committed frozen derive.Engine interface (engine.go) to eliminate the shared-file race. Task 5 = DM normalized deriver (step-0: 80-tx migration sweep w/ Σ=7,337 assertion; golden = recon's 3 bit-exact borrowers + liq vector; may add ONE additive chain.TxCalldata method). Task 6 = Aave deriver (mandatory deployed-source fold derivation w/ citations; tx-grouped pairing tests from 0x7714…09d; golden = 2 recon borrowers vs cast views; aave_-prefixed testdata; scoped -run Aave if DM tests mid-flight). Both: pathspec commits, disjoint files, -p2 reports. After landing: standard reviews + Codex adversarial per D-006 (money code).
- Task 6 LANDED (7549e8a, 18/18 -run Aave, pushed): MAJOR DISCOVERY — deployment upgraded 3×, TWO fold regimes (switch block 23088584): Regime A direct half-up folds; Regime B v3.5 TokenMath w/ INVERSION vs tracked scaled state (s′=ceil(N·RAY/i), claimed unique i≥RAY). Golden bit-exact both borrowers CROSS-REGIME at pin 25593800. DISCLOSED GAP: Pool impl 0xbe82113a (blocks 24196552-24920566) unverified anywhere — "sandwich" pinning. NOTE FOR TASK 7: runner must merge all 5 aave-engine streams in (block,logIndex) order from genesis. Standard review: APPROVED w/ independent re-proof (inversion uniqueness re-derived incl. sub-RAY counterexample showing i≥RAY guard load-bearing; 0x7714 burn re-derived from raw bytes bit-exact; unverified-window traversal CONFIRMED — deficit event inside it; deficit zero-out deviation from brief judged source-justified). 1 Important: boundary-block 23088584 exactness proven for only 2/5 streams — fix = getLogs emptiness check per remaining aToken OR tx-aware switch (block==23088584 && logIndex>=542). Minors: warm-start detector (s==0 && BalanceIncrease>0 impossible-from-genesis → loud error); shared payload map pointer in BalanceTransfer fan-out (copy per event); citation sub-range drift (cosmetic); decode's ATokenBurn doc still regime-A-only (predates discovery). HOLDING for Codex Task 6 verdict → one consolidated wave.
- Task 5 LANDED (298c43e, pushed): step-0 sweep PASS (80/80 txs, selectors 0xdcfdeb60×11 + 0xcfc32570×69, Σ=7,337, Count agreement); goldens BIT-EXACT all 4 vectors via full pipeline (raw bytes→decode→Process); RECON ERRATUM claimed (liq removed 15,289,230 → 15,289,260, digit typo proven vs recon's own beforeDebt/view figures — BOTH reviewers tasked to independently verify the arithmetic; if wrong, goldens are fitted to buggy code); 21 DM tests + chain.TxCalldata additive (+1 test); full suite green. Disclosed: ctx.Background() in migration fetch; IIU→rate_indexes not position events; rpcfixtures build-tag generator. Standard review: APPROVED — reviewer re-implemented the fold IN POWERSHELL from committed bytes (net 963,813 / atPIN 1,004,681 bit-exact, zero Go); ERRATUM CONFIRMED AIRTIGHT (recon's …230 internally inconsistent with its own beforeDebt/view; …260 proven); residue rule verified against DebtManagerCore.sol control flow (single-pass exemption contract-faithful); warm-start debit-errors-loudly w/ documented under-report-until-first-debit limit; chain change additive-only confirmed. 1 Important: dm_golden_liq fixture PROVENANCE STRING still carries the recon typo (artifact contradicts its own proof — one-line fix). Minors: per-tx vs per-call residue keying (documented tradeoff, practically unreachable divergence); "non-stable" misnomer for USDT (brief-verbatim); ctx.Background (disclosed); IIU design choice ratified. Codex T6 verdict (session 019f8dfd-a9dd-7e50-950b-48b188048733): NO-SHIP, ALL FIVE ACCEPTED — [critical] restart/rewind state lifecycle: empty-constructor + no hydration = silent corruption via successful-but-wrong inversions (concrete i=6RAY/5 drift-amplification example) → fix: hydration source in constructor (BalancesFor-shaped), rewind-reset contract w/ runner, restart-equals-continuous-replay tests + the drift example as a test; [high] stale-index acceptance → same-tx RDU requirement (contract-faithful per recon) + negative tests; [medium] unverified window NOT bit-pinned (window repays are rounding-INSENSITIVE — golden traversal proves less than claimed) → archive scaledBalanceOf before/after assertions for window users; [medium] boundary block per-emitter (converges w/ standard) → tx/log-order-aware cutoff + fixture-bound regression test; [low] deficit order-independence claim FALSE (liq-first then deficit fails) → enforce deficit-first, reverse-order loud error, remove claim. Codex T5 verdict (session 019f8e04-f2b2-7fb3-81c7-2d81bb11b71e): needs-attention, 3 high, ERRATUM RE-CONFIRMED. [high] in-memory state advances pre-persistence (no commit/rollback boundary — retries reuse uncommitted state, rewinds orphan it) — CONVERGES with Aave critical; [high] map-absence-means-zero warm start (hydration unenforced); [high] valid USDT/frxUSD borrow freezes cursor (my dispatch's "error on non-USDC" was too blunt — they ARE stable-snapped per recon; need decimal-conversion for configured stables + TYPED terminal error for genuinely unsupported). LIFECYCLE WAVE LANDED (7eafe37 + 9d164c4 + 6f1c27c, pushed): 77 test runs green, GOLDENS UNTOUCHED to the wei, window pinning ARCHIVED (all 8 drpc archive points asserted — upgraded from sandwich argument to bit-pin), 266/266 fixture actions same-tx-RDU compliant (deficits exempt-as-deployed), ErrMissingSameTxIndex + ErrUnsupportedBorrowToken exported, erratum corrected in all three places. Wave concerns for approval pass: Reset-scenario subset-equality harness; paired LiquidationCall now stricter; archive constants depend on drpc free depth. D-006 approval pass DISPATCHED (Codex, both prior sessions' findings, attack list incl. whether archive pairs actually discriminate the rounding rules or only the insensitive totals; deficit RDU exemption soundness; discard-path double-mutation). Approval round 2 (session 019f8e3a-f94c-70c2-96a1-82b1182c5f05): 5/8 prior RESOLVED; 3 new high + 1 medium, ALL ADJUDICATED → round-2 fixer dispatched (fable): [high] indeterminate-commit desync (PG commits, ApplyDerived errors, DiscardBatch preserves stale memory vs advanced cursor) → CONTRACT fix: any ApplyDerived error ⇒ Reset+rehydrate (always safe — hydration reads committed truth), DiscardBatch only for known-pre-persistence failures, plan Task 7 note + desync-contrast test; [high] frxUSD exactness guard rejects contract-valid events (MY round-1 spec bug — contract floors) → floor conversion, rejection test becomes successful-fold; [high] out-of-band stable snap → adjudicated to documented-invariant posture (payload price_source marker + golden/reconciliation empirical enforcement + Task 9 standing guard; per-event archive pricing of 305k events infeasible free-tier); [medium] window archive points don't discriminate floor-vs-half-up (Codex proved both rules agree on observed repays) → claim narrowed to outcome-pinning per Codex's own alternative. Ops note: orphaned broker process pid 76216 holds a scratchpad dir (harmless, outside repo).
Round 2 LANDED (7e7ab18, pushed): 79 runs green, goldens bit-exact (+price_source payload only), Reset-on-error contract w/ live-store desync-contrast test, frxUSD floor cited to DebtManagerCore.sol:378, snap invariant documented-with-mitigation, window claim narrowed. Approval round 3 (session 019f8e5c-de3c-7602-b6f8-c5d03473b7dc): items 1+2 RESOLVED (goldens verified — 122 golden borrows all-USDC, arithmetic path unchanged), ZERO new code findings; blocked SOLELY on two documentation overclaims: snap comment promised exhaustive detection + nonexistent standing reconciliation (reality: 3 goldens + ≥25 sample, no recurring check until P3/riskd); one surviving "empirical proof" phrase. Controller applied honest-language fixes directly (2ebcc96, tests green, pushed): sampled-evidence-with-detection-limits wording; outcome-pins/does-NOT-discriminate wording. Honesty-closure rounds (all comment-only, controller-applied): 2ebcc96 (sampled-evidence language) → round blocked on tense → ecf20d9 (attained-vs-planned) → round blocked on verbatim drift + engine.go runner claim → e11e126 (verbatim restore + prospective tense) → round found 7 more present-tense runner sites → 3b864ac CLASS FIX (package TERMINOLOGY note: "the runner" = Task 7's planned runner, all references prospective; + all cited sites reworded; residual grep clean; 79 tests green). Closing sweep verdict: **APPROVED** (session 019f8e8a-ead9-7001-9760-57d01e32e45e, "approve", no material findings). DERIVER LAYER CLOSED under D-006 at 3b864ac. Full derivation stack Codex-approved: decode d8c462b, derive 3b864ac. Lesson recorded: fix the CLASS on first occurrence, not the citations.
- Task 7 LANDED (44f2765/5add4c2/1703b9d/f481ca2, pushed): 224 tests green (35 new), all binding rules pinned by call-sequence tests. DISCLOSED DEVIATIONS under adjudication-by-review: 2 store WRITES beyond reads-only instruction (SaveSnapshot — plan-required; DeleteRateIndexesAbove — prevents reorg-changed-index SaveRateIndex divergence-wedge, but its OWN crash-window vs RewindDerived's tx is the sharpest review question, incl. rate-obs "last-wins" claim vs divergence-abort semantics contradiction); chain.Failover +Call; best-effort post-commit SaveSnapshot; BorrowApySet not persisted. Standard review: NEEDS FIXES — 1 Important CONFIRMED+SHARPENED (I1: DeleteRateIndexesAbove outside RewindDerived's tx AND non-re-triggerable — post-ack HasUnackedReorg=false makes rewind() unreachable, so a crash in the 2-statement gap permanently wedges SaveRateIndex AND drops the post-rewind re-sweep; fix = fold rate deletion into RewindDerived's tx + re-fire onRewind). All 7 named risks otherwise PASS (Reset-on-error both branches traced sound — determinism makes re-derivation idempotent, reorgs delete-before-re-derive; epoch pruning chain-correlated; snapshotter failure taxonomy precise). Deviations adjudicated: SaveSnapshot justified (brief mandated the write); DeleteRateIndexesAbove justified-in-principle/flawed-in-placement; Failover.Call justified; BorrowApySet scope-justified but rationale wrong (M3). Minors: M1 snapshot-error advance handling; M2 log wording; M4 forward-flag for Task 8 poller reorg-awareness (eth epochs pruned once aave acks — poller needs cursor or acceptance). 224/224 independently reproduced. Codex runner verdict (session 019f8eb4-ee2e-7622-8c7f-d81526338e65): DO NOT SHIP — critical converged w/ standard I1 (rate cleanup after ack, non-retriggerable); 4 high: partial-sweep-vs-zero indistinguishable; snapshot rows labeled with blocks their collateral doesn't represent (as-of lie); unhealthy engines skip mandatory reorg repair + no health surface; migration fan-out 14.6k serial non-resumable calls; 2 medium: rates-ahead-of-rejected-window; round-counted backoff elapses in ms under hot loop. ALL ADJUDICATED → consolidated fix wave dispatched (fable, 8 fixes incl. migration 00003 snapshot_sweeps, ApplyDerivedWithRates one-tx, side-scoped snapshot documents [runner=debt@throughBlock, snapshotter=collateral@multicall-block, composition=P3 read-time], bulk bounded fan-out, time-based backoff w/ jitter, mandatory-repair-before-unhealthy-gate + Runner.Health). Codex cleared: Reset/cursor-reread no dup-or-skip; sibling independence; epoch-prune min-ack correctness. RUNNER FIX WAVE LANDED (8996975/7a2cbc3/619f6ed/52d50ab, pushed): 241/241 (was 224), all 6 injections in, goldens untouched. Design: rate deletion inside RewindDerived tx; startup sweep = durable crash-recovery leg; ApplyDerivedWithRates one-tx (divergent rate → Reset, stricter); side-scoped snapshots; snapshot_sweeps 00003 three-way status; time-based jittered backoff. Disclosed: aave collateral has no history rows under debt-side-only rule (P3 read-time extension); in-memory carry-over best-effort. Approval verdict (session 019f8eef-1bd0-7da0-9964-161df69b2c74): NOT APPROVED — resolved: rates-one-tx, health surface, backoff, rate-hygiene atomicity confirmed store-side; NOT resolved: startup-sweep durability (memory-only progress, repeated-crash prefix reprocessing), mixed-side SaveSnapshot survives + NO collateral history documents at all, carry-over unbounded/unfair (sort-truncate starvation), balance-vs-status tx divergence, all-reverted head-of-line wedge; NEW [high]: reorg-orphaned phantom collateral (RewindDerived preserves snapshot rows, registry rebuilt from surviving debt events → account w/ reorged-away debt keeps stale collateral forever); 2 injections miss boundaries. SWEEP-DURABILITY REDESIGN LANDED (e2f885a/1dd8267/d17099a, pushed): 248/248 (was 241), snapshotter holds ZERO in-memory progress, mid-tx injection genuinely executes-then-aborts, migration 00003 in-place w/ goose cycle. Disclosed: post-completed-sweep restart waits cadence (durable bump supersedes startup backstop — approval pass told to trace the crash-window specifically); attempts replay-overcount narrows budget (conservative); RecordSnapshotSweep retired. Approval verdict (session 019f8f1b-c396-72a2-997a-bd5080ced0d0): GENERATION MODEL HELD (durability/atomicity/classification/FIFO/disclosures all RESOLVED) but 4 NEW highs, 2 of them CONTROLLER design bugs: (a) side in payload not KEY → debt/collateral history overwrite at same block (00004: side column in PK); (b) edit-in-place 00003 unsafe — it was PUSHED at 52d50ab, baselines skip edits → restore 00003 + forward 00004 + upgrade-path test (PROCESS RULE AMENDED: edit-in-place only for never-pushed migrations); (c) stale-failover collateral regression (lagging endpoint's latest rolls balances backward) → monotonic last_success_block guard FOR UPDATE; (d) rewind leaves snapshots-table history (above target + orphans) → history invalidation joins the rewind tx. Round-2 LANDED (5b63614 + 17e8ce7, pushed): 276/276, both commits individually green, upgrade-path PROVEN from exact pushed v3 baseline (UpToContext scratch-schema rebuild, zero loss, cold-start lag semantics). Disclosed: dev dbs in edited-00003 state need one cycle (00004 header); down-migration lossy (documented); stale-guard successes-only (deliberate, pinned). Approval round (session 019f8f41-c3eb-77c3-9b80-a6d2bcb52359): 3/4 RESOLVED (migration path faithful, side-keyed history valid, rewind invalidation transactional); 1 NEW high — responsive-frozen endpoint defeats the stale guard end-to-end (Failover rotates only on RPC ERROR; semantic staleness invisible → identical rejected batch forever, generation never completes). Rotation fix LANDED (5b28730, pushed): RotateActive/EndpointCount escape hatch, rotation-on-stale + full-cycle DEGRADED, both Codex regressions in; 255/255 (delta vs 276 = test replacement — closing pass told to verify not-lost-coverage); stray err.log removed. Closing pass (session 019f8f54-8fed-7773-acc2-e330db82470f): stale-rotation RESOLVED on the serialized path; test-count delta SETTLED (accounting difference, nothing lost — 279 comparable PASS events at HEAD); 2 blockers: [high] RotateActive not linearized vs in-flight do() sticky writes (in-flight completion can pin active BACK to rejected endpoint under documented-concurrent use) → revision-counter CAS design + RotateAwayFrom(token) + blocking-fake interleaving tests; [medium] stale-streak survives generation-transition progress (false DEGRADED) → reset on every progress record + lastSeenGeneration. Wave 5 LANDED (ab40f9c, pushed): revision-counter conditional sticky write, RotateAwayFrom(token) replaces RotateActive, CallWithToken additive, streak resets on all progress transitions; 259 top-level / 283 PASS events (+4, 1 renamed). MID-WAVE GOVERNANCE EVENT: W1 claim lease expired 14:36Z; agent recovered via sanctioned release+suspend (fa11de7) → reopen gen-4 (c7e5b77), roadmap-only, doctor green, owner override used — FLAGGED TO OWNER FOR RATIFICATION in chat. Gate round (session 019f8f85-a820-7192-91cc-578a79e1f6b9): both NOT RESOLVED w/ concrete counter-schedules — [high] unrelated walker error-rotation legitimately re-pins the rejected endpoint (shared hint can't carry caller-specific exclusion) → wave 6: caller-scoped PERSISTENT preference (CallFrom(startIndex) leaves shared hint untouched; preferredStart until progress; shared-hint RotateAwayFrom likely removed as superseded); [medium] commit-indeterminate sweep batch leaks stale streak across durable progress → remembered-batch reconciliation vs sweep-row generations at next Step. -race added to gates (Codex request). Wave 6 LANDED (cc96526, pushed): RotateAwayFrom + revision counter DELETED (attacked surface removed); CallFrom hint-free walk; snapshotter-owned persistent preferredStart; Codex's exact schedule = passing regression; commit-indeterminate reconciliation via SweepGenerations probe; 288 PASS/264 top (+8/−3); -race clean via golang:1.24 container (host lacks cgo — packages fake-based, equivalent). Gate (session 019f8fb5-d6d9-7ef0-89c2-8d75cc54bc9c): Fix 1 [high] RESOLVED (deletion design held — no semantic shared-state writer remains; unconditional sticky write safe again). Fix 2 still open via protocol-deep schedule: negative probe races LATE COMMIT VISIBILITY (pgx COMMIT-error while server resolving; MVCC serves old stamp) → one-shot clear discards evidence that later lands. Wave 7 LANDED (3dda135, pushed): probe machinery deleted (zero dangling refs, grep-confirmed), conservative-ambiguity reset, 262 PASS, -race clean (container). Closing gate (session 019f8fda-34f9-7b82-9db2-89c6b317bb4a): wave-7 probe deletion RESOLVED; 2 new mediums — recordProgress conflated telemetry w/ ROUTING (preferredStart release on ambiguous error re-enables the A-bounce; my wave-7 spec error) + one-cycle-late claim false under recurring stale/error interleave (counter resets indefinitely). Wave 8 LANDED (9107c63, pushed): telemetry/routing split, preference retained across ambiguous errors, suppression bound honestly documented repo-wide, both Codex regressions in; 263 PASS, -race 22/22. Final gate (session 019f8ffd-4aca-7201-a374-bfb5cdce6f06): #2 RESOLVED (documentation honest, retired phrase only in repudiation contexts); #1 flipped to mirror image — retention now TOO sticky (consecutive ambiguities pin B forever, recovered A never reprobed). Wave 9 LANDED (d1e7d54, pushed): bounded ambiguity lease per Codex's own recommendation, its exact regression schedule pinned, 265 PASS, -race clean. CLOSING GATE: **APPROVED** (session 019f901f-4762-7db0-b7a5-f0348eef9b6e, "approve", no material findings — termination trace constructed: (2,0,0)→(0,0,0)→stale-branch→(1,0,1), all endpoints reprobed for n≥2, n=1/n=0 correct). **TASK 7 CLOSED under D-006 at d1e7d54** — 9 waves total (44f2765→d1e7d54). Runner+snapshotter+daemon senior-approved.
- Task 8 (prices): first dispatch DIED on session limit (nothing written — tree clean, verified); REDISPATCHED fresh (fable), identical spec. Design decisions carried in: chainlink_feed processing gets its OWN derive cursor (windowed RawLogsInRange reader → prices rows) which resolves review flag M4 (epoch pruning now waits for the feed cursor); feed reorg handling joins the unacked-epoch gate with prices-above-target deletion in its ack path; OP poll path = PriceProviderV2 engine-exact; weETH composition rows; staleness → WARN + health, manual config (honest deferral per plan). UNIFYING FIX spec was (fable): DURABLE SWEEP GENERATIONS (sweep_generations table + generation-stamped snapshot_sweeps = lagging-set-as-durable-queue, restart-resumable, inherently oldest-first), ApplySweepBatch one-tx (balances+collateral-history-docs+status), per-account failure classification, reorg snapshot invalidation inside RewindDerived's tx (anti-join delete + generation bump), bounded FIFO carry-over (10k cap drop-newest+WARN), side-restricted SaveSnapshot, real-boundary injections. Task 8 HELD until runner approval (its chainlink_feed deriver integrates with the runner — D-006 clause 5). Binding review-accumulated rules in dispatch: resume-from-DeriveCursor; ApplyDerived-error→Reset; ErrUnackedReorgEpoch recovery; per-chain (block,logIndex) stream merging w/ engine address sets; epoch pruning; lock liveness; error backoff; multicall3 snapshotter w/ SOLVENT_SNAPSHOT_INTERVAL; store additive-reads-only; no mainnet daemon run (Task 9's). Original wave spec was: Engine interface amended (controller-authorized unfreeze — BeginBatch(hydrator)/CommitBatch/DiscardBatch/Reset; StateReader with explicit no-row=zero-post-genesis semantics); attempt-scoped state both engines + restart-equals-continuous tests + Codex's drift example; same-tx RDU; archive window-pinning (honest NEEDS_CONTEXT if archive unavailable); tx-aware boundary switch; deficit-first enforcement; stable-snap conversion + typed errors; payload-map copy; ATokenBurn regime doc; T5 fixture provenance + generator + recon/derivation-notes.md ERRATUM annotation (in W1 scope).
- Task 1 (config): COMPLETE (in commit 65c93f5, review clean — Approved, 0 Critical/Important). 4-vs-6 discrepancy resolved: all 6 new tests present verbatim, 12/12 verified by reviewer's fresh run; empty-name check correctly recognized as pre-existing (test-only addition). Minors: implementer report's "store/ isolated" line factually wrong (race artifact); Phase 1 task-1-report.md clobbered (naming fix: -p2 suffix from Task 3 on). Root cause of race confirmed: the BRIEF ITSELF prescribed bare `git commit` — pathspec commit form (`git commit -m ... -- <paths>`) now mandatory in dispatches AND future plan-authoring.
- Task 2 (store): content landed INSIDE 65c93f5 (git staging race: config agent's plain `git commit` swept store agent's staged files; store agent verified byte-correct at HEAD, declined history surgery — correct call, 7cc91b3 was already on top). 12/12 store tests VERIFIED by reviewer's fresh run. Standard review: Approved, 0 Critical/Important. Minors for final wave: doc-note that SaveBatch assumes no intra-batch identity collisions (walker guarantees it upstream); explicit column list vs SELECT * schema-drift hazard; per-row time.Now().UTC() vs old single now() default; temp-table DDL on nil-logs calls. Codex senior pass (session 019f8cd9-b2fb-7502-aefd-2fd23b2b0405): do-not-ship, 1 high + 1 medium + 1 low. ADJUDICATED — all ACCEPTED, fixer dispatched (sonnet): fork-safe cursor guard (equal-height only when chain_id+hash unchanged), intra-batch identity validation in SaveBatch (coalesce identical/reject divergent — converged with standard reviewer's ⚠️), empty-batch DDL skip + explicit column lists + single batch timestamp, 4 new tests incl. single-connection temp-table reuse. Lock-liveness fencing rider: REMAINS deferred to Task 7 (already a deliverable there; new context noted — SaveBatch writes don't run on the locked session). Codex cleared as safe: temp-table/pool reuse, LIKE-clone column ordering, BYTEA[] NULL semantics. Won't-fix-now: EXPLAIN/benchmark scaffolding (YAGNI; live backfill in Task 9 is the real benchmark). Codex process note: its first job was self-killed in error and rerun clean; wedged state file left at codex-openai-codex/state/.../review-mrwwe97j-101biq.json (harmless, informational). COMMIT-RACE FIX for all future implementer dispatches: agents must commit with explicit pathspec `git commit -m "..." -- <paths>` (never plain `git commit`), which commits named paths regardless of what else sits in the shared index.
- Task 2: COMPLETE (65c93f5 store portion + fix 53fe65b, pushed). Fix re-review: Approved — all 5 changes verified (guard disambiguation re-derived), sampleLogs ride-along cleared (test-only, no weakened assertions, reuse test genuine). Cosmetic nit: fmt.Sprintf map key (ignore).
- Task 3 (schema): commit fa5125d, 22/22 live-db. KNOWN PLAN DEFECT under review: position_events PK (chain_id,tx_hash,log_index) can't hold multi-event-per-log (Liquidated tuple array in Task 5 ⇒ N events from 1 log) — expected fix: seq SMALLINT in PK + PositionEvent.Seq; migration 00002 is local-only so edit-in-place + goose down/up beats an ALTER layer. Implementer's engine-in-PK concern likely theoretical (engine←stream←address topology). Standard review verdict: Needs fixes — PK defect CONFIRMED EMPIRICALLY (reviewer reproduced the 2-events-1-log batch abort live); seq-in-PK endorsed over synthetic log_index offsets (destroys traceability, collision risk); defect localized (RewindDerived/BalancesFor agnostic to fan-out). PLAN-DEFECT ADJUDICATION: plan self-contradicts (Task 3 schema vs Task 5 Liquidated requirement) → schema amendment is the only coherent reading; proceeding without user gate, flagged in chat for override. Also from standard review — Important: zero-balance row asymmetry (live apply leaves amount=0 row; rewind rebuild HAVING SUM<>0 drops it — same logical state, two table shapes; needs comment + Task 7/9 handling); Important: RewindDerived takes chainID but rebuild half ignores it (benign at 1:1 engine↔chain; latent trap). Minors: per-event round-trip loads; no BlockNumber<=throughBlock guard. Engine-in-PK concern: confirmed theoretical (disjoint chains+addresses). NUMERIC ::text read path verified unconditionally safe (PG never emits scientific notation). Cleared: record-only skip, testDeriveStore duplication (right call for scope). HOLDING consolidated fix until Codex lands (overlapping attack surface).
- Task 8 (prices) LANDED (bf72d8e, pushed pending): D-006 hold released by Task 7's approval. internal/prices/{prices,poller,feed}.go + store/prices.go (ApplyPrices epoch-gated/idempotent, RewindPrices source-scoped delete+ack one tx) + config/feeds.go + daemon wiring; 5,016 lines/15 files, all inside allowed_paths. CONTROLLER-VERIFIED INDEPENDENTLY (not agent-claimed): 348 PASS / 0 FAIL / 0 SKIP vs measured 265 baseline at 3cc34da, go build+vet clean, gofmt empty. Agent reports -race clean in golang:1.24 container (host lacks cgo). Refactor of note: rewindTarget extracted in store/derive.go so RewindDerived+RewindPrices share epoch arithmetic — THIS TOUCHES CODEX-APPROVED CODE (3b864ac/d1e7d54) and re-opens it under D-006. 8 disclosed deviations; sharpest is D-3 SELF-FLAGGED: poller placed on the reorg epoch gate though the settled decision mandated that only for the feed path, making polled-price rewind LOSSY AND UN-RE-DERIVABLE (polled prices are point-in-time contract reads, not replayable from logs) — worst case a full-rewalk rewind wipes all polled history; agent states the switch to ack-but-keep is one call site. Also unverified: ratio.decimals:18 declared not on-chain-checked; daemon price pass has no unit test; 26h staleness default matches no feed's heartbeat; liveSlackBlocks=64 Ethereum-calibrated. Codex adversarial pass DISPATCHED with 12-item attack list (D-3 lossy rewind first, derive.go extraction second, staleness logic, surviving-row window at rewind target, commit-indeterminacy compliance, non-positive prices, ErrDeriveCursorRegression attribution, M4 feed-cursor pruning, feeds.json additions, cap-adapter doc overclaim, pollViews closed map, multicall3 duplication).
- GOVERNANCE SESSION 2026-07-24/25 (owner present, three decisions): (1) D-007 ACCEPTED (3cc34da) ratifying the generation-4 override that D-006's wave-5 lease expiry forced — the flag had lived ONLY in chat and did not survive; basis verified not asserted (governance commits roadmap-only, 5 product commits inside allowed_paths, all Task 7 waves 5-9 Codex-closed at d1e7d54). (2) Claim released+reopened at GENERATION 5 (30462fc/44892f2). (3) D-008 ACCEPTED (b5945aa): SERIAL-MODE LEASES ARE NON-BINDING. ROOT CAUSE FOUND for all three Task 7 governance recovery cycles — 8h default lease is shorter than a Codex review cycle AND claim.py refuses to mutate a claim while >1 worktree is registered ("found 2, use an external atomic allocator") while Codex reviews RUN in worktrees ⇒ lease expires mid-review and CANNOT be renewed ⇒ forced release/suspend/reopen under owner override ⇒ owes ratification. Lease never prevented a conflicting write (serial mode: nothing to fence; RULES 2/4 + skill say claims are accountability metadata, not locks). Correctness enforcement explicitly UNTOUCHED: D-006 gate, allowed_paths scope, protected-surface owner review, evidence discipline. Owner-delegated judgement, verbatim basis recorded in D-008 frontmatter.
- TOOLING/CI FINDINGS (2 insights committed 6f1ae80/d0cafea, candidate status): (a) SERENA'S GO TOOLS HAD NEVER WORKED — .serena/project.yml declared go but gopls was absent and ~/go/bin did not exist; ALL of Phase 1-2 Tasks 0-7 ran on silent grep fallback. Installed gopls; install does NOT take effect until the MCP server restarts (verified: install ok, calls still failed, worked after restart). Serena confirmed live via get_symbols_overview on store/derive.go. (b) GOVERNANCE-BEARING DIRECT PUSHES ALWAYS FAIL the advisory candidate scope-review job ("owner approval requires a positive decimal pull-request number") — doctor+selftest PASS, only the scope replay fails, BY CONSTRUCTION per RULES 8/22. Confirmed by CONTROLLED comparison not inference: run 30135164815 (insights-only push, immediately following) passes all three advisory jobs including scope review; only the protected-surface variable differs. Precedent 30018437004 = the generation-4 push. NOT fixable by upgrade (manage.py plan: receipt matches current bundle digest, NO-OP/REFUSED) and NOT fixable by adopting PRs (the pull_request step also omits --pull-request-number/--policy-approval); real fix needs a server-side CONTROL_PLANE_POLICY_APPROVAL token + hosting ruleset required-workflow = GitHub admin work, owner's call, not urgent while posture is honestly labelled advisory. Controller decision: KEEP the workflow (doctor+selftest genuinely work and catch drift; killing three jobs to silence one structurally-unsatisfiable job is a bad trade), and D-008 makes protected-surface pushes rare so the noise mostly self-resolves.
- PENDING AT SESSION BOUNDARY (all blocked on the running Codex review's worktree C:/wtcodex1, detached at bf72d8e — DO NOT remove while the review runs): (i) git worktree prune — the stray worktree blocks ALL claim mutations and fails the installer's git-hooks adapter check closed (manage.py plan CONFLICT); (ii) claim.py renew --hours 8760 per D-008 (current lease still the 24h one, expires 2026-07-25T23:09:59Z); (iii) push bf72d8e + b5945aa; (iv) adjudicate the Codex verdict; (v) RESCOPE W1 to add .superpowers/sdd/**/*.md — THE LEDGER YOU ARE READING IS GITIGNORED (.gitignore:5 `.superpowers/` plus a nested `*`) and exists only on this disk, which contradicts the control plane's own premise that durable authority lives in the repository; markdown is 396K vs 1.9M of review diffs, so track the .md and keep ignoring *.diff. Rescope precedent: db4b926 added recon/derivation-notes.md. Do NOT force-add around the scope gate.
- Task 8 FIX WAVE 1 LANDED (ce053bd, pushed): all 11 Codex round-1 findings, Codex's own recommendation implemented for each. CONTROLLER-VERIFIED 423 PASS / 0 FAIL / 0 SKIP (baseline 348), build+vet clean, gofmt empty; agent reports -race clean all 9 packages in golang:1.24 incl. live-Postgres via host.docker.internal. Cluster A: unpackMulticallResult keeps the discarded block hash; migration 00005 (FORWARD-only) adds price_poll_anchors + prices.owner_engine; reorg repair walks anchors newest-first, re-checks via HeaderHashFrom, passes verifiedFloor that RAISES the target above the sparse-log ancestor; RewindPrices dropped its sources param and scopes by owner. Cluster B: freshness per (asset,source) hydrated from durable rows; feed freshness hydrated from raw_logs pre-verdict and re-hydrated post-rewind; per-feed heartbeat+grace replaces global 26h (SOLVENT_FEED_STALENESS now REFUSED not ignored); /readyz /healthz /health surface. Cluster C: freshness staged during decode, merged only on confirmed commit, discarded+rehydrated on every apply error. Cluster D: quarantine + LatestUsablePrice with a CHECK making a valid non-positive row unrepresentable. internal/decode/decode.go touched ADDITIVELY (exports ChainlinkAnswerUpdatedTopic0 derived from embedded ABI) — re-opens the layer approved at d8c462b. Honest disclosure: only the ETH/USD 3600s heartbeat is evidence-backed; three 86400s values are published Chainlink params NOT bytecode-confirmed, recorded with a per-row provenance column in recon/derivation-notes.md as OPEN WORK. .env.example edit reverted (outside allowed_paths) so SOLVENT_HEALTH_ADDR is documented only in config.go — follow-up owed.
- Codex ROUND 2 (session 019f974b-5122-76e3-9fdf-299db764187e, job review-mrzsz6d2-4ybbhj): **NO-SHIP**, 4 high + 4 medium, ALL ACCEPTED. RESOLVED: A2 owner-scoping, migration 00005, B3 provenance labelling, decoder export, adapter-equivalence docs. STILL OPEN: [high] A1 NOT closed — verifiedFloor collapses anchor-read errors / HeaderHashFrom failures / 8-anchor-probe exhaustion into floor=0, then acks the epoch and deletes every poll-owned row above the sparse target; THE COMMITTED FALLBACK TEST EXPLICITLY CONFIRMS a transient hash-probe outage empties polled history (we shipped a test documenting the data loss as expected). [high] same-height frozen RPC keeps freshness+readiness green forever (store accepts equal-height/equal-hash replay as idempotent success without inserting or touching observed_at, yet the poller stamps process time on any nil error). [high] LatestPriceFreshness deliberately includes INVALID rows, so a zero/negative-returning oracle stays "fresh" and /readyz stays green. [high] readiness ignores walker/derivation/snapshot failures entirely — only price workers contribute, so /readyz can be 200 while DM/Aave ingestion is stalled. [medium] future updatedAt clamped to now() is a non-durable receipt time that re-clamps on every hydration, reintroducing the restart reset. [medium] no-anchor cause-unknown branch can STALL FOREVER (frozen-below-cursor endpoint: every eth_call succeeds, every apply regresses, no anchor can ever land, error-driven failover never fires) — Codex CORRECTS ITS OWN round-1 wording: separate diagnosis from recovery. [medium] frontier-below-cursor misattribution is not bounded to one pin. [medium] readiness starts Ready=true before registry/db/migrations/chain-verify/workers.
- ROOT PRINCIPLE for wave 3 (why "fix the class" only half-landed in wave 1): **health may be refreshed ONLY by a durable, newly-observed fact.** Wave 1 fixed "don't read freshness from process memory"; round 2 found four more variants — nil error refreshes health, invalid row counts as fresh, clamped process time becomes the receipt, readiness green before verification. Codex's concrete remedy: the store must RETURN which rows were actually inserted with their DB timestamps, and only those may refresh health. Second theme: **destructive operations must fail CLOSED** — verifiedFloor degrading to 0 on a transient probe outage is the mirror image of readiness defaulting to Ready=true; both pick the permissive default on missing information.
- REVIEW-VISIBILITY DEFECT (found by round 2, fixed at 211966c): Codex COULD NOT READ task-8-codex-round1.md or the fix-wave report — gitignored, and the job ran in a worktree pinned at ce053bd so they were absent. Codex disclosed this rather than fabricating; it verified a fix wave without being able to read the findings it was verifying. Fixed by rescoping W1 (generation 6) to track .superpowers/sdd/** — root .gitignore now `.superpowers/*` + `!.superpowers/sdd/`, nested .superpowers/sdd/.gitignore now `*` + `!.gitignore` + `!*.md`; verified 33 markdown tracked, 0 diffs (1.9MB of *.diff still ignored). The ledger is finally repository state, as the control plane's own premise requires.
- DISPATCH HYGIENE CONFIRMED: round 2 backgrounded the dispatch call and used ONE poll path — status went running→verifying→completed, no wedge. Round 1's four wedged attempts were caused by a foreground 2-minute timeout SIGTERMing the detached Codex child while its state JSON still read "running". Always background the dispatch itself; do not spawn hedge monitor loops.
- Task 8 FIX WAVE 3 LANDED (8907588, pushed): all 8 round-2 findings, built on TWO PRINCIPLES rather than 8 patches. Principle 1 made STRUCTURAL: ApplyPrices/ApplyPolledPrices return store.ApplyResult (rows ACTUALLY inserted, DB-timestamped via the INSERT's own RETURNING observed_at, + AnchorInserted); recordDurableInserts is the only function that advances a health signal and its only input is that result; Step's success path contains no p.now(). An idempotent replay returns an empty result ⇒ a frozen same-height endpoint cannot refresh health BY CONSTRUCTION. Principle 2: verifiedFloor's collapse-to-0 deleted; Poller.repair refuses RewindPrices while it owns rows it cannot prove canonical (no ack, no delete, poll_rewind_blocked on /readyz, retry next Step, anchor probes PAGINATE deeper per Step instead of degrading after 8); floor=0 now means "nothing above the target to lose" proved by CountOwnedPricesAbove; healthState starts CLOSED (status "starting", 503) clearing only via markInitialized. The wave-1 data-loss test was replaced with a refusal test (RewindPrices never called, row survives, epoch stays unacked, /readyz red, + a retry leg proving a recovered probe completes the repair retaining the row). CONTROLLER-VERIFIED: 451 top-level PASS / 510 incl. 59 subtests / 0 FAIL / 0 SKIP (baseline 423 top-level), build+vet clean, gofmt empty. MEASUREMENT CONVENTION PINNED (this delta has now confused us twice — Task 7 had the same scare): controller baselines 265/348/423/451 are TOP-LEVEL `^--- PASS` counts; agents may report all-PASS including indented subtests. Always state which. No migration (new durable timestamps read columns 00002/00005 already created; 00001-00005 untouched). internal/ingest/walker.go touched ADDITIVELY (records the (head,cursor) pair each Step already reads + HeadLag()) — re-opens a round-1-cleared file.
- Codex ROUND 3 (job review-mrzvr1vm-punv5x, target 8907588 vs ce053bd): **NO-SHIP**, 4 high + 1 medium, ALL ACCEPTED. Trend 11 → 8 → 5, converging. [high] A1 STILL OPEN — anchors are probed newest-first; a failed probe sets probeFailed but a LOWER MATCHING anchor then returns floorVerified WITHOUT considering that failure, so repair rewinds to the lower floor and deletes canonical history above it without ever proving it non-canonical; the replacement test only exercises TOTAL probe failure and misses the mixed failure-then-match path. [high] PERMANENT DEADLOCK: a pending epoch with legacy unanchored rows — Step always routes an unacked epoch to repair before legacy adoption, repair refuses when owned rows lack anchors, adoption is only reachable once the epoch is not pending, and ack advances only through rewind; no production path clears it, and the test MASKS the cycle by assigning st.unacked=false directly, a transition the real store cannot perform. [high] snapshot semantic stall with green readiness (wrapper treats every nil error as recovery; the all-endpoints-stale path returns (false,nil) forever; snapshot cursors absent from the no-progress pass). [high] readiness does not require derivation workers to catch up to their INPUT FRONTIER — only cursor recency, so a worker advancing tiny backfill windows never trips no_progress; CONTRADICTS the claimed not-ready-until-chain-head behaviour, which the controller had already relayed to the owner as fact. [medium] future-timestamp refusal is not durable across restart (wall-clock dependent; the same log is rejected now and accepted after a restart, moving lastUsable to a future time and greening readiness for tolerance + a whole heartbeat+grace window) — the "identical refusal across restarts" claim is false.
- TEST QUALITY IS NOW A FIRST-CLASS DEFECT CLASS (three waves, three test-integrity failures): wave 1 shipped a passing test asserting "the polled history is gone — this loss is real"; wave 3's replacement covers only total probe failure and misses the mixed path where the loss still occurs; wave 3's legacy-adoption test fabricates a store transition (st.unacked=false) that cannot happen in production, masking a permanent deadlock. Common cause: tests written to demonstrate the intended path rather than to FALSIFY the implementation. A green suite has now twice actively vouched for a defect. Controller lesson: verifying that a test asserts the right thing is NOT the same as verifying it covers the case space — I checked the former and skipped the latter. Wave 4 makes test adequacy a deliverable: no test may assume a state transition the real store cannot produce, and every guard/refusal test must cover PARTIAL and MIXED failure, not just total failure.
- A1 has survived THREE attempts, each fixing the cases its author imagined. Wave 4 must ENUMERATE the anchor-verification state space (no anchors / legacy unanchored / all probes fail / SOME probes fail / mixed failure-then-match / canonical anchor below a page boundary / interleaved walker rewinds) with a test per case. Invariant: never delete or ack without positive proof of non-canonicality for everything above the floor.
- Task 8 FIX WAVE 4 LANDED (e262f0a, pushed): all 5 round-3 findings. A1 stopped being patched and became an ENUMERATED PARTITION — floorOutcome states each outcome's delete/ack permission in its type doc; a match becomes a floor only if no probe above it failed (paging resume lowered only by a fully-clean page) AND no owned row above the deletion boundary sits at an unanchored height (CountUnanchoredPricesAbove); boundary = max(floor, effective target) with the effective target now READ from the store (PriceRepairExposure) instead of assumed equal to the cursor — that assumption was itself a hole nobody had named. Deadlock got BOTH Codex options: ack-when-nothing-above-effective-target for the provable case + NeutralizeUnverifiablePrices for the realistic one (retain every row, mark unprovable ones unreadable/unverifiable, honour a verified floor, drop anchors above the boundary, ack) — nothing deleted, nothing unprovable trusted, plus an insertPrice supersede arm without which a chain head reaching a neutralized height would fail every round on unresolvable divergence. Snapshot: durable SweepProgress drives no_progress for an OPEN generation that stops landing batches. Frontier: new frontier_lag compares every raw-log consumer (runners + feed deriver) against the min ingest cursor of its streams, bound 5000. Timestamp: validity bound to raw_logs.ingested_at, acceptance capped at min(reported, ingested_at). CONTROLLER-VERIFIED 472 top-level / 536 incl. 64 subtests / 0 FAIL / 0 SKIP (baseline 451/510), build+vet+gofmt clean. TEST-INTEGRITY FIX VERIFIED BY THE CONTROLLER DIRECTLY (grep + enclosing-function resolution): `unacked = false` now appears ONLY inside the fake's RewindPrices and NeutralizeUnverifiablePrices bodies, mirroring the two real store calls that advance acked_epoch; no test body fabricates it; the cycle is pinned against live Postgres in the new internal/store/prices_repair_test.go. Wave self-falsified two of its OWN claims and weakened them ("no un-neutralize" false — the supersede arm restores usability; "identical forever" false — a rewind reassigns ingested_at). Disclosed deviation: wave 3's floorAllOrphaned refuse-forever became a DELETION path when anchors cover every row. internal/store/derive.go touched a THIRD time (+78).
- Codex ROUND 4 (job review-mrzyqzt4-z21lpx, target e262f0a vs 8907588): **NO-SHIP**, 3 high + 1 medium, ALL ACCEPTED. Trend 11 → 8 → 5 → 4. [high] A1 STILL OPEN, ATTEMPT 5 — but the missing dimension is TIME, not case coverage: probeResumeFrom is reused across Steps carrying only a HEIGHT, not bound to the reorg epoch or a live-chain checkpoint, so a mismatch proven under one chain state is still trusted after a LATER reorg makes those same skipped anchors canonical again; the next Step then accepts a lower match or concludes floorProvenOrphaned and deletes now-canonical rows without re-probing. Wave 4's interleaving test mutates only the effective target and leaves observed hashes fixed, so it exercises the scenario's shape without its substance (THIRD time a test passed while missing the case it was written for). [high] **A FIX INTRODUCED A REGRESSION** — wave 4's frontier pass ERASES wave 3's feed health: setWorkerConditions replaces every condition under a worker prefix and applyProgressConditions/rc.publish run AFTER stepPriceWorkers, so the feed deriver's staleness/timestamp/RPC-lag/step_error conditions are deleted (entirely with no lag, or all but frontier_lag with lag) — /readyz can go green and even clear startup after a feed Step failure. [high] closed DEGRADED sweep generations are treated as healthy: CompleteSweepGeneration closes a generation after accounts exhaust their retry budget and reports it only via WARN, and per-account failures return nil from ApplySweepBatch, so readiness stays green with missing/stale collateral (the controller had explicitly asked Codex to judge this disclosed exemption — it is unsafe). [medium] the two 5000-block bounds COMPOSE: walker 5000 behind head + consumer 5000 behind walker, both permitting equality ⇒ ~10000 blocks ≈ 33 HOURS behind head on Ethereum's own 12s calibration while /readyz stays green; copying headLagBound is not a justification for a liquidation-facing bound.
- SIGNAL CHANGE WORTH ACTING ON: waves 1-4 were all "the fix did not go far enough". Round 4's finding 2 is the first "the fix BROKE something that worked". The daemon health surface now composes poller + feed + snapshot + walker + derivation state and its PUBLICATION ORDER carries correctness — it has become a subsystem while still being edited as a few call sites. Decomposition question for the owner: split the health/readiness surface from the price pipeline as its own reviewable unit. Lesson generalised: any CACHED VERIFICATION must be invalidated by the epoch/checkpoint it was computed against and revalidated immediately before the destructive act; enumerating states is not enough when the truth being proven is time-varying.
- Task 8 FIX WAVE 5 LANDED (cb00f09, pushed): all 4 round-4 findings. A1 got a TWO-PART TEMPORAL BINDING — PriceRepairExposure.ReorgGeneration (read in the same tx as the counts) stamps paging state and discards it when it moves, PLUS a live-chain checkpoint (highest anchor probed, its hash, the endpoint that answered) re-read IMMEDIATELY BEFORE rewindTo/neutralize; changed means discard the pass and restart from the newest anchor, unreadable means retry keeping the pass. Rationale: the generation alone is insufficient because it only moves once the walker notices; the checkpoint covers that window. Round-4 regression fixed STRUCTURALLY: setWorkerConditions DELETED (controller verified by grep — only a historical comment remains); the sole mutator publishRound takes the WHOLE round composition so no per-worker replace primitive exists, and a repeat publication within a round merges rather than replaces, logging at Error. Snapshot counts exposed through SweepProgress; end-to-end lag replaced the two composing 5000-block bounds with a single chain-head-relative bound (50 ETH / 300 OP). CONTROLLER-VERIFIED 480 top-level / 558 incl. 78 subtests / 0 FAIL / 0 SKIP (baseline 472/536), build+vet+gofmt clean. CONTROLLER RAN AN INDEPENDENT MUTATION TEST: disabling the checkpoint gate made TestPollerRewindRefusesWhenTheCheckpointMovedBeforeDeletion and TestPollerRewindRefusesWhenTheCheckpointCannotBeReRead FAIL, then restored the file and re-verified clean — the tests genuinely detect the defect. Controller also confirmed the A1 fake now INVERTS canonicality between Steps (0xbeef for every anchored height in Step 1, real hashes after), fixing round 4 shape-without-substance complaint. Touched three previously-approved files: internal/store/derive.go (4th time), internal/ingest/walker.go, and internal/snapshot/snapshot.go (Task 7 code, closed at d1e7d54).
- Codex ROUND 5 (session 019f9825-d59c-7d53-b2cd-06564ddc4dcb, job review-ms01i4w8-drwxsm, target cb00f09 vs e262f0a): NO-SHIP, 2 high + 1 medium, ALL ACCEPTED. Trend 11 to 8 to 5 to 4 to 3. [high] A1 ATTEMPT 6 — the checkpoint validates only the endpoint that answered the HIGHEST probe, but probes deliberately failover ACROSS endpoints, so a proof is assembled from multiple possibly-divergent ancestries: endpoint 0 mismatches the highest anchor while retaining the middle one, endpoint 1 mismatches that middle anchor on another fork, endpoint 0 matches a lower anchor, and the unchanged endpoint-0 checkpoint then authorises deleting a middle anchor that IS canonical on the checkpoint chain. THE FAKE RETURNS HASHES BY HEIGHT ONLY, so the new tests are STRUCTURALLY INCAPABLE of modelling this. [high] snapshot readiness green while collateral is failed or absent — gated on Exhausted>0, but a first failed account read gives Failed>0 / Exhausted==0 so readiness stays green for an account that never produced collateral, and generation rollover drops the failed row; THE REGRESSION TEST EXPLICITLY EXPECTS Ready=true WITH TWO CURRENT FAILURES, codifying the unsafe policy rather than detecting it. [medium] fixed block counts do not enforce the stated 10-minute bound — nominal 12s/2s cadence conversion is a unit fallacy: missed slots make 50/300 blocks span longer, and head freshness proves the HEAD is current, not the cursor block timestamp, so the liquidation-facing time guarantee can be false-green; fix = gate on head timestamp minus cursor block timestamp.
- A1 FIVE DIMENSIONS, and the strategic way out. Round 3: case space incomplete, wave 4 enumerated it. Round 4: missing dimension was TIME, wave 5 bound proofs to generation + checkpoint. Round 5: missing dimension is WHICH CHAIN VIEW (multi-endpoint ancestry coherence). This is the signature of solving a distributed-consistency problem incrementally — proving non-canonicality from several untrusted, independently-forked RPC views. Every one of the five attempts exists solely to justify a DELETION. Wave 4 already built the alternative: NeutralizeUnverifiablePrices retains every row and marks unprovable ones unreadable. IF THE POLLER NEVER DELETES, THE PROOF OBLIGATION DISAPPEARS AND A1 DISSOLVES instead of being fixed a sixth time. Cost: some permanently-unusable rows in a Postgres table. Raised to the owner as a strategy decision.
- TEST INTEGRITY — FOURTH failure, same class as the FIRST. Wave 5 snapshot regression test expects Ready=true with two current failures, codifying unsafe policy, exactly like wave 1 test asserting data loss was expected, after four rounds of instruction. AND THE FAKE IS NOW THE LIMITING FACTOR: keyed by height only, it cannot express endpoint disagreement at all, so no test-writing discipline can cover finding 1 until the harness changes. CONTROLLER OWN MISS, RECORDED: my mutation check proved the checkpoint tests detect a disabled gate and I reported that as strong evidence — it was, but it said nothing about whether the fake could express the failure mode, nor whether other tests codified unsafe policy. Mutation testing validates the tests you HAVE; it is silent on the tests you CANNOT WRITE.
- BOTH disclosed rationales the controller asked Codex to judge were WRONG: (a) gating on Exhausted rather than Failed, (b) the 50/300-block derivation of a 10-minute bound. Disclosure is not soundness — but naming them as review targets is why both were caught. DISPATCH HYGIENE: two more jobs wedged from foreground dispatch (review-ms01ai29-cscf5i, review-ms01esp2-2dtxh6) — third round running; ALWAYS background the dispatch call.
- Task 8 FIX WAVE 6 LANDED (ed2f26e code + 9f43a9e report, pushed) — PRICE-PIPELINE UNIT ONLY, under D-010. HARNESS FIRST: the fake's four height-keyed maps became views map[int]*endpointView (one private chain per endpoint) and HeaderHashFrom now walks endpoints from the requested start exactly as chain.Failover.doFrom does, stamping the token with whoever answered. It can now express endpoint disagreement at the same height, silent failover, and one-endpoint-healthy-except-at-one-height. The agent ran the rebuilt harness against the UNCHANGED poller first (all existing tests still passed — which is what makes later failures meaningful), then reproduced Codex's round-5 scenario against wave-5 code: RewindPrices(toBlock 5000, verifiedFloor 4800), the mixed pass taking "4900 orphaned" from endpoint 1 while the endpoint-0 checkpoint authorised destroying a round canonical on that chain. A1 REPRODUCED VERBATIM BEFORE BEING FIXED. REMOVED: RewindPrices moved from PriceStore to FeedStore so PollStore declares NO deletion primitive; Poller.rewindTo, both rewind arms, the bootstrap rewind, probeCheckpointBy, and GATE 1 with its probeFailed bookkeeping all deleted (a pass now ends at its first failed probe, so GATE 1's situation cannot arise). poller.go -92/+108, the +16 being clause 4's backlog surface, so the repair path is net -11. CONTROLLER VERIFIED STRUCTURALLY: PollStore and its embedded PriceStore carry no RewindPrices, FeedStore does (correct — feed rows are replayable from raw_logs), and a REFLECTION TEST pins the invariant. CONTROLLER MUTATION: removing the end-the-pass-on-first-failed-probe branch killed FOUR tests including TestPollerRepairRefusesAProbeSilentlyServedByAnotherEndpoint (round 5's exact scenario); restored and re-verified clean. 487 top-level / 565 incl. 78 subtests / 0 FAIL / 0 SKIP (baseline 480/558). DISCLOSED METHOD DEFECT: the agent ran git checkout -- internal/prices internal/store during its first mutation loop while its work was UNCOMMITTED, destroying it; it rebuilt from its edit record, re-verified identically, and committed before re-running any mutation.
- Codex ROUND 6 (job review-ms06uswe-q9doja, target ed2f26e vs cb00f09): NO-SHIP with **ONE** finding. Trend 11 to 8 to 5 to 4 to 3 to 1. CONFIRMED RESOLVED: the poller's deletion route is structurally removed; the rebuilt harness matches real Failover semantics; the wave-7 split did not damage snapshot/readiness scope. [high] MINORITY-FORK NEUTRALIZATION IS NOT RELIABLY RECOVERABLE — a pass may trust one coherent endpoint that is on a minority fork; the poller reads only latest, so once the head advances past height H it NEVER observes H again; a fresh same-identity observation at H is the ONLY implemented un-neutralize path and can never fire for a past height; and neutralization ALSO DELETES THE AFFECTED ANCHORS, destroying the provenance revalidation would need. So a self-consistent minority-fork pass leaves canonical rows permanently valid=false and excluded from usable-price reads, while newer polls clear the acute health signals and hide the historical gap.
- GOVERNANCE CORRECTION: **D-010 SUPERSEDED BY D-011** (27b2968). D-010's whole argument was an asymmetry — deletion unrecoverable so remove it, marking recoverable so prefer it — and the second half was ASSERTED, NOT VERIFIED. That was a controller authoring error, recorded plainly because asserting-instead-of-verifying is the failure this project keeps paying for. Note also that D-010 said "never delete polled price ROWS" and the implementation honoured that literally while still deleting ANCHORS: the letter kept, the intent (retain enough to recover) lost. D-011 carries clauses 1-4 forward (round 6 confirmed clause 1 is enforced by the TYPE SYSTEM) and adds: (5) neutralization must NOT delete anchors; (6) recovery must work for PAST heights without a new poll there, with Codex's exact regression required; (7) marking requires CROSS-ENDPOINT AGREEMENT, disagreement retains data unmarked, retention is always the safe default; (8) a cleared acute signal must not hide a historical gap. Under "gold standard, no shortcuts" both of Codex's alternative remedies are required, not one.
- MILESTONE: round 6 is the first round where the finding count reached 1, AND the remaining finding is not a coding defect but a FALSE PREMISE IN A GOVERNING DECISION. The review has moved from implementation to design.
- Task 8 FIX WAVE 7 LANDED (8f0eeef code + 6460d36 report, pushed) — D-011 clauses 5-8. KEY INSIGHT: a PRICE at height H can only be re-read by executing at H (impossible: Failover.CallWithToken/CallFrom both issue CallContract(..., nil) = latest only, verified not assumed), but the ANCESTRY of H is a header read available at any height forever, and the round recorded which block it ran against — so recovery needs a new PROOF about H, not a new POLL at H. Clause 5: anchors survive neutralization; NewestPollAnchor excludes neutralized heights, pruneOldPollAnchors exempts them. Clause 6: NeutralizedPriceAnchors + RevalidateNeutralizedPrices + Poller.revalidateNeutralized, proof checked against the recorded anchor INSIDE THE TRANSACTION so a caller cannot restore rows with a hash of its own invention. Clause 7: checkpointCorroborated gates every marking arm. Clause 8: backlog re-read after every landed round. Required regression TestPollerRevalidatesAPastHeightWithoutAnotherPollThere asserts no apply touches H, the head really moved past H, and THE ROW observed_at IS UNCHANGED (a supersede re-stamps it, a revalidation must not — the exact distinction D-010 got wrong). The agent's OWN NEW TEST caught a latent hazard: adoptLegacyAnchors records the chain CURRENT hash, harmless under D-010 but under clause 6 means revalidation checks the chain against a copy of itself; fixed in UnanchoredPriceBlocks and AdoptPollAnchor. CONTROLLER-VERIFIED 497 top-level / 575 incl. 78 subtests / 0 FAIL / 0 SKIP (baseline 487/565). CONTROLLER MUTATION of the clause-7 gate killed TestPollerRefusesToMarkWhenASecondEndpointContradictsThePass and TestPollerRepairRefusesAProbeSilentlyServedByAnotherEndpoint; restored, re-verified. Scare investigated: 3 files showed modified after commit — git diff EMPTY, a CRLF artifact, content-identical to HEAD.
- Codex ROUND 7 (session 019f98fa-6b89-7710-ba57-4d7e4b9e2320, target 8f0eeef vs ed2f26e): NO-SHIP, 2 CRITICAL + 2 high + 2 medium + 1 low, ALL ACCEPTED. Trend 11-8-5-4-3-1-7; the RISE is legitimate — wave 7 added an entire revalidation subsystem and three findings are defects in that new machinery. [critical] CIRCULAR PROVENANCE: applyPrices supersedes only observations present in the new round then unconditionally inserts ONE anchor for the whole engine/height, so if unanchored rows at H were neutralized and a later latest-poll also lands at H with some assets skipped (or every oracle reverting), the old rows stay marked while a NEW current-chain anchor is committed; NeutralizedPriceAnchors then associates the old rows with that new anchor and revalidation restores values possibly read from a REPLACED block — a third path around both adoption guards, making the proof circular. [critical] STARVATION: NeutralizedPriceAnchors always returns the OLDEST EIGHT heights; a genuinely orphaned height stays neutralized and keeps its anchor so it appears in every query, and once eight such exist every later candidate is unreachable FOREVER — including canonical rows wrongly marked by a later repair. Defeats clause 6. [high] unanchored rows are still marked through an undo that cannot fire, AND THE TESTS EXPLICITLY ASSERT THIS STATE LASTS FOREVER. [high] the one-endpoint carve-out VIOLATES CLAUSE 7: checkpointCorroborated treats agreementUnobtainable as authorization to mark, so a single endpoint on a minority fork can neutralize canonical history on its own word. [medium] RewindPrices still deletes anchors needed by retained neutralized rows (poller cannot reach it, but Store exposes it and repo tests call it with poll-engine identities) so clause 5 is NOT structurally enforced across all store paths. [medium] NeutralizedPriceStats runs a full-history aggregate every minute while any backlog remains, unindexed, cost scaling with TOTAL history not backlog size. [low] the operator warning states the OPPOSITE of wave-7 recovery behaviour. Codex adjudicated the report §8: items 1,3-5,8-10 acceptable, item 2 a BLOCKER, item 6 needs an index, item 7 unsafe through starvation.
- MUTATION TESTING HAS A FAILURE MODE AND I OVERSOLD IT. Two of wave 7's mutations were MISLABELLED AS SAFETY EVIDENCE: M12 "merely pins the starvation-causing order" and M14 "kills the fail-closed behavior required by the accepted decision". I introduced mutation testing this session and reported it as strong evidence. It IS strong evidence for "does this test detect a change to this code?" and SILENT on "is this code behaviour correct?". A killed mutation proves the tests are load-bearing on CURRENT behaviour; if that behaviour is wrong, the mutation certifies the wrongness. Mutation evidence must be paired with a statement of the PROPERTY the behaviour should have, or it is confidence-theatre.
- TEST INTEGRITY — FIFTH failure, same class as the first and fourth: wave 1 asserted data loss was expected, wave 5 asserted Ready=true with live failures, wave 7 asserts permanent unusability lasts forever. Three waves have shipped a PASSING test that RATIFIES a loss the reviewer then had to find. The instruction has been in every brief since round 3 and has not taken.
- GOVERNANCE ESCALATION: finding 4 is an IMPLEMENTATION-ONLY EXCEPTION to an accepted decision. D-011 clause 7 requires cross-endpoint agreement unconditionally; checkpointCorroborated treats agreementUnobtainable as authorization. Legitimate resolutions are (a) fail closed below two corroborating views, or (b) amend and ratify D-011. An implementation may not quietly carve an exception into governance — escalated to the owner.
- ORCHESTRATOR HANDOFF (2026-07-25): owner switched the controller model (opus → fable) and asked for an independent review of all session decisions before proceeding. Review verdict: agreed with D-007/D-008/D-009, tooling fixes, decomposition, escalations; DISAGREED with D-011's prescription. Owner delegated under the standing directive "gold-standard solutions, no shortcuts. If we do something we do it right."
- **D-012 ACCEPTED (1da0fa4, supersedes D-011): POLLED PRICES ARE SAMPLES, NOT A LEDGER.** The deciding analysis: a wrongly-neutralized row is OBSERVATIONALLY EQUIVALENT to a missed poll, which the system already tolerates with no makeup mechanism (60s telemetry, holes routine); D-011's online revalidation mandate applied ledger-grade guarantees to samples and its subsystem hosted BOTH round-7 criticals. Recovery is NOT given up — anchors + rows persist forever (clause 2), so OFFLINE revalidation stays possible at any future time; only the hot-path loop is removed (clause 3). Verified, not assumed: the unanchored-legacy population is ZERO in production (Task 9 backfills from scratch with 00005 in place). Codex round 7 itself named this path twice ("obtain a ratified decision exception"; "amend and ratify rather than embed an implementation-only exception"). Clause 4 ratifies the endpoint rule: ≥2 configured ⇒ agreement mandatory, fail closed; exactly 1 configured ⇒ single-view marking permitted (configuration, not fault). Clause 1 adds structural RewindPrices rejection for poll-owned engines. TEST-INTEGRITY MECHANISM CHANGED (five failures via instruction): every guard test must CITE the D-012 clause it enforces — binding tests to specified properties, not current behavior.
- Gen-7 claim reopen (a45b911 release + 0e964fe reopen): lease had expired mid-cycle; ROUTINE per D-009, no override drama, ~3 minutes.
- Task 8 FIX WAVE 8 LANDED (92d21c5 code + 63bf5b9 report, pushed): D-012 implemented as a REMOVAL wave — repair path NET −97 non-comment lines. R1: revalidation subsystem deleted (both round-7 criticals dissolve with their host); kept NeutralizeUnverifiablePrices, the supersede arm, no-deletion-primitive + reflection test, (endpoint,height) harness, AdoptPollAnchor's neutralized-height refusal (the hazard is the fabricated anchor, retained forever, not the removed consumer). R2: RewindPrices refuses PollOwnedEnginePrefix before chain binding and before the tx; prices' cursor prefix now DERIVES from the store constant so the strings match by construction. R3 audit FALSIFIED a wave-7 claim: the prune exemption is NOT self-limiting (the monotonic cursor guard makes a supersede at an aged-out height impossible → permanence turned into specification with a citing test). R4: configured-count rule re-checked at the decision site; zero-configured now fails closed (wave 7's <=1 would have marked). R5: transition-driven recount via ApplyResult.Superseded — NO index, NO migration 00006; removing the cadence removed the cost. R6: WARN reports rowsAnchored/rowsUnanchored split computed inside the marking UPDATE via CTE. R7: 5 tests removed with subject, 6 added, 3 re-specified, all cited. MUTATION HONESTY MILESTONE: 12 run, 11 killed, 1 SURVIVED AND REPORTED (M5 absorbed by the decision-site re-check; M5b removes both guards and dies — the property is enforced twice on purpose). Each row states the PROPERTY. CONTROLLER-VERIFIED: 498 top-level / 576 incl. subtests / 0 FAIL / 0 SKIP (baseline 497/575), build+vet+gofmt clean, symbols grep-confirmed gone from production code, own mutation of the R2 guard killed TestRewindPricesRefusesAPollOwnedEngineAndChangesNothing (live-Postgres), restored clean. Flagged for round 8: verifiedFloor has no production caller; RewindPrices' retention predicate is deliberately untested defence-in-depth; R5 evidence is call-frequency not EXPLAIN.
- WAVE-9 PREP WORKFLOW (ultracode, 14 agents map→design→adversarial-verify→synthesize, 0 failures, ~1.26M tokens): both health-surface designs SURVIVED ONLY WITH AMENDMENTS — 4 for the snapshot gate (A1 rewind clamp: RewindDerived never resets last_success_block so the monotonic guard wedges post-reorg while fresh stamps keep the gate green, certifying exactly the invalidated population; A2 ==-replay stamp guard: crash-replay idempotence would re-stamp now() with zero new observation — the 6th/7th instances of the durable-fact disease caught PRE-implementation; A3 bound arithmetic: SweepWorkBatch never re-selects a current-gen success so refresh period is S+I and max(2I,15m) goes PERMANENTLY RED under healthy operation; A4 placement pin vs the !p.Open early return) and 7+2 for the lag gate (L1 no-cursor population: the naive design deleted the ONLY red covering a never-ingested walker — the draft's "acceptable residual" claim was proven factually false; L2 future-skew sanity vs memoized corrupt timestamps; L3 frontier_lag structural gating; L4 chainDown per-round + 30s fetch cooldown — without it a dead chain burns 10s/round in the hot loop, ~20× collapse of a concurrent backfill; L5 memo (chainID,block) + memo-beats-chainDown + restamp throttle — without it header fetches add 25-50% to Task 9 backfill wall time; L6 input decoupling; L7 shutdown carve-out; L8 comment scoping; L9 name-collision guard vs wave-8's shipped condition names). CONTROLLER RULINGS on the workflow's open questions recorded in the brief: OQ1 read-failure erasure → option (b) explicit unmeasured-red, CHANGES the pinned no-verdict precedent (health_test.go:943), flagged for round 9; OQ3 self-adaptive bound accepted (no invented ceiling); OQ4 rename approved (local-only); OQ5 check Migrate-at-startup first; OQ6 operational costs accepted. Brief: task-8-wave9-brief.md.
- R-001 DECISION INPUT (paper analysis, verified against config+walker code): ~152k RPC calls total (~25.4k windows, 10 streams); 42/N hours at N sustained req/s (N=2 ⇒ under a day) BUT no client-side rate limiter exists — sustained-429 pacing degrades to 1-5 WEEKS at the backoff floor. Levers before paying: window >2000, merge the 5 identical-startBlock aave streams (walker already takes address lists; ~10-25× ETH reduction; stream name keys cursors so a merge is a schema/config decision). Recommendation shape: one KEYED free-tier endpoint likely suffices; run a short live probe at Task 9 start, then decide. Full numbers in the wave-9 prep synthesis.
- WAVES 10-13 + ROUNDS 9P/10H/10P (two-unit parallel cadence, all pushed): Wave 10 (c6d7267/8b6c25d/e16d39f) closed round-8's 7; ADD-1/ADD-2 ratified (fdb9f8d, task-8-normative-addenda.md — append-only citation targets). Round 9P (archive task-8-codex-round9-prices.md): 2H+2M — read-side consumers still spoke height-joins (one-arm disease at the data layer); adoption-over-pruned-anchors fabrication engine. Wave 11 (c146853/827a9e6/d81e2e3) closed round-9H's 4: deep-stale reuse arm (green-proof by construction), migration 00008 pass-duration durability, skew-at-every-reuse, real quiet-refusal test via fake CHAIN; hard-case measured 9 vs 180 reads/20 rounds. Round 10H (archive task-8-codex-round10-health.md): 2 findings — slow-SUCCESSFUL fetches defeat the throttle (latency axis unmeasured), clock rollback absorbed by old headers (one time authority per verdict). Wave 12 (6 commits to 872137d) closed round-9P's 4: shared unprovableRow predicate (definition cannot drift), floor CLAMPED to one honest boundary, ADOPTION DELETED on a mechanical argument (cannot set anchor_block, so post-P1 it cannot make any row provable), both exemption clauses individually load-bearing fixtures; DISCLOSED: perl -0pi mutations silently unapplied on CRLF (matrix entries re-run verified-applied), a git checkout destroyed two uncommitted tests. Wave 13 (92a454d/48b9bcb/5c6af36) closed round-10H's 2: completion-stamped monotonic scheduling + per-chain refresh budget with STARVATION and DEADLOCK found by measurement (due-queue rejected: stalled cursor would block the chain; rotation keys on who-asked), Store.Now = DB clock as the single verdict time authority (derive.go 7th mod, +28/-0); measured 9→780 completed rounds/5min. 561/655/0/0 at 48b9bcb; 15/15 mutations.
- Round 10P (verdict in chat, NOT yet archived to its own file): needs-attention, ONLY 2 MEDIUM, both OPERATOR-TEXT: (1) the clamp WARN logs the rejected verifiedFloor as the validity boundary while the returned clamped boundary is authoritative (regression: floor 5000 clamped to 4999, row at 5000 marked, WARN says 5000 valid); (2) the unprovable-classification WARN claims recorded-hash retention for rows whose binding dangles after retention-prune ("unanchored means no SURVIVING anchor linked"). All substance closed; Codex adjudicated residuals (a) acceptable-with-index-correction (inner lookup served by price_poll_anchors PK not prices_anchor_binding_idx — fix the report claim), (b) acceptable-worth-pinning (two-chain fixture), (c) acceptable (delete AnchoredHeights to reduce misuse). PRICES TREND 7→4→2(text-only). WAVE 14 = the two WARN texts + log assertions + optionally (a)'s report correction, (b)'s two-chain fixture, (c)'s deletion.
- HEALTH ROUND 11 PENDING on wave 13 (target 5c6af36 vs 872137d). NOMINATED for its attack list by wave 13 itself: no_progress has the SAME clock defect K2 fixed (now.Sub(p.UpdatedAt) daemon-vs-DB) — verdictClock type exists, ~40 call sites; caught-up-red disclosure now ⌈W/R⌉ windows under slow endpoints; parallel-wave hazard: wave 12's mutation harness wiped wave 13's uncommitted derive.go twice (rule: mutation harnesses restore from in-memory backups ONLY, never git checkout — wave 6's rule, violated once since).
- MERGED-HEAD VERIFICATION still owed by controller before ANY closure claim: full suite + -race at the final tip, both DBs quiet. Then: prices wave 14 → round 11P; health round 11 → possible tiny wave; two approvals ⇒ TASK 8 CLOSES under D-006 (decode d8c462b, derive 3b864ac, runner d1e7d54, prices+health pending). Then Task 9 (backfill; R-001 probe first — see r001_input in the ledger above), Task 10, P2 exit review.
- Round 11H (job review-ms18i530-5xkqba, session 019f9c73-e466-7811-b911-b560b1a8cbee, target 5c6af36 vs 872137d): NO-SHIP, 1 high + 2 medium, ALL ACCEPTED. [high] ROTATION DEADLOCK ON A STOPPED ASKER (main.go:1166-1200): admitRefresh records every scope in refreshAsked but a new window resets only refreshSpent; a scope recorded while the budget was exhausted that then STOPS asking (exact/memo/near-head arm now answers it) is never served and never expires, so every other scope returns false forever — contradicts the stopped-askers-drop-out claim, invalidates the ceil(W/R) bound, catch-up red indefinitely; the fairness test keeps all nine cursors asking so it cannot see it. FIX: track request epochs/expire inactive askers OR remove pending membership when another path satisfies a scope; budget-one tests (alternating callers; frequent+stopped caller; catch-up satisfied via near-head arm). Wave 13's mechanism history: naive budget STARVED → due-queue would DEADLOCK (rejected) → shipped rotation ALSO deadlocks, one layer up. [medium] no_progress daemon-vs-DB clock (main.go:2217, the nominated item confirmed): use the DB verdict clock for cursor AND sweep comparisons, degrade to progress_unmeasured on read failure; rollback tests crossing 15min for CursorProgress.UpdatedAt and an open generation; material case = snapshotter quiet stale-refusals (no step error, collateral_unusable inside its wider cadence). [medium] pass-start DB instant reused across tens of seconds of slow sequential reads under-ages cursors at publication (~36s fail-open at the 10-min boundary with the 9x4s harness; Store.Now round-trip adds bias): advance the trusted instant by MONOTONIC elapsed through the pass or take a completion-time reading before publishing; boundary tests just inside/outside 10min with slow fetches. Confirmed clean: 00008 byte-identical, rewind untouched. HEALTH TREND 2→4→2→3. Wave 15 = these three.
- Wave 14 LANDED (4e44cc6/eafb17f/e06f40f/82dc7ec/b8e57fa, pushed): both round-10P text mediums fixed as COMPOSITION rules (repair arms pass evidence; WARN + justification built AFTER the store returns from the returned boundary; floorDisposition partitions none-offered/admitted/below-target/clamped; the overclaim fixed at all five sites). Classification WARN: unanchored = no SURVIVING anchor linked; split rowsUnanchoredBindingPruned vs NeverBound; hash retention claimed only for rowsAnchored ("clause 2 protects from now on, cannot restore already-pruned"). Residuals: (a) index claim corrected in report AND doc — inner lookup served by 00005's price_poll_anchors_scan_idx, measured live; (b) TestProvenanceReadsAreScopedToTheirOwnChain (schema cannot enforce: 00005 keys anchors (engine,block_number), chain_id outside key); (c) AnchoredHeights deleted + absence test. 563/657/0/0 from archive export (own baseline 561/655 at 2bf5442); -race prices+store in container; 9/9 mutations, M7 killed ONLY by the new fixture (wave-12 prediction confirmed), M8 found none-offered untested (82dc7ec), M4 applier asserts exactly-one-occurrence so no unverified SURVIVED possible. Round 11P dispatch = expected CLOSING round for prices. Wave 15 (health: rotation liveness, no_progress clock, pass-start staleness) in flight.
- Round 11P (job review-ms1adscg-9xmmzh, target da11403 vs 872137d): NEEDS-ATTENTION, 4 MEDIUM, ZERO behavioral — all text/test/process. CLOSED: erratum+index doc (item 3), AnchoredHeights deletion (5). PARTIAL: (1) rejected-floor overclaim SURVIVES at poller.go:885-886 (floorOutcome doc) and 1106-1110 (verifyFloor) — "everything at or below the verified floor keeps validity" is false per wave 14's own regression (offered 5000 clamped to 4999, row at 5000 marked); rewrite both to distinguish OFFERED floor from RETURNED boundary. (2) blanket "provenance retained forever / offline reconciliation stays possible" survives at poller.go:1768-1773 (neutralize WARN — contradicts the store's corrected split when rowsAnchored=0), poller.go:1943 backlog WARN, prices.go:129-142, store/prices.go:1296-1300; limit generic text to row/value retention, hash-based reconciliation only when a surviving anchor exists; ADD a Poller-level regression for the pruned-binding case. (3) floorDisposition's below-target arm has NO test (admitted/clamped/none-offered pinned) — dedicated test offered<returned boundary + mutation flipping the arm to admitted. (4) the exactly-once mutation APPLIER is prose-only — commit/archive the applier + mutation transcript tied to the tested SHA, rerun M4/M7/M8 + the new below-target mutation with the assertion visible. Wave 16 = these four. PRICES TREND 7→4→2→4-text-only.
- Wave 15 LANDED (7b8c9fa/868c84f/9552a93/417e505, pushed): all three round-11H findings. [high] rotation liveness: refreshAsked maps scope→round of most recent request; INVARIANT stated on admitRefresh ("only a scope that asked in the current round or the one before it can block the rotation"); expiry clears the VETO not the PLACE (the tidier drop-from-both rule passed the finding's tests then starved worker 6 to zero — being served is what makes a scope go quiet, so forgiving its turn while quiet hands it a fresh turn every window); counts ROUNDS not time so M15's stopped-clock cannot freeze liveness; counterfactual measured (revert: eth:slow-a 0 refreshes/6 windows; fixed: 5). [medium] no_progress clock: ONE trusted clock read at the top of applyProgressConditions measures every comparison; stalenessJudge LOSES its clock field entirely (structural single-authority). [medium] pass-start staleness: passClock carries the DB instant forward by monotonic elapsed, anchored BEFORE the read so the round trip charges the caller (fail-closed); boundary measured 9m59s+5s→RED (was GREEN), 9m54s+5s→GREEN both. KEY CHANGE disclosed: clock failure now progress_unmeasured (not staleness_unmeasured) — the clock is the whole pass's authority; a wave-13 reviewed test was edited to match; a no-clock round pays for zero durable/header reads. 575/672/0/0 at 9552a93 (parent da11403 baseline 563/657, +12/+15, PASS-list diffed both directions, nothing deleted); -race 9/9; 16 mutations/35 runs/4 passes, all assertion-killed, zero compiler kills. derive.go NOT touched (Store.Now sufficed — no 8th modification). Round 12H = expected health CLOSING round.
- **HEALTH UNIT CLOSED under D-006** (round 12H, job review-ms1b28qd-8ie1nq, session 019f9cb5-72b7-7362-87f8-08dfe8cc4472, target ff42a80 vs 5c6af36): **APPROVE — no material HEALTH-unit issue survives.** All three round-11 findings confirmed closed: rotation expiry preserves served order + restart resets state + uint64 wrap needs 2^64 rounds; ALL gating comparisons use one passClock; no in-repo consumer depends on the renamed clock-failure key; the catch-up test proves the near-head transition by asserting the worker STOPS ASKING; clock/key mutations use independently movable fixtures; +12 top-level/+3 subtests reconciled from source, no deletions. Non-blocking: wave-15 report opening says 19 mutation runs vs the detailed 35 — typo, correct in a later doc pass. HEALTH TREND FINAL: 2→4→2→3→0. Waves 9/11/13/15, rounds 9H/10H/11H/12H. Remaining for TASK 8: wave 16 (prices text/process, in flight) → prices closing round → CONTROLLER MERGED-HEAD VERIFICATION (full suite + -race, quiet DBs, committed-blob gofmt incl. the derive.go item wave 10 flagged) → close Task 8, then Task 9 (R-001 probe first).
- Wave 16 LANDED (0302352/4d8edc7/e3fe3e8/a48c873/ab2a603, pushed): all four round-11P mediums. Class sweep for finding 1 found a WORSE instance than either citation (NeutralizeUnverifiablePrices' own doc claimed verifiedFloor honoured "exactly as RewindPrices honours it" — false since the clamp, 14 lines above the AUTHORITATIVE paragraph); retention text fixed at 4 cited + 3 more sites, clause 2 stated as a FORWARD guarantee; TestPollerNeutralizationPromisesNoOfflineRecoveryForAPrunedBinding (rowsAnchored=0 population, asserts both WARNs and retired sentences' ABSENCE); below-target test with a row IN THE GAP (offer 4900, boundary 5000, row 4950); REVIEWABLE APPLIER committed (.superpowers/sdd/wave16-mutations/: mutate.py with exactly-one-occurrence in apply_edit, mutations.json, two transcripts tied to SHAs; .gitignore class rule makes <wave>-mutations/ trackable). 7/7 mutants killed, M10/M11/M12 each by EXACTLY ONE (new) test, M7 reconfirmed through the committed applier. 577/674/0/0 at e3fe3e8 vs measured 575/672 baseline (+2 = the two new tests; health's 3 interleaved commits verified markdown-only). HONEST FLAGS for the closing round: finding 1 is comment text and CANNOT have a mutation (stated in mutations.json meta); the poller cannot report the anchored/unanchored split without a signature change (zero-behavioral round — text scoped to worst population, points at the store WARN; a behavioural wave if Codex demands it). PRICES = the last open gate on Task 8.
- **TASK 8 CLOSED UNDER D-006** (2026-07-26). PRICES UNIT APPROVED at round 14 (job review-ms1dplu6-8gon94, session 019f9cf9-56f3-75d2-b078-9589c9cd61a5, target fb28061 vs cd2522b): "APPROVE ... The repaired comment matches the code: verifyFloor offers a floor, while NeutralizeUnverifiablePrices computes/clamps the effective boundary, marks valid rows only above it, and returns that authoritative boundary. Final production-comment sweeps found no misleading offered-floor prose or unscoped hash-retention claims. No material findings." Rounds 13/14 survivors were controller-applied comment-only fixes (cd2522b, fb28061), both class sweeps controller-re-run. FULL SENIOR-APPROVED STACK: decode d8c462b, derive 3b864ac, runner+snapshotter+daemon d1e7d54, HEALTH ff42a80 (round 12H), PRICES fb28061 (round 14). Task 8 totals: 16 waves, 14 Codex rounds, two-unit decomposition after round 5, three governing decisions (D-010→D-011→D-012), 2 ratified addenda, migrations 00005-00008, final merged-HEAD controller verification 577 top-level / 674 incl. subtests / 0 FAIL / 0 SKIP all nine packages. REMAINING IN W1: Task 9 (full backfill + cmd/reconcile + invariant scans — START WITH THE R-001 LIVE PROBE, decision numbers in the r001_input ledger entry; W1's acceptance evidence comes from Task 9) and Task 10 (anvil-fork replay + phase gate), then receipts/stamping and the P2 exit whole-branch review per D-006 clause 2.
- R-001 PROBE RUN (2026-07-26T06:30-06:35Z; artifacts .superpowers/sdd/r001-probe/{probe.py, r001-results.json, transcript-e969fa0.md}; production-identical 6-call windows per walker.go, axes varied/fixed stated in the transcript): OBSERVED -- mainnet.optimism.io 5 req/s sustained clean (429 onset at 10 rps; OP total 15,612 calls => ~52 min, OP is a non-problem). publicnode 403s EVERY deep-history getLogs (277/277, capability refusal, headers clean to 20 rps) so the configured ETH pair degenerates to unkeyed drpc + failover churn. Unkeyed drpc NOT dependable: optimism.drpc collapsed at 2 rps (48/90 429); eth.drpc 429 at 2/5 rps, one clean 45s phase at 10, 429 again at 20 -- ONE shared provider budget across both configured drpc endpoints. Heads OP 154,724,338 / ETH 25,615,191 => 25,916 windows / 155,496 calls, ETH = 90% of the work (139,884). Ceilings: 10k-block getLogs served by every logs-serving endpoint, 20k rejected everywhere; walker.Step retries a failed window at the SAME span (no adaptive split) so widening risks a permanent stall at a logs-cap range -- window lever deferred. Merge lever (5 identical-startBlock aave streams -> 1): ETH 139,884 -> 80,004 calls (-43%); from-scratch backfill = no cursor migration, but stream naming keys cursors so it needs a scoped naming check first. CONCLUSION: backfill INFEASIBLE on the configured ETH endpoints; one keyed free-tier ETH endpoint at ~10 req/s => ETH ~3.9h as-is (~2.2h merged) + OP ~52 min in parallel on the existing endpoint; paid tier unnecessary for a one-shot backfill. -> R-001 OWNER DECISION presented.
- R-001 DECIDED (owner, 2026-07-26): KEYED FREE TIER + skip the merge lever (both AskUserQuestion answers = recommended options). Path there had one dead end the probe-first discipline caught in seconds: owner's Alchemy free key VERIFIED WORKING for chainId/heads on both networks but Alchemy's CURRENT free tier caps eth_getLogs at a 10-BLOCK range (HTTP 400 code -32600, verbatim in session) -- unusable for backfill (~5M getLogs), tier POLICY != infra capability. Owner then created a dRPC free key; OBSERVED through it (production shapes, Go-http-client/1.1 UA -- lb.drpc.org's Cloudflare 403s naked python UA, error 1010, geth's UA passes): 2000- AND 10000-block deep-history getLogs served on BOTH chains; ETH sustained 10 req/s 100.0% clean (450/450, getLogs p95 0.54s), 20 req/s 98.7% (8x429/600). Final endpoint posture (.env local-only, key never committed): ETH = keyed drpc primary + publicnode second (its headers/eth_call clean to 20 rps; getLogs 403 only ever costs failover churn), OP = mainnet.optimism.io primary (observed 5 rps clean) + keyed drpc second; two-provider diversity preserved on both chains for poller agreement. RUNNER PACING check (main.go round loop): inner loop runs HOT while any worker advances, but all 10 walkers step SEQUENTIALLY in one goroutine (stepsPerRound=5, 6 calls/window) => single request in flight, ~4-6 req/s aggregate, zero 429 pressure, latency-bound backfill ETA ~12-14h wall. DB reset for from-scratch: pre-drop residue recorded (raw_logs 4, ingest_cursors 1, derive_cursors 3, position_events 1, position_balances 1, prices 2, reorg_epochs 3 -- Task 8 live-test leftovers), DROP/CREATE DATABASE, 0 tables; daemon migrates 00001-00008 on start (main.go:2155). BACKFILL LAUNCH follows in this session.
- P0 DEFECT FOUND BY FIRST REAL-CHAIN CONTACT (Task 9 backfill launch, 2026-07-26): the price poller can NEVER anchor a round on a real chain. Mechanism, live-proven (eth_call of empty tryBlockAndAggregate on mainnet at block 25,615,440 returned blockHash = 0x000...0): Multicall3.tryBlockAndAggregate computes blockhash(block.number), and EVM BLOCKHASH serves only the 256 blocks BEFORE the executing one -- the field is deterministically zero on every real chain (known Multicall3 gotcha). prices.go's zero-hash guard (:511-512, correct paranoia) therefore refuses EVERY round on BOTH chains ('block hash at N is zero -- refusing to anchor', observed at 1/min cadence, 0 rows in price_poll_anchors). WHY 16 WAVES AND 14 CODEX ROUNDS MISSED IT: every fake multicall in the test fleet returns a NONZERO blockHash -- a value physically impossible on real EVM; the fixture-realism lesson in its sharpest form yet. The snapshotter is UNAFFECTED (its own unpackMulticallResult ignores the hash field; sweep generation=1 completed live). The anchor design's atomic premise (hash + observations from one execution context) does not exist on real EVM; the honest replacement is the walker's reviewed coherent-window pattern: pin N + HeaderHash(N) both sides of a BLOCK-PINNED eth_call, all reads endpoint-coherent, anchor (N, hashBefore), multicall hash field ignored with the WHY stated. Fix wave = task-9-wave1-brief.md (internal/prices + additive chain pinned-call; D-006 prices-unit review). Backfill walkers/derivation/feed-deriver UNAFFECTED and running throughout.
- ROUND 1 (task 9, prices unit; job review-ms1iuebv-8b2i2j, session 019f9d7c-e7a2-7530-ba62-99e7dcc41ace, target 84ca9d2 vs 13ea0da): NEEDS-ATTENTION, NO-SHIP -- 1 high, 1 medium, both ACCEPTED (archive task-9-codex-round1.md). [high] coherence discards never advance routing state => an endpoint serving heads but failing pinned calls starves the poller FOREVER with a healthy peer idle -- the fail-closed-not-fail-forever class a FOURTH time (waves 13/15 precedents). [medium] the anchor contract overclaims: an EndpointToken names a URL not a BACKEND; one lb hostname is many nodes, so fork-A headers + fork-B number-pinned call at the same height pass every wave-1 guard -- fabrication through the side door; plus stale Multicall3-hash prose at prices.go:99-104. ADJUDICATION UPGRADE: controller live-tested Codex's EIP-1898 strengthening on ALL FOUR reachable endpoints (drpc-keyed eth+op, op foundation, publicnode eth): hash-pinned eth_call executed exactly at the pinned block everywhere, fabricated hash REJECTED ('block not found') everywhere -- negative controls included. Wave 2 therefore MANDATES the hash pin (bind execution to hashBefore itself) instead of the documentation fallback; gold standard = make the guarantee true, not shrink the claim. Wave 2 brief: task-9-wave2-brief.md (F1 routing advance on every discard + multi-cadence recovery regression; F2 EIP-1898 pin + same-height split-fork regression + contract rewrite + prose class sweep). Backfill unaffected throughout (429 noise absorbed; ~2k blocks/s).
- ROUND 2 (task 9, prices unit; job review-ms1kz3ny-wa0wgd, session 019f9db3-800d-7211-905b-615b3bacb8a0, target 115e4d2 vs cad9d76): NEEDS-ATTENTION, NO-SHIP -- 2 high, both ACCEPTED (archive task-9-codex-round2.md). CONFIRMED SOUND: EIP-1898 pin end-to-end, W2M1s ALONE claim, contract rewrite. [high 1] the mid-round-reorg discard arm (deliberately excluded from routing advance in wave 2) starves forever under a STABLE same-token backend split -- the false-attribution justification collapses because caller-scoped exploration attributes nothing. [high 2] routing advance gated on isBlockNotFoundErr recognizing the FINAL error -- a trailing transport failure masks the class; malformed envelopes and closing-header failures share the shape; fail-closed became fail-forever for availability. CONTROLLER RULING (the per-arm approach missed twice; close the CLASS): 'Landing is the only outcome that keeps the starting point' -- once a serving endpoint is resolved, EVERY non-landing outcome advances the caller-scoped exploration start; classification decides ERROR POSTURE only; shared hint never written. Wave 3 = one structural seam (future arms get the advance for free), regressions incl. stable split + mixed rejection/transport + both-endpoints-half-broken liveness, W2M5 REVERSED. task-9-wave3-brief.md dispatched. Prices trend this task: 1H1M -> 2H (narrowing: core confirmed, liveness class remains).
- ROUND 3 + OP INCIDENT (2026-07-26, same hour). ROUND 3 (job review-ms1mzexw-zmr815, session 019f9de6-fc29-79f0-9b5c-3c35ebd87538, target 1b89fb5 vs 8bd1f29): NEEDS-ATTENTION, NO-SHIP -- 1 medium, ACCEPTED (archive task-9-codex-round3.md). SEAM CONFIRMED STRUCTURALLY SOUND. Residual: last-error-wins posture classification is failover-ORDER-dependent; mixed persistent outage alternates discard/error, and the daemon reads nil discards as success -- backoff streak resets, step_error flickers, 10-min cap never approached. Adjudication: 'one time authority per verdict' transposed -- one CLASSIFICATION authority per round outcome, computed from AGGREGATE per-attempt results (discard ONLY when every failed attempt is a recognized pin rejection; any transport/unknown involvement keeps error posture; routing advance unconditional either way). Wave 4 brief dispatched; additive chain-layer outcome propagation SANCTIONED. P0-fix trend: 1H1M -> 2H -> 1M (core + seam both confirmed). OP INCIDENT: op:debt-manager wedged 2.5h+ at 150,103,227 -- 19 IDENTICAL 'log at window tip does not match anchored tip hash' failures; foundation proved internally consistent at the tip block (0x3d9573...fe23, header==logs), drpc-OP implicated in every consistent explanation of a deterministic mismatch (cross-provider windows: headers one provider, logs another after mid-window failover); direct drpc-OP/publicnode-OP witnesses unavailable during diagnosis (429/403). REMEDIATION: SOLVENT_RPC_OP = foundation ONLY (.env), daemon restarted on the PRE-WAVE binary (governance-clean; cursors durable; fresh backoff). OPEN WORK RECORDED, deliberately out of wave-4 scope: walker content-validation failures never rotate endpoints -- the ingest-layer sibling of the poller liveness class; needs its own brief + review after backfill completes. Monitor watchdog caught the slowdown; per-stream cursor freshness (not aggregate remaining) is the metric that exposed it -- lesson noted.
- P0 ROOT CAUSE #2, FORENSICALLY PROVEN (2026-07-26, same night): go-ethereum v1.13.0 CANNOT HASH 2026 OP-MAINNET HEADERS. Chain of evidence (probes archived in r001-probe/): (1) OP walker wedged on 'log at window tip does not match anchored tip hash' -- 19+5 identical failures across TWO configs (dual-provider AND foundation-only, falsifying the cross-provider theory); (2) the exact walker range getLogs [150,103,228..150,105,227] returns 397 logs with CANONICAL tip hashes on BOTH providers -- provider data was never wrong; (3) hashcheck.go (committed): ethclient.HeaderByNumber's locally recomputed h.Hash() = 0x70f6bea2... vs provider-reported 0x3d957321... for block 150,105,227 -- MATCH FALSE. v1.13.0 (Sept 2023) predates the 2025/2026 OP header shape; keccak(RLP(known fields)) is silently non-canonical for every modern OP block. WHY 582K BLOCKS WALKED FINE: computed hashes are SELF-consistent (cursor stored computed, reorg-check compares computed-vs-computed); the tip-log check is the ONLY computed-vs-REPORTED comparison and only fires when a DM log lands exactly on a window tip (397 logs/582k blocks). The fail-closed validation caught a systemic silent defect. BLAST RADIUS: OP ingest wedged (ETH empirically fine -- dense post-Cancun ranges walked with zero mismatches, v1.13.0 hashes ETH correctly); OP ingest_cursors.last_block_hash values are computed-garbage (raw_logs rows are canonical -- they come from the provider); THE WAVE-1/2/3 POLLER pins EIP-1898 by the SAME recomputed hash -- on OP it would present a hash no node recognizes, discard forever: the P0 anchor fix is incomplete without this. THE FIX (wave 5, after wave 4 lands -- same files, no concurrent writers): chain layer returns the provider-REPORTED hash for headers, never local recomputation -- trust-posture-honest (L2 consensus is unverifiable locally; recomputation was a false guarantee that broke the cross-checks that reported hashes would have satisfied); all walker protections stay meaningful (they compare provider-reported values against each other). Post-fix: OP stream state reset (cursors + chain-10 raw/derived rows) and re-walk (~1h) -- stored OP cursor hashes are unusable for reorg checks against reported hashes. OP walker left wedged meanwhile (clean bounded backoff, no damage); ETH backfill unaffected and continuing.
- ROUND 4 (job review-ms1p0c9t-t8kg6j, session 019f9e1a-e7f7-7e41-87f5-5544e708899d, target 0a46218 vs d25b1e8): **APPROVE -- SHIP, ZERO FINDINGS.** 'Total-failure aggregates are complete, partial cancellations fail closed, daemon-replica backoff/reset behavior matches production, routing remains unconditional, and all eight mutation edits are uniquely applicable with credible kill evidence.' Caveat disclosed by Codex: its read-only sandbox denied go's temp build dir, so suite/mutation claims were evaluated structurally -- controller's own executions (build/vet/suites green at f2bdae7 tip, committed transcripts) carry the execution evidence. PRICES-UNIT ROUTING/CLASSIFICATION CYCLE CLOSED: waves 1-4, rounds 1-4, trend 1H1M -> 2H -> 1M -> 0. REMAINING before daemon restart: wave 5 (provider-reported header hashes -- the OP unblocking fix; brief 0808f10, dispatching now), round 5, merged-HEAD verification, then controller restart + OP stream reset + re-walk + LIVE anchor proof on both chains (the evidence the P0 saga closes on).
- ROUND 5 (job review-ms1qlj40-33f6dj, session 019f9e43-9c7f-7af1-8d1a-13cdc01cf028, target ce21cd3 vs 1e3941c): NEEDS-ATTENTION, NO-SHIP -- 1 high, 1 medium, both ACCEPTED (archive task-9-codex-round5.md). The reported-hash PRINCIPLE held; what fell was decoder strictness: [high] validateReportedHeader never requires Number == requested height (a latest-for-numeric proxy passes -- false-green HeaderTime freshness, spurious walker mass-rewind instead of rotation; fakes always answer correctly so the suite was structurally blind); [medium] non-pointer Time decodes omitted timestamp as ZERO and passes (old types.Header decoder REJECTED it) -- epoch-aged heads stop failover at a malformed primary. CLASS: swapping a strict decoder for a minimal one silently DROPPED two protocol gates; trusting reported fields must pair with verifying the response ANSWERS THE QUESTION ASKED (right height, required fields) -- violation = failed attempt = rotation, the zero-hash posture uniformly applied. Missing fixture layer identified: hermetic RAW-JSON adapter tests below the fake seam. Wave 6 brief dispatched (exact-height equality, presence-tracked required fields, null-vs-violation discrimination, rotation regressions). Chain-reopen trend: 1H1M; expected closing next round.
- ROUND 6 (job review-ms1s7mk3-fffqvi, session 019f9e6c-fa9b-7eb3-b493-8e8428b6a60a, target 15a4e11 vs 8352af0): NEEDS-ATTENTION, NO-SHIP -- 1 medium, ACCEPTED (archive task-9-codex-round6.md). v1.13.0's hexutil decoders accept EMPTY JSON STRINGS as zero: 'timestamp':'' passes the new presence gate as non-nil epoch, 'number':'' passes HeadFrom as height 0 -- the F2 failover-stopping class one decoder-layer deeper (the raw-JSON matrix only covered 0xnope). Adjudication: 'the response must answer the question asked' must hold at the BYTES level -- non-canonical quantity = protocol violation regardless of decoder leniency. Wave 7 (surgical): strict quantity wrappers rejecting empty/non-canonical before conversion + real-Dial rotation regressions where ''-quantities fail the attempt and the healthy secondary LANDS. Chain-reopen trend 1H1M -> 1M -> 1M, strictly narrowing (semantics -> representation); closing expected next round. Backfill: ETH ~13M remaining, pace in the overnight slow band, errors absorbed.
- ROUND 7 (job review-ms1toiek-df1jxc, session 019f9e92-a078-7d13-863c-4876e9d72fc9, target 93b7658 vs e3a2150): NEEDS-ATTENTION, NO-SHIP -- 2 medium, both ACCEPTED (archive task-9-codex-round7.md). The wave-7 gate itself SOUND; the findings are the SAME empty-quantity class in paths the wave-7 brief scoped out: eth_blockNumber ('' -> height 0 -> success -> walker starves below confirmations, no rotation) and eth_getLogs log metadata (logIndex '' -> present zero -> persisted as raw-log identity -- source-of-truth corruption risk). ADJUDICATION OWNS THE SCOPE-NARROWING AS A CONTROLLER BRIEFING ERROR: the rule demanded package scope, the brief applied it to two fields. Wave 8 = the package-wide sweep with a mandatory gated-or-justified TABLE for every wire-decoded field (BlockNumber, log metadata, chainId, tx paths, opaque call envelopes), both fixes + real-Dial rotation regressions. Trend 1H1M -> 1M -> 1M -> 2M: numerically wider, same single class propagating outward; the sweep table makes round 8 decidable. Backfill: ETH ~15.0M remaining (~740/s overnight band).
- ROUND 8 (job review-ms1vkhfp-nmd7zj, session 019f9ec3-03cf-7ab1-9897-1f96cd219938, target f0a5560 vs f9347f7): NEEDS-ATTENTION, NO-SHIP -- 1 medium, single finding adjacent to the newest gate; ACCEPTED (archive task-9-codex-round8.md). The 26-row sweep, both round-7 fixes, and the null-window discrimination SURVIVED. Finding: the tx answered-identity gate compared the response's independently-supplied hash FIELD -- which types.Transaction.UnmarshalJSON IGNORES -- so the signed body was never bound; a primary echoing the asked hash with a substituted body passes, and on the migration path substituted calldata with the same seed count silently corrupts borrower/debt genesis state. ADJUDICATION DRAWS THE RECOMPUTATION LINE: banned where the type model is incomplete (headers, wave-5 law), REQUIRED where complete and it is the only authentication (signed tx bodies -- tx.Hash() over the decoded body; undecodable types fail loudly into rotation). Wave 9 (surgical): decoded-hash comparison + the INVERSE regression (lying hash field, wrong body, secondary lands) + its mutation. Trend 1H1M -> 1M -> 1M -> 2M -> 1M; closing expected next round. Backfill: ETH ~13.4M remaining, slower band (~410-720/s), errors absorbed.
- DRPC ARCHIVE ROUTING CONTRACTION (2026-07-26 ~09:35-14:30 local, ongoing at entry time): dRPC free-tier getLogs below ~25.3M on ETH now returns 400 code 12 'Can't route your request to suitable provider' -- the free routing pool stopped serving archive-depth getLogs after ~7h/~90k requests (capacity cycle or soft-quota deprioritization; range-size NOT the trigger). With publicnode 403ing archive, 'all rpc endpoints failed' wedges all 7 unfinished ETH streams (feeds stuck first ~09:35-10:03 at deeper blocks; atokens/aave followed ~14:15 at 25.0-25.3M). TWO STREAMS FINISHED FIRST: eth:atoken-frax and eth:atoken-pyusd are AT HEAD (25,617,551) and tracking. Remaining ETH work: ~9.4M blocks / ~4.7k windows (aave 0.3M, atoken-usdc 0.6M, atoken-weeth 0.38M, feed-usdc 0.9M, feed-pyusd 1.4M, feed-frax 2.7M, feed-weeth 3.1M). POSTURE: fail-slow is working as designed (bounded ~12min backoff retries, zero damage, self-recovery when routing returns); no autonomous provider roulette (the Alchemy lesson); the $10 dRPC Growth escalation is an OWNER decision prepared for the morning report if routing has not recovered. OP (4.6M) is INDEPENDENT of this -- it runs on foundation once the hash-fix stack restarts. Wave-9/round-9 cycle continues in parallel.
- ROUND 9 (job review-ms1wu9wi-epcbuj, thread 019f9ee3-9da4-7211-bc00-90663bc7bf89, target 990c9f0 vs 2c259e6): **APPROVE -- SHIP, ZERO FINDINGS.** 'The recomputed hash gate covers every transaction type v1.13.0 decodes, unsupported types fail into rotation, the empty-calldata property remains pinned, and the inverse regression plus W9M1 evidence close the round-8 defect.' (Verdict retrieved DIRECTLY from job state JSON after the reviewer agent hit the mid-poll stall pattern -- nudged via SendMessage + controller fallback watcher on the exact state file; the mechanics held.) **CHAIN-LAYER REOPEN CLOSED**: waves 5-9, rounds 5-9, trend 1H1M -> 1M -> 1M -> 2M -> 1M -> 0. Combined with round 4's approve (waves 1-4: EIP-1898 anchor acquisition, routing seam, aggregate classification): THE ENTIRE P0 STACK IS SENIOR-APPROVED at 990c9f0/83c6e6b. Controller restart sequence begins: merged-HEAD full-suite verification, binary rebuild, daemon stop, OP stream reset (computed-garbage cursor hashes make OP state unusable for reported-hash reorg checks; ETH cursors SAFE -- v1.13.0 hashes ETH correctly so stored==reported), restart, OP re-walk from 149,521,228 on foundation, LIVE ANCHOR PROOF on both chains.
- **P0 SAGA CLOSED ON LIVE EVIDENCE** (2026-07-26 ~08:02 local, daemon restarted on the senior-approved stack at 9fa371b, binary rebuilt, merged-HEAD full suite 633/0/0 gate-ON): **BOTH POLLERS ANCHORED THEIR FIRST-EVER REAL-CHAIN ROUNDS** -- price_poll_anchors: chain 1 @ 25,617,732 and chain 10 @ 154,739,673; ALL polled price rows bound (20/20 OP, 1/1 ETH anchor_block non-null). OP reset executed pre-restart in one transaction (counts: raw_logs 10,352 / position_events 12,833 / position_balances 29,475 / rate_indexes 4,969 / snapshots 44,744 / sweep rows 7,387 / cursors+epochs) and op:debt-manager is re-walking from 149,521,228 on foundation (+20k blocks in minutes). SELF-HEALING BONUS DISCOVERY: on restart the reported-hash reorg check found stored ETH cursor hashes ABOVE ~22,543,769 are ALSO old-binary computed garbage -- v1.13.0 mis-hashes post-~22.54M ETH headers too (consistent with a 2025 header-shape fork; exact boundary not pinpointed; the ETH walk never tripped because no log ever landed exactly on a window tip in that span -- the same statistical blindness as OP, now closed by the reported-hash law). feed-weeth's own check fired first ('reorg detected, rewound to verified ancestor' 22,543,892 -> 22,543,769, verified=true); derive targets epoch-lowered; the D-012 neutralization pass ran live with its reviewed WARNs verbatim (bootstrap, rowsNeutralized=0). COST: all 9 ETH streams re-ingest from ~22.54M (~21M block-walks, ~63k calls, ~8-16h at observed free-tier rates) -- the honest price of 7h walked on a broken hash authority, paid automatically by the machinery built for exactly this. Wave-9 reviewer postscript: its 'critical anomaly' report was the CONTROLLER's own worktree cleanup + ledger commits observed from outside -- no parallel automation; its independently-extracted verdict matched the controller's byte-for-byte, and its source spot-checks (rawTxJSONFor in diff, the gate at chain.go:752) strengthen the anti-decoy record. TASK-9 P0 CYCLE COMPLETE: 9 waves, 9 rounds, 2 root causes (Multicall3 BLOCKHASH semantics; v1.13.0 header-shape obsolescence on BOTH chains), 1 principle ('the response must answer the question asked') applied from anchor acquisition to byte-level decode.
- WAVE 10 DESIGN COMPLETE (12-agent workflow wf_e0bd62de: 4 readers / 3 designs / judge / 3 adversarial lenses / synthesis; 1.16M tokens): cmd/reconcile + invariant scans designed and adversarially pre-verified; every BLOCKING finding resolved by design change. HEADLINE CATCH: TEST_DATABASE_URL and SOLVENT_DATABASE_URL point at the SAME database and live-db tests Migrate+TRUNCATE 12 tables incl. raw_logs -- running W1's own acceptance command list as-is would have DESTROYED the backfill; design mandates a physical DB split (docker-compose solvent_test) + DSN-identity tripwire (exit 2) + SOLVENT_RECON_DATABASE_URL gating for read-only evidence tests. CONTROLLER GATE RUNS (amend the design's Phase 0): deep pinned-state eth_call FAILS on both configured ETH endpoints (publicnode 'historical state unavailable' -32000; drpc-keyed 500/408 archive volatility) but ALCHEMY FREE SERVES DEEP ARCHIVE STATE (EIP-1898 pinned balanceOf OK at 25,584,990 / 25,593,800 / 20,779,893) => reconcile gets its OWN RPC config (SOLVENT_RECON_RPC_*; owner's Alchemy key qualifies at $0, getLogs cap irrelevant -- reconcile makes none). W1 allowed_paths AMENDED (+docker-compose.yml, +Makefile, +.env.example) for the DB-split deliverable -- disclosed here, staged with the brief. Wave 10 brief = .superpowers/sdd/task-9-wave10-brief.md (controller preamble + full synthesis, 41k chars). ALSO: three standing expert personas created at owner request (~/.claude/agents/): chain-truth (TrueBlocks/Graph/IndexSupply/Dune/Flashbots/geth-internals + session canon), risk-quant (Gauntlet/Chaos/BlockAnalitica/Aave-math/postmortem forensics), oracle-sentinel (Chainlink/RedStone/Pyth/Chronicle/manipulation forensics + D-012 constitution).
- RISK-QUANT CONSULT (first ruling from the new standing persona; report consult-riskquant-wave10.md, committed 24ad989): NUMBERS DO NOT HOLD -- 1 BLOCKING + 7 findings on the wave-10 design, delivered BEFORE implementation returned. F1 (blocking): PHANTOM-DEBT BLINDNESS -- the sampling universe is position_events, so a never-derived borrower is structurally unselectable; zero-net probes cover derived-to-zero, not never-derived; a Safe with real chain debt and no rows would land in a PASS receipt. Fix (verified against DebtManagerCore.sol: all four mutation paths move totalNormalizedBorrowingAmount and the per-user map in lockstep, incl. the silent residue 1-wei at :551-552): AGGREGATE COMPLETENESS WELDS join the gated set -- DM Sum(derived net normalized <= P) == borrowTokenConfig.totalNormalizedBorrowingAmount@pinHash per token (zero bound); Aave Sum(scaled) == scaledTotalSupply()@pinHash per reserve; weld-over-sampled-only mutant must be killed. F2: the two-sided +/-1-wei residue epsilon was WRONG-PLANE (mechanism is one-directional, surfaces as 1-2 USD-wei) -- replaced by exact classification, epsilon DELETED. F3: Scan-2 predicate diverged from the fold predicate -- named sub-assertion added. F5: two SQL-only scans added (events->raw_logs referential; same-block-IIU coverage). Consult also UPHELD: DM zero tolerance is a theorem (ceil/floor chain verified to contract lines), Aave zero tolerance theorem, index-integrity conditional verified sound. Amendment sent to the in-flight wave-10 agent as BINDING. The persona experiment validated on its first invocation: a generalist design workflow (12 agents) missed what the tuned latent space caught in one pass.
- WAVE 10 LANDED (2d475b9 impl / dc6782b spec+schemaGateOK / 725026e transcript / 889ca18 report; controller: 7b821eb claim RESCOPE to generation 8 -- the gen-7 claim was scope_hash-bound pre-amendment and the gate correctly refused the three new-scope files, agent STOPPED per brief instead of working around; 0b19670 DB-split files committed post-rescope; f95e518 Aave V4 research memo). cmd/reconcile + six evidence scans: 633 -> 726/0/0 (+93, 0 lost, gate ON), 20/20 mutations incl. the amendment's TestComputeDMWeldInputsCoversAllAccounts, -race all 10 packages, committed-blob gofmt clean. Amendment fully absorbed: F1 welds GATED both engines, F2 exact residue classification (epsilon DELETED), F3 wide predicate + named sub-assertions, F5 scans 4-5 gated; F4/F6/F7/F8 carried as named notes. BONUS SIGNAL: the six evidence scans ran read-only against the LIVE mid-backfill DB during verification -- ZERO violations. DB-split verified on both bring-up paths without touching the running container; DSN tripwire fired live (exit 2, brief-verbatim message). 10 deviations disclosed in the report. Controller verification at 0b19670: build/vet green, reconcile+store(solvent_test, 106s live suite)+chain green. ROUND 10 dispatched (target 0b19670 vs bf9db2a). V4 RESEARCH (memo committed): V4 live on ETH mainnet since 2026-03-30; whitelabel ARFC 2026-07-14 pre-Snapshot, NO OP contracts yet -- the Observatory's before-state window is OPEN; 36 events cataloged; supply-side share rate has NO index event => hub-weld mandatory; red flags: hub-level TransferShares bypasses spoke events, Sweep/Reclaim, ether.fi-held upgrade keys.
- ROUND 10 (job review-ms28cdjg-y57vdp, session 019fa00a-70db-7493-b832-bad9ab0d96d7, target 0b19670 vs bf9db2a): NEEDS-ATTENTION, NO-SHIP -- 3 high 2 medium, ALL ACCEPTED (archive task-9-codex-round10.md). CLASS: correctness that depended on OPERATOR DISCIPLINE instead of structure. [H1] Makefile test target neither requires nor validates TEST_DATABASE_URL -- make test could still truncate the live backfill, and unset => skips => vacuous suite-green. [H2] acceptance taints are metadata: -accounts/-collateral-replay 0/-max-head-lag 0 bypass quotas/replay/lag-gate with acceptance=true and pass/exit-0 intact -- taints never reach computeResult. [H3] a failed borrowTokenConfig read silently VANISHES from the F1 weld (universe = derived-plus-readable, not getBorrowTokens-union) -- absent conflated with zero; the risk-quant empty-state requirement enforced one level deeper. [M4] DSN tripwire compares connection-string-shaped identity -- IPv4/IPv6/socket aliases of one cluster fail OPEN; fix = system_identifier + db OID/name, fail closed on unresolvable. [M5] the RR snapshot is HELD ACROSS header RPCs in Phase 1 (xmin retention under a 20-min timeout while the daemon writes) -- commit before any network. Wave 11 = make the safe path the ONLY path (the wave-3 structural turn applied to the harness): shared destructive-test guard calling the F4 identity mechanism, taints structurally non-pass, weld universe authoritative with read-presence first-class, no-network-under-snapshot restructure. Reviewer incident disclosed: first dispatch foregrounded and SIGTERM-wedged its job (output discarded), replacement clean -- the round-1/3/5 wedge class, now documented for review dispatch too. Backfill: 5.8M remaining, zero errors this window.
- ORACLE-SENTINEL CONSULT (B3; report consult-oraclesentinel-b3.md): PROVENANCE HOLDS, blocking list EMPTY -- and B3 is UNCLOSABLE AS WRITTEN: heartbeat/deviation live in the DON's off-chain OCR config (only shadow = opaque mutable ConfigSet blob); 'bytecode verification' cannot exist for these values. ADJUDICATION -- B3 REDEFINED (accepted): (a) proxy->aggregator lineage record at a stated block; (b) consumer-constructor hunt (the weETH evidence class -- a deployed consumer that BET MONEY on the bound) recorded found-or-not; (c) THE CORE: empirical-heartbeat scan over our own backfilled AnswerUpdated history -- RoundId-continuity gate FIRST (ingest holes must not masquerade as oracle silence), gap stats on UpdatedAt deltas, assertions A1 (zero gaps > heartbeat+grace), A2 (stables' p95 >= ~20h -- the distribution moding at 86400 IS the operational heartbeat signature), A3 (weETH as method control), A4 (tail-liveness), A5 (anti-vacuity: min rounds + >=90% span coverage). Zero-RPC SQL, joins the Task 9 scan family (deferring past Task 9 would be deferral without cost justification -- accepted). Exposure asymmetry named: heartbeat-shorter = up-to-25h green death during a depeg (compounds with the disclosed uncapped-stream divergence); heartbeat-longer = self-announcing false reds. KEY QUARANTINE FACT (why B3 was never W1-blocking): thresholds gate ALARMS only -- never row validity, never ApplyPrices, never LatestUsablePrice (which disclaims freshness by design, Pyth school). ESCALATION TRIGGER RECORDED: the moment P3 wires heartbeat-derived bounds into risk reads, an unverified heartbeat becomes BLOCKING -- re-consult at that boundary. F2 (should, open work): no construction-time proxy->aggregator lineage check; re-resolution fires only AFTER staleness => >=25h silent phase-change window on the stables. Scan lands with the evidence wave; derivation-notes provenance-row upgrade is controller pen at closure.
- CHAIN-TRUTH CONSULT (walker rotation class; report consult-chaintruth-walker-rotation.md): CUSTODY HOLDS pre-fix (availability defect, one silent variant -- 'liquidation-risk data can be perfectly custodied and uselessly stale'). CLASS CONFIRMED LIVE post-hash-law, and WORSE than recorded: Failover.do RE-PINS the shared hint onto the endpoint whose getLogs returned the rejected content -- the offender is actively pinned, not merely tolerated. NEW DEFECT FOUND (F2, blocking): the walker's silent discards (tip-changed/cursor-changed mid-fetch return false,nil) hit bo.success() at main.go:666-677 -- backoff RESET every round, no step_error, the round-3 pacing/visibility defect shipped at ingest in SILENT form. SHARED-HINT NUDGE REFUTED from repo history: RotateAwayFrom was deleted in Task 7 wave 6 because a sibling walker's legitimate success re-pins the offender within one round -- per-stream startPref machinery is required (retention-not-reset; the poller's deferred-seam shape verbatim; additive BlockNumberFrom/LogsFrom, NO aggregate propagation needed since routing advances on both postures). GEOMETRY RULING kills the thrash worry: bracketing reads sit at fixed to=head-5, so the hash moves only on a >=5-deep reorg -- discards ARE cross-view disagreement; every discard advances, caught-up keeps the start, rewind counts as landing. HARNESS PRECONDITION: walker_test's single-view fake is structurally incapable of expressing endpoint disagreement (the round-5 law present at ingest) -- per-endpoint fake FIRST, run against the UNCHANGED walker. ADJUDICATION: wave 12 = F1+F2 + harness + R1-R5/R8/R9 + M1-M7 (the consult's recommended cut); F4 rider at implementer discretion with justification; F3 (single-witness Rewind below finality -> corroboration posture) DEFERRED to a proposed decision post-Task-9 -- it transposes D-012 clause-4 posture to ingest and deserves ratified text, not a silent import; F5/F6 disclosed. Wave 12 runs PARALLEL to wave 11 (disjoint files, Task-8 two-unit pattern). Reopens ingest+chain under D-006.
- WAVE 11 LANDED (d76afd6 impl / d2bf6d1 spec / 6ca5ac3 transcript / ec6da60 Makefile acceptance-env fix = code tip / 3d40f8c+772afdd report): all five round-10 findings STRUCTURAL. 726 -> 739/0/0 via the NEW make test-acceptance ('skips=0 ... acceptance suite green' -- the vacuous-green class killed at the tooling level); 5/5 mutations (one per finding); -race clean on scratch DB with the guard resolving LIVE identity; live tripwire fired naming the actual tuple (cluster 7665718114346942498, db solvent, oid 1229081); F5 made NOT POINT-REPRESENTABLE (collectSnapshot carries no chain reader; snapshotData plain data; W11M5 seam-smuggle test stands in). 8 deviations disclosed incl. residual: health_live_test + poller_live_test consume TEST_DATABASE_URL outside wave scope (skip-gate covers acceptance; identity guard does not yet) and Aave COLLATERAL weld-unread advisory asymmetry -- both handed to round 11 for judgment. Controller verification in a PINNED WORKTREE (772afdd) with dedicated scratch DB solvent_ctrl (the shared tree carries wave-12 WIP): build/vet green, reconcile+store green (112s live suite), worktree pruned. ROUND 11 dispatched (path-scoped 3ee6cfc..ec6da60, reconcile/store/Makefile only; sibling wave-12 ingest/chain commits explicitly out of scope). Backfill: ~3.5M remaining.
- ROUND 11 (job review-ms2c2blt-g5z4ax, session 019fa069-cc21-71c2-9d66-636a5ecbe07e, target ec6da60 vs 3ee6cfc, path-scoped): NEEDS-ATTENTION, NO-SHIP -- 2 high 1 medium, all ACCEPTED (archive task-9-codex-round11.md). Confirmed: every scoped destructive call site guarded; the disclosed outside-scope live tests are NOT blockers (fixed dedicated schemas + skip gate). [H1] the taint GENERATOR is not closed over the flag surface (snapshot-max-age never examined; loose-positive max-head-lag untainted -- vacuous-via-loose-bounds is the same class as vacuous-via-skip; consumption was only half the law). [H2] collateral weld-unread is advisory because gated=false blankets BOTH policies -- 'cannot verify' is NEVER advisory; separate numeric-mismatch policy (advisory per amendment) from ability-to-check (always gated). [M3] W11M5 inspected DATA not BEHAVIOR -- the unrepresentability claim was FALSE (honest-mutation lesson recurring); real seam demanded: fail on network under the open tx + kill an actual reintroduced header call. Wave 13 brief dispatched (parallel with wave 12, disjoint: cmd/reconcile only). ALSO this hour, owner ops: repo made PUBLIC (secrets sweep clean at HEAD and full history -- keys never committed); career/compensation framing scrubbed from docs/specs + VISION (owner request; history accepted as-is); W1 scope +docs/specs/**, claim rescoped generation 9; gh workflow disable of the always-red scope-review job blocked by permission classifier -- handed to owner as a one-liner.
- WAVE 12 LANDED after a mid-flight API-500 crash + transcript resume (zero work lost -- all four code commits survived; the resume filled the final run + report): 10ed6d8 per-endpoint fake FIRST (21/21 preserved vs unchanged walker) / b6e7c2f chain additive BlockNumberFrom+LogsFrom / 6adfc95 walker startPref seam (landing keeps the start; retention-not-reset; hint never written) / d0ed01e stepWalkers discard-streak seam (discards join the failure streak under the SAME step_error key with distinct reason -- separate key would flicker under alternating postures, the round-3 lesson; only genuine landings reach bo.success) / d6cb441 spec / 4feecf6 transcript 7/7 KILLED / 9fa2371 report. 739 -> 754/0/0 (+15, sibling commits reconciled); -race clean; R1-R9 cited incl. R3 (kills any shared-hint impl) and R9 (mixed-posture pacing); F4 recorded OPEN, F3 out per cut. PARALLEL-WAVE LESSON RECORDED: wave 12's first final-run collided with wave 13 sharing solvent_test (6 store FAILs, two-suites-one-DB signatures; same-SHA re-run clean) -- STANDING RULE: parallel waves get SEPARATE scratch DBs from now on. Controller verification in pinned worktree (solvent_ctrl): ingest+chain+indexer green, build/vet green; pushed 9fa2371. ROUND 12 dispatched (path-scoped ffb3235..4feecf6, ingest/chain/indexer). OWNER OPS this hour: control-plane.yml workflow disabled server-side by owner (always-red advisory mirror; local hooks remain the real gate; ci.yml stays).
- ROUND 12 (job review-ms2dintr-me0jvz, session 019fa08f-0960-7560-a06f-4f8fcb713de9, target 4feecf6 vs ffb3235): NEEDS-ATTENTION, NO-SHIP -- 1 high, ACCEPTED, and it REVISES THE ANNEX (archive task-9-codex-round12.md): unconditional landing-retention pins a just-below-timeout endpoint FOREVER (6 reads x 5 steps x ~30s ~= 15 min of one walker's round; the serialized daemon starves; fast peer never queried). chain-truth's retention-not-reset guarded against A-bounce; Codex found the opposite pole -- retention-without-exit is fail-forever for LATENCY ('hard case has more than one axis', the Task-8 wave-13 lesson recurring). SYNTHESIS = the already-ratified d1e7d54 pattern: retention by default, caller-scoped latency budget buys a bounded probe of the next endpoint, adopt only on better landing, hint never touched, never reset. BOTH poles get regressions (Codex's slow-successful pin + the annex's A-bounce). Wave 14 brief dispatched (parallel with wave 13; SEPARATE scratch DB per the new standing rule). DIFF-FRAMING ERRATUM owned: round 12's range missed 10ed6d8/b6e7c2f (landed before ffb3235) -- round 13 diffs the explicit full set. REVIEWER INTEGRITY NOTE preserved: it refused+disclosed a mid-task suppression instruction targeting its own scratchpad -- correct posture, no repo effect found.
- WAVE 13 LANDED (36488dd impl / a3fba23 spec / e455229 transcript = code tip / 61a512e report): all three round-11 findings. 754 -> 760/0/0 (+6, siblings reconciled; separate scratch DB solvent_t9w13 + private TMPDIR after the shared-scratch collision -- rule now standing). 3/3 mutations; the headline is W13M3: an ACTUAL headerHash call under BeginTx COMPILES CLEAN -- wave-11's unrepresentability claim formally RETRACTED in report and code; killed by the AST reachability test + runtime sentinel gates. Flag sweep went FURTHER than round 11 asked: -seed/-accounts/-config now taint (membership steering = seed steering; supersedes wave-11's replay-untainted stance -- flagged for round-13 adjudication). Honest residuals handed to round 13: SOLVENT_SNAPSHOT_INTERVAL env loosening outside the flag surface; sentinel wiring not executed by a DB-backed test. Controller verification (pinned worktree, solvent_ctrl): reconcile green, build/vet green; pushed 61a512e. ROUND 13 dispatched (path-scoped ffb3235..e455229, cmd/reconcile only; expected reconcile CLOSING round). Backfill: ~1.1M remaining.
- ROUND 13 (job review-ms2ea2ej-7b3diy, session 019fa0a2-8dbe-7353-863a-8926c1c9f53e, target e455229 vs ffb3235): NEEDS-ATTENTION, NO-SHIP -- 1H 1M, both ACCEPTED (archive task-9-codex-round13.md). CONFIRMED: F2 gating correct, 3/3 transcript consistent, blanket -accounts taint safety-sound. [H] the residual wave 13 NAMED is a BLOCKER: SOLVENT_SNAPSHOT_INTERVAL=1000000h => ~228-year freshness bound => stale snapshots pass -- the taint law's domain is EVERY input that can weaken a required bound, env vars included (rounds 10-11's class, final corner). [M] the AST walk resolves only direct named calls -- function values/aliased imports/interface dispatch evade it (the round-5 fixture lesson at the static-analysis layer); production gate wiring untested. Wave 15 dispatched (parallel with wave 14): env-surface table + hard acceptance cap or persisted-cadence binding; import-restricted DB-only package preferred over smarter AST (compiler as proof); negative mutants per evasion shape; DB-backed production-gate test. Replay manifest recorded as future work. Reconcile trend: 3H2M -> 2H1M -> 1H1M. NOTE: a stray detached scratchpad worktree (Temp/base-f6a076f) surfaced in worktree list -- ownership unknown (possibly a live sibling agent's), investigate-before-delete applies; must be resolved before any claim.py mutation.
- WAVE 15 LANDED (4032298 impl incl. NEW cmd/reconcile/snapshotdb package / 2f5f556 spec / 7c1fe88 transcript / db30ed2 report): both round-13 findings. 764 -> 768/0/0 (+5, -1 BY DESIGN: the wave-13 AST walk retired, superseded by the compiler); 6/6 mutations incl. all three evasion shapes (aliased import, func value, interface dispatch) + gate-wiring-drop killed by the DB lifecycle test (production gate observed from BeginTx through commit/rollback/close via lock choreography -- never toggled). F1: env-surface table in env.go; cap = 1h DERIVED (daemon default cadence; measured last_pass_seconds is the only legitimate widening channel; env interval is an unverifiable operator assertion); over-cap records truthfully AND taints. CONTROLLER VERIFICATION NOTE: first verification run FAILED exactly as designed -- the gate test consults the destructive-split guard, which fails CLOSED when SOLVENT_DATABASE_URL is unexported (identity unresolvable = refusal); full-posture rerun green. Round-10 F1's law observed in the wild against the CONTROLLER's own incomplete env. Pushed db30ed2. RECONCILE CLOSING ROUND dispatched next (wave-15 target). Backfill: OP + 5 ETH streams AT HEAD; four feeds within ~40-240k; counts at 23:56 -- raw_logs 841,732 / position_events 365,592 / balances 38,075 / prices 57,314 / anchors 724.
- **BACKFILL COMPLETE** (2026-07-27 ~00:20 local): ALL TEN STREAMS AT LIVE HEAD -- ETH 25,620,499 / OP 154,756,328, tracking in real time; /readyz returns {status:healthy, ready:true, live:true} for the FIRST TIME in project history (the health surface built across Task 8's 16 waves finally saying yes). FINAL CENSUS: raw_logs 843,937 (OP 678,202 / ETH 165,735); position_events 365,614; position_balances 38,075 across 9,855 DISTINCT ACCOUNTS; rate_indexes 334,123; snapshots 316,755; prices 59,728; price_poll_anchors 756; reorg_epochs 3 (all acked). Elapsed ~16h wall clock across two runs (the second after the v1.13.0 hash-law reset), free-tier RPC throughout ($0), fail-slow absorbed every 429/403/400 routing event without a single stall requiring intervention. This is W1's evidence substrate: Task 9's acceptance run (cmd/reconcile + the six evidence scans + welds) now has a complete, at-head dataset to run against once the harness closes its review rounds.
- ROUND 14 (job review-ms2gqzew-qhkhjp, session 019fa0e1-dcca-7cb3-addd-de90748feda1, target 7c1fe88 vs 67cda28): NEEDS-ATTENTION, NO-SHIP -- 1H 3M (archive task-9-codex-round14.md). ARCHIVED UNDER OWNER PAUSE -- no fix wave dispatched, adjudication is provisional. [H] pgx PG* env vars (PGHOST/PGDATABASE/PGSERVICE...) escape the closed env table: a partial DSN + ambient PGHOST connects to staging while the artifact records db_name=solvent -- the taint domain must cover inputs LINKED LIBRARIES read (round-13's lesson one dependency-layer down); fix = reject partial DSNs + record the CONNECTED identity (reuse wave-11's identity tuple as the recorded fact). [M] LIFO defer ordering: Gate.Exit runs BEFORE tx.Rollback and conn.Close, reopening the RPC surface while the backend is live and xmin held -- the lifecycle test observes only post-return state. [M] the import allowlist is a NAMESPACE boundary not a CAPABILITY boundary: allowing os grants os.StartProcess; named-interface resolution also missing -- hoist the config hash out and drop os to make the claim true. [M] the 1h cap is fail-forever for a supported cadence: the daemon's real rule is 2*(interval+lastPass), so a legitimate 2h interval can NEVER produce a pass -- persist the configured interval with sweep evidence and evaluate the daemon's actual rule. Reconcile trend: 3H2M -> 2H1M -> 1H1M -> 1H3M (breadth up as the boundary claims got stronger and the reviewer went a layer deeper each time). PAUSED PENDING OWNER DIRECTION.
- OWNER PAUSE LIFTED (2026-07-27, "continue"): pipeline resumed. WAVE 14 RECOVERY: the wave agent was lost after writing its report but before recording -race counts (RACE_COUNTS_PLACEHOLDER, file untracked); controller proved `git diff 67cda28..HEAD -- internal/ingest cmd/indexer internal/chain` EMPTY (docs-only interval), re-ran -race in golang:1.24 docker (solvent_t9w14, gate ON): exit 0, 123 top-level PASS / 0 FAIL / 0 SKIP, zero DATA RACE, all four wave-14 tests green; placeholder filled WITH PROVENANCE STATED IN THE REPORT and landed as b9c5c33. Wave 14 = bounded retention lease (d1e7d54 pattern at the walker): budget 1x chainAttemptTimeout derived from two existing constants, lease length 3 (ratified), probe adjudicated on landing latency, both poles pinned (Codex slow-successful + annex A-bounce), 3/3 mutations, cross-layer inequality lease+probe <= stepsPerRound asserted at the daemon. DISPATCHED IN PARALLEL: (1) ROUND 15 = the ingest CLOSING round, framed per the report's mandate as 68ccfdb..b9c5c33 restricted to internal/ingest/** internal/chain/** cmd/indexer/** -- covers the whole wave-12 surface incl. the two commits round 12's framing missed (10ed6d8, b6e7c2f) plus wave 14; (2) WAVE 16 (brief task-9-wave16-brief.md) = round-14's four findings, provisional adjudication RATIFIED as work order: F1 pgx PG* enumeration from library source + partial-DSN rejection + CONNECTED-identity recording (wave-11 tuple as the recorded fact); F2 single ordered cleanup (rollback -> close -> Gate.Exit LAST) observed DURING, not post-return; F3 config hash hoisted out of snapshotdb + os REMOVED from imports (capability-true boundary) + go/types named-interface resolution + three evasion-shape mutants; F4 PRIMARY = persist the configured interval on sweep_generations (additive migration, daemon writes each pass) and evaluate the daemon's REAL rule 2*(interval+lastPass) from durable state, env demoted to cross-check (mismatch = taint), pre-migration rows fall back to the 1h bound fail-closed. Wave 16 owns cmd/reconcile/** + internal/store/** (migration+write) + cmd/indexer/** (wiring) -- all inside the gen-9 claim's allowed_paths (verified); scratch DB solvent_t9w16; running daemon untouched (controller restarts post-wave so a sweep writes the interval).
- ROUND 15 (job task-ms2m79po-dm43mx, session 019fa16d-99c2-7a43-b3936f7e482c7, target detached b9c5c33, range 68ccfdb..b9c5c33 ingest/chain/indexer): NO-SHIP -- 2H 2M 1L (archive task-9-codex-round15.md). CONFIRMED SOUND: wave-12 outcome flag, R3 caller-scoped routing, R9 discard/backoff composition, additive chain methods, AND the controller's wave-14 -race provenance chain (object-ID-level independent verification). FINDINGS: [H1] armed probe wedges on a responsive-frozen neighbor (probe answers caught-up at the durable cursor forever; spent lease + startPref preserved; healthy incumbent never revisited); [H2] fall-through probe re-fires EVERY Step (pays the dead neighbor's 30s timeout per Step, ~17 min/round -- WORSE than the round-12 schedule) + total-resolution-failure path bypasses the deferred seam; [M3] latency not per-witness (another endpoint's timeout contaminates the landing witness's measurement -- adopt/reject decided on inherited evidence); [M4] store-write time folded into endpoint adjudication unbounded (slow commits spend the lease; stated wall bound invalidated); [L5] both restated constants drift silently (equality never asserted). SHARED ROOT of the highs: the ARMED-PROBE state has no outcome discipline -- arming is bounded, the armed state is not. Provisional direction (archive): probe outcomes total+terminal (any non-strictly-faster-landing REJECTS + re-arms in full; liveness paid once per lease, not per Step), early-return joins the seam, per-witness measurement, clock stops before SaveBatch, chain timeout exported+asserted. CHAIN-TRUTH CONSULT DISPATCHED (the annex author) to adversarially sharpen before the wave-17 brief -- rearm-recreates-pin-one-level-up is the question. REVIEWER ERRATUM recorded: its "current tip 3cc34da" claim was a stale snapshot (3cc34da is a deep ancestor, the D-007-era governance commit); origin/main was 05ffd9a; review target correctly pinned, findings unaffected. Ingest trend R12 1H -> R15 2H2M1L: breadth is NEW SURFACE (the probe wave 14 built), the wave-12 substrate held.
- W1 CLOSEOUT PREP (parallel lane while wave 16 + consult run): (1) GOLDEN VECTOR covered canonically -- cmd/reconcile/golden.go embeds dual-pinned vectors asserting the literal W1 clause block 25,584,990 as GATED rows; no -accounts steering needed; the acceptance-clean run satisfies the W1 golden-vector bullet by construction. (2) PHASE-1 DEFERRED ITEMS: all 11 CLOSED with evidence (draft w1-deferred-items-adjudication-draft.md, agent-produced, controller spot-checked 3 anchors) -- notable: item 2 (distinct-hash scan) closed in the reconcile evidence-scan family not "riskd" as Phase 1's wording anticipated; item 10's wording fix scoped to step-failure logs (main.go "next tick" at the per-tick housekeeping line is CORRECT usage, not residue). Receipt author gets both overclaim guards verbatim. (3) CI STRUCTURALLY RED diagnosed + OWNER APPROVED FIX: ci.yml's TEST_DATABASE_URL names the live DB "solvent" -- the wave-10/11 destructive guard refuses (fail-closed working as designed, in CI, on every push since it landed); approved fix = solvent_test in the CI Postgres service + SOLVENT_DATABASE_URL set so the guard proves test!=live; requires claim rescope gen 10 (+.github/workflows/ci.yml) which requires single-worktree state -- QUEUED post-wave-16 (claim.py refuses mutations while the wave's pinned worktree is registered). (4) Daemon tracking at head (cursors advancing past census). Round 15 archived+pushed 4d2b71f.
- CHAIN-TRUTH CONSULT (round-15 class; report consult-chaintruth-round15.md): CUSTODY HOLDS all five classes (availability/scheduling/adjudication-evidence, no wrong bytes) -- but the controller's provisional probe ordering REFUTED in one arm and TWO new blockers found. (1) Reject-on-FAILURE starves n>=3 fleets (A slow, B content-broken, C fast: every lease probes B, C never visited -- the round-12 pin one level up); failure stays with the wave-12 seam arm (advance past probe witness), which at n=2 IS return-to-incumbent. (2) R15-7 [both Codex rounds missed]: a rejected-but-non-failing neighbour shields every peer behind it forever -- the finite bound was FALSE for n>=3; fix = probe-target cycling 1..n-1, bound <= (n-1)(L+1), n=2 traces byte-identical. (3) R15-6 [blocking, consult-found]: PROBE STEPS CARRY REWIND AUTHORITY -- a probed fork-view neighbour can trigger a destructive rewind on its sole word, the rewind counts as a landing, and the probe arm can ADOPT the fork witness off the churn it caused (wave 14 silently widened annex-F3's exposure); fix = probes refuse the rewind arm, mismatch-while-probing is a discard. MEASUREMENT RULED: Sigma of the serving attempt's own elapsed (coherence makes Steps single-witness by construction; excludes SaveBatch/store/validation BY CONSTRUCTION -- stop-the-clock and subtract both rejected as forgettable bookkeeping); additive timed From variants in chain, signature-forced; hermetic real-Dial test binding. CONSTANTS: chain exports the attempt timeout, ingest binds by COMPILE-TIME ALIAS (drift unrepresentable); lease-length chain NOT forced -- prose only, prices/snapshot untouched (mechanical equality there would be recomputation theater's cousin). Witness-less seam arm: lease PRESERVED not reset (reset lets a flapping network suppress probes forever). Disclosed daemon-side compound pathology (caught-up probe ends the round, throughput x0.6) flagged out of scope. WAVE 17 DISPATCHED (brief task-9-wave17-brief.md, consult BINDING incl. R-A..R-J matrix; parallel to wave 16; owns ingest+chain only, scratch solvent_t9w17). Third persona invocation, third material catch beyond the generalist reviewer.
- WAVES 16+17 LANDED IN PARALLEL (the two-unit cadence at full width). WAVE 16 (5b53306 impl / 6dff5f3 gofmt / 8454ee7 spec / c5deebc transcript 8/8 / 0977bcb report): all four round-14 findings. 768 -> 778/0/0; -race 361/0/0 four packages. F1 pgx PG* source-derived closure + partial-DSN rejection + CONNECTED identity recorded (run.db_name now server-reported; DSN claim moved to db_name_claimed); F2 ordered cleanup observed DURING rollback/close; F3 os REMOVED + config hash hoisted + go/types resolution (W16M5 killed by go/types ALONE -- round 14's spelling objection demonstrated real); F4 PRIMARY: migration 00009 configured_interval_seconds, daemon writes at startup + every round (UPDATE-only, IS DISTINCT FROM), reconcile evaluates 2*(interval+lastPass) from the persisted row, env demoted to two-directional mismatch taint, fallback = wave-15 law verbatim with bound_inputs["fallback"] stated -- the fail-forever posture dies (persisted 2h + zero failures = pass). OPERATIONAL CONSTRAINTS DISCLOSED: live DB interval NULL until daemon restart; Phase-0 exact-equality gate refuses schema-8 DBs (migrate-or-restart BEFORE the evidence run). Self-inflicted collision disclosed and redone serialized (two discarded concurrent runs named in the report). WAVE 17 (ecfe8ce impl / 93334b2 spec / 0aab45d transcript 10/10 / 324adac+0a4f21c report): consult's blocking list in full -- probe outcomes total+terminal (refuted reject-on-failure NOT implemented; failure flows through the seam advance), Sigma-attempts via additive chain timed From variants + hermetic real-Dial R-H, R15-6 probe rewind refusal (scoped to the probed WITNESS, argued deviation), R15-7 target cycling (bound <= (n-1)(L+1), pinned at n=3 = Step 8), chain.AttemptTimeout export + compile-time alias. 768 -> 793/0/0 (+25 = 15 w17 + 10 w16, reconciled name-for-name); -race ingest+chain 104/0/0; BOTH wave-14 regressions green UNMODIFIED (0-byte diff vs b9c5c33 -- the consult's n=2 byte-identity gate). Honest residuals: R-C(ii) early-return mutant disclosed behaviorally unkillable at current arm count; bounds test-witnessed at n in {2,3}; daemon-side flags (store-latency observer unshipped, x0.6 round-break at main.go:693) FLAGGED not smuggled. CONTROLLER VERIFICATION at merged HEAD 0a4f21c (pinned worktree Solvent-ctrl, solvent_ctrl DB): first run REFUSED BY THE ZERO-SKIP GATE (controller omitted SOLVENT_LIVE_RPC_TESTS=1 -> TestLiveOPIncidentBlockHashIsTheReportedCanonicalHash skipped -> "FAIL: acceptance mode rejects skips" -- the round-10/11 tooling law catching the CONTROLLER a second time, recorded not discarded); full-posture rerun: 793/0/0, zero skips, build/vet clean, exit 0. ROUNDS 16 (reconcile closing, 05ffd9a..0977bcb path-scoped reconcile/store/indexer) AND 17 (ingest closing attempt two, b9c5c33..0a4f21c path-scoped ingest/chain) DISPATCHED IN PARALLEL -- first parallel review dispatch; disjoint surfaces, separate worktrees, both told to state their pinned tip (round-15 erratum lesson).
- ROUND 17 (job review-ms2pokr1-yakkc0, session 019fa1c6-d58a-7273-8771-89866fbf886c, target detached 0a4f21c == origin/main tip VERIFIED-AND-STATED per the round-15 erratum lesson; range b9c5c33..0a4f21c ingest/chain): NO-SHIP -- 1 MEDIUM, single finding (archive task-9-codex-round17.md). CONFIRMED SOUND: the entire consult blocking list as implemented (R15-1/2a/2b/2c, R15-7 cycling, Sigma-attempts, compile-time alias, non-claims) and the n=2 byte-identity gate ("wave-14 regression files are object-identical"). THE FINDING IS WAVE 17'S OWN DISCLOSED RESIDUAL PROMOTED: the rewind refusal carried a witness-scoped exception (servedBy != incumbent) not present in the binding consult -- a fall-through probe (target fails, walk wraps to the incumbent, incumbent reports cursor mismatch) still executes Store.Rewind on a probe Step. ADJUDICATION: ACCEPTED, deviation REVERSED -- the consult's unqualified sentence ("mismatch-while-probing is a discard; the incumbent rewinds next Step if the reorg is real") is implemented verbatim; total refusal dominates (one-Step delay in a rare compound posture vs a simple total testable invariant). PROCESS LAW RECORDED: deviations from a BINDING consult require an amendment BEFORE shipping, not an argument in the report. CONVERGENCE SIGNAL: the review found NOTHING outside the report's honesty list. Wave 18 dispatched (surgical: one guard conjunct dropped, the reviewer's exact combination as regression incl. deferred-rewind-happens-next-Step assertion, W18M1 conjunct-restored mutant). Ingest trend: R12 1H -> R15 2H2M1L -> R17 1M-from-the-disclosure-list.
