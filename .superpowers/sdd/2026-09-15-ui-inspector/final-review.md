### Passes read

Package `review-branch-d989286..6d403ad.diff` (15,571 lines) read in five passes by file group, after the plan header through "Test ID contract", spec §3.2–3.5, §5.3, §6–8, and the ledger's deferred/ruling lines:

1. **lib** — `percent`, `human-price`, `lookup-error`, `recent-lookups`, `activity-rows`, `address-lookup`, `address-stress`, `inspector-headline`, `inspector-position`, `inspector-view`, `room-history`, `trust`, the `factorPriceGuard` comment fix; the deleted `inspector-lines` skimmed.
2. **kit** — `AddressField`, `TrustChecklist`, `VerdictHeader` (kicker widened), `index.ts`, `kit.module.css`.
3. **app** — `InspectorLanding`, `page.tsx` ×2, `InspectorSurface`, `InspectorTiles`, `BackingTable`, `TrustCard`, `HistoryCard`, `ActivityTable`, `LegacyCard`, `StressTable`, `InspectorDrawer`, `MeasuredSparkline`, `money.ts`, `inspector.module.css` (every `font-size` checked for a `var(--type-*)` token); the four deleted components skimmed only where the new surface replaced a law (the old card's `positionVerdict` call, see Critical 1).
4. **tests** — `inspector.spec.ts` (the contract), `p0/p1b/r1/runbook-bsplit/shell/state-matrix` re-expressions, the deleted `r3/r4/r6`, every unit pin, `demo-inspector-weld`, `fixture-clock-law` census, `helpers/cash-position`, the uncommitted `screenshots.spec.ts`.
5. **fixtures/generator** — `generate-demo-inspector.mjs`, `demo/index.ts`, `screenshot-pages.mjs`; JSON bodies judged through the generator and the weld spec.

Cross-checked outside the package: `wireGuard.ts` (`isWireScale`, `readWirePopulation`), `headroom.ts`, `freshness.ts` (`humanAge`), `history-series.ts` (`engineNeverPresent`), `api/openapi.yaml` (Position / Leg / PriceInput / StressState required fields and nullability), `packages/client-ts/src/refine.ts` and `decimal.ts` (`liquidation_verdict` sealing, `positionVerdict`), `tests/fixtures/inspector.ts` (the contract Aave position), and the old `InspectorPositionCard.tsx` at `d989286`.

### Strengths

- **One view model, honestly branched.** `lib/inspector-view.ts` is genuinely the one place the state is decided: it branches only on `lookup.outcome` and the reader's `computed`/`refusal`, an empty `found` is refused rather than counted into "no position", a withheld Cash book under `found` is `cannot-compute` (never a Cash negative), and every found arm threads the floor. The ten `data-state` values match the plan's contract exactly and the surface prints `view.state` without re-deciding it.
- **The Cash reader is careful where it matters.** `readCashPosition` guards every decimal with `isWireDecimal`, refuses negatives as not-positions, keeps a refused row's readable debt for display only, and R15's complement law (`usedPercent = 1000 − roomTenths`) is implemented from the same tenths so the pair always sums to 100.0 — pinned at 96.2/3.8, 107.8/−7.8, 41.9/58.1 and the zero-cap arm.
- **`boundaryOf` follows the wire's solve, not the mockup's prose** (R3): factor assets move together, held assets stay flat and are deduped, an empty factor list is phrased on the axis, a half-served joint solve is `unreadable` naming `prices[i].lowest_healthy_price`, and both contradiction arms (breached / above-spot beside a not-liquidatable verdict) are named rather than printed. `classifyFactorPrice` runs before any entry read.
- **Room history is a gap-first series** (R4): six point kinds, every batch and block through `readWirePopulation`, a duplicate batch is a `WireIntegerError`, `0/0` is undefined rather than "past the cap", a no-debt point is a knowable 100 %, and the streak refuses NaN and backwards spans. `values` are the only floats and they are geometry.
- **The resume law is re-expressed, not dropped.** `useAddressLookup` keeps a failed background repair from replacing a rendered position, a repair rides an in-flight foreground load instead of aborting it, and the contract test counts the aborted repair request (`expect.poll(() => addressRequests).toBe(1)`) so a page that ignored `pageshow` would fail it.
- **The demo dataset is generated with provenance**: batch envelope and sweep watermark read verbatim from `book.demo.json`, the weETH witness from `meta.demo.json`, scenario definitions from `scenarios.json` by id, sha256 tx hashes, integer wobble, every body through `checkClocks()` before write, and `demo-inspector-weld.spec.ts` proves the near account IS the mockup's ($4,822 / $5,012.50 / $190.50 / 3.8 % / boundary $3,818.57 / 14-batch streak). The census moved in the same commit (77 → 97, 32 → 38 batch-bearing).
- **Trust never goes green on silence**: an unmeasured age, an empty weld, an empty stamp or an absent receipt is `dim`; the oldest price keeps its own budget; wire words ride `title`; the state is spoken for AT (`.srOnly`).
- The e2e contract pins the laws R14 promised: refused-never-`$0` on three states, "never no position" on `cannot-compute`, never-summed on the contract fixture, the address carried in the deep link, the first-viewport and 390-px width contract, and every headline string is the one `lib/inspector-headline.ts` produces.

### Issues

#### Critical (Must Fix)

**C1 · `LegacyCard` judges the legacy market by the Cash engine's boolean, so every legacy position renders "Not computed" with a refused health-factor tile.**
`web/app/inspector/[addr]/LegacyCard.tsx:10-14,19`. The card reads `position.liquidation_verdict` and maps `unknowable → "Not computed"` / tone `refused`. On the wire `Position.liquidatable` is *null on Aave by contract* (`api/openapi.yaml` Position: "Null on Aave, which publishes a continuous health factor instead"), and the client seals null to `unknowable` (`packages/client-ts/src/refine.ts:117-124`), documenting that `positionVerdict()` in `decimal.ts` is the cross-engine answer (`aaveVerdictFromWad(hf.wad)`, `infinite → not-liquidatable`). So for *every* legacy position — including the contract fixture's, `tests/fixtures/inspector.ts` (`liquidatable: null`, HF `1.08`, `status: "computed"`) — the card prints Status "Not computed" beside a computed 1.08 and paints the HF tile in the refused register. That is a wrong verdict on a rendered card and a direct breach of the standing law "the legacy Aave market [is judged] by its health factor"; the deleted `InspectorPositionCard.tsx` (`d989286`, line 198) used `positionVerdict(position)` and this rebuild regressed it. No pin catches it: `inspector.spec.ts:151-158` and `state-matrix.spec.ts:247` assert the HF text and "stale price" but never the status word or tone.
Fix: `const verdict = positionVerdict(position)` (exported from `@solvent/client`), keep the "Not computed" arm for a genuinely null/refused HF, and add a pin in the contract test that the legacy card with HF 1.08 reads "Healthy" and the liquidatable-HF fixture reads "Liquidatable".

#### Important (Should Fix)

**I1 · The "every scale passes `isWireScale`" law is enforced on the page (`money.ts`) but skipped in the lib readers that print the headline and the collateral table.**
`web/lib/inspector-position.ts:63` (`decimals: position.value_decimals`), `:147` (`humanAmount(amount, leg.decimals)`), `:148` (`humanPrice(priceValue, input.decimals)`); `web/lib/inspector-view.ts:145` (`cashWire?.value_decimals ?? 6`) and `:214`; `web/lib/inspector-headline.ts:57` (`humanUsdFull(v, p.decimals)`). The client validates none of these (`openapi` says `type: integer`, no minimum; `refine.ts` touches only the booleans). The Task 11 review ruled "every wire scale through `isWireScale` before formatting" and landed it as `[addr]/money.ts`, so the *tiles* print `unreadable scale` for a bad `value_decimals` — while the *headline* and dek on the same screen print `humanUsdFull` at the raw scale. Concrete failure: `value_decimals: -2` → `rescale()` computes `shift = 4`, and the verdict sentence reads "Within $1,905,000 of its borrow cap" beside tiles reading "unreadable scale"; `value_decimals: 1.5` throws `RangeError` out of `deriveInspectorView` instead of a named `WireIntegerError`. Same class for `leg.decimals` / `input.decimals` in the backing table's Amount and Price cells, which are strings the component cannot re-guard. `wireGuard.ts`'s own header calls a mis-scaled integer rendered as a plausible price "the worst class of all".
Fix: `readWireScale(position.value_decimals, "value_decimals")` in `readCashPosition` (throw → route refusal, the Plan 1 law) or carry `decimals: number | null` and let `cashHeadline` print the refused register; `isWireScale(leg.decimals)` / `isWireScale(input.decimals)` before `humanAmount`/`humanPrice` in `collateralTable`, null → "—". Unit-pin a negative and a fractional scale.

**I2 · Three section components derive state from `reading` instead of `view`, so the "one view model that the surface AND the tests read" seam has three side doors.**
`web/app/inspector/[addr]/HistoryCard.tsx:29-33` reads `reading.history` and builds the legacy HF series (with the C1-ruled `engineNeverPresent` gate) itself; `StressTable.tsx:33` reads `reading.stress` and calls `stressReading` itself, then carries a five-arm `emptyText` ladder; `InspectorDrawer.tsx` reads `reading.lookup.value.response.batch` and `reading.params`. `deriveInspectorView` exposes `historyOutcome`, `room`, `streak` but not `legacySeries` or `stress`, so the view's unit spec (`inspector-view.spec.ts`) cannot pin the legacy-chart gate, the stress reading's `withheld` copy, or the drawer's provenance section — only e2e can, and e2e pins the happy row. It also means `HistoryCard.tsx:37-53` carries an eight-level nested ternary of state copy that belongs beside the headline copy in lib. A fragile seam rather than a wrong number today, but it is exactly the second-source-of-truth the plan's architecture paragraph forbids.
Fix: lift `legacySeries: HistorySeries | null` and `stress: StressReading | null` (plus the history/stress "finding" sentences) into `InspectorView`; the components then only place them.

**I3 · The Cash room chart lacks the never-present gate the legacy chart has, so a legacy-only or no-position address renders an all-gap sparkline captioned "The newest batch is absent; the streak cannot be read".**
`web/lib/inspector-view.ts:158` builds `roomSeries` whenever the Cash engine is listed (the API lists every engine even with zero points — the C1 ruling's own premise), while `HistoryCard.tsx:33` gates the legacy chart on `!engineNeverPresent(legacyEngine)`. For an address that never held a Cash position, `room.points` is 100 % `no-row` gaps, `streak.newestKind === "no-row"`, and `HistoryCard.tsx:50` prints the "cannot be read" sentence over an empty line; `TrustCard` likewise prints "History · room % over the last N batches" with nothing plotted. The honest sentence already exists one branch down ("No Cash history for this account in the covered window."). The same predicate applied to the Cash engine (`engineNeverPresent(engine) ? null : roomSeries(...)`) fixes both cards.

**I4 · Three copies of the same "a, b and c" joiner, created by this plan.**
`web/lib/inspector-headline.ts:51` `engineList`, `web/lib/inspector-position.ts:189` `list`, `web/lib/trust.ts:76` `andList` — identical semantics (no Oxford comma, `names[0] ?? ""` fallback), three names, three test surfaces. Plus four copies of `const n = (v) => v.toLocaleString("en-US")` (`inspector-headline.ts:34`, `inspector-view.ts:82`, `trust.ts:43`, `HistoryCard.tsx:11`). None is constrained by the frozen-lib rule; a senior reader will assume the modules were written without looking at each other. Fix: one `lib/prose.ts` (`joinAnd`, `groupInt`) imported by all three.

**I5 · The near-cap dek claims liquidation at exactly the cap, contradicting the strict rule the liquidatable dek states.**
`web/lib/inspector-headline.ts:81`: "A 3.8% fall in collateral value, or $190.50 more debt, makes this account liquidatable." Room is floored to tenths (`headroomTenths`), so a 3.8 % fall leaves the account *above* its cap; and `$190.50` more debt makes `debt == cap`, which the same module's liquidatable dek says is healthy ("the strict rule is debt > cap"). The healthy arm has it right ("would have to fall 58.1% before this account reaches its cap"). One sentence a risk lead will quote and a quant will correct. Fix: "…brings this account to its cap." (keeps the money, drops the false verdict) and re-pin in `inspector-headline.spec.ts:585` and `inspector.spec.ts:46`.

#### Minor (Nice to Have)

- **M1 · The drawer's substituted formula does not balance at the cents.** `InspectorDrawer.tsx:33` prints `Room = cap − debt = $5,012 − $4,822 = $190.50` (pinned at `inspector.spec.ts:270`); the cap's `.50` is dropped by the `humanUsdFull` ruling (progress.md:24, whole dollars ≥ $1,000) while the room keeps its cents. The drawer's job is "the formula with substituted numbers"; use `wireExact` there (the exact register the drawer already prints two paragraphs later) so the equation is arithmetically true. Same for `:30` (Σ legs = cap).
- **M2 · `pricesChip` reads `age_seconds` unguarded while `trust.ts` guards the same field.** `inspector-position.ts:276-278` `Math.max(...ages)` → `humanAge` clamps negatives to `0s`. For a Cash position `trustChecklist` throws first so it is moot; for the `legacy-only` path (`priceSource = legacy`, `inspector-view.ts:186`) a negative Aave `age_seconds` prints "Aave oracle · 0s" — a manufactured zero age. `readWirePopulation` in `oldestPriceAge` closes it.
- **M3 · Trust's Price provenance item swaps label and detail in its warn arm.** `trust.ts:178` sets `label` to "weETH price is from an uncapped feed" and `detail` to "not oracle-direct" (pinned, `trust.spec.ts:312-313`), so the checklist's label column reads a sentence for that one item while the other four keep fixed labels. Keep `label: "Price provenance"` and move the clause to `detail` ("weETH price is from an uncapped feed · not oracle-direct") unless the ruling meant to change the row's identity.
- **M4 · Two registers for the same age on one page.** `trust.ts:135,128` print raw seconds ("35s · within 180s", "210s old · budget 180s") where the Prices chip and the backing table's finding use `humanAge` ("3m"); `inspector.spec.ts:171,174` pins both side by side. Pick one (exact seconds are defensible for a trust detail — then say so in the item's `title`).
- **M5 · `formatTenths` re-implements `headroomPercent`'s body** (`percent.ts:12` vs `headroom.ts:167-176`). Frozen-lib constraint applies to `headroom.ts`, so this joins the ledgered `group`/`rescale` duplicate for the Plan 4 consolidation; note it there.
- **M6 · Float in a printed horizon.** `StressTable.tsx:17` `Math.round(seconds / 86_400)` → a 36-hour horizon prints "2d". Integer arithmetic (`Math.floor` in bigint or a `≈` marker) keeps the "floats only as geometry" law literal; today's fixture horizons (30 d / 90 d) hide it.
- **M7 · `engineName` compares against the literal `"debt_manager"` but the `LEGACY` constant** (`inspector-headline.ts:46-47`); use `CASH`.
- **M8 · Dead exports:** `ACTION_LABEL` (`activity-rows.ts`), `UNREADABLE_SCALE` (`money.ts`), `RECENT_KEY` (`recent-lookups.ts`) have no consumer outside their module or tests; `percentOf` is used only in `inspector-position.ts` (fine) — drop the `export` on the first three or pin them.
- **M9 · `HistoryCard.tsx:69` `batchId ?? 0`** manufactures "batch 0" for an empty series; unreachable today (`known` always holds `historyBatchId`) but it is the zero-for-unknown pattern the laws forbid — use `room.points[0]` non-null via the type or omit `xLabels`.
- **M10 · Kit CSS breakpoint drift:** `kit.module.css:151` adds a `720px` rule for `.searchIn` beside the kit's single `900px` stack rule; the ledgered missing blank line at :130 stands.
- **M11 · Plan file-structure drift, harmless:** `lib/address-lookup.ts` (plan says `.tsx`; no JSX, so `.ts` is right — just update the plan's table); `stressReading(lookup, account)` dropped the planned `decimals` argument (ledgered in the interface table).
- **M12 · Comments that narrate review rounds** in shipped code: `room-history.ts:9-17` ("Review round 1: …"), `activity-rows.ts:75` "(r74)", `inspector-position.ts:174,190` "p0-9 / p0-8", the test names "fix round 1 — …" (`inspector-headline.spec.ts:669`, `address-stress.spec.ts:171`, `inspector-position.spec.ts:166`). The laws are worth keeping; the round numbers are not what a portfolio reader wants to see.

### Recommendations

1. Land C1 with a pin before anything else — it is a one-line regression against a helper the client already exports, and the contract test already has the fixture (HF 1.08) to pin it.
2. Fold I1 into the reader (`readWireScale` at `readCashPosition`, `isWireScale` on leg/input decimals) rather than adding more page-side guards; `money.ts` then becomes a thin adapter and the headline and tiles can never disagree about a scale.
3. Take I2 and I3 together: moving `legacySeries`/`stress` into `InspectorView` and gating the Cash series on `engineNeverPresent` lets the view spec pin the legacy-only history and stress copy that only e2e reaches today.
4. Fix I4/I5/M1 in one copy-and-prose commit with the pins updated in the same hunk; they are all sentence-level.
5. Ledger M2–M12 as deferred if not taken now; none changes a number on the primary state.

### Assessment

**Ready to merge?** No

**Reasoning:** The Cash path is the strongest surface in the repo — one view model, exact arithmetic, honest gaps, a generated dataset welded to the Book — but the legacy card prints a wrong verdict on every Aave position it will ever show (C1), and the scale guard the Task 11 review ordered landed on the tiles but not on the headline they sit under (I1); both are small fixes with obvious pins, after which this merges.

## Re-review 1 (f440ec9)

**RE-REVIEW: closed.**

Read: `review-6d403ad..f440ec9.diff` in full (app components, lib modules, tests) and `task-14-fix-report.md`; cross-checked `lib/feed-view.ts` (`scaled` → `renderNullableDecimal`, the client's `assertScale`) for the one path the fix did not touch. Read-only; no server, no e2e.

| Finding | Fixed as ruled | Pin can fail | New defect |
|---|---|---|---|
| **C1** legacy verdict | `LegacyCard.tsx:12` `positionVerdict(position)`; "Not computed"/refused only for `unknowable` (null or refused HF); tiles carry `inspector-legacy-hf` / `inspector-legacy-status` | `inspector.spec.ts:41-44` pins "Healthy", `data-tone="ok"`, HF tile `neutral`, `not.toContainText("Not computed")` on the HF-1.08 fixture — all four failed on 6d403ad | none |
| **I1** scale guard in the readers | `inspector-position.ts:63-69` `value_decimals` through `isWireScale` first; a refused scale nulls `decimals`, `debt`, `cap`, `collateral` (the malformed-decimal shape); `:103` `isComputedCash` requires a scale so `ComputedCash.decimals: number` keeps `cashHeadline` typed non-null; `:147-148` per-cell `"unreadable"` for a leg/input scale, absent stays null; `inspector-view.ts:145` `decimals = cash?.decimals ?? null` (the `?? 6` is gone), `:251` last-readable debt gated; `:130` cause names `value_decimals` after the refusal/status arms; `money.ts:13` `moneyFor(null)` → "—"; `InspectorTiles.tsx:40`, `InspectorDrawer.tsx:20` gate the exact register | reader (−2, 1.5 → every amount null, no refusal invented, refused row keeps its code), table (leg −1, input 2.5, absent amount stays null), view (state `not-computed`, exact dek, no "$", no throw) — on 6d403ad 1.5 threw `RangeError` and −2 printed a mis-scaled headline | Every consumer of `InspectorView.decimals` handles `null` (`moneyFor`, `exactDebt`, `exactMoney`); no `null` reaches a formatter. See residual R1 for the one raw-scale hand-off left. |
| **I2** view owns legacy series + stress | `inspector-view.ts` `legacySeries` (`:200`, `engineNeverPresent` + `knownBatchAxis`), `stress` (`:203`), `historyLoad`/`stressLoad` (`LoadPhase`), `historyFinding` (`:301`), `stressEmptyText` (`:320`); `HistoryCard`/`StressTable` take `{ view }` only; the drawer keeps `reading` as ruled | legacy entries `[1 refused, 2 computed]`, newest "1.08", listed-empty → null, withheld-only → presence; eight `historyFinding` arms and five `stressEmptyText` arms each pinned to the exact sentence; loading-state fields pinned | ladder moved verbatim, `room === null` still before the streak arms; see residual R2 (pre-existing) |
| **I3** never-present Cash | `inspector-view.ts:186-196` `roomSeries` only when `!engineNeverPresent(engine)` | Cash listed with zero points → `room`/`streak` null, `historyOutcome` "found", finding "No Cash history…" — an all-gap series on 6d403ad | none |
| **I4** one joiner | `lib/prose.ts` `joinAnd`/`groupInt`; `engineList`, `list`, `andList` and the four `n` lambdas gone | `prose.spec.ts` (former `engineList` pins moved, plus three-name, empty, bigint) | none |
| **I5** near-cap dek | `inspector-headline.ts:76` "…brings this account to its cap." | exact dek + `not.toContain("liquidatable")`; e2e `:17` re-pinned | none |
| **M1** drawer balances | `InspectorDrawer.tsx:18-20,32-37` every term through `wireExact` | e2e `:68` `5,012.500000 − 4,822.000000 = 190.500000` | none (no "$", ASCII minus — accepted) |
| **M2** chip age guard | `inspector-position.ts:283-287` `isWirePopulation` filter | −5 beside 35 → 35; −5 beside null → null; 1.5 → null; chip "age unknown", `not.toContain("0s")` | none |
| **M3** provenance label | `trust.ts:170-176` label fixed, caveat in detail | five re-pinned arms | none |
| **M6** horizon label | `address-stress.ts:113-128` integer arithmetic only | 3h / 30d / 1d 12h / 30m / 23h / 90d, `not "2d"` | residual R3 (sub-minute arm) |
| **M7 / M8 / M9 / M12** | `CASH` compared; three exports dropped; `xLabels` only from real batches; round/task words gone from comments and test titles | n/a | none |

### Residuals for the ledger (not blocking; none is a taken item left open)

- **R1** `InspectorSurface.tsx:37-41` still hands the activity table the raw `cashWire.value_decimals` / `legacy.value_decimals` while the guarded `view.decimals` is one line away. On a refused scale that path ends in the client's `assertScale` (a thrown route refusal via `renderNullableDecimal`) rather than the page's refused register — the law holds, in a different shape from the tiles beside it. Implementer's Concern 4; outside the taken rulings. One-line fix when next touched.
- **R2** `inspector-view.ts` `empty()` (lookup loading / errored / contradictory) sets `room` and `historyOutcome` to null but `historyLoad` to the real phase, so once the history has answered "found", `historyFinding` reads "No Cash history for this account in the covered window." beneath a lookup that is still loading or 503'd. Pre-existing on 6d403ad (the old `HistoryCard` computed the same sentence from the same nulls), unchanged by the move — now a pure function the view spec can pin. Fix: an explicit arm for `historyOutcome === null` while `historyLoad` is ready ("History not read for this lookup") or carry the outcome through `empty()`.
- **R3** `address-stress.ts:117` `horizonLabel(1..59)` → "0m" (verified: 30 → "0m", 59 → "0m") — a nonzero horizon printed as a zero. Unreachable on the committed scenarios (30 d / 90 d) and the accepted minutes arm covers ≥ 60 s; a `seconds < MINUTE → "<1m"` arm closes it.

Also noted from the implementer's report, outside the 26 files: the screenshot run wrote untracked `screenshots.spec.ts-snapshots/inspector-{dark,light}.png` baselines (Playwright `updateSnapshots: "missing"`), captured post-I5 — the controller should adopt or delete them before the pin is trusted.

## Re-review 2 (c3ef561)

**RE-REVIEW: open** — one item, outside the four files but inside the question asked.

Read `git show c3ef561` in full (25 insertions, 0 deletions; `lib/inspector-view.ts`, `lib/address-stress.ts`, their two unit specs). Read-only; no server, no e2e.

- **R2 in `historyFinding` — correct and complete.** `inspector-view.ts:304-306`: after the history's own loading/error arms, `state === "loading"` → "History waits on the lookup." and `state === "invalid" | "unavailable"` → "History not read — the lookup could not be completed." The four `empty()` call sites (`:145` invalid, `:146` loading, `:148` and `:155` unavailable) are the only producers of a null `historyOutcome` beside a ready history, and they map to exactly those three states; every other state derives `historyOutcome` from a ready history, and a not-ready history is caught by the two arms above. Ordering is right: a loading or errored history speaks for itself before the lookup's state does. Pins (`inspector-view.spec.ts:400-416`) cover a ready history under a loading lookup, a failed lookup (with `not.toContain("No Cash history")`), and the found-but-empty contradiction — on f440ec9 all three read "No Cash history for this account in the covered window.", so each can fail.
- **R3 — correct.** `address-stress.ts:124` `seconds < MINUTE → "<1m"`; pinned at 1, 59 (both "0m" on f440ec9) and 60 ("1m"). Truncation elsewhere unchanged.
- **Nothing else changes** in the four files: the doc comment on `horizonLabel` still lists four arms and does not mention the new `<1m` one (cosmetic; not blocking).

**Open — the other path to a definitive history sentence with a null `historyOutcome`:**
`web/app/inspector/[addr]/TrustCard.tsx:35` prints `"History · no Cash history for this account"` whenever `room === null`, with no look at `historyLoad`, `historyOutcome`, or `state`. That sentence therefore renders (a) while the history itself is still loading on every normal page load, (b) under a loading or failed lookup — the exact R2 shape `historyFinding` just closed, one card to the left, and (c) when the history is **withheld** (`historyOutcome === "unknowable"`), where the spec's law and `historyFinding`'s own arm say a withheld history is never "no history". The same law is now enforced in the History card and not in the Trust card. Fix: gate the spark head on the same outcome — `historyLoad.phase !== "ready"` → "History · loading…", `state` loading/invalid/unavailable → "History · waits on the lookup", `historyOutcome === "unknowable"` → "History · withheld this batch", `"not-found"`/no Cash engine → the existing "no Cash history" line — or hand the Trust card a `roomHead` string derived beside `historyFinding` in the view, and pin the withheld and lookup-loading arms.

## Re-review 3 (b0934f5)

**RE-REVIEW: closed.**

Read `git show b0934f5` in full (38 insertions, 4 deletions; `lib/inspector-view.ts`, `app/inspector/[addr]/TrustCard.tsx`, `tests/unit/inspector-view.spec.ts`). Read-only; no server, no e2e.

- **Ladder order is right.** `inspector-view.ts:324-334` `historyHead` runs history loading → history error → lookup loading → invalid/unavailable → `historyOutcome === "unknowable"` → `room === null` → batch count: the same order as `historyFinding`, so a withheld history reaches "History · withheld this batch" before the null-room arm can say "no Cash history". The one arm `historyFinding` has that `historyHead` folds is `not-found`, which falls to "History · no Cash history for this account" — a true sentence for a definitive negative from a complete lookup (no history on any engine implies none on Cash), so no dishonesty is introduced. Nothing else in the two source files prints "no Cash history": `TrustCard.tsx` `emptyWords` speaks of the position, not the history, and `historyFinding`'s own "No Cash history…" sits behind the same guards.
- **Pins can fail on c3ef561.** `historyHead` did not exist, and the old ternary at `TrustCard.tsx:35` could only produce "no Cash history" or the batch count, so each of the five new arm strings (loading, unavailable, waits on the lookup, not read, withheld) is unattainable by the old logic; the withheld arm additionally carries `not.toContain("no Cash history")`, and the batch-count arm on `dmHistory(FOUND_ADDR, 2)` pins "over the last 3 batches".
- **The card changes nothing else.** The `batches` local and the ternary are removed; `plotted`, the `room !== null` sparkline gate, the checklist branch and `emptyWords` are untouched — the card only places the string.

Cosmetic, not blocking: `inspector-view.ts:322-323` — the `/** The stress table's words … */` doc comment for `stressEmptyText` was left above the new `historyHead` block, so it now precedes `historyHead`'s own doc comment and is orphaned from its function; move it down to sit on `stressEmptyText` at the next touch.
