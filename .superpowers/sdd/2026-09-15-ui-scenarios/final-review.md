# Plan 3 (Scenarios) — whole-branch review, 9aa5f17..c92f4fd

Read: the plan header (Global Constraints, R1–R16, the test-ID contract), `final-triage.md` (26 lines; the twelve truncated ones read whole from `progress.md`), and the review package in ranges — every lib under `web/lib/lab-*.ts` and `lab-engine.ts`, every file under `web/app/lab`, the kit's `ScenarioLibrary`/`Heatmap`/`DotPlot` and CSS, the e2e contract `web/tests/e2e/lab.spec.ts` in full, the generator and the weld spec in full, the unit specs for what they pin, the Plan 2 libs the address mode leans on (`address-stress.ts`, the Inspector's `StressTable.tsx`, `inspector-view.ts`), the fetch layers (`runbook.ts`, `runbookSet.ts`), and the helpers whose sign and guard behaviour the findings turn on (`human-usd`, `human-price`, `percent`, `prose`, `wireGuard`, `freshness`, `KpiTile`). Read-only; nothing run.

## Branch verdict

**Sound. No Critical. Five Importants, all bounded to one close-out round; no law is broken in the primary path.** The cross-task seams the per-task reviewers could not see hold almost everywhere: one reader per engine (`readEngine`) is genuinely the only reader — the workspace, the library row and the drawer all print from it; one row verdict (`rowVerdict`) feeds the address headline and the address library word; the hold law (`heldOf`/`withRunning`/`withSettled`) carries the same `definitionSkew` predicate the row uses; `setMembership` gates the set before `compareRows` reads it; nothing dispatches on a cold load and the e2e counts the POSTs; the demo weld reproduces the Book's figures to the dollar and the generator asserts every sum it states. Copy is in `lab-headline`/the view models for every headline and every library word, and the e2e pins those sentences verbatim against the demo Book.

Where the branch does break its own laws is one cluster: the one-address table (`AddressWorkspace.tsx`) was transplanted from the Inspector's `StressTable` without the Inspector's projection arm and without the branch's own "a side the tiles refuse yields no figure and no verdict word" gate — so the table can say "No" where the headline says "becomes liquidatable within 90d", and can print a room where the tile above it prints "—". The other Importants are a missing sentence for a wire state the branch itself admitted (a negative net count), unguarded envelope arrays that would throw at render on a version-skewed 2xx body (a regression from the old Lab's `Array.isArray` sweep), and a library highlight that can name a different scenario than the address headline.

## Findings

### Critical

None.

### Important

**I1. The address table's verdict column does not speak from `rowVerdict`; a projection row with a liquidatable horizon prints "No".**
`web/app/lab/AddressWorkspace.tsx:43` reads `r.flips` directly: `flips === null → "Cannot say" · true → "Yes" · false → "No"`. For a projection row `flips` is computed from the spot sides (`web/lib/address-stress.ts:73–76`), which the projection leaves unchanged, so it is `false` — while `rowVerdict` (`web/lib/lab-address.ts:136–154`) judges the same row by its horizons and the headline (`rowHeadline`) and the library word (`rowOutcome`, `:268–282`) both say "becomes liquidatable within 90d" (warn). The Inspector's own table handles exactly this arm (`web/app/inspector/[addr]/StressTable.tsx:60–66`: `projected ? (flipsAt === undefined ? "No" : "Within {horizon}")`), and the lab dropped it. The same cell also prints "Yes"/"No" for a `not-a-position` side (a negative wire figure) where the headline is a cannot-say. Laws: one row verdict shared by the headline and the library word (e695cc1); R8 (the Inspector's registers); refusals render honestly. **Plan text mandates it: yes** (R8 names the Inspector's registers, and the Inspector's register already says "Within 90d"). The demo's rate step has no liquidatable horizon, so no e2e pin sees it. Fix: the cell speaks from `rowVerdict(r)` (export a `rowVerdictWord` beside `rowOutcome`, or print `rowOutcome(r).text`); one pin on a projection whose horizon is liquidatable (the unit spec's `projected(1, true)` fixture is already there).

**I2. The address table prints a room figure beside an unknowable verdict, and in a different word than the tile above it.**
`web/app/lab/AddressWorkspace.tsx:38,41` print `money(r.before?.room)` / `money(r.after?.room)`; `address-stress.ts:59` computes `room` from debt and cap whatever the verdict, so an unknowable or refused side prints "$1,234" in the table while the tiles for the same row refuse it (`computable`, `web/lib/lab-address.ts:84–94`, the law Task 6 landed over four rounds). A negative room prints "−$1,069" in the table (`humanUsdFull`) and "over cap by $1,069" in the tile (`roomWords`, `:107–109`) — one figure, two words, one screen (triage line 16). Inherited from Plan 2's `StressTable.tsx:31` (`money(side?.room)`), so the Inspector page has the same shape. **Plan text mandates it: partly** — R8 says the Inspector's registers, which is what the table does; the branch's own gate law (lab-address.ts:84–94, "a side the tiles refuse yields no figure and no verdict word anywhere") goes further and the table is on that page. Fix now in the lab: export `sideRoomWords` from `lab-address` and let the table's two room cells print it (it already refuses missing, unreadable and unknowable sides and words a negative room the tile's way); carry the Inspector's `StressTable` alignment to Plan 4 (`web/lib/**` is untouched under this plan).

**I3. A negative net count has no sentence: the headline claims what the wire does not say, and the tile prints an ASCII hyphen.**
6855c01 made `newly_eligible_accounts` a signed net by ruling ("a negative net is an answer, never a malformed field") and `lab-view.spec.ts` pins `newly: -3` reading as a result — but nothing downstream has an arm for it. `web/lib/lab-headline.ts:83–89`: `newly > 0` fails, so a net of −3 falls to "No Cash account becomes liquidatable under {label}, but k change band." — a claim the wire's net cannot support (three could have become liquidatable and six healed; the movers may carry "Yes" pills under that headline). `web/lib/lab-library.ts:101` gives the row "k change band". `web/app/lab/LabTiles.tsx:44` prints `groupInt(-3)` → "-3" (ASCII hyphen, tone "ok" from `:48`). Laws: R4 ("the headline and tiles state the wire's own aggregates"); R11 (a signed figure prints `MINUS`). **Plan text mandates it: the arm, no** — R3's templates cover `newly > 0` and `newly = 0` only, a plan gap; the honesty of the sentence, yes. Reachable under the contract (`isWireSignedCount`), not under today's committed set (every shock is downward). Fix: a fourth template ("{n} fewer Cash accounts are liquidatable under {label}." ok/warn by band movement), the library word ("−{n} net liquidatable"), the tile through a signed-count formatter with `MINUS`; three pins.

**I4. Envelope arrays are read unguarded at render; a 2xx body missing one throws inside `deriveLabView` and takes the route with it.**
`web/lib/runbookSet.ts:270` casts the whole 2xx body; then `web/lib/lab-compare.ts:163` (`for..of set.requested_scenario_ids`), `:133` (`set.results.map`), `:71` (`[...r.engines.map(), ...r.withheld_engines, ...r.unmeasurable_engines.map()]` — a spread of `undefined` throws), `:101` (`r.engines.find`) all run during render. The run-book's fetch touches only `engines` (`web/lib/runbook.ts:145`, and a throw there at least lands as an "unreachable" outcome), so `web/lib/lab-engine.ts:55` (`run.excluded_engines.find`) is the first read of that array and it is in render. The old Lab's tornado guarded the set's four arrays with `Array.isArray` (Task 7 deferred it as "the page task's call"; the page task did not take it). Law: `lab-classify.ts`'s own header — "THE WALKER NEVER THROWS … a classifier that threw on the bodies it exists to refuse would reopen the route-boundary hole it closes" — and the version-skew class p1b named. **Plan text mandates it: not by sentence**; the branch's classifier law and old-Lab parity do. Fix: `Array.isArray` on the five set arrays in `rowOf`/`censusBreak`/`setMembership` (→ `contradictory`/`failed` naming the field) and on `engines`/`excluded_engines` in `readEngine` (→ `unreadable` naming the field); one pin per arm. (The listing's `scenarios`/`engines`/`shocks` have the same shape through the client's typed call; note it, same fix, lower priority.)

**I5. In one-address mode the library's highlight and the workspace's subject can name different scenarios.**
`web/app/lab/LabSurface.tsx:183–189` keeps `selected: r.id === view.selectedId` (the listing's selection) in address mode, while the workspace's subject is `space.selected` (`web/lib/lab-address.ts:222`: the id when the stress rows carry it, else `rows[0]`). Click a scenario the address was not stressed under ("Not on this address"): the highlighted row is that one; the headline, the tiles and the Scenario chip are `rows[0]`'s. **Plan text mandates it: R15 mandates the fallback, not the highlight**; the branch's own "row and workspace agree" law (745627e) does. Fix: `selected: r.id === (space.selected?.id ?? null)` in the address-mode map; one pin (click the not-on-this-address row, expect the highlight to stay on the workspace's subject or the headline to follow — decide which; the former keeps R15).

### Minor

M1. **Three word tables for one `EngineReading`.** `web/lib/lab-view.ts:171` maps `unreadable` to the `contradictory` state and "contradicts itself"; `web/app/lab/LabTiles.tsx:7–19` (`refusedWord`) and `web/app/lab/TransitionCard.tsx:34–46` (`words`) map the same kinds again in components; `web/lib/lab-library.ts:93–98` says "Unreadable". One body, one reader, but the row says "Unreadable" and the workspace says "contradicts itself" (the dek names the true cause). Lens (a)/(b). Carry to Plan 4: an `EngineTiles`/finding view in `lab-view` so the components print and the pins read the model; the words agree by construction.

M2. **Comments that narrate rounds and cite retired functions.** `web/lib/lab-classify.ts:32–33, 47–52, 109, 113–115, 164, 209–210, 246` carry "p1b-9 (Codex round, finding 2)", "p1b-10", "p1b-11 (finding B)", "p0-8", "Task 1's review", and name `cellPrimaryOutcome`, `readTransitions`, `belowOneLanes`, `belowOneCount`, `measuredCount`, `moversDisclosure` — all deleted in 9279806. `web/lib/lab-compare.ts:153` says "the old Lab's R57 gate". Global Constraint: comments state the law, never the round. Fix now (comment-only; moved verbatim by ruling, deferred to this triage).

M3. **`sentence`/`refused` duplicated and drifted.** `web/lib/lab-address.ts:42–50` re-implements `lab-headline`'s two helpers; `sentence("")` returns "." here and "" there. Lens (a). Fix now: export from `lab-headline`.

M4. **The dispatch guard follows the commit, not the call; the set sentinel lives in the legal id space.** `web/lib/lab-reading.ts:88–90`: `runsRef` is refreshed in an effect, so two `run(id)` calls in one tick both pass (`controllers.current.set` then overwrites the first controller, whose `.then` deletes the second's entry). `:109`: `"__set__"` shares the map with scenario ids and matches `SCENARIO_ID_PATTERN` (`/^[a-z0-9_]{1,64}$/`). Law: one POST per ask. Fix now: `controllers.current.has(id) ||` before `canDispatch`, and a separate `setController` ref (triage line 11 said "fold into whichever task next opens the hook"; 9b23b07 opened it and did not).

M5. **`MoversTable` empty text claims "No account moved".** `web/app/lab/MoversTable.tsx:36`: with `movers: []` and `movers_total > 0` the table says "No account moved under this scenario." under a caption saying "showing 0 of 118". Improbable wire, but lens (e). Condition on `table.total === 0`, else "none listed on this page".

M6. **Singular slip in the card finding.** `web/app/lab/TransitionCard.tsx:26` prints "1 accounts change band" (the headline's `movementSentence` handles the singular; the card does not). Same family as triage line 2. Fix now with those.

M7. **An empty Engines chip.** `web/lib/lab-view.ts:139`: a run with no rows and no refusals (the "hole" the e2e pins as withheld) yields `value: ""`. Say "none served".

M8. **`?scenario=<unlisted>` says nothing.** `web/app/lab/LabSurface.tsx:132–140` runs nothing and the workspace falls to the first scenario while the URL still names the ghost; `?scenarios=` names its filtered ids in the notice. One clause of notice for the single form.

M9. **Arms the model cannot reach.** `web/lib/lab-headline.ts:77–81` (`heat === null` — `EngineResult.heat` is never null; the `flipped` pin reaches it only directly), `web/app/lab/LabTiles.tsx:78` ("movement not readable"), `web/app/lab/StaleBanner.tsx:45` (`retained === null` under `retained-refused`), `web/app/lab/CompareCard.tsx:63–72` (a point without tenths), `web/components/kit/Heatmap.tsx:87` (`count === 0` — `isWireOccupancy` refuses it upstream), `web/lib/lab-address.ts:150` (`flips === null` behind the gate). Lens (c). Trim each when its file is next opened; `lab-address.ts:150` now, with I1.

M10. **`?? 0` crutches on the null path.** `web/app/lab/LabTiles.tsx:46,78` build `groupInt(r?.beforeEligible ?? 0)` inside template strings that `sub()` discards when `r === null`. Harmless today; lens (e). Build the sub inside the `r !== null` branch.

M11. **Pin-only exports.** `web/lib/lab-library.ts:59–61` `cashEngineOf`/`cashRefusalOf` are read only by `tests/unit/lab-library.spec.ts:70–77`. Lens (g). Delete both and the pin (triage line 17).

M12. **`reloadListing` is never called.** `web/lib/lab-reading.ts:127`; `listing-unavailable` offers no retry. Lens (g). Wire a "Try again" on that headline or drop the member.

M13. **A second minus.** `web/app/lab/money.ts:29` writes a literal "−" beside `MINUS` in `human-usd`. Lens (a). Use `MINUS`.

M14. **`LibraryOutcomeKey`/`LibraryOutcomeTone` defined twice.** `web/components/kit/ScenarioLibrary.tsx:6–9` and `web/lib/lab-library.ts:38–39` — a deliberate structural mirror (like `LabChip`), but they drifted once (Task 11 round 4 added `definition-changed` to both) and tsc catches only the lib-to-kit direction. Lens (a). Carry: a types-only module both import.

M15. **Kit geometry owned by the page.** `web/components/charts/DotPlot.tsx:26` `CHAR_W = 7` estimates a mono width against the kit's `useMonoCharWidth` law; `web/app/lab/lab.module.css:17` `.plotFrame > svg { max-width: none }` overrides the kit's `.chart` clamp from a page. Triage lines 23–25. Carry to Plan 4 (kit).

M16. **Chip copy composed in components.** `web/app/lab/LabSurface.tsx:150–156` ("Computed · N ago" / "age unknown"), `web/app/lab/AddressWorkspace.tsx:21` (the address chips), against `lab-view`'s chips for the book. Lens (b). Carry with M1.

M17. **A legacy book withheld in every scenario is unnamed in Compare.** `web/app/lab/CompareCard.tsx:124` folds the legacy plot only when it has a point; a set whose legacy engine is refused throughout shows nothing for it. Refusals-honest law, secondary engine. Carry (Plan 3b): fold when the legacy view has any row.

M18. **Lane-side checks not yet mirrored.** `web/lib/lab-transitions.ts:178` compares the histogram's `count` unguarded (the classifier guards it upstream through `readEngine`); the after-histogram vs `to_rows`, the two tallies vs `from_rows[8]/[9]`, and the histogram's own `wad_scale` are unchecked (triage lines 1, 3). No printed number moves. Carry.

M19. **The plan's contract table lags the code.** `docs/plans/2026-09-15-ui-scenarios.md:117` (Banner `data-kind ∈ stale-input · superseded`; the code emits four, and line 113 already lists them) and `:113` (`busy` as a book `data-state` no `BookState` path produces — `web/lib/lab-view.ts:44–59`, `RunBookOutcome` has no busy). Lens (d). Fix now (plan text, two lines).

M20. **Two "never replaced" questions with no ruling.** `web/lib/lab-reading.ts:31–35,45`: a new ok body whose Cash reading is a refusal releases the hold — the computed figures vanish behind a loud, fresh refusal; and Compare has no hold at all (a failed second set replaces the plot with the failure). Not silent, so not a law break; design questions for Plan 4, with the two design notes in triage line 22.

M21. **`batch.age_seconds` is not guarded before it anchors.** `web/app/lab/LabSurface.tsx:150` passes `book.run.batch.age_seconds` to `resultReceipt`; `humanAge` floors a negative to "0s ago". Plan 1/2 convention on every page. Carry (app-wide).

M22. **A scenario id renamed by capitalisation.** `web/lib/lab-headline.ts:160` runs `sentence()` on the joined faults: an id-leading fault prints "Eth_minus_30 was answered …". Global Constraint: the config is the law and the page does not rename it. Fix now (`terminated()` or a fixed lead) — triage line 25.

M23. **"answered 0".** `web/lib/lab-headline.ts:152` prints `d.status ?? 0`; unreachable from the view (both callers pass a status) but the type invites it. Make `status` required on that arm (triage line 2). Fix now.

M24. **Copy parsed from copy.** `web/lib/lab-address.ts:208` takes the unavailable cause by `view.headline.dek.split(". ")[0]`. Needs a `lookupLoad` on `InspectorView` (Plan 2's lib). Carry.

M25. **A JSDoc that misstates the gate.** `web/lib/lab-address.ts:158` says "unreadable figures or an unknowable verdict"; the gate covers missing too. Fix now with I1.

M26. **Pins the contract lacks.** `?address=<not an address>` — the only path to `data-state="invalid"` (`web/lib/lab-address.ts:205`) — is unpinned (the e2e's "0xnope" goes through the field, which refuses inline, so the state stays `idle`); the address table's verdict column is unpinned for a projection row (I1's gap); "no denominator · +$5" is unpinned (triage line 25). Fix now: three pins.

M27. **Moved-verbatim leftovers.** `web/lib/lab-deep-link.ts:38` `listWords` duplicates `prose.joinAnd`; `:99` "were dispatched" in a notice whose docblock says present tense. Carry.

M28. **`.libMode { font-weight: 400 }`** (`web/components/kit/kit.module.css:158`) is bare, like the kit's own `:11` and `:56`; stylelint governs font-size only. Nit; drop.

## Triage of the deferred list (26 lines, by their `progress.md` line)

1. (53) Task 2 fix-round note — after-histogram vs `to_rows`, the two tallies, label identity, the histogram's `wad_scale`: **carry to Plan 4** — none changes a printed number and the classifier guards the counts upstream (M18).
2. (57) Task 3 minors: **fix now** — the three singular slips (plus M6's), `status` required on the `failed` arm (M23), the `flipped` pin to `toBe`; **carry** the recapitalised leading ticker (needs an app-wide rule); **drop** the redundant negation in `signedUsd` (harmless) and the plan's `$1.28M`/`$40.8K` examples (the pins are the truth).
3. (60) Task 2 minors — the same four lane-side checks: **carry to Plan 4** (M18).
4. (61) Task 4 rulings — the helper type, the sealed readers, the narrowing (landed in Task 5), `COVERAGE` summed, "R58 item 6" (reworded in Task 12; no R58 remains in `lab-deep-link.ts`), `SCENARIO_ID_PATTERN` unused: **drop** — every item landed or was accepted.
5. (64) Task 5 concerns — "unreadable" shares the display string type, `mover.engine` unchecked: **carry to Plan 4** — a value-or-refusal union for `MoverRow` is a model change the table would then tone.
6. (68) Task 4 minors — `heatOf` comment (the function is gone), `usd_decimals` in `outcomeLine` (moot: `readEngine` guards it), the merge-discriminating band-change pin, the `outcomeLine` docblock (present), the `DeepLinkDecision` docblock (restored), the helper's `COVERAGE` unread: **drop** — closed or moot; the merge pin is a one-liner for whoever next opens `lab-transitions.spec`.
7. (73) Task 5 deferred — the string type, `mover.engine`, a negative side printing a room (`roomFromRatio(-5n, 10n)` reads "over cap"), the `decimals: 0` stand-in (unreachable from the page: `readEngine` guards the scale first), the `no debt` arm, the `total` two-step: **carry to Plan 4** — the sign class is app-wide.
8. (77) "Task 5 complete; carried minors stay above": **drop** — a pointer, not a nit.
9. (78) Task 6 deferred: **fix now** item 6 (`sentence`/`refused` from `lab-headline`, M3 — they have already drifted); **carry** item 3 (the dek split, M24); **drop** 4 (the `roomAfter` regex — the spec now pins the tone and the exact dek), 8 (no-position names the lookup's batch — design), 9 (test-only `!`), 10 (the law comments landed in rounds 2–4).
10. (86) Task 7 — `v.rows[0]!` in the spec: **drop** — a non-null assertion on a fixture in a test.
11. (90) Task 8 minors — the pre-commit double-dispatch window, the `"__set__"` sentinel, the redundant annotation, the two law comments: **fix now** (M4) — the hook was opened by 9b23b07 and the hardening was not folded; the sentinel is a legal id.
12. (100) Task 7 minors — the four set-run arrays assumed arrays; the `accounts === 0` parity note: **fix now** — the arrays are I4 (a render-time throw, not a nit); **drop** the parity note (a zero is an answer).
13. (102) Task 6 re-review 2 residuals — dek "Room today" (the stress `before`) vs the `roomBefore` tile (the lookup's Cash) may disagree; minors 3/4/6/8/9/10; the regex: **carry to Plan 4** — the "two todays" needs a cross-response weld (refuse when the stress `before` and the lookup's position differ), which the Inspector page lacks too; the rest are decided under line 9.
14. (104) Task 9 review deferred — 3 (`busy` in the book vocabulary, unreachable) and 5 (moot after Task 10): **fix now** (plan text: strike `busy` from the book `data-state` set, M19) — a state the page cannot emit misleads Plan 4's pins.
15. (109) Task 6 re-review 4 nits — the unreachable `flips === null` arm, the JSDoc missing "missing": **fix now** (M9, M25) — I1 opens the file.
16. (114) Task 11 look deferred — the table's "−$1,069" vs the tile's "over cap by $1,069": **fix now** in the lab (I2, the table prints `sideRoomWords`); the Inspector's word is carried to Plan 4.
17. (115) `cashEngineOf`/`cashRefusalOf` pin-only exports: **fix now** — delete both and the pin (M11).
18. (117) Task 10 dataset facts — the contract examples disagree on eth's reach; the demo Book's Aave waterfall arm (0 eligible) vs its Aave card (46): **carry** — Plan 1's dataset, the owner's list; nothing the Scenarios page prints reads either.
19. (132) Task 12 rulings — (3) `Access-Control-Expose-Headers` absent so `Retry-After` is unreadable cross-origin; (6) `lab-classify.ts`'s round references: **fix now** (6) — M2, a Global Constraint on shipped code; **carry** (3) to the owner's list (API, out of plan).
20. (139) Task 11 re-review 3 — the plan's `data-banner` vocabulary; running over a hold shows "Running…" without disclosing the hold: **fix now** the plan line (M19); **carry** the design note (M20).
21. (141) Task 11 re-review 4 — "whether R13 should say both is a plan question"; the contract-table vocabularies: **fix now** — the same two-line plan edit as line 20 (counted once as work).
22. (142) Task 11 design notes — running over a hold hides the hold; a superseded-and-stale hold says superseded only: **carry to Plan 4** (M20).
23. (144) Task 13 rulings — Compare does not write `?scenarios=` (Plan 3b); the `.chart` clamp patched at the page: **carry** — Plan 3b for the URL, Plan 4 for the kit (M15).
24. (146) Task 13 review — present-tense freshness wording (taste), C26's in-flight window (precedent), the `.chart` clip: **carry** the kit item (M15); **drop** the two not-taken.
25. (149) Task 13 re-review residuals — (1) `sentence()` capitalises an id-leading fault; (2) `CHAR_W = 7`; (3) "no denominator · +$5" unpinned; (4) result order vs request order: **fix now** (1) and (3) (M22, M26); **carry** (2) (M15); **drop** (4) (immaterial under the ranking).
26. (154) Task 15 step 4 — the package and the Codex rounds: **drop** — a process line.

**Fix now: 11 lines** (2, 9, 11, 12, 14, 15, 16, 17, 19, 20, 21; lines 20 and 21 are one edit). **Carry to Plan 4/3b/owner: 10** (1, 3, 5, 7, 13, 18, 22, 23, 24, and the carried halves of 2, 9, 12, 19, 25). **Drop: 5** (4, 6, 8, 10, 26).

## Strengths

- **One reader, one verdict, one predicate — and it is true, not just stated.** `readEngine` is the only place a run-book engine is read; the library row, the workspace, the tiles, the drawer and the legacy fold all consume it (`lab-library.ts:93`, `lab-view.ts:157–158`). `rowVerdict` is the only address verdict. `definitionSkew` is asked once for the hold and once for the row, with the same arguments. The `movers_total: -1` seam pin (`lab-library.spec.ts:60–66`) proves the row and the workspace cannot disagree.
- **The hold law is complete and the pins bite.** Held from a settled ok only, through running and every failure, released only by a new ok; a retained body whose definition changed is disclosed by batch and never shown (`lab-view.ts:198–209`; e2e "a failed re-run over a held result…" both sequences). The view-model pins fail on the parent by trace.
- **Refusals are dashed everywhere the primary path can reach.** No path in `laneReading`, `readEngine`, `moversTable`, `compareRows` or `addressWorkspace` turns a null, "", or a refusal into a printed 0n/"$0"; the not-measured cell carries null through `addDebt`; `isWireOccupancy` refuses the zero-row cell; the e2e's `expectNoCashZero` and the not-covered pin's `not.toContainText("0")` guard the surface.
- **The set membership gate and the honest mock.** `setMembership` is the old R57 gate clause for clause, `mockLab` shapes the set to the POST so the happy path is honest, and one pin feeds the demo body verbatim to prove the refusal.
- **The demo weld.** The generator asserts every sum it states (histogram = margin, card = census, waterfall = after side, the naive-reshock count, the realization rule reproduced against the contract's own weETH example), refuses to write a body the clock law fails, and the weld spec re-sums 941/425/118/27 through `laneReading` itself.
- **The Computed chip anchors on the settle clocks**, so a re-selected result does not restart its age, and the receipt spec pins the arithmetic.
- **Type discipline.** No `any`; the only `as` casts are the classifier's `as unknown as Record<string, unknown>` (the walker's own gate) and a CSS custom-property cast; no `!` in shipped code.
