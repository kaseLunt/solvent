# Task 10 review — Debt: the Inspector and the Book (R11, second set)

Package: `de8dcbe..646aee9` (one commit, 19 paths, by pathspec, subject only, no attribution line; no never-touch path, no fixture JSON, no `*-snapshots/` in it). Read-only review: nothing was built, no suite was run, no server was started, no file other than this one was written. Task 9 has uncommitted edits in the tree, so every file:line below is the COMMIT (`git show 646aee9:<path>`), and every "would it have failed" is reasoned from `git show de8dcbe:<path>`.

Verdicts

- **Spec compliance: PASS WITH FIXES.** All six addendum items are closed by the letter, each with a pin that can fail and would have failed on `de8dcbe`. Three things keep it from a clean pass, none of them a regression: the distance chart applies its own zero law to one of its two bounds only (I1); the "previous lookup" disclosure has a second consumer the report does not name (I2); the census reader written for `/proof` prints a withheld engine's `0` as a count (I3, pre-existing, carried into new code).
- **Code quality: APPROVE.** Small, well-seamed changes; words moved into the lib byte-for-byte where they had to stay stable; the pixel-pinned states provably do not move; comments state laws; no dead export, no unguarded wire read, no copy newly composed in a component. No Critical.

---

## Spec compliance

| Item | | Trace | Pin — can it fail? would it have failed on `de8dcbe`? |
|---|---|---|---|
| 1 · `readCashPage` judges `page.engine` before `refused` | CLOSED | `lib/cash-rows.ts:124-126` precedes the `refused` read at `:127-134`; doc law `:113-121` | Unit `cash-rows.spec.ts:201` — yes: the parent read `refused` first and returned `{kind:"refused", code:"SWEEP_FAILED"}` (assertion failure, as reported). E2E `book.spec.ts:383` — yes: the parent would land `walk.refused` → "The Cash book could not be computed this batch." + "Collateral sweep failed."; every positive assertion fails. (The `not.toHaveText("…could not be computed…")` right after the `toHaveText` is redundant, harmless.) |
| 2 · Over-delivery wording | CLOSED | `lib/book-headline.ts:79-83` `censusFaultWords`; call site `lib/cash-book.tsx:262` (+ import `:30`) | Unit `book-headline.spec.ts:127` — can fail on any wording drift; on the parent it fails at load (no export) — it could not fail by assertion because the sentence had no unit seam. E2E `book.spec.ts:355` — yes, by assertion: parent `short = last ? delivered !== total` → "the walk delivered 2 of the 1 rows the wire advertised". This is the pin that guards the CALL SITE; the unit pin guards only the words. |
| 3 · The distance chart's mid-walk qualifier on both bounds | CLOSED as read (see "Item 3" below and I1) | `lib/cash-summary.ts:91-95` `walkQualifier`, `:117-134` `bandsFinding`; `app/book/BookSurface.tsx:41,49,206-231` | Unit `cash-summary.spec.ts:82,136` — can fail (exact strings in all four registers + settled `$0`); parent fails at load. E2E `book.spec.ts:256-262` (pending walk), `:305-311` (census stop), `:314` (held page two) — yes by assertion: the parent printed "$0 sits within 10% of the cap · walking…" and had no `book-bands-note`. |
| 4 · Empty `waterfall.points` is a named refusal | CLOSED | `lib/cash-view.ts:111-114` `emptyGrid`, `:194`; the card prints `Preview withheld: {reason}.` (`app/book/StressPreview.tsx:34`), the Overview's line inherits it (`cash-view.ts:231`) | Unit `cash-view.spec.ts:131` — yes: parent `stressPreview` → `[].every(…)` is true → `{kind:"absent"}` (`lib/stress-preview.ts:79`). E2E `book.spec.ts:344` — yes: parent renders "The Cash engine is not on this batch's stress grid." |
| 5 · Resume/stress disclosure as data on the view | CLOSED for the Inspector; second consumer open (I2) | `lib/address-lookup.ts:40,44,100,131,174`; `lib/inspector-view.ts:102,219,299,395-417` | Unit `inspector-view.spec.ts:507` — yes (field undefined on the parent). E2E `inspector.spec.ts:376` — yes: the parent's note reads "Stress for batch 18,251; the position above is batch 18,252." and the row label "batch 18,251". It also pins the premise (stress request count unchanged across the repair). |
| 6 · Verification reads the census only | CLOSED | `app/proof/VerificationSurface.tsx:56,78-87,113`; `lib/verification-view.ts:42,45,52-56,66-70` | Unit `verification-view.spec.ts:57` — can fail (guard, mapping, the `null` arms); parent fails at load. E2E `verification.spec.ts:181` — yes by assertion: the parent mounted `useCashBook`, whose walk effect asks `/v1/positions` page one as soon as the book lands → `asked.positions === 1`. |
| StressTable negative-room word | n/a | moved to Task 9 by the addendum; not in the diff | — |
| Process | ✅ | 19 paths, pathspec, no attribution; no prettier signature in `web/lib/**` / `web/tests/**` hunks; unit count +6 | — |

### Item 2 — is the `cash-book.tsx` touch the smallest honest change?
Yes. The sentence lived at `cash-book.tsx:262` as an inline template, so ANY fix of the wording touches that line; the plan's guess of `cash-summary.ts` / `book-headline.ts` was wrong about where the words were. (It was already "in `web/lib`", so the copy law was not broken before — the move is for the unit seam: `cash-book.tsx` is a hook file the node unit project cannot reach.) The touch is one import and one call, the short-walk bytes are unchanged (the existing pins `book.spec.ts:296` and `book-headline.spec.ts:120` still hold), `book-headline.ts` imports nothing from `cash-book.tsx` (no cycle), and Task 9's files are disjoint from it.

### Item 3 — is the reading honest and complete?
Honest, and the right reading of "both bounds": the card carried two walk-derived lower bounds (the finding's figure; the bars' dollars and counts) and one trailing qualifier. Checked figure by figure on the card at `646aee9`:

- **The finding's figure** (`bandsFinding`, sum of bands `0-2`/`2-5`/`5-10`) — walk-derived. Mid-walk positive: "at least **$X** sits within 10% of the cap · {qualifier}". Settled: unchanged bytes. Correct.
- **Each bar's dollars and count** (`components/kit/BandBars.tsx:49-64`, `humanUsd(band.value)` · `count`) — walk-derived; covered by `book-bands-note` in both registers. Rows only append within a walk, so "lower bound" is true of each bar.
- **Nothing on the card is a census figure.** The census figures on the page — `SectionHead` "1,412 borrowing accounts" (`BookSurface.tsx:90`), Debt outstanding, Standing bad debt, Not computed — do not wear the qualifier. The median tile (not a bound at all mid-walk) is pending while walking and dashed when stopped (`:162-175`). No figure wrongly wears "lower bound".
- **"—" for a walk-derived zero is right**: it is the tiles' law (`BookSurface.tsx:143-145`: a figure only when `count > 0 || settled`) and the headline's (`nearCapSentence(…, complete)`: a zero mid-walk is silence). It returns to "$0" on completion: `bandsFinding` tests `summary.settled` first (`cash-summary.ts:122`), pinned by `cash-summary.spec.ts` (`figure: "$0"` over the settled committed page).
- `walkQualifier(summary)` is equivalent to the parent's `walking ? … : walkStopped !== null ? … : ""` in every reachable state (`summary.stopped = walkComplete ? null : walkFailure.message`, `cash-summary.ts:53`; `view.walking = !walkComplete && walkFailure === null`, `cash-view.ts:187`), so the Needs-attention card's line does not move.

**Not complete in one respect — I1:** the zero law is applied to the finding and not to the bars.

### Item 5 — does `lookupRepaired?` (optional) weaken the law?
At the type level, yes: a caller that builds an `AddressReading` literal can omit it and `deriveInspectorView` reads absence as `false` — silently no disclosure. In production, no: `useAddressLookup` is the only producer (`address-lookup.ts:174`), and both consumers (`app/inspector/[addr]/InspectorSurface.tsx:29-31`, `app/lab/LabSurface.tsx:90-93`) pass the hook's reading through whole. The only literal the optional protects is Task 9's `tests/unit/lab-address.spec.ts:13` `reading()` helper. Make it required once Task 9 has landed (M2). `InspectorView.stressFromPreviousLookup` IS required, which is where it matters for view consumers.

`StressTable.tsx` at `646aee9` does render the seam: `:33` `const note = stressBatchNote(view)`, `:54` `note.rowLabel` on every row, `:89-93` `note.disclosure` in `role="note" data-testid="inspector-stress-batch"`. Verified — no wiring needed **for the Inspector**. (Task 9's working-tree `StressTable.tsx` still calls `stressBatchNote` at `:35,56,93`, so the seam survives their edit as of now.)

Repair mechanics checked: a repair that rides an in-flight foreground load lands `repaired: false` (closure of the foreground call, `:90,100`); a failed repair keeps the previous result with its previous flag (`:107-109`); `reload()` lands `repaired: false` and replays the stress under the new epoch; same batch on both sides → `null` note (right: one batch's stress is that batch's).

### Item 6 — what was checked
- `pipelineSteps` consumes `reading.phase`, `reading.book.batch.id`, `reading.book.batch.computed_at` and `cashAccounts` only (`verification-view.ts:255-277`). No walked row. The census is enough.
- The compute step still names its batch and count: value `n(book.batch.id)`, sub `batch · {n} Cash accounts`, sentence "Batch N computed at …" — same strings, pinned by `verification.spec.ts:66-74` (existing) and `:181` (new).
- The Overview is untouched: `app/overview/**` is not in the diff; `OverviewSurface.tsx:190` still passes `reading={reading} cashAccounts={view.positions}`; `Pipeline.tsx`'s prop is still `reading: BookReading`.
- Failed / 503: named, never a zero, never an absent step — `bookFailed` (`:52-56`) keeps the hook's mapping (compare `cash-book.tsx:143-146`); the step prints `—` + "no servable batch" / "unavailable" with tone `refused` (`:257-267`); existing e2e pins at `verification.spec.ts:233-262` cover both. A malformed `positions` throws by name into the route boundary (`app/error.tsx`), as the parent did through `deriveCashView` — and the page now throws for fewer fields it does not print (`value_decimals`, `computed_positions`, `refused_positions` are no longer read on `/proof`).
- **Withheld whole inside a 200 book: not a named state on this step — I3.**
- Resume: `/proof`'s evidence and meta asks never had a resume repair (one-shot effect, `[]` deps, both at `de8dcbe` and now). The parent's compute step alone refreshed on resume — which could set a fresh compute batch beside the one-shot "Live batch" chip and Index step. Now all three share one load epoch. A stale census after resume is not flagged as stale, but it is never undated: the step's sentence carries `computed_at` as an absolute time, and `/proof` never rendered an age. Consistent and arguably more coherent than before; whether `/proof` wants a resume repair for all three asks is an owner's call — carry-forward, not a defect of this task.

### The two e2e assumptions
- **`\$[\d.,]+[KMB]?` vs `humanUsd`** — holds. Demo page one's near-cap rows: 27 accounts, `830422189997` at 6 decimals (computed from `positions-dm-demo-page-1.json` with the bands' own cross-multiplied edges; page two adds none). `humanUsd` (`lib/human-usd.ts`): dollars ≥ 10,000 → `$` + `groupThousands(dollars/1000)` + `K` = **`$830K`**. "at least $830K sits within 10% of the cap · walking the book, figures are a lower bound" matches the first regex; "· $830K sits within 10% of the cap" matches the second; after release the card holds neither "at least" nor "lower bound" (the Needs-attention card is a different element).
- **`asked.book === 1` on `/proof`** — holds. Callers of `/v1/book` at `646aee9`: `useCashBook` (`cash-book.tsx:130`, mounted by `BookSurface` and `OverviewSurface` only) and `VerificationSurface.tsx:79`. The shell asks nothing of the book: `layout.tsx` mounts `PostureProvider` (the SSE stream only, `posture.tsx:210`), `MetaConstantsProvider` (`/v1/meta`), `AppShell`, `DegradationBanner` — no `.book(` / `/v1/book` in any of them. The client sets only `Accept` (no preflight) and has no retry. Production build → no StrictMode double effect. The pin compares `pathname === "/v1/book"`, so the nav's `/book` prefetch is not counted.

### The two residues
- **"The walk stopped before the last page (the walk delivered 2 rows for a census of 1)"** — carry-forward, not Important for this task. The frame is equally false for the SHORT terminal page, where it is pre-existing and pinned (`book.spec.ts:299`, `book-headline.spec.ts:120`): both census faults strike ON the last page. Fixing it means `walkSentence` / `bookHeadline` learning the stop's kind (census fault vs transport), i.e. a register threaded through `CashSummaryInput` — more than the wording the item names. The new e2e pin asserts only the cause words, so it does not weld the frame further. One thing to carry with it: under OVER-delivery, "every figure is a lower bound" / "at least $X" is not provable — the landed rows (`cash-book.tsx:258-262` keeps them) may hold duplicates, so the sums may exceed the book. Pre-existing claim; this task's "at least" restates it in the state where it is weakest.
- **Old stress beside a new lookup after `reload()`** — carry-forward. `useKeyedFetch` keeps the previous result until the new one lands (`address-lookup.ts:52-69`), so the window is bounded by one stress request and ends in a fresh stress or a stated error; while it lasts a batch skew is disclosed by the plain mismatch note, and a same-batch stress is the same data. History has the same window. Recording the load epoch on both results is the clean close; not this item.

### Pixel-pinned primary states
Neither moves.
- **Demo Book**: the pin waits for `book-kpi-near` not `aria-busy` (`screenshots.spec.ts:51`) → `walking` false. Demo census 1,412 = page one 1,000 + page two 412, `next_cursor: null` on page two → the walk completes → `bandsFinding`'s settled arm: `lead` + `<b>$830K</b>` + " sits within 10% of the cap", `barsNote: null`. The parent's JSX text (`…counts printed ·{" "}<b>…</b> sits within 10% of the cap` + empty `walkNote`) is the same string; the new fragment adds no element. The demo waterfall has 6 points → `emptyGrid` returns `null` → `stressPreview` as before (Book and Overview).
- **Demo near-address Inspector**: `DEMO_STRESS_NEAR.batch.id` = `DEMO_ADDRESS_NEAR.batch.id` (18,251) and no repair runs → `stressBatchNote` is `null`, as before.

---

## Strengths

- Words moved, not rewritten: the two walk qualifiers and the short-walk census sentence are byte-identical to the parent's, so every existing pin holds and the settled Book renders the same pixels — verified, not assumed.
- Item 6 was read before it was written: the claim "no walked row is read" is exact, the census helper uses the same guard and field name as `deriveCashView`, and the failure mapping is the hook's own. `/proof` also stops crashing on three malformed fields it never printed.
- Item 5's flag is set at the one place the fact is known (the landing of a `keepOnFailure` request) and the ride-an-in-flight-load arm is correctly NOT marked. The e2e pins the premise (no stress request across the repair), not only the words.
- `emptyGrid` also turns a non-list `points` from a render-time `TypeError` into a named refusal, and the withheld engine's cause still wins (pinned).
- Honest report: the three load-failure "fail-firsts" are called what they are, and the e2e pins that fail by assertion on the parent are identified for each.
- Comments state laws; none names a task, round or review.

## Issues

### Critical
None.

### Important

**I1 · The distance chart states "a zero is claimed only by a complete walk" and prints "$0 · 0" bars under it — NOT plan-mandated by letter; law-mandated (Global Constraint: an unknown value never looks like a zero; and the item's origin: the chart was "softer than the tiles' dash-until-settled").**
`bandsFinding` dashes the finding's walk-derived zero (`cash-summary.ts:130-132`), but every unread band still reaches `BandBars` as `value: 0n, count: 0` (`BookSurface.tsx:34-40`) and prints `$0 · 0` (`BandBars.tsx:49-64`). On a pending walk that is all seven bars; on any running walk it is the ≥ 50% band — where most of the book lives — until page two lands. The note beneath does qualify them ("every bar and every count is a lower bound"), so no figure is *unqualified*; but the card applies its zero law to one of its two bounds, and its own sentence contradicts the bars above the note. The pins were narrowed around this rather than reporting it: `book.spec.ts:260,308` assert `not "$0 sits within 10%"`, while the withheld-state pin on the same card asserts `not "$0"` outright (`:140`). Fix shape: an unsettled zero band prints `—` (and no `· 0`) — the mapping is `BookSurface.tsx:34-40` (owned) but the count is printed by the kit's `BandBars` (not owned), so it needs either a `value: null` + `count: null` arm in the kit or a controller ruling that a noted `$0 · 0` bar is acceptable. Controller's call whether this rides Task 10's fix round or goes to the kit's owner; it should not be lost.

**I2 · The "previous lookup" disclosure has a second consumer, and the report says there is nothing to wire — integration, not the implementer's files.**
`app/lab/LabSurface.tsx:90-93` mounts the same `useAddressLookup` and derives the same `InspectorView`; the lab's address workspace has the only literal "Stress for batch" CHIP in the product (`app/lab/AddressWorkspace.tsx:21-27`) and its own cross-batch headline (`lib/lab-address.ts:242-243`). After the same resume repair it says "Cannot say — the stress result is for batch N; the position above is batch M." — a named mismatch, never silent, but without "from the previous lookup". The plan's phrase is "beside the stress batch chip". Both files are Task 9's / never-touch for Task 10, so the implementer was right not to edit them; the field is already on the view (`view.stressFromPreviousLookup`), so the wiring is two reads in `lab-address.ts` + the chip. For the integrator after Task 9 lands — the report's "Nothing for the integrator to wire" is true of the Inspector only.

**I3 · `cashCensus` prints a withheld Cash engine's aggregate count — by the contract's own refused-engine shape, `0` — PRE-EXISTING, not a regression, not plan-mandated; carried into new code whose docstring says "no count, never a zero".**
`verification-view.ts:66-70` reads `engines[debt_manager].positions` whenever the engine is listed; it does not consult `refused_engines` or `engine.refused`. The contract's refused-engine example carries `positions: 0, computed_positions: 0` (`tests/fixtures/book-engine-refused.json`, the Aave arm), so a Cash engine withheld the same way prints "batch · 0 Cash accounts" at tone `neutral` on `/proof`. The parent did the same through `deriveCashView(...).positions`, and the Overview still does (`OverviewSurface.tsx:190`); no pin covers the compute step under a withheld engine on either page. The fix is three lines in an owned file (`null` when the book withholds the engine → "unavailable Cash accounts"), but doing it on `/proof` alone splits "both pages read one law", and the brief says to leave the Overview's use unchanged — so this needs a ruling: fix both through one reader, or ledger it. (Same family, outside this task's items: the Book's `SectionHead` prints "0 borrowing accounts" under a withheld engine, `BookSurface.tsx:90`.)

### Minor

- **M1 · `emptyGrid` runs before the waterfall's own `excluded_engines` check.** `cash-view.ts:194` → a waterfall that excludes the Cash engine by name AND serves no points now says "no points published" instead of the engine's cause (`stress-preview.ts:76-77`). The book-level withheld cause is protected (and pinned); the waterfall-level one is masked by a less specific refusal. Also: the guard lives in the caller, so a second caller of `stressPreview` re-opens the `absent` hole. Home it in `stressPreview` after the `excluded_engines` check when that file is in someone's list.
- **M2 · `AddressReading.lookupRepaired` optional** (`address-lookup.ts:40`) — make it required once Task 9's `lab-address.spec.ts` literal can take the field; see the item 5 note.
- **M3 · Three copies of the near-cap band set** — `NEAR_BANDS` (`BookSurface.tsx:19`, still used for tone), `NEAR_BAND_IDS` (`cash-summary.ts:98`), `NEAR_CAP_BANDS` (`cash-rows.ts:204`). And two derivations of "within 10% of the cap" on one page: the chart sums the BANDS (every computed row in them), the Near-cap tile uses `summary.nearCap` (excludes a liquidatable verdict). They agree on the demo (27 · $830K); pre-existing; one source would make disagreement impossible.
- **M4 · `bookFailed` is a second copy of the hook's failure mapping** (`verification-view.ts:52-56` vs `cash-book.tsx:143-146`). Correctly identical today; the hook should call the lib function when `cash-book.tsx` is next open, or `/proof` and the Overview can drift on the 503 law.
- **M5 · The hook half of item 5 has no unit seam.** `repaired: keepOnFailure` and the ride-an-in-flight-load arm are reachable only in a browser; the e2e pins the first, nothing pins the second (a repair that rides a foreground load must NOT be marked).
- **M6 · Stale comment outside the owned list:** `app/proof/page.tsx:9` still says the steps come from "the Overview's meta and book readers". `/proof` no longer mounts the Overview's book reader.
- **M7 · The zero-arm sentence reads oddly beside a dash:** "— within 10% of the cap: a zero is claimed only by a complete walk · walking the book, figures are a lower bound" — "figures are a lower bound" qualifies a figure the sentence has just declined to print. Copy only; `bandsFinding` is the one place.
- **M8 · `BookSurface.tsx:233`** still composes "Material first, then by room" + `walkNote` in the component. Pre-existing; the qualifier half is now the lib's, the lead is not.
- **M9 · Over-delivery residues** (the "before the last page" frame; "lower bound" unprovable under over-delivery) — see "The two residues"; ledger both together.

## Assessment

The task did what the addendum asked, six for six, with pins that are real: I traced each one against the parent's code and each fails there — three by assertion in the unit project, all six by assertion in the e2e project (unrun). The two riskiest changes are the cleanest: `/proof` genuinely needed only the census, and the pixel-pinned Book provably renders the same settled bytes. The `cash-book.tsx` touch is the smallest honest change available — the sentence lived on that line.

What is left is about completeness of laws rather than correctness of code. Item 3's reading is the right one, but it stops one step short of itself: the card now says a zero is claimed only by a complete walk while its bars print zeros (I1) — that needs the kit or a ruling. Item 5's data field is right and rendered on the Inspector, but the lab's address workspace is the same hook, the same repair and the only real "stress batch chip", and nobody has been told to wire it (I2). And the new census reader inherits an old hole: a withheld engine's `0` is printed as a count on `/proof` and the Overview alike (I3) — a ruling, because fixing one page alone breaks "one law, two pages".

None of these blocks the integrator's e2e run; the two e2e assumptions the implementer flagged both hold by reading.

### Not verified

- Nothing was run: `tsc`, `eslint`, the unit project (920) and every e2e pin are taken from the report or reasoned from source. **No one has yet observed any of the seven new e2e tests in a browser.**
- The pixel pins were not rendered; "does not move" is from the text/DOM of the settled arm and the demo fixtures (walk completes 1,000 + 412 = 1,412; six waterfall points; stress and address both batch 18,251), not from a capture. Both themes and the `book-bands-note` line's layout at 390px mid-walk were not seen.
- Playwright's whitespace normalisation for RegExp `toContainText`, and that an intercepted cross-origin GET surfaces exactly one `request` event, are from memory of Playwright's behaviour, not from its source. The `$830K` figure was recomputed from the fixture with my own re-implementation of the band edges, not by calling `roomBands`.
- `PageTransitionEvent("pageshow", {persisted:true})` reaching `repair()` was taken from the two existing resume tests that use the same dispatch (`inspector.spec.ts:291,363`); `useAnchoredAgeSeconds` was not re-read.
- The real wire's shape for a whole-engine-withheld `debt_manager` (I3) is inferred from the contract's Aave example (`positions: 0`); the Go handler was not read.
- Task 9's working tree was consulted once, read-only, only to confirm `StressTable.tsx` still calls `stressBatchNote`; nothing else of theirs was reviewed, and their final commit may differ.
- The origin of "beside the stress batch chip" (Inspector row label vs the lab's chip) could not be settled from the ledger: `progress-ui-overhaul.md:4483` says only "the resume repair moving the lookup but not the stress".

---

# Re-review 1 (3066133)

Package: `6df392a..3066133` (one commit, 19 paths, by pathspec, subject only, no attribution line). Read-only, as before: nothing built, no suite run, nothing written but this section. Task 9's and the integrator's uncommitted edits are not part of it; every file:line is the COMMIT (`git show 3066133:<path>`), every "would it have failed" is reasoned from `git show 6df392a:<path>`.

**RE-REVIEW: closed.** I1, I2 and I3 are each CLOSED with a pin that can fail and would have failed on the parent. The round introduced no Critical and no Important. What remains is carry-forward for the Task 12 wave — one of them wider than the controller's note (b) has it (see "(b)" below).

## Rulings

| Ruling | | Trace | Pin — can it fail? would it have failed on `6df392a`? |
|---|---|---|---|
| I1 · mid-walk a zero-so-far band is "—", no count, zero height; positive-so-far prints; settled prints "$0 · 0" | CLOSED | WHO DECIDES: `lib/cash-summary.ts:152-159` `bandsSoFar` (`count`/`debt` null unless positive while `!settled`; identity once settled). `BookSurface.tsx:34` passes through (one line). Kit: `BandBars.tsx:8-9` `count: number \| null`; `:31-33` a null never reaches the guard, every non-null still passes `readWirePopulation`; `:36` weight `?? 0` / `?? 0n`; `:64` no `data-count`; `:68` no `<small>` | Unit `cash-summary.spec.ts:136` — yes (fails at load on the parent: no export; can fail by assertion on either arm flipping). E2E `book.spec.ts:260-269` (pending), `:317-327` (census stop: "$4,200 · 1" stands, six dashes, `[data-count]` ×1), `:364` (settled "$0 · 0", `data-count="0"`) — yes by assertion: the parent printed "$0 · 0" on every unread bar. |
| I2 · the lab's chip discloses a repaired lookup from the ONE lib sentence; `lookupRepaired` required | CLOSED | `lib/inspector-view.ts:387-394` `StressBatchNote.chipValue`; `:411,416` and `:425,428` — `rowLabel` is literally `` `batch ${chipValue}` ``. `lib/lab-address.ts:159-162` takes the note's `chipValue`; `:178-184` the dek is the note's `disclosure` after a repair, the old sentence byte-for-byte otherwise. `app/lab/AddressWorkspace.tsx:27` prints `space.stressBatchChip`. `lib/address-lookup.ts:40` `readonly lookupRepaired: boolean` | Unit `lab-address.spec.ts:363` + the `chipValue` arms in `inspector-view.spec.ts:485-545` — yes by assertion (no `chipValue`, no `stressBatchChip` on the parent). E2E `lab.spec.ts:599` — yes: the parent's chip reads "18,251" alone and its dek is the plain sentence. |
| I3 · one law, two pages: a withheld census is a refusal, read by one reader | CLOSED | `lib/verification-view.ts:66-69` `CashCensus`; `:81-93` `cashCensus`; `:252` `pipelineSteps(meta, evidence, reading)`; `:280,293-307` the refused arm (value = the batch, sub "batch · Cash accounts withheld" / "batch · Cash engine not in this batch", tone `refused`, the sentence names the cause). `VerificationInput.cashAccounts` gone (`:689-693`); `Pipeline.tsx:7-12,39-42` prop gone; `OverviewSurface.tsx:190` | Unit `verification-view.spec.ts:87` — yes by assertion: the parent's `cashCensus` returned `2` / `0` and the step was neutral. E2E `verification.spec.ts:204`, `overview.spec.ts:89` — yes: the parent prints "batch · 0 Cash accounts" / "batch 1 · 0 Cash accounts". |
| M2 taken | ✅ | required field; both literals carry it (`inspector-view.spec.ts:30`, `lab-address.spec.ts:25`); the hook's return (`address-lookup.ts:174`) is the only other builder of a reading — `git grep AddressReading` at the commit finds no fourth | — |

### I1 — the checks asked for
- **Every non-null count still passes the guard:** `countOf` (`BandBars.tsx:31-33`) is called for every band in the render (`:50`) and, under count weighting, inside `weight` (`:36`). Only `null` short-circuits.
- **`BookLegacy.tsx` unchanged:** not in the diff; `LegacyBand.count` is a `number`, so its bars never see the null arm. `count?.toLocaleString(...) ?? "—"` (`:55`) is the same string for a number; `data-count={count ?? undefined}` is the same attribute. Settled Book DOM is the same too (`bandsSoFar` is the identity there) — the pixel-pinned demo Book does not move.
- **The weight arithmetic cannot be skewed by a null:** a null weighs `0n` in both modes, so it cannot raise `max` (`:40`), and `px` is `0` whenever `w === 0n` (`:52`) — `MIN_PX` applies only to a positive weight. A dashed band therefore has no bar; a positive band's height is relative to the positive bands only, as before.
- **The e2e pins assert `not "$0"` outright mid-walk:** `book.spec.ts:260,317,353`. (On the held-page-two test the line cannot fail on the parent either — demo page one reads in all seven bands: $6,949 · $282K · $115K · $432K · $2.8M · $7.9M · $8M — but the pending and census-stop pins do, so the law is pinned.)
- **`toHaveCSS("height","0px")`:** `.bar i { display:block; width:100% }` (`kit.module.css:87`) sets no min-height and the inline style is `height: 0px`; by reading, the computed value is `0px`.
- **"— · N" (accounts read, no debt between them): honest — keep it.** The count is a positive read and a true lower bound ("at least N accounts in this band"); dropping it would hide something the walk knows, which is the opposite fault. The dash declines only the dollar claim, which is the card's own sentence ("a zero is claimed only by a complete walk"). The pattern is the product's existing one — refused rows are "counted, not hidden" beside a dashed figure. Reachable only by zero-debt computed rows (they land in ≥ 50%); unit-pinned (`cash-summary.spec.ts`, the `debtless` arm).

### I2 — the checks asked for
- **The phrase exists once:** `git grep "previous lookup"` over `web/lib web/app web/components` at the commit hits only `inspector-view.ts` (the label `:409`, the two disclosure heads `:414,:423` — one function) and one doc comment in `lab-address.ts:44`. Neither page composes it: `StressTable.tsx` prints `note.rowLabel` / `note.disclosure`, `AddressWorkspace.tsx:27` prints `space.stressBatchChip`.
- **The lab e2e drives the resume by `pageshow`**, exactly as the Inspector's twin does (`lab.spec.ts` new test; `inspector.spec.ts:376`), and pins the premise (no stress request across the repair). The lab mounts the same `useAddressLookup` (`LabSurface.tsx:90`), whose own `useAnchoredAgeSeconds(…, repair)` carries the repair regardless of the page.
- Not from this round, for the lab's owner: when the stress body names NO readable batch the lab shows no chip, no disclosure, and compares the tiles (`lab-address.ts:159,178` both require `stressBatchId !== null`) — the Inspector says "batch not readable … not compared". The note now carries `chipValue: "not readable…"`, so the lab could print it; the ruling said "present exactly when the chip showed before", so this is a carry-forward, not a miss.

### I3 — the checks asked for
- **The contract reading is right.** `api/openapi.yaml:2452-2505` `Aggregate`: `positions`, `computed_positions`, `refused_positions` are in `required` and typed `integer` (not nullable); `refused` — "True when this ENGINE's whole book is withheld, whatever the position counts say"; `total_collateral` — "NULL on a refused engine, never '0'". `EngineRefusal` (`:2379-2401`) — a refusal "survives an empty account set … persists a zeroed rollup carrying only the code". `cmd/api/handlers.go:331-361` copies the five counts verbatim and, on `RefusalCode != ""`, sets `Refused`, fills `Refusal`, leaves both totals nil. So a withheld card's counts are placeholders, and the contract's own example (`book-engine-refused.json`) carries 0. Reading them as a census was the defect.
- **The Overview's served arm is byte-identical.** `Pipeline.tsx`'s JSX is unchanged apart from the prop (`:42-53`: ordinal, name, description, `{before}<b>{figure}</b>{after}`, `data-value`). The count arm's strings are character-for-character the parent's (`verification-view.ts:308-317` vs the parent's `:268-277`), and `census.accounts` is `readWirePopulation(engine.positions)` over the same book the parent's `view.positions` read. Demo: `refused_engines: []`, `debt_manager.refused: false`, `positions: 1412` → "batch **18,251** · 1,412 Cash accounts" at both revisions. The pixel pin does not move.
- **The refused step, both pages.** `/proof` tile: value "1", sub "batch · Cash accounts withheld", `data-tone="refused"` (`VerificationArchitecture.tsx:39-41` renders `step.tone`), sentence "Batch 1 computed at …; the Cash book is withheld this batch (collateral-flag custody unproven)." Overview: "batch **1** · Cash accounts withheld", `data-value="1"`. The Overview's `Pipeline` renders no tone for ANY step (pre-existing — "unavailable" is likewise words only), so there the refusal is carried by the words alone. The batch still prints, which is right: the batch was computed; its census is what is refused.
- **The widening: the right call, and proportionate.** A `cashCensus` that returns a refusal while `pipelineSteps` still takes a `cashAccounts` argument leaves "one law" as a convention — the Overview was handing it `deriveCashView(...).positions`, a DIFFERENT reader, which is precisely how the two pages could disagree. Reading the census inside `pipelineSteps` makes the second reader unrepresentable; the cost is one prop and one argument removed, the happy bytes unchanged, and one unreachable arm ("unavailable Cash accounts") deleted with its pin rather than left to rot. Round 0's instruction was to keep the prop NAME `reading` and the Overview's use of it — both are kept. No dead export is left behind: `cashAccounts` survives nowhere (`git grep` at the commit), `cashCensus`/`CashCensus` are read by `pipelineSteps` and the pin, `view.positions` is still read on the Overview (see (b)).

## The implementer's two questions

**(a) `cashCensus` restates `cash-book.tsx`'s private `wholeRefusal` — is there drift TODAY? No, not on any contract-conforming input → carry-forward, not Important.**
`wholeRefusal` (`cash-book.tsx:98-107`): listed in `refused_engines` → withheld with the head's code/detail; else the card's `refused` → withheld with `refusal?.code ?? ""`; else null. `cashCensus` (`verification-view.ts:85-88`): the same two sources in the same order (the head's entry wins). They agree on withheld-or-not for every book whose `refused` is a boolean, and on the CAUSE: `plainCause("", "")` and `plainCause("", undefined)` are both "the engine gave no reason" (`refusal-phrasebook.ts`), and a coded refusal resolves through the same phrasebook. The only inputs on which they disagree are out of contract: a truthy non-boolean `refused` (`"true"`, `1`) — the hook tests truthiness, the census tests `=== true`, so the Book would say withheld while the pipeline prints the count. Not a today-defect; it is the argument for one pure home (`lib/` module with the predicate and the missing-engine words — "the Cash engine is missing from this batch" now also exists twice: `cash-view.ts:159`, `verification-view.ts:90`). That home closes M4 as well.
One thing that is NOT drift but should be written down beside it: the hook's `refusedWhole` has a third source, the positions endpoint refusing the engine (`cash-book.tsx:331`), which is not part of `wholeRefusal` and which a book-only reader cannot see. On the Overview that state shows a refused strip above a neutral "N Cash accounts" step — at both revisions. The count there IS the book's own statement, so it is honest; it is simply the one state in which the page's two halves speak from different evidence.

**(b) The Book under a withheld Cash engine — confirmed, and wider than two sites.**
At `3066133`, `deriveCashView` reads all three counts whenever the engine is listed, withheld or not (`cash-view.ts:125-129`), and they print at FIVE sites:
1. Book `SectionHead` — "N borrowing accounts" (`BookSurface.tsx:90`);
2. Book Coverage chip — "C / N computed", tone `neutral` (`cash-view.ts:176-183`);
3. Book "Not computed" tile VALUE — `String(view.refusedPositions)` (`BookSurface.tsx:197`), beside the withheld cause as its sub;
4. Overview live strip "Accounts" stat — `view.positions` (`OverviewSurface.tsx:148`);
5. Overview Coverage chip — the same `view.chips` (`OverviewSurface.tsx:135`).
With the contract's shape those read "0 borrowing accounts", "0 / 0 computed", "0", "Accounts 0", "0 / 0 computed". The existing pin (`book.spec.ts:108`, "…nothing rendered as zero") does not look: it asserts the verdict, five dashed tiles (`debt, liquidatable, near, median, baddebt` — `notcomputed` is outside the loop and is only checked to CONTAIN the cause), the bands card, the preview, no crit tone, and `not "$0"` on two elements. And its fixture leaves the served counts standing (`{ ...e, refused: true, total_debt: null, total_collateral: null }` → positions 2, computed 1, refused 1), so it could not see a zero even if it looked — it would see "2 borrowing accounts" and "1 / 2 computed", placeholders of the other kind. The Overview twin (`overview.spec.ts:65`) has the same fixture shape and asserts `body not "$0"` only.
After THIS round the Overview carries both answers on one page under the contract's shape: the pipeline says "Cash accounts withheld" while the strip above it says "Accounts 0". That is an improvement over two zeros, not a regression, but it is the reason sites 4–5 belong in the same Task 12 item as the Book's.
How `cashCensus` would answer: `{kind:"refused", reason:"withheld", code, cause}` for all five — it gives the verdict (enough to dash a count and tone a chip `refused`) but only the `positions` count, not `computed`/`refused`. The smaller fix is inside `deriveCashView`, which already holds `withheld` (the hook's `refusedWhole`, all three sources): `positions`/`computedPositions`/`refusedPositions` → `null` when `withheld !== null` fixes all five sites at once ("accounts unavailable", Coverage "unavailable" in the refused tone, "—"). The Task 12 item should also re-shape both pins' fixtures to the contract's zero counts and assert the five sites, or the title "nothing rendered as zero" stays untested.

## Left Minors — should any not wait for Task 12?
None needs to jump the queue. M1, M3, M5, M6, M7, M8, M9 are unchanged by this round and none is reachable on a pinned state. M4 grew by one (the `wholeRefusal` restatement and the second copy of the missing-engine words) and is best taken together with (b) as one item: one pure home for "is the Cash engine withheld, and why", read by the hook, `deriveCashView` and `cashCensus`. The reasons given for leaving each are sound (M7's sentence is pinned verbatim in three tests; M1's honest home is a file not opened).

## Anything the round introduced
- **A pin that cannot fail:** none that matters. Two weak lines: `book.spec.ts:353` (`not "$0"` on the held demo walk passes on the parent too — all seven demo bands are positive on page one), and the older sum at `book.spec.ts:410-412` (foreign engine → `[data-count]` sums to 0) is now satisfied by the ABSENCE of the attribute rather than by seven zeros — it still fails if a foreign row enters a band, but `toHaveCount(0)` would say what it now means.
- **Copy composed in a component:** none new. `AddressWorkspace.tsx:27` prints the lib's string; `:103` (the section qualifier composing both batches) is pre-existing and Task 9's.
- **An unguarded wire read:** none. A withheld card's count is deliberately not read at all (pinned: a `-0` placeholder does not throw); a served count still passes `readWirePopulation` and still throws by name into the route boundary.
- **A comment naming the round or the review:** none in the added lines.
- **Dead exports / leftovers of the removed parameter:** none (`cashAccounts` is gone from `web/`; no orphaned import in `VerificationSurface.tsx` or `OverviewSurface.tsx`). `lab-address.ts:178` repeats the `crossBatch` condition it already holds at `:159` — for narrowing; trivial.
- **Words:** a withheld cause that arrives as free-text `detail` ending in a period would print "(… .)." in the step sentence — `bookHeadline` trims that (`causeWords`), the step does not. Unreachable for the phrasebook codes; copy-level.

## Not verified (re-review)
- Nothing was run: `tsc`, `eslint`, the unit project (923) are the report's; **none of this round's seven new or widened e2e pins has been observed in a browser**, nor have round 0's.
- The pixel pins were not rendered. "Does not move" is from the JSX and the step strings at both revisions and from the demo fixtures (`refused_engines: []`, `debt_manager.refused: false`, 1,412 positions; the settled walk), not from a capture.
- `toHaveCSS("height","0px")` is reasoned from `kit.module.css:85-93` and the inline style, not from a computed style in Chromium; no other rule targeting `.bar i` was found by grep, but media-query or theme overrides elsewhere were not exhaustively searched.
- That the lab's `pageshow` reaches `repair()` exactly once (`expect.poll(() => repairs).toBe(1)`) rests on `useAddressLookup` being mounted once in `LabSurface` and on the Inspector twin's behaviour; `useAnchoredAgeSeconds` was not re-read, and whether the lab's other resume listeners could also ask `/v1/address/*` was checked only by grep for callers of `.address(`.
- The demo page-one band sums were recomputed with my own re-implementation of the band edges, not by calling `roomBands`.
- The Go side was read only at `cmd/api/handlers.go:325-365`; where `RefusalCode` is persisted and whether a withheld `debt_manager` rollup really carries zero counts in production (as opposed to counts left standing) was not traced — both shapes are handled by the new reader, and both are wrong at the five sites in (b).
- Task 9's and the integrator's uncommitted files were not read.
