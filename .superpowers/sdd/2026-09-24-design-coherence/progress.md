# After Plan 5 — the push, CI green, a design-coherence review · ledger

## Direction (the owner's words)
- 2026-09-24 ~22:29 USMST: "go ahead and push . why is the ci failing? fix it".
- ~22:47: "do a style and design review, make sure everything is presented in a coherent way. a lesser model designed this".
- ~22:58 (AskUserQuestion): "Approve ci.yml change (Recommended)" — the owner's acknowledgement for the protected workflow edit.

## Log
- **Pushed** `f36b4d4..7514ad5` (243 commits, fast-forward; no hooks). GitHub now shows the Plan 5 README and screenshots. Expected and documented: direct-pushed governance commits fail the advisory "Control plane" scope-review job (INS-52cb393e); that workflow is `disabled_manually` on GitHub today, and `ci.yml` is the only active workflow.
- **CI diagnosis on the pushed tree** (run 35960263556), every failure traced to its root:
  1. `go` job — gofmt: 7 COMMITTED Go files unformatted since 2026-07-30 (so `go vet` / `go test` had never run in that job). The local checkout's CRLF makes `gofmt -l` on disk list far more; the check that matches CI is `git show :<f> | gofmt -l`. Fixed; clean under gofmt 1.26 AND the go1.24.0 toolchain's gofmt.
  2. `race` job — the whole Go suite passes under -race except exactly three tests: the two `cmd/reconcile` source-derivation tests (they read `recon/cash-v3/**`, gitignored, never fetched in CI) and `TestLiveDatabaseSchemaGateRefusesUntilMigrated` (CI sets SOLVENT_DATABASE_URL — the destructive-test guard needs it — to a fresh, never-migrated database; the smoke failed to READ `goose_db_version` instead of asserting the gate's refusal).
  3. `web` job — 1,438 passed, 2 failed: `lab.spec.ts:843` (390 wide: 28px sideways scroll) and `overview.spec.ts:223` (entries' foot 913.64 > 900). Root cause: the font stacks are system stacks; the Linux runner draws system-ui in DejaVu Sans, wider than Segoe UI. Forcing Verdana locally reproduces CI's number EXACTLY (913.640625), and a sweep of all eight pages at 390 found THREE real sideways-scroll defects, not one — Scenarios (the Cash heatmap's `1fr` tracks, 35px), Activity (Plan 5's "Filter applied …" chip, nowrap, 37px), Verification (Plan 5's longer "Aave v3 market (legacy) · account comparisons" label, `.k` nowrap, 17px).
- **Fix** (reviewed by a two-lens adversarial workflow wf_aca4c782-809: Go/CI "ship", web "ship"; three of their minors taken — the smoke's refusal pinned to error 42P01 not a substring; the Overview fit split so CI reports the reference-face half as SKIPPED rather than passing a claim it did not check; the web job pinned to ubuntu-24.04):
  - `0fdf19e` gofmt (formatting only).
  - `6d131dd` ci.yml — the go and race jobs shallow-fetch etherfi-protocol/cash-v3 at 247faab (the commit the README pins; MIT; files byte-identical to the local recon copy; no .go files in it); the web job pinned to ubuntu-24.04. The scope gate refused it twice — owner acknowledgement required, then "protected enforcement changes must be isolated from product paths" — so it is its own commit, made with CONTROL_PLANE_OWNER_REVIEWED=1 on the owner's answer.
  - `f0e73ef` the schema smoke: `to_regclass('goose_db_version')` NULL → the gate must refuse, with the database's own undefined_table (42P01); otherwise the old version comparison. Read-only either way.
  - `0ff0ee5` web: below 900px `.heat { overflow-x: auto }`, `.chip { white-space: normal }`, Verification's `.k { white-space: normal }` (1440 pins cannot move); NEW `tests/e2e/wide-font.spec.ts` — every demo page at 390 under a forced wide face (Verdana / DejaVu Sans) never scrolls sideways (failed on the old CSS on exactly the three pages); `tests/e2e/demo-pages.ts` shares the demo mocks and page list (moved out of screenshots.spec.ts, byte-equivalent); the Overview fit test split (entries begin in the fold everywhere; end in it on the reference faces — skipped in CI, like the pins).
  - Local: tsc / eslint / stylelint / build clean; whole suite 1,464 passed (local) and 1,448 passed + 16 skipped with CI=1.
- Pushed `7514ad5..0ff0ee5`; CI run 35962319987 running.
- **CI GREEN — run 35962319987 on `0ff0ee5`: go 3m9s, race 6m4s, web 3m57s; the first passing run since 2026-07-30.** Annotations only (setup-node@v4 targets the deprecated Node 20 runtime; ubuntu-latest moves to Ubuntu 26 on 2026-10-19 — the web job is already pinned to 24.04; the go/race jobs are font-free). `e5ac266` the README's status says what CI runs; CI fetches the same cash-v3 commit; the live schema smoke's sentence corrected (it passes by asserting the refusal on an unmigrated database). Pushed.
- Owner's list additions: bump `actions/setup-node@v4` → v5 (a protected ci.yml edit — needs the owner's acknowledgement again).

## Design coherence
- Captures (scratchpad `design-review/`): every page and honest state at 1440 (both themes, fold + full), the eight pages at 390, three at 1920 — 108 PNGs from a production build with the flag.
- **Review workflow wf_f5b9986e-d25** (Opus except where an agent type pins its own model): four audit lenses — visual system (solvent-design, 26 findings), information and voice (solvent-clarity, 29), design system in code (serena-coder, 28), responsive (solvent-design, 24); the hiring-evaluator lens (portfolio-strategist) FAILED at once: its agent type pins the fable model, which was at its limit. A design-director synthesis (solvent-design) deduped and tried to refute every finding against the captures → 6 areas / 42 items, 17 rejected with reasons, 12 deferred, 12 rulings; a completeness critic (solvent-clarity) → 13 misses, 6+ doctrine breaks in states the captures do not show, under-specified items, non-disjoint file sets. Rendered verbatim: `design-plan.md`, `design-critique.md`, `design-audits.md`.
- **Integrator's adjudication → `design-amendments.md`** (supersedes the plan where they conflict): R1–R12 adopted with amendments (R12 also rules a FAILED receipt crit everywhere incl. the Inspector's trust item; drift stays warn); K13 radius tokens and S6 per-class normalisation dropped; the critic's doctrine fixes made binding (a fetch failure gets its own "unavailable" register, never refused; the U+2212 minus is display-only, the clipboard keeps ASCII; Activity's chips keep the order key; no bare-dash state rows; Run only when served-not-run; compare-headline arms for ties / zeros / refused / projection-only; the connected pill must fit 390); the 13 misses added (arrow grammar, one no-answer headline register, over-cap wording, one money precision per column, one price-source name, R10 across all Cash copy, Scenarios answer-first on phones, one pipeline vocabulary, one kicker, sentence case everywhere, the all-"Yes" column); three waves so no two parallel agents share a file (formats alone → kit ‖ charts → the three page groups; cross-cutting e2e specs are the integrator's in wave 3).
