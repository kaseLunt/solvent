# After Plan 5 — the push, CI green, a design-coherence review · ledger

## Direction (the owner's words)
- 2026-09-24 ~22:29 USMST: "go ahead and push . why is the ci failing? fix it".
- ~22:47: "do a style and design review, make sure everything is presented in a coherent way. a lesser model designed this".
- ~22:58 (AskUserQuestion): "Approve ci.yml change (Recommended)" — the owner's acknowledgement for the protected workflow edit.

## Log
- **Pushed** `f36b4d4..7514ad5` (243 commits, fast-forward; no hooks). GitHub now shows the Plan 5 README and screenshots. Expected and documented: direct-pushed governance commits fail the advisory "Control plane" scope-review job (INS-52cb393e); that workflow is `disabled_manually` on GitHub today, and `ci.yml` is the only active workflow.
- **CI diagnosis on the pushed tree** (run 35960263556), every failure traced to its root:
  1. `go` job — gofmt: 7 COMMITTED Go files unformatted since 2026-07-30 (so `go vet` / `go test` had never run in that job). The local checkout's CRLF makes `gofmt -l` on disk list far more; the check that matches CI is `git show :<f> | gofmt -l`. Fixed; clean under gofmt 1.26 AND the go1.24.0 toolchain's gofmt.
  2. `race` job — the whole Go suite passes under -race except exactly three tests: the two `cmd/reconcile` source-derivation tests (they read `recon/cash-v3/**`, gitignored, never fetched in CI) and `TestLiveDatabaseSchemaGateRefusesUntilMigrated` (CI sets SOLVENT_DATABASE_URL — the destructive-test guard needs it — to a fresh, never-migrated database; the smoke failed to READ `goose_db_version` instead of asserting the gate's refusal).
  3. `web` job — 1,438 passed, 2 failed: `lab.spec.ts:843` (390 wide: 28px sideways scroll) and `overview.spec.ts:223` (entries' foot 913.64 > 900). Root cause: the font stacks are system stacks; the Linux runner draws system-ui in DejaVu Sans, wider than Segoe UI. Forcing Verdana locally reproduces CI's number EXACTLY (913.640625), and a sweep of all eight pages at 390 found THREE real sideways-scroll defects, not one — Scenarios (the Cash heatmap's `1fr` tracks, 35px), Activity (Plan 5's "Filter applied …" chip, nowrap, 37px), Verification (Plan 5's longer "Aave v3 market (legacy) · account comparisons" label, `.k` nowrap, 17px).
- **Fix** (reviewed by a two-lens adversarial workflow wf_aca4c782-809: Go/CI "ship", web "ship"; three of their minors taken — the smoke's refusal pinned to error 42P01 not a substring; the Overview fit split so CI reports the reference-face half as SKIPPED rather than passing a claim it did not check; the web job pinned to ubuntu-24.04):
  - `0fdf19e` gofmt (formatting only).
  - `6d131dd` ci.yml — the go and race jobs shallow-fetch etherfi-protocol/cash-v3 at 247faab (the commit the README pins; MIT; files byte-identical to the local recon copy; no .go files in it); the web job pinned to ubuntu-24.04. The scope gate refused it twice — owner acknowledgement required, then "protected enforcement changes must be isolated from product paths" — so it is its own commit, made with CONTROL_PLANE_OWNER_REVIEWED=1 on the owner's answer.
  - `f0e73ef` the schema smoke: `to_regclass('goose_db_version')` NULL → the gate must refuse, with the database's own undefined_table (42P01); otherwise the old version comparison. Read-only either way.
  - `0ff0ee5` web: below 900px `.heat { overflow-x: auto }`, `.chip { white-space: normal }`, Verification's `.k { white-space: normal }` (1440 pins cannot move); NEW `tests/e2e/wide-font.spec.ts` — every demo page at 390 under a forced wide face (Verdana / DejaVu Sans) never scrolls sideways (failed on the old CSS on exactly the three pages); `tests/e2e/demo-pages.ts` shares the demo mocks and page list (moved out of screenshots.spec.ts, byte-equivalent); the Overview fit test split (entries begin in the fold everywhere; end in it on the reference faces — skipped in CI, like the pins).
  - Local: tsc / eslint / stylelint / build clean; whole suite 1,464 passed (local) and 1,448 passed + 16 skipped with CI=1.
- Pushed `7514ad5..0ff0ee5`; CI run 35962319987 running.
- **CI GREEN — run 35962319987 on `0ff0ee5`: go 3m9s, race 6m4s, web 3m57s; the first passing run since 2026-07-30.** Annotations only (setup-node@v4 targets the deprecated Node 20 runtime; ubuntu-latest moves to Ubuntu 26 on 2026-10-19 — the web job is already pinned to 24.04; the go/race jobs are font-free). `e5ac266` the README's status says what CI runs; CI fetches the same cash-v3 commit; the live schema smoke's sentence corrected (it passes by asserting the refusal on an unmigrated database). Pushed.
- Owner's list additions: bump `actions/setup-node@v4` → v5 (a protected ci.yml edit — needs the owner's acknowledgement again).

## Design coherence
- Captures (scratchpad `design-review/`): every page and honest state at 1440 (both themes, fold + full), the eight pages at 390, three at 1920 — 108 PNGs from a production build with the flag.
- **Review workflow wf_f5b9986e-d25** (Opus except where an agent type pins its own model): four audit lenses — visual system (solvent-design, 26 findings), information and voice (solvent-clarity, 29), design system in code (serena-coder, 28), responsive (solvent-design, 24); the hiring-evaluator lens (portfolio-strategist) FAILED at once: its agent type pins the fable model, which was at its limit. A design-director synthesis (solvent-design) deduped and tried to refute every finding against the captures → 6 areas / 42 items, 17 rejected with reasons, 12 deferred, 12 rulings; a completeness critic (solvent-clarity) → 13 misses, 6+ doctrine breaks in states the captures do not show, under-specified items, non-disjoint file sets. Rendered verbatim: `design-plan.md`, `design-critique.md`, `design-audits.md`.
- **Integrator's adjudication → `design-amendments.md`** (supersedes the plan where they conflict): R1–R12 adopted with amendments (R12 also rules a FAILED receipt crit everywhere incl. the Inspector's trust item; drift stays warn); K13 radius tokens and S6 per-class normalisation dropped; the critic's doctrine fixes made binding (a fetch failure gets its own "unavailable" register, never refused; the U+2212 minus is display-only, the clipboard keeps ASCII; Activity's chips keep the order key; no bare-dash state rows; Run only when served-not-run; compare-headline arms for ties / zeros / refused / projection-only; the connected pill must fit 390); the 13 misses added (arrow grammar, one no-answer headline register, over-cap wording, one money precision per column, one price-source name, R10 across all Cash copy, Scenarios answer-first on phones, one pipeline vocabulary, one kicker, sentence case everywhere, the all-"Yes" column); three waves so no two parallel agents share a file (formats alone → kit ‖ charts → the three page groups; cross-cutting e2e specs are the integrator's in wave 3).
- **Wave 1 (formats) — wf_b642b0ae-de6** (implementer → review "changes" → fix-up): the review's one Important — the Inspector evidence drawer's "effective" line had turned into a zoneless typeset instant — fixed (the drawer prints the wire ISO via `blockTimeTitle`, pinned in inspector.spec). Integrator follow-ups: ages join number and unit with U+00A0 ("6 min" never breaks; 12 unit specs re-pointed with the ` ` escape); a signed zero prints "$0" (pinned); the BTC axis comment corrected (mainnet WBTC as the BTC/USD axis instance). DEVIATIONS the implementer verified: **A7 — "RedStone" is FALSE** (docs/specs/2026-07-28 §14 errata: "No RedStone anywhere. Public copy must never repeat it."; internal/config/feeds.go:28-34; recon/derivation-notes.md) — the one phrase is `CASH_PRICE_SOURCE` = "Cash's own price contract, PriceProvider v2 on OP Mainnet" (chip "PriceProvider v2"), and the Overview's footer and pipeline step still say RedStone — wave 3 (area 4) removes it; S1's axis words follow the CONTRACT's axes (eth_usd, weeth_eth_rate, stable_usd, asset_usd, borrow_apy) — ETHFI is an asset_usd shock named through ASSET_WORD; `exactUtc` invents no seconds a minute-only instant lacks. Gates: tsc / eslint clean; unit 1,193; build + e2e 301/301 (pixel pins excluded — they move again; re-baselined once at the end). **`569d90f`**.
- Ruling (the review's title question): U+2212 in a DISPLAY-register tooltip is fine; only exact strings and the clipboard keep ASCII (the reason is a pasted value must parse).
- **Wave 3 handoff from wave 1** — area 4: remove "RedStone" (app/overview/copy.ts FOOTER_STACK, Pipeline.tsx) using CASH_PRICE_SOURCE; NeedsAttention / OverviewSurface private hex shorteners → shortHex. Area 5: SCENARIO_GISTS for the over-length committed scenarios (dm_composition_census 231 chars of internal jargon, btc_leg_minus_20, stable_depeg_099_boundary) + a test that EVERY committed scenario's gist is ≤ 70; AssumptionsDrawer → `exactDecimal` and drop the `wireExact` alias in app/lab/money.ts (two signatures under one name); `address-stress.ts` horizonLabel units ("30m" / "3h" / "<1m" → the humanAge grammar); lab-headline `signedUsd` → `signedBookMoney`. Area 6: the When columns' header "When (UTC)" + `blockTimeTitle` titles (P3 — a RELEASE GATE: until then the typeset instant has no zone on the page); activity-rows shortHash, evidence.ts:334, verification-view shortKey → shortHex.
- **Wave 2 (kit ‖ charts) — wf_efa5a4bc-813** (6 Opus agents, 0 errors; both reviews "changes", both fix-ups done). Kit review caught: `max-content` tables had stopped every wrapping cell from wrapping; the edge-fade mask clipped the scroll region's focus ring (also on the nav / toggle strips); the styleguide still printed capitals (STREAM CONNECTED …). Charts review caught: the Inspector's "3.8%" newest label sat on its own series (now clear of the series, the reference line and its label); the new default reference tone would turn the legacy Aave health-factor 1.0 line (the liquidation boundary) from crit to warn → a BINDING wave-3 handoff (`referenceTone="crit"`). Deviations accepted: K1's shell top offset left OFF by the kit agent (backward compatible) — the integrator switched it on for wave 3 (`globals.css .shell padding-top: var(--frame-top)`); KpiTile / StateCard take a `state` (each state has its own frame); heatmap columns `minmax(min-content, 1fr)` + a 56px floor; the degradation banner's "Unavailable" is a solid neutral line (crit is a verdict or an age tier only); the route boundary is the unreadable register; Activity's TYPE filter is MULTI-select (verified), the other toggles single-select.
- Integrator follow-ups on wave 2: VerdictHeader gains tone `absent` beside `refused` (one look, two words in the DOM — an unavailable / not-run headline is never marked a refusal), mapped in the kit's exhaustive tone table and the `LabHeadline` union; the header pill keeps an AGING / STALE age on the line in the phone form (only the batch folds; a warning is never folded away) — styleguide specimen `sg-live-phone-aging` + e2e pin; ToggleGroup forgets its roving position on blur (Tab re-enters on the pressed option); p1a-fixes' pill assertions pointed at the age span. Gates: tsc / eslint / stylelint clean; unit 1,235; build + functional e2e 305/307 → the two pill selectors fixed → the kit-owned specs 64/64. **`f657116`** charts, **`755468a`** kit and chrome.
- **Wave 3 (three page areas in parallel) — wf_f3a8b6af-a20** launched: Book/Overview (B1–B6 + R4/R9/R10/R11, remove the false "RedStone", StepStrip pipeline, LegacyFold, the live dot), Inspector/Scenarios (S1–S8 + gists ≤ 70 for every committed scenario, compare-headline arms, R7, S6 global-max, failed receipt crit on the trust item, Run only when served-not-run, answer-first on phones, the legacy HF line crit), secondary pages (P1–P7 + the unavailable register, the chips that keep the order key, "When (UTC)" + ISO titles — the release gate, History "accounts" on Cash, R6 labels, StepStrip on Verification). Cross-cutting e2e specs are the integrator's.
- **Wave 3 (pages) — wf_f3a8b6af-a20** (8 Opus agents, 0 errors). Book/Overview APPROVED at first review (the false "RedStone" is gone from the front door — the compute step names CASH_PRICE_SOURCE, the footer the chip form; one Book grid with no stretched void; "Room to cap" signed percent; "No verdict"; "account" on Cash; the pipeline is the kit StepStrip; R10 verified — risk_positions is keyed (batch_id, engine, account)). Inspector/Scenarios: review "changes" → fix-up (two stale e2e specs it owns; the set's legacy fold vanished after a Compare — restored; a 429 / busy in book mode wore the refused register — now absent/unavailable, library rows solid; R12 needs a kit patch — TrustChecklist has no crit state). Secondary pages: review "changes" → fix-up (CRITICAL: the raw integers became grouped but their titles still said "verbatim" — every unscaled hover now says what is printed; the API page's copy moved to the lib; "Copy debt (USD)" from the lib).
- **Integration workflow wf_194094bf-9f1** launched (one helper owns the build and :3111): the tsc fix (the step tone union gains crit), the R12 kit patch (TrustChecklist crit), StepStrip figures aligned at the foot of each step, every reported cross-area re-point (state-matrix, p1a/p1b/r1-fixes, keyboard, inspector activity card, kit / tokens-contract / proof-evidence unit pins), then the whole suite (and CI=1) until green except the pixel pins — a test re-pointed ONLY to an intended change with its strength kept, a real defect fixed in the product; then an adversarial review of every changed assertion.
- **Integration wf_194094bf-9f1** (helper → adversarial review "changes" → fix-up): every reported re-point applied, the R12 kit patch (TrustChecklist crit), StepStrip figures at the foot of each step; the review caught a LOST ELEMENT the re-point had accepted — a stale legacy price had lost its amber "Stale price" pill (restored, pinned in inspector.spec, state-matrix and unit) — and the Inspector Activity table not carded (now in a kit card; its scroll region named "Activity" at 390). Integrator follow-ups: the last R12 path (a wire-rejected receipt with nothing this card can tally → crit, the judge's finding in the title; pinned), the route-refusal card's doubled top offset, the library row's wire label as its hover, the case-insensitive proof negatives, three test titles re-worded. Final: tsc / eslint / stylelint / build clean; **unit 1,282; e2e 1,592 passed + the 16 pixel pins failing (they move deliberately)**. **`53f16bf`** (wave 3 + integration, 143 files).
- **Design gate wf_45a39953-b8f** launched on 108 fresh captures (scratchpad `design-gate/`, the before set in `design-review/`): the visual director and the clarity lead judge independently vs the original audit → one merged, verified must-fix list → one fix round.
- **Design gate wf_45a39953-b8f → `design-gate.md`**: both judges "not at the bar" yet — and both call the build "one instrument" now, a large visible step up in both themes and all widths, no doctrine regressed. Regressions the change itself introduced (named honestly): two filled primary buttons on the Overview hero (the AddressField's new Inspect) and on Scenarios before a run; the Book's band labels touching the card edge once the card hugged its content; "delta, to level" mixing registers ("+$118K, to $118K"); half-empty tile rows on Activity / API; legacy-fold slogans; a panel slab down the phone Activity table; the stress-table caption defining dollars over percent cells; "90 d" in prose; the degraded state saying its absence four times. The merger (which tried to refute each against captures and code) kept **15 items (D1–D15)** and rejected 13 with reasons (mostly: the approved mockup specifies the current form). **Fix round wf_be22ca11-c22** launched: two agents on disjoint files (kit + Scenarios + Inspector ‖ Book money + Activity + Verification + History + API), each reviewed, one fix-up.
- **Gate fix round wf_be22ca11-c22** (6 Opus agents, 0 errors; both reviews "changes", both fix-ups done). Kit/Scenarios/Inspector review caught a CRITICAL — after a failed re-run the Compare "Computed" age re-anchored at first sight and understated the held comparison's age (now anchored on the set's own settle clocks, unit-tested) — plus a legacy summary claiming a universal over rows it never read, a compare view left with no filled action, and components composing the projection sub-line (moved to the lib). Book/secondary review caught a proxy's non-2xx error page classified as an "unreadable answer" (the client raises MalformedResponseError for both; only a 2xx body that fails to parse is unreadable now). Integrator: the legacy fold's "· N refused" → "· N with no verdict" (the one state word), two stale `verification-receipt` test ids, the API's error table carded, the Activity live-strip test measures its sentence's line (the strip now shares the tile row), History's malformed-hour test pins that the caption gives no change. Suite: unit 1,303; the whole suite green locally and with CI=1 (17 skipped); pins re-baselined after reading overview / lab / verification / activity (replays 16/16); README screenshots regenerated. **`52e96f1`** gate fixes, **`960f164`** pins + README screenshots.
- **Codex round (D-006)** on `5f8f6d0..960f164` dispatched (brief `codex-brief.md`).
- **Codex round → `codex-findings.md`: NOT APPROVED — 6 findings (all rated CRITICAL)**; clean: the formatters (registers, paired precision, truncation, exact instants, shortHex, ages, ASCII clipboard), copy placement, presentation-only (no calculation / contract / fetch change), the new chart domain, held-comparison ages, receipt tones, the price-source phrase, the Cash account identity, band wording. **Adjudication: all six FIXED, none overruled** — each is a false sentence or a law on a reachable state, and each fix is small: (1) the All-engines Amount column right-aligned (a digit axis across engines — the amendments' own rule); (2) "No accounts" on a Median tile over accounts with no cap; (3) the all-zero compare headline claiming scenarios it excluded; (4) a book read in flight labelled unavailable; (5) the Overview overwriting "No committed receipt"'s refused register; (6) "did not reach the service" asserted from a status-less fetch rejection. Fix agent dispatched (test-first, whole suite).
- **Codex fixes (one agent, test-first; each new test seen failing on the old code, e2e checks by reverting and rebuilding): `35d322a`** — the six findings plus three follow-ups the fixer surfaced and the integrator sent back: the stale legacy-summary unit pins (MY miss in 52e96f1 — a regex that missed the "debt not computed · N refused" form, hidden by `tail -1`), a shared fetch-failure classifier (`web/lib/fetch-failure.ts`: a 2xx body that fails to parse is UNREADABLE — "The service answered, but the page could not read the answer…"; a non-2xx malformed answer is a failed request with its HTTP status; a status-less rejection "could not get a response") for Activity, History and Verification, and Verification's Live batch chip reading "no servable batch" in the refused register when the book's 503 says so. Whole suite: **1,646 passed / 0 failed** (1,311 unit, 335 e2e); no pixel baseline moved. Codex re-verification dispatched (`codex-fix-brief.md`).
- **Codex re-verification → `codex-fix-findings.md`: the six CLOSED; one NEW CRITICAL** — the new "Not measurable" sentence ("No account has a borrow cap…") claimed a whole-book negative over refused accounts whose caps are unknown. FIXED, Codex's own words: "No computed account has a positive borrow cap to measure room against", pinned with a zero-cap computed account beside a refused one. **`bfa7ab4`**. Sent back to Codex (third look, that fix only).
- **Codex round 3 → APPROVED** (session 01a0d396-0875-7fa2-b966-9ab61b68f578): the not-measurable finding CLOSED at `bfa7ab4`, the sentence true of every backend-produced state; one new MEDIUM (unreachable — Go rejects negative borrowings, dm.go:105): `cash-rows.ts` accepts a positive cap over a negative denominator as computed. Deferred to Plan B (the reader classification workstream; the reader predates this pass).
- Final tree `bfa7ab4`: tsc / eslint / stylelint / build clean; **1,646 passed / 0 failed** locally; CI=1 1,629 passed + 17 skipped. **Design coherence CLOSES** — close entry in `.superpowers/sdd/progress-ui-overhaul.md`.
