# Task 10 review: the Scenarios demo dataset (commit 8eef607)

Read-only review of `review-6eee7fd..8eef607.diff` against `task-10-brief.md`, `task-10-report.md`, the plan's Global Constraints and R16. No files edited, no test suite run, no agents spawned. Where a figure is stated below as recomputed, I recomputed it with a node script in the scratchpad from the committed source bodies (`book.demo.json`, the contract fixtures, `stress-demo-near.json`, the positions pages) and from the committed JSON. Line numbers are the committed files' (the diff's `+` lines, offset by the hunk header).

One verification worth stating up front: I copied the committed generator to the scratchpad, redirected only its `writeFileSync` target, ran it, and compared its three outputs to the committed JSON with `cmp`. **All three are byte-identical.** The fixtures are the generator's output, not hand-shaped; every "the JSON says X" below is therefore also "the generator produces X".

## Spec Compliance

| # | Item | Verdict | Trace |
|---|------|---------|-------|
| 1 | `DEMO_SCENARIOS`, `DEMO_RUN_BOOK_ETH`, `DEMO_RUN_BOOK_SET` exported from `tests/fixtures/demo/index.ts` with the brief's types | ✅ | `index.ts:34-37`: `Schemas["ScenariosResponse"]`, `Schemas["RunBookResponse"]`, `Schemas["RunBookSetResponse"]`, each via `load(...)`; doc comment is the brief's. |
| 2 | Fixtures generated, never hand-shaped; generator under `tests/fixtures/demo/` with a provenance header | ✅ | `generate-demo-lab.mjs:1-75` header; regeneration byte-identical (above). |
| 3 | Every stamped body passes `checkClocks` before it is written | ✅ | `clock-law.mjs:268-284` returns `{checked, resolutions, failures}`; `generate-demo-lab.mjs:690-696` refuses on `failures`, pins `checked` against `CLOCK_TRIOS` (0/2/2). Regeneration printed `0 / 2 / 2 clocks checked`, so the census entries are the law's own count. |
| 4 | Census moves in the same commit | ✅ | `fixture-clock-law.spec.ts:273-274` (+2 entries), `:319` `CENSUS_TOTAL` 97→101, `:646` batch-bearing 38→40. Counts as reported; I did not run the spec (see "not verified"). |
| 5 | R16: Cash before side IS the Book, after side IS the waterfall at 0.70, deltas are their difference | ✅ | Generator `:220-247`: `before.eligible_*` from `waterfallAt("1000000000000000000")`, `after.*` from `waterfallAt("700000000000000000")`, totals from the engine card, deltas computed (`:283-284`). JSON `engines[1]`: 49→167 (+118), `6949455788`→`1286949455788` (+`1280000000000`), `239603961`→`41020000000` (+`40780396039`). Book: `waterfall.points[0|3].engines[debt_manager]` states exactly those. Spec pins each side against the Book (`demo-lab-weld.spec.ts:39-62`) and the three R16 literals beside them (`:59, :61, :63`). |
| 6 | R16 lanes: from_rows `[41,8,14,13,73,153,300,804,0,6]`, to_rows `[155,12,135,43,129,188,320,424,0,6]`, held 465, laneChanged 941, bandChanged 425, near 27/27, improved 0 | ✅ | `CASH_TABLE` `:207-217` is the plan's table. JSON `hf_transitions`: from_rows/to_rows as stated, `held_rows` 465, `lane_changed_rows` 941. Re-summed by hand: held = 41 + 424; crossings = 14+13+73+6+12 = 118; band changes under R1's merge = 14+13+73+6+12+135+43+129 = 425; near-cap today = lanes 2–3 = 27, all → lane 0. Spec `:69-88` pins from_rows[0..8] against `DEMO_BOOK.hf_histogram` first, then the literals; `:83-88` runs `laneReading` for 118/425/0/27/27. Generator `:232-233` refuses unless the table's crossings equal the waterfall's newly-eligible and the after side's lanes 0–1 sum to the waterfall's 167. |
| 7 | The weld spec pins against the Book, not against copies of themselves | ✅ | `:39-62` (Book card + waterfall), `:69-74` (Book histogram, card `positions/refused/computed`), `:104-107` (legacy card + histogram). The set test `:139-144` compares the eth row to `cash()`; that is copy-vs-copy on its face, but `cash()` is itself pinned to the Book at `:39-62`, so the chain closes. `:56` `after.total_debt_usd == before.total_debt_usd` is self-referential but states the law (Cash debt is USD and does not move). |
| 8 | `DEFINITION_ETH` reconciled to the listing's `eth_minus_30` bytes (Task 9 ruling) | ✅ | `helpers/run-book-engine.ts:176-192`. Programmatic check: the 14 string literals in the block (id, version, label, description, path_assumption, 2 engines, axis, 6 `out_of_model`) equal `scenarios.json`'s entry in order; shock 70/100. Nothing yet *pins* the two together (Minor 1). |
| 9 | Listing is the committed set verbatim, re-clocked | ✅ | `scenarios-demo.json` equals `scenarios.json` with only `served_at` replaced (checked by JSON equality after masking `served_at`). Spec `:31-35`. |
| 10 | Refusals are honest nulls, never "0" | ✅ | Cell (9,9): `debt_before_usd`/`debt_after_usd` null (JSON Cash `outflows[9].cells[0]`; generator `:141-142`). `becomes_liquidatable: null` on both projection horizons (schema `openapi.yaml:3082` nullable). DM `hf_dropped_accounts: null`, Aave `flipped_to_eligible: null` (`:541`). |
| 11 | Every figure derivable from the Book or the contract example, and the header says which | ✅ (one false sentence, Important 1) | Typed constants in the generator: `CASH_TABLE`/`LEGACY_TABLE` (the plan's tables, `:207-217`, `:302-311`), `ETHFI_DESIGN` (`:373`, the brief's +$9,800/+$1,200/2), `MOVERS_CARRIED` 20 (`:218`), `DEPEG_BPS` 500 (`:445`, guarded by `def.description.includes("5 percent below")` at `:444`), `YEAR_SECONDS` (`:374`), `CLOCK_TRIOS` (`:688`). Everything else is read from a body and the header names the body. |
| 12 | Nothing in the demo contradicts the Book's numbers where the page shows both | ✅ | Every Book figure the page prints as "today" (R2's `before.*` subs, the four tiles' before sides) is the Book's own (item 5). The dataset facts in the Issues section are cross-page or cross-body, none on one Scenarios surface. |
| 13 | Movers: 20 of the demo pages' own rows crossing under ×0.7, ranked by ratio after, `movers_total` = 118 | ✅ (wording, Important 2) | Generator `:248-266`; spec `:91-108`. Page 1 has 207 crossing rows (recomputed), so no padding. |
| 14 | Set run: one result per committed scenario in listing order, eth Cash row IS the run-book, shares read `+4.5% / +<0.1% / 0% / 0%` | ✅ | JSON `results[]` ids in `scenarios.json` order; eth Cash row 49→167, deltas `1280000000000`/`40780396039`, `flipped_to_eligible` 118. Shares: `lab-compare.ts:46` "truncated toward zero" — 1280000000000 ÷ 27828808216758 = 4.599% → `+4.5%`; 9800000000 ÷ same = 0.035% → `+<0.1%` (`:54`); zero deltas → `0%` (`:53`). |
| 15 | Commit by pathspec, gate OK, no attribution lines | ✅ | Report; commit message in the review package carries no `Co-Authored-By`. Eight paths (the brief's seven + the helper the Task 9 ruling named). |

**Spec: ✅.**

### The 15 deviations, each checked

1. **`writeChecked` reads `failures`/`checked`.** True: `clock-law.mjs:268` `@returns {{checked, resolutions, failures}}`. The brief told the implementer to confirm exactly this. Intent preserved.
2. **Histograms built from the example's shape.** True: the Book's per-engine histogram entry carries `refused`, `refusal`, `engine` and no `wad_scale` (`book.demo.json hf_histogram.engines[]` keys), while the run-book's `HfHistogram` is `comparator, wad_scale, buckets, infinite_count, refused_count, note`. Spreading the Book's would have added three keys and dropped a required one. The generator instead asserts edges and comparator equal (`:183-186`) and counts/tallies equal (`weldHistogram :196-200`). Intent (the histogram IS the Book's) preserved and now proven rather than assumed.
3. **Null debts on the not-measured cell.** True and load-bearing. Schema `openapi.yaml:4551-4562`: "NULL, never "0", when this run measured none of this cell's rows, which today is exactly the (N+1, N+1) cell". `lab-transitions.ts:92-98` `cellDebt` returns a contradiction for null anywhere else and accepts it only there. The brief's `transitionsOf` wrote `"0"` on that cell (brief `:264`) — a contradiction under the page's own reader — and its pin summed `BigInt(x.debt_before_usd)` over a `string | null` (TS2345; `BigInt(null)` throws). Recomputed from the JSON: Cash measured cells sum to `27828808216758` on both sides, legacy to `190000000000000`; the remainder (7 and 6) sits on the largest cell (Cash 7→7 424 rows, legacy 6→5 4055 rows), as the header states. Intent preserved; a brief bug fixed.
4. **`transitionsOf` takes the engine's own template.** True: the example's Aave `hf_transitions.note` says "8-decimal", the Cash one "6-decimal"; comparators `hf_wad` vs `hf_num/hf_den`. The brief would have put the Cash note and comparator on the legacy engine. Improvement.
5. **Cash `hf_dropped_accounts` null, legacy `flipped_to_eligible` null.** True: `openapi.yaml:1611, :1638` examples and the row notes ("null here because this engine does not speak it" / "because the Debt Manager has no health-factor wad"). The brief passed 941 and 61 on Cash rows.
6. **`flipped_to_eligible` = the table's crossings, asserted = 118.** `:232` `if (crossings(t) !== newly) fail(...)`. Same value as the brief's; now welded.
7. **Reach shapes.** True: `shock_reach` is an object (`run-book-set.json results[].shock_reach`), so the brief's `.every` would have thrown, and its `?? templateResult.shock_reach` fallback would have given the rate step the eth reach. Arms come from `run-book-set.no-denominator.json` and are asserted (`armOf :420-424`: `no_shocks_declared` for the depeg, `projection_no_spot_pass` for the rate step). ETHFI's `every_mark_moved` is built over the Inspector's own applied ETHFI shock (1250000→625000, 50/100, asserted to be the definition's shock moved, `:432-436`) with the example sentence's count re-figured to "(1 of 1)". Intent preserved.
8. **Projection from the Inspector demo.** True: neither contract fixture carries a projection (all `projection: null`; the brief's own code anticipated this with a throw). Source `stress-demo-near.json dm_rate_horizon_plus_200bps`; the formula floor(debt × 200 bps × t / year) is proven against the Inspector's two horizons before use (`:466-468`), then applied over the Cash book's total debt. Recomputed: 27828808216758 → 45745986109 (30 d) and 137237958329 (90 d), matching the JSON. `apy_observed_at_block` 155323444 = the Book's debt_manager watermark (`:462`). `becomes_liquidatable: null` — the schema allows it and an aggregate cannot state a per-account verdict; honest.
9. **weETH realization derived, not the brief's constants.** Recomputed: collateral at risk 16768539245 × 500 / 10000 = **838426962** (floor of 838426962.25). The brief's `838000000000` ÷ 838426962 = 999.49, so "×1000" is right to three figures; at 6 decimals it is a $838,000 shortfall on $16,768.54 of collateral at risk — impossible. The brief's `41020000000` is exactly the eth waterfall's 0.70 `cumulative_bad_debt_usd` and exceeds the depeg's eligible debt (6949455788), also impossible. Derived: uncovered = 6949455788 − (16768539245 − 838426962) = −8980656495 < 0 → max(current bad debt) = **239603961**. Legacy: 9100 × 5 % = 455; 4600 − 8645 < 0 → 0. The formula reproduces the contract's own weETH example exactly (its Cash row: 5 % × 4000000000 = 200000000 shortfall; 4620000000 − 0.95 × 4000000000 = 820000000 bad debt at liquidation), which is the strongest possible provenance — but the generator does not assert that reproduction the way `projectionOf` does (Minor 2). The deviation is correct and strictly better than the brief.
10. **Notes re-figured by rule.** True: the brief's spread would have written "IDENTITY: this result is scenario eth_minus_30" onto all four results — an identity lie on the wire — and left "(1 minus 0 = 1)" denominators. The composition is proven against both example notes before use (`:520-523`). JSON: `(8552 minus 0 = 8552)` / `(1406 minus 0 = 1406)`, NOT COVERED only on the two DM-only results.
11. **Coverage from the Book.** True: the example's 6/4/2 and 2/1/1 are its toy book; the Book's 9964/9958/6 partitions exactly and the engine cards sum to it (asserted `:580-583`). `RunBookResponse.coverage` and `BookResponse.coverage` share the `BookCoverage` component (the template's key set equals the Book's).
12. **`movers_note` restated; `held_flat` + ETHFI.** Restated by rule with guards that the example still says what is being replaced (`:265-268`). ETHFI joins `held_flat` at 1250000 from `stress-demo-near.json eth_minus_30.held_flat`, consistent with the set's ETHFI applied `before` 1250000 — every demo surface now holds the same marks flat. Wording of the ranking clause: Important 2.
13. **Clock pins; legacy deltas read from the template.** `:296-297` read `600000000000` / `66666666667` from the example engine. Note: those are also, byte for byte, the Book's own Aave waterfall arm at 0.70 (`cumulative_debt_eligible_usd`, `cumulative_bad_debt_usd`) — see Minor 3.
14. **`!` in the weld spec.** True: `BookResponse.waterfall` is nullable (`openapi.yaml:2737-2739`), `Position.health_factor` is nullable (`:2931-2933`). Pinned values unchanged.
15. **`DEFINITION_ETH`.** Verified byte-equal (item 8). The Serena double-prefix mishap was repaired; the committed helper is clean.

### The 5 concerns, adjudicated

1. **207 rows cross on the pages vs the Book's 118.** I recomputed, and the report's diagnosis is wrong in a way that matters for the record: the positions pages are **not** "Plan 1's 1,000-row sample". Page 1 (1000 rows) + page 2 (412) = 1412 = the Cash card's `positions`; `page-1.total_positions` is 1412. Bucketed on the Book's own edges, the two pages give exactly `[41, 8, 14, 13, 73, 153, 300, 804]`, 0 infinite, 6 refused, 49 liquidatable — the Book's histogram to the row. The naive `num×7 < den×10` test crosses 207 of them: all 100 of lanes 2–4 and **107 of lane 5's 153**, where the plan's table (R16) says 18. Whether that is a contradiction depends on collateral: the Book's waterfall scales only ETH-linked collateral, the naive test scales the whole cap, so 207 is an upper bound and 118 is consistent with 89 lane-5 accounts holding enough non-ETH collateral. No Scenarios surface shows 207: the page reads `movers` and `movers_total` only (R5; `lab-movers.ts:1` keeps wire order), and the plan reads the positions pages nowhere but this task (plan grep). A reader on the Positions page can count for themselves — cross-page, not "the page will show both". **Verdict:** a dataset fact to record in the ledger (the pages, the histogram and the waterfall are all consistent with one another under held-flat collateral; the plan's "18 of lane 5" is the design assumption that reconciles them). **This task must fix one thing:** the generator header's sentence "the pages are a 1,000-row sample whose crossing census is not the Book's" (`:29-30`) is false on both counts — Important 1.
2. **Legacy states two movement counts (`movers_total` 14 on the run-book, `hf_dropped_accounts` 8499 on the set).** Plan-mandated: brief `:400` (`newly = 14`, `movers_total: newly`) and `:507` (`lane_changed_rows` as the dropped count). The contract's Aave law is that `movers_total` counts strictly-dropped accounts, of which 8499 lane-changed is a floor. The two figures never sit on one surface (the legacy `<details>` caption says "showing 0 of 14"; Compare shows shares). **Verdict:** dataset fact to record; a controller ruling, not an implementer defect.
3. **ETHFI −50 %: collateral and collateral at risk held flat under a −50 % mark.** Plan-mandated: brief `:511` passes `cashRun.before.total_collateral_usd` and `before.collateral_at_risk_usd`. The Book does not decompose collateral by asset, so no honest ETHFI share exists to derive. Compare (Task 13) reads shares only. **Verdict:** dataset fact to record.
4. **The Aave card's dust bad-debt figures.** The Book's `bad_debt[aave]` is `4600` / `9100` at 8 decimals ($0.000046 / $0.000091) — Plan 1's Book. The legacy after side is therefore internally incoherent (eligible debt $6,000.00005 against collateral at risk $0.0000637 and bad debt $666.67) but that is the brief's own arithmetic (`:419-421`, ×0.7 on collateral at risk, the example's deltas on the rest). Collateral at risk is not one of R2's tiles and the drawer prints the four tiles only. **Verdict:** dataset fact; a defect for Plan 1's Book generator if anyone wants the legacy arm coherent (see also Minor 3).
5. **weETH mainnet witness: the run-book's applied shock says 400000000000, `meta.demo.json` says 999900000000.** `meta.demo.json` is itself split: weETH on OP is `4000000000` at 6 decimals ($4,000) and on mainnet `999900000000` at 8 ($9,999). The run-book's `applied_shocks` ride verbatim from the contract example (brief-mandated) and agree with the OP witness, not the mainnet one. The drawer (R10) prints applied shocks; the meta witness prints on another page. **Verdict:** dataset fact; a defect for whichever generator owns `meta.demo.json` (Plan 1 Task 11's demo dataset), not this task.

Two further dataset facts I found that the report does not list, for the ledger:

- The contract's own two examples disagree on eth's reach: `run-book.eth_minus_30.json` applies three marks (WETH-OP `0x4200…0006`, weETH-OP, weETH-mainnet) while `run-book-set.json`'s eth reach says "(2 of 2)" (the two weETH marks). The demo inherits both verbatim: the run-book's `applied_shocks` has 3, the set's eth `shock_reach.applied_shocks` has 2, and WETH-OP is absent from every held-flat list in the set because the mark census is built from the set example's reach (`:377-383`). Contract-level; not this task's.
- The Book's Aave waterfall arm states cumulative eligible 0 at base and 1 at 0.70, while the Aave card says 46 liquidatable today; the legacy engine welds to the card (per the brief), so the run-book says 46 → 60. Plan 1's Book; the header already calls the Aave arm a design assumption.

## Strengths

- **Regenerable and proven so.** The committed JSON is byte-identical to the generator's output. Every weld the header states is enforced by a `fail(...)` before a byte is written: histogram edges, counts and tallies (`:183-200`), card row census (`:203-207`), the Book's three statements of today's eligible set (`:227-230`), table crossings = waterfall newly-eligible (`:232`), after-side lanes 0–1 = waterfall 167 (`:233`), coverage partition (`:580-584`), the example sentences it restates (`:265-268`, `:432-436`, `:520-523`, `:587`), the projection formula against the Inspector's own horizons (`:466-468`), the result key set (`:568`), the trio count (`:693`).
- **Better than the brief where the brief was wrong**, and each departure is stated with its reason: the null cell debts (deviation 3), per-engine notes and comparators (4), the schema-null movement counts (5), the reach arms (7), the notes that would have lied about identity (10), the weETH figures that were off by a thousandfold and exceeded eligible debt (9). The report's 15-item list is complete and every reason checks out against the compiler, the schema or the contract fixture.
- **No smuggled constants.** The six typed values are the plan's tables, the brief's ETHFI design figures, the 20-mover cap, the definition's own 5 % (text-guarded), the year, and the trio pins. The legacy deltas are read from the template rather than typed (deviation 13).
- **The weld spec pins the Book first and the R16 literals second**, so a drift in either the Book or the plan's numbers fails a named line.
- **Honest nulls throughout**: the not-measured cell, `becomes_liquidatable` on an aggregate projection, the engine-specific movement count, the seizure block's derivation never printing a "0" for an unknowable.
- **Held-flat marks unified across demo surfaces** (ETHFI at 1250000 on the run-book and the Inspector demo alike; the set's census deduplicated and sorted as the contract sorts).

## Issues

### Critical

None.

### Important

1. **The provenance header states a false fact about the positions pages** (`generate-demo-lab.mjs:29-30`: "`movers_total` is the Book's newly-eligible count (the pages are a 1,000-row sample whose crossing census is not the Book's)"). The pages are the whole 1,412-row Cash book and bucket to the Book's histogram exactly (concern 1 above). The honest sentence is that the naive ×0.7 test over the pages crosses 207 rows — 107 of lane 5 — where the plan's table (and the Book's waterfall, which scales only ETH-linked collateral) states 18 and 118; `movers_total` is the Book's by R16. A provenance header is the record the Global Constraints require ("whose header records provenance"); a false sentence in it misleads the next round's reader and the report repeats it. **Plan-mandated?** No — this is the implementer's own analysis, not the brief's text. Fix: rewrite the sentence, regenerate (no byte of any figure moves; only the header).

2. **The Cash `movers_note` describes the ranking backwards** (`:267`: "ranked here by the exact ratio AFTER the shock, nearest the cap first"; JSON `engines[1].movers_note`). The sort is ascending ratio-after (`:250`), and every mover's ratio-after is below the cap, so the first row is the account **furthest past** the cap after the shock (mover 1: 88657973425 / 126213525430 = 0.702; the 20th is the closest to 1.0). "Nearest the cap first" is true only of the before side (ascending ×0.7 ratios = ascending today's ratios = least room today first). R5 carries `movers_note` verbatim on hover beside a table whose rows visibly contradict it. **Plan-mandated?** Partly: the brief's own comment (`:343` "nearest the cap after first") and the pin's comment (`:121` "ascending (nearest the cap first)") carry the same inversion, so the **order** is the controller's ruling; putting that phrase on the wire is the implementer's (the example sentence it replaces said "largest first", a phrase that did state its order). Fix: keep the order; reword the clause (e.g. "ranked by the exact ratio AFTER the shock, lowest first — the least room today, furthest past the cap after") and regenerate. No number moves.

### Minor

1. **Nothing pins `DEFINITION_ETH` to the fixture.** The Task 9 reviewer asked for the two to be unable to disagree; the reconciliation copies bytes and no spec asserts `DEFINITION_ETH` equals `SCENARIOS.scenarios.find(id === "eth_minus_30")`. A one-line `toEqual` in `lab-library.spec.ts` or the weld spec closes it; deriving the constant from the fixture would close it harder.
2. **`realizationOf` is not proven against the contract's own example** the way `projectionOf` is (`:466-468`). The formula reproduces `run-book.weeth_market_depeg_oracles_held.json`'s Cash row exactly (200000000 / 820000000); one assertion would turn the header's derivation claim into a weld.
3. **The legacy deltas could weld to the Book.** `600000000000` / `66666666667` are read from the contract example (`:296-297`), but the Book's own Aave waterfall arm at 0.70 states the same two numbers. Reading them from the Book would make the legacy after side "the Book's" as the Cash side is — though the arm's base (0 eligible vs the card's 46) is itself a Plan 1 design artifact, so leave it unless Plan 1's Book is repaired. Record, do not act.
4. **Round narration in the census spec.** `fixture-clock-law.spec.ts:267` and `:320` say "plan 3 Task 10". The file's existing entries do the same (`:250` "plan 2 Task 10", `:301` "W-TN"), so this follows precedent, but it is literally what the Global Constraint forbids in shipped code. Precedent; not this task's to fix alone.
5. **Two `!` the types could have avoided.** `DEMO_BOOK.waterfall!` and `row!.health_factor!` (spec `:16`, `:100-101`) are compiler-forced, but a guard that throws with a message would fail more legibly than a TypeError if the Book ever refused its waterfall. Style.
6. **The histogram-edge check runs four times.** `histogramFromRows` (`:183-186`) re-asserts edges and comparator on every call (before/after × two engines); `weldHistogram` asserts the counts once. Fold the edge check into `weldHistogram`.
7. **`crossings` restates the page's law.** `:176` defines a crossing as lanes 2–7 → lanes 0–1, the same rule `lab-transitions`' `crossedCap` applies. Acceptable in a `.mjs` generator that cannot import the lib, but it is a second statement of the law; a comment naming the lib function it mirrors would make the coupling visible.
8. **"Cannot disagree" is transitive, not extracted.** The header (`:73-75`) and the census comment (`:267-269`) say the generator's `CLOCK_TRIOS` and the census cannot disagree; they cannot, because both are checked against the same law on the same bytes — but unlike the lab-book and feed generators, the pins are not extracted and compared (`GENERATOR_PIN_SHAPE :362` is unchanged). This matches the Inspector generator's precedent (`:250-256`), so it is consistent, just weaker than the file's strongest pattern.
9. **Legacy after side is incoherent on collateral at risk** (concern 4): $0.0000637 at risk against $6,000 newly eligible. The brief's arithmetic (`:419-421`); nothing on the page prints the figure. Record for Plan 1.

## Assessment

**Task quality: Approved.**

The substance is fully correct and I could verify almost all of it independently: the fixtures are the generator's byte-identical output; every R16 figure appears exactly and is pinned against the Book first; the clock law's own count is the census's count; refusals are nulls; the six typed constants are the plan's tables and the brief's design figures, named as such; and each of the 15 deviations is true for the reason given and preserves or improves the brief's intent — three of them (the null cell debts, the identity notes, the weETH figures) fix brief-level errors that would have shipped contradictions onto the wire. The two Importants are sentence-level: one false provenance sentence in the header, one wire sentence that describes its own ordering backwards. Neither moves a number, both are fixed by editing the generator and regenerating, and the second should land before Task 11 renders the hover. The five concerns are all dataset facts for the ledger; the first is misdiagnosed in the report (the pages are the full book, not a sample) and that misdiagnosis is what Important 1 corrects.

**Not verified from the diff:** the test runs the report cites (96 passed; typecheck; lint) — not run, per the review's terms. `CENSUS_TOTAL` 101 and the batch-bearing count 40 depend on the whole fixture directory and were not recounted (the two new entries' 2/2 I did confirm from the law's own output). `laneReading`'s 425 / 27 / 27 were re-summed by hand from the table under R1's band merge, not executed.

## Re-review 1 (91e68ba)

Scoped to the fix-round package `review-ff78ac6..91e68ba.diff` (one commit; generator, regenerated run-book JSON, weld spec) and the report's "## Fix round 1". Read-only; suite not run. I re-ran a scratchpad copy of the new committed generator (only its write target redirected): all three bodies are byte-identical to the committed ones, and a path-by-path deep diff of the run-book between `ff78ac6` and HEAD names exactly one path, `$.engines[1].movers_note`. The listing and the set are unchanged. The working tree is clean on the three files.

**I1 — the provenance sentence.** Now true, and each clause is enforced:
- "the two positions pages are the WHOLE 1,412-row Cash book" — `weldPages` (`generate-demo-lab.mjs:222-225`) fails unless `PAGES.length === card.positions`, `page1.total_positions === card.positions` and `page2.next_cursor === null`. On the committed pages: 1000 + 412 = 1412, `total_positions` 1412, `next_cursor` null.
- "bucket to the Book's histogram exactly" — `:227-240` buckets every computed row by the exact rational comparison `n·WAD ≥ lower·d` / `n·WAD < upper·d` on the Book's own edges and fails unless the eight counts, `infinite_count` and `refused_count` all match. Not a tautology: the counts come from the rows, the targets from `book.demo.json`. The `infinite` branch reads `health_factor.infinite`, a required schema field (`openapi.yaml:2769`) present on all 1,406 computed rows and true on none, so its 0 is a measured 0, not a vacuous one.
- "(asserted before anything is read from them)" — `weldPages(card, hist)` is called at `:313` before the `crossing` filter reads a row.
- "a reader who re-shocks every row by ×0.7 by hand finds 207 (asserted…)" — `NAIVE_CROSSINGS = 207` (`:245`) compared to `crossing.length` computed from the rows (`:319`); a page edit that changes the census fails the generator, so the sentence cannot go stale. Not a tautology.
- "most of them from lane 1.25–1.50, where the table moves 18" — recomputed: crossing by lane `[0,0,14,13,73,107,0,0]`; 107 of 207 from lane 5; the table moves 6 + 12 = 18 out of lane 5. True.
- "the Book's transition table, not a naive re-shock of the rows, is the law for the crossings" — unchanged `:232` still fails unless the table's crossings equal the waterfall's newly-eligible count. True.

**I2 — the movers order.** The sort is unchanged (`:317`: ascending `ratio`, ties by account). Row 1 of the committed JSON is 88657973425 / 126213525430 = 0.702, the lowest ratio-after, i.e. the account furthest past the cap; the 20th is the highest. The new sentence — "ranked here by the exact ratio AFTER the shock, ascending: the account furthest past the cap first" / "`movers` carries the 20 furthest past the cap; the other 98 are not on this page." — describes that order correctly. The weld spec's test title, its comment (`demo-lab-weld.spec.ts:106`) and two `toContain` pins (`:97-98`) quote both sentences, and the ratio-monotonicity loop (`:107-109`) is the same law. The lookup map now spans both pages (`:100`), matching the generator's `PAGES`.

**Minor (a) — the realization rule welded to the contract.** One rule, `realizationFigures` (`:541-547`), proven at module load against every engine of `weethRunBook` — the contract fixture read from disk at `:104`, not a copy — before `realizationOf` uses it (`:550-556`; fails if either figure differs). The spec re-does the rule in TS and applies it to `RUN_BOOK_WEETH_BATCH_1`, which `tests/fixtures/lab-book.ts:39-41` loads from `run-book.weeth_market_depeg_oracles_held.json` (a real read of the contract fixture), on both engines, then pins that example's Cash block to `["200000000", "820000000"]` and the demo's to `["838426962", "239603961"]` (`:131-137`), and checks the demo rows against `cash().before` / `legacy().before`, which the earlier tests pin to the Book. This is a real weld on both ends. (The rule text is now stated in three places — the header, the generator's doc comment and the spec — which is the pin re-doing the sum, as every other pin here does; acceptable.)

**Minor (b) — `DEFINITION_ETH` pinned.** `:140-145` deep-equals the helper's constant to `DEMO_SCENARIOS`' `eth_minus_30` on `id, version, label, description, path_assumption, engines, shocks, out_of_model`. `ScenarioDefinition`'s required set is exactly those eight fields (`openapi.yaml:3139`), so the pin covers every field the helper copies; and `DEMO_SCENARIOS.scenarios` is itself pinned equal to `SCENARIOS.scenarios` (`:32`), so the weld reaches the contract listing too.

**No figure moves — confirmed independently** (regeneration byte-identical; the only run-book path that changed is the `movers_note` string).

Nothing residual from the two Importants or the two minors taken. The remaining Minors from the first review (3–9) were not in scope for this round and stand as recorded.

**RE-REVIEW: closed.**
