---
id: E-w2-acceptance
type: evidence
title: "W2 acceptance evidence: reconcile r10 pass — 0 gated failures at fresh hash-welded pins; every gated class 0-failed"
status: recorded
work: W2
result: pass
observed_at: 2026-08-02T02:40:00Z
tested_commit: 5b4549890766d39d95cccdffbc1ac2c2cafb7216
environment: "Windows 11 dev box; Go 1.24; Postgres 16.11 in docker (solvent-db-1); live DB `solvent` at goose schema v19 with the daemon RUNNING (batch era, AlgorithmRevision 5); API live on :8080; RPC: dRPC free tier (ETH+OP) with the in-shell localhost→127.0.0.1 DNS substitution (env only, .env untouched — the root-cause fix for the r5/r7 transport flake); direct `go run` because `make` re-includes .env; awake-hours launch policy; session-owned background run (r10, launched 18:55 PT 2026-08-01)"
contract_fingerprint: sha256:b76454b22ae956c40f05da37d4e4197448490c1a40dea44bff7a9a26ac90c64b
input_fingerprint: sha256:964a2946065efc7d8a37fc5227f1f86cace9a3e08108973f919e7caf2502b7cb
commands:
  - "go run ./cmd/reconcile -rps 1.0 -timeout 120m -rpc-attempts 8"
updated: 2026-08-01
---

# E-w2-acceptance — W2 acceptance evidence (2026-08-01 local / 2026-08-02Z)

Acceptance run **r10** (the receipt run) executed at `5b45498` — the commit that recorded
"round 9 SHIP; r10 receipt run launched" — against the LIVE database with the daemon running;
exit code captured directly (exit 0). The three pacing flags (`-rps 1.0 -timeout 120m
-rpc-attempts 8`) are all classified taint-free by the round-11 flag-surface closure; no
verdict-bearing flag deviated from the default posture.

1. **`cmd/reconcile` (r10):** `status: completed`, `result: pass`, **0 gated failures**.
   Comparison sha256 `a34d7a53af58a117c74333f156864de73f13927f6d41f2c8d4b6485c287978e0`.
   Pins (hash-welded before AND after): eth 25,664,030
   (`0x678ad6d0e16409abd733537147febd9eadfae08329c84fa713ea6d4619ea1fe4`); op 155,018,419
   (`0x15d560877d01b9fa4789cf45ffdd90dcfd07822dd63a7ce203636590a5305924`); goldens A/B
   re-welded. Every gated class clean: DM custody rows 29/29 exact; `dm_boolean_weld`
   28,760 comparisons, 0 failed (vs `liquidatable@pin` + empty-set probes); `aave_hf`
   1,143 comparisons, 0 failed (7-component gate); `realized_liquidation_backtest`
   623 comparisons, 0 failed — the 31-case frame fully evaluated over complete baskets
   (the H9 closure; partial-liquidation legs priced at N−1, no gated residue);
   param weld / registry / tokenconfig / heartbeat scans all 0-failed.
2. **Artifacts:** committed in the receipt commit `0c5f317`
   (`roadmap/evidence/artifacts/w1-reconcile/drift-report.json` + `drift-report.txt` —
   the harness's fixed output path carries the `w1-` prefix from its W1 origin; the bytes
   committed at `0c5f317` ARE the r10 run). Session log `accept-r10.log` (untracked).
3. **Run provenance:** ten acceptance attempts r1–r10. The failures were autopsied, never
   waived: r5/r6 killed by transport (localhost-DNS flake + a machine reboot; r6 aborted
   with ZERO real drift), r9's five gated failures were ONE harness frame-reader defect
   (`readBacktestFrameState` under-fetched partial-liquidation baskets) fixed as wave H9
   (`a45b173`) with a 31/31 live re-adjudication and a loud-failure negative control.
   Every defect was fixed inside the verdict; none was waived around it.

The other four W2 acceptance legs were attained and re-attained at the landing gates across
the phase (pointers, not re-assertions — the ledger `.superpowers/sdd/progress-phase3.md` is
authoritative): full suite green live-db both DSNs at every H-wave landing, including the
first fully race-verified run in repo history (full suite under `-race` exit 0, local WinLibs
gcc, plus the owner-approved `ci.yml` race lane `ccaaa0e`); opted-in pipeline-replay 3/3 legs
(reorg leg included); fork replay 3/3 census-gated legs; `packages/client-ts` 202/202 at its
Task 8 landing (`d664886`, zero runtime deps enforced by test, `npm ci` reproducible, packed
not published — publishing is W3/P5) and regenerated byte-identical 312/312 at contract 1.3.0.

Review provenance: D-006 program, Codex rounds 1–9 on the pre-receipt range (the H-saga
rounds 1–7 closed SHIP at round 7; round 8 NOT-SHIP on one vacuous green — the H9 live
re-adjudication test greening on t.Logf'd verdicts — fixed `1b56d77` with per-case hard
assertions and a LIVE mutant-kill; round 9 SHIP: "no remaining substantive false-green path
found"). Every finding fixed-and-verified or accepted-and-disclosed under D-013's
adjudication line. Session IDs and disclosures: `.superpowers/sdd/progress-phase3.md`.
