# Task 7 review — the pre-kit components retired; the styleguide shows the kit (R7)

Package: `ac3bbde..50c62a3` (one commit, 23 paths, by pathspec, no attribution lines). Read-only review: nothing was built, no suite was run, no file other than this one was written. The working tree under `web/{app/styleguide,components,tests/e2e,lib}` is clean at `50c62a3`, so file:line below is the commit.

Verdicts

- **Spec compliance: PASS WITH FIXES.** Every R7 line and every brief step is met or carries a ruled deviation, except one Global-Constraint line inside a specimen (I1, the cross-engine fold sum) and the controller's pending item (I2, `refused` vs the Book's `—`). Both are in the same file and belong to the same fix round.
- **Code quality: APPROVE after the fix round.** The deletions are clean, the pins are real and mostly stronger than what they replace, the untouched blocks are provably untouched. No Critical.

---

## Spec Compliance

| Requirement | | Trace |
|---|---|---|
| R7 / Step 1 — each deleted component had no consumer outside the styleguide at the parent | ✅ | table below (all eleven checked with `git grep` at `ac3bbde`, not only five) |
| R7 / Step 3 — no import of a deleted file or CSS module survives | ✅ | `git grep` at `50c62a3` for `components/<Name>`, `./<Name>"`, `verdict.module.css`, `table.module.css` over `web/`: the only hits are two comments, `lib/stream-posture.ts:41,146` (`components/Ribbon.tsx`). No code reference. |
| Ruling — `StatusChip` + `chip.module.css` stay | ✅ | `components/states/{EmptyDefinitive,RefusedCard,SupersededCard,UnavailableCard}.tsx` import `StatusChip`; both files present; commit message says so |
| Ruling — `states/*` stay | ✅ | six cards + `states.module.css` present; only consumer is `app/styleguide/page.tsx` (as ruled) |
| Ruling — `ribbon.module.css` (DegradationBanner) and `primitives.module.css` (RouteRefusal) stay unpruned | ✅ | both present; `DegradationBanner.tsx:4`, `RouteRefusal.tsx:3` import them; not in the diff |
| Ruling — comment-only edits in RouteRefusal / StatusChip | ✅ | diff hunks are comment lines only (RouteRefusal 1 line; StatusChip two doc blocks); no code token changed |
| Ruling — drawer label "Methodology & evidence" | ✅ | `DrawerDemo.tsx` button + `Drawer title`; identical to the product's (`app/book/BookSurface.tsx:87`, `app/feed/ActivityDrawer.tsx:18`, `app/observatory/HistoryDrawer.tsx:22`, `app/developers/ApiDrawer.tsx:18`); `shell.spec.ts:112` clicks it by role+name — one such button on the page |
| Step 2 — section order kept | ✅ | old order `tokens type verdict freshness dimensions exact states table pagination drawer ribbon charts interaction statcard severity chips marks stampline truth`; new `tokens type verdict freshness dimensions exact states table pagination drawer identity charts interaction kpi pills truth` — same sequence, three renamed in place, three retired from the tail group (`page.tsx:142…686`) |
| Step 2 — the self-measuring contrast block untouched | ✅ | the `sg-tokens` section is md5-identical between `ac3bbde` and `50c62a3` (`2bd18490…`); `ContrastSpecimens.tsx`, `InteractionRegisterDemo.tsx`, `lib/contrast.ts`, `app/tokens.css`, `app/globals.css`, `components/kit/**` have an empty diff. `sg-type`, `sg-exact`, `sg-states`, `sg-charts`, `sg-interaction`, `sg-freshness` sections are md5-identical too (the report's "byte-for-byte" claim holds; `sg-freshness`'s two comment edits are above the section, `page.tsx:80-96`) |
| `p1a-6` contrast pin byte-untouched | ✅ | test body md5-identical at both revisions (`p1a-fixes.spec.ts:203-240`, `e46bf546…`); its selectors (`contrast-pair` ×17, `contrast-probe-bad`) collide with nothing the new specimens emit |
| VerdictHeader ×4 tones + the identity law | ✅ | `page.tsx:191,205,217,229` (crit · warn · ok · refused) and `:249-254` `chips={[]}` → `VerdictHeader.tsx:26-27` renders `{Identity · missing · refused}`; `.chipRefused` dashed (`kit.module.css:47`). This pin is the ONLY pin anywhere of the kit's empty-chip fallback (grep: `p1a-fixes.spec.ts:273-274` alone) — new coverage of a Global Constraint |
| KpiTile incl. refused-dashed and pending | ✅ | `page.tsx:633-660`: neutral · crit · warn · ok · refused (`value="withheld"`, dashed via `kit.module.css:70`) · pending (`KpiTile.tsx:29` prints `…`, `aria-busy`; the handed value `9,964` is provably not rendered — pinned `p1a-fixes:462`). Brief said ×5; six shipped — fine |
| StatusPill full vocabulary incl. projection | ✅ | `page.tsx:671-677`; `.pillProj` dashed, `background: none` (`kit.module.css:112`) |
| IdentityChips | ✅ | `page.tsx:537-563`: four-truth strip with trailing slot; tone strip ok/warn/crit/refused |
| KitTable with a dim refused row + small/dust toggle | ✅ (law shown) / ❌ (two specimen-content defects) | `TableSpecimen.tsx:132` `dim: refused`; `:159-164` `SmallToggle`; refused row never folds (`small:false`). Defects: I1 (the fold sums two engines) and I2 (`refused` word vs the product's `—`) |
| Drawer | ✅ | kit `Drawer` from `@/components/kit` (`index.ts` last line), ghost button, key → value rows |
| Honest UI — no refused value as 0/$0 | ✅ | refused tile prints `withheld`; refused row prints a word (pending → `—`), never `0`; the only `$0.00` on the page is a real zero (`dust-no-debt`, debt `$0.00`, status "no debt", HF `∞`) |
| Honest UI — engines never summed in a specimen | ❌ | **I1** — `TableSpecimen.tsx:88-90,163` |
| `shell.spec` walk = emitted ids | ✅ | sixteen ids in `shell.spec.ts:91-107` = the sixteen `data-testid="sg-*"` sections in `page.tsx`, same order; no stale `sg-ribbon/statcard/severity/chips/marks/stampline` or `sg-verdict-{current,superseded,empty,partial,refusal-law}` id survives anywhere in `tests/`, `app/`, `lib/` (only the mapping comment `shell.spec.ts:86-87`) |
| r1-fixes (10) deletion lost nothing | ✅ | at `ac3bbde` the test body was `await muteStream(page)` + four RETIRED comments — zero `expect`. The replacing note (`r1-fixes.spec.ts:135-140`) names all four homes, each verified: `history.spec.ts:106` (dek) + `:240` (drawer body); `activity.spec.ts:102` (`ACTIVITY_DEK`, pinned verbatim in `tests/unit/activity-view.spec.ts:162`) + `:475`; `verification.spec.ts:54` + `:267`; `api.spec.ts:47` + `:165` |
| Test policy — `web/lib` unit count not decreased | ✅ | `tests/unit` 79 files at both revisions; no unit spec in the diff |
| Test policy — one-line ledger note per file | ⚠ open | `progress-ui-overhaul.md` has no Task 7 entry yet (p1a-fixes: two arms retired; r1-fixes: (10) retired; shell: walk re-expressed). The ledger has been the controller's file on earlier tasks — flagged so it is not lost (M8) |
| Process — by pathspec, no attribution, no prettier on tests | ✅ | `git show -s 50c62a3`: subject only, author Kase Lunt, 23 paths, all the implementer's |

### Deletion spot-check (import sites at `ac3bbde`, `git grep -E "from ['\"][^'\"]*/<Name>['\"]"` over `web/`)

| Deleted | Import sites at the parent | Outside the styleguide? |
|---|---|---|
| `StatCard` | `app/styleguide/page.tsx:18` | none |
| `Stampline` | `styleguide/DrawerDemo.tsx:5`, `styleguide/page.tsx:24` | none (`stamplineHiddenSummary`: own file only) |
| `Ribbon` | `styleguide/page.tsx:25` | none (`TIER_CLASS`: a comment in page.tsx:85 only; `TONE_CLASS` in `app/proof/*` are unrelated local constants) |
| `EngineChip` | `styleguide/{PaginationDemo:7,TableSpecimen:18,page:20}` | none |
| `RefusedTag` | `styleguide/page.tsx:23` | none (the old note's "15 production consumers" were already gone) |
| `AddressMono` | `styleguide/{PaginationDemo:6,TableSpecimen:17,page:21}` | none |
| `VerdictBanner` | `styleguide/page.tsx:8` | none |
| `DataTable` | `styleguide/{PaginationDemo:4,TableSpecimen:16}` | none (`LoadMoreFooter`, `WindowingSeam`, `RowTone`, `ColumnSortState`: own file + PaginationDemo) |
| `MarksStamp` | `styleguide/page.tsx:22` | none |
| `ProjectionBadge` | `styleguide/page.tsx:17` | none |
| `SeverityHF` | `styleguide/{TableSpecimen:19,page:19}` | none |
| `verdict.module.css` | `components/VerdictBanner.tsx:4` | none |
| `table.module.css` | `components/DataTable.tsx:4`, `styleguide/DrawerDemo.tsx:6` | none |

The report's deletion table is accurate on every row I checked.

### Pin dispositions

| Pin | Report says | Verified |
|---|---|---|
| p1a-6 contrast ("every text swatch's LIVE ratio clears 4.5…") | untouched | ✅ byte-identical |
| five banner variants → "the four VerdictHeader tones…" (`p1a-fixes:241-276`) | re-expressed | ✅ and STRONGER on what made it valuable: the old pin asserted `data-variant` + a non-empty strip per variant and never a colour; the new one keeps both and adds tone → colour per tone, read against the live token (`.emCrit/.emWarn/.emOk` → `--{tone}-text`, `.emRefused` → `--ink-2`; `kit.module.css:33-36`). `data-variant` is what `VerdictHeader.tsx:30` emits; the strip is `IdentityChips`' `data-slot="identity"` (`IdentityChips.tsx:28`) |
| — arm: ExactValue inside the banner (`[title="$8,468.238278"]`) | retired, unexpressible | ✅ genuinely unexpressible: `emphasis`, `rest`, `dek` are `string` (`VerdictHeader.tsx:8-11`) and `IdentityChip.value` is `string`. See I3 for where the law can live instead — the product's answer is the drawer |
| — arms: `data-identity-refusal` / "Verdict withheld" | retired; replaced by the empty-chip arm | ✅ the kit's equivalent is pinned (`:269-275`); `verdictBannerModel` keeps its unit pins but guards nothing mounted (M3) |
| freshness tiers | kept, title reworded | ✅ body unchanged; section md5-identical |
| nine dimensions + three compositions | untouched | ✅ it never read the `ProjectionBadge` (`:315-348`) |
| six states + "refused table cells say the word" (`:399`) | kept, count 2 → 3 | ✅ arithmetic right today (collateral, debt, HF; exact-match excludes the h2, the footer and the pill). ❌ law wrong per the pending ruling — I2 |
| NEW kit table (`:402-438`) | new | ✅ values real: `.tbl tr.dim td` (0,2,2) beats `.tbl td` (0,1,1) → `--ink-3` (`kit.module.css:100,106`); `.refcell` → `--warn-text` (`styleguide.module.css:260-263`); `td.nth(2)` is Collateral; 3 → 5 rows; toggle `role="switch"`/`aria-checked` from `SmallToggle.tsx:17-18`. Two arms need re-expression in the fix round (I1 label, I2 word) and the "dimmed" arm is visually vacuous today (I2 note) |
| NEW kit specimens (`:440-494`) | new | ✅ values real: `.kpiCrit .kpiV` → `--crit-text` (`:67`); `.kpiRefused` dashed + `.kpiRefused .kpiV` → `--ink-2` (`:70-71`); pending `aria-busy` + `…`; pill labels/`title`; `.pillProj` dashed, transparent (`:112`); `.chipOk/.chipWarn/.chipCrit b` → `-text` grades (`:42,44,46`); `.chipRefused` dashed (`:47`) |
| r1-fixes (10) | deleted, note in place | ✅ assertion-less; four homes verified |
| shell walk | re-expressed | ✅ ids match |

No computed colour in either new pin looks wrong against `tokens.css`. All expected values are resolved from the live token through `resolveAppbarToken` in the same theme as the element, so the dark theme's `--x-text == --x` collapse (`tokens.css:179-182`) cannot produce a false failure; and every pair a pin discriminates is distinct in both themes (`--ink` / `--ink-2` / `--ink-3`: `#1a2327/#56676e/#637075` light, `#dbe5e8/#93a5ab/#71868e` dark). `readChipRegister` reads `borderTopStyle`, so the dashed assertions do not depend on shorthand serialisation.

---

## Strengths

- The retirement is exact. Eleven components, two CSS modules, 1,803 lines gone, and a name sweep at `50c62a3` finds no code reference — only comments. The implementer caught the pre-flight error on `StatusChip` by checking references rather than trusting the brief, and said so in the commit message instead of shipping a false one.
- The "untouched" claims are true at the byte level, which is rare: seven sections and the contrast pin hash identically across the commit.
- The five-banner pin got better in translation. The old pin never asserted a colour; the new one pins tone → colour against live tokens, and the law specimen is now the only pin in the repo of `VerdictHeader`'s empty-chip refusal — a Global Constraint that had no direct test before this commit.
- The specimens follow the product's composition, not an invented one: `TableSpecimen`'s `toRow` / `short` / `StatusPill tone="refused" title=<wire code>` / `SmallToggle` label grammar mirror `app/book/NeedsAttention.tsx:21-63,110-116`; the drawer button is the product's label and class; the warn/ok/refused headers quote R2/R3's own sentences and deks.
- The pending tile is handed a value on purpose and the pin asserts it is NOT printed — a specimen that can fail.
- r1-fixes (10): deleting a green-and-empty test rather than keeping it, with a note that names each law's new home, is the right call and every home checks out.
- The report is honest about what was not run and ranks its own likeliest e2e failures; its Concern 5 found the controller's pending item unprompted.

---

## Issues

### Critical
None.

### Important

**I1 · The table specimen's fold toggle counts and sums across two engines — PLAN-MANDATED (Global Constraint: "Cash and the legacy Aave market are never summed on any surface").**
`TableSpecimen.tsx:88-90`: `SMALL_COUNT` counts the two `small` rows and `SMALL_DEBT = "$14.55"` is, in the file's own words, "summed by hand from the specimen values above ($14.55 + $0.00)" — the `$14.55` row is `debt_manager` (`dust-liquidatable`), the `$0.00` row is `aave_v3` (`dust-no-debt`). The label `Show 2 small & dust positions ($14.55)` (`:163`) is therefore one count and one sum over Cash + the legacy market, in the same component whose footer says "Engine populations reconcile separately — never one summed count", and the new pin welds it verbatim (`p1a-fixes.spec.ts:428`). The addend being `$0.00` hides it numerically; the canon still teaches the pattern. The product cannot do this: the Book's table is Cash-only with no Engine column and its toggle sums `summary.liquidatable.sums.belowLine` for one engine (`NeedsAttention.tsx:80-86,110-116`). Secondary: in the product only below-the-line LIQUIDATABLE rows fold; the specimen also folds a healthy no-debt row, which the Book's table never contains. Fix (same file as I2): make the specimen single-engine like the Book's table, or keep both folded rows on one engine and let the label name it; derive the label's sum from the rows rather than a hand constant; re-express `p1a-fixes:428`.

**I2 · The refused row prints the word `refused` where the product prints `—` — the controller's PENDING item; knock-on list for the fix round.**
Product: `NeedsAttention.tsx:55-56` (`room: "—"`, `debt: … "—"`), the refusal named by the `StatusPill` (`:58-60`). Canon: `TableSpecimen.tsx:4,103-106,140-142`. What must move together: (a) the file header comment and `refusedCell()`; (b) `.refcell` (`styleguide.module.css:259-263`) loses its only user; (c) `p1a-fixes.spec.ts:375,397-399` — the test title "…and refused table cells say the word", its comment "never an em dash that reads as an empty zero" (now the opposite of the product) and the count `3`; (d) the new kit-table pin's `--warn-text` word arm (`:414-417`); (e) `README.md:79-80` law 2 already says "null is an em dash, never 0", so the README agrees with the product, not with the specimen.
Worth knowing while re-expressing (d): today NOTHING in the dim row renders dim. The `td` colour is `--ink-3`, but every text node in the row sets its own colour — `kit.addr` → `--ink` (`kit.module.css:102`), `.refcell` → `--warn-text`, the pill → `--ink-2` — so `p1a-fixes:410-413` passes on a computed style no glyph inherits. With `—` as a bare text node the dash inherits `--ink-3` and the row reads dim exactly as the Book's does; the re-expressed pin should measure the colour where the dash is (the `td` then is the right element) and keep the pill's label/title arm. The address staying `--ink` matches the product (`NeedsAttention.tsx:51`), so that is not a defect.

**I3 · "An exact string is underneath" has no kit-composed specimen — and `ExactValue` has no product consumer at all.**
The retired arm was the canon's only demonstration of §7 *in composition* (human `$8.5K` over exact `$8,468.238278` inside an answer). It is rightly unexpressible on `VerdictHeader`. But `grep '<ExactValue' app/` finds only `app/styleguide/page.tsx` — no product page mounts the affordance; `sg-exact` and its pin (`p1a-fixes:350-373`, unchanged) now document a component the product does not use, the same position as `states/*`. Where the product actually puts the exact value is the drawer (`app/inspector/[addr]/InspectorDrawer.tsx:17-36,90-93`, "Exact wire values"). Kit-compatible homes, in order of fidelity to the product: a human → exact row in `sg-drawer`'s key → value list (follows the Inspector); `KpiTile.sub` (a `ReactNode`, `KpiTile.tsx:9`); a `KitTable` cell (`ReactNode`). Not plan-mandated — a controller decision: add the one drawer row + one pin line in the fix round, or rule `ExactValue` styleguide-only beside `states/*` and ledger the arm's retirement as final. As shipped, the crit header prints `$8.5K` with no route to its exact figure anywhere in the specimen.

### Minor

- **M1 · `web/README.md` still teaches the retired components** — `:25` ("Feeds the Ribbon"), `:81` (`SeverityHF`), `:83` (`RefusedTag`), `:85` (`MarksStamp`, Ribbon watermark vector), `:89` (`ProjectionBadge`), and `:23` "see the styleguide for all of them live". Not in the report's Concern 4. The honest-UI laws list is the README's most-read part; it should name the kit (`StatusPill`, dashed `KpiTile`, dim `KitTable` row).
- **M2 · More dangling than the report lists.** Confirmed as reported: `EngineTag` (`StatusChip.tsx:93`) + `.engineTag` (`chip.module.css:127`) — zero consumers (only a comment in `tests/unit/kit.spec.ts:4`); stale comments at `lib/stream-posture.ts:41,146`, `lib/freshness.ts:623`, `lib/pagination.ts:3`, `lib/useMeasuredWidth.ts:14`, `lib/posture.tsx:4`; dead rules in `primitives.module.css` (section headers at `:4,58,70,120,196,209,274,293` still name the deleted components) and `ribbon.module.css`. Additional: `chip.module.css:2` still says "MATCH the landed appbar family in ribbon.module.css byte-for-byte" (the twin comment in `StatusChip.tsx` was fixed, this one was not); `STREAM_CONNECTED` / `STREAM_RECONNECTING` (`lib/stream-posture.ts`) lost their last app consumer with the Ribbon specimens; `snapshotChipParts` (`lib/freshness.ts`) is now consumed by the styleguide only; `SurfacePlaceholder` (kept by R7 by name) has NO importer anywhere; `ExactValue` is styleguide-only (I3).
- **M3 · `lib/kit.ts`'s verdict-banner model is pin-only.** `VerdictVariant`, `VERDICT_TONE_CLASS`, `VERDICT_IDENTITY_REFUSAL`, `VERDICT_IDENTITY_ORDER`, `verdictIdentityMissing`, `verdictIdentityChips`, `verdictBannerModel`: consumers are `lib/kit.ts` and `tests/unit/kit.spec.ts` only. The unit pins stay green while guarding a component that no longer exists; the live law (`VerdictHeader.tsx:26-27`) is inline and e2e-only, behind a build flag. Deleting the model drops unit pins, which the test policy forbids by count — so the later wave should move the law (empty chips → the refusal chip) into a pure function with its own unit pin before the old model goes. `CHIP_TONE_CLASS`, `refusedChipSegments`, `exactValueMode`, `exactAriaLabel` are still live.
- **M4 · The crit header's chip set is the old strip's, not the Book's.** Kicker "Book · Cash" with `Coverage 2/2 engines` and `Evidence 3 pins` (`page.tsx:194-203`); the product's Book emits Batch · Snapshot (`48s · fresh`, tier-toned) · Coverage (`N / M computed`) · Current (`not projected`) (`lib/cash-view.ts:155-173`). A two-engine coverage chip under a one-engine kicker is a small incoherence in the canon; the old specimen's "$22.8M book" was the Aave engine's figure and now reads as Cash's.
- **M5 · The partial / superseded answers left the header canon.** The old five-variant grammar's note ("a banner whose data is superseded, refused, or partial switches variant — it never silently keeps the happy sentence") has no `VerdictHeader` specimen; partial coverage survives as one warn chip (`page.tsx:553`), superseded as `SupersededCard` in `sg-states`. Consistent with R2 (four tones); worth one sentence in the section note so the law is not lost with the variants.
- **M6 · Register details.** `StatusPill` "Projection · ETH −20% v3" is sentence-case inside all-caps `StatusChip` rows (`page.tsx:390,455`); the `sg-identity` note says "the border keeps the chroma" but `.chipOk` sets no border colour (`kit.module.css:42` vs `:43,45`); `.kvDim` and `kit.sub` put a value ("—") and a state ("no debt", "end reached") in `--ink-3`, which `tokens.css:27` reserves for captions — the kit itself does the same (`.tbl tr.dim td`, `.kpiPending .kpiV`), so this is the kit's register, noted not charged. The legacy engine is named by wire name only (`aave_v3`) in the table and its footer — carried over from the old specimen; if "labelled legacy wherever named" is meant to reach mono wire names, the single-engine rewrite of I1 removes the question.
- **M7 · Six `<h1>`s on `/styleguide`** (the page's + five `VerdictHeader` specimens, `VerdictHeader.tsx:32`). Dev-only route, no test asserts a single H1 there; harmless today, relevant if Task 12's QA walks heading structure over every route.
- **M8 · Ledger.** Test policy §7 / R8 want the retirements in `progress-ui-overhaul.md`: p1a-fixes (ExactValue-in-banner arm; `missing-identity-strip` / "Verdict withheld" arms; superseded/empty/partial variants), r1-fixes (10), shell walk (three ids renamed, three retired), styleguide sections `sg-chips` / `sg-marks` / `sg-stampline`. No Task 7 entry exists yet.
- **M9 · `kit.kv/kvK/kvV` unused while the styleguide adds a page-local `.kv`** (report Concern 6) — agreed as the smaller evil for this task; the two grids differ (`max-content 1fr` on a `dl` vs `1fr auto`), so adopting the kit class is not a drop-in. Later wave.

---

## Assessment

The task did what R7 asked and was careful where care mattered: references checked symbol by symbol, a wrong pre-flight assumption caught and reported, the contrast block and its pin left byte-identical, and the pins re-expressed with real, discriminating values that I could trace rule-by-rule to `kit.module.css` and `tokens.css`. The re-expressed five-banner pin is stronger than its ancestor, and the identity-law specimen gives a Global Constraint its first direct test.

The fix round is one file plus its pins. `TableSpecimen.tsx` has to follow the product twice over: print `—` in the refused row (the controller's pending item — and doing so is what makes the "dimmed" arm mean something), and stop folding two engines into one count and one sum (I1, plan-mandated, currently welded by the new pin at `p1a-fixes:428`). Making the specimen single-engine, as the Book's table is, resolves both the sum and the unlabelled legacy name in one move. I3 is a decision rather than a defect: say where the exact figure lives on the kit (the drawer, as the Inspector does) or rule `ExactValue` styleguide-only.

Everything else is dangling prose and dead exports for the pruning wave; the list in M1–M3 is longer than the report's and includes `web/README.md`, which should not wait long.

### Not verified

- The e2e project was not run (by instruction) — no pin, new or old, was observed passing in a browser; the colour assertions are verified by reading the cascade, not by rendering. Both themes, 390px no-horizontal-scroll for the new specimens, and the keyboard path through the drawer were not observed.
- `tsc`, `eslint`, `lint:css`, the unit project (899) and the build were not re-run; the report's gate results are taken as stated.
- The styleguide pins skip unless the build sets `NEXT_PUBLIC_SHOW_STYLEGUIDE=1` (CI does, `.github/workflows/ci.yml:74`); whether the controller's shared build does was not checked.
- The `find_referencing_symbols` sweep (38 symbols) was not repeated; consumers were verified textually with `git grep` at both revisions, which cannot see a dynamic import or a string-built path (none were found by name sweep).
- The dead-rule inventory inside `ribbon.module.css` / `primitives.module.css` (report Concern 3) was not re-derived class by class.

---

# Re-review 1 (de8dcbe)

Package: `b928a0b..de8dcbe` — one commit, six paths, subject line only, author Kase Lunt, no attribution lines. Read-only: nothing built, no suite run, only this section written. Every file outside `app/styleguide/**` was read from the commit (`git show de8dcbe:<path>`), not the working tree; `git status` shows the six paths clean, so the styleguide files in the tree are the commit's.

**RE-REVIEW: closed.** I1, I2, I3, M1, M4, M5 are each CLOSED; the untouched claims hold by hash; nothing the round introduced is Critical or Important. Four Minor notes (N1–N4) for a later wave, none a blocker.

## Rulings

| Ruling | | Trace |
|---|---|---|
| **I1** single-engine table, Book's columns, derived count + sum, footer sentence gone, pin re-expressed | **CLOSED** | Every row is Cash: no `engine` field survives in `SpecimenRow` (`TableSpecimen.tsx:21-33`), `DECIMALS = 6` is one scale (`:19`). Columns are the Book's — `Account · Room · Debt · Status` (`:102-107`) = `NeedsAttention.tsx:91-96`. Count is `belowLine.length`, sum is a `reduce` over those rows' debt (`:155-157`), label composed exactly as the product's (`:173` vs `NeedsAttention.tsx:115`). The footer is one sentence about Cash rows (`:176-179`); "aave_v3 … debt_manager … reconcile separately" is gone. Order follows the Book (`material, near, refused`, then `belowLine` appended — `:154-159` vs `NeedsAttention.tsx:84-85`). Pin: header list exact (`p1a-fixes.spec.ts:418`), label `($75.75)` (`:438`), 4 → 6 rows (`:439,444`), `$61.20` / `$14.55` on the unfolded rows (`:445-446`), old id gone |
| — the arithmetic | ✅ | `61_200_000 + 14_550_000 = 75_750_000` → `humanUsd(…, 6)` = cents 7575 → `$75.75` (`human-usd.ts:39-47`). `4_200_118_139 + 2_640_120_139 = 6_840_238_278` = 6,840.238278 at scale 6 → `humanUsd` truncates to `$6,840` (`:45`) — the crit header's emphasis (`page.tsx:194`) and the drawer row (`DrawerDemo.tsx:42`). Near-cap: debt `142_142_980_000` → `$142K` (`human-usd.ts:44`), room 6,077.02 over cap 148,220.00 = exactly 4.1% — the format `headroomPercent` emits (`headroom.ts:167-176`), and within the 10% the dek claims. Both folded debts are under `MATERIAL_LINE_USD = 100` and both shown liquidatable debts are over it (`materiality.ts:5,24-28`). Coverage `551 / 552 computed` agrees with the one refused row and "1 position could not be computed" |
| — nothing on the page sums or counts across engines | ✅ | The table, its toggle, its footer and the crit header are one engine. What still names two engines: `Coverage 2/2 engines` and `1/2 · DM withheld` (`page.tsx:547,556`; `sg-dimensions :327`) — a count OF engines answered, which is what coverage is, not a sum of their populations or money; and `PaginationDemo` (not in this package), which alternates two engines' rows under a loaded-row count — the same shape as Activity's cross-engine paged record (R4 "rows loaded"), a row count, not a population or a money sum. No violation |
| **I2** "—" as a bare text node under the refused pill; `refusedCell()`, header comment, `.refcell` gone; six-states retitled, comment replaced, count arm re-expressed; dim arm measures a `td` whose glyph inherits | **CLOSED** | `room: "—"`, `debt: row.debt === null ? "—" : humanUsd(…)` (`TableSpecimen.tsx:125-126`) — character-for-character `NeedsAttention.tsx:55-56`; `StatusPill tone="refused"` with the cause in `title`, label "Not computed" (`:127-131` = `NeedsAttention.tsx:58-60`). `refusedCell`, the "never an em dash" header and `.refcell` are absent at `de8dcbe` (grep: no hit in the three files). Test retitled (`p1a-fixes:376`); its comment now states the law as it stands (`:397-398`); arms: 4 cells (`:402`), 2 cells matching `/^—$/` (`:403`), no cell matches `/^\$?0(\.0+)?$/` (`:404-406`), pill label (`:407`) |
| — the cascade for the measured `td` | ✅ | `refused.locator("td").nth(1)` is Room (columns `:103-104`). `KitTable.tsx:46` puts `styles.dim` on the `tr`, `:48` gives the `td` class `r` only (text-align, `kit.module.css:96,101`). Colour candidates on that `td`: `.tbl td` → `--ink` (0,1,1) and `.tbl tr.dim td` → `--ink-3` (0,2,2) — the latter wins (`kit.module.css:100,106`). The cell's content is the JS string `"—"`, so React emits a text node directly under the `td`; nothing between glyph and `td` sets a colour, so the glyph renders `--ink-3`. The pin proves exactly that chain: text is `—` (`:422`), `childElementCount === 0` (`:423`), computed colour = the live `--ink-3` (`:424-426`). `web/components/**` has an empty diff `50c62a3..de8dcbe`, so the cascade is the one traced in the first review. The arm can fail three ways (dim class dropped → `--ink`; dash wrapped in a coloured element → count ≠ 0; dash replaced → text) — it is no longer vacuous |
| **I3** one human → exact drawer row, one pin, `sg-exact` untouched | **CLOSED** | `DrawerDemo.tsx:38-43`: `liquidatable debt` → `$6,840 · exact wire value <code>6,840.238278</code>`. The exact string is in `wireExact`'s register — grouped, full scale, no `$` (`app/inspector/[addr]/money.ts:34-40`). Shape vs `InspectorDrawer.tsx:90-93`: the product puts the FIELD NAME in `<code>` and the value in text; the specimen puts the VALUE in `<code>`. Same home, same register, mirrored markup — noted, not charged. Pin: `p1a-fixes:451-458` opens by `sg-drawer-open`, scopes to `getByRole("dialog")`, asserts `$6,840` and `code` = `6,840.238278`; it fails if the row leaves the drawer. `sg-exact` section, its pin and `ExactValue.tsx` hash identically at `ac3bbde`, `b928a0b`, `de8dcbe` (`30d223ed…`, `4f258834…`, `2db5fcea…`) |
| **M1** README names the kit; every law's sentence kept; no new section | **CLOSED** | `README.md:23,25,81-91`. Laws 3, 4, 5, 7 keep their sentences verbatim with the component names swapped; laws 1, 2, 6 untouched. Each new claim checked against the commit: `LivePill.tsx:7,23` reads `usePosture`; the Inspector drawer's as-of line IS per-input (`InspectorDrawer.tsx:65`, balances · params · sweep blocks); `tone="projection"` has exactly the three product users named in the report (`StressTable.tsx:49`, `AddressWorkspace.tsx:47`, `LabSurface.tsx:39`). No heading added |
| **M4** the crit header wears the Book's strip, Cash-plausible | **CLOSED** | Kicker `Cash book · right now` = `BookSurface.tsx:74`. Emphasis/rest/dek are `bookHeadline`'s own sentences verbatim: `book-headline.ts:93-94` (emphasis, rest), `materiality.ts:83` (the below-line sentence, `$100` line), `book-headline.ts:49` (near cap, singular), `:55` (not computed, singular). Strip = `cash-view.ts:154-173` at the commit: Batch `18,251` (`n()` groups), Snapshot `48s · fresh` with tone ok (`tierTone`), Coverage `551 / 552 computed`, Current `not projected` (`page.tsx:198-201`) |
| **M5** one sentence in the `sg-verdict` note | **CLOSED** | `page.tsx:261-263`, one sentence; it points at the two places the law still shows (the warn Coverage chip, the state card) |
| Untouched: `sg-tokens`, `ContrastSpecimens.tsx`, the p1a-6 contrast pin | ✅ | re-hashed at `ac3bbde` / `b928a0b` / `de8dcbe`: `sg-tokens` `2bd1849038ac` ×3; contrast pin body `e46bf5467f2a` ×3; `ContrastSpecimens.tsx` `9bab08306ab7` ×3. `InteractionRegisterDemo.tsx`, `tokens.css`, `globals.css`, `components/**`, `lib/kit.ts`, `lib/human-usd.ts`, `lib/refusal-phrasebook.ts`, `shell.spec.ts`, `r1-fixes.spec.ts`: empty diff `50c62a3..de8dcbe` |
| M2, M3, M6–M9 not touched | ✅ | none of their files or lines is in the six paths' hunks; one side effect in M6's favour — the `kit.sub` "no debt" state left with the no-debt row |

## The implementer's two questions

**(a) The refused pill's title — `collateral sweep failed · SWEEP_FAILED`.**
The code is real: `SWEEP_FAILED` is in the phrasebook (`refusal-phrasebook.ts:6` → "collateral sweep failed"), in the store's own enumeration (`internal/store/migrations/00013_risk_tables.sql:273`: `G1 | G2 | G3 | SWEEP_NEVER | SWEEP_FAILED | ENGINE`), and is the code every product fixture uses (`book.spec.ts:265`, `cash-rows.spec.ts:173`, `inspector-position.spec.ts:50`). The composition is the product's: `notComputedCause` returns `` `${plainCause(code, detail)} · ${code}` `` (`cash-rows.ts:80-81`); the specimen writes `` `${plainCause(code)} · ${code}` `` (`TableSpecimen.tsx:128`) — identical with no `detail`. The implementer's claim about the old code is also right: `sweep_failed_no_success` is not a wire code anywhere in `internal/`, `cmd/` or `packages/`; it is the P1 canon mockup's specimen string (`docs/specs/2026-08-09-p1-foundation-canon.html` ×7), and through the phrasebook it would print `refused (sweep_failed_no_success)`.
My read on the rest of the page: **not a defect for this round — a note for Task 12's wave.** The old string is not confined to the six paths: it is the default tag inside `components/states/RefusedCard.tsx:33` (`states/*` is ruled to stay untouched), it is a unit pin (`tests/unit/kit.spec.ts:175-179`, "canon specimen"), and three p1a arms weld it (`p1a-fixes:266,390,495`). Changing it is one canon decision across `states/*`, a unit spec and three e2e arms, and it comes with a wording choice too ("sweep failed twice" is the canon's cause; "collateral sweep failed" is the phrasebook's). What this round leaves is a page that spells one refusal two ways — the table in the product's real register, five other specimens in the mockup's. That is honest as far as it goes (each specimen is internally consistent and pinned), but the canon should not teach a code the phrasebook cannot read. Recommended for Task 12: convert all sites to `SWEEP_FAILED` through `plainCause` in one commit, or ledger the lowercase string as canon history and say so in a one-line note under `sg-pills` (N1).

**(b) `TableSpecimen.tsx` imports `lib/human-usd` and `lib/refusal-phrasebook`.**
Confirmed. Every money string in the table is `humanUsd` output — room (`:140`), debt (`:126,141`), the toggle's sum (`:173`) — from bigints at one scale; the plain cause is `plainCause` (`:128`). No dollar figure in the file is a typed string any more. Neither lib is in Tasks 9/10's edit set and both have an empty diff since `50c62a3`, so the specimen reads the same functions the product does at this commit. Copy the lib already owns, composed in the component: only one, and deliberately — the `" · "` join of cause and code re-states `notComputedCause` (`cash-rows.ts:81`) because that function takes a `CashRow` and `lib/cash-rows.ts` is under Task 10's uncommitted edit. Correct call for this round (N3 for after Task 10 lands). The toggle's label is composed in the component in the product too (`NeedsAttention.tsx:115`), so the specimen mirrors the product rather than bypassing a lib. `roomPercent: "4.1%"` is typed, but in `headroomPercent`'s exact format and arithmetically exact for the row's own cap. Outside the table, the header's and the drawer's figures in `page.tsx:194-196` and `DrawerDemo.tsx:42` are typed strings that equal what `bookHeadline` / `humanUsd` / `wireExact` would print for the table's rows — verified by hand above, tied by no code and no pin (N4).

## Anything the round introduced

- **A refusal rendered as zero / "No" / absence:** none renders. The refused row prints `—` twice and a named pill; the pin forbids a zero cell. Two latent coalescings exist in the file and cannot fire with the static rows: `humanUsd(row.room ?? 0n, …)` (`TableSpecimen.tsx:140`, mirroring `NeedsAttention.tsx:22`) and `sum + (row.debt ?? 0n)` (`:157`) — see N2.
- **A value in `--ink-3` that is a state, not a caption:** none added. The dash is the kit's own dim-row register (exempt by your framing); the one new `kit.sub` use is `· exact wire value` (`DrawerDemo.tsx:42`), a caption beside the value, which itself stays `--ink` mono (`.kv dd`, `.kv dd code { font: inherit }` — `styleguide.module.css:289-299`). The earlier `kit.sub` "no debt" state is gone.
- **A pin that cannot fail:** none. Each new or re-expressed arm has a concrete failing render (traced above); the previously vacuous dim arm is now the strongest arm in the test. The zero-regex loop runs over `allInnerTexts()` after the count assertions, so it cannot pass on an empty list (count 4 is asserted first).
- **A comment naming the round or the review:** none. A scan of every added line for round / review / issue ids / "was", "used to", "no longer" is empty; the new comments state laws ("One engine, as the Book's table is…", "The exact layer's kit home…", "The specimen follows the Book's table…"). `(plan 2026-09-16, R7)` in the file header is carried from `50c62a3` and matches the other tasks' convention.

## Minor notes (none blocks; all for a later wave)

- **N1 · Two spellings of one refusal on the canon page** — (a) above. Sites: `page.tsx:241,359,469,562,677`, `components/states/RefusedCard.tsx:33`, `components/StatusChip.tsx:58` (a doc example), `tests/unit/kit.spec.ts:175-179`, `p1a-fixes.spec.ts:266,390,495`.
- **N2 · The specimen's row type admits the null it then coalesces to zero.** `room: bigint | null` and `debt: bigint | null` for every status, then `?? 0n` at `TableSpecimen.tsx:140,157`. Unreachable today. The product closes the second path by type (`SizedCashRow = CashRow & { debt: bigint }`, `cash-rows.ts:191`); a two-arm union in the specimen (refused: nulls; sized: bigints) would make both coalescings unwritable in the file that teaches "never a zero". The first path exists in the product as well (`NeedsAttention.tsx:22`) and belongs to Task 10's set.
- **N3 · After Task 10 lands,** let the specimen's refused row be a `CashRow` and call `notComputedCause`, or lift the join into the phrasebook, so the title's format has one owner.
- **N4 · "One number in three places" holds by arithmetic, not by construction.** `$6,840`, `$75.75`, `$142K`, "2 accounts", "1 position" and `6,840.238278` are typed in `page.tsx` / `DrawerDemo.tsx`; the table derives its own. A shared specimen constant (rows exported from `TableSpecimen`, the header built with `bookHeadline`) would weld them — not before Task 10, since `book-headline.ts` / `materiality.ts` are in the `book-*` neighbourhood. Also: the row with id `dust` carries $14.55, which the lib tiers as `small` (`SMALL_LINE_USD = 1`, `materiality.ts:6,24-28`); both folded rows are "small" by the product's own line. A test-id nicety only — the toggle's label is the product's fixed wording.

## Not verified (re-review)

- The re-expressed and new pins were not run; no browser observed the dash computing `--ink-3`, the `thead th` list, the `/^—$/` filter matching exactly two cells, or the drawer row. The cascade and the matchers were verified by reading `kit.module.css`, `KitTable.tsx` and the spec, as before.
- `tsc`, `eslint`, `lint:css`, the unit project (904) and the build were not re-run. The implementer's one `tsc` error (`tests/unit/cash-summary.spec.ts(92,7)`, Task 10's uncommitted edit) was not reproduced or examined; I can only confirm that file is not among this commit's six paths.
- Whether the controller's shared build sets `NEXT_PUBLIC_SHOW_STYLEGUIDE=1` (the pins skip without it) was not checked.
- Both themes, 390px, and the drawer's keyboard path were not observed.
- `humanAge(48)` printing `48s` was taken from the existing freshness pin's text (`SNAPSHOT 48s`), not read from `lib/live-age.ts`.
- Task 9/10's uncommitted edits were not read; every claim about `lib/cash-*`, `lib/book-*`, `lib/materiality.ts`, `lib/headroom.ts` and `app/book/**` is about their content at `de8dcbe`, which the working tree may no longer match.
