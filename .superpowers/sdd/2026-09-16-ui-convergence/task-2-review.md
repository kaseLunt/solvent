# Task 2 review — demo dataset for History and Activity (R9)

Package: `review-cb22370..ee88513.diff` = commit `ee88513` exactly (7 files, 12,838 insertions, 1 deletion; `git diff cb22370 ee88513 --stat` matches the package line for line). Working tree at review time is byte-identical to the commit for all seven paths.

Method (read-only against the repo): read the brief, the plan header, the report, the generator, the weld spec and the two small edits in full; read the schema arms (`ObservatorySeriesPoint`, `ObservatorySeriesResponse`, `SweepStamp`, `RateIndex`, `EventDisplayType`, `EventAmountUnit`, `SeizedCollateral`, `LiquidationDetail`, `ChainEvent`, `EventFilter`, `EventsResponse`) from `api/openapi.yaml`; read `clock-law.mjs`, `buildBucketAxis`, `feedAmount`, `splitUntimedTail`, the store's classification maps (`internal/store/p5_events.go`, `collateralflags.go`) and the server's DM extract; audited all three committed bodies with a throwaway node script in the scratchpad (key sets, types, ordering, weld, drift, holes, rates, sweep ages, feed laws); re-ran the committed generator in a scratchpad mirror populated from `ee88513` blobs and hash-compared its output. No suite, no tsc, no eslint were run; no repo file was edited.

## Spec Compliance

| Requirement (brief / R9 / Global Constraints) | Verdict | Trace |
|---|---|---|
| Files: create generator + 3 bodies; modify `index.ts`, `fixture-clock-law.spec.ts`; test `demo-secondary-weld.spec.ts`; `generate-demo-lab.mjs` untouched | ✅ | Commit stat: exactly those 7 paths. Lab generator not in the diff. |
| 168 hourly buckets at the native stride per engine | ✅ | Both bodies: 166 points, oldest `2026-08-01T21:00:00Z` = `from`, newest `2026-08-08T20:00:00Z` (the hour of `book.batch.computed_at` 20:22:08Z); `to: null`, `step_seconds: null` (the contract's "every native bucket served"). `buildBucketAxis` inserts absent entries only between captured rows, so 166 points + 2 absent = 168 entries. |
| Hours 40 and 41 back absent — no point, not zero points | ✅ | The only non-1h gap in each series is `2026-08-07T02:00Z → 05:00Z` (3 h): buckets 03:00 and 04:00 carry no point. 20:00 − 40 h = 08-07T04:00, − 41 h = 03:00. No captured point anywhere has a `"0"` or null figure (audit: 0 zero-valued, 0 null-figured captured points). |
| Hour 90 withheld, the schema's exact withheld arm | ✅ | Point at `2026-08-05T02:00:00Z` (= 20:00 − 90 h): `refused: true`, `refusal_code: "FLAG_CUSTODY_UNPROVEN"`, `accounts: null`, `liquidatable_positions: null`, `debt_usd: null`, `collateral_usd: null`, `refused_positions: 0`, `rates: []`, `sweep_recorded: true`, DM sweep present / Aave sweep null. Field-for-field the shape of the contract example's own withheld point (`observatory-series-dm.json` points[1]), whose code the generator reads rather than retypes. Nulls, never "0". |
| Newest captured bucket = the Book's engine aggregates | ✅ | Audit against `book.demo.json` (the same blob the spec loads as `DEMO_BOOK`): DM `debt_usd` 27828808216758 = card `total_debt`; `collateral_usd` 153171572777189 = `total_collateral`; `accounts` 1412 = `positions`; `refused_positions` 6, `liquidatable_positions` 49 verbatim; `batch_id` 18251; `last_block` 155323444, epochs 0/0 = watermark; sweep deep-equal to both the watermark's and the card's; `usd_decimals` 6 = `value_decimals`. Aave: 190000000000000 / 410000000000000 / 8552 / 0 / 46 / 25714690 / sweep null / 8. All seven weld checks true on both engines. |
| Weld pins compare against the Book's own fields, not copies | ✅ | `demo-secondary-weld.spec.ts:46-47,94-116`: `card(engine)` = `DEMO_BOOK.engines.find(...)`, `watermark(engine)` = `DEMO_BOOK.batch.watermarks.find(...)`; every expectation reads `c.total_debt`, `w.last_block`, `w.sweep`, `c.sweep`, `DEMO_BOOK.batch.id`. The generator asserts the same weld before writing (`generate-demo-secondary.mjs:281-294`). No literal figure appears in either. |
| Drift ±0.4 %/h, landing exactly on the Book | ✅ | Bounded BigInt walk in whole bps, `offset(0) = 0`, step in [−40, +40], reflected at ±800. Audit: max |debt step| = 40 bps per hour on both engines; DM debt offsets −673…+40 bps (report: −674…+40 — a truncation-direction difference in my measurement, same walk); collateral > debt on every captured point. |
| `liquidatable_positions` = Book's L newest, ±1 elsewhere; `refused_positions` = Book's | ✅ | `L + ((7h+1) mod 3) − 1`. Histogram exactly 55/55/55 over {L−1, L, L+1} on both engines; newest = L. `refused_positions` 6 / 0 on every captured point. |
| Monotone provenance | ✅ | `batch_id` and `last_block` strictly increasing oldest→newest on both series; both engines observe the same `batch_id` per bucket; bucket 1 = 18206 @ 155322769 = `history-demo-near.json`'s last DM point before 20:00 (verified: 18206 @ 19:59:38Z, balances_block 155322769). |
| Rates | ✅ | One `RateIndex` per captured bucket with the exact key set; `as_of_block = last_block − 10` on every point (the example's own law: 154794000 → 154793990); DM row: `borrow_index` / `index-1e18`, asset = OP USDC `0x0b2C…Ff85` (chain-correct; the example's DM row carries mainnet USDC on an OP engine, which the generator correctly declines to copy); Aave row: `liquidity_index` / `ray-1e27` / `1.05e27`, asset = the Book's mainnet held-flat USDC. 165 distinct values (5 %/yr linear unwind, stated as a design figure). |
| Feed: `EventsResponse` shape | ✅ | Envelope exact key set; 50 rows; every row carries exactly `ChainEvent`'s 16 keys in the contract example's key order (generator asserts this at line 474); every `liquidation` carries exactly `LiquidationDetail`'s 11 keys; every seized leg exactly `SeizedCollateral`'s 5; `amount_decimals` null on every row; `liquidation` non-null iff `type === "liquidation"`; `filter` and `notes` are the contract example's verbatim; `limit` 50. |
| Feed: newest-first by `block_time`, untimed tail of 2 last | ✅ | 48 timed rows non-increasing by `block_time` (20:21:05Z → 17:35:15Z, all < `served_at`); rows 49–50 are the only `block_time: null` rows; tail `chain_id` [10, 1] (the chain-aware tiebreak, not chronology); per-engine `block_number` non-increasing across the whole page; every height ≤ the engine's Book watermark; chain ids = the Book's watermark chains. |
| Feed: 3 liquidations with the extract; 1 deficit pairing | ✅ | Exactly 3 `type: "liquidation"`. Two DM rows on distinct liquidatable demo positions (`0x5c0c…e25f`, `0xf0d7…4334`; both `status: computed`, `liquidatable: true`): `before_debt_usd` = position `total_debt`, `debt_repaid` = half (716240 → 358120; 819525 → 409762), `debt_decimals` 6, `debt_asset`/`deficit_paired`/`realized_bonus_bps`/`configured_bonus_bps` null (the schema's DM arm; `cmd/api/p5_feed_db_test.go:112` pins the same null), `interest_index` 1.05e18, one weETH leg at the meta's chain-10 witness ($4,000 at 6 dec) with bonus 5e18 in the 100e18 denomination — re-derived: 0.358120 × 1.05 / 4000 = 0.0000940065 weETH = 94006500000000 wei, matches. One Aave row = the contract example's extract verbatim with `deficit_paired: true` (deep-equal verified), and the `deficit_created` row shares its tx hash and block at `log_index + 1`, served first; `tx_hash` unique except that intended pair. |
| Feed: units per the store's law | ✅ | Every (engine, type) → (raw_type, amount_unit) combination in the body matches `dmEventDisplay` / `aaveEventDisplay` in `internal/store/p5_events.go:125-159` and the `collateralflags.go` constants: `supplied`/`withdraw_borrow_token`/`aave_supply`/`aave_withdraw`/toggles → `none` with null amount; borrows +, repays/liquidations/deficit −; DM amounts `dm_normalized_debt` (fixed point at the Book's 6 — `feedAmount(row, {engineValueDecimals: 6})` yields `rawUnits: false`, per `feed-view.ts:168-182`); Aave `aave_scaled` with `amount_decimals: null` (`rawUnits: true`, verbatim, per `feed-view.ts:150-166`). The spec pins exactly this (deviation 2, controller-accepted). All amounts match `^-?[1-9]\d*$`. |
| Feed: every display class drawn; accounts from demo positions | ✅ / accepted deviation | Type census covers all 8 classes (generator refuses to pad). All 21 DM rows are demo positions' own accounts; non-liquidation DM rows are computed, non-liquidatable. Aave accounts sha256-derived (deviation 1, controller-accepted; the demo has no Aave positions). |
| `next_cursor` set | ✅ | base64url over the last row's coordinates (`untimed.1.25713780.0x37ec.6.0`); non-null; opaque to the UI. |
| Clock law over the three bodies; census updated | ✅ | Generator imports `checkClocks` and refuses to write on failure or a pin mismatch (`writeChecked`). Re-run output: "166 / 0 / 0 clocks checked". Every DM point's `sweep.age_seconds` = floor(served_at − max_updated_at) (audit true on all 166, withheld included); `max_updated_at` shifts with the bucket. `CENSUS["demo/observatory-demo-dm.json"] = 166`; `CENSUS_TOTAL` 101 → 267 (101 + 166) with its derivation comment; the census walk recurses into `demo/` and requires an entry only where `checked > 0`, so the Aave series and the feed page (0 trios) correctly have none; batch-bearing count stays 40 because `isBatchBearing` keys on a `batch` member and a series point carries `batch_id`. The weld spec additionally extracts `CLOCK_TRIOS` from the generator source and cross-checks each body. |
| Generator idempotent / bodies are the generator's output | ✅ (proven) | The committed generator, run in a scratchpad mirror over the `ee88513` blobs of every input it reads, reproduced all three bodies sha256-identical to the committed blobs (dm `7b7b8adb…`, aave `91cf78e1…`, feed `5487ee67…`). No `Date.now`, no `Math.random`; all draws are sha256-seeded BigInt LCG; `JSON.stringify(body, null, 2)` as the lab template. |
| Exports | ✅ | `index.ts` adds `DEMO_OBSERVATORY_DM`, `DEMO_OBSERVATORY_AAVE`, `DEMO_FEED_PAGE_1`, typed `Schemas["ObservatorySeriesResponse"]` / `Schemas["EventsResponse"]`, with the generator's provenance sentence as the doc comment (the lab pattern). |
| Global constraints | ✅ | No backend, contract, `packages/client-ts` or `web/lib` change. Commit by pathspec with exactly the brief's message; no attribution lines (`git show -s --format=%B`). Fixtures keep refusals honest (withheld → nulls + code; absent → no point). |
| Nothing else moved | ✅ | Seven files; the only deletion is the `CENSUS_TOTAL = 101` line. |

**Spec Compliance: ✅**

## Strengths

- The weld is asserted twice — in the generator before a byte is written, and in the spec against the Book's own fields — and the byte-identity claim in the report is not just plausible, it reproduces.
- Chain identity is handled with more care than the contract's own examples: the DM rate row gets OP USDC where the example carries mainnet USDC on an OP engine; the DM seized leg uses the chain-10 weETH witness; the chain ids on feed rows are asserted equal to the Book's watermark chains.
- The withheld shape was read from the schema and the example, not from the brief's wording (`withheld: true`) — the body uses the contract's `refused` / `refusal_code`, and the withheld point keeps its sweep stamp as capture-time evidence exactly as the example does.
- The store's classification maps are pinned and the two contract example rows are asserted to match the pin, so a vocabulary drift in `p5_events.go` fails the generator loudly rather than silently mislabelling a row.
- Every "noted, not mine" in the report checks out (below), and the deviations are argued from the schema and the lib, not from convenience.

## Issues

No Critical. No Important.

**M1 — The header's overlap claim is wider than the data.** `generate-demo-secondary.mjs:44-47` says bucket 1 is welded to the Inspector history "so the two demo surfaces agree where they overlap". They overlap on batches 18152–18206, all of which `history-demo-near.json` places inside 19:32–19:59Z (bucket 1). The series then steps 12 batches an hour, so 18194, 18182, 18170 and 18158 land in buckets 2–5 (15:00–19:00Z) — earlier than the history says they were computed. Bucket 1 itself agrees; the clause overstates. Nothing renders or tests the join, so this is provenance prose, not a figure defect. Fix: either narrow the sentence to bucket 1, or start bucket 2's ids below the history's oldest (18152) before stepping 12/h. Plan flag: process — "comments state the law" (the provenance header is the law for these bytes and must be true).

**M2 — Four layout constants are not named in the provenance header.** `DEFICIT_AT = 4`, `LIQUIDATION_AT = {5, 19, 37}`, the log-index draw `1 + draw(40)` and the untimed tail's spacing `300 + draw(301)` s are design choices visible only in code, while the header opens with "Every figure names its source." Positions, log indices and spacing are not figures a reader would trust as data, so this is a discipline nit against the template's own standard, not a smuggled constant. Fix: one clause in the FEED bullet.

**M3 — The two DM liquidations read as sub-dollar events.** The stride-7 picks (indices 0 and 7 of the liquidatable rows) have `total_debt` 716240 and 819525 (USD-6: $0.72, $0.82), so the Activity page will show liquidations of −0.341066 / −0.390249 normalized USDC seizing 0.000094 weETH. The figures are the positions' own and the arithmetic is right; but the root is upstream — 47 of the demo Book's 49 liquidatable rows carry sub-dollar debt (median $0.59; only two ≥ $1,000; healthy rows run $301–$119K). For R10's "realistic figures" the two large rows would read better; either way the sub-dollar liquidatable set belongs on the owner's list for `generate-demo.mjs`. Plan flag: R9 "monotone-plausible figures" / R10 pixel pins — realism, not correctness.

**M4 — (owner's list, not this task)** The contract's own Aave witness `tests/fixtures/observatory-series-aave.json` states `scale: "ray-1e27"` with `value: "1050000000000000000"` (1e18 scale). This generator's Aave rate value `1.05e27` is the correct ray and therefore disagrees with that witness; the witness is the inconsistent one.

### The report's two "noted, not mine" fixture inconsistencies — both real

- **`events-demo-near.json`** (Inspector demo; `generate-demo-inspector.mjs:302-304`): DM `supply` rows with `raw_type: "supply"`, `amount_unit: "opaque"` and a non-null amount, and a DM `collateral_enabled` row with `raw_type: "collateral_enabled"`. Against the store: `dmEventDisplay` (`p5_events.go:152-159`) has `supplied → supply / none` and no DM collateral toggle at all (`aave_collateral_enabled` / `aave_collateral_disabled` are Aave-only, `collateralflags.go:52-53`). Against the schema: the *shape* validates (`raw_type` is a free string, `opaque` is in the enum), but the `EventDisplayType` description states the mapping `supply ⇐ aave_supply, supplied` and `collateral_enabled/disabled ⇐ the Aave usage-as-collateral toggles`, and `raw_type` is "verbatim from custody". So: schema-description- and store-classification-level, not a validation failure. Real. Owner: the Inspector demo generator.
- **`feed-liquidations.json`** (`generate-feed.mjs`): the DM row's `configured_bonus_bps: "500"`. The schema says the Debt Manager's rows "serve null rather than a silent unit conversion", and `cmd/api/p5_feed_db_test.go:112` asserts the server serves nil on DM rows. Real. Owner: the contract-fixture generator. This task correctly followed the schema (null).
- The report's third concern (meta mainnet weETH $9,999 vs chain-10 $4,000; the example's Aave seizure implies $4,000) is confirmed from `meta.demo.json` prices; it originates in `generate-demo.mjs` and the contract example, not here.

## Assessment

**APPROVE.** Every figure in the three bodies traces by rule to `book.demo.json`, `meta.demo.json`, `history-demo-near.json`, the demo positions pages or a contract example, with the design figures (drift, cadence, index unwind, nominal range, write-off) stated in the header. The withheld arm is the schema's, the absent hours are absent, the weld holds on both engines against the Book's own fields, the feed obeys `EventsResponse` and the store's unit law row for row, `checkClocks` walks all three bodies with the census moved by exactly the trios it finds, and the generator's byte-identity claim reproduces. The four Minors are prose, realism and an upstream witness; none touches a served figure. Deviations 1 and 2 stand on the schema and the lib and were accepted by the controller.

Not verified here (read-only mandate): the reported test runs (108 passed for the two specs; 842 for the unit project minus the sibling's half-written spec), `tsc --noEmit`, and `eslint` over `tests/fixtures/demo` / `tests/unit` (I could not confirm from `web/eslint.config.mjs` whether `.mjs` under `tests/fixtures` is in scope). The generator and `checkClocks` were exercised only in a scratchpad mirror.
