# Task 4 report — `activity-view` and the Activity page

Status: DONE (unit gates green; the e2e contract is written and NOT run — the controller runs it on the shared build per the pre-flight ruling).

Commits (scope gate OK on both): `f300b04` (lib + 13 pins), `e02d39f` (the page, the contract, the retirements, the shared-spec re-points).

## What was done

- `web/lib/activity-view.ts` — `deriveActivityView(input): ActivityView`. State: `refused` (a refusal held) > `error` (a fetch failure) > `exhausted` (no rows, no cursor) > `loading` (no rows, a cursor ahead) > `ok`. Kicker `Activity · cross-engine` / `Activity · {engineName(engine)}`. Headline = `feedTakeaway(rows, mode, hasMore)`, rest "", tone `ok` (`refused` before the first page answers); a refusal → `refused("Page refused · {code ?? bad_request}: {message}")`; an error → `refused("Page fetch failed: {message}")`; dek `The live strip and the paged record never blend.`. Chips Scope · View (`all actions` / `liquidations ledger`) · Order (`mode`) · Rows (`groupInt`) · Filter echo (today's echo string, `readWirePopulation` on `since_block` and `limit`, only with an envelope). Tiles: rows loaded (sub `more available` / `end of the filtered feed`) and liquidations in the loaded window (sub `mode`); with nothing loaded outside `exhausted` both are an em dash under the state's word, tone `refused` for refused/error. Rows = FeedList's law: `feedRowKey`, `dim` = cross-engine and no header time, `when` = `renderBlockTime`, `engineName`, `feedTagTone`, `feedAmount(event, { engineValueDecimals })` printed arm by arm (`amount` = display or `record-only`; `unit` = unit chip · `RAW_UNITS_TAG` when unscaled · symbol), `txExplorerUrl`, the liquidation extract as one line (`renderBps` for both bonus figures, every unestablished field an em dash). `orderNote` = today's two sentences by mode; `emptyText` = today's `empty`/`emptyExhausted` words; `doctrine` = the intro, `History: recorded chain actions`, the method line, the forensics note, the tail note; `drift` = the ORDERING DRIFT sentence when `splitUntimedTail` reports a violation. Exports `ACTIVITY_DEK`, `ACTIVITY_INTRO`, `ACTIVITY_LIST_TITLE`, `ACTIVITY_METHOD`, `ACTIVITY_FORENSICS`, `ACTIVITY_TAIL_NOTE`, `ACTIVITY_DRIFT`, `LEDGER_TYPES_NOTE`.
- `web/tests/unit/activity-view.spec.ts` — 13 pins on `DEMO_FEED_PAGE_1` (+ the committed cross/aave fixtures): 50 rows, the last two dim with `block 155,318,218` / `block 25,713,780`; 3 liquidations crit with their extracts (`bonus realized — / configured 500 bps`); DM scaled by the wire's 6 / raw + tagged without it, Aave raw + tagged, record-only, no `$`; tx link/label/title; engine-scoped null time not a tail; drift named; the headline EQUALS `feedTakeaway`; chips in order with the echo and its guard throwing; tiles (incl. the dash); refused; error (a refusal outranks it); exhausted; loading; order note and doctrine verbatim. Written first, red on the missing module, then green.
- `web/app/feed/page.tsx` — title `Activity`; renders `ActivitySurface`.
- `web/app/feed/ActivitySurface.tsx` ("use client") — FeedSurface's state machine MOVED, unchanged in law: the eight states, the `usePosture` → `valueDecimals` memo, the scope/mode, `fetchPage` with the `isCurrent` epoch gating of the envelope and the refusal, `useCursorPages`, the `resetRef` and auto-load effects, `restartWalk`, `switchEngine` (the since-block drop with both notice strings), `switchView`, `toggleType`, `applySinceBlock`. Then `deriveActivityView(...)` → root `activity-surface` (`data-state`, `data-mode`, `aria-busy`); `VerdictHeader` `activity-verdict` with the drawer island as `actions`; `KpiTile`s `activity-kpi-rows` / `activity-kpi-liquidations` (`pending` while loading, the view's tone); `FeedLiveStrip`; `SectionHead` `History: recorded chain actions`; `ActivityControls`; the order note `activity-order`; the strips `activity-notice` (status), `activity-refusal` + `activity-restart` (alert), `activity-error` + `activity-retry` (alert), `activity-drift` (alert, outside any fold); `ActivityTable`; the foot `activity-foot` with `activity-load-more` or `activity-end`.
- `web/app/feed/ActivityControls.tsx` — kit ghost buttons at chip size with `aria-pressed`: `activity-engine-all`, `activity-engine-{engine}` (labelled `engineName`), `activity-view-all`, `activity-view-ledger`, `activity-type-{type}` inside `activity-types` (or `activity-types-note` in the ledger view), `activity-since` (`data-possible`) holding either the `inputMode="numeric"` input `activity-since-input` with Enter handling, `activity-since-apply` and `activity-since-applied`, or the stated impossibility.
- `web/app/feed/ActivityTable.tsx` — `KitTable` `activity-table`, columns When · Engine · Type · Account · Amount · Tx; rows `activity-row-{key}` with `dim`; `StatusPill` crit for a crit type with the extract as its `title`; the account a `Link` to the Inspector in `kit.addr`; the amount `activity-amount` + the unit `activity-unit` (title = the unit's hover); the tx an explorer link or a titled span. `emptyText` from the view.
- `web/app/feed/ActivityDrawer.tsx` — the button `activity-drawer` and the kit `Drawer` whose body `activity-drawer-body` prints the doctrine paragraph by paragraph.
- `web/app/feed/FeedLiveStrip.tsx` — kept; stylesheet → `activity.module.css`; ids `activity-live` (root) and `activity-live-{state,none,batch,unavailable,degraded}`; logic untouched (F3's accent/unknown registers included).
- `web/app/feed/activity.module.css` — page-local rules on `--type-*` tokens only: the page grid, two tiles (1 column under 640px), the live strip and its tone classes, the controls (pressed ghost buttons take the accent via `[aria-pressed="true"]`), the since input, the order note, the warn/refused strips, the unit/type/tx cells, the foot, the drawer body.
- `web/tests/e2e/activity.spec.ts` — 14 pins: cold load (50 rows, headline = `feedTakeaway` = the H1, kicker, dek, five chips, two tiles, the row id shape, the Inspector link, no `$`); the untimed tail (rows 48/49 dim with their block numbers, 47 not, 2 dim, no drift); the ledger view (types=liquidation requested cursor-less, 3 rows, crit pills with the extract title, chips/note/tiles, back to all); since_block (impossible cross-engine, real engine-scoped via Enter, echo, dropped with the notice, no cursor across modes, no bound cross-engine); a 400 refusal (words, dashed header, rows survive, restart); load more (52, tiles/chips, end stated, headline without the cursor clause); 429/500 (strip, state, dashed header and tiles that never read zero, retry); the empty filter (exhausted, the sentence, true zeros); amount units (FEED_UNITS); cross-engine order is header time; ordering drift (alert, headline, dim only the untimed row); live posture (no base / a snapshot); the drawer (doctrine verbatim, the intro not on the page, Escape + focus); answer before evidence.
- Deleted: `web/app/feed/FeedSurface.tsx`, `web/app/feed/FeedList.tsx`, `web/app/feed/feed.module.css`, `web/tests/e2e/feed.spec.ts`.

## Retirement ledger — `feed.spec.ts` (14 pins) and the Activity pins elsewhere

| Old pin | Disposition |
|---|---|
| cross-engine walk: time-ordered head, DISCLOSED untimed tail across pages (`feed-row`, `feed-takeaway`, `untimed-divider` + its counted fold, `feed-time` block fallback, `feed-load-more`, "end of the filtered feed") | RE-EXPRESSED across activity.spec "cold load", "the untimed tail" and "load more appends": rows by `activity-row-*`, the tail as dim rows each with its block number, the headline with and then without the cursor clause, `activity-end`. The divider line, its count and its fold are RETIRED: the tail is the dim rows; the ordering rationale is drawer doctrine (`ACTIVITY_TAIL_NOTE`) |
| cross-engine order is header time — heights are visibly NOT the order (`order-note`, `feed-time` ×3) | RE-EXPRESSED "cross-engine order is header time": the When cells in wire order, no dim row, `activity-order` |
| engine-scoped: height order, no tail, since_block round-trip; a cursor never crosses modes (`since-block-impossible`, `since-block-control`, `order-note`, `feed-foot` echo, `feed-notice`, request law) | RE-EXPRESSED "since_block: …": `activity-since` (`data-possible`), `activity-since-input` (Enter applies; `inputmode` asserted), `activity-since-applied`, the Filter echo chip, `activity-notice`, the kicker/Scope/Order chips, the block fallback not dim, the same request-law loops |
| amount units render honestly (`feed-amount-unit` ×3, "record-only", raw opaque, no `$`) | RE-EXPRESSED "amount units render honestly": `activity-unit` filters (+ the raw-units count with the stream muted), `activity-amount` record-only and the raw opaque integer, no `$` in the table |
| liquidations ledger: typed extracts open, unestablished bonus stays a dash (`view-ledger`, `liquidation-detail` ×2 on FEED_LIQUIDATIONS) | RE-EXPRESSED in part — "the ledger view pins the type": the demo page's own three liquidations, `activity-view-ledger` pressed, crit pills with the extract on their `title` (`liquidator 0xBBbB`, `seized 0.65625 weETH`, `bonus realized — / configured 500 bps`). The DM two-leg / `configured 500 bps` assertions on FEED_LIQUIDATIONS are RETIRED: the extract is one line behind the pill here; the typed extract with its legs lives on in the Inspector's activity table (`lib/activity-rows.ts`, `tests/unit/activity-rows.spec.ts`, `tests/e2e/inspector.spec.ts`) |
| all-actions view: an UNESTABLISHED extract renders OPEN (`detail-toggle` aria-expanded) | RETIRED: the table has no fold and no toggle; the extract is the pill's hover (concern 1) |
| all-actions view: a FULLY-ESTABLISHED extract starts closed and opens on demand | RETIRED with the line above |
| ordering drift: the alert OUTSIDE the fold, the takeaway withholds its newest claim, the divider counts rows actually missing time | RE-EXPRESSED "ordering drift": `activity-drift` visible with `role="alert"` and its words, the headline sentence verbatim, only the untimed row dim. The divider's count clause is RETIRED with the divider |
| W-3L (432): the foot method line + the counted method-note fold (`feed-foot-method`, `feed-foot-forensics`) | RE-EXPRESSED "the doctrine lives in the drawer": `ACTIVITY_METHOD` and `ACTIVITY_FORENSICS` verbatim in `activity-drawer-body`; RETIRED as visible foot copy (R3) |
| a refused cursor (400) renders the envelope's own words + an honest restart | RE-EXPRESSED "a refused cursor (400)": `activity-refusal`, `activity-restart`, plus `data-state`, the dashed header and the surviving tile count |
| degraded envelopes: 429 and 500 render their reasons; retry recovers | RE-EXPRESSED "degraded envelopes": `activity-error`, `activity-retry`, plus the dashed header and the tiles that dash rather than read 0 |
| an empty filtered feed states its reason (`feed-empty`, `feed-takeaway`) | RE-EXPRESSED "an empty filtered feed is a real answer": `activity-table`'s words, `data-state="exhausted"`, the headline, the true zero tiles |
| live posture: no base frame → nothing pretended (`feed-live-strip`, `feed-live-none`, `feed-live-batch`) | RE-EXPRESSED "live posture" (first half): `activity-live`, `activity-live-none`, `activity-live-batch` |
| live posture: a delivered snapshot renders the batch's real watermark vector | RE-EXPRESSED "live posture" (second half) |
| p1a-fixes F3 ×2 (`feed-live-state` accent / awaiting base; `feed-live-none`) | RE-POINTED to `activity-live-state` / `activity-live-none`; the computed-color law unchanged |
| p1b-fixes fix 2 (the stale walk's echo; `feed-foot`, `type-chips`) | RE-POINTED: the echo is the header's `[data-chip="Filter echo"]`; the type buttons are `activity-types` (still named by the type words); the assertions unchanged |
| r1-fixes (4) ×2 (`feed-amount`, `feed-amount-raw`) | RE-POINTED to `activity-amount`; the raw tag is asserted inside the `activity-unit` cell |
| r1-fixes (6) `/feed` in the no-eyebrow sweep | KEPT unchanged (still true) |
| r1-fixes (10) the Feed intro in `main` | RETIRED in place with a one-line note; RE-HOMED to activity.spec's drawer pin (the intro verbatim in `activity-drawer-body`; `main` does not carry its opening; the dek keeps the closing clause, R3) |
| shell.spec.ts `/feed` h1 "Feed" | RE-POINTED to `/^Page fetch failed: /` — with every `/v1/**` aborted the first page fails and Activity says so as its H1 (`translate` rethrows a network error verbatim) |
| state-matrix.spec.ts feed cells ×7 (`feed-row`, `untimed-divider`, `feed-time`, `feed-empty`, `feed-load-more`, `feed-refusal`, `feed-restart`, `feed-error`, `feed-live-*`, `feed-live-strip`) | RE-POINTED one line at a time; the divider count → the second row `dim` with its block number |

## Gates

- `npx playwright test --project=unit tests/unit/activity-view.spec.ts`: 13 passed. Whole unit project: 884 passed, nothing excluded (the sibling's `verification-view.spec.ts` is committed and green; Playwright does not typecheck).
- `npx tsc --noEmit`: clean for every file of mine; the only errors are the sibling's in-progress `tests/e2e/history.spec.ts` (2; earlier in the session `tests/unit/history-view.spec.ts`, 1).
- `npx eslint app/feed lib tests`: clean. `npm run lint:css`: clean.
- `tests/e2e/activity.spec.ts` (14 pins) and the re-pointed shared specs: NOT run (no build during the parallel phase).

## Deviations

1. `ActivityRow` carries four fields beyond the brief's interface: `unitTitle` (the unit's hover, which FeedList showed and the brief's `unit: string` cannot), `txLabel` and `txTitle` (the brief's `tx: string | null` is the explorer link — nullable because `txExplorerUrl` is — and the cell also needs its short label and its full-hash title), `detail` (the liquidation extract; the brief lists `renderBps` and `liquidationEstablished` as consumed but the interface had nowhere to put an extract). `ActivityView` carries `drift` (the ORDERING DRIFT alert the headline refers to with "see the alert below"), and each tile carries `tone: "neutral" | "refused"` so a nothing-loaded page renders a dash, never a zero.
2. Tile subs follow the brief (`more available` / `end of the filtered feed`; Step 1 pins `more available`), not R4's sketch (`of the filtered feed` / `end reached`).
3. The doctrine has a fifth paragraph, `ACTIVITY_TAIL_NOTE`: the retired divider fold's rationale given its antecedent ("The untimed tail: rows without custodied header time render dimmed, each showing its block number, after every timed row, ordered by the chain-aware tiebreak … never compared as time."). The other four are verbatim.
4. `History: recorded chain actions` is both the table's `SectionHead` title and the doctrine's second paragraph, verbatim (the brief named it a doctrine note; the two-instruments naming also needs the heading over the list).
5. The liquidation extract is the crit pill's `title` (one line); there is no fold and no toggle. See concern 1.
6. The row's provenance (`block N · log N · seq N`, today's rules: block only beside a time, seq only when nonzero) is folded into the tx link's title so the p1b-14 population guards on `log_index`, `seq` and `chain_id` stay live at the read.
7. `feedAmount`'s option is `engineValueDecimals` (the brief wrote `decimals`).
8. The kicker, the Scope chip, the engine buttons and the Engine column print `engineName` ("Cash", "Aave v3 market (legacy)" — the legacy market labelled legacy wherever named); the wire ids stay verbatim where they are the wire's own words: the Filter echo, the since-block notice (moved unchanged) and the engine-scoped order note.
9. Two files outside the brief's list were edited one line/cell at a time because they pin this page's H1 and ids: `tests/e2e/shell.spec.ts` and `tests/e2e/state-matrix.spec.ts` (as Task 6 did). Revert if unwanted.
10. Commit `e02d39f` committed the staged index rather than a pathspec: `p1b-fixes`, `r1-fixes`, `shell` and `state-matrix` carry Task 3's live, unstaged History hunks, and a pathspec commit would have swept them in. Only my hunks were staged — a `-U0` patch filtered to the Activity hunks applied with `git apply --cached`, and the one mixed shell hunk (the adjacent `/observatory` and `/feed` lines) hand-split. Their hunks remain in the tree, unstaged and untouched; scope gate OK on the 17 paths.
11. Sub-ids beyond the contract table, all under `activity-` (R8): `activity-order`, `activity-drift`, `activity-types`, `activity-types-note`, `activity-since-apply`, `activity-since-applied` (and `data-possible` on `activity-since`), `activity-retry`, `activity-end`, `activity-foot`, `activity-amount`, `activity-unit`, `activity-live-{state,none,batch,unavailable,degraded}`; plus `VerdictHeader`'s own `activity-verdict-headline` / `-dek` / `-identity`.
12. The headline's tone before the first page answers is `refused` (no verdict yet — the lab's convention for a run in flight); the tiles render `pending` then.
13. The comments in the moved state machine were rewritten to state the law without round names (the brief's rule); the code is byte-for-byte the old surface's.
14. `activity.module.css` is on `--type-*` tokens throughout (the old `feed.module.css` used the `--fs-*` aliases).

## Concerns

1. The liquidation extract is hover-only. The W-3L 430 fence (an em dash never behind a closed fold) is honoured in letter — there is no fold — but an unestablished bonus now sits behind a `title`. The doctrine states the law and the Inspector carries the full extract with its legs. If the controller wants it visible, `ActivityTable` is the one place to add a line under the pill; the lib already carries `detail`.
2. The e2e contract is unrun. Watch: (a) `toHaveClass(/dim/)` relies on Next keeping the local class name in production CSS-module classes (`book.spec.ts` relies on the same); (b) the since-block pin applies via Enter on `activity-since-input`; (c) the live-posture pin's second half `unroute`s the stream and re-navigates; (d) `activity-row-{key}` ids carry `·` and the full tx hash — `getByTestId` with a template string; (e) the shell H1 regex assumes the aborted fetch's message reaches `Page fetch failed:` (it does by code: `translate` rethrows non-HTTP errors); (f) the `main` locator for "the intro is not page copy".
3. Task 3 edits the same four shared specs; the r1-fixes (10) title still names Feed — the controller reconciles once both land.
4. `liquidationEstablished` in `lib/feed-view.ts` is no longer consumed by any page (only by its unit spec); left in place (the lib unit count does not decrease).
5. Register items for Task 11/12: the Engine column prints "Aave v3 market (legacy)" per row (wide but nowrap); the type vocabulary is eight ghost buttons plus the since control (about two rows at 1280); 390px untested.

## e2e round

The controller ran `tests/e2e/activity.spec.ts` on the shared production build at b5f78eb: 13 of 14 passed. The one failure was the contract's own ambiguity, not the page's: in "cold load … state ok", `head.getByRole("link", { name: /^0x/ })` matched two links — the account (`0x3548…3937`, the Inspector) and the tx (`0x3354037a…`, the explorer) — a strict-mode violation before the `href` assertion.

Fix (commit `4afe8f1`, by pathspec; tsc and `eslint app/feed tests` clean; scope gate OK): the table's account link carries `data-testid="activity-account"` and the tx cell (link or titled span) `data-testid="activity-tx"`; the pin targets `head.getByTestId("activity-account")` for the Inspector href and now also pins `head.getByTestId("activity-tx")` to the row's Optimistic Etherscan URL. The row id form is unchanged. No lib change — the labels were never the view's.

Two more sub-ids under the `activity-` prefix (R8): `activity-account`, `activity-tx`. Not run here; the controller re-runs the spec.

## Fix round 1

The review (task-4-review.md): spec ✅ / Approve with changes; I2 was `4afe8f1`. I1 and I3 landed in one commit, `091beb7` (by pathspec; scope gate OK on six paths).

**I1 — the liquidation extract renders visibly.** `deriveActivityView`'s `detail` is now parts, not a string: `ActivityLiquidation { liquidator, liquidatorHref (/inspector/{liquidator}), repaid, repaidAsset | null, seized, bonusRealized, bonusConfigured, note }` — the amounts as `groupDecimalString(renderNullableDecimal(...))` at the extract's own decimals, both bonus figures as `renderBps` gives them (an unestablished one the em dash, never an estimate; the DM rows' null configured bonus a dash, never "0"). `ActivityTable` prints it beneath the pill in every view as a stacked line (`activity-liquidation`: `liquidator <Inspector link activity-liquidator> · debt repaid {repaid} {asset} · seized {legs} · bonus realized {—|bps} / configured {—|bps}`), the wire's note as the line's title; `.detail` is a block that wraps inside the nowrap cell (`max-width: 44ch`). The pill carries no title any more. `KitTable`'s row has no detail slot (read: `KitRow { key, cells, dim?, testId? }`), so the Type cell stacks. Pins: unit — the Aave extract as parts (`toEqual`), the DM extracts' nulls as dashes with no "0", the third row's Inspector href; e2e — the ledger pin now reads the visible line (`toBeVisible`, `bonus realized — / configured 500 bps`, the liquidator's href, the DM rows' `configured —` and never `configured 0`), and a new all-actions pin on `FEED_CROSS_PAGE_1` (`realized —` visible on cold load, no click, no hover, beneath the crit pill in its own row). These re-express the two RETIRED W-3L 430 pins.

**I3 — every notice from the lib.** `lib/activity-view.ts` exports `sinceBlockDroppedNotice(sinceBlock, candidate)` (both since-block sentences, words unchanged), `notABlockNumberNotice(draft)` (the 32-character quote, words unchanged) and `END_OF_FEED`; `ActivitySurface` prints them (the state machine's control flow is otherwise untouched) and the foot reads `END_OF_FEED`, which the rows tile's sub also uses — the sentence lives once. Unit pin added for the three.

Gates: `activity-view.spec.ts` 14 passed; whole unit project 892 passed; `tsc --noEmit` clean; `eslint app/feed lib tests` clean; `lint:css` clean. `activity.spec.ts` (now 15 pins) not run here — the controller runs it on the shared build.

New sub-ids under the `activity-` prefix: `activity-liquidation`, `activity-liquidator`.

Concerns: the extract line is up to 44ch wide and wraps to 2–3 lines beneath the pill, so a liquidation row is taller than its neighbours and the Type column widens — a register item for Task 11/12's width and read-through gates; M5 (the wire id in the engine-scoped order note and in the since-block notice) is left as the review recorded it — both are the wire's/the moved code's words; M6 (provenance in the tx hover) stands as a register note for Task 12.
