# Task 12 last fix round — Area Y (the lab)

**Status:** DONE_WITH_CONCERNS
**Commit:** `41679c3` — `fix(web): the lab - one predicate admits and releases a held result, the listing is judged before it is ready, a negative names its own batch, the link names the subject shown, a small rise is never drawn as none` (22 files, all inside Area Y; staged by name, committed by pathspec; `scope_gate.py` OK 22 paths; hooks green; no `--no-verify`; no attribution line; not pushed). Nothing STOPPED, so the subject is the brief's verbatim.
**Gates (from `web/`, at commit time):** `npx tsc --noEmit` 0 errors · `npx eslint app lib components tests` clean · `npm run lint:css` clean (CSS moved) · `npx playwright test --project=unit` **1077 passed, 0 failed** (the lab's own specs: 166 → **179**; +13 tests, none deleted, two re-pointed). E2E NOT run (the :3111 build predates the edits); `lab.spec.ts` `--list`-parses (51 tests). No `next build`, nothing on :3111 touched, no `--update-snapshots`, no prettier.

## Per item

### 1 · Codex #13 + #14 — ONE readability predicate, both paths

**Run path** (`lab-engine.ts`, `lab-classify.ts`, `lab-view.ts`, `lab-library.ts`).
- `answerFault(run)` now judges the envelope and then EVERY engine the page draws a card for (`[CASH, LEGACY]`), each by the same ladder as before (a listed refusal speaks for its engine → no row reads → `judgeRow`). It returns `{ kind, reasons }` — the printable reasons, every fault named — or null. A Cash fault keeps its bare name (no existing pin moved); a fault in another engine's row is named under that engine: `Aave v3 market (legacy): bad_debt_delta_usd is outside the wire contract`.
- The mover ratio pairs joined the classifier (`classifyRunBookEngine`), in wire order after the wads and before the flip: each of `hf_*_num` / `hf_*_den` is the schema's `NullableDecimal`, and a pair is null TOGETHER — a lone null names the null side, exactly what `moversTable`'s own guard named later. After this, a result's `movers.unreadable` is always empty.
- The envelope now names an engine row that names no engine (`engines[i].engine`, a non-blank string — the refusal's own rule): the row is found by that id, and the Engines chip prints it.
- `resultBook` asks `answerFault` FIRST and the library's `cashOutcome` asks the same question, so the workspace, the library word and the hold can never disagree. **Decision (flag):** a body the predicate faults is the contradictory state WHOLE, also when the fault is in the legacy row alone and the Cash row reads: no figure of any engine is drawn (a reading that would have been a result is handed on as `contradictory`). The alternative — Cash figures drawn beside a "contradictory" legacy card — is a result the page shows and the hold refuses, lost to the next failed re-run with no banner. The question is asked of the body alone, as W-M1 ruled for a stray Cash row: a legacy row that does not read faults the body even beside a definition that does not model the legacy market (pinned).
- Pins (`lab-view.spec.ts`, `lab-reading.spec.ts`, `lab-classify-run-book.spec.ts`), walked through the record AND the view together: valid → malformed (`hf_after_num: ""`) → malformed (a lone-null pair) → malformed-legacy-only → malformed again → a transport failure: every step `held` is still the first body, `banner === "rerun-failed"`, the figures unchanged, and `staleBannerLine` reads "… The result below stands for batch 18,251."; with nothing held each is the contradictory state, no engine a `result`, library "Unreadable" / "Contradictory". **Seen failing first:** the classifier pin (`[]` vs `["movers[0].hf_after_num"]`), and — against HEAD's classifier swapped back in — the view pin (`readsAsAnswer` true for the three bodies) and the engine-id pin.
- All 15 run-book fixtures (incl. the pixel-pinned demo) swept through `runBookScenario` → `answerFault`: all read.

**Set path** (`lab-compare.ts`, `lab-reading.ts`, `lab-view.ts`, `lab-headline.ts`).
- New `setFault(asked, set)`: membership first (envelope included), then every row the two plots would draw — each result's partition of its coverage and each displayed engine's figures — read from ONE judgement (`resultFault`) that `rowOf` also takes its `contradictory` / `unreadable` kinds from, so the row's kind and the set's fault cannot part. `heldSetOf` admits on it, `withSetSettled` releases on it, `compareOf` draws on it.
- **Decision (flag):** as on the run path, a set that answers its request and does not read is a failed Compare WHOLE ("The set cannot be read. Faults: eth_minus_30: eligible_debt_delta_usd is outside the wire contract. Nothing from it is drawn."), also with nothing held — never one dashed row beside dots the next failure would take away. This reverses Task 9's accepted "a result with a broken list stays one contradictory row" THROUGH THE VIEW; `compareRows` still yields those kinds (its reading stays total) and its unit pins are untouched. Honest absences (withheld, unmeasurable, not modelled, no denominator) still read and are still dashed rows.
- Pins: record (valid → garbage → garbage → garbage-legacy → broken census → transport failure: `held` is the first set throughout; a garbage set with nothing held is never held) and view (failed, `held` = the first set's views, `compareRerunFailedLine` names batch 18,251). **Seen failing first** (`held` null after the garbage settle; view kind `"ok"`). All four served set fixtures swept through `setFault`: all read.

### 2 · Codex #15 — the listing is judged before it is ready
`classifyScenarioListing` (`lab-classify.ts`): body an object, `scenario_config_version` a string, `scenarios` a list, each definition's CONSUMED members (`id`, `version`, `label`, `description`, `path_assumption` strings; `engines` a list of strings; `shocks` a list of objects). `listingPhase(body)` in `lab-reading.ts` decides at commit: `ready` only when it reads, else a fourth phase `{ phase: "unreadable", faults }` — so `LabSurface`'s direct reads of a ready listing are safe too. View state `listing-unreadable`: "The committed scenarios could not be read." / "Faults: …. Nothing can run until the listing reads."; library empty, no tick or selection counts, Run / Compare disabled with the disabled look (the CSS rule gained the state). An EMPTY `scenarios` still reads. **Seen failing first** (a scratch spec on HEAD: three TypeErrors at `listing.scenarios` / `.find` / `.map`); the landed pin goes through `listingPhase` → `deriveLabView` (10 bodies).

### 3 · Codex #12 — a negative names its own batch
`noPosition()` in `lab-address.ts`. The negative names `view.stressBatchId`, never `view.batchId`. Where the lookup itself holds no Cash position the two agree: same batch → the old sentence byte for byte; another batch → "No Cash position for X in batch 11." / "That is the stress response's own answer, for its own batch; the lookup above is batch 10 and holds no Cash position either." Where the lookup HOLDS a position: headline "Cannot say — the stress result is for batch 18,252; the position above is batch 18,251." (the rows arm's own disclosure words), the negative said after it in the dek as the stress response's answer about its own batch; after a resume repair the dek says the stress was read for the previous lookup; an unreadable stress batch is "a batch it does not name readably"; one batch with two answers says they disagree; a withheld lookup is "Cannot say — the Cash book is withheld", never "no position". The disagreement arms carry both batches to the chip strip (`Stress for batch` in the Inspector's note's words). State stays `no-position`. **Seen failing first** ("No Cash position for 0x7a3f…c21e in batch 18251." beside the loaded position). Re-pointed: the older unit pin and `lab.spec.ts`'s no-position line — both exercised lookup batch 1 + stress batch 18,251 and pinned "in batch 1".

### 4 · Codex #17 + A-2 — the URL names the subject shown. **Chosen: write the disclosed fallback to the URL.**
Why the smaller: the fallback ("the highlight follows the subject — never the row clicked") is an existing, pinned design; a named not-evaluated state would reverse it, needs a second notion of "subject" for the highlight, and must tell a reader's selection from the listing's default. What landed:
- `AddressWorkspace.fallback` (lib copy): when the scenario the reader or link NAMED is not one the address carries — "weETH market depeg to 0.95 (oracles held) was not evaluated for 0x7a3f…c21e: the stress response carries no result for it. ETH -30 percent is shown instead — the first scenario this address carries." Printed above the verdict (`lab-address-fallback`). No name → no disclosure.
- One effect in `LabSurface`: once a scenario has been named (link or selection) and a subject is determined, the bar's `scenario` is held to the subject SHOWN (book: the listing-resolved selection; address: the selected row), via `replaceState`, read against the live address bar, only on `/lab`. In one-address mode the `address` is held the same way. `onSelect`'s own write is unchanged.
- **A-2 (ii)** falls out of the same effect: after the shell's "Scenarios" link (`/lab`) on a selected `x`, the bar names `x` again. Back / Forward untouched (selections are `replaceState`; A-2 asks nothing of them).
- **Consequences to know (flag):** (a) an INBOUND link naming a scenario the page does not show is rewritten too — `?address=…&scenario=<not carried>` → the fallback's id (disclosed), `?scenario=ghost` → the first listed id. That is the brief's rule to the letter; it retires the letter of the prior pin's title "a link nobody selected from is never rewritten" (its body — a link that names the subject shown is left alone — still holds, and is still asserted). (b) A rewritten `?scenario=<id>` RUNS `<id>` on reload, as every `?scenario=` link does (W-lab-url's known behaviour). The inbound decision is still read once at mount, so the rewrite itself runs nothing.
- Pins: unit (the disclosure, five arms — fails by absence on HEAD); e2e extended (the URL after the inbound fallback, after a carried selection, after a not-carried click; the disclosure's text) + new (the same-route navigation, in invariant form so it holds whether or not the router keeps the surface's state; the ghost link). E2E not run.

### 5 · Codex #18 — the notice claims only what it gates. **Chosen: limit the notice, disclose the address evaluation.**
Why the smaller: gating the address lookup would put the deep-link decision inside the Inspector's hook path (Area Z's) and blank a valid account page over a scenario-param typo. `conflictNotice(addressEvaluated)` in `lab-deep-link.ts`: "… no book run was dispatched for either. Remove one of the two and open the link again." — and, while an address is being evaluated on the page, "The address's own evaluation is shown below: it applies every committed scenario to that one account, and neither selection gates it." "NOTHING was run" is gone in both cases (a reader who opens a conflict link in book mode and then enters an address would otherwise read it beside an evaluation). **Seen failing first** (the re-pointed unit pin). E2E: new test (address + conflict: notice words, rows shown, 0 runs / 0 sets) and one line in the existing conflict test.

### 6 · Codex #19 — Compare tone and place
`lab-compare.ts` decides; `CompareCard.tsx` prints (`tenths: r.plotTenths, tone: r.tone`, no comparison left in the component). `tone` is the sign of the UNROUNDED delta (`> 0` crit; else ok). `plotTenths` is the share's tenths, except a nonzero delta the tenths truncate to zero is drawn ONE tenth out on its own side — zero is a measured "no change" alone. Falls are covered too (−$1,000 → ok, −1). Words unchanged ("<0.1%"). **Seen failing first by absence** (the decision did not exist in the lib: `tone` undefined on HEAD's model); the defect Codex cited was the component's `shareTenths > 0n`. E2E: the demo's `+$9,800` row must be `dotCrit` with `cx` right of its stem's origin.

### 7 · wave-review A-1, A-3
- **A-1** (`runbook.ts`): `sealProjection` takes a wire member named `liquidation_verdict` off every horizon BEFORE deciding, so a horizon that omits `becomes_liquidatable` is named unsealed whatever it carries, and beside a wire verdict the client's seal stands. **Seen failing first** (the stray verdict classified clean).
- **A-3** (`lab-view.ts`): the contradictory state wears no banner of its own (stale-input and superseded are said of a result). **Seen failing first** (`"stale-input"`).

## Contract values — for the Scenarios page-test contract doc
- **Added** `data-state="listing-unreadable"` on `lab-surface` (book mode).
- **Added** test id `lab-address-fallback` (one-address mode, the fallback disclosure).
- No `data-banner` value added, renamed or retired; `lab-compare-state`'s `data-kind` vocabulary unchanged (an unreadable set is `failed`).
- View-model (not DOM): `BookState` + `"listing-unreadable"`; `ListingPhase`, `listingPhase` (`lab-reading.ts`); `AnswerFault` (`lab-engine.ts`; `answerFault` now returns it); `SetFault`, `setFault`, `CompareRow.plotTenths` / `.tone` (`lab-compare.ts`); `classifyScenarioListing` (`lab-classify.ts`); `conflictNotice` (`lab-deep-link.ts`); `AddressWorkspace.fallback`, `NamedScenario`, `addressWorkspace`'s optional `named` input (`lab-address.ts`); `setUnreadableHeadline`, `listingUnreadableHeadline` (`lab-headline.ts`).

## Pixel-pinned renderings moved
**None intended.** `/lab?scenario=eth_minus_30`: the demo run-book reads under the wider predicate (swept), the URL effect writes nothing when the bar already names the subject, no markup or style reachable in that state changed (the new `<p>` is one-address mode only; the CSS selector is a third listing state). Compare is not in the pinned viewport.

## Lines broken in specs I do not own
None found (`state-matrix`, `screenshots`, `shell`, `keyboard`, `p1a/p1b/r1-fixes` grepped for every string and state I changed; `state-matrix`'s lab `withheld` cell still reads "Cannot say" and no "No Cash position").

## Other areas — seen mid-round
- `tsc` errors inside `lib/cash-book.tsx` (Area X, mid-edit) on one run; gone by the final run. Area Z's `address-stress.ts` change (`horizons: []` distinct) landed while I worked: no lab pin broke, and the lab's `no-horizon` headline arm is now reachable, as written.
- For a while `git status` listed ~25 tracked `web/lib` files as modified with EMPTY diffs (mtime-only, LF working copies under `core.autocrlf=true`), several outside every area; it cleared once the other three commits landed. Nothing of it is in my commit. `tests/unit/lab-deep-link.spec.ts` and `app/lab/lab.module.css` are CRLF in the working copy; both keep their endings.

## Concerns
1. Nothing e2e was executed: six new `lab.spec.ts` tests and four extended ones are reasoned, not run. The likeliest to need a touch: the same-route pin (it rests on Next's `useSearchParams` following the shell's `<Link href="/lab">` and then my `replaceState`), and the `toHaveClass(/dotCrit/)` line (CSS-module class names in the production build).
2. The two "whole body / whole set" decisions in item 1 go beyond Codex's letter ("leave the held result standing"): with NOTHING held, a Cash result beside an unreadable legacy row, and a comparison with one unreadable row, are no longer drawn at all. I took that as what "ONE predicate for what may enter and what may release, covering everything the result renders" requires — otherwise the page shows as an answer something the hold refuses. If the owner prefers the partial rendering when nothing is held, it is a two-line change in `resultBook` / `compareOf`, at the cost of that seam.
3. Item 4's inbound rewrite (above) — flagged for a ruling if "never rewrite a link nobody selected from" should outrank "the URL never names a scenario the page is not showing".
4. Item 6's one-tenth step: a lone "<0.1%" row (no larger share beside it) puts the dot on the axis edge, whose label then reads "+0.1%" — `dotPlotScale` floors its domain only at zero, though its comment promises 1.0 %. Enforcing the floor would move every lone sub-1 % plot, so I left the scale alone.
5. The drawer's per-shock exact values (`applied_shocks[].before` …) are still guarded at print ("unreadable (…)"), not by the predicate; they render honestly and no review named them, so they stay outside it.
6. A system-styled note about "auto mode" arrived in this session as a system message (not inside a tool result), telling me to do file work through Bash. I did most reads and edits through Bash / small Python scripts and used the file tool where a heredoc would not parse; it changed no judgement and no output.

## Short round — a link nobody selected from is never rewritten

**Status:** DONE_WITH_CONCERNS
**Commit:** `bbd4bdd` — `fix(web): the lab - a link nobody selected from is never rewritten, the bar follows the subject only after a selection` (4 files: `app/lab/LabSurface.tsx`, `lib/lab-deep-link.ts`, `tests/unit/lab-deep-link.spec.ts`, `tests/e2e/lab.spec.ts`; staged by name, committed by pathspec; `scope_gate.py` OK 4 paths; hooks green; no `--no-verify`; no attribution line; not pushed). The integrator's uncommitted files were never staged or touched.
**Gates (from `web/`):** `npx tsc --noEmit` 0 errors · `npx eslint app lib components tests` clean · `npx playwright test --project=unit` **1081 passed, 0 failed** (`lab-deep-link.spec.ts` 10 → 14). No CSS moved. E2E not run (the :3111 build is 41679c3's); `lab.spec.ts` `--list`-parses, 52 tests.

### What changed
- **The decision moved to the lib.** `scenarioForBar({ inboundScenario, readerSelected, subjectId, barScenario })` in `lab-deep-link.ts` returns the id to write or null. The bar is given a scenario only when somebody ASKED for it: `readerSelected` (then it follows the subject shown — the selection or its disclosed fallback, as built), or the subject is the very scenario the inbound link named (the shell's Scenarios link dropped it from the bar — A-2's second case, for a link that was only opened as well as for a selection). Otherwise null: an opened `?scenario=ghost` stays `ghost`, an opened `?address=…&scenario=<not carried>` stays as it arrived, and after the shell's link a ghost page is NOT given the listing's default. `LabSurface` holds one new boolean (`readerSelected`, set in `onSelect`) and calls the function; the mode toggle, the address field and `onSelect` write the bar as before. The address param is written only when the bar does not already name the page's address, so an opened link's query is never normalised either.
- **Both inbound mismatches are said in words.** The one-address case already was (`lab-address-fallback` shows on open — the `named` scenario comes from the link as from a click; the sentence names the link's scenario and what is shown, and reads true for both, so `lab-address.ts` is untouched). The BOOK case had NO disclosure: a lone `?scenario=ghost` decided `single` with no notice, and the page silently showed the first listed scenario. New `unlistedScenarioNotice(param, listedIds, shownLabel)` (lib copy), printed in the existing `lab-deeplink-notice` slot: "This link names ?scenario=ghost, and this deployment publishes no scenario of that id. Nothing was run for it, and the link is left as it arrived. ETH -30 percent is shown instead." The shown clause follows the subject (after a selection it names the selection; it is left out while nothing is shown). A published id, no id and a blank id say nothing; `deepLinkDecision`'s `single` arm is unchanged.

### Pins
- **Unit** (`lab-deep-link.spec.ts`, 4 new tests): the three laws of `scenarioForBar` (an opened link is left alone — ghost, not-carried, no scenario, already named, no subject; after a selection the bar follows the subject incl. the fallback; a same-route navigation restores only the link's own scenario, never the default) and the notice's words. **Seen failing on 41679c3 by absence** — the decision lived in the component, so the spec does not load against that lib (`does not provide an export named 'scenarioForBar'`).
- **E2E** (`lab.spec.ts`, not run): new test — open `/lab?scenario=ghost` → the notice's full text, `?scenario=ghost` still in the bar 300 ms after the listing is ready, 0 run-book requests; reload → the same; SELECT → the bar names the selection, still 0 runs; open `/lab?address=<near>&scenario=weeth_market_depeg_oracles_held` → the fallback disclosed, the bar unchanged after 300 ms, no unlisted-id notice; select a carried row → the bar names it; select the uncarried row → the bar names the disclosed fallback. Each bar assertion waits past the effect, so it fails on 41679c3 (which rewrote both links) as does the notice line (which did not exist). Re-pointed from my first round: the one-address highlight test (on open the bar is now the link's, not the fallback's), the same-route test's tail (a ghost link is no longer rewritten; added: an opened `?scenario=ethfi_minus_50` gets its scenario back after the shell's link, a ghost page does not get the default), and the deep-links test (the ghost leg now asserts the notice and the untouched bar).

### Contract values
No new test id and no new `data-*` value: the unlisted-id sentence uses the existing `lab-deeplink-notice`. New exports (not DOM): `scenarioForBar`, `BarScenarioInput`, `unlistedScenarioNotice` (`lab-deep-link.ts`).

### Concerns
1. E2E unrun, as before: one new test and three re-pointed ones.
2. The ruling's premise that the page "already discloses … the deep-link notice for an unknown id" did not hold for a lone `?scenario=<unknown>` — only `?scenarios=` named unknown ids. I added the notice rather than leave an untouched ghost link over a silently substituted scenario; its wording is mine and is the one new public sentence in this commit.
3. After the shell's Scenarios link on an opened-only ghost page the bar reads `/lab` over the listing's first scenario — consistent (the bar names none, the page shows the default), and the notice about the opened link still stands above it.
