# Plan 2 (Inspector) — Codex correctness wave report

**Status:** DONE_WITH_CONCERNS
**Commit:** `14a5927` on main — `fix(web): inspector - the Codex round's eight: unknowable horizons are cannot-say, the drawer speaks the lookup's state, the stress batch is named and a mismatch disclosed, the streak is the lookup's batch's own, durations and scales guarded, a pagination failure shown beside its rows, the history sentence true`
**Files (13, staged by name):** `web/lib/address-stress.ts`, `web/lib/inspector-view.ts`, `web/lib/inspector-position.ts`, `web/lib/activity-rows.ts`, `web/app/inspector/[addr]/StressTable.tsx`, `web/app/inspector/[addr]/InspectorDrawer.tsx`, `web/app/inspector/[addr]/ActivityTable.tsx`, `web/app/inspector/[addr]/BackingTable.tsx`, `web/tests/unit/address-stress.spec.ts`, `web/tests/unit/inspector-view.spec.ts`, `web/tests/unit/inspector-position.spec.ts`, `web/tests/unit/activity-rows.spec.ts`, `web/tests/e2e/inspector.spec.ts`

No file under `web/app/lab` or `web/lib/lab-*.ts` was touched. No CSS touched (`lint:css` not run). No kit component touched.

## Fix 1 — an unknowable horizon is a cannot-say, never "No" (StressTable)

**What changed.** The judgement moved into the lib. `web/lib/address-stress.ts` gains `StressVerdict` (`not-applicable` · `cannot-say` naming the unknowable horizon or none · `liquidatable` with `within`/`already` · `inside` with `through`) and `stressVerdict(row)`, mirroring `rowVerdict` in `lab-address.ts`: the gate first (not applicable, or `flips === null` — a missing or unknowable side), then a projection is judged by its horizons only (an unknowable horizon refuses the row and names the horizon, even when a later horizon would flip; a liquidatable one is the first in wire order; otherwise inside through the longest), then a spot shock by its flip. `stressVerdictWords(verdict)` returns `{ text, tone, title }` so the words live in the lib; `StressTable.tsx` prints from it (a pill when `tone` is set, a titled span otherwise). The old `flipsAt === undefined ? "No"` branch is gone.

**Pin.** Unit (`address-stress.spec.ts`, "stressVerdict: an unknowable horizon…"): the Codex shape (spot sides not liquidatable, 30d unknowable, 90d not-liquidatable) → `cannot-say` naming the 30d horizon, words "Cannot say" / refused / title "the 30d horizon carries no verdict"; unknown-then-flip still cannot-say; "Within 90d"; wire order for `within`; "Not within 90d"; the side gate before horizons; Yes / Already liquidatable / No / the reason. E2e (`inspector.spec.ts`, "stress: an unknowable horizon…"): the demo stress with the 30d horizon `becomes_liquidatable: null` → the projection row's last cell is the refused pill "Cannot say" with that title and never contains "No"; the untouched demo → "Not within 90d", row 0 "Yes".

**Deviations.** (a) The verdict carries `already` like the lab's: a spot row liquidatable on both sides prints "Already liquidatable" (crit, titled) rather than the old "No" — a liquidatable account answered "No" under "Becomes liquidatable?" is the same class of untruth. (b) A projection that holds prints "Not within 90d" (its longest horizon) instead of "No": a projection cannot speak past its horizons. Both are the lab's own rules (`rowOutcome` says "Stays inside its cap through 90d" / "Liquidatable today and after"). (c) The Inspector's gate is `flips === null` (side missing or unknowable); the lab additionally refuses negative figures as "not-a-position". Not extended here — the Inspector's verdict is the engine's boolean, and the table's room cells already print the wire's figure — noted for the Scenarios follow-up if the two pages should agree on it.

## Fix 2 — the drawer speaks the lookup's state (InspectorDrawer)

**What changed.** `drawerEmptyText(view)` in `inspector-view.ts`: `no-position` / `legacy-only` → "No Cash position in this batch; nothing to calculate."; `cannot-compute` → the headline's own emphasis ("Cannot say — the Cash book is withheld this batch.") + "A withheld book is never “no position”; there is no calculation to show."; `loading` → "Looking up this address… nothing to calculate yet."; everything else (unavailable, invalid, the unreachable Cash-state-without-wire) → the headline's emphasis + "There is no calculation to show." The drawer's `p === null || cash === null` arm renders it under `data-testid="inspector-drawer-empty"`.

**Pin.** Unit (`inspector-view.spec.ts`, "drawerEmptyText speaks the view's state…"): `ADDRESS_UNKNOWABLE` → the withheld sentence, never "No Cash position"; a withheld Cash book under `found` beside a legacy position → the same; not-found and legacy-only → the no-position sentence; error and loading arms. E2e ("the drawer, open across a refresh that withholds the book…"): the near account, drawer opened and showing the formula; the address route re-mocked to `ADDRESS_UNKNOWABLE` (address field set to the route's, since the reading is keyed by the route's address); a `pageshow persisted` resume repair (the same trigger the resume test uses, its request counted) lands the withheld lookup; the surface goes `cannot-compute`, the drawer stays open, its body reads the withheld sentence and never "No Cash position" / "nothing to calculate".

## Fix 3 — the stress batch is the stress response's own (`stressBatchId`)

**What changed.** `StressReading` carries `batchId: number | null` on all three arms, read from `lookup.response.batch.id` through `isWirePopulation` (null when absent or refused by the guard). `InspectorView.stressBatchId` (named exactly so) is `stress?.batchId ?? null`. `stressBatchNote(view)` returns `{ disclosure, rowLabel } | null`: when the stress batch differs from the lookup's, `disclosure` = "Stress for batch 101; the position above is batch 100. Each row's before and after are read for batch 101 and are not compared against the position above." and `rowLabel` = "batch 101"; when the stress body names no readable batch, "The stress response names no readable batch; the position above is batch 100. …" / "batch not readable"; null when they agree or the stress has not answered. `StressTable.tsx` renders the disclosure as a `role="note"` line under the section head (`inspector-stress-batch`) and each row's scenario cell wears `rowLabel`. The table's "Room today" and "Room after" were already the stress body's own sides; `flips` was already computed within the stress body — nothing in the table ever compared against `view.cash`, so the fix is the disclosure and the labels, plus the field the Scenarios wave consumes.

**Pin.** Unit (`address-stress.spec.ts`, "the reading carries the stress response's own batch…"): batch 1 / 101 / no-position / withheld arms; `-1`, `1.5`, `-0`, `NaN` → null. Unit (`inspector-view.spec.ts`, "the stress batch is the stress response's own…"): lookup 100 + stress 101 → `stressBatchId` 101, the Batch chip still "100", the exact disclosure and row label; the withheld arm carries it; an unreadable id → the "names no readable batch" disclosure; no note when they agree or before the stress answers. The earlier `toEqual({ kind: "rows", rows: [] })` pin now includes `batchId: 1`. E2e ("stress answering for another batch…"): stress at 18,252 under a lookup at 18,251 → the identity says "Batch 18,251", the note (role note) says "Stress for batch 18,252; the position above is batch 18,251." and "not compared against the position above", each of the three rows carries "batch 18,252", "Room today" is the stress body's own $190.50, the flip still "Yes"; the fix-1 e2e also pins the note's absence when the batches agree.

**Deviations.** The brief says null "only when the wire omits it"; an id the population guard refuses is treated as no id too (the law: a wire value passes its guard before use), and that case is disclosed rather than silently tied to the position's batch. The disclosure carries a second sentence after the brief's exact one; the pins check the brief's sentence by containment and the whole by equality.

## Fix 4 — the headline's streak is the lookup's batch's own

**What changed.** `deriveInspectorView` passes `cashHeadline` a `streakOfThisBatch` = the history's streak only when `historyBatchId === batchId`; otherwise null, so the near-cap headline states the current batch alone. `view.streak` stays the history's own (the History card's `historyFinding` still says the run with its vantage clause).

**Pin.** Unit (`inspector-view.spec.ts`, "the headline's streak is the lookup's batch's own…"): history vantage 2 (near cap in 0–2) under a lookup at batch 4 → the dek is exactly the two base sentences with no "for the last", while `historyFinding` says "Within 10% of its cap for the last 3 batches · history as of batch 2, position as of batch 4 …"; the same history at vantage 2 under a lookup at 2 → the dek says "It has been within 10% of its cap for the last 3 batches (≈2m)." The pre-existing assertion in the "near cap" test (lookup 3, history 2) encoded the defect (`toContain("for the last 3 batches (≈2m).")`); it is now `not.toContain("for the last")` with a comment stating the law.

**Deviation.** The gate is the history's vantage batch, not its newest point's batch: `roomSeries` always places the vantage on the axis, so a vantage equal to the lookup's batch means the newest point IS that batch (a real point or a no-row gap, which zeroes the streak). The Codex scenario's "batch 3 healthy" cannot be expressed by a vantage-2 history; the pin asserts the claim is simply not made.

## Fix 5 — `horizon_seconds` guarded

**What changed.** `horizonLabel` returns `UNREADABLE_HORIZON` ("—") unless `isWirePopulation(seconds)`, before any comparison, remainder or division. In `row()`, a horizon whose duration fails the guard carries the verdict `"unknowable"` whatever the wire said, so `stressVerdict` makes the row a cannot-say naming it, and `stressVerdictWords` titles it "a horizon with an unreadable duration carries no verdict". `StressHorizon.seconds` stays `number` (the lab compares it and I may not touch the lab); its doc says it is unread until `horizonLabel`.

**Pin.** Unit: `horizonLabel(60.5)`, `(-1)`, `(-0)`, `(NaN)`, `(Infinity)`, `(2**53)` → "—"; `(0)` → "<1m". Unit ("a horizon whose duration fails the population guard…"): the STRESS_DM projection with `horizon_seconds: 60.5` / `-1` → verdict unknowable, the row cannot-say, the title above.

## Fix 6 — a refused `value_decimals` refuses every USD figure of the position (collateralTable)

**What changed.** `collateralTable` reads `cash.decimals` (the reader's guarded scale): when null, each leg's `value` and `contribution` are null (so `ltv` is null and both sums and both welds are null), and the new `CollateralTable.usdRefusal` names it once: "USD figures withheld: the engine published an unreadable value scale (value_decimals) for this account, so no value, contribution or LTV is read from it." The legs' own scales (`amount` at `leg.decimals`, `price` at the input's) still read. `BackingTable.tsx` prints `usdRefusal` once under the table (`inspector-backing-refusal`); the money cells already printed "—" through `moneyFor(null)`; the LTV cell now prints "—" too.

**Pin.** Unit (`inspector-position.spec.ts`, "collateralTable: a refused value_decimals…"): `value_decimals: -2` with a leg `value_usd: "100"`, `max_borrow_contribution: "50"` → no `ltv`, no `value`/`contribution`, null sums and welds, the sentence; amount "2.1" and price "$4,000.00" untouched; a licensed scale → `usdRefusal` null and "50%".

## Fix 7 — a pagination failure shown beside its rows (ActivityTable)

**What changed.** `lib/activity-rows.ts` gains `activityEmptyText(loading, error)` (the words the component composed inline) and `activityFailureText(error)` = "More activity could not be loaded: {message}. The rows above stand; nothing beyond them was read." `ActivityTable.tsx` keeps the rows and renders the failure as `<p role="status" data-testid="inspector-activity-error">` under the table whenever `error !== null && rows.length > 0` (with no rows the table's empty words carry it, so it is never printed twice). `hasMore` is untouched by a failure, so "Load more" stays for a retry.

**Pin.** Unit (`activity-rows.spec.ts`): the three empty arms and the failure sentence. E2e ("activity: a refused 'Load more'…"): the events route answers the first page with `next_cursor: "page-2"` and any `cursor=` request with the 503 envelope; six rows, no error line, the takeaway says more exist; click "Load more" → the status line reads "More activity could not be loaded: 503 unavailable: no complete risk batch is available…" and "The rows above stand…", the six rows remain, the table never says "Activity unavailable", the button is visible and enabled.

## Fix 8 — the History sentence speaks from the streak and the newest point's own kind

**What changed.** `historyFinding` reads `room.newest` and the streak together: a zero-cap newest → "The newest batch carries a zero cap — debt with no counted collateral, past the cap; no room percent to read" (+ "; under the 10% line for the last N batches" when the run is ≥ 2); any other non-computed newest → the existing "is {kind}; the streak cannot be read"; a run ≥ 2 → the existing streak sentence; a run of exactly 1 → "Within 10% of its cap in the newest batch" + what ended it ("; the batch before was above the line" / "; the batch before is {kind}, so no longer run can be read" / " — the only batch in the window"); "Room has stayed above the 10% line in the newest batch" only for a computed newest outside any run (true by the streak's own rule). The vantage clause and the dashed-line note are unchanged.

**Pin.** Unit (`inspector-view.spec.ts`, "historyFinding speaks from the streak…"): the Codex inputs — cap 100000000, debts 80000000 then 95000000 → "Within 10% of its cap in the newest batch; the batch before was above the line · …" and never "above the 10% line"; a run of one ended by a withheld batch; a lone point; a lone zero-cap point → the refusal sentence, never "above the line"; a zero cap heading a run of 2; a computed newest above the line still says so.

## Test line

`tsc --noEmit` clean · `eslint app/inspector lib tests` clean · unit project **771 passed** · `next build` clean · on that build, `playwright test --project=e2e tests/e2e/inspector.spec.ts tests/e2e/state-matrix.spec.ts` **48 passed** (inspector 19 = 15 + 4 new; state-matrix 29); the stale :3111 server (PID 39496) was killed first and Playwright's `webServer` owned and released the port. `scope_gate.py` OK (13 paths); pre-commit hook passed (doctor OK, scope gate OK). No `--update-snapshots`; no prettier on lib/ or tests/.

## Concerns

- **The tree is shared and moving.** While this wave ran, files outside my scope became modified in the working tree (`web/lib/book-headline.ts`, `cash-rows.ts`, `cash-summary.ts`, `live-pill.ts`, `positions.ts`, `stress-preview.ts`, `web/tests/e2e/lab.spec.ts`, plus the pre-existing `screenshots.spec.ts` / `screenshot-pages.mjs` edits). They were not staged; but my `tsc`/`eslint`/unit/build runs necessarily compiled whatever state they were in at the time. The commit is self-contained; a re-run of the gates on a clean checkout of `14a5927` is the honest confirmation.
- **Screenshot baselines.** `inspector-dark.png` / `inspector-light.png` are committed viewport captures at 1440×900; the stress table (whose projection row now reads "Not within 90d" instead of "No") sits below the fold, so the baselines should be unaffected. Not run (not a gate of this wave); if `screenshots.spec.ts` fails on the Inspector after this, that row is the cause and the owner re-baselines.
- **`useKeyedFetch` double dispatch — none traced outside StrictMode.** `useKeyedFetch(addr, valid, epoch, fetchStress)` has deps `[addr, enabled, epoch, fetcher]`; `fetchStress` is `useCallback(…, [addr])`, `valid` is derived from `addr`, `epoch` moves only on `reload()`. One effect run per `(addr, epoch)`, one request; the cleanup aborts it on change. The surface is also remounted with `key={addr}`, which discards the hook and runs a fresh effect — the old instance's cleanup aborts its request, so still one live request per address. StrictMode's mount→cleanup→mount in dev is the only replay path, and it aborts the first. Not changed.
- **A resume repair moves the lookup but not the stress.** `loadLookup({ keepOnFailure: true })` (the `pageshow`/clock resume) refreshes the position without bumping `epoch`, so the stress reading can legitimately stay one batch behind after a resume — exactly the mismatch fix 3 now discloses rather than hides. Whether the resume should also replay the stress is a design question for the owner (the brief says the stress-on-entry design is approved; a resume replay is a different question).
- **Fix 1's negative-figure gate** differs from the lab's (`not-a-position`); see deviation (c). If the Scenarios follow-up wants both pages to refuse negative sides identically, `stressVerdict` is the one place to add it.
- **Fix 3's null batch** treats a guard-refused id like an absent one and discloses it; the brief's wording ("null only when the wire omits it") could be read as wanting a throw there instead. Easy to flip if the controller prefers the `readWirePopulation` posture.
