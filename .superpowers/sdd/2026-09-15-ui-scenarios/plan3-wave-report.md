# Plan 3 close-out wave — report

Status: DONE. One commit on main, `626d01e` (parent `c92f4fd` plus the other waves' commits landed in between), 25 paths by pathspec, `python roadmap/tools/scope_gate.py` OK (integrator claude-integrator; 25 paths), the pre-commit hook OK, no Co-Authored-By, no prettier on lib/ or tests/, no `--update-snapshots`.

Gates (from `web/`): `npx tsc --noEmit` clean for the wave's files (the one error during the round was the Book/Overview wave's `LivePill.tsx` mid-change; it resolved before the build); `npx eslint app/lab lib components tests` clean; `npm run lint:css` clean (CSS moved: `charts.module.css`); `npx playwright test --project=unit` 812/812; `npm run build` then `npx playwright test tests/e2e/lab.spec.ts` 37/37 (C1–C28 green plus C29–C37).

Files: `lib/lab-address.ts`, `lib/lab-headline.ts`, `lib/lab-library.ts`, `lib/lab-reading.ts`, `lib/lab-view.ts`, `lib/lab-compare.ts`, `lib/lab-engine.ts`, `lib/lab-classify.ts`, `app/lab/{AddressWorkspace,CompareCard,LabSurface,LabTiles,LegacyCompare,TransitionCard}.tsx`, `app/lab/money.ts`, `components/charts/DotPlot.tsx`, `components/charts/charts.module.css`, `tests/unit/{lab-address,lab-headline,lab-library,lab-reading,lab-view,lab-compare,demo-lab-weld}.spec.ts`, `tests/e2e/lab.spec.ts`. Nothing touched under `lib/inspector-*`, `lib/address-*`, `app/inspector/**`, `app/book/**`, `app/overview/**`, `lib/cash-*`, `docs/**`.

## A · From the whole-branch review

**A1 (I1 / Codex 2) — the verdict column speaks from `rowVerdict`.** `lib/lab-address.ts` exports `rowVerdictWord(row): { text, tone, title }` (the Inspector's cell shape) spoken from `rowVerdict`: liquidatable → "Yes" (crit pill); a projection → "Yes · within 90d" (warn pill — the projection's tone everywhere else in the lab: `rowOutcome`, `rowHeadline`; the brief's "crit pill" is read as the spot case); already liquidatable → "Already liquidatable" (crit, the Inspector's word for the arm the brief did not name); inside → "No" (a projection carries the longest horizon as the hover); cannot-say → "Cannot say" (refused pill, the cause as the hover); not-applicable → the reason, plain. `AddressWorkspace.tsx` composes nothing: the cell is the word, pill when the tone is non-null. Pins: unit (`lab-address.spec` "rowVerdictWord and sideRoomWords…": the demo rows, the `projected(1, true)` fixture whose `flips` is false yet says "Yes · within 90d", the negative wire figure whose `flips` is true yet says "Cannot say", the missing side, the already arm, the inapplicable arms); e2e C29 (the demo address with the projection's 90d horizon flipped and ETHFI's shocked debt negative, shaped through the mock's `stress` option — the parent printed "No" and "Yes" there).

**A2 (I2) — the room cells are the tiles' words.** `sideRoomWords(side, decimals: number | null)` is exported; the two room cells print it: "$190.50", "over cap by $1,069", "not computed" for a missing/unreadable/unknowable side, and the refused scale word where the position's scale failed the guard (`UNREADABLE_SCALE`, moved from `app/lab/money.ts` into `lib/lab-headline.ts` so the copy lives in the lib; `money.ts` imports it). Pins: unit (the same test: each register, and no "−" on a dollar figure); e2e C29 (the eth row's "over cap by $1,069" where the parent printed "−$1,069"; ETHFI's "not computed" beside "Cannot say", no "$4,822" in that row, no "−$" in the table).

**A3 (I3 / Codex 7) — the signed net has its sentence.** `lib/lab-headline.ts` `resultHeadline`: when `newly ≤ 0` and the merged lanes' `crossedCap > 0` → emphasis "Net, 3 fewer Cash accounts are liquidatable under {label}," (or "Net, no more Cash accounts are liquidatable under {label},") rest "though 118 accounts cross the cap." tone warn, the dek unchanged; `newly < 0` with no crossing → "{n} fewer Cash accounts are liquidatable under {label}." tone ok; the singular in both. Only the wire's two figures are stated (the net, the lanes' crossings) — no derived "leave it" count, because the matrix and the summary are not reconciled and a derived figure could contradict the lanes (the demo's own lanes show 0 leaving band 0 beside any negative net). `signedCount(n)` (the true minus for a negative, the plain count otherwise — no plus, a count is not a delta) prints the tile in `LabTiles.tsx`. `lib/lab-library.ts` row word: "Net −3 accounts · 118 cross the cap" (warn) / "Net −3 accounts" (ok). Pins: unit (`lab-headline.spec`: the four arms and `signedCount`; `lab-library.spec`: the four words); e2e C30 (`withCash` with `newly_eligible_accounts: -3`: the exact headline, `data-variant="warn"`, the tile's "−3" and not "-3", the row's word, and "No Cash account becomes liquidatable" absent from the page).

**A4 (I4 / Codex 6) — envelope arrays and null subtrees never throw.** `lib/lab-compare.ts`: `censusBreak` names any of `covered_engines`/`engines`/`withheld_engines`/`unmeasurable_engines` that is not a list ("engines is not a list" → contradictory row); `setMembership` names `requested_scenario_ids`, `results` and `evaluation.scenarios_evaluated` (the count it reads) before any iteration; `compareRows` never dereferences a missing `results` (the gate refuses it first). `lib/lab-engine.ts` `readEngine`: `excluded_engines`/`engines` guarded by name; the classifier runs first and its faults are the reading's before any nested read (`before: null` → `["before"]`, `hf_transitions: null` → `["hf_transitions"]`); the reader's own duplicate guards (every one already in the classifier's walk) are gone with the early return. `lib/lab-view.ts` `resultBook` reads the two lists through the same guard for the chips and the identity. Pins: unit (`lab-compare.spec` "the envelope's lists…": each list, the bare `{}` body, the four engine lists; `lab-view.spec` "readEngine never throws…": both lists, the null side, the null matrix, and both through `deriveLabView` → the contradictory state with the field named). Not guarded (outside the ruled arrays): `set.batch.id` and `evaluation.freshness`/`newest_servable_batch_id` in `compareRows`, `run.batch` in `resultBook` — a body missing those objects would still throw; noted as a concern below.

**A5 (I5) — the highlight is the subject.** `LabSurface.tsx` address-mode map: `selected: r.id === (space.selected?.id ?? null)`. Pin: e2e C31 (`?address=…&scenario=weeth_market_depeg_oracles_held` → eth_minus_30 highlighted with the headline; clicking ethfi moves both; clicking weeth again falls back to `rows[0]` (R15) and the highlight follows it — my first draft of the pin assumed the previous subject stays; the lib's fallback is the first row, and the pin now says so).

## B · From Codex's Plan 3 round

**B3 — the hold survives a malformed answer.** `lib/lab-reading.ts` `withSettled`: `held = heldOf(prev)` always (the previous ok result). `lib/lab-view.ts` `bookOf`: an ok body whose Cash reading reads as an answer (result, withheld, not-covered, definition-changed) stands; one that reads `unreadable`/`contradictory` over a hold is a failure under the shared `overHeld` law — the held result under `rerun-failed` with the contradictory headline as `rerunFailure`, a retained body whose definition changed disclosed as `retained-refused`. `outcomeLine` follows: the held word stands under a malformed 200. Pins: unit (`lab-reading.spec`: the hold survives an ok settle and the newest ok moves into it on the next run; `lab-view.spec` "a 2xx body that does not read…": banner + failure headline + held figures + the row's word, the no-hold case unchanged, the contradictory matrix, the withheld release, the retained-refused arm; `lab-library.spec`: the word); e2e C32 (run, then a 200 with `eligible_debt_delta_usd: "1e6"` → the banner's exact sentence and batch, state result, 118, the heatmap, the row's word; then the same body with nothing held → contradictory, no banner).

**B4 — a failed Compare holds the comparison.** `SetRecord` gains `held: HeldSet | null` (`{ ids, response, at }`), on both phases; `withSetRunning`/`withSetSettled` (pure, exported) hold the last ok set that passed `setMembership` through every failure and release it only on a new ok that passes (`heldSetOf` recomputes the gate; a set that did not answer its request is never held). `CompareState.failed` carries `held: { cash, legacy } | null` — two views, not one, because the ruling keeps the legacy fold too (deviation from the brief's `CompareView | null`). The card draws the held views under the finding "Compare again failed — {emphasis} {dek} The comparison below stands for batch N." (composed in the card from the headline, as `StaleBanner` composes "Run again failed"), `data-held="true"` on the state span. Pins: unit (`lab-reading.spec` "a set that answered…"; `lab-view.spec` "compare: a failed Compare never replaces…"); e2e C33 (the demo compare, then 429 → the exact line, both dots, the legacy fold, the button freed). The hook's `runSet` uses the two pure functions.

**B5 — the legacy fold renders on any legacy row.** `CompareCard.tsx`: the fold mounts when `legacyRows.length > 0`, not when a point exists; refused rows are dashed rows with their word. Pin: e2e C34 (the legacy engine withheld on eth and not modelled on ethfi → the fold, both rows refused, no circle, no "$").

## C · The deferred lines

**C1** — `lib/lab-classify.ts` comments at the seven sites reworded to the law (no round, no retired function: the histogram counts, the matrix's own scale, the cell floor, the classifier's delegation now names `readEngine`, the movers total names the caption); `lib/lab-compare.ts` "the old Lab's R57 gate" dropped. The file header's "Moved verbatim from the old Lab's …" is provenance, not a round; left.

**C2** — the singulars: "1 account becomes", "but 1 changes band", "1 improves" (`lab-headline.ts`), and the card's "1 account changes band; 1 crosses the cap; 1 improves" (`TransitionCard.tsx`, M6); `status` required on the `failed` arm through an overload of `failureHeadline` (M23; pinned with `// @ts-expect-error`, which `tsc --noEmit` checks since tests are in the program); the `flipped` pin is `toBe` on the whole dek; `sentence`/`terminated`/`refused` exported from `lab-headline` and imported by `lab-address` (its own copies deleted — its `sentence("")` returned "." where the shared one returns "").

**C3** — `lib/lab-reading.ts`: `run` refuses when `controllers.current.has(id)` before `canDispatch`; the set's controller lives in its own stable slot (`setSlot`, no `"__set__"` key in the id space), checked before `canDispatchSet`, aborted on unmount; the redundant `SetRunOutcome` annotation gone; the law comments on the refs, the controller maps and the unmount effect. Pin: e2e C37 — two synchronous clicks on Run in one tick, then on Compare: one POST each (React flushes a discrete event's updates after the handler, so the second click meets no committed record; the controller in flight is what refuses it).

**C4** — `cashEngineOf`/`cashRefusalOf` deleted with their pin; the `EngineRefusal` type export and the `LabRunBookEngine` import that existed only for them went with them.

**C5** — `setMembershipHeadline` uses a fixed lead: dek "Faults: {terminated(faults)} Nothing from it is drawn." — no fault is recapitalised. Pins: the existing dek pins updated (unit `lab-headline.spec`, `lab-view.spec`); the id-leading case pinned ("Faults: eth_minus_30 was dispatched…"); the e2e C28 `toContainText` pins hold unchanged.

**C6** — the three missing pins: e2e C35 `?address=0xnope` → `data-state="invalid"`, the sentence, the table's empty word, no lookup; C29 the projection row's verdict column; C36 "no denominator · +$5" (the demo set with ethfi's `total_debt_usd_before: "0"`, delta `5000000`). C35 and C36 pin behaviour the parent already had (the review's "unpinned"), C29 fails on the parent.

**C7** — the unreachable `flips === null` arm removed from `rowVerdict` (the gate makes `flips` a boolean past it; the spec's `{ ...eth, flips: null }` pin of that arm removed with it — it pinned an impossible fixture); `rowHeadline`'s JSDoc says "missing, unreadable figures or an unknowable verdict".

## D · The added item

**D1 — the Compare card reads at a glance.** (a) `compareHeadline(view)` in `lib/lab-headline.ts`: the largest share by magnitude first ("ETH -30 percent moves the most: +$1.2M more liquidatable Cash debt, 4.5% of the book."), the rest in the view's rank order ("ETHFI -50 percent: +$9,800, under 0.1%."), a fall as "less", every refused row "{label} could not be evaluated: {words}.", all refused → "No scenario could be evaluated for the Cash book: {label} withheld; …", all zero → "{labels} leave liquidatable Cash debt unchanged.", the legacy view in its own words. The row words moved into the lib as `compareRowWords(row, engine)` (the card's `KIND_WORD`/`noteOf`/not-covered constants are gone; the value column and the finding print the same words). (b) `compareCaption(view)`: "Change in liquidatable Cash debt per scenario, as a share of the Cash book (batch 18,251)." — "(batch 18,251 — superseded)" only when not the newest; rendered under the plot frame as `lab-compare-caption`; "Absolute figures beside." and "(still the newest)" are gone; the superseded notice with its pill stays (e). (c) `DotPlot` takes `valueHeader` ("share · change", over the value column, mono) and the cells keep "+4.5% · +$1.2M"; `shareWords` prints "<0.1%" unsigned under the tenths (the sign is the dot's side and the figure's); row labels wear `.rowLabel` (`var(--sans)`, `--ink`); the value column stays mono. (d) A stem (`line[data-role="stem"]`) from zero to each dot in `.stemCrit/.stemOk/.stemWarn` — `color-mix(in srgb, var(--tone) 45–55%, transparent)` as the heat tints are mixed, stroke-width 2 — refused rows keep the dashed track and no stem; the axis keeps −max · 0 · +max and takes `axisCaption` ("share of the Cash book"; the legacy fold passes "share of the legacy book"), drawn only with the axis, so a plot of refused rows carries neither. (e) Nothing else on the card changed; the legacy fold keeps its words. `charts.module.css` (the DotPlot's own stylesheet, not on the brief's list) carries the four classes — the ruling could not land without it; `lint:css` clean. All-refused: the dashed rows still draw (refusals stay visible, per the page's law) with no axis, stems or caption — my reading of "no plot is drawn (as today)". Pins: unit (`lab-headline.spec` "compareHeadline: …" — rank order, the refused arm, no-denominator, all refused, the falling lead, all zero, the legacy view, no rows; "compareRowWords and compareCaption: …" — the words, the caption in each freshness); e2e C24 (the exact finding sentence, the caption, the header and axis-caption text in the SVG, one stem per point, the "<0.1% · +$9,800" cell and no "+<"); C25 (the refusal named in the sentence); C26 (the superseded caption); `lab-compare.spec` and `demo-lab-weld.spec` `+<0.1%`/`−<0.1%` pins updated to "<0.1%".

## Deviations and notes

- The e2e pins could not be run red against the parent: the parent's production build failed while the Book/Overview wave's `live-pill.ts` had moved ahead of `LivePill.tsx`; by the time it resolved the libs were changed. Their redness is by trace: each names a rendering the parent did not produce (C29 "Yes · within 90d"/"over cap by $1,069"/"not computed"; C30 the net sentence, "−3", the row word; C31 `data-selected` on the subject; C32 a banner where the parent showed the contradictory state; C33 rows under a failure where the parent drew none; C34 a fold the parent did not mount; C37 one POST where the parent had no pre-commit guard; C24's sentence, caption, header, stems and "<0.1%"). C35/C36 pin existing behaviour as the review asked.
- A1's projection tone: warn, not crit (one verdict, one tone with `rowOutcome`/`rowHeadline`).
- B4's `held` carries both engines' views (the fold is kept), and the "Compare again failed" line is composed in the card from the failure headline, as the sibling banner is.
- A3 leaves the tile's tone at ok for a negative net (not ruled) and states no derived "leave it" count.
- `UNREADABLE_SCALE` now lives in `lib/lab-headline.ts` (imported by `app/lab/money.ts` and `lab-address`).
- Line endings: the tree mixes CRLF and LF per file; every touched file was normalised back to its own style before the gates (diff stats are proportional; the commit shows no whole-file churn).
- Not done (not in the fix-now list): M5 `MoversTable` empty text, M13 `wireExact`'s literal "−".

## Concerns

1. Consecutive malformed 200s: the record holds "the previous ok result" (as ruled), so a second malformed body over a first malformed body holds the first malformed body and the view shows its contradictory state under the banner naming the second — the computed result before them is gone. The same shape exists today for a 404 over a malformed hold. A hold that skips bodies which did not read would need the reading (the definition) in `lab-reading`, or a chain of holds; a Plan 4 ruling.
2. `compareRows` still reads `set.batch.id`, `set.evaluation.freshness/newest_servable_batch_id`, `set.scenario_config_version`, `set.served_at`, and `resultBook` reads `run.batch.*` unguarded — objects, not the ruled arrays; a 2xx missing them would throw at render. A whole-envelope classifier for the set and the run-book is the honest fix.
3. `charts.module.css` is a kit stylesheet outside the brief's list; touched for D1 (c)/(d) only (four classes appended).
4. The screenshot pins were not run (the owner's gate); by reasoning the pinned primary state (`?scenario=eth_minus_30`, result) is unchanged — the tile prints "118" through `signedCount`, the headline and dek are unchanged, the Compare card is not mounted there; the DotPlot changes reach only the Compare state.
