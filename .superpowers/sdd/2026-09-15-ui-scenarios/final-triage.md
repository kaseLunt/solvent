53:- Task 2 fix round 1/5: DONE, commit ef8477f (parent df3383b; 2 paths; 8/8; typecheck/lint clean) — all six items as ruled. Implementer's unasked note (after-histogram vs `to_rows`, the two tallies, label identity, the histogram's `wad_scale` unchecked) → handed to the re-reviewer to classify; default: deferred minors. Scoped re-review → a0f211cfa6217b184.
57:Task 3: minor (deferred): singular-verb slips on count-of-one paths (`1 improve` → `improves`, `1 account become` → `becomes`, `but 1 change band` → `changes`) — copy; `failureHeadline("failed", {})` prints "answered 0" (make `status` required on that arm); the `flipped` dek pinned with `toContain` not `toBe`; `description` through `sentence()` recapitalises a leading ticker; `signedUsd` negates redundantly; plan R11's examples (`$1.28M`, `$40.8K`) are stale beside `humanUsd`'s tiers (`$1.2M`, `$40K`) — plan text only.
60:Task 2: minor (deferred): the AFTER histogram vs `to_rows` (the mirror of the before-side check that landed — worth a line), the two tallies (`infinite_count`/`refused_count`) vs `from_rows[8]/[9]`, bucket label identity, the histogram's own `wad_scale` vs the matrix's — none changes a printed number today.
61:- Task 4 report: DONE_WITH_CONCERNS, commit fd1f27f (parent ef8477f; 5 paths; 20/20 incl. the moved deep-link block, move proven by empty diffs; typecheck/lint clean). Rulings: (1) the brief's `settled` helper type was non-distributive → `Extract<RunRecord, { phase: "settled" }>["outcome"]` — accepted, plan defect; (2) the readers are typed on the sealed `LabRunBook`/`LabRunBookEngine` (what `RunBookOutcome.ok.response` IS) — accepted; Task 9's `readEngine(run)` and `BookWorkspace.run` are `LabRunBook`, carried into Task 9's dispatch; (3) `laneReading` takes the WIRE engine type so `lab-library` bridges with `{ ...cash, projection: null }` — Ruling: narrow `laneReading`'s parameter (`lab-transitions.ts`) to `Pick<RunBookEngine, "usd_decimals" | "hf_transitions" | "before">` (type-only, no behavior) and Task 5's `moversTable` to the Pick of what it reads, so both accept the sea
64:- Task 5 report: DONE_WITH_CONCERNS, commit cf124d3 (parent fd1f27f; 4 paths; 21/21 across movers/transitions/library/headline; typecheck/lint clean). The narrowing ruling landed: `LaneEngine` (lab-transitions) and `MoverEngine` (lab-movers) Picks; the lab-library bridge removed. Rulings: `hfAfter` pin "0.75" → "0.756" (the brief flagged it; `truncateToDisplay` keeps three truncated digits — the Book's law) accepted; the unreadable-test scaffolding rebuilt so the spec's own helper does not throw on "1e9" — accepted (expects verbatim); `RunBookEngine` kept local — accepted; "five paths" was the controller's miscount, four is right. Concerns → deferred minors unless the reviewer rates them higher: a half-null ratio pair dashes quietly (a contradiction the table does not name); "unreadable" shares the display string type (the page must consult `unreadable[]` to tone a cell); `m
68:Task 4: minor (deferred): the `heatOf` comment overstates its law (Task 5 removed the bridge — check the comment went with it); `usd_decimals` guarded only transitively in `outcomeLine` (add `|| !isWireScale(cash.usd_decimals)` so a bad scale reads "Unreadable", not "Contradictory"); the band-change pin does not discriminate `merge` (a `{6:{5:k}}` table would); `outcomeLine` lacks a docblock stating its check order; the `DeepLinkDecision` type's one-line docblock (tornadoLines.ts:33) was outside the moved range — Task 12 carries it over; the helper's `COVERAGE` is a claim nothing reads yet.
73:- Task 5 review (task-5-review.md): spec ✅ (rooms, tiers, money, wads recomputed; narrowing type-only and exact), quality Approved with one Important (plan-mandated): `moversCaption` on a scale-refused table prints "showing 0 accounts moved · total not stated" — a zero-looking unknowable. Ruling: plan defect; `moversCaption` refuses by name when `unreadable` names `usd_decimals` ("not readable: unreadable scale"), one branch + one pin; the half-null ratio pair (exactly one side null) names the null side in `unreadable[]` in the same round (two lines). Deferred minors: "unreadable" shares the string type (a value-or-refusal union is the page task's call); `mover.engine` unchecked (outside the Pick); a negative side would print a room (app-wide sign class); `decimals: 0` stand-in on a refused table (the page must not print it); `no debt` arm unreachable from this server; the `total
77:Task 5: complete (commits fd1f27f..88f0518, review clean). Carried minors stay in the deferred list above.
78:- Task 6 review (task-6-review.md): spec ✅ (both frozen literals recomputed; the ladder, tones and R8/R15 traced), quality Approved with two plan-mandated Importants. Rulings: I1 — the projection row's headline ignores its horizons (a liquidatable horizon would sit under "stays inside its cap") → add the projection arm the reviewer describes (an `unknowable` horizon → refused; a `liquidatable` horizon → "becomes liquidatable within {horizonLabel} under {label}" warn; else "stays inside its cap through {longest horizon} under {label}" ok; dek from `extraInterest` via `humanUsdFull`), pin the demo's projection emphasis exactly; I2 — `money()` prints a negative wire figure and the before tiles ignore `view.refusedTiles` → refuse negatives, gate the before tiles on `refusedTiles`. Taken minors: 5 (split the "No scenario applies…" arm by cause — empty rows / unreadable sc
86:- Task 7 review (task-7-review.md): spec ✅ (moves reproduced; ranking recomputed; every concern verified), quality Approved with two plan-mandated Importants. Rulings: I1 — the result envelope is trusted; the old tornado's partition check (`engines` + `withheld_engines` + `unmeasurable_engines` = `covered_engines`, no overlaps) did not travel → add it in `rowOf`: a break is `kind: "contradictory"` with the reason naming the partition, before any engine is read; pin one break each way (a summary for an uncovered engine; a covered engine in no array). I2 — a compound fault under-names → always run `classifySetRunEngine`; `kind` is `unreadable` when every malformed field is one of the three read fields, else `contradictory`; `reason` names all of them. Taken minors: 1 (`shareTenths` delegates to `percentTenths`), 2 (a negative share over a tenth pinned, e.g. `−150%`), 3 (`?? 
90:Task 8: minor (deferred): the pre-commit double-dispatch window (unreachable from one action; harden with `controllers.current.has(id) ||` before `canDispatch`, and the set equivalent — fold into whichever task next opens the hook, e.g. Task 11/12 if the harness drives a deep link and a click on one id); the `"__set__"` sentinel shares the controller map with legal scenario ids (a separate `setController` ref, aborted on unmount); a redundant `SetRunOutcome` annotation; the unmount effect and the controllers ref lack law comments.
100:Task 7: minor (deferred): the four set-run arrays are assumed arrays (`runBookSet()` casts `res.json()`; four `Array.isArray` checks → `contradictory` naming the field would close it — the page task's call); the old tornado also flagged a numeric row with `accounts === 0` (parity note, not a refusal at zero).
102:- Task 6 re-review 2 (8441749, f338b53): RE-REVIEW: open — A, B, C, 2b ADDRESSED and pinned whole-headline; one residual of A: the projection arm sits above the unreadable-side gate, so a projection row over an unreadable/unknowable spot side still prints its horizon verdict beside refused tiles. Ruling: real (same class as A, the law `rowHeadline` itself states) → fix round 3/5 → implementer a80e588b9290eb46c: `computable(side)` helper carrying the condition once (after tiles, `sideRoomWords`, the gate), gate hoisted above the projection arm, stale duplicate JSDoc removed, one pin (projection over negative spot debt → refused cannot-say + NOT_COMPUTED). Still deferred: dek "Room today" (stress before) vs `roomBefore` tile (lookup's Cash) may disagree (minor 8's family); minors 3, 4, 6, 8, 9, 10; `roomAfter` regex.
104:- Task 9 review (task-9-review.md): spec ✅ (exports, ladder, banners, chips, R3 signatures, demo figures recomputed 941/49/167/118 and the $1.2M headline), quality Approved; two plan-mandated Importants. Rulings: I1 — `readEngine` re-checks `newly_eligible_accounts` with `isWirePopulation`, but the wire contract (wireGuard's assignment table, the classifier) makes it a SIGNED net count; a contract-legal negative would read "unreadable" → drop it from the `pops` list (the classifier already guards it; `resultHeadline` gates on `newly > 0`) — the contract is the law; cost if wrong: one lost duplicate check. I2 — the library row (`outcomeLine`, Task 4) and the workspace (`readEngine`) judge one response by two guard sets (e.g. `movers_total: -1` → workspace contradictory, row "+$1.2M liquidatable" crit) → ONE reader: move `EngineResult`, `EngineReading`, `readEngine` (and 
109:- Task 6 re-review 4 (6eee7fd): RE-REVIEW: closed — the gate covers missing, unreadable and unknowable ahead of every verdict word in every row kind; spot pins byte-for-byte unchanged; new pins fail on the parent by trace. Nits deferred to the final triage: the `flips === null` arm is now unreachable (remove when the file is next opened); `rowHeadline`'s JSDoc says "unreadable figures or an unknowable verdict" (add "missing"); minors 3, 4, 6, 8, 9, 10; the `roomAfter` regex; the "two todays" nit.
114:- Task 11 look (`screenshot-pages.mjs`, demo fixtures routed; scratch lab-look/): the primary state matches the mockup and R16 to the dollar — headline "$1.2M more Cash debt becomes liquidatable, across 118 accounts.", tiles 118 · +$1.2M · +$40K · 941, 7×7 heatmap (49 held over cap; 14/13/73/18 crossing; 135/43/129 worsening; 932 held ≥ 20 %; 6 not measured dashed), caption 425/118/none/6, 20 of 118 movers, legacy folded; both themes read. Bare `/lab`: "ETH -30 percent — 1 committed shock, not run yet.", tiles "— not run", the grid placeholder (R7 ✓). `?address=`: the frozen headline/dek and the eight tiles ✓, three rows incl. the projection's horizons ✓. Three defects found and fixed in the tree: (1) the kit `AddressField` overflowed the 300 px column (`.searchIn { min-width: 320px }`) → `.libAddress .searchIn { min-width: 0; flex: 1 1 100% }`, the hint hidden th
115:- Task 9 fix round 1/5: DONE, commit 6855c01 (parent 8eef607; 5 paths; four specs 30/30; whole unit project 1246; typecheck/eslint clean; `find_referencing_symbols(readEngine)` clean). I1, I2 and minors 1, 2, 4, 6, 7 as ruled; seam pin `movers_total: -1`. Concerns: `cashEngineOf`/`cashRefusalOf` are pin-only exports now (final triage: delete with Task 4's pin or keep); `readEngine`'s `definition` typed from the schema to keep the graph acyclic — accepted. Scoped re-review over 6855c01 (review-8eef607..6855c01.diff) → aae38a7240bf43d70.
117:- Task 10 review (task-10-review.md): spec ✅ (the generator re-run into the scratchpad reproduces all three JSON bodies byte-identically; every R16 figure pinned against the Book; all 15 deviations check out against compiler/schema/contract — three fix brief-level errors; deviation 9's arithmetic confirmed: shortfall 838426962, bad debt at liquidation 239603961, the brief's constants ×999.5 and larger than the eligible debt), quality Approved; two Importants, both implementer-level: I1 the generator's provenance sentence is false (the positions pages are the FULL 1,412-row Cash book bucketing to the Book's histogram exactly; a naive ×0.7 re-shock crosses 207 where the Book's table moves 18 from lane 5) → reword, regenerate, prove no figure moves; I2 the Cash `movers_note` states its order backwards (ascending ratio-after: row 1 is furthest past the cap) → reword, regenerate
132:- Task 12 report: DONE_WITH_CONCERNS, commits 1f05a9a (the contract: 21 pins; two page fixes; lib comment carry-overs) + 9279806 (retirements: 31 old app/lab files, FrontierLedger.tsx, 17 e2e specs, 16 unit specs, set-run-outcome trimmed to lib-only; the overall ledger's retirements entry). Production build at 9279806: typecheck/lint/lint:css clean; 926 total, 916 passed, 10 skipped (pre-existing), 0 failed (unit 750, e2e 176); the contract 21/21. Rulings on its concerns: (1) `router.replace` never wrote a search-param-only URL on the same static route in the production build → the page writes through `window.history.replaceState` (`useRouter` removed), pinned — accepted pending the reviewer's soundness check (Next ≥ 14.1 syncs the native history API into `useSearchParams`); carry to Task 13's Compare; (2) the stacked grids' implicit `auto` column overran 390 px by 5 px → `mi
139:- Task 11 re-review 3 (9b23b07): RE-REVIEW: open — the hold's law complete and honest (held only from a settled ok; through running and any failures; released by a new ok); the held book keeps its own identity/clocks/chips; unit pins and C13 bite on 18ff863. Two residuals: R3 — a held body whose definition changed rendered as `definition-changed` under a "the result below stands…" banner with nothing below while the row said "+$1.2M" (R17's exact shape); R4 — `rerun-failed` overwrote the held result's own stale-input/superseded disclosure. Rulings: R3 — the attempt's own failure is the state; the retained body is DISCLOSED (`banner: "retained-refused"`, `retained: { batchId, skew }`) and the row's word is the failure; the sibling gap (an ok result under a version skew printed the result word) closes with a "Definition changed" library word (`LibraryOutcomeKey` gains `defini
141:- Task 11 re-review 4 (745627e): RE-REVIEW: closed — R3: a retained body is never shown in any register, disclosed with its batch by the banner alone; row and workspace agree in both arms (the same `definitionSkew` predicate before `readEngine`); R4: the plain banners byte-identical; the hold carries `resultBook`'s own precedence (superseded first) and loses nothing relative to a plain result (whether R13 should say both is a plan question, not load-bearing). Pins bite on 9b23b07. Docs minor for the close: the plan's contract table vocabularies — `data-outcome` + `definition-changed`; `data-banner` + `rerun-failed · retained-refused`.
142:- Task 11: complete — ff78ac6 d3d56ab 853236d e695cc1 9b23b07 745627e (four fix rounds; the breaker did not trip). Design notes for the close: running over a hold shows the running headline without disclosing the hold; a superseded-and-stale hold says superseded only.
144:- Task 13 report: DONE_WITH_CONCERNS, commit 8249d6a (parent 745627e; 6 paths: CompareCard.tsx new, LabSurface, lab.module.css, lab-deep-link.ts + its spec (`dispatchNotice` retired, zero references), lab.spec.ts C24–C27; tsc/eslint/stylelint clean; unit 755; e2e lab 27/27 on a fresh build). Rulings on its concerns: (1) Compare does not write `?scenarios=` (writing it beside a `?scenario=` would render the conflict notice falsely) — accepted; a Plan 3b candidate, recorded for the close; (2) a MEASURED zero share toned warn per the brief's mapping — RULING: a measured zero is "nothing moved", the ok tone (the library's "No band change" is ok); one line, folded into the fix round after the review; (3) the card shows every result the set returned, not only the ticked ids (the demo mock answers four for two asked) — the wire's answer is the truth; the reviewer weighs whether the 
146:- Task 13 review (task-13-review.md): spec ✅ (ids with the kit's `data-kind`; four arms honest; `rowsOf` by arm with no path to 0n or a signed refusal; the legacy plot on its own words and scale; freshness words true to the contract; the button law; C25's brief fixture really contradictory under `censusBreak`; `dispatchNotice` zero references), quality Needs work. Rulings: I1 (plan-mandated) — the set MEMBERSHIP law (the response's `requested_scenario_ids` equals the asked ids as a multiset; results cover exactly them — the old R57 gate) was lost in the move and its retirement unledgered → restored in the lib (`setMembership(asked, response)` in lab-compare; `compareOf` passes `set.ids`; a fault → `failed` with a headline naming it, copy in lab-headline); `mockLab`'s set route ANSWERS THE REQUEST (the fixture shaped to the POST's ids) so the happy path is honest; C24 re-pin
149:- Task 13 re-review 1 (c92f4fd): RE-REVIEW: closed — `setMembership` matches the old R57/R58 gate clause for clause and the contract's multiset law; the mock's shaping honest (only the echo, the results list and `scenarios_evaluated` change); C28 and the C24 re-pin red on 8249d6a; `LegacyCompare` measures at its own mount (pin non-vacuous); the `DotPlot` change minimal. Residuals for the final triage: (1) `setMembershipHeadline` runs `sentence()` on the joined faults — an id-leading fault gets capitalised ("Eth_minus_30"); one line (`terminated()` or a fixed lead); (2) `CHAR_W = 7` estimate sizes the value column — against the kit's own LF-8 law (`useMonoCharWidth`), pre-existing from Task 1; kit item beside the `.chart max-width` clip; (3) "no denominator · +$5" unpinned; (4) result order vs the contract's request order unchecked (immaterial under the ranking).
154:- Task 15 Step 4: whole-branch package review-final-9aa5f17..c92f4fd.diff (36 commits; deletions by name; fixture JSON excluded; 939 KB — read in ranges); the deferred-minor list extracted to final-triage.md for the reviewer. Codex CLI is 0.154.0 now (the owner upgraded) — the three rounds are unblocked: slim diffs cut for Plan 3 (9aa5f17..c92f4fd), Plan 2 (d989286..9aa5f17), Plan 1 (its workspace's slim diff).
