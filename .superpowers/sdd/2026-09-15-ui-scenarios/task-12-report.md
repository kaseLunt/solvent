# Task 12 report — the page-test contract; the old Lab retired

Status: DONE_WITH_CONCERNS. Commits on main: `1f05a9a` (the contract, the two page fixes, the lib comment carry-overs) and `9279806` (the retirements and the ledger), both by pathspec through the scope gate; the tree afterwards holds only another agent's `.superpowers/sdd/.gitignore` change and this untracked task directory.

## What was done

- `web/tests/e2e/lab.spec.ts` rewritten as the Scenarios page-test contract: 21 pins (the brief's 16, with the corrected expectations below, plus five re-expressions the retirements needed a home for: the hole, a malformed field, a failed re-run, a failed listing, and `?scenarios=`). Every headline is `lib/lab-headline.ts`'s or `lib/lab-address.ts`'s; every figure is the demo Book's. The mock answers the two POST routes' CORS preflight and counts only the POST.
- Two page defects the contract found, fixed in Task 11 files:
  1. `app/lab/LabSurface.tsx`: `router.replace` onto the same static route with other search params never wrote the URL in the production build (it did on the dev server, verified by instrumenting `onMode`; no console error, no RSC fetch). The page now writes its URL through `window.history.replaceState`, which the app router folds into `useSearchParams`. `useRouter` is gone from the file. Pinned by C19: leaving one-address mode drops `?address=`, re-entering it restores it.
  2. `app/lab/lab.module.css`: at 390 wide the workspace's implicit `auto` grid column sized to the tiles' max-content (371px inside a 342px box: two 179.5px tiles plus the gap) and overran the viewport by 5px. `.workspace`, `.legacyBody`, `.tilesPair` and `.method` now declare `grid-template-columns: minmax(0, 1fr)`. Pinned by C21 (no horizontal overflow at 390).
- The mixed specs re-expressed: `shell.spec.ts` (the `/lab` H1), `state-matrix.spec.ts` (four `lab` cells + a `mockLab` helper shaped like the contract's; `API` removed as unused), `inspector.spec.ts` (the toolbar href), `r1-fixes.spec.ts` (the three "(5)" pins and the Lab arm of "(10)"), `p1b-fixes.spec.ts` (trimmed to its two non-Lab pins).
- Carry-overs from the moves: `lib/lab-deep-link.ts` got the `DeepLinkDecision` docblock back and its two "R58 item 6" comments now state the law; `lib/wireGuard.ts` no longer names the deleted `app/lab/matrixCells.ts`; `lib/lab-classify.ts` no longer names the deleted `tornadoCellState` (its consumer is `compareRows` in `lab-compare`). The `isNullableWirePopulation` docstring was already present in `lab-classify.ts` (nothing to restore); the decision's "two R58 comments in lab-classify.ts" were in `lab-deep-link.ts` — fixed there.
- Retired: 31 old files under `web/app/lab`, `web/components/charts/FrontierLedger.tsx` (its only importer was `LabFrontier.tsx`; `WaterfallSteps.tsx` stays for the styleguide), 17 e2e specs, 16 unit specs; `set-run-outcome.spec.ts` trimmed to its lib-only pins. The ledger entry is appended to `.superpowers/sdd/progress-ui-overhaul.md` under `## 2026-09-16 · Plan 3 (Scenarios) — retirements`.

## Test line

From `web/` on the production build at the closing commit: `npm run typecheck` clean; `npm run lint` clean (0 warnings); `npm run lint:css` clean; `npx playwright test` — 926 total, 916 passed, 10 skipped (the pre-existing skips), 0 failed: unit 750, e2e 176. The contract alone: 21 passed. Unit count at Plan 2 close was 1136; the 16 deleted specs held 471 pins and the trim removed 23, so 642 remained and Plan 3's Tasks 1–11 had added 108.

## The retirement table

Contract numbers (Cn) are `tests/e2e/lab.spec.ts` in file order: C1 cold load · C2 one click one POST · C3 heatmap · C4 movers · C5 in flight · C6 `?scenario=` / conflict · C7 `?scenarios=` · C8 selection per scenario · C9 withheld refusal · C10 the hole (neither array, both arrays, partial) · C11 malformed field · C12 404/503/429/500 · C13 a failed re-run replaces · C14 listing unavailable · C15 superseded / stale input / definition changed · C16 contradictory matrix · C17 legacy fold · C18 drawer · C19 one-address mode · C20 invalid and not-found · C21 first viewport · C22 not covered (fix round 1).

| Old file | Pins | Re-expressed → | Retired — why |
|---|---|---|---|
| e2e/lab.spec.ts | 30 | 15: cold arrival zero requests → C1; committed list from the listing → C1 (+ unit lab-library); `?scenario=` one / bare none → C6, C1; unpublished id runs nothing → C6; address mode secondary → C19/C20; honest 404 → C12; served run-book → C2; 503 no-batch → C12; found:null cannot be established → unit lab-address (withheld) + state-matrix `lab × refused`, C20 not-found as the negative; address 429 → state-matrix `lab × error:429`; invalid address never a request → C20; supersession named/kept/never mixed → C15; row declares its shock axes → C1 + C18; chips from the wire's set → C1; exact rationals / held_flat / snap → C18 | 15: the frontier ×5 (the Book's stress preview, Plan 1 unit pins); the matrix's five cell states + disclosure (library + workspace states in C1/C9/C12/C16; its "not-covered never looks like withheld" law → C22); depeg flagship + boundary/PROJECTION panel (Plan 2 address-stress unit pins, Inspector stress table); W-3L dek-at-head + CommittedDetail (the verdict header; C2's DOM order); r83 ×3, r84 (the page never re-reads the Book); W-3L (194) settlement line |
| e2e/runbook-bsplit.spec.ts | 26 | 8: distributions + movers → C2, C4; shift reading → C2; book reconciles → unit lab-transitions + demo-lab-weld; DM movers → C4 + unit lab-movers; Aave wads → unit lab-movers; moved nothing → unit lab-headline + lab-library; nothing without a served book → C1; mover opens the Inspector (its deep-link sub-assertion with it) → C4 | 18: unpriced holding / collateral breakdown / colliding rows (no breakdown); VIEW 4 ×2 + r88 ×2 (wad_scale guard in laneReading); VIEW 5 ×2, r89, r90 ×2, r92, r93 (flip strip gone; unreadable ranking is moversTable.unreadable); VIEW 7 ×2 + r95 ×2 (rate view gone). File deleted |
| e2e/runbook-transition.spec.ts | 17 | 6: one matrix per engine → C3 + C17; unmeasured cell → C3; note verbatim → C18; contradictory not drawn → C16; none without a served book → C1; withheld engine → C9 | 11: the DM tint pin (ruling: for Cash the over-cap band IS `debt > cap`, so the tint is the verdict); the method line (the drawer names the fields); the flow ribbons ×9 (chart gone; the one-ended law is laneReading's `unmeasured`). File deleted |
| e2e/tornado.spec.ts | 20 | deep links (`?scenarios=` filtered ids named, both params, `*`) → C6, C7 + unit lab-deep-link; refused contributes nothing / no denominator → unit lab-compare; refused-locally resolves → unit set-run-outcome | the set-run surface laws (exact ids, one POST, in-flight refusal, superseded arm, busy settlement) → Task 13's Compare pins (Plan 3b if Compare is cut); everything on the tornado's drawing. File deleted |
| e2e/chart-spec-v4.spec.ts | 13 | AC-49 → C2; AC-51 → C2/C4 + human-usd/human-price units | 11 frontier/distribution ACs (frontier is the Book's; distributions are the heatmap's margins). Every test was a Lab test: file deleted |
| e2e/w3l-slots.spec.ts | 9 | the order law → C2's boundingBox ordering | DOM-order laws for deleted components. No non-Lab test remained: file deleted |
| e2e/book-charts.spec.ts | 3 | wire notes as counted details → C18 | held-flat details, realization gloss (Plan 2 Inspector stress). Lab-only: file deleted |
| e2e/r1-fixes.spec.ts | 4 Lab | not-found complete answer → C20; mode toggle words → C1; no pre-lookup empty state → C1 | the Lab arm of "(10) intros" (the page answers first, spec §5.4; no intro paragraph) |
| e2e/r10-fixes.spec.ts | 4 | (1) ×2 → C5, C12 | (2) older row re-running (no cohort; C8); (3) frontier. File deleted |
| e2e/r11-fixes.spec.ts | 2 | (A) naming nobody → C10 | (B) the anchor (no cohort; C8). File deleted |
| e2e/r8, r16, r17-fixes | 2+1+1 | → C13 (a failed re-run replaces; nothing retained) | — . Files deleted |
| e2e/r9-fixes.spec.ts | 2 | — | watermark vs as-of claim (no cohort; the batch chip is the result's own, C2/C15). File deleted |
| e2e/r12-fixes.spec.ts | 2 | (A) both arrays → C10; (B) v2 under v1 → C15 | — . File deleted |
| e2e/r13-fixes.spec.ts | 4 | (C) ×2 → C13 | (A), (B) delisted rows / cohort (results per scenario, C8). File deleted |
| e2e/r14-fixes.spec.ts | 3 | (A) → C15; (B), (C) → C10 | — . File deleted |
| e2e/r15-fixes.spec.ts | 1 | — | running skewed attempt (C5's running state; no remedy text). File deleted |
| e2e/p0-fixes.spec.ts | 8 | p0-2 ×2 → C2 + unit lab-library/lab-headline; p0-8 → C11; p0-9 ×3 → C11, C15 | p0-1 ×2 stale barrier (workspace bound to the inspected address; C19 pins identity). File deleted |
| e2e/p1b-fixes.spec.ts | 12 Lab of 14 | p1b-2 ×2, p1b-9 f2, p1b-12 → C11 + unit lab-classify-run-book / wire-guard; p1b-5 ×2 → C19; item 7 → C20; p1b-9 f1, p1b-10 → unit result-identity; p1b-11 → unit lab-classify-run-book / lab-transitions; p1b-3 → unit lab-classify-set-run (surface: Task 13) | item 8 blind resume (unit freshness-blind-resume / freshness-resume-evidence, where `useAnchoredAgeSeconds` is pinned); the stale barrier. The two non-Lab pins stay |
| e2e/shell.spec.ts | 1 | `/lab` H1 → "The committed scenarios could not be listed." (the smoke serves no API) | — |
| e2e/state-matrix.spec.ts | 4 | ok → rows; refused → withheld, never "no position"; 429 → unavailable; 404 → not-served | — |
| e2e/inspector.spec.ts | 1 | href `#stress` → `/lab?address=<DEMO_NEAR_ADDR>` | no "Open Scenarios →" href pin existed in tests/ (the brief's "likewise" had nothing to re-pin) |
| unit ×16 | 471 | engine-classification (28) → lab-classify-run-book (28); set-run-classification (11) → lab-classify-set-run (11); tornado-lines' 8 deep-link pins → lab-deep-link (8) | the rest bound to deleted modules (55 of tornado-lines pinned its lines and cells) |
| unit/set-run-outcome.spec.ts | 37 | 14 kept (lib/runbookSet client); the busy and refused-locally sentences → `failureHeadline` | 23 tornado cell decisions |

## Files deleted

`web/app/lab/`: LabBatchStamp, LabBookPanel, LabBoundaryGroup, LabClient, LabFrontier, LabMatrix, LabProjectionView, LabRealization, LabRunBookDetail, LabRunBookTransition, LabScenarioChips, LabScenarioDetail, LabTornado (.tsx); addressBinding, badDebtRate, engineClassification, flipRanking, frontierScale, frontierView, labDek, labPanelLines, labReadingLines, labRunBookLines, labTransition, labTransitionFlow, matrixCells, moverDumbbells, scenarioLines, setRunClassification, tornadoCells, tornadoLines (.ts). `web/components/charts/FrontierLedger.tsx`. e2e: tornado, runbook-transition, runbook-bsplit, chart-spec-v4, w3l-slots, book-charts, r10-fixes, p0-fixes, r11-fixes, r8-fixes, r9-fixes, r12-fixes, r13-fixes, r14-fixes, r15-fixes, r16-fixes, r17-fixes. unit: address-binding, bad-debt-rate, engine-classification, flip-ranking, frontier-scale, lab-dek, lab-frontier, lab-matrix, lab-panel-lines, lab-runbook-lines, lab-transition, matrix-outcome, mover-dumbbells, scenario-lines, set-run-classification, tornado-lines. Verified before deletion: no file outside `app/lab` imported a deleted module except `FrontierLedger.tsx` (deleted with them) and comments in `lib/wireGuard.ts` (reworded); typecheck clean after.

## Deviations from the brief, with reasons

1. The brief's spec pinned things the page does not do; each pin follows the page's law: in one-address mode there is no `lab-run` (pinned absent; Inspect is the action); the library rows read their own verdicts ("Becomes liquidatable", "Stays inside its cap through 90d", "Not on this address"), not "Applies to this address"; the 503 dek reads "Retry after 5s." — the envelope's `retry_after_seconds` — because the API sends no `Access-Control-Expose-Headers`, so a cross-origin browser cannot read `Retry-After` and the client's header-first preference never sees it (the mock still sends the 30s header nobody can read); the first-viewport pin holds the library's head and first row within 900px, not the whole four-row list (984px — the brief said "the library head"); the "never $0" pins are scoped to the Cash tiles and the verdict, since the legacy fold prints its own honest zero ("$0 → $666.66").
2. `shell.spec.ts`: the smoke aborts every `/v1/**` request, so the `/lab` H1 is the listing-unavailable headline, not the demo listing's not-run headline; routing `DEMO_SCENARIOS` for one row would break the smoke's premise.
3. More retirements than the brief listed: r8, r9, r12–r17 (all-Lab files, the cohort/watermark/re-run-banner machinery), p0 (all-Lab), r11 (all-Lab) — each pin mapped above; `w3l-slots`, `book-charts`, `chart-spec-v4`, `r10-fixes` are deleted whole because no non-Lab test remained in them.
4. `set-run-outcome.spec.ts` was neither kept as-is (it imported the tornado) nor deleted: trimmed to the lib-only client register, which had no other home (`classifySetRunRefusal` / `runBookSet` are pinned nowhere else).
5. Two commits instead of the brief's one: the contract (with the two page fixes it demanded and the lib comment carry-overs), then the retirements with the ledger. Each tree typechecks on its own.
6. Five pins the brief did not have, added so retired laws keep a page-level home: C7 (`?scenarios=` posts exactly the listed ids, pre-ticks them, names a filtered id), C10 (the hole in three shapes), C11 (a malformed field is the contradictory state, named, the route standing), C13 (a failed re-run replaces the result), C14 (a failed listing says so in the library and the workspace and nothing runs).

## Concerns

- API: `cmd/api/middleware.go` sets no `Access-Control-Expose-Headers`, so `Retry-After` is invisible to any cross-origin browser client; the page falls back to the envelope's `retry_after_seconds`. If the header is meant to govern, the API should expose it (`Access-Control-Expose-Headers: Retry-After`).
- Next 16.2.12 production: `router.replace` to the same statically prerendered route with only search params changed did not write the URL (the dev server did). Fixed on the page with the documented native `history.replaceState`; other pages only `router.push` to different routes, so nothing else is affected, but the pattern is worth knowing for Task 13's Compare.
- `lib/lab-classify.ts` still carries pre-existing round references in its comments (p1b-9, p1b-10, p1b-11, p0-8, "Task 1's review"); outside this task's brief, left as is.
- The shell's `main` gutter is 24px at 390 wide (the spec says 16px); Plan 1's shell, not touched.
- The library's four rows with full descriptions run to 984px at 1440×900; the head and the first row fit. Task 14's visual pins may want the description line clamped; not changed here.
- `.superpowers/sdd/.gitignore` shows modified in the working tree; not mine, not staged.

## Fix round 1

Review: task-12-review.md (spec ✅, quality needs work — I1, I3, M1; I2 is the controller's ruling and C13 is left exactly as it was).

- I1 — C22 `not covered` appended to `web/tests/e2e/lab.spec.ts` (at the end, so C1–C21's numbers in the ledger stay true): the demo listing with `eth_minus_30`'s engines shaped to the legacy engine only, run by deep link against the demo run-book (eth_minus_30's own body, so no definition skew and no banner — pinned). Pins: `data-state="not-covered"`; the headline is exactly "ETH -30 percent does not model the Cash book." and the dek "It models the Aave v3 market (legacy). The legacy result is below." (`notCoveredHeadline` with `legacyBelow`); the library row is `data-outcome="not-covered"` reading "Not modelled for Cash" with its engines line "Aave v3 market (legacy)"; the four Cash tiles read "—" / "not modelled", tone refused, and contain no "0"; the transitions card's words are "This scenario does not model Cash." (`engineName(CASH)`), no grid, no movers; the legacy fold stands with its own 14; and the retired law — nothing on the page says "withheld" (`main` not matching /withheld/i). The retirement table now routes the old matrix pin's not-covered law to C22.
- I3 — C18 extended: the applied-shocks list is asserted item by item, in order, as the drawer prints it — `<asset> (chain <id>, <source>): <before> → <after> × <num>/<den>` plus the " · snapped / base snapped / cap bound" suffix when the wire set a flag — with every value from `DEMO_RUN_BOOK_ETH.applied_shocks` (three shocks; no demo shock carries a flag, and the exact-text equality proves the suffix absent); the held-flat list likewise — `<asset> (chain <id>, <source>) at <value>` from `DEMO_RUN_BOOK_ETH.held_flat` (three inputs); both lists' counts pinned. The table's "exact rationals / held_flat / snap → C18" and "r85 → C18" claims are now true.
- M1 — the table: runbook-bsplit is 8 re-expressed + 18 retired (the "mover deep link" entry was a sub-assertion of the mover-click pin, folded into it); the blind-resume home is `freshness-blind-resume` / `freshness-resume-evidence` (no `live-age` spec exists).

Test line: contract 22/22 on the production build at 9279806 (HEAD unchanged; the :3111 server is that build); `npx tsc --noEmit` clean; `npx eslint tests/e2e/lab.spec.ts` clean. Commit: `18ff863` (web/tests/e2e/lab.spec.ts only, by pathspec, scope gate OK). At commit time the working tree also carried the integrator's in-progress I2 edits (LabSurface.tsx, StaleBanner.tsx, lab-library.ts, lab-reading.ts, lab-view.ts and their unit specs) — not staged, not touched.

Concerns: the committed ledger (`.superpowers/sdd/progress-ui-overhaul.md`, the Plan 3 entry) carries the same two M1 slips ("9 re-expressed … 17 retired" for runbook-bsplit; "unit freshness-blind-resume / live-age") and does not name C22; the round's commit is scoped to the spec, so those three ledger lines are left for the integrator to mirror (the report's wording above is the replacement).
