# Plan 3 close-out wave — the brief

One round, one commit. Every item below is a controller ruling; each fix carries a pin that fails on the current code (unit where the law is pure, e2e in `web/tests/e2e/lab.spec.ts` appended as C29+ where it is a page state). Files you may touch: `web/lib/lab-*.ts`, `web/lib/lab-engine.ts`, `web/app/lab/**`, `web/components/charts/DotPlot.tsx`, `web/components/kit/{ScenarioLibrary,Heatmap}.tsx` and `kit.module.css` (only if an item needs it), `web/tests/unit/lab-*.spec.ts`, `web/tests/unit/demo-lab-weld.spec.ts`, `web/tests/e2e/lab.spec.ts`. Do NOT touch `web/lib/inspector-*.ts`, `web/lib/address-*.ts`, `web/app/inspector/**`, `web/app/book/**`, `web/app/overview/**`, `web/lib/cash-*`, `docs/**` — other waves own them. The laws: a refusal never renders as zero, "No" or "nothing"; wire values pass their guards before arithmetic; engines never share an axis; copy lives in the libs, never in components; a computed result is never silently replaced; comments state the law, never the round or the review.

## A · From the whole-branch review (`final-review.md`, Findings)

A1 (= Codex 2). `web/app/lab/AddressWorkspace.tsx` ~:43 — the "Becomes liquidatable?" column prints `r.flips` ("Yes"/"No") instead of speaking from `rowVerdict(r)` (lib/lab-address.ts): a projection row with a liquidatable 90d horizon prints "No" under a headline saying "becomes liquidatable within 90d"; an unreadable side with a true wire flip prints "Yes" beside "Cannot say" tiles. Fix: the column speaks from `rowVerdict` — liquidatable → "Yes" (crit pill; a projection: "Yes · within 30d"), inside → "No", cannot-say → "Cannot say" (refused pill), not-applicable → the reason. Export whatever small word function the lib needs so the component composes nothing. Pin (unit on the word function; e2e on the demo address's projection row).

A2. `AddressWorkspace.tsx` ~:38,41 — the table prints a room beside an unknowable verdict, and words a negative room "−$1,069" while the tile says "over cap by $1,069". Fix: the room cells speak from the same side words the tiles use (`sideRoomWords`/`roomTile` in lib/lab-address.ts — export what is needed): a computable side prints "over cap by $1,069" / "$190.50"; an uncomputable side prints the refused word. Pin.

A3 (overlaps Codex 7). `web/lib/lab-headline.ts` ~:83–89, `lib/lab-library.ts` ~:101, `app/lab/LabTiles.tsx` ~:44 — `newly_eligible_accounts` is a signed NET (admitted since 6855c01) but has no sentence: net ≤ 0 with crossings > 0 says "No Cash account becomes liquidatable"; a negative net prints "-3" with an ASCII hyphen. Fix: when the net is ≤ 0 and the merged lanes' `crossedCap` > 0, the headline states the net and the gross ("Net, no more Cash debt is liquidatable under {label}: {crossed} accounts cross the cap and {improved} leave it." — pick the exact words, pin them); a negative net prints with the typographic minus (the money formatters already do — reuse `signedUsd`'s convention for counts); the library row's word follows the same law ("Net −3 accounts" in the refused/ok tone as ruled by the headline). Pin unit (headline, library) and e2e if a fixture can be shaped without touching the demo JSON (shape via `withCash` in the e2e helper).

A4 (overlaps Codex 6). `lib/lab-compare.ts` ~:71,101,133,163 and `lib/lab-engine.ts` ~:55 — envelope arrays are read unguarded at render: a 2xx set body missing `results` / `requested_scenario_ids` / `engines` / `withheld_engines` / `unmeasurable_engines`, or a run-book missing `excluded_engines`, throws inside `deriveLabView`; `readEngine` dereferences `before` / `after` / `hf_transitions` before checking the classifier's structural faults (`before: null` → TypeError). Fix: `Array.isArray` guards on every envelope array (a missing array is a named unreadable field, never a throw); in `readEngine`, return the unreadable state as soon as the classifier reports structural faults, before any nested read. Pin each input (unit).

A5. `app/lab/LabSurface.tsx` ~:183–189 — in address mode the library highlight follows the listing's selection while the workspace's subject falls back to `rows[0]` when the selected scenario is not on the address: two scenarios named on one screen. Fix: in address mode the library's `selected` flag follows `space.selected?.id` (the subject the workspace actually shows). Pin (e2e: `?address=…&scenario=<an id not on the address>` → the highlighted row is the subject row).

## B · From Codex's Plan 3 round (ledger adjudications)

B3. `lib/lab-reading.ts` `withSettled` — the hold is released on a transport-ok before the body is read: a 200 whose Cash reading is unreadable/contradictory loses the held result and shows the contradictory state with no banner. Fix: the hold survives an ok settle (`held` = the previous ok result, always); `bookOf` (lib/lab-view.ts) releases it only when the new body READS as an answer (`readEngine` result / withheld / not-covered — honest wire answers); when the new body reads `unreadable` or `contradictory` (malformed classes) and a hold exists, the held result stands under `rerun-failed` with the contradictory/unreadable headline as the named failure; `outcomeLine` follows the same law. Pins: lab-reading (the hold survives an ok settle), lab-view (a malformed 200 over a hold → result state + rerun-failed + the failure headline; a malformed 200 with no hold → contradictory as today), e2e (run, then a 200 with `eligible_debt_delta_usd: "1e6"` → the held figures under the banner).

B4. `lib/lab-reading.ts` ~:111, `lib/lab-view.ts` `compareOf`, `app/lab/CompareCard.tsx` — `SetRecord` has no hold: a failed Compare re-run (429/503/busy) discards the computed comparison. Fix: `SetRecord.held` (the last ok set response with its settle clocks, kept through failures, released by a new ok that passes `setMembership`); `CompareState.failed` carries `held: CompareView | null` (the held rows and their batch); the card keeps the dots and the legacy block under the failure sentence with a banner-like line ("Compare again failed — {failure}. The comparison below stands for batch N."). Pins: lab-reading (set hold), lab-view (failed with held), e2e (compare, then 429 → dots kept, sentence shown).

B5. `app/lab/CompareCard.tsx` ~:172 (`legacyPoints`) / `LegacyCompare.tsx` — a legacy block whose rows are all refused vanishes (no numeric share → nothing rendered). Fix: render the legacy block whenever any legacy row exists; refused rows are dashed rows with their named refusal. Pin (e2e with the withheld-legacy set fixture the C25 helper shapes).

## C · The deferred lines the review marked "fix now" (`final-review.md`, Triage)

C1. Comments in `lib/lab-classify.ts` still narrate rounds and retired functions ("p1b-9 / p1b-10 / p0-8 / Task 1's review"); `lib/lab-compare.ts` says "R57" — reword each to the law it states.
C2. The singular slips the review names (read them there); `sentence` / `refused` duplicated between lab-address and lab-headline — export from lab-headline, import in lab-address.
C3. `lib/lab-reading.ts` — the dispatch-window and `"__set__"` hardening the Task 8 review deferred (read the review's exact wording in final-review.md; implement as written).
C4. `lib/lab-library.ts` — `cashEngineOf` / `cashRefusalOf` are dead exports (only a pin reads them): delete both with their pin.
C5. `lib/lab-headline.ts` `setMembershipHeadline` runs `sentence()` on the joined faults, capitalising an id-leading fault ("Eth_minus_30"): use a fixed lead clause ("Asked N ids, the response names M." always first) or `terminated()`; pin the id-leading case.
C6. The three missing pins the review names (read them there: incl. "no denominator · +$5" on a refused compare row) — add them.
C7. `lib/lab-address.ts` — the two one-liners the review names (read them there: the unreachable `flips === null` arm; the JSDoc's missing "missing").

Skip (owned elsewhere): the two plan-table lines (`data-kind` vocabulary; `busy` unreachable) — the controller edits docs; the stress-batch identity item (Codex 1) — the controller lands it after the Inspector wave exposes `stressBatchId`; the kit's `CHAR_W` estimate and the `.chart` clip — Plan 4.

## Gates and commit

From `web/`: `npx tsc --noEmit`; `npx eslint app/lab lib components tests`; `npm run lint:css` if CSS moved; `npx playwright test --project=unit`; then a production build (`kill` any :3111 listener by netstat PID + `taskkill //PID <pid> //F //T`; `npm run build`; `npm run start` in the background) and `npx playwright test tests/e2e/lab.spec.ts` (C1–C28 green plus yours). NEVER `--update-snapshots`; if `tests/e2e/screenshots.spec.ts` would change because a rendering moved in the primary state, STOP that item and report — the pixel pins are the owner's gate. Commit by pathspec, ONE commit: `git commit -m "fix(web): scenarios close-out - the address table speaks the row verdict and the tiles' room words, a net count has its sentence, envelope arrays and null subtrees never throw, the address subject is the highlighted row, a hold survives a malformed answer and a failed compare, all-refused legacy shares render, the deferred nits" -- <paths>`; stage by name; `python roadmap/tools/scope_gate.py` first; no Co-Authored-By; BLOCKED if the hook refuses. No prettier on lib/ or tests/.

Report to `plan3-wave-report.md` in this directory: per item what changed and its pin, deviations with reasons, anything STOPPED and why. Return only: status, commit, one-line test summary, concerns.
