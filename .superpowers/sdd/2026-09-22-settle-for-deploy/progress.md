# Plan 5 — Settle for deploy · ledger

Plan: `docs/plans/2026-09-22-settle-for-deploy.md` · Spec: `docs/specs/2026-09-22-settle-for-deploy-design.md`.

## Direction (the owner's words)
- 2026-09-21 13:36 "what is left to do in the next session" → 15:01 "the goal is the resume piece. keep moving toward that. use your judgement".
- 2026-09-22: on the deploy question, "just do a local build for now, i will deploy when everything looks settled"; on the approaches, "let's take care of A i guess if it is a quick win, then we can start on B". Integrator's read: A is low-risk but ~8 small tasks; trimmed to what changes the first three minutes; B (every page's reader classifies its envelope, + the kit's pending headline) is the next plan.

## Log
- 2026-09-22 ~12:20 lease renewed through 2026-09-23T19:18Z (f28d89d).
- Understand workflow wf_fbdabd4f-2b1 (seven read-only readers, each followed by an adversarial skeptic; `model: fable` with fallback): interrupted by a usage limit ~12:49; owner switched accounts 13:20; resumed from the journal (13 of 14 agents cached; the vocabulary skeptic re-ran). Results: `understand.json` (reads + verdicts), `understand-verdicts.md` (every refuted / unverifiable claim, proposal problem and miss), `understand-proposals.md`.
- KEY CORRECTIONS the skeptics made (binding on the plan): the Book's 2 + 47 below-line = History's 49 (the same partition; the Book never states it); Verification's two welds (29 + 14) are NOT a subset of the 87 checked rows — 10 of the 14 legacy rows are advisory, so "the other 44 gated checks" would have been FALSE; the Cash movers are accounts whose eligibility flips false→true ranked by debt (= the "Newly liquidatable 118"), the legacy movers are health-factor drops; the demo's Cash liquidations are SUB-DOLLAR (0.35812 USD repaid) — the reader's "358.12 USD" was 1000× too large; the Overview hero's 70,000 is an external active-cards figure (Crypto Briefing 2026-09-04: ~70,000 active cards, ~300,000 accounts; recon/v4-proposal-arfc-post1.html:7 "tens of thousands of cardholders"), not borrowers, and no source is in the system; the Go engine never persists debt on a refused Debt Manager row (the demo's ~$178K "not computed debt" exists only in the fixture); a README may cite a component as proven only with a recorded Codex approval (D-006 clause 5); the committed reconcile artifact reports 30,838 gated rows (the page's 87 is the contract example).
- Spec and plan written under the owner's delegation (the brainstorming gate taken by "use your judgement" + "let's take care of A"); rulings of record in spec §6 for the owner's review.
- Spec + plan committed **3f9836d** (the findings as `understand-verdicts.md` / `understand-proposals.md`; the raw `understand.json` stays local — only markdown is admitted in the ledger directories).
- **Implementation workflow wf_3dfa8178-397 launched** (2026-09-22 ~13:45): five areas in parallel on disjoint files — **B** Tasks 1→2 (hero, the Book's figures, near-cap fold, legacy finding, demo refused rows), **C** Tasks 3→4 (Verification's one vocabulary, the API's inline markers), **D** Task 5 (Scenarios' three populations, the kit's disabled register), **E** Tasks 6→7 (Activity's scale / units / types / filter chip, the Inspector's activity card, History's record, the sweep item), **A** Task 8 (README). Per area: implement (unit gates only; one commit per task, message verbatim) → an adversarial read-only review against the spec, plan and laws (it re-verifies every new public string against code / contract / Go source) → up to two fix rounds with re-review. `model: fable` with a logged fallback. The phrasebook sweep (Task 9) and the build / pins / screenshots (Task 10) are the integrator's after the areas land. The vocabulary skeptic of the understand workflow (resumed as wed9yzs1t) had not returned at launch: its corrections, if any, go to areas C / E's fix rounds.
- **MODEL RULE CHANGED BY THE OWNER (2026-09-22 13:34):** "lots of fable agents failed, go ahead and use opus 5.5". In wf_3dfa8178-397 all five `fable` implementers failed on their FIRST request (zero tool uses each — verified from their transcripts); the script's fallback had already restarted all five on the session model (Opus 5.5), which are the ones editing now. The workflow was NOT stopped (stopping five implementers mid-edit would strand partial work and a resume would restart them against a dirty tree); its later review / fix stages still try `fable` first and fall back at once. **From now on every agent and workflow runs on Opus 5.5 (no `model: 'fable'`); this supersedes the earlier "all subagents on fable" rule.**
- Understand workflow wf_fbdabd4f-2b1 COMPLETED (13:40): its seven `fable` skeptics failed on the limit and the fallback re-ran them on Opus; the vocabulary verdict landed AFTER the implementation launch → `understand-vocab-verdict.md` (49 checks, 6 not confirmed). Corrections to carry into the areas' follow-up round, checked against their commits when the workflow returns: **C** — "gated" survives in ~7 more public strings (VERIFY_EMPTY, RECEIPT_EMPTY_TITLE, verificationDek, the receiptLine empty/drift/failed arms, RECEIPT_EMPTY_STATUS evidence.ts:888, the rejected detail) and in trust.ts:225 on the Inspector (area E's file); the registry row says "matches the service's, by construction" (the registry is loaded at runtime from --feeds — never "the build's"); the Verify tile sub must fit 265px ("checked rows exact · 0 drifted"); pins verification.spec :590/:593/:596; the API quickstart sample's comment "(no client method yet — plain fetch)" is false — `@solvent/client` has `evidence()`. **E** — the filter chip says "all engines" (not "any engine" — the Scope chip already says "all engines"); the engine-scoped example pairs the legacy engine with an Ethereum-range block; the Activity dek's "(deficit_created)" is OUR composition and must follow `typeLabel`; more "(s)" / "custodied" in the Inspector activity takeaway (activity-rows.ts:115-134). **D** — AssumptionsDrawer composes more headings/sentences in the component; MoversTable composes its column headers; lab-view pins :90/:111/:112/:139/:361/:374/:720.
- **Implementation workflow wf_3dfa8178-397 COMPLETED (~14:27):** 44 agents — 22 `fable` attempts failed on the limit at their first request, 22 Opus agents did the work. Commits (15, on 3f9836d): **B** 57fc04f 95b7238 + fixes 37b175c aa0a477 · **C** 4365829 97407c9 + fix 55b3fff · **D** 71b9ce0 · **E** ad05e1b 780f314 + fixes 735b478 ed86977 · **A** c4a2c06 + fix 4464487. Landed tree: tsc clean, **unit 1,111 / 0** (from 1,081). Verdicts: **A CLOSES** (1 round; its review caught a CRITICAL — the README's run block said the API needs only a database URL; it also refuses without SOLVENT_DATABASE_URL and both SOLVENT_RPC_*), **C CLOSES** (1 round; "gated" and "<wire id> weld" survived in the REJECTED states; the welds disclosure was false in the 0/0 weld state), **D CLOSES** (0 rounds), **E CLOSES** (2 rounds; the dek's "(deficit_created)" and the headline's raw type words after a filter; "any engine" beside "Scope all engines"; the Inspector trust card's empty-receipt words disagreed with Verification's), **B NOT CLOSED after 2 rounds** — the "nothing computed" law (a served Cash engine with computed 0 and refused > 0): this round made the tiles refuse there, but the Distance chart still says "$0 sits within 10% of the cap" and bars print "$0 · 0", and the legacy fold draws zeros in an all-refused histogram (server: handlers.go:464, :502-505) — the page contradicts itself. Pre-existing zeros, but a contradiction this round created and a law the round wrote (cash-summary.ts:163-167).
- The vocabulary skeptic's corrections, checked against the landed code: resolved — no public "gated" outside the glossed drawer row, no "the build's", no "any engine", the false quickstart comment gone; OPEN — "(s)" on the lab's deep-link notices (lab-deep-link.ts:155,161).
- **Closing round wf_fc9cff27-7c4 launched (Opus; no fable):** job **book** — the nothing-computed law on the bands finding, bandsSoFar, the legacy histogram, the median room tile and (if the server can serve "0") the standing bad debt; the walkTile comment; the styleguide specimen's own dust-toggle label → `belowLineToggleLabel`. Job **minors** — the lab's "(s)" plurals; the legacy zero movers claim only what the contract's note licenses; Verification's surviving "(s)"; the Inspector's empty-receipt words agree with Verification's; ruling §6.4 applied to the demo Inspector's REFUSED specimen (it still served a debt). Each: implement → adversarial review → one fix round if needed.
- **Closing round wf_fc9cff27-7c4 COMPLETED (Opus, 6 agents, 0 errors):** job **book** CLOSES at **f87e6ba** (0 fix rounds; every unit pin seen failing on the parent via namespace-import copies) — the bands finding / bars / legacy histogram / median room say no account was computed; the reviewer CONFIRMED in Go that an all-refused Cash engine serves NO bad_debt[debt_manager] row (read.go:692-719, waterfall.go:220-224, handlers.go:663-695), so the tile prints "—" / "not reported" (no zero; wording follow-up only); the nothing-computed e2e fixture now serves bad_debt [] as the server does. Job **minors** CLOSES at **56d59d9** + **4707b57** (1 fix round: Verification's publishability refusal counts fragments in a real plural; a refused or verdictless Cash row that still carries a debt prints no "before" figure on Scenarios). Carried Minors → the whole-branch review: the Cash zero movers caption is still an unscoped negative; the legacy caption arm is never mounted (the legacy fold's movers link opens the Cash table); the demo's refused specimen still serves collateral legs and price inputs; the median room in the "unreadable" register prints unqualified; the nothing-computed fixture still spreads BOOK's waterfall and coverage.
- **Task 9 (integrator) — 709c1cf:** SWEEP_NEVER → **"collateral never read"** — DEVIATION from the plan's "never successfully swept", on the ENGINE's own words: internal/riskfeed/assemble.go:1002-1006 details "its collateral has NEVER been read" (no sweep row) and "no successful sweep ever … collateral size is unknown" (attempted, failed) — the phrase is true of both, keeps a subject when composed ("Stress withheld: Cash — collateral never read."; "Collateral never read. No verdict is served for it."), and is shorter than the tile's old 26 characters. Nine specs re-pointed (incl. the capitalised sentence forms in inspector-view.spec and inspector.spec); the demo generator's invented refusal detail "collateral sweep never ran for this account" replaced by the engine's real text (assemble.go:1002-1003) and regenerated (address-demo-refused.json, history-demo-near.json: one detail line each; the detail never renders — the phrasebook wins for known codes). Unit **1,119 / 0**; tsc / eslint clean.
- Task 10: the production build + whole e2e RUNNING (background b7swgs48j; flag set; log scratchpad `e2e-p5.log`).
- Task 10: shared build at 709c1cf (flag set): lint / lint:css / build clean; **e2e 303 passed / 4 failed — all four pixel pins moved BY RULING** (overview ×2: the hero and the live card's dek; activity ×2: scaled Cash amounts, record-only dash, plain type words, the applied-filter chip); the book, verification, lab, history and inspector changes sat inside the 1% tolerance. Re-baselined ALL SEVEN moved-by-ruling pages (14 files) after reading the captures; two replays 16/16. **4a54141** the pins. README screenshots produced (seven dark first-viewport captures, 844 KB) into `docs/readme/`.
- **BLOCKED on the control plane:** `docs/readme/**` is outside W3's allowed_paths though W3's own Acceptance item 5 requires README screenshots. The integrator amended W3 (AMENDMENT 2) and rescoped the claim (generation 16; the lease reset to 2026-09-23T06:12Z), but the scope gate refuses: "owner acknowledgement required for durable transition: work scope expanded: W3: docs/readme/**" — rerun with CONTROL_PLANE_OWNER_REVIEWED=1 after review. That flag is the OWNER's acknowledgement; not set by the integrator. Everything unstaged, the working-tree changes kept (W3 amendment, rescoped claim, README sentence tweak, seven PNGs); asked the owner.
- **Owner acknowledged the W3 scope expansion** (AskUserQuestion, 2026-09-23 evening USMST: "Approve docs/readme"). The lease had expired overnight (2026-09-23T06:12Z; now 2026-09-24T03:33Z): claim restored, renewed and committed **77dd5ae**; rescope re-applied (generation 16, lease to 2026-09-24T11:34Z); **8c1e794** W3 AMENDMENT 2 + the claim, committed with CONTROL_PLANE_OWNER_REVIEWED=1 on the owner's answer (the gate logged it as a local owner acknowledgement); **80ffb17** the README's regeneration sentence + the seven screenshots.
- Task 11 QA workflow **wf_728870e1-c83** launched (all Opus; read-only; parallel): a `solvent-user` **persona re-walk** of every targeted finding + the README as a hiring evaluator lands on it (52 fresh captures in scratchpad `gate-p5/`) → `qa-persona-rewalk.md`; a **whole-branch review** of 3f9836d..80ffb17 (seams between the five areas and the sweep, the laws at the final state, the README's claims against D-006 clause 5, tests as a whole) → `final-review.md`; the **Codex round** (D-006; `codex-reviewer`; brief `codex-brief.md`; no config touched) → `codex-findings.md`.
- **Owner direction on the Codex round (2026-09-23 20:39 USMST):** "if you find that codex's findings are not important you can overrule". Applied as D-006 clause 6 (refuse-with-traced-reasoning): a Codex finding is FIXED when it breaks a law on a reachable state or makes a public statement false; otherwise it may be OVERRULED with the reasoning recorded in the adjudication and resurfaced in the close entry for the owner.
- **QA workflow wf_728870e1-c83 COMPLETED.** Persona re-walk (`qa-persona-rewalk.md`): 4 CLOSED (hero, "$109.45", near-cap fold, API markers), 8 PARTLY, 0 NOT CLOSED; 11 NEW; README read as a hiring evaluator. Whole-branch review (`final-review.md`): "ready to close after one fix wave" — 0 Critical, 3 Important, 11 Minor. Codex (`codex-findings.md`, session 01a0d17d-4492-7721-92d7-580076159b3d, static): 6 findings (4 "CRITICAL", 2 MEDIUM); clean: Activity's scale fallback, engine separation, the Book's registers and near-cap fold, receipt green-state parity, `inlineParts`, the movers cap (20) and both SWEEP_NEVER cases.
- 2026-09-24 03:57Z lease renewed through 2026-09-25T03:57Z (**aa0c4bf**).
- **ADJUDICATION (integrator; owner's rule of 2026-09-23 20:39 applied to Codex, the same test to every other finding):**
  - **FIX in the wave** — I-1 "never swept" on the trust card + drawer (false on the attempted-never-succeeded SWEEP_NEVER arm); I-2 = persona NEW-4 "29/29 Cash rows" vs "account comparisons"; I-3 = Codex #1 the README's "only approved components are cited here" (later changes to cmd/api, cmd/riskd, the client undisclosed); Codex #2 the Node floor ("20 or later" admits 20.8, which cannot run the locked Next / Vite); Codex #3 "nothing was compared" under the empty arm (gated_rows 0) while a weld may still carry comparisons — a false sentence on a state the Go acceptance admits; Codex #4 IN PART (BookLegacy composes its eligible-debt words); Codex #5 + M-1 one guarded liquidation-repaid figure for both surfaces (unreachable today, but the fix merges M-1's two diverging implementations); Codex #6 own-property `typeLabel` (unreachable today; three lines); M-4 "bucket" legend; M-5 realised/realized + LiquidationLine's composed words; M-7 "reconcile-welded"; M-8 README wording and stale counts; M-9 the Makefile's false run-api comment; M-10 = persona item 8 "lane" glossed from the contract; M-11 plan names in comments; persona NEW-1 the liquidation note's raw backticks (rendered through `inlineParts`, words verbatim); NEW-2 two scales in one Amount column (the raw marker moves into the Amount cell); NEW-3 the six dash rows and "this batch" vs "never read" (words only; absence stated only when true); NEW-5 the sweep item leads with THIS account's state; NEW-6 the dotted wire field name; NEW-8 the movers caption states the display order; item 12 the disabled Compare states why; README: a screenshot above the fold, the gallery before the run block, the caption says the pictured receipt is the demo's sample (87 / 29 / Jul 29) not the committed one (30,838 / Aug 2), Verification added to the gallery, a "Where to look first".
  - **OVERRULED (Codex, clause 6)** — #4's hero half: `web/app/overview/copy.ts` is a copy-only module (no JSX, no component); the law's point — components compose nothing — holds; relocating it changes no pixel and no sentence. It joins M-6's copy sweep in Plan B. Severity note: #3 and #4 are CRITICAL by the brief's letter (a false sentence / law 5), but #3's state needs an accepted receipt with 0 checked rows and ≥1 advisory comparison, which no committed artifact has.
  - **PLAN B** — M-2 one "nothing computed" predicate; M-6 copy composed in components (AssumptionsDrawer, MoversTable heads, TransitionCard, BookSurface tiles) + the hero's copy module into web/lib.
  - **OWNER'S LIST (added)** — M-3 + NEW-9 the Liquidatable tile's register ("$0" beside refused; "≥ $100" over a sub that counts all 49; accounts vs positions); persona item 3 (headlines say 2, History says 49), item 6 (legacy's 46 in a collapsed foot line), item 7 ("what the 87 are"; "87/87" six times in one viewport), item 10 (≈20 builder words still public), NEW-7 (a repo path on verificationFailed), NEW-10 (five names for the six accounts), NEW-11 (literal "* " list markers on the API page); README: a one-command DEMO MODE (the served app has none; an evaluator cannot see it without RPC + Postgres + a backfill), the red CI badge, the approval-UUID column (partly addressed in the wave); the API sample's `@solvent/client` import while the client is unpublished (goes with the hosted API). Carried from Task 12 unchanged: the §4 list.
  - **OWNER DECISIONS** — PUSH: origin/main is 235 commits behind; GitHub still shows the old 21-line README ("(coming) … web"), so nothing of Plans 1–5 reaches an evaluator (persona BLOCKER). CI: the last pushed run (31245009467, 2026-08-08) fails three jobs — `go` at gofmt, `web` at typecheck, `race` at `TestLiveDatabaseSchemaGateRefusesUntilMigrated` (goose_db_version absent) and two cmd/reconcile source-derivation tests; `.github/workflows/ci.yml` is inside W3's scope, so a green CI is a candidate quick win before any push.
- **Fix wave wf_a612c282-03c launched** (Opus; five areas on disjoint files, no agent commits): **trust-evidence** (I-1, I-2, NEW-5, Codex #3, M-7, NEW-6, "advisory" gloss only if the source defines it), **activity** (Codex #5 + M-1, #6, NEW-1, NEW-2, M-5, M-11), **book-history** (NEW-3, Codex #4 legacy half, M-4), **lab-kit** (NEW-8, M-10, item 12, M-11), **readme** (I-3 / Codex #1, #2, M-8, M-9, the persona's README items). Per area: implement (unit gates) → an independent read-only review that re-verifies every new sentence against Go / contract → one fix-up pass on Important+.
- CI diagnosis (read-only, for the owner's CI decision): at HEAD, 7 COMMITTED Go blobs fail gofmt (cmd/api/fixture_test.go, cmd/api/p5_runbook_example_db_test.go, cmd/reconcile/{heartbeat_scan.go, snapshotdb/boundary_test.go, tokenconfig_sweep.go, wave_h2_fixes_test.go, wave_h3_fixes_test.go}) — the local checkout's CRLF makes `gofmt -l` on disk list far more, so the check was run on `git show HEAD:<f> | gofmt -l`; the `web` job's 2026-08-08 failure was a type error in tests/unit/lab-runbook-lines.spec.ts that today's tree no longer has (local tsc clean) — but CI runs the WHOLE e2e suite incl. the pixel pins, whose Linux behaviour is unverified; `race` needs the DB schema-gate test's migrations and two cmd/reconcile source-derivation tests investigated. Scoped as its own small workstream, proposed to the owner before any push.
- **Fix wave wf_a612c282-03c COMPLETED** (13 Opus agents, 0 errors): **trust-evidence** APPROVED (0 fix-ups; beyond the brief and verified in Go: a non-SWEEP_NEVER refusal with sweep_block 0 gets no invented cause; "succeeded" is claimed only for computed rows, since refused rows never carry the stale flag — assemble.go:1087, :1170-1180; "advisory" glossed from cmd/reconcile/main.go:1343-1361 + :604; `receiptComparedNothing` → `receiptCheckedNothing`); **readme** APPROVED (the later-change audit re-run by the reviewer: ingest 0, forkreplay 0, reconcile 9, risk 8, riskd 10, riskfeed 12, cmd/api 29, client-ts 19, openapi 15 commits since approval, each placed in a closing round or listed "no closing approval recorded"); **activity** 1 fix-up (Important: `renderBps` printed the NullableDecimal bonus fields unguarded — now "unreadable" off the pattern); **book-history** 1 fix-up (CRITICAL: a refused row's served-but-malformed debt read as "never served" — now `debtUnreadable`, the cell says "unreadable", the dek's absence clause needs every refused row genuinely unserved); **lab-kit** 1 fix-up that could not finish (the Compare hint needs `components/kit/ScenarioLibrary.tsx`, outside its area) — it left a checked patch; **applied by the integrator** (hint beside the disabled button + aria-describedby; `.libHint`; keyboard.spec pins).
- Integrator follow-ups on the reviews' minors: `inlineParts` moved to its own module `web/lib/inline-parts.ts` (api-view re-exports it) so Activity's client bundle never pulls the 203 KB generated contract through api-view; the demo's Cash movers now select as the service does — the 20 LARGEST by debt of the 118 crossing, with the service's own truncation sentence (cmd/api/p5_runbook.go:1148-1151) — because the web's caption (from the contract) said "the 20 largest … by debt" over a demo list of the 20 furthest past the cap, and the README pictures the demo (generator + `run-book-demo-eth_minus_30.json` regenerated; demo-lab-weld.spec re-pointed and now proves no left-off crossing row out-debts the smallest carried); README: the Node floor excludes 23.0–23.5 ("22 (22.18 or later) or 24 and later"), the float-check sentence says what the check does (type-checked expressions + big.Float) instead of "no float literal".
- Carried minors (not fixed; recorded): the T4 unit test uses a Cash weld 1/1 beside gated 0 — unreachable (DM rows are always gated); the reachable case is the legacy weld's advisory rows; the guard it tests holds either way. No unit pin on the T5 rewording of PROVEN/OPERATIONAL/LIVE notes. LIVE_NOTE's "the reconcile covers the receipt's pinned run, not this batch" is weak. `ActivityLiquidation` keeps loose fields beside its `line` parts (test-only readers). An unreadable Activity amount's reason rides a title on an empty unit span. BookLegacy's "Σ withheld" for an ABSENT legacy bad-debt row (Go serves no row when the grid lacks the 1.0 point) — pre-existing; Plan B's envelope work. "risk bucket" also covers the no-debt / not-measured lanes. Compare has no upper bound (the service takes 1–24 ids). package.json `engines` still say ">=20" (owner's list with CI).
- Gates on the integrated tree: tsc / eslint / stylelint clean; **unit 1,148 / 0**. The stale Task 10 server on :3111 (PID 53368, `next start`, started 2026-09-22 15:09) stopped by the integrator; production build + whole e2e running (flag set; scratchpad `build-p5fix.log`, `e2e-p5fix.log`).
- Build + e2e on the integrated tree (flag set): build clean; **e2e 304 passed / 4 failed — all four pixel pins moved BY RULING** (Overview ×2, Book ×2: the no-verdict sentence gained the unserved-debt clause, one line); the captures read, all four re-baselined; the pages that moved by ruling INSIDE the 1% tolerance (Activity, Scenarios, Inspector, Verification) force-re-baselined so later drift is measured from today (History byte-identical); two replays **16/16**. README screenshots recaptured (eight pages incl. the new `verification-dark-fold.png`; History and API byte-identical); the capture server (PID 48184) stopped by the integrator.
- **Commits:** de802e4 (trust/evidence; the Inspector spec's trust hunks staged alone via `git apply --cached`), 671bd4e (Activity + the Inspector spec's activity hunk), 1219673 (Book/History), e7b3e22 (Scenarios/kit + demo movers), c25053d (pins), 28b1553 (README + Makefile + screenshots).
- **Codex re-verification (D-006; task `task-muf1xdb5-m25sfj`, session 01a0d1be-6a78-7c03-a218-dc570c5856ea, ~4.5 min, read-only) → `codex-fix-findings.md`: all six CLOSED; the overrule of #4's hero half ACCEPTED ("an accepted location exception, not literal compliance with `web/lib` placement"); no new findings. APPROVED.** Codex restates, as carried, that `web/package.json` `engines ">=20"` is a false compatibility declaration → the owner's list (with the CI work).
- Plan amended: "Amendments at the gate and the close" appended to `docs/plans/2026-09-22-settle-for-deploy.md` (every place the landed code supersedes the plan's words).
- **Plan 5 CLOSES** — close entry in `.superpowers/sdd/progress-ui-overhaul.md`.
