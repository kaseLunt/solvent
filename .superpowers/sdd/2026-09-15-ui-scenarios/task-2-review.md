# Task 2 review: `lab-transitions` (b3a9308..b8b7cd3)

### Spec Compliance

- ✅ Files: exactly the brief's three, all new — `web/lib/lab-transitions.ts`, `web/tests/unit/helpers/run-book-engine.ts`, `web/tests/unit/lab-transitions.spec.ts`. No existing `web/lib/**` file touched; `web/app/lab/labTransition.ts` untouched (D7 verified: the diff's file list is three additions, 594 insertions, 0 deletions).
- ✅ Exports named in the Interfaces block: `laneReading(engine, { merge })` (`web/lib/lab-transitions.ts:247`), `roomBoundLabel(wad)` (`:74`), `CONTRACT_LANE_EDGES` (`:20-28`), types `RoomBand` (`:30`), `HeatCell` (`:39`), `HeatMovement` (`:38`), `HeatmapView` (`:48`), `LaneReading` (`:71`). Helper: `cashEngine`/`legacyEngine` (`run-book-engine.ts:149-150`), `lanes()` (`:19`), `transitionsOf(table, opts)` (`:44`), `MoveTable` (`:35`), `DEMO_CASH_TABLE` (`:153-163`).
- ✅ R1 merge rule keyed on the exact edge set: `edgesMatchContract` (`lab-transitions.ts:199-211`) requires `bucketCount === 8` and every lane's `lower_wad`/`upper_wad`, read through `wireBigInt`, to equal the contract's seven wads (null at the two open ends). `merged = options.merge && edgesMatchContract(...)` (`:250`). Pin 5 (shifted 1.05 → 1.06) renders verbatim with the wire's label `"1.00 – 1.05"`.
- ✅ Merged cell is the SUM of the wire's cells: accumulation at `:270-271` adds `rows` and (via `addDebt`) both debts per band pair; never re-bucketed — band membership is a fixed lane→band map (`:252-253`), no HF value is re-read.
- ✅ Band labels are the true converted bounds: `roomBoundLabel` (`:74-82`) computes `(wad − 1e18) × 10000 ÷ wad` in bigint, floors to basis points, prints with trailing zero dropped. Recomputed by hand: 1.05 → 476 bp → `4.76%`; 1.10 → 909 → `9.09%`; 1.25 → 2000 → `20%`; 1.50 → 3333 → `33.33%`; 2.00 → `50%`; 1.00 → `0%`. `mergedBands` (`:217-230`) uses these for `< 4.76%`, `4.76% – 9.09%`, `9.09% – 20%`, `≥ 20%`; the hardcoded `"below 1.00"` / `"1.25 and above"` titles are safe because they are only reachable after the exact-edge match.
- ✅ Verbatim path: `verbatimBands` (`:232-244`) keeps the wire's own labels and kinds, one band per lane; unmeasured is a visible band in both modes (`:228`, `:240`).
- ✅ Wire guards, each present and named (the brief's module enumerates them; every one is in `guards`, `:116-197`): `usd_decimals` via `isWireScale` (`:121`); lane count vs histogram buckets + 2 (`:122-124`); lanes/outflows/from_rows/to_rows same length (`:125-127`); comparator vs `before.hf_histogram.comparator` (`:128-130`); `wad_scale` via `isWireDecimal` (`:131`); each lane's `index === i` and bucket wads via `wireBigInt` (`:132-138`); ordering buckets → infinite → unmeasured (`:140-144`); `total_rows`/`measured_rows`/`unmeasured_rows`/every margin entry via `isWirePopulation` (`:145-152`); nullable `held_rows`/`lane_changed_rows` via `isWirePopulation` only when non-null (`:153-154`); per cell `rows` via `isWirePopulation` (`:164`), `to` integer in `[0, laneCount)` (`:168`), both debts via `cellDebt` → `wireBigInt` (`:172-177`); outflow sum vs `from_rows[i]` with both values stated (`:182`); arrivals vs `to_rows[j]` with both stated (`:184-186`); `from_rows` sum vs `total_rows` (`:187-188`); `measured + unmeasured === total` (`:189-191`); `held + lane_changed === measured` when both non-null (`:192-194`). No `BigInt(...)` call anywhere in the module; every string→bigint goes through `wireBigInt`. A contradicted matrix returns `{ kind: "contradictory", reasons }` and never a view (`:249`).
- ✅ Refusals render honestly: `heldRows: t.held_rows` / `laneChangedRows: t.lane_changed_rows` (`:301-302`) carry null through; pin 7 asserts `toBeNull()` on both.
- ✅ Counts: `bandChanged` (`:274`, both ends measured, band indexes), `crossedCap` (`:278-279`, bucket not-over-cap → bucket over-cap, lane resolution), `improved` (`:277`, bucket lane rose or bucket → infinite), `nearToday` (`:306`, `from_rows[2] + from_rows[3]`, merged only), `nearCrossed` (`:280`), `nearLabel` (`:308`, `roomBoundLabel(1.10)` = `9.09%`, null when not merged).
- ✅ Pins verbatim from the brief: the spec file is the brief's Step 2 text unchanged (all 7 tests, same assertions, same order). No recount was needed (see arithmetic below).
- ✅ Demo-table arithmetic, redone by hand from `DEMO_CASH_TABLE` (`run-book-engine.ts:153-163`):
  - `from_rows` = `[41, 8, 14, 13, 73, 153, 300, 804, 0, 6]`; sum 1412 = `totalRows`; unmeasured 6; measured 1406. ✅
  - `to_rows` = `[155, 12, 135, 43, 129, 188, 320, 424, 0, 6]`; sum 1412. ✅
  - `heldRows` = (0,0) 41 + (7,7) 424 = **465**; (9,9) excluded as unmeasured. ✅
  - `laneChangedRows` = 1406 − 465 = 8+14+13+73+6+12+135+43+129+128+60+320 = **941**. ✅
  - `crossedCap` = from lanes 2..7 into lanes 0..1 = (2,0)14 + (3,0)13 + (4,0)73 + (5,0)6 + (5,1)12 = **118**. ✅
  - `bandChanged` = measured cells whose band differs: 14+13+73+6+12 (→ over cap) + 135 (4→1) + 43 (4→2) + 129 (4→3) = **425**; (1,0), (6,5), (7,5), (7,6), (7,7) stay in-band. ✅
  - `improved` = 0 (no cell has `to > from`). ✅ `nearToday` = 14+13 = **27**; `nearCrossed` = 27. ✅
  - Merged cells: (0,0) = 41+8 = 49 held; (1,0) = 14 worse; (4,0) = 6+12 = 18, `debtBefore` = 18 × 1_000_000 = 18_000_000n; (4,1) = 135; **(4,4) = 128+60+320+424 = 932** = `maxRows`; (6,6) = 6 unmeasured. ✅
- ✅ Pin 3 arithmetic: `improved` = (2,3)5 + (6,7)4 = 9; `laneChangedRows` = 5+4+3 = 12; `bandChanged` = 5 (only 2→3 crosses a band); (6,7)+(6,6) merge to band (4,4) = 5 held; (1,0) → band (0,0) held. ✅
- ✅ Pin 6: for the `{2:{0:3, 2:2}, 9:{9:1}}` base, each patch produces the substring asserted — traced `total_rows 7` → `"total_rows states 7 and from_rows sums to 6"`; `held 1 / changed 1` → `"held_rows 1 + lane_changed_rows 1 is not measured_rows 5"`; `measured 4` → `"measured_rows 4 + unmeasured_rows 1 is not total_rows 6"`; `"1e6"` → `outflows[2].cells[0].debt_before_usd "1e6" is outside the wire Decimal contract` (cells[0] is `to: 0` because `Object.entries` orders integer keys ascending); `to: 12` → `"... names lane to 12, and there are 10 lanes"`; fractional → `"from_rows[2] 1.5 is not a wire population"`.
- ✅ Deviations D1–D7, each verified in the diff: D1 `infinite_count: from_rows[INFINITE]`, `refused_count: from_rows[UNMEASURED]` summed (`run-book-engine.ts:101-102`), matching the schema's `from_rows[N] == infinite_count`, `from_rows[N+1] == refused_count`; D2 `cellDebt(value, unmeasuredCell)` (`lab-transitions.ts:87-93`) with `unmeasuredCell` computed by lane kind on both ends (`:172`), `HeatCell.debtBefore/debtAfter: bigint | null` (`:43-45`), `addDebt` null-absorbing (`:96-98`) — the schema (`api/openapi.yaml` `RunBookTransitionCell.debt_before_usd`) says null is "exactly the (N+1, N+1) cell" today, and the fixture `web/tests/fixtures/run-book.eth_minus_30.json` carries `null, null` on `outflows[9].cells[0]` for both engines, so refusing null would refuse every real response and coercing would print a zero for an unknowable; D3 float debt for a non-integer count (`run-book-engine.ts:58-61`) — the count guards refuse in phase 1 before any debt is read, so the float never reaches a `BigInt`; D4 (9,9) null debts (`:62-64`); D5 no `groupInt` import (`lab-transitions.ts:10`); D6 `transitionsOf(table, opts)` (`run-book-engine.ts:44`); D7 old guard layer untouched. None breaks a law.
- ✅ Comments state the law, never the round: module header (`:1-8`), `cellDebt` doc (`:86`), `HeatCell` doc (`:42`), helper's float-debt comment (`:58-60`). The only cross-reference is `(plan R16)` on `DEMO_CASH_TABLE`, which is the brief's own text.
- ✅ Type safety: no `any`; no non-null assertion in the module or helper. The one `!` is `o.cells[0]!` in the spec (`lab-transitions.spec.ts:107`) on helper-built fixture data, which the table guarantees non-empty — acceptable.
- ⚠️ Cannot verify from diff: the reported `7 passed`, `tsc` exit 0, `eslint` exit 0 (not re-run; the arithmetic and the traced reasons above are my basis for believing the pins hold). The Playwright unit project's `testMatch` is `**/unit/**/*.spec.ts` (`web/playwright.config.ts:28`), so the new `helpers/run-book-engine.ts` is not collected as a test — checked because a helper under `tests/unit/` was a concrete risk.

### Strengths

- The guard is two-phase by design (`:155` then `:195`): shape and count contracts first, arithmetic reconciliation second, so the second phase never indexes past a mismatched length or adds a non-integer. That is the correct order and it is what makes the fractional-count pin refuse cleanly instead of throwing.
- Every reconciliation reason states both sides by name (`outflows[2] sums to 2 rows and from_rows[2] states 5`, `to_rows[0] states 4 and the cells arriving there sum to 3`) — a reader of the refusal can find the field without the code.
- The lane-ordering check uses `findIndex` for the first `infinite` and first `unmeasured` (`:140-144`), which is robust to a stray non-bucket lane anywhere in the bucket run; and because `kind` is a three-value enum, first-infinite at N−2 and first-unmeasured at N−1 forces every earlier lane to be a bucket, so `bucketCount` is exact.
- The null-debt law is enforced *by name*: `cellDebt` refuses null with `"is null, and only the not-measured cell carries a debt nobody computed"`, and the merged sum inherits the null through `addDebt` rather than a silent `0n` fallback. The doc comment on `HeatCell` tells the renderer what it must do with it.
- The helper is genuinely wire-true: margins, totals, `held`/`changed`, histogram counts, `infinite_count`, `refused_count`, `accounts`, `newly_eligible_accounts`, and `bad_debt_delta` are all derived from the same table; nothing is a copied constant that a table change could desynchronise. That is why pin 6 can patch one field and get exactly one class of contradiction.
- The pins assert concrete numbers derived independently (I recomputed them); a wrong merge map, a wrong band label, or a manufactured count would fail a specific assertion.

### Issues

#### Critical (Must Fix)

None. Every count, label and merge I recomputed matches; no contradiction the guards recognise draws.

#### Important (Should Fix)

1. **Plan-mandated — `cells[].rows` is checked with `isWirePopulation`, not `isWireOccupancy`, so a `{ rows: 0 }` cell draws** — `web/lib/lab-transitions.ts:164`. The schema declares `RunBookTransitionCell.rows` `minimum: 1` ("An empty cell is ABSENT, never a row of zeros"), and the repo's guard module binds this exact field to `isWireOccupancy` in its assignment table (`web/lib/wireGuard.ts:102-104`, definition at `:168`) precisely because a zero-row cell "passes EVERY margin and census sum (0 changes nothing), so this floor is the only gate that refuses it". The existing Lab classifier already applies it (`web/app/lab/engineClassification.ts:198`). Here `isWirePopulation(0)` is true, both reconciliations pass, the cell is pushed to `cells` (`:180`) and surfaces as a `HeatCell { rows: 0, movement: "better"|"worse"|... }` — contradicting the view's own `cells` doc ("Only occupied cells", `:51`) and giving the renderer an empty cell to colour. The brief's supplied code had the same predicate, so this is inherited, but the new module is strictly weaker than the existing surface on the same field. Fix: import `isWireOccupancy` and use it at `:164` (reason text: `is not a wire occupancy`); add one pin patching a `{ to: 5, rows: 0, debt_before_usd: "0", debt_after_usd: "0" }` cell into a reconciling outflow and expecting a contradiction naming `rows`.

#### Minor (Nice to Have)

1. **A refused cell produces a second, false reason about the outflow sum** — `web/lib/lab-transitions.ts:173-179`. `sum += c.rows` and the arrivals update run only after the debt check passes, so a cell refused for an unreadable debt (or out-of-range `to`) is also omitted from the sum, and the module then reports `outflows[2] sums to 2 rows and from_rows[2] states 5` — a statement the wire does not make (its cells do sum to 5). Pin 6 passes because it only asserts the first substring. Moving `sum += c.rows` (and, after the range check, the arrivals update) ahead of the debt check would keep every printed reason true.
2. **`wad_scale` is validated for shape but never compared to the scale the module reasons with** — `:131` reads it through `isWireDecimal`; `edgesMatchContract` (`:199-211`) and `verbatimBands.overCap` (`:241`, `upper <= WAD`) then assume 1e18 regardless. A wire stating a different scale would fail the merge (safe) but in verbatim mode could mark every bucket over-cap and print `crossedCap` = 0. A one-line `wireBigInt(t.wad_scale) !== WAD` refusal closes it. Not in the brief's enumeration; noted as hardening.
3. **The histogram beside the matrix is only checked for lane count and comparator** — `:122-130`. The schema states lanes 0..N−1 have the same labels and edges as `hf_histogram.buckets[]` and `from_rows[i] == buckets[i].count`; neither equality is checked, so pin 5's shift of the histogram buckets (`lab-transitions.spec.ts:81`) is dead setup. Outside the brief's enumerated guards; a divergence here would show one number on the Book histogram and another on this margin.
4. **Two loose substrings in pin 6** — `lab-transitions.spec.ts:97` (`"lanes"`) and `:111` (`"rows"`) would also match unrelated refusals (`"the matrix's lanes, outflows and two margins…"`, `"total_rows …"`). They are the brief's verbatim pins; tightening to `"states 9 lanes"` and `"from_rows[2] 1.5"` would make each fail only for its own cause.
5. `RunBookTransitions` is exported from the module (`:14`) but unused inside it; harmless, lint-clean, but nothing consumes it yet.

### Assessment

**Task quality:** Needs fixes

**Reasoning:** Spec compliance is complete and every number I recomputed holds; the one Important finding is a one-line predicate swap (`isWirePopulation` → `isWireOccupancy` at `lab-transitions.ts:164`) plus a pin, inherited from the brief's code but a regression against the repo's established guard on this exact field.

## Re-review 1 (ef8477f)

Scope: `git show ef8477f` only (parent `df3383b`; two files, `web/lib/lab-transitions.ts` +37/−9 and `web/tests/unit/lab-transitions.spec.ts` +23/−3; the helper unchanged). Read-only; no pin re-run — every trace below is on paper and none raised a concrete doubt.

**RE-REVIEW: closed.**

### Items

1. **Important 1 (occupancy floor) — ADDRESSED.** `isWireOccupancy` imported (`lab-transitions.ts:10`) and applied to `c.rows` with reason `… .rows 0 is not a wire occupancy` (`:187-190`); the comment beside it states the law (an empty cell is absent; a zero passes every sum). Pin (`spec.ts:120-122`): a `{ to: 5, rows: 0, "0", "0" }` cell appended to `outflows[2]` of the `{2:{0:3, 2:2}, 9:{9:1}}` base is `cells[2]`; traced: occupancy fails → return before `sum`/arrivals, so the outflow sum stays 5 = `from_rows[2]`, arrivals unchanged, histogram unchanged → exactly `["outflows[2].cells[2].rows 0 is not a wire occupancy"]`. Asserted with `toEqual` on the whole array; under the old predicate the reading would be `ok` and `contradictory()` would throw, so the pin can fail.
2. **Minor 1 (no false sum reason) — ADDRESSED.** `sum += c.rows` now follows the occupancy check and precedes the `to` range check (`:193`); the arrivals update follows the range check (`:198`); both precede the debt reads; `cells.push` stays last. Pin (`spec.ts:114-116`): `"1e6"` on `cells[0]` (to 0, rows 3) → sum 5, arrivals[0] 3, one debt refusal → exactly `['outflows[2].cells[0].debt_before_usd "1e6" is outside the wire Decimal contract']`, asserted with `toEqual`. The ordering the implementer chose is sound: `c.to` is indexed only after the range check (`:198`, `:199`), and the occupancy check precedes the sum so a refused count never enters it.
3. **Minor 2 (`wad_scale` vs 1e18) — ADDRESSED.** `else if (wireBigInt(t.wad_scale) !== WAD)` after the Decimal-shape check (`:141`), reason `wad_scale states X and this matrix is read at 1e18`, in phase 1; the comment states the law. Pinned with `"1000000"` (`spec.ts:110`). A negative or non-1e18 Decimal is refused; a leading-zero `"01…"` reads to 1e18 and is accepted, which the `^-?[0-9]+$` contract permits.
4. **Minor 3 (before-histogram vs lanes) — ADDRESSED.** In phase 2 (`:167-177`), for each bucket: `buckets[i].count === from_rows[i]` and `sameEdge` on both edges (`:100-105`: both null, or both readable and equal — an unreadable histogram edge is therefore a named contradiction, not a silent pass). Phase 1 already forces `laneCount === buckets.length + 2` and buckets-first ordering, so the `lane === undefined || lane.kind !== "bucket"` skip at `:169` is unreachable and harmless. New pin (`spec.ts:93-103`): histogram-only edge shift names both `lanes[2].upper_wad` and `lanes[3].lower_wad` with the exact wire strings (traced against the helper's `EDGES[2]`), and a count-only bump yields exactly `["from_rows[2] states 1 and the distribution beside it counts 2 in 1.00 – 1.05"]`. The existing edge-shift pin still renders verbatim because it shifts lane and histogram together. Checked against the real fixture from my first pass: both engines' `before.hf_histogram` counts equal `from_rows[0..7]` and every edge string is identical, so `run-book.eth_minus_30.json` still reads `ok` under the new guard.
5. **Minor 4 (exact substrings) — ADDRESSED.** `"states 9 lanes"` (`spec.ts:106`) matches only the lane-count reason; `"from_rows[2] 1.5"` (`:125`) matches only the phase-1 population refusal.
6. **Minor 5 (unused export) — ADDRESSED.** `RunBookTransitions` removed (`:13-14`). Checked for breakage: the only other importers are `web/lib/lab-headline.ts:8` (`HeatmapView`) and `web/tests/unit/lab-headline.spec.ts:18` (`laneReading`); neither names the dropped type.

### New breakage in this diff

None found. Every existing pin re-traced under the added guards: the demo table, pin 3, pin 4, pin 7 and the edge-shift pin all build their histogram from the same table as their matrix, so the count/edge equalities hold; every pin-6 patch that reaches phase 2 leaves the histogram–margin relation intact; the fractional patch is refused in phase 1 before the histogram check could add a second reason.

### The implementer's unasked notes

- **Out-of-range `to` now also reports `to_rows[0] states 3 and the cells arriving there sum to 0`** — not a defect. It is a true statement about that wire: its column margin claims three arrivals in lane 0 and no in-range cell delivers them. The reason removed in Minor 1 was false (the wire's cells did sum to `from_rows[2]`); this one is not. Root cause is listed first because outflows are checked before margins.
- **AFTER histogram vs `to_rows`; `infinite_count`/`refused_count` vs `from_rows[8]`/`[9]`; bucket label byte-identity; the histogram's own `wad_scale`** — deferred minors, not defects in this diff's scope. The fix list named the before-histogram, and this diff does exactly that. Of the four, the AFTER-side check is the one worth a follow-up line: it is the exact mirror of what landed (`after.hf_histogram.buckets[i].count === to_rows[i]`), and without it the Book's after-distribution and this matrix's column margin can disagree while the before side is guaranteed. The two tallies are the same shape and the helper already sums them, so a pin is trivial. Label identity and the histogram's scale change no number this module prints (merged titles and verbatim labels come from the lanes; edges are compared by value), so they are lowest priority.
