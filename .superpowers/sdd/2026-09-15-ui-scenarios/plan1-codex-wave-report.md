# Plan 1 Codex wave — kit / Overview / Book: verification and fixes

Status: DONE. One commit on `main`: 52abcd9 (parent c92f4fd), 29 paths, staged by name; `scope_gate.py`: OK; no Co-Authored-By.

Verified each of the 23 findings against HEAD (c92f4fd) before touching anything. Line numbers below are HEAD's, not the old diff's.

## Verdict table

| # | Verdict | Where it stood on HEAD | What landed |
|---|---|---|---|
| 1 | already-fixed | 3a2768b — `cash-book.tsx:149` checks `page.batch.id` on every page, reloads once per superseding id, states a second mismatch as a failure. Pinned by `book.spec` "a positions page from another batch reloads the book once". | nothing |
| 2 | real | `cash-book.tsx:166` mapped `page.positions` with no engine check: a legacy page, or one legacy row among Cash rows, entered the Cash bands. | `readCashPage` refuses a page whose `engine`, or any row's `engine`, is not the walk's; the hook lands it as `invalid-response`. |
| 3 | real | `summarizeCash` always called `bookHeadline`; `cash-view.ts:98` printed it — rows `[]` + `walkComplete:false` read "Nothing material is liquidatable… No position is liquidatable. No account is within 10%…"; after a failure `walking` went false and the zero tiles lost `pending`; the Overview entry said "$0 within 10% of cap · walking". | The headline law: a positive stands as soon as read (with the walk sentence); a negative only over a complete walk. Pending variant while walking, "The Cash book could not be fully read this batch." when stopped; `nearCapSentence` claims no negative mid-walk. Tiles: a walk-derived zero is a dash until settled, a stopped walk wears the refused tone and "walk stopped"; kicker says "walk stopped"; `attentionEmptyText` never clears the book over an unfinished walk; Overview `bookEntryLine` says "Walking the book…" / "Walk stopped before the book was read". |
| 4 | real | `page.refused` was never read: a refused page with `positions:[]`, `next_cursor:null` completed the walk as an empty healthy book. | `readCashPage` kind `refused` → the reading's `cash.refusedWhole` (refused headline with the plain cause, refused tiles, nothing walk-derived). |
| 5 | real | `complete = page.next_cursor === null` only. | `total_positions` must pass `isWirePopulation` on a non-refused page, must equal the book aggregate's `positions`, must not change mid-walk; the terminal page completes the walk only when rows delivered equal the census (over-delivery refuses too). The rows a short page did deliver stay as a lower bound beside the named failure. |
| 6 | real | `readCashRow` dereferenced `row.health_factor.num` on an omitted field; the throw was inside `.then(onFulfilled, …)` so no failure landed. | `readCashPage` validates `account`, `engine`, `status`, `value_decimals`, `health_factor` shape, `positions`, `next_cursor`, `refused` before deriving; `readCashRow` reads `health_factor ?? null`; the hook wraps decoding in try/catch → `invalid-response`. |
| 7 | real | `book-headline.ts:61` — `below ?? "No position is liquidatable."` regardless of refused accounts; one refused + zero computed → ok headline. | Beside refused accounts the dek says "No computed position is liquidatable."; a complete walk with zero computed and refused > 0 is "No Cash account could be computed this batch." in the refused register. |
| 8 | already-fixed | 3a2768b — `deriveCashView`: withheld → `summary` null, `refusedTiles`; Overview "Live figures". Pinned by `book.spec` "withheld whole" and `overview.spec` "withheld Cash engine". | (this wave also counts the aggregate's own `refused` flag as withheld — `wholeRefusal`.) |
| 9 | real (residual only) | Chips already said "unavailable", the Overview "Live figures", `refusedPositions ?? 0` is unreachable with a null engine. The residual: `BookSurface.tsx:146` printed "nothing refused" under a 503 or an absent engine. | "not computed" / "loading…" when the count is unknown; "nothing refused" only when the aggregate counts 0; a nonzero count with no key → "cause not stated". |
| 10 | real | `BookLegacy` ignored `engine.refused` / `refused_engines` / `histogram.refused`: `book-engine-refused.json` rendered "0 positions · debt withheld · 0 liquidatable · 0 refused" and eight zero bars. | `deriveLegacyView` (cash-view.ts): withheld → the cause in the summary line and a note, no population read, tiles "—", no bars; a refused histogram alone → "Histogram withheld: …", no bars. |
| 11 | already-fixed | 3a2768b — `NeedsAttention.tsx:85` appends refused rows after the slice. Pinned by `book.spec` demo test (6 dim rows). | nothing |
| 12 | real | `NeedsAttention.tsx:99` — one $50 liquidatable row, toggle off → "No account needs attention." | `attentionEmptyText`: "Nothing material needs attention; N liquidatable position(s) under $100 (Σ) is/are behind the small & dust toggle." |
| 13 | real | `cash-rows.ts:44` — `computed` ignored the verdict: an unknowable verdict on a computed row was "Near cap", in the near-cap Σ, absent from the refused rows. | `computed` requires `liquidation_verdict !== "unknowable"`; `notComputedCause` names the withheld verdict on the pill. |
| 14 | real | `BookSurface.tsx:33,44`, `OverviewSurface.tsx:60`, `BookLegacy.tsx:30,49,58` — bare `BigInt(v)`: `""` → $0, `"0x10"` → $16. | `readWireMoney` (through `wireBigInt`) in the view for engine totals, bad debt, legacy debt and eligible debt; malformed → "—" with `<field> is not a wire decimal` on the sub, refused tone. |
| 15 | real | `value_decimals` / `usd_decimals` unvalidated everywhere; rows summed on the book's scale whatever their own; `humanUsd(x, -0)` multiplied; stress subtracted across scales. | `readWireScale` on `engines[debt_manager].value_decimals`, `bad_debt[*].usd_decimals`, the legacy scales (route refusal by name); `readCashPage` requires every row's `value_decimals` to be a wire scale AND the book's; `stressPreview` refuses a non-wire `usd_decimals` and any point at another scale; `humanUsd` and the materiality line refuse a non-wire scale (`WireIntegerError`, named). |
| 16 | real | `stress-preview.ts:65,75` — `null − 1 = -1` → "-1 accounts". | `cumulative_eligible_accounts` through `isWirePopulation` on every point; a falling cumulative population refuses ("eligible accounts fall between grid points"). |
| 17 | real | `BookLegacy.tsx:26` / `BandBars.tsx:26` — `BigInt(band.count)`: -0 weighed a bar, 1.5 threw namelessly. | Legacy bucket counts through `readWirePopulation` (`hf_histogram[aave_v3_etherfi].buckets[i].count`) in the view; `BandBars` reads every count through `readWirePopulation` before weight or print. |
| 18 | real | `stress-preview.ts:40–44` filtered the engine's points: on the base only → a view with zero lines under "Every figure below is a projection". | The engine must be on every grid point; missing from one → refused "the engine is missing from grid point N"; fewer than two points → refused "the grid has no shocked point". |
| 19 | real | Nothing read `coverage.excluded_by_this_layer` / `coverage.excluded`. | `stressPreview(waterfall, engine, coverage)` carries `unmeasured` ({count on this engine, bookWide, causes}); the card prints `unmeasuredSentence`; the drawer's coverage line adds "N excluded from the stress arithmetic" and lists each excluded account with its cause and code. `API_RECONSTRUCTION_MISMATCH` joined the phrasebook. |
| 20 | real | `OverviewSurface.tsx:61–64` — any non-view preview → "Committed scenarios". | `previewLine` in the view: "Preview withheld: {cause}" for a withheld engine, a refused grid, and an engine absent from the grid; the Book's `StressPreview` reads the same `view.preview`. |
| 21 | real | `LivePill.tsx:31` — `unresolved` → `ageSeconds: null` → the age clause vanished. | `LivePillInput.ageUnresolved`; `livePillWords` says "age unknown" (dim) and the pill renders it beside the batch. |
| 22 | misread (ruling) | Verified by design: the liquidatable tile's sub restates "N more under $100", the dek carries `belowLineSentence`, the toggle "Show N small & dust positions (Σ)" reveals them — `book.spec` demo test. | headline materiality unchanged |
| 23 | dropped (ruling) | copy stays in the libs and view models | nothing moved |

Counts: already-fixed 3 (#1, #8, #11) · real, fixed 18 (#2–#7, #9 residual, #10, #12–#21) · not reproducible 0 · stopped 0 · rulings 2 (#22, #23).

## Fixes and their pins

Every pin below fails on HEAD's code (the shapes were reproduced by reading; the unit pins were written against the new law and the e2e pins assert the strings HEAD did not render).

- **cash-rows.ts** — `readCashPage`, `notComputedCause`, the unknowable-verdict law. Pins: `tests/unit/cash-rows.spec.ts` — "an unknowable verdict on a row the engine calls computed…" (#13); "readCashPage: a refused page…" (#4); "…a foreign engine never enters the Cash walk" (#2); "…the census…" (#5); "…a row without a health factor is a malformed page, never a throw" (#6); "…another scale…" (#15); "…shape…" (#6).
- **cash-book.tsx** — the walk reads every page through `readCashPage`, reconciles delivered rows against the advertised census, catches decoding into `invalid-response`, folds a refused page into `cash.refusedWhole`, reports `legacy.refusedWhole`, skips the walk for an absent or unreadable-scale engine. `PositionsFailure` gains `invalid-response` (positions.ts). Pins (e2e, `tests/e2e/book.spec.ts`): "a refused positions page is the engine's refusal" (#4); "a terminal page short of the advertised census stops the walk by name" (#5); "a positions page from another engine never enters the Cash walk" (#2).
- **book-headline.ts / cash-summary.ts** — `computed`, `complete`, `stopped` on the headline input; pending and stopped variants; `walkSentence`; `nearCapSentence(…, complete)`; `attentionEmptyText`; `asSentence`. Pins: `book-headline.spec.ts` — "a negative is claimed only over computed accounts" (#7), "a complete walk that computed no account…" (#7), "an unfinished walk with nothing material is pending" (#3), "a stopped walk names its cause" (#3), "a material finding stands mid-walk as a lower bound…" (#3); `cash-summary.spec.ts` — pending, stopped, `attentionEmptyText` (#3, #12).
- **cash-view.ts** — `readWireMoney` / `moneyText` / `malformedSub`, `debt` / `collateral` / `badDebt` readings, `readWireScale` at every scale ingress, `walkStopped`, `bookEntryLine`, `previewLine`, the preview refused for a withheld engine, `deriveLegacyView`. Pins: `cash-view.spec.ts` — stopped walk (#3), preview line (#20), money guard (#14), scale guard (#15), legacy view (#10, #14, #17).
- **stress-preview.ts** — every-point coverage (#18), population guard and monotone accounts (#16), scale validation and reconciliation (#15), `unmeasured` + `unmeasuredSentence` (#19). Pins: `stress-preview.spec.ts` — four new tests.
- **live-pill.ts / LivePill.tsx** — `ageUnresolved` → "age unknown". Pin: `live-pill.spec.ts` (#21). (No e2e: a blind resume cannot be staged from a route mock; the component passes `age.unresolved` straight through.)
- **human-usd.ts / materiality.ts** — non-wire scale refuses by name. Pins: `human-usd.spec.ts`, `materiality.spec.ts` (#15).
- **BandBars.tsx** — counts through `readWirePopulation`. Pin: `book.spec.ts` "a fractional legacy bucket count refuses the route by name" (#17; the serialized book's first `"count":0` is the legacy histogram's first bucket — verified before pinning).
- **BookSurface.tsx** — tiles from the view's readings; dash-until-settled; walk-stopped register; `notComputedSub` (#9). Pins: `book.spec.ts` — "while the walk is still running the verdict is pending" (#3), the transport-failure test's new assertions (#3), 503 test additions (#9), "a malformed wire money string is named" (#14).
- **NeedsAttention.tsx** — `attentionEmptyText`, `notComputedCause` on the pill. Pin: `book.spec.ts` "a liquidatable position under the $100 line is hidden by the display rule, and the table says so" (#12).
- **BookLegacy.tsx** — renders `LegacyView`. Pin: `book.spec.ts` "the legacy engine withheld whole" (#10).
- **StressPreview.tsx / BookMethodology.tsx** — the unmeasured sentence; the drawer's exclusion count and list. Pin: `book.spec.ts` "positions the stress arithmetic excluded are named beside the preview and counted in the drawer" (#19).
- **OverviewSurface.tsx** — `moneyText(view.debt/collateral)`, `view.bookEntryLine`, `view.previewLine`. Pins: `overview.spec.ts` — "while the walk runs the strip is pending…" (#3), "a refused stress preview is named on the Scenarios entry" (#20), "a malformed Cash debt on the strip is a dash" (#14).
- **refusal-phrasebook.ts** — an empty code reads "the engine gave no reason"; `API_RECONSTRUCTION_MISMATCH` phrased.

## Gates

- `npx tsc --noEmit`: clean for every file of this wave. The shared tree reports 11 errors, all in the Lab implementer's in-flight files (`app/lab/CompareCard.tsx` importing `compareCaption`/`compareHeadline`/`compareRowWords` that `lib/lab-headline.ts` does not yet export; `tests/unit/lab-library.spec.ts`, `lab-reading.spec.ts`, `lab-view.spec.ts`). Typecheck of HEAD + this wave's files (the build copy below): clean, tests included.
- `npx eslint app/book app/overview lib components/kit tests`: 0 errors; 15 warnings, all unused imports in the Lab implementer's in-flight files.
- `lint:css`: not run — no CSS touched.
- Unit: 678 passed (every `tests/unit` spec except `lab-*.spec.ts`, which cannot load — `lab-library.spec.ts` imports an export that does not exist yet and aborts the run). Run through a scratchpad config without the web server, because the unit project's `webServer` needs a production build the shared tree cannot produce.
- E2E on a production build: `book.spec.ts` + `overview.spec.ts` + `state-matrix.spec.ts` — 55 passed. Pixel pins `screenshots.spec.ts` (book, overview × dark, light) — 4 passed; nothing this wave changes the demo state's pixels.
- The production build could not be made in the shared tree (`next build` fails on `app/lab/CompareCard.tsx`'s missing exports). It was made from a copy: `git archive HEAD web packages/client-ts` into a temp dir, this wave's 29 files overlaid, `node_modules` junctioned to the real one, `turbopack.root` widened to `C:/Users/kasel` in the copy's `next.config.ts` (Turbopack refuses a junction that resolves outside its root), the copy placed at `C:\Users\kasel\AppData\Local\Temp\swb` because Turbopack's chunk names exceed Windows' path limit under the scratchpad path. The copy was removed after the run; the junction was unlinked first so the real `node_modules` was never touched.
- `python roadmap/tools/scope_gate.py`: OK, 29 paths under the integrator claim.

## Deviations

- `web/lib/cash-view.ts` is edited though not in the named file list: it is the view model both surfaces read, not in any other implementer's set, and the money/scale/entry-line laws belong there rather than in the components.
- `humanUsd` and the materiality line now throw `WireIntegerError` on a non-wire scale. Other surfaces reach them only after their own `isWireScale` checks (`lab-movers`, `inspector-position`, `lab-classify` verified); the 678-test unit run confirms no consumer passes an out-of-contract scale.
- `Headline.variant` gains `"pending"`; the Overview strip's `data-variant` is `"pending"` while walking. The Book header's `data-variant` is its tone, `"refused"` for both pending and stopped (the dim register; no CSS added).
- `plainCause("", …)` now reads "the engine gave no reason" instead of "refused ()"; no pin depended on the old string.
- The report is not staged: `.superpowers/sdd/2026-09-15-ui-scenarios/` is untracked in this tree.
- A failed `npm run build` attempt in the shared `web/` may have left a partial `.next/` (gitignored); the next successful build overwrites it.

## Concerns

- The census cross-check holds a positions page's `total_positions` to the book aggregate's `positions` for the same batch. The contract says `total_positions` is the qualifying count only under `min_value`, which the walk never sends, so the two must agree; if the API's aggregate ever counts differently from the positions endpoint on one batch, the walk will name it as an invalid response rather than complete.
- The pending headline's tone is `"refused"` (dim). If the design wants a distinct pending colour, that is a kit/CSS change outside this wave.
- The Lab implementer's tree state blocks the shared build and the whole-suite unit run; the gates above were taken on HEAD + this wave's files, which is what the commit is.
