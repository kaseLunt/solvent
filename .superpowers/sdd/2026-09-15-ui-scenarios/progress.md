# SDD ledger — plan: docs/plans/2026-09-15-ui-scenarios.md

Spec: docs/specs/2026-09-15-ui-product-register-design.md (§5.4, §3.5, §7, §8, §9). Plan committed 9b7158f. BASE at start: 9b7158f.
Mode (owner ruling, unchanged from Plans 1–2): work on `main`, scope-gated, no extra worktree; integrator builds Tasks 1, 11, 14, 15 personally; subagents (`model: fable`, `serena-coder`) build 2–10, 12, 13; a task reviewer gates every task; fix rounds resume the same implementer; whole-branch review + Codex round close. Every commit by pathspec. SDD scripts overwrite `.superpowers/sdd/.gitignore` → restore (`git checkout -- .superpowers/sdd/.gitignore`; the three re-includes are committed). Owner choices (brainstorm): one plan with Compare last/cuttable; no auto-run on cold load.

## Pre-flight conflict table

| Pair / task | Produces vs consumes | Found |
|---|---|---|
| T1 ↔ T11 | `ScenarioLibrary` (`items: LibraryItem[]` outcome key/tone, `run`, `compare`, `addressSlot`), `Heatmap` (`bands`, `cells: HeatCellView[]`, `merged`, `cellTestIdPrefix`) vs the surface and `TransitionCard.cellsOf` | agree |
| T1 ↔ T13 | `DotPlot` rows (`tenths`, `valueText`, `note`, `tone`) vs `CompareCard.rowsOf` | agree |
| T2 ↔ T3 | `HeatmapView` (`bandChanged`, `improved`, `nearToday`, `nearCrossed`, `nearLabel`) vs the dek sentences | agree; the near sentence is silent when `bandChanged === 0` (plan text fixed while writing) |
| T2 ↔ T4/T9 | `laneReading(engine, { merge })` → `LaneReading` vs `outcomeLine` and `EngineResult.heat` | agree |
| T2 ↔ T10 | helper `DEMO_CASH_TABLE` vs generator `CASH_TABLE` | identical tables; the weld re-sums every count (118 · 425 · 941 · 465 · 27) |
| T3 ↔ T9 | headline signatures incl. `EMPTY_LISTING`, `failureHeadline(kind, detail)` | agree |
| T4 ↔ T8/T9 | `RunRecord`/`SetRecord` shapes; `libraryRows(listing, records, selectedId, checked: ReadonlySet)`; `definitionSkew(def, configVersion, run)` | agree |
| T4 ↔ T11/T12 | `deepLinkDecision(scenario, scenarios, listed)` kinds `none/single/conflict/set{runIds, overCap, notice}` (moved verbatim) | agree with the old type (tornadoLines.ts:34–70) |
| T5 ↔ T9/T11 | `moversTable(engine)` → `MoversTable` (`rows[].roomBefore/roomAfter/debtText/tier/becomesLiquidatable`, `moversCaption`, `unreadable`, `note`) | agree |
| T6 ↔ T11 | `addressWorkspace({ address, view, selectedId })` → `AddressWorkspace`; rows are Plan 2's `StressRow` | agree; two pins (after-cap literals) frozen from the fixture at build time — named in the task |
| T7 ↔ T9/T13 | `compareRows(set, engine)` → `CompareView`; `CompareRow.kind/shareText/deltaText/newly/reason` | agree; ranking |share| → |Δ| → wire order (plan text aligned across T7, T10, T13) |
| T8 ↔ T9/T11 | `LabReading` fields | agree |
| T9 ↔ T11 | `LabView.book.*`, `library`, `selectedId`, `checked`, `configVersion`, `compare` | agree |
| T10 ↔ T12/T14 | demo exports `DEMO_SCENARIOS`, `DEMO_RUN_BOOK_ETH`, `DEMO_RUN_BOOK_SET` | agree |
| T11 ↔ T12 | the test-id contract | agree (table in the plan header) |
| T12 ↔ T13 | T13 appends to the contract file; T12's tornado retirements name T13 as home | agree; if Compare is cut, 3b owes those pins |
| T2 self | pins vs code: the demo table's counts, `roomBoundLabel` values, each guard's reason word | arithmetic re-done while writing; agree |
| T3 self | every pinned sentence vs the templates | agree |
| T5 self | formatter pins (`60%`, `$50`, `0.75`) vs `formatTenths`/`humanUsdFull`/`hfDisplayFromWad` | verified against the modules; `hfDisplayFromWad` places noted as a check |
| T9 self | `contradictory` (numbers disagree) vs `unreadable` (malformed field) both → state `contradictory` | agree; noted in the task |
| T10 self | generator vs weld: `from_rows`/`to_rows` sums, ≥ 20 crossing rows on page 1 (207 exist) | agree |
| T12 self | 16 contract tests vs the mapping; every old file named | agree |

Rulings before Task 1:
- Ruling: `ViewChip` (Plan 1's `lib/cash-view.ts`) may lack `title`; `lib/cash-view.ts` is frozen. `lab-view` declares its own `LabChip { label; value; tone?; title? }` (structurally an `IdentityChip`) instead of importing `ViewChip` — carried into Task 9's dispatch. Cost if wrong: a duplicate four-field type.
- Ruling: the plan's Task 12 `lab × refused` state-matrix cell pairs `ADDRESS_UNKNOWABLE` with `stress-unknowable.json`; the two bodies' addresses may differ — the implementer re-identifies the stress body to the lookup's address (Plan 2's practice). Cost if wrong: one cell mocks a mismatched address and fails visibly.
- Ruling: Task 1 is the integrator's; its review is a task review like any other.

## Progress
- Task 1 (integrator): DONE, commit b3a9308 (8 paths by pathspec; geometry pins 2/2; typecheck/lint/lint:css clean). Report task-1-report.md. Review → task reviewer (fable) on the package 9b7158f..b3a9308.
- Task 1 review dispatched → reviewer ab038a6df28dc13ec (fable) on review-9b7158f..b3a9308.diff.
- Task 2 dispatched → implementer a263d6bf0d92771d0 (serena-coder, fable); BASE b3a9308; brief task-2-brief.md; report task-2-report.md.
- Briefs pre-generated for Tasks 3–10, 12, 13 (task-N-brief.md); sdd .gitignore restored from the scratchpad backup after each script run.
- Task 1 review (task-1-review.md): spec ❌ on three plan-mandated lines (wrong "on" token `--panel-2` → `--chip-bg`; dead active-mode colour by specificity; `role="table"` without rows) + 9 minors. Ruling: all three Importants are plan defects — fixed as the reviewer says; minors 4–12 taken in the same round (cheap; 7 changes `DotPlotRow` to a discriminated union, carried into Task 13's dispatch). Fix round 1/5 by the integrator: commit ff534e9. Scoped re-review → ab038a6df28dc13ec.
- Task 2 report: DONE_WITH_CONCERNS, commit b8b7cd3 (parent b3a9308; 3 paths; 7/7; typecheck/lint clean). Rulings on its concerns: (1) histogram `infinite_count`/`refused_count` summed from the table — accepted (a copied constant would contradict `from_rows`); (2) cell debts are `NullableDecimal` on the wire and null on the not-measured cell → `HeatCell.debtBefore/debtAfter: bigint | null`, null accepted only on the unmeasured cell, anywhere else a named contradiction — accepted; Task 11's `TransitionCard` prints a null debt as "—" (`bookMoney` already does); (3)–(5) helper/interface-line drift — accepted, the code is the law; (6) the old `labTransition.ts` guard layer retires in Task 12 — noted; (7) `roomBoundLabel` floors to basis points — accepted (pinned; the seven contract edges are exact). Review package review-b3a9308..b8b7cd3.diff → task reviewer.
- Task 3 dispatched → implementer (serena-coder, fable); BASE ff534e9; brief task-3-brief.md; report task-3-report.md.
- Task 2 review dispatched → reviewer a0f211cfa6217b184 (fable) on review-b3a9308..b8b7cd3.diff. Task 3 implementer id: a2ad4458993b25407.
- Task 1 re-review 1 (ff534e9): RE-REVIEW: closed — 3 Importants and minors 4–12 addressed; no new breakage.
Task 1: complete (commits b3a9308..ff534e9, review clean). Carry-notes: (a) Task 11 — `ScenarioLibrary` returns `<aside>` + footnote `<p>` as a fragment; the surface wraps it in one block (`.libCol`) so the page grid has one library item and `.lib`'s `align-self` acts; (b) Task 12 — the demo's unmeasured count (6) must stay above zero for the dashed-cell pin; (c) Task 13 — `rowsOf` builds the `DotPlotRow` discriminated union (a value with a signed tone | null with `refused`).
- Task 2 review (task-2-review.md): spec ✅ (every count recomputed, every guard traced); quality Needs fixes — Important 1 (plan-mandated): `cells[].rows` through `isWirePopulation` lets a `{ rows: 0 }` cell draw; the repo binds that field to `isWireOccupancy` (`wireGuard.ts:102-104`). Ruling: plan defect, fix as the reviewer says + pin. Minors 1 (a refused cell yields a false second reason about the outflow sum — move the sum/arrivals update ahead of the debt check), 2 (`wad_scale` never compared to the module's WAD — refuse a mismatch), 3 (histogram beside the matrix: labels/edges/counts not checked against the lanes — check `from_rows[i] === buckets[i].count` and the edges), 4 (two loose substrings in pin 6 → exact), 5 (unused export) — all taken in the same round. Fix round 1/5 → implementer a263d6bf0d92771d0.
- Task 3 report: DONE_WITH_CONCERNS, commit df3383b (parent ff534e9; 2 paths; 5/5; typecheck/lint clean). Rulings: D1 (the wire's `path_assumption` is quoted as served after "Path:", not recapitalised — `terminated()` beside `sentence()`) accepted; D2 (the legacy market takes its article via `modelled(id)`, importing `LEGACY` from Plan 2's `inspector-position`) accepted — the Consumes line drift is noted; concerns 3 (`beforeEligible` carried unused — the tiles print it, Task 11), 4 (`failureHeadline("failed", {})` → "answered 0" — the view always passes the status), 5 (`notCoveredHeadline` with Cash in `engines` unreachable) noted, not acted on. Review package review-ff534e9..df3383b.diff → task reviewer.
- Task 4 HELD until the Task 2 fix round lands (it appends to `tests/unit/helpers/run-book-engine.ts`, which that round may touch; one implementer at a time).
- Task 3 review dispatched → reviewer ace398b618fd7411f (fable) on review-ff534e9..df3383b.diff.
- Task 2 fix round 1/5: DONE, commit ef8477f (parent df3383b; 2 paths; 8/8; typecheck/lint clean) — all six items as ruled. Implementer's unasked note (after-histogram vs `to_rows`, the two tallies, label identity, the histogram's `wad_scale` unchecked) → handed to the re-reviewer to classify; default: deferred minors. Scoped re-review → a0f211cfa6217b184.
- Task 4 dispatched → implementer (serena-coder, fable); BASE ef8477f; brief task-4-brief.md; report task-4-report.md.
- Task 3 review (task-3-review.md): spec ✅ (brief blocks diffed against the files; demo and "better" deks recomputed), quality Approved.
Task 3: complete (commits ff534e9..df3383b, review clean).
Task 3: minor (deferred): singular-verb slips on count-of-one paths (`1 improve` → `improves`, `1 account become` → `becomes`, `but 1 change band` → `changes`) — copy; `failureHeadline("failed", {})` prints "answered 0" (make `status` required on that arm); the `flipped` dek pinned with `toContain` not `toBe`; `description` through `sentence()` recapitalises a leading ticker; `signedUsd` negates redundantly; plan R11's examples (`$1.28M`, `$40.8K`) are stale beside `humanUsd`'s tiers (`$1.2M`, `$40K`) — plan text only.
- Task 2 re-review 1 (ef8477f): RE-REVIEW: closed — Important 1 and minors 1–5 addressed; no new breakage; the out-of-range pin's extra `to_rows[0]` reason is a true statement, not a defect.
Task 2: complete (commits b3a9308..ef8477f, review clean).
Task 2: minor (deferred): the AFTER histogram vs `to_rows` (the mirror of the before-side check that landed — worth a line), the two tallies (`infinite_count`/`refused_count`) vs `from_rows[8]/[9]`, bucket label identity, the histogram's own `wad_scale` vs the matrix's — none changes a printed number today.
- Task 4 report: DONE_WITH_CONCERNS, commit fd1f27f (parent ef8477f; 5 paths; 20/20 incl. the moved deep-link block, move proven by empty diffs; typecheck/lint clean). Rulings: (1) the brief's `settled` helper type was non-distributive → `Extract<RunRecord, { phase: "settled" }>["outcome"]` — accepted, plan defect; (2) the readers are typed on the sealed `LabRunBook`/`LabRunBookEngine` (what `RunBookOutcome.ok.response` IS) — accepted; Task 9's `readEngine(run)` and `BookWorkspace.run` are `LabRunBook`, carried into Task 9's dispatch; (3) `laneReading` takes the WIRE engine type so `lab-library` bridges with `{ ...cash, projection: null }` — Ruling: narrow `laneReading`'s parameter (`lab-transitions.ts`) to `Pick<RunBookEngine, "usd_decimals" | "hf_transitions" | "before">` (type-only, no behavior) and Task 5's `moversTable` to the Pick of what it reads, so both accept the sealed engine; the bridge is removed in Task 5's commit — carried into Task 5's dispatch. Cost if wrong: a wider parameter type on two pure functions. (4) `COVERAGE` summed from `BATCH` in the helper — accepted; (5) two moved comments carry "R58 item 6" verbatim — the byte-for-byte law wins for the move; deferred minor for Task 12 (which deletes the source) to reword to the law; `SCENARIO_ID_PATTERN` not imported (unused) — accepted. Review package review-ef8477f..fd1f27f.diff → task reviewer.
- Task 5 dispatched → implementer (serena-coder, fable); BASE fd1f27f; brief task-5-brief.md; report task-5-report.md; carries the narrowing ruling (touches lab-transitions.ts and lab-library.ts).
- Task 4 review dispatched → reviewer a872cf8c769cc6b20 (fable) on review-ef8477f..fd1f27f.diff. Task 5 implementer id: acac802f0ab3f3dac.
- Task 5 report: DONE_WITH_CONCERNS, commit cf124d3 (parent fd1f27f; 4 paths; 21/21 across movers/transitions/library/headline; typecheck/lint clean). The narrowing ruling landed: `LaneEngine` (lab-transitions) and `MoverEngine` (lab-movers) Picks; the lab-library bridge removed. Rulings: `hfAfter` pin "0.75" → "0.756" (the brief flagged it; `truncateToDisplay` keeps three truncated digits — the Book's law) accepted; the unreadable-test scaffolding rebuilt so the spec's own helper does not throw on "1e9" — accepted (expects verbatim); `RunBookEngine` kept local — accepted; "five paths" was the controller's miscount, four is right. Concerns → deferred minors unless the reviewer rates them higher: a half-null ratio pair dashes quietly (a contradiction the table does not name); "unreadable" shares the display string type (the page must consult `unreadable[]` to tone a cell); `mover.engine` unchecked against the parent engine. Review package review-fd1f27f..cf124d3.diff → task reviewer.
- Task 6 dispatched → implementer (serena-coder, fable); BASE cf124d3; brief task-6-brief.md; report task-6-report.md.
- Task 4 review (task-4-review.md): spec ✅ (the move reproduced by four empty diffs; every word/tone/order checked), quality Approved.
Task 4: complete (commits ef8477f..fd1f27f, review clean).
Task 4: minor (deferred): the `heatOf` comment overstates its law (Task 5 removed the bridge — check the comment went with it); `usd_decimals` guarded only transitively in `outcomeLine` (add `|| !isWireScale(cash.usd_decimals)` so a bad scale reads "Unreadable", not "Contradictory"); the band-change pin does not discriminate `merge` (a `{6:{5:k}}` table would); `outcomeLine` lacks a docblock stating its check order; the `DeepLinkDecision` type's one-line docblock (tornadoLines.ts:33) was outside the moved range — Task 12 carries it over; the helper's `COVERAGE` is a claim nothing reads yet.
- Task 5 review dispatched → reviewer a2caa2b2a58f4759a (fable) on review-fd1f27f..cf124d3.diff. Task 6 implementer id: a80e588b9290eb46c.
- Task 6 report: DONE, commit a444953 (parent cf124d3; 2 paths; 4/4; typecheck/lint clean). The two sanctioned literals frozen: dek "Room today $190.50; after the shock, over cap by $1,069.", capAfter "$3,752" (the demo's eth_minus_30 moves weETH only: 8,400 × 0.7 × 50 % + 812.50 = 3,752.50). Concerns noted: the unavailable dek splits the Inspector's dek at its first ". " (brief's design); `roomAfter` stays a regex (not sanctioned to freeze); a rows state with a null `cash`/`decimals` prints "No scenario applies…" (unreachable — `stressReading` is Cash-scoped). Review package review-cf124d3..a444953.diff → task reviewer.
- Task 7 dispatched → implementer (serena-coder, fable); BASE a444953; brief task-7-brief.md; report task-7-report.md.
- Task 6 review dispatched → reviewer a7720bb06aec32d36 (fable) on review-cf124d3..a444953.diff. Task 7 implementer id: abdc9d64cf5ae7ed5.
- Task 5 review (task-5-review.md): spec ✅ (rooms, tiers, money, wads recomputed; narrowing type-only and exact), quality Approved with one Important (plan-mandated): `moversCaption` on a scale-refused table prints "showing 0 accounts moved · total not stated" — a zero-looking unknowable. Ruling: plan defect; `moversCaption` refuses by name when `unreadable` names `usd_decimals` ("not readable: unreadable scale"), one branch + one pin; the half-null ratio pair (exactly one side null) names the null side in `unreadable[]` in the same round (two lines). Deferred minors: "unreadable" shares the string type (a value-or-refusal union is the page task's call); `mover.engine` unchecked (outside the Pick); a negative side would print a room (app-wide sign class); `decimals: 0` stand-in on a refused table (the page must not print it); `no debt` arm unreachable from this server; the `total` two-step. Fix round 1/5 → implementer acac802f0ab3f3dac.
- Note: the Task 5 fixer (lab-movers.ts + spec) and the Task 7 implementer (lab-classify, lab-compare + specs) are live together — disjoint files, both commit by pathspec; the overlap is deliberate.
- Task 5 fix round 1/5: DONE, commit 88f0518 (parent a444953; 2 paths; 5/5; typecheck/lint clean) — both items as ruled. Scoped re-review → a2caa2b2a58f4759a.
- Task 5 re-review 1 (88f0518): RE-REVIEW: closed — both items addressed; a focused `tsc` on the aliased-predicate narrowing held; no new breakage.
Task 5: complete (commits fd1f27f..88f0518, review clean). Carried minors stay in the deferred list above.
- Task 6 review (task-6-review.md): spec ✅ (both frozen literals recomputed; the ladder, tones and R8/R15 traced), quality Approved with two plan-mandated Importants. Rulings: I1 — the projection row's headline ignores its horizons (a liquidatable horizon would sit under "stays inside its cap") → add the projection arm the reviewer describes (an `unknowable` horizon → refused; a `liquidatable` horizon → "becomes liquidatable within {horizonLabel} under {label}" warn; else "stays inside its cap through {longest horizon} under {label}" ok; dek from `extraInterest` via `humanUsdFull`), pin the demo's projection emphasis exactly; I2 — `money()` prints a negative wire figure and the before tiles ignore `view.refusedTiles` → refuse negatives, gate the before tiles on `refusedTiles`. Taken minors: 5 (split the "No scenario applies…" arm by cause — empty rows / unreadable scale / no Cash position beside rows), 7 (`afterStatus` uses `headroomBand` so a 1 % room after reads "Near cap" warn like the Inspector). Deferred: 3 (the unavailable dek splits at ". " — later `lookupLoad` on `InspectorView`), 4 (`roomAfter` regex — freeze next time), 6 (`sentence`/`refused` duplicated from lab-headline — export from there in the final wave), 8 (no-position names the lookup's batch), 9 (looser pins, `w.tiles!`), 10 (law comments on the helpers). Fix round 1/5 → implementer a80e588b9290eb46c (live beside the Task 7 implementer; disjoint files).
- Task 7 report: DONE_WITH_CONCERNS, commit 409d4c8 (parent 88f0518; 5 paths; 42/42 = 28 + 11 + 3, the classifier counts equal the originals'; move proven by empty diffs; typecheck/lint clean). Rulings: (1) the plan's pin `shareTenths(-1n, 3n) → 0n` was arithmetically wrong (−333n) → `(-1n, 3000n) → 0n` pins truncation toward zero — accepted, plan defect; (2) `unmeasurable_engines[].reason` is an enum → the pin uses `no_positions_in_batch` — accepted; (3) the read-field guards now run BEFORE the classifier so a malformed decimal is `unreadable` (named) and only a classifier hit is `contradictory` — accepted (the plan's order made the arm unreachable); (4) `MINUS` from human-usd — accepted; (5) round language carried byte-for-byte in the moved classifier and the copied spec headers (listed in the report) → Task 12's reword when it deletes the source; the set-run copy's dropped `isNullableWirePopulation` docstring (the `eligible_accounts_delta` → `isWireSignedCount` guidance) to be re-homed there too — carried into Task 12's dispatch. Review package review-88f0518..409d4c8.diff → task reviewer.
- Task 8 dispatched → implementer (serena-coder, fable); BASE 409d4c8; brief task-8-brief.md; report task-8-report.md (live beside the Task 6 fixer; disjoint files).
- Task 7 review dispatched → reviewer a09a8b4b9f1f86d58 (fable) on review-88f0518..409d4c8.diff. Task 8 implementer id: a7c2a407c1cb47283.
- Task 8 report: DONE_WITH_CONCERNS, commit 1323510 (parent 409d4c8; 2 paths; 3/3; typecheck/lint clean). Rulings: (1) the render-time ref assignments moved into a no-deps `useEffect` (`react-hooks/refs`) — the brief's own fallback, accepted; (2) `setListing({ phase: "loading" })` moved out of the effect into `reloadListing` (`react-hooks/set-state-in-effect`) — accepted, behavior unchanged; (3) path comments — fine; (4) two `run(id)` calls in one synchronous tick both pass `canDispatch` (the refs update after commit) — handed to the reviewer to rate; the synchronous guard would be `controllers.current.has(id)`; (5) `"__set__"` is a legal scenario id and would share a controller slot with the set run — handed to the reviewer; a separate ref for the set controller is the fix. Review package review-409d4c8..1323510.diff → task reviewer.
- Task 9 dispatched → implementer (serena-coder, fable); BASE 1323510; brief task-9-brief.md; report task-9-report.md; carries the LabChip ruling and the sealed `LabRunBook` types.
- Task 8 review dispatched → reviewer aa68cb583845ad2a1 (fable) on review-409d4c8..1323510.diff. Task 9 implementer id: a8c7230083accec24.
- Task 6 fix round 1/5: DONE_WITH_CONCERNS, commit c1fd2c6 (parent 88f0518; 2 paths; 7/7). Frozen projection literals: emphasis "0x7a3f…c21e stays inside its cap through 90d under Debt Manager borrow APY +200bps (PROJECTION)." ok; dek "Room today $190.50; 30d: +$7.92 interest; 90d: +$23.77 interest."; statusAfter Near cap warn. Rulings: the after-side guard widened to the whole side (`readable(side)`: a null or negative debt/cap refuses debt, cap, room and status together) — accepted, the Inspector's own law; NEAR_BANDS restated locally as {0,1,2,3} (the Inspector's real set; private there) — accepted; the after-room tone beside a "Near cap" status → taken now as round 1b (one token), then one scoped re-review over both commits.
- Task 7 review (task-7-review.md): spec ✅ (moves reproduced; ranking recomputed; every concern verified), quality Approved with two plan-mandated Importants. Rulings: I1 — the result envelope is trusted; the old tornado's partition check (`engines` + `withheld_engines` + `unmeasurable_engines` = `covered_engines`, no overlaps) did not travel → add it in `rowOf`: a break is `kind: "contradictory"` with the reason naming the partition, before any engine is read; pin one break each way (a summary for an uncovered engine; a covered engine in no array). I2 — a compound fault under-names → always run `classifySetRunEngine`; `kind` is `unreadable` when every malformed field is one of the three read fields, else `contradictory`; `reason` names all of them. Taken minors: 1 (`shareTenths` delegates to `percentTenths`), 2 (a negative share over a tenth pinned, e.g. `−150%`), 3 (`?? 0n` crutch removed by a narrowing filter). Deferred: 4 (`v.rows[0]!` in the spec). Fix round 1/5 → implementer abdc9d64cf5ae7ed5.
- Task 6 round 1b: DONE, commit 477e513 (parent c1fd2c6; 7/7) — `roomAfter`'s tone follows `statusAfter`. Scoped re-review over c1fd2c6 + 477e513 → a7720bb06aec32d36.
- Task 8 review (task-8-review.md): spec ✅ (the two lint moves reproduced against the project's rules and traced as behavior-preserving; eslint re-run clean), quality Approved.
Task 8: complete (commits 409d4c8..1323510, review clean).
Task 8: minor (deferred): the pre-commit double-dispatch window (unreachable from one action; harden with `controllers.current.has(id) ||` before `canDispatch`, and the set equivalent — fold into whichever task next opens the hook, e.g. Task 11/12 if the harness drives a deep link and a click on one id); the `"__set__"` sentinel shares the controller map with legal scenario ids (a separate `setController` ref, aborted on unmount); a redundant `SetRunOutcome` annotation; the unmount effect and the controllers ref lack law comments.
- Task 7 fix round 1/5: DONE_WITH_CONCERNS, commit 00a18eb (parent c1fd2c6; 2 paths; 44/44 = 28 + 11 + 5). Rulings: overlap judged over the concatenation (a duplicate inside one part is a break) — accepted, stricter is honest; the brief's `held`/`absent` pin rows corrected to obey the census law (`covered_engines` names the withheld/unmeasurable engine) — accepted; the four-arrays assumption and the `usd_decimals`-only → `unreadable` classification handed to the re-reviewer. Scoped re-review → a09a8b4b9f1f86d58.
- Task 6 re-review 1 (c1fd2c6, 477e513): RE-REVIEW: open — all five items ADDRESSED (the `readable(side)` widening is the Inspector's own law); two new consistency gaps in the fix diffs: A — an unreadable after side still lets `row.flips === true` print "becomes liquidatable" crit beside refused tiles; B — `view.cash === null` also holds for `cannot-compute`, so the disagreement arm prints a definitive "No Cash position…" under a withheld book. Ruling: both real (the first law) → fix round 2/5: A — after the projection arm, an unreadable non-null side → refused "Cannot say whether {short} becomes liquidatable under {label}." with the dek + " The shocked figures are not a position."; B — before the not-found sentence, `state === "cannot-compute"` → refused "Cannot say — the Cash book is withheld for {short}." with the disagreement dek; pin both (the negative case's emphasis/tone; `UNKNOWABLE_ADDR` beside the demo stress). Taken residual: gate the three after money tiles on `side.verdict !== "unknowable"` (the before side's `refusedTiles` symmetry). → implementer a80e588b9290eb46c.
- Task 9 report: DONE_WITH_CONCERNS, commit 19d7576 (parent 00a18eb; 2 paths; 9/9; whole unit project 1231 passed; typecheck/lint clean). Rulings: (1) the demo-result pin builds its run from the listing's own definition (`ETH_DEF`) because the helper's `DEFINITION_ETH.path_assumption` is a shortened copy of the fixture's — `definitionSkew` rightly reports a skew; accepted, the code is the law; (2) Ruling: reconcile the helper's `DEFINITION_ETH` to the fixture's `eth_minus_30` bytes (description, path_assumption, out_of_model, shocks) in Task 10's commit (a test-helper path added to it), so future pins can use either — cost if wrong: one helper literal; (3) the pre-ruled deviations (LabChip local; sealed types; `Extract` helper type; unused import dropped) — accepted. Review package review-00a18eb..19d7576.diff → task reviewer.
- Task 10 dispatched → implementer (serena-coder, fable); BASE 19d7576; brief task-10-brief.md; report task-10-report.md; carries the DEFINITION_ETH reconciliation (live beside the Task 6 fixer; disjoint files).
- Task 9 review dispatched → reviewer aae38a7240bf43d70 (fable) on review-00a18eb..19d7576.diff. Task 10 implementer id: a44e8d32b942a7905. The visual companion server exited on its idle timer at ~14:41 — restart with the same `--project-dir` at Task 14.
- Task 6 fix round 2/5: DONE_WITH_CONCERNS, commit 8441749 (parent 477e513; 9/9) — A, B and the residual C as ruled. Its concern (the dek prints room words from an unknowable after side while the tiles refuse) → round 2b (one condition in the room words), then one scoped re-review over 8441749 + 2b.
- Task 11 (integrator) started in parallel: every lib it composes has landed; files written under app/lab/ (uncommitted until the gates and the look); the build/e2e wait for Task 10's report so its unit runs are not disturbed.
- Task 7 re-review 1 (00a18eb): RE-REVIEW: closed — both Importants and the three minors addressed; the stricter overlap reading is the old tornado's own parity; the corrected pin inputs are wire-lawful; a focused run 5/5.
Task 7: complete (commits 88f0518..00a18eb, review clean).
Task 7: minor (deferred): the four set-run arrays are assumed arrays (`runBookSet()` casts `res.json()`; four `Array.isArray` checks → `contradictory` naming the field would close it — the page task's call); the old tornado also flagged a numeric row with `accounts === 0` (parity note, not a refusal at zero).
- Task 6 round 2b: DONE, commit f338b53 (parent 8441749; 9/9) — the dek prints "not computed" for an unknowable side. Scoped re-review 2 over 8441749 + f338b53 → a7720bb06aec32d36. (Its typecheck note about `./LabSurface` was the integrator's Task 11 files landing mid-write; both files exist now.)
- Task 6 re-review 2 (8441749, f338b53): RE-REVIEW: open — A, B, C, 2b ADDRESSED and pinned whole-headline; one residual of A: the projection arm sits above the unreadable-side gate, so a projection row over an unreadable/unknowable spot side still prints its horizon verdict beside refused tiles. Ruling: real (same class as A, the law `rowHeadline` itself states) → fix round 3/5 → implementer a80e588b9290eb46c: `computable(side)` helper carrying the condition once (after tiles, `sideRoomWords`, the gate), gate hoisted above the projection arm, stale duplicate JSDoc removed, one pin (projection over negative spot debt → refused cannot-say + NOT_COMPUTED). Still deferred: dek "Room today" (stress before) vs `roomBefore` tile (lookup's Cash) may disagree (minor 8's family); minors 3, 4, 6, 8, 9, 10; `roomAfter` regex.
- Task 11 (integrator): the deep-link decision is now derived at render from the listing (notice never stored; `?scenarios=` pre-ticks its ids in the `useState` initializer; the effect only dispatches once) — clears `react-hooks/set-state-in-effect`. `npx eslint app/lab` clean; typecheck clean on Task 11 files; build/look wait for Task 10.
- Task 9 review (task-9-review.md): spec ✅ (exports, ladder, banners, chips, R3 signatures, demo figures recomputed 941/49/167/118 and the $1.2M headline), quality Approved; two plan-mandated Importants. Rulings: I1 — `readEngine` re-checks `newly_eligible_accounts` with `isWirePopulation`, but the wire contract (wireGuard's assignment table, the classifier) makes it a SIGNED net count; a contract-legal negative would read "unreadable" → drop it from the `pops` list (the classifier already guards it; `resultHeadline` gates on `newly > 0`) — the contract is the law; cost if wrong: one lost duplicate check. I2 — the library row (`outcomeLine`, Task 4) and the workspace (`readEngine`) judge one response by two guard sets (e.g. `movers_total: -1` → workspace contradictory, row "+$1.2M liquidatable" crit) → ONE reader: move `EngineResult`, `EngineReading`, `readEngine` (and their private helpers) to a new `web/lib/lab-engine.ts`; `lab-view.ts` and `lab-library.ts` both import it; `outcomeLine` delegates to `readEngine(o.response, CASH, definition)` and maps its kinds to the existing keys/tones/texts (not-covered → "Not modelled for Cash"; withheld → "Withheld"; unreadable → failed "Unreadable"; contradictory → failed "Contradictory"; result → the crit line from `deltaEligibleDebt`/`decimals`, else band words from `heat.view.bandChanged`); `heatOf` and the two-field guard go (I1's library copy vanishes with it); Task 4's pins keep their words. Task 11's uncommitted `app/lab/*` imports move to `@/lib/lab-engine` by the integrator (the fixer never touches app/lab). Cost if wrong: one module boundary. Taken minors: 1 (drop the dead `definitionChips(definition, null)` arm and its manufactured "unstated" chip), 2 (type `EngineResult.heat` as `HeatmapView`; the contradictory arm returns earlier), 4-part (dedupe `identity.engines` — `ResultIdentity.engines` says distinct, wire order; the nobody/both-arrays chip words stay: those bodies already read withheld), 6 (no leading colon when the set-run refusal `code` is ""), 7 (one sentence stating `resultBook`'s precedence). Deferred: 3 (`busy` — plan-mandated enum), 5 (moot once Task 10 reconciles the helper). Fix round 1/5 → implementer a8c7230083accec24 — HELD until Task 10 reports (both would edit test files in the one working tree).
- Task 6 fix round 3/5: DONE_WITH_CONCERNS, commit b42074a (parent f338b53; 10/10; typecheck 0 errors repo-wide; eslint clean) — `computable(side)` carries the law once (after tiles, `sideRoomWords`, the gate, and `afterStatus` folded in), the gate hoisted above the projection arm, orphaned JSDoc gone, two pins (projection over negative spot debt; projection over a null verdict). Concern ruled real: a projection has no shock → round 3b: inside the gate, a projection row's dek reads "under the projection, not computed." and its unreadable cause "The projected figures are not a position." (spot rows keep the shock words; the unknowable cause is shared) → a80e588b9290eb46c. Then scoped re-review 3 over b42074a + 3b.
- Task 6 round 3b: DONE, commit 3332a45 (parent b42074a; 10/10) — a projection row refuses in projection words. Scoped re-review 3 over b42074a + 3332a45 (review-f338b53..3332a45.diff) → a7720bb06aec32d36.
- Task 6 re-review 3 (b42074a, 3332a45): RE-REVIEW: open — the residual of A closed as named (gate ahead of every verdict word, law carried once, round-2 pins unchanged in meaning, new pins fail on the parent); one narrower hole the reviewer's own round-2 condition carried: the gate filters null sides, so a projection row with a missing after side passes to `projectionHeadline` while its tiles refuse. Ruling: real (a verdict word on an unknown; same class) → fix round 4/5 — per the loop a FRESH implementer (serena-coder aeceb7a067e294d56); the owner's standing ruling keeps it on fable, the fix being two lines and one pin fully specified: `[before, after].some(s => computable(s) === null)` trips the gate; cause = unreadable figures when a non-null side fails `readable`, else the withheld/unknowable sentence; pins `after: null` and `before: null` on the projection row. The `flips === null` arm below becomes unreachable — left unless TS objects.
- Task 6 fix round 4/5: BLOCKED at the commit only — the control-plane pre-commit hook refused it because the integrator claim lease had expired at 22:06Z (nothing in the change). Integrator renewed the lease (`claim.py renew claude-integrator --hours 24`, f7f572a) and landed the fixer's two files by pathspec: 6eee7fd (11/11; typecheck 0; eslint clean; scope-gate OK). Note: the index carried other agents' staged paths (Task 10's, 10 in all) — the pathspec commit left them staged and uncommitted, as intended. The fixer's concern 3 (pins not run against the pre-fix code, to avoid a stash in the shared tree) — accepted; the reviewer traces it. Scoped re-review 4 over 6eee7fd (review-3332a45..6eee7fd.diff) → a7720bb06aec32d36.
- Task 6 re-review 4 (6eee7fd): RE-REVIEW: closed — the gate covers missing, unreadable and unknowable ahead of every verdict word in every row kind; spot pins byte-for-byte unchanged; new pins fail on the parent by trace. Nits deferred to the final triage: the `flips === null` arm is now unreachable (remove when the file is next opened); `rowHeadline`'s JSDoc says "unreadable figures or an unknowable verdict" (add "missing"); minors 3, 4, 6, 8, 9, 10; the `roomAfter` regex; the "two todays" nit.
- Task 6: complete — a444953 c1fd2c6 477e513 8441749 f338b53 b42074a 3332a45 6eee7fd (four fix rounds; the breaker did not trip).
- Task 10 report: DONE_WITH_CONCERNS, commit 8eef607 (parent 6eee7fd; 8 paths; weld+clock specs 96/96; the three lib specs 21/21 after the DEFINITION_ETH reconciliation; typecheck/lint clean; census 97 → 101, batch-bearing 38 → 40). Fifteen deviations from the brief's code, each argued from the compiler, the schema or the contract (task-10-report.md §Deviations) — provisionally accepted: the contract and the schema are the law over the brief; the reviewer recomputes 3 (null debts on the not-measured cell) and 9 (the weETH realization derived: 838426962 shortfall, 239603961 bad debt at liquidation — the brief's constants were ×1000 and larger than the eligible debt). Five dataset-level concerns (positions sample 207 crossings vs the Book's 118; the legacy engine's two movement counts; ETHFI −50 % with collateral held; dust Aave bad-debt figures; the set's eth reach on the contract example's prices) → the reviewer classifies each (fix here / other task / dataset fact). Serena note recorded: `replace_symbol_body` on an exported `const` re-emitted the `export const` prefix and a `;;` — check the file after any const replacement. Review package review-6eee7fd..8eef607.diff → reviewer a58e0cd578cf033a4 (fable).
- Task 11 (integrator): `npm run build` exit 0 on the tree at 8eef607 + the uncommitted app/lab files (started before the Task 9 fixer so its typecheck could not race a half-edited lab-view.ts). Next: :3111, the look in both themes, `/lab` bare and `?address=`.
- Task 9 fix round 1/5 dispatched → implementer a8c7230083accec24 (BASE the current HEAD; other tasks' commits interleave). Ruling amended: `lab-view.ts` RE-EXPORTS `readEngine`, `EngineReading`, `EngineResult` from `lab-engine` (the page's components read the view module; Task 11's uncommitted imports stay as written) — supersedes "the integrator moves app/lab imports"; `lab-library` imports `lab-engine` directly (no cycle). One seam pin required (a classifier-only fault → the library row shows the workspace's kind).
- Task 11 look (`screenshot-pages.mjs`, demo fixtures routed; scratch lab-look/): the primary state matches the mockup and R16 to the dollar — headline "$1.2M more Cash debt becomes liquidatable, across 118 accounts.", tiles 118 · +$1.2M · +$40K · 941, 7×7 heatmap (49 held over cap; 14/13/73/18 crossing; 135/43/129 worsening; 932 held ≥ 20 %; 6 not measured dashed), caption 425/118/none/6, 20 of 118 movers, legacy folded; both themes read. Bare `/lab`: "ETH -30 percent — 1 committed shock, not run yet.", tiles "— not run", the grid placeholder (R7 ✓). `?address=`: the frozen headline/dek and the eight tiles ✓, three rows incl. the projection's horizons ✓. Three defects found and fixed in the tree: (1) the kit `AddressField` overflowed the 300 px column (`.searchIn { min-width: 320px }`) → `.libAddress .searchIn { min-width: 0; flex: 1 1 100% }`, the hint hidden there (the placeholder "0x…" is the hint); (2) the kicker uppercased the address ("0X7A3F…C21E") → the Inspector's `.kickAddr` rule PROMOTED to the kit (`kit.kickAddr`; inspector.module.css loses its copy; InspectorSurface switched), the lab kicker wraps the address in it; (3) Ruling — the plan's address-mode run button (plan line 4173: "Run against this address", `onRun` a no-op) is a plan defect: a button that does nothing is dishonest; the address mode's action is the field's own Inspect → `ScenarioLibrary.run` is now OPTIONAL (kit; the footer renders only with a run or a compare) and address mode passes none. Task 12's contract: `lab-run` exists in book mode only. Cost if wrong: one kit prop made optional. Two more paths join the commit: web/components/kit/ScenarioLibrary.tsx, web/components/kit/kit.module.css, web/app/inspector/inspector.module.css; the screenshot script gains `labBare` and `labAddress` keys. Deferred (final triage): the address table prints a negative room after as "−$1,069" while the tile says "over cap by $1,069" — check the Inspector's StressTable word and align once. The Task 9 fixer's in-flight `heat: HeatmapView` (my minor-2 ruling) reached my LabTiles/TransitionCard — adapted (no `kind`/`view`/`reasons`); app/ and components/ typecheck clean; the rebuild + re-look wait for the fixer's commit.
- Task 9 fix round 1/5: DONE, commit 6855c01 (parent 8eef607; 5 paths; four specs 30/30; whole unit project 1246; typecheck/eslint clean; `find_referencing_symbols(readEngine)` clean). I1, I2 and minors 1, 2, 4, 6, 7 as ruled; seam pin `movers_total: -1`. Concerns: `cashEngineOf`/`cashRefusalOf` are pin-only exports now (final triage: delete with Task 4's pin or keep); `readEngine`'s `definition` typed from the schema to keep the graph acyclic — accepted. Scoped re-review over 6855c01 (review-8eef607..6855c01.diff) → aae38a7240bf43d70.
- Task 11 (integrator): DONE_WITH_CONCERNS, commit ff78ac6 (parent 6855c01; 17 paths — the plan's 14 plus ScenarioLibrary.tsx, kit.module.css, inspector.module.css; scope-gate OK; build exit 0 with `/lab` static; eslint/stylelint/typecheck clean; unit project 1246 passed after the kit's run slot went optional). Report task-11-report.md (four rulings, three concerns: the address table's "−$1,069" vs the tile's "over cap by $1,069"; the address-mode outcome override lives in the surface; the "Computed" chip placed by index). Review package review-6855c01..ff78ac6.diff → reviewer (fable), with the Task 12 carry-question: which Plan 2 pins on the Inspector's `/lab` hrefs must be re-expressed.
- Task 10 review (task-10-review.md): spec ✅ (the generator re-run into the scratchpad reproduces all three JSON bodies byte-identically; every R16 figure pinned against the Book; all 15 deviations check out against compiler/schema/contract — three fix brief-level errors; deviation 9's arithmetic confirmed: shortfall 838426962, bad debt at liquidation 239603961, the brief's constants ×999.5 and larger than the eligible debt), quality Approved; two Importants, both implementer-level: I1 the generator's provenance sentence is false (the positions pages are the FULL 1,412-row Cash book bucketing to the Book's histogram exactly; a naive ×0.7 re-shock crosses 207 where the Book's table moves 18 from lane 5) → reword, regenerate, prove no figure moves; I2 the Cash `movers_note` states its order backwards (ascending ratio-after: row 1 is furthest past the cap) → reword, regenerate. Minors taken: pin the weETH realization formula against the contract's own example (200000000 / 820000000); pin `DEFINITION_ETH` to `scenarios-demo.json` (closes Task 9's minor 5 for good). The five concerns are dataset facts, recorded: concern 1 was misdiagnosed in the report (not a sampling artifact); 2–4 are plan-mandated arithmetic; 5 is meta.demo.json's own weETH split ($4,000 OP vs $9,999 mainnet). Two more facts: the contract's run-book (3 applied marks) and set (2 of 2) examples disagree on eth's reach, inherited verbatim; the demo Book's Aave waterfall arm (0 eligible at base) contradicts its Aave card (46) — Plan 1 dataset, for the owner's list. Fix round 1/5 → implementer a44e8d32b942a7905.
- Task 9 re-review 1 (6855c01): RE-REVIEW: closed — I1, I2, minors 1/2/4/6/7 verified line by line; the module graph is strictly lab-view → lab-library → lab-engine; every app/lab import still served; the seam pin bites both ways; nothing else moved. Final-triage note: `cashEngineOf`/`cashRefusalOf` are pin-only exports.
- Task 9: complete — 19d7576 6855c01.
- 15:30 — the session rate limit (reset 15:40) cut the Task 10 fixer mid-edit (generate-demo-lab.mjs modified, uncommitted) and the Task 11 reviewer before it read anything. Both resumed at 15:40 with their state named; nothing re-dispatched.
- Task 10 fix round 1/5: DONE, commit 91e68ba (parent ff78ac6; 3 paths; five specs 117/117; unit project 1248; typecheck/eslint clean). I1 and I2 as ruled, both facts asserted in the generator (`weldPages` over both pages; `NAIVE_CROSSINGS = 207`); the realization rule proven against the contract's weETH example on both engines and pinned; `DEFINITION_ETH` deep-equalled to the listing fixture. No-figure-moves proof: listing and set bodies IDENTICAL; the run-book differs at exactly `engines[1].movers_note`. Scoped re-review over 91e68ba (review-ff78ac6..91e68ba.diff) → a58e0cd578cf033a4.
- Task 10 re-review 1 (91e68ba): RE-REVIEW: closed — the generator re-run reproduces all three bodies byte-identically; the run-book differs from ff78ac6 at exactly `$.engines[1].movers_note`; every header clause enforced by a non-tautological assertion (`weldPages` 1412 rows bucketed on the Book's own edges; `NAIVE_CROSSINGS` 207 recomputed, [0,0,14,13,73,107,0,0] by lane vs the table's 18 from lane 1.25–1.50); the realization rule proven against the contract weETH fixture read from disk; the `DEFINITION_ETH` pin covers `ScenarioDefinition`'s required set. First-review minors 3–9 stand as recorded.
- Task 10: complete — 8eef607 91e68ba.
- Task 11 review (task-11-review.md): spec ✅ (every contract id present; additive extras only; deep-link law exact incl. both-present and cold load; the effect dispatches once, never before the listing, StrictMode-safe; a stale `?scenarios=` tick cannot leak — lab-view filters `checked` to listed ids; all four look rulings sound; the hidden hint still reaches AT via `aria-describedby`), quality Approved; four plan-mandated Importants — rulings, mine to fix as the implementer: I1 TransitionCard's refusal words named "the Cash book" for any engine → the card takes `engine` and names it via `engineName`; I2 the "Computed" chip printed "0s ago" for a null age and re-anchored at 0 on re-selection → the chip anchors the WIRE's `run.batch.age_seconds` (the run-book batch carries it) at the clocks the tab settled the result on: `AgeReceipt` gains optional `receivedAtMs`/`receivedAtWallMs` (+ `ReceivedAt`), `useAnchoredAgeSeconds` anchors there when given, `resultReceipt(id, age, receivedAt?)`, `RunRecord.settled` gains `atMonotonicMs` (`withSettled(…, now, monotonicNow)`), `BookWorkspace.receivedAt`; no chip while the age is null, "age unknown" warn while unresolved; I3 a failed listing read as empty → `listing-unavailable` shows its headline's emphasis; I4 the address-mode word ignored the row's own verdict → `rowOutcome(row)` in lab-address (pinned: flips / cannot-say / the engine's reason / not on this address). Cost if wrong: one optional receipt field on a Plan 1 law. Carries: Task 12 — `lab-run` in book mode only; pins to re-express: tests/e2e/inspector.spec.ts:92 (`#stress` → `/lab?address=${DEMO_NEAR_ADDR}`), and three old-Lab specs the brief omits — p0-fixes.spec.ts (8), p1b-fixes.spec.ts (10), r11-fixes.spec.ts (2) — goto `/lab` with old ids; no "Open Scenarios →" href pin exists. Task 13 — the lib's `dispatchNotice` is unused (nothing calls `reloadListing`). Minor: StressTable's link carries the wire's account (may differ from the route addr in case).
- Task 11 fix round 1/5 (integrator): commit d3d56ab (parent 91e68ba; 14 paths; unit project 1249; typecheck/eslint clean). Scoped re-review over d3d56ab (review-91e68ba..d3d56ab.diff) → a01765f139732b89c.
- Task 11 round 1 correction: d3d56ab carried formatting churn — `npx prettier --write` (no repo prettier config, script or CI check exists; the default width reflowed seven lib/spec files other tasks wrote wide). Restored from 91e68ba with only the semantic hunks re-applied: 853236d (7 paths; unit 1249; typecheck/eslint clean). Net 91e68ba..853236d is +114/−43 over 14 files. Law for the rest of the plan: prettier only on files this plan's integrator wrote under app/lab and components/kit; never on lib/ or tests/. Package replaced: review-91e68ba..853236d.diff → a01765f139732b89c.
- Task 11 re-look after round 1 (scratch lab-look3/): "Computed 42s ago" on the book result (the demo batch's wire age); address mode rows "Becomes liquidatable" crit · "Not on this address" · "Stays inside its cap" ok; everything else unchanged.
- Task 12 dispatched → implementer af20928e69e8c5e05 (serena-coder, fable); BASE 853236d; brief task-12-brief.md; report task-12-report.md; carries: `lab-run` book mode only; the state-matrix re-identification ruling; unmeasured 6 > 0; `rowOutcome` words; the listing-failure text; inspector.spec.ts:92 re-expression and the three omitted old-Lab specs (p0-fixes 8, p1b-fixes 10, r11-fixes 2) under the brief's own re-express-or-retire rule; no "Open Scenarios →" pin exists; the keep-list of the new app/lab files; the `DeepLinkDecision` docblock and the "R58 item 6" rewording; the :3111 stale-server rule. Live beside the Task 11 re-review (read-only).
- Task 11 re-review 1 (d3d56ab, 853236d): RE-REVIEW: open — I1–I4 ADDRESSED as ruled (the age law intact: settle clocks captured in the callbacks, no clock in render, the resume tracker not fooled, the p1b-5-M2 pin kept); two residuals, both real: R1 a receipt anchored on past clocks returned the raw wire age until the first 60 s tick; R2 `rowOutcome` judged from `flips` alone, without the gate and the projection-horizon arm `rowHeadline` applies (a liquidatable 30 d horizon read "Stays inside its cap"; an unreadable side with a true flip read "Becomes liquidatable"). Fix round 2/5 (integrator): e695cc1 (parent 853236d; 4 paths; unit 1250; typecheck/eslint clean) — `rowVerdict(row): RowVerdict` (not-applicable · cannot-say{not-a-position, withheld, no-horizon, horizon-unknowable} · liquidatable{within, already} · inside{through}) decides once; `rowHeadline` speaks from it (every Task 6 headline pin byte-identical; `projectionHeadline` folded in) and so does `rowOutcome` ("Becomes liquidatable within 30d" warn; "Stays inside its cap through 90d" ok; "Liquidatable today and after" crit; "Cannot say" for any refused side); the hook reconciles at once through a zero-delay timer when the receipt carries past clocks (cleared in teardown); pins: the widened rowOutcome test (projection within/through, the unreadable-side flip, today-and-after) and the pure past-anchor law in freshness-receipt.spec.ts (42 + 360 = 402). Task 12 carry (told at dispatch): the address-mode word for eth_minus_30 is "Becomes liquidatable"; the DM projection row reads "Stays inside its cap through 90d". Scoped re-review 2 over e695cc1 (review-853236d..e695cc1.diff) → a01765f139732b89c.
- Task 11 re-review 2 (e695cc1): RE-REVIEW: closed — R2: `rowVerdict` decides in `rowHeadline`'s old order; all eleven headline arms byte-identical against 853236d; four widened pins fail on the parent. R1: the zero-delay reconcile is off the render path, torn down with the interval, absent for receipts without clocks (Inspector/Book unchanged); a fresh Lab result schedules it too (wire + ≈0, harmless). Limitation recorded: the hook's render arm is unreachable from the unit project — the R1 fix is verified by reading; the freshness-receipt pin states the pure law but does not bite on 853236d. Task 12 carry restated: address-mode words "Becomes liquidatable" (eth) and "Stays inside its cap through 90d" (DM) — sent to the implementer.
- Task 11: complete — ff78ac6 d3d56ab 853236d e695cc1 (two fix rounds).
- Task 12 report: DONE_WITH_CONCERNS, commits 1f05a9a (the contract: 21 pins; two page fixes; lib comment carry-overs) + 9279806 (retirements: 31 old app/lab files, FrontierLedger.tsx, 17 e2e specs, 16 unit specs, set-run-outcome trimmed to lib-only; the overall ledger's retirements entry). Production build at 9279806: typecheck/lint/lint:css clean; 926 total, 916 passed, 10 skipped (pre-existing), 0 failed (unit 750, e2e 176); the contract 21/21. Rulings on its concerns: (1) `router.replace` never wrote a search-param-only URL on the same static route in the production build → the page writes through `window.history.replaceState` (`useRouter` removed), pinned — accepted pending the reviewer's soundness check (Next ≥ 14.1 syncs the native history API into `useSearchParams`); carry to Task 13's Compare; (2) the stacked grids' implicit `auto` column overran 390 px by 5 px → `minmax(0, 1fr)`, pinned — accepted; (3) API finding: `cmd/api/middleware.go` exposes no `Access-Control-Expose-Headers`, so `Retry-After` is unreadable cross-origin; the 503 dek reads the envelope's `retry_after_seconds` — the pin follows the wire; the API change is OUT of this plan's scope → owner's list at close; (4) brief corrections (no `lab-run` in address mode; round-2 address words; the first-viewport pin holds the library head + first row — the four-row list runs to 984 px at 1440×900, a Task 14 consideration; "never $0" scoped to the Cash tiles/verdict, the legacy fold prints an honest "$0 → $666.66"; the shell smoke's /lab H1 is the listing-unavailable headline) — accepted; (5) scope beyond the brief: r8, r9, r12–r17, p0, r11 retired with per-pin reasons; w3l-slots, book-charts, chart-spec-v4, r10-fixes deleted whole (no non-Lab tests left); five pins added so retired laws keep a page home — the reviewer samples the table; (6) the "R58 item 6" comments were in lab-deep-link.ts (reworded there; `DeepLinkDecision` docblock restored); lab-classify.ts still carries pre-existing p1b-9/p1b-10/p0-8/"Task 1's review" references in code Task 7 moved verbatim → final triage; (7) `.superpowers/sdd/.gitignore` M is the integrator's restore (three re-includes) — committed with the workspace at close. The :3111 server (build at 9279806) is left running for the look. Review package review-e695cc1..9279806.diff (deletions by name, A/M full) → reviewer a66e5c042a64d4bc4 (fable). Task 13 waits for this review: its implementer would share tests/e2e/lab.spec.ts and LabSurface.tsx with any Task 12 fix round.
- Task 12 review (task-12-review.md): spec ✅ (contract coverage reconciled; retirement counts reconcile on every file against `git show e695cc1`; sampled pins' reasons true; `history.replaceState` sound under Next 16.2.12 — app-router dispatches ACTION_RESTORE so `useSearchParams` observes it; `minmax(0, 1fr)` minimal and pinned; concern 3 confirmed at cmd/api/middleware.go:132-161 — the pin follows the wire), quality Needs work (narrow). Rulings: I1 — no page pin for the book-mode `not-covered` state (R3/R12) → implementer adds it with a shaped listing (legacy-only engines) and re-routes the retired "not-covered never looks like withheld" law to it; I3 — drawer pin C18 skips the applied shocks and held-flat inputs R10 mandates while the table claims them → extend C18 from the demo fixture's values; bookkeeping (runbook-bsplit 8 + 18; the `unit live-age` home → freshness-blind-resume / freshness-resume-evidence). I2 — C13 codifies "a failed re-run replaces the result it had", against the Global Constraint "A result is never silently replaced" and the retired r8(2)/r16/r17 law (the held result returns with its batch pin, the failure named beside it): RULING — the constraint wins; this is Task 11's page behaviour, the integrator's: the run record keeps the last ok outcome through a re-run (`held` on the record), a failed re-run renders the held result under a banner naming the failure (R13's banner-on-a-result pattern), C13 re-pinned to that law — executed as Task 11 round 3 AFTER Task 12's fix round lands (both edit tests/e2e/lab.spec.ts). Cost if wrong: one record field and one banner kind. Fix round 1/5 → implementer af20928e69e8c5e05.
- Task 12 fix round 1/5: DONE, commit 18ff863 (lab.spec.ts only; contract 22/22 on the 9279806 build; typecheck/eslint clean) — C22 "not covered" (legacy-only engines, run by deep link; the whole state pinned incl. "nothing says withheld"); C18 extended with every applied shock and held-flat input from DEMO_RUN_BOOK_ETH in the drawer's print format; the table's slips corrected. The overall ledger (progress-ui-overhaul.md) mirrored by the integrator: runbook-bsplit 8 + 18; blind-resume home freshness-blind-resume / freshness-resume-evidence. Scoped re-review over 18ff863 (review-9279806..18ff863.diff) → a66e5c042a64d4bc4.
- Task 11 round 3 (integrator, in progress; the I2 ruling): `HeldResult` on `RunRecord` (`withRunning` holds the last ok result with its settle clocks; `withSettled` keeps it through a failure, releases it on a new ok); `bookOf` renders a failed re-run over a held result as the held `resultBook` with `banner: "rerun-failed"` and `rerunFailure: LabHeadline` (`failureOf` factors the five failure kinds); `outcomeLine` speaks for the held result too (`cashOutcome` factored); `StaleBanner` gains the kind ("Run again failed — {failure} The result below stands for batch {id}."); C13 re-pinned to the held law; unit pins in lab-reading (held through failures, released by a new ok) and lab-view (result state + banner + failure carried; a first-run failure unchanged). Unit 752; typecheck/eslint clean; build + contract spec pending.
- Task 11 round 3/5 (the I2 ruling): commit 9b23b07 (parent 18ff863; 9 paths; unit 752; contract 22/22 on its own production build; typecheck/eslint clean). Scoped re-review over 9b23b07 (review-18ff863..9b23b07.diff) → a01765f139732b89c. Task 13 dispatches once this and the Task 12 re-review close.
- Task 12 re-review 1 (18ff863): RE-REVIEW: closed — C22 discriminates (the shaped listing against a body that still carries a Cash row; not-covered decided from the definition), rendered as withheld it fails six ways; C18's lists are asserted from the fixture's own arrays in the drawer's print format; the table's claims true. Overall ledger mirrored by the integrator: C22 in the numbering with the retired law routed to it; C13 renamed to the held law (9b23b07).
- Task 12: complete — 1f05a9a 9279806 18ff863.
- Task 11 re-review 3 (9b23b07): RE-REVIEW: open — the hold's law complete and honest (held only from a settled ok; through running and any failures; released by a new ok); the held book keeps its own identity/clocks/chips; unit pins and C13 bite on 18ff863. Two residuals: R3 — a held body whose definition changed rendered as `definition-changed` under a "the result below stands…" banner with nothing below while the row said "+$1.2M" (R17's exact shape); R4 — `rerun-failed` overwrote the held result's own stale-input/superseded disclosure. Rulings: R3 — the attempt's own failure is the state; the retained body is DISCLOSED (`banner: "retained-refused"`, `retained: { batchId, skew }`) and the row's word is the failure; the sibling gap (an ok result under a version skew printed the result word) closes with a "Definition changed" library word (`LibraryOutcomeKey` gains `definition-changed`; `outcomeLine(record, definition, configVersion)` now takes the listing's config version so `definitionSkew` can speak in the row); R4 — say both: the banner keeps `data-kind="rerun-failed"`, adds `data-held={condition}` and the held condition's own sentence after the failure's (`heldCondition` on the book). Round 4/5 (integrator): lab-view (`Retained`, `HeldCondition`, the held arm split), lab-library, StaleBanner (sentences factored: `supersededWords`, `staleWords`, `failureWords`), LabSurface, the kit's `LibraryOutcomeKey`; pins: lab-library (held word; retained body → the failure word; ok under a version skew → "Definition changed"), lab-view (retained-refused state/retained/cash null/row word; stale-input hold beside the failure), e2e C23 (both sequences on the C15 fixtures). Unit 755; typecheck/eslint clean; build + contract pending. Deferred to the close: the plan's contract table `data-banner` vocabulary (+ rerun-failed, retained-refused); running over a hold shows the running headline without disclosing the hold (design note).
- Task 11 round 4/5: commit 745627e (parent 9b23b07; 8 paths; unit 755; contract 23/23 on its own production build; typecheck/eslint clean). Scoped re-review 4 over 745627e (review-9b23b07..745627e.diff) → a01765f139732b89c.
- Task 11 re-review 4 (745627e): RE-REVIEW: closed — R3: a retained body is never shown in any register, disclosed with its batch by the banner alone; row and workspace agree in both arms (the same `definitionSkew` predicate before `readEngine`); R4: the plain banners byte-identical; the hold carries `resultBook`'s own precedence (superseded first) and loses nothing relative to a plain result (whether R13 should say both is a plan question, not load-bearing). Pins bite on 9b23b07. Docs minor for the close: the plan's contract table vocabularies — `data-outcome` + `definition-changed`; `data-banner` + `rerun-failed · retained-refused`.
- Task 11: complete — ff78ac6 d3d56ab 853236d e695cc1 9b23b07 745627e (four fix rounds; the breaker did not trip). Design notes for the close: running over a hold shows the running headline without disclosing the hold; a superseded-and-stale hold says superseded only.
- Task 13 dispatched → implementer a7e652b73875defff (serena-coder, fable); BASE 745627e; brief task-13-brief.md; report task-13-report.md; carries: the `DotPlotRow` union; `history.replaceState`; `view.checked`/`view.compare` as the only inputs; address mode has no Compare; engines never summed; `dispatchNotice` used or deleted with its pin; e2e pins appended as C24+; the :3111 stale-server rule; cuttable → BLOCKED rather than a workaround.
- Task 13 report: DONE_WITH_CONCERNS, commit 8249d6a (parent 745627e; 6 paths: CompareCard.tsx new, LabSurface, lab.module.css, lab-deep-link.ts + its spec (`dispatchNotice` retired, zero references), lab.spec.ts C24–C27; tsc/eslint/stylelint clean; unit 755; e2e lab 27/27 on a fresh build). Rulings on its concerns: (1) Compare does not write `?scenarios=` (writing it beside a `?scenario=` would render the conflict notice falsely) — accepted; a Plan 3b candidate, recorded for the close; (2) a MEASURED zero share toned warn per the brief's mapping — RULING: a measured zero is "nothing moved", the ok tone (the library's "No band change" is ok); one line, folded into the fix round after the review; (3) the card shows every result the set returned, not only the ticked ids (the demo mock answers four for two asked) — the wire's answer is the truth; the reviewer weighs whether the card must say "asked N · returned M"; (4) `DotPlot` has no `viewBox` and the kit's `.chart` caps width, so a narrow frame clips — patched at the page (`.plotFrame > svg { max-width: none }`); kit ownership → final triage. Deviations accepted as reasoned (the `DotPlotRow` union by arm; per-engine words; the freshness enum worded; the legacy plot's own frame; C25's fixture keeps the engine in `covered_engines` under the census law; the pins use the helper's counters and a `setDelayMs` mock option). Review package review-745627e..8249d6a.diff → reviewer a07dadf73c21c8619 (fable). Task 14 (integrator) starts in parallel: gate captures at 8249d6a and the companion page; the pixel pins wait for Task 13 to close.
- Task 14 (integrator) prep at 8249d6a: the Scenarios mockup block captured from pages-console.html (scratch gate4/gate4-mockup-scenarios.png; the mockup's 5-band grid vs the build's 7 merged bands is R1, ruled); the four states captured in both themes (lab, labBare, labAddress, labCompare — the screenshot script gains `labCompare: "/lab?scenarios=eth_minus_30,ethfi_minus_50"`); gate-scenarios.html drafted (options approve-scenarios / changes-scenarios); the companion server restarted on the same project dir. Compare look findings for Task 13's fix round: (1) the dot plot's right-hand labels clip ("+4.5% of the Cas…") — RULING: the row value carries the share and the absolute figure only; "of the Cash book" is the caption's word (already there); (2) the two zero-share rows are hollow amber dots — the ok tone per the earlier ruling. The gate push and the pixel pins wait for Task 13 to close (the clip would draw a "changes first").
- Task 13 review (task-13-review.md): spec ✅ (ids with the kit's `data-kind`; four arms honest; `rowsOf` by arm with no path to 0n or a signed refusal; the legacy plot on its own words and scale; freshness words true to the contract; the button law; C25's brief fixture really contradictory under `censusBreak`; `dispatchNotice` zero references), quality Needs work. Rulings: I1 (plan-mandated) — the set MEMBERSHIP law (the response's `requested_scenario_ids` equals the asked ids as a multiset; results cover exactly them — the old R57 gate) was lost in the move and its retirement unledgered → restored in the lib (`setMembership(asked, response)` in lab-compare; `compareOf` passes `set.ids`; a fault → `failed` with a headline naming it, copy in lab-headline); `mockLab`'s set route ANSWERS THE REQUEST (the fixture shaped to the POST's ids) so the happy path is honest; C24 re-pinned to two rows; one mismatch pin; the R57 pins' new home to be ledgered. I2 (implementer) — the legacy plot's `useMeasuredWidth` ref attaches after the hook's only effect → 880 fallback forever → hoist the fold into its own component; width pin. Look findings folded in: the plot's right labels clip → share + absolute only ("of the Cash book" is the caption's); a measured zero share → ok tone. Minors taken: the absolute delta shown beside a dashed refused share; the `none_servable` notice states the contract's meaning. Not taken: present-tense freshness wording (taste); C26's in-flight window (precedent); the `.chart` clip → kit, final triage. Fix round 1/5 → implementer a7e652b73875defff.
- Task 13 fix round 1/5: DONE_WITH_CONCERNS, commit c92f4fd (parent 8249d6a; 10 paths incl. LegacyCompare.tsx new and the kit's DotPlot.tsx; unit 761; e2e lab 28/28 incl. C28 on a fresh build; tsc/eslint clean). I1 `setMembership` + `setMembershipHeadline` run by `compareOf` on the asked ids; `mockLab`'s set route answers the request (`answerSet`/`shapeSet`; `setVerbatim` for the mismatch pin); I2 `LegacyCompare` owns its measured width (pinned ≠ 880); labels "+4.5% · +$1.2M" with the value column sized in `DotPlot` (the kit; minimal, its only consumer) and the frame scrolling past 480; a measured zero ok; the refused row's note carries the absolute delta; the `none_servable` notice states the contract. Concerns accepted: the old gate's fault sentences kept verbatim; the kit edit; the 7px/char reserve (a long refused note widens the chart; the frame scrolls). The R57 retirement line mirrored into progress-ui-overhaul.md. Scoped re-review over c92f4fd (review-8249d6a..c92f4fd.diff) → a07dadf73c21c8619.
- Task 14 (integrator) at c92f4fd: the screenshot script's set route now ANSWERS THE REQUEST (the demo set shaped to the POST's ids — the first Compare capture showed the membership law refusing the verbatim four-result fixture for a two-id ask: the law right, the mock wrong); `screenshots.spec.ts` gains the `lab` entry (`/lab?scenario=eth_minus_30`; ready = state result, heatmap visible, 20 mover rows), the scenarios listing + run-book routes, and the `[data-chip='Computed']` mask; gate captures re-taken; gate-scenarios-v2.html pushed to the companion (session 221119-1789604786, port 50691) with the mockup, the primary state full/fold dark+light, the cold load, one-address mode, and the Compare state ("+4.5% · +$1.2M" / "+<0.1% · +$9,800"). Owner click pending (approve-scenarios / changes-scenarios). Note: a scratchpad sweep removed earlier sessions' scratch .ts drafts (temporary files, nothing in the repo).
- Task 13 re-review 1 (c92f4fd): RE-REVIEW: closed — `setMembership` matches the old R57/R58 gate clause for clause and the contract's multiset law; the mock's shaping honest (only the echo, the results list and `scenarios_evaluated` change); C28 and the C24 re-pin red on 8249d6a; `LegacyCompare` measures at its own mount (pin non-vacuous); the `DotPlot` change minimal. Residuals for the final triage: (1) `setMembershipHeadline` runs `sentence()` on the joined faults — an id-leading fault gets capitalised ("Eth_minus_30"); one line (`terminated()` or a fixed lead); (2) `CHAR_W = 7` estimate sizes the value column — against the kit's own LF-8 law (`useMonoCharWidth`), pre-existing from Task 1; kit item beside the `.chart max-width` clip; (3) "no denominator · +$5" unpinned; (4) result order vs the contract's request order unchecked (immaterial under the ranking).
- Task 13: complete — 8249d6a c92f4fd.
- Task 15 Step 1 (gates at c92f4fd): typecheck / eslint / stylelint / build clean; `npx playwright test` 936 passed, 10 skipped (pre-existing), 0 failed (unit 761; e2e 175) — against Plan 2's close 1,444 / 10 the drop is the old Lab's retired pins (33 specs) net of the contract's 28 and the new unit pins.
- Task 15 Step 2 (widths, dark, production server at c92f4fd): overview · book · inspector · lab × 390 / 1366 / 1440 / 1920 / 2560 — no horizontal scroll anywhere (scrollWidth = clientWidth on every cell); the shell's max-width steps 1280 / 1280 / 1280 / 1520 / 1680 unchanged; at 390 the library stacks above the workspace (true), side by side from 1366 up.
- Task 15 Step 3 (contrast, from tokens.css): light — ink on heatWorse (crit 55 %) 6.85, on heatBetter (ok 45 %) 8.94, on heatHeld (ink-3 30 %) 10.72; ink-2 on panel 5.90; warn-text on panel 5.44; ink-3 on panel 5.12. Dark — 5.66 / 5.37 / 8.90; 6.76; 7.25; 4.53. Every pair the page prints ≥ 4.5:1; no tint ceiling moves; ink-3 stays on captions and band labels over the plain panel.
- Task 15 Step 4: whole-branch package review-final-9aa5f17..c92f4fd.diff (36 commits; deletions by name; fixture JSON excluded; 939 KB — read in ranges); the deferred-minor list extracted to final-triage.md for the reviewer. Codex CLI is 0.154.0 now (the owner upgraded) — the three rounds are unblocked: slim diffs cut for Plan 3 (9aa5f17..c92f4fd), Plan 2 (d989286..9aa5f17), Plan 1 (its workspace's slim diff).
- Task 15 Step 4 dispatched: whole-branch reviewer ac6ba780386b7bf3f (fable; review-final-9aa5f17..c92f4fd.diff + final-triage.md → final-review.md); Codex rounds via codex-reviewer agents — Plan 2 acaea08611ce71ded (plan2-review-slim.diff, 369 KB), Plan 1 a6de2d6e0a90818b3 (the Plan 1 workspace's final-review-slim.diff), Plan 3 (final-review-slim.diff recut to added/modified code, 354 KB). The Plan 3 slim diff first came out at 1.57 MB because it carried the deleted files' bodies — recut with `--diff-filter=AMR`.
- Codex round, Plan 3 (task-mu4tp7yu-ms3jqa; session 01a0ace1-2ee2-7761-b0b1-e0897ae9f989; the first dispatch wedged at `queued` on this host and one replacement ran; read-only): eight findings, adjudicated — (1) P1 lab-address.ts:214 the address workspace names the LOOKUP's batch over stress figures from an independently fetched stress response (a different batch would render beneath "Result for batch N" undisclosed) → REAL: the stress body's batch identity must be kept and a mismatch refused/disclosed — final wave; (2) P1 AddressWorkspace.tsx:43 the table prints `r.flips` ("Yes") beside a refused headline instead of speaking from `rowVerdict` → REAL — final wave; (3) P1 lab-reading.ts:43 `withSettled` releases the hold on a transport-ok before the body is read; a 200 with an unreadable body loses the held result and shows contradictory with no banner → REAL under "never silently replaced": the hold survives an ok settle and `bookOf`/`outcomeLine` use it only when the new body reads unreadable/contradictory (malformed classes; withheld/not-covered are honest answers) — final wave; (4) P1 lab-reading.ts:111 `SetRecord` has no hold — a failed Compare re-run discards the computed comparison → REAL, the same law: `SetRecord.held`, `CompareState.failed` carries the held view, the card keeps the dots under the failure sentence — final wave; (5) P2 CompareCard.tsx:172 a legacy block with only refused rows vanishes → REAL: render the block whenever a legacy row exists — final wave; (6) P2 lab-engine.ts:64 `readEngine` dereferences `before`/`after`/`hf_transitions` before checking the classifier's structural faults → a TypeError on `before: null` → REAL: return unreadable on structural faults first — final wave; (7) P2 lab-headline.ts:89 `newly_eligible_accounts` is a signed NET: net 0 with one entering and one leaving prints "No Cash account becomes liquidatable" → REAL: when the net is ≤ 0 but the lanes' crossings are > 0 the words state the net and the gross ("Net, no more Cash debt is liquidatable: N cross the cap, M leave it") — final wave; (8) P2 "the view model composes the page's copy" → DROPPED: the prompt's law 8 was read inverted; this repo's law puts copy in the libs and view model, never in components.
- Codex round, Plan 2 (task-mu4tmqbm-4fzi63; session 01a0acdf-69b1-7631-a3a5-70ad6c1de356; read-only): nine findings, adjudicated — (1) P1 StressTable.tsx:69 an all-unknowable projection prints "No" → REAL (cannot-say; the judgement into the lib as the Scenarios page's `rowVerdict` does); (2) P1 InspectorDrawer.tsx:26 an open drawer over a refreshed withheld lookup says "No Cash position" → REAL (the drawer speaks the view's state); (3) P2 inspector-view.ts:203 the stress batch envelope discarded → REAL (the same root as Plan 3's finding 1: `StressReading.batchId`, `InspectorView.stressBatchId`, a mismatch disclosed and the sides not compared across batches); (4) P2 address-lookup.ts:131 the Inspector fetches stress on entry → NOT A DEFECT (Plan 2's owner-approved design: the stress table is part of the address reading); the claimed double dispatch outside StrictMode to be traced, not changed; (5) P2 inspector-view.ts:255 the history streak asserted for a lookup batch the history does not reach → REAL; (6) P2 address-stress.ts:123 `horizon_seconds` unguarded (60.5 → "1m", −1 → "<1m") → REAL; (7) P2 inspector-position.ts:162 LTV computed under a rejected `value_decimals` → REAL; (8) P2 ActivityTable.tsx:66 a pagination failure hidden when rows exist → REAL; (9) P2 inspector-view.ts:318 the History sentence's fallback calls a one-batch near-cap streak "above the line" → REAL (its "copy in the view model" half dropped — the inverted law). Plan 2 Codex fix wave dispatched → serena-coder a622f5245e9e38f1f (fable), files under app/inspector + lib/inspector-*/address-* + their specs; the Plan 3 wave follows (it consumes `stressBatchId`).
- Codex round, Plan 1 (task-mu4tmn88-b1lgnf; session 01a0acdf-590a-79d2-b207-708e1e21dde9; read-only; against Plan 1's ORIGINAL slim diff, older than HEAD): 23 findings — the Book/Overview walk's honesty (a positions page under the wrong batch on the first-page race; legacy rows entering the Cash walk; an unfinished/failed walk reading as a quiet negative; a refused positions response as an empty book; the terminal cursor unchecked against the census; row-decoding throws bypassing the failure handler; refused/unknowable accounts permitting "no position is liquidatable"; whole-engine refusals as zeros; missing Cash data as zero populations; refused rows dropped by the row limit (said corrected later); materiality-hidden accounts as an empty book; unguarded money strings and scales; stress populations and histogram counts unguarded; missing stress points and exclusions silent; the Overview's refused preview as marketing copy; the pill omitting an unresolved age). Rulings: #22 (materiality changes headline counts) is a MISREAD — the headline reads at the $100 line by the spec's own display rule with the full aggregate restated below; #23 (copy in the libs) DROPPED — the inverted law; #1–21 → a Plan 1 wave that verifies each against HEAD first (some corrected by Plans 2/3), fixes the real ones with pins, and STOPS any item that would move a pixel pin (the owner's gate). Dispatched → serena-coder (fable), files under app/book, app/overview, lib/cash-*, book-headline, materiality, human-usd, stress-preview, live-pill, positions, refusal-phrasebook, kit BandBars/LivePill/KpiTile — disjoint from the Plan 2 and Plan 3 waves.
- Whole-branch review ac6ba780386b7bf3f (final-review.md): VERDICT Sound — no Critical; the cross-task seams hold (one reader, one verdict, one skew predicate through the hold, `setMembership` before `compareRows`, nothing dispatched cold, the demo weld to the dollar). Five Importants: I1 the address table's verdict column reads `r.flips` (= Codex 2); I2 the table prints a room beside an unknowable verdict and words a negative room "−$1,069" vs the tile's "over cap by $1,069" (fix the lab; the Inspector's word → Plan 4); I3 a negative/zero NET `newly` has no sentence and prints an ASCII hyphen (overlaps Codex 7); I4 envelope arrays and null subtrees read unguarded at render — a throw inside `deriveLabView` (overlaps Codex 6); I5 in address mode the library highlight follows the listing's selection while the workspace's subject falls back to `rows[0]` — two scenarios on one screen. Triage: 11 of 26 deferred lines fix now, 10 carried (Plan 4 / 3b / owner's list), 5 dropped. Plan 3 close-out wave brief written (plan3-wave-brief.md: A1–A5 from the review, B3–B5 from Codex, C1–C7 the fix-now nits; the stress-batch item and the two plan-table lines stay with the integrator) → serena-coder aea24d2304912d00c (fable). Plan 1 wave: ae8a0e458aab8a28e. The two plan-table lines done (busy struck; the Compare data-kind vocabularies). Plan-doc lines for the integrator: strike `busy` from the book `data-state` set; add the Compare `data-kind` vocabulary.
- Plan 2 Codex wave: DONE_WITH_CONCERNS, commit 14a5927 (parent c92f4fd; 13 paths; tsc/eslint clean; unit 771; inspector.spec 19/19 incl. 4 new pins; state-matrix 29/29 on its build; no lab/kit/CSS touched). Rulings on its concerns: the shared moving tree → the close's suite run on the final HEAD is the confirmation; the Inspector's stress table now words a both-sides-liquidatable spot row "Already liquidatable" and a holding projection "Not within 90d" (the lab's verdict words) — accepted, honest; a stress `batch.id` failing the population guard reads as absent and is disclosed — accepted; `useKeyedFetch` traced: no double dispatch outside StrictMode; a resume repair refreshing the lookup without bumping the stress epoch (the stress may trail by a batch — now disclosed) → Plan 4 design note; the Inspector's `stressVerdict` lacks the lab's negative-figure gate → Plan 4 (the two pages should share one verdict). `InspectorView.stressBatchId` landed for the integrator's lab-address item. Scoped re-review over 14a5927 (review-c92f4fd..14a5927.diff) → reviewer (fable).
- Owner gate (18:30–18:33): the gate page rendered without images — the companion serves `/files/<name>` from the content directory itself, not a `files/` subfolder (Plan 2's PNGs sat beside its html); images moved, page reloads correctly. The owner's first reading: the Compare card is hard to follow. RULING (D1, folded into the live Plan 3 wave, its files): a finding sentence leads the card from the lib (`compareHeadline`: the largest share by magnitude first, absolute beside share, refused rows named); the caption becomes one plain line (the freshness word only when the batch is not the newest); a header row for the value column ("share · change"), "<0.1%" for a sub-tenths share, sans row labels; a stem from zero to each dot (lollipop) and an axis caption "share of the Cash book"; refused rows dashed without stems. Pins unit + e2e; the pixel pins do not move (Compare is not in the pinned primary state). The gate re-opens with a new Compare capture after the wave lands; the owner holds the click.
- Plan 2 Codex wave re-review (plan2-codex-wave-review.md): RE-REVIEW: closed — all eight addressed as ruled, minimal, pinned to fail on c92f4fd; the extensions honest; scope 13 files; the 1440×900 Inspector baselines cannot have moved (the demo's batches all 18,251; the changed section far below the fold). Four follow-ups → Plan 4: an empty-horizons projection collapses to a spot "No" on both pages; the negative-figure gate differs from the lab; two near-duplicate verdict judges (the Inspector's `stressVerdict`, the lab's `rowVerdict` — one home); a resume moves the lookup but not the stress.
- Plan 2 Codex round: closed — 14a5927.
- Plan 1 Codex wave: DONE, commit 52abcd9 (parent 14a5927; 29 paths; scope-gate OK). Verdicts: already-fixed 3 (#1, #8, #11 in 3a2768b), real-fixed 18, rulings 2 (#22 by design; #23 dropped), none stopped. Landed: `readCashPage` judges every positions page (refused page → the engine's refusal; foreign engine/row, null or contradicted census, another scale, missing health_factor → a named `PositionsFailure`); the walk completes only when delivered rows equal the census; decoding in try/catch; `bookHeadline(computed, complete, stopped)` — pending while walking, "could not be fully read" when stopped, negatives only over a complete walk, "No computed position is liquidatable" beside refused accounts; unknowable → not computed; `readWireMoney`/`readWireScale` at every ingress; `deriveLegacyView` withholds a refused engine's populations/bars; `stressPreview` guards populations/scales and carries `unmeasured`; `humanUsd`/`materialityTier` refuse non-wire scales (THROW — consumers guard first, to be verified); BandBars guards counts; LivePill "age unknown"; hidden sub-$100 positions named. Gates on a temp copy of HEAD + the 29 files (the shared tree was mid-edit by the Plan 3 wave): unit 678 (non-lab), e2e book/overview/state-matrix 55, pixel pins book/overview ×2 themes 4 passed. Concerns: cash-view.ts edited beyond the list (the shared view model) — accepted pending review; the "pending" headline variant without CSS; #21 unit-only (a blind resume cannot be staged from a route mock). Scoped re-review over 52abcd9 (review-14a5927..52abcd9.diff) → reviewer (fable).
- Plan 3 close-out wave: DONE, commit 626d01e (parent 52abcd9; 25 paths; tsc/eslint/stylelint clean; unit 812; lab e2e 37/37 incl. C29–C37 and C24–C26 re-pinned for D1). Concerns → rulings: pins not run red on the parent (its build was broken by the sibling wave's in-flight edit) — by trace, the reviewer judges; consecutive malformed 200s hold the first malformed body → Plan 4 ruling (the hold's definition in lab-reading); `set.batch`/`evaluation.freshness`/`run.batch` read unguarded as objects → Plan 4 (a whole-envelope classifier); charts.module.css gained four classes for D1 — accepted; deviations (A1's projection "Yes · within 90d" warn; B4's held view carrying both engines and its line composed in the card; A3 stating net and crossings only; UNREADABLE_SCALE moved; EngineRefusal export removed; D1's all-refused arm dashed rows only) — to the reviewer. Scoped re-review over 626d01e (review-52abcd9..626d01e.diff) → reviewer a8112c9750b6d4d9a (fable).
- Integrator (Codex Plan 3 finding 1): 0991261 — `addressWorkspace` reads `view.stressBatchId`; a stress result for another batch than the position refuses the comparison (headline "Cannot say — the stress result is for batch B; the position above is batch A.", tiles null, rows kept), the surface adds a warn chip "Stress for batch B" and the section qualifier names both batches; unit pin (a mismatch and the same-batch control); unit project green.
- Integrator: the batch-mismatch arm first fired ahead of the lookup's own answers (two Task 6 pins red: a withheld book and a missing position read as a batch mismatch) → moved after the withheld / no-position / unreadable-scale arms: 372e198; unit 813.
- Plan 1 Codex wave re-review (plan1-codex-wave-review.md): RE-REVIEW: closed — all 18 addressed as ruled and pinned red on 14a5927; #1/#8/#11 hold on HEAD; materiality untouched; throw-safety holds (every humanUsd/materialityTier consumer behind a scale gate, lab consumers included); the census cross-check cannot refuse a valid book (the same aggregate the API serializes; batch id and 409 precede it); no pixel baseline touched. Residuals → Plan 4: readCashPage judges `refused` before `page.engine` (a foreign refused page folds into the Cash refusal — fails safe); the over-delivery wording; the distance chart's mid-walk qualifier; an empty waterfall.points reading as absent.
- Plan 1 Codex round: closed — 52abcd9.
- Final tree 372e198: unit 813; e2e 200 passed / 10 skipped on the second run (the first run had one transient failure the reporter did not attribute — rerun green; if it recurs, Plan 4 names it); the full suite 1,013 + 10 skipped. Gate re-captured from the final build (gate5-*; the Compare card with its finding sentence, the header row, the stems); gate-scenarios-v3.html pushed; the owner's click pending.
- 19:03–20:41 — the session limit cut the Plan 3 wave re-reviewer mid-read (at lab-classify); resumed at 20:41 with its state named. The gate (v3) has no click yet; the companion server is alive (session 221119-1789604786).
- Plan 3 close-out wave re-review (plan3-wave-review.md): RE-REVIEW: closed — A1–A5, B3–B5, C1–C7, D1 addressed with trace and red on 52abcd9 by trace; the primary pinned state unchanged by trace. Adjudicated: consecutive malformed 200s hold the first malformed body → Plan 4 ruling (the banner's "The result below stands" clause untrue in that sequence); `set.batch`/`run.batch` read unguarded — a render-time throw on a 2xx missing `batch`, pre-existing → Plan 4 whole-envelope classifier (`evaluation` now gated by `setMembership`). Carries → Plan 4: the newly tile's tone for a net ≤ 0 (ok under a warn headline; one line — the tile follows the headline's tone); the "Compare again failed" line is a second component copy site (the StaleBanner precedent's twin) — fold both into one lib sentence (M16).
- Plan 3 Codex round + close-out wave: closed — 626d01e 0991261 372e198. Every review of the plan is closed. Open: the owner's gate click (pins commit), then Step 5 (the close entry, the workspace commit).
- Owner approved in the terminal at 21:50 (2026-09-16). Pins committed 867dedc; the resolved screen pushed to the companion. Close entry appended to progress-ui-overhaul.md; workspace committed. Plan 3 CLOSES.
