# SDD ledger — plan: docs/plans/2026-09-16-ui-convergence.md

Spec: docs/specs/2026-09-15-ui-product-register-design.md (§5.5, §4, §6–§8, §10.4–5, §11–12). Plan committed as HEAD after a941a39. Execution: subagent-driven (the owner's standing choice), all subagents `model: fable`; the owner delegated this plan's gates at 21:56 on 2026-09-16 ("go with your best judgement") — every gate decision the integrator takes is recorded here with its time.

## Pre-flight scan (2026-09-16 21:58)

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 → T3–T6 | kit exports `Drawer`; the pages import it | agree; until T1 lands a page may import `@/components/Drawer` directly (same component) |
| T1 ↔ T9 | T1 removes the lab's `.plotFrame > svg` patch; T9 does not touch lab.module.css | disjoint |
| T2 → T3, T4 | `DEMO_OBSERVATORY_DM/AAVE`, `DEMO_FEED_PAGE_1` consumed by the unit pins and contracts | T3/T4 wait for T2 |
| T5 ↔ T8 | T5 refactors `app/overview/Pipeline.tsx`; T8 edits `OverviewSurface.tsx`'s href | disjoint files; the Overview's pins are T5's gate too |
| T3–T6 → T7 | the deletions need zero consumers | T7 after all four land |
| T9 ↔ T10 | both named `app/inspector/[addr]/StressTable.tsx` | RULING: T10's negative-room word folds into T9(e) (the judge lives there); T10 no longer touches StressTable |
| T3–T6 → T11 | the contracts' ids are the pins' ready predicates | T11 after T7 |
| Each task's own text | ids in the contract table vs the components each task emits | agree; the words of the deks fixed in R3 |
| Concurrency (shared tree) | four page tasks in parallel would each kill :3111 and build | RULING: during the parallel phase implementers run unit specs, tsc, eslint and lint:css ONLY; the integrator builds once per landing and runs the task's e2e contract, returning failures as a fix round; reviewers are read-only |

Clean otherwise. Execution order: T1 ∥ T2 ∥ T5 ∥ T6 → T3 ∥ T4 (after T2) → T7 → T8 → T9 → T10 → T11 → T12.

## Execution

- Plan committed 1d546f0. BASE for the first wave: 1d546f0. Dispatched in parallel (all serena-coder, fable; unit gates only — the integrator runs e2e on a shared build): Task 1 ac0b1daedabf76f58 (brief task-1-brief.md; note: no `useMonoCharWidth` exists — the implementer locates or creates the mono-glyph hook under the LF-8 law); Task 2 ae966d216af6e2779 (task-2-brief.md; template generate-demo-lab.mjs; the weld to DEMO_BOOK's engine aggregates); Task 5 a77f2822d31d1ec83 (task-5-brief.md; `pipelineSteps` must reproduce the Overview's pinned strings; `/proof#architecture`); Task 6 afcd5f8e7ae92131a (task-6-brief.md; the page stays a server component with a client drawer island).
- Task 1 report: DONE_WITH_CONCERNS, commit d7c6a4c (parent 1d546f0; 6 paths; dot-plot-geometry 5/5; unit 818 with the sibling's half-written api-view.spec excluded; tsc/eslint/stylelint clean). Rulings: `dotPlotColumns` lives in lib/lab-geometry.ts (the unit runner cannot load a .tsx importing a CSS module) — accepted; the pins state the file's actual law (two pads, floors) — accepted, the plan's arithmetic was a sketch; the hook already existed (`useMonoCharWidth` in lib/useMeasuredWidth.ts, fallback 7.4) — accepted; `.chart1to1` carries `min-width: 0` and `> svg { max-width: none }` (the lab patch relocated to the kit) — accepted; `"use client"` on DotPlot — accepted. The lab e2e frame pins (`frameOf` now the kit wrapper) → verified by the integrator's shared build after the wave lands. Review package review-1d546f0..d7c6a4c.diff → reviewer (fable).
- Task 6 report: DONE, commits ac5eb15 (lib + 5 pins) + cb22370 (the page, api.spec.ts 12 pins unrun, developers.module.css and developers.spec.ts deleted, r1-fixes (10)'s API arm re-homed, shell.spec/state-matrix one-line re-points); unit 823; tsc/eslint/stylelint clean. Rulings: the two out-of-brief one-line edits (shell.spec h1 regex; state-matrix ids) — accepted, they pin this page; r1-fixes (10)'s title reconciled by the integrator after the wave; `Drawer` from `@/components/Drawer` (Task 1's export landed later) — accepted, Task 7 may switch imports to the kit; extra `api-*` sub-ids additive — accepted; gate register items (the 17-button TOC at ~3 rows; the Endpoints head link's nowrap at 390) → Task 11/12. Review package review-d7c6a4c..cb22370.diff → reviewer (fable, dispatched 22:23).
- 22:26 → 02:50 — the session limit cut all four live agents (Task 2 with its generator and specs written but unrun; Task 5 at the start with only its unit spec written; the Task 1 and Task 6 reviewers before reading). Resumed at 02:51 with their states named; nothing re-dispatched. The companion server exited on its idle timer meanwhile — restart at Task 11.
- Task 1 review (task-1-review.md): spec ✅ (12/12; the four accepted deviations honoured), quality Approved; no Critical/Important. Eight minors → final triage: the sans label at the mono budget is safe over-budgeting; the pre-measurement "never clips" claim holds via the 8px pad, not the 7.4 fallback; a discriminating input for ceil vs round (7.48); the legacy 1024 width pin's slack shrank (12–33px; could flip under other mono metrics — watch the shared build); no pin proves DotPlot feeds chPx into dotPlotColumns; the `.chProbe` comment contradicts itself; nested scrollers `.plotFrame`/`.chart1to1`; the probe positioned inside an un-positioned scroller. The lab e2e Compare pins are the integrator's shared-build check.
- Task 1: complete — d7c6a4c.
- Task 2 report: DONE, commit ee88513 (parent cb22370; 7 paths; weld 16 + clock law = 108; unit 842 with the sibling's half-written verification-view.spec excluded; tsc clean outside it; the generator byte-identical on re-run; census 101 → 267, batch-bearing 40). Rulings on its concerns: Aave feed accounts sha256-derived (no committed body asserts a cross-chain identity) — accepted; Aave amounts render raw under the Feed's own unit law (R1 item 4) — accepted, the pin follows the law; the census jump honest — accepted; two pre-existing fixture inconsistencies noted for the owner's list (events-demo-near.json's DM supply raw_type/collateral toggle vs the store's map; feed-liquidations.json's DM configured_bonus_bps "500" where the schema says null); the hole offsets (40, 41 absent; 90 withheld) are generator constants. Review package review-cb22370..ee88513.diff → reviewer (fable). Tasks 3 and 4 dispatched in parallel (BASE ee88513): Task 3 a741371b1b5bc0063, Task 4 af10b049e5567f936; Task 2 reviewer aaaaeb473947b59a8.
- Task 6 review (task-6-review.md): spec PASS, Approve pending the integrator's e2e run; one Important — the Endpoints head's link label is nowrap and exceeds the 390px shell (the width contract) → fix round 1/5 → implementer afcd5f8e7ae92131a: shorten the label to "Verification →" (the kit's `.sec a` rule untouched); minor taken: `Drawer` from the kit. Minors → triage: copy composed in ApiSurface (section titles/qualifier — brief-sanctioned); CodeBlock already a client component; the `[data-chip] b` coupling; `SectionHead` lacks a text separator (kit); the "H1 = nav label" tension (plan-wide — the nav label is the kicker's first word, the headline the sentence; the H1 element is the headline per the kit's VerdictHeader — recorded).
- Task 6 fix round 1/5: DONE, commit 9b029bf (two lines; tsc/eslint clean; unit 858). The review's Important addressed by trace (the label shortens under the nowrap rule); the scoped re-review folds into the integrator's shared-build e2e run of api.spec.ts (a two-line diff needs no separate reviewer) — Task 6 completes when that run is green.
- Task 5 report: DONE, commits 2abc3ef (lib + 16 pins; `Pipeline.tsx` renders `pipelineSteps` with its ids and data-values reproduced; `PUBLIC_ENDPOINTS` moved from app/overview/copy.ts to the lib) + 67affb5 (the page; verification.spec.ts 16 pins unrun; ProofSurface/proof.module.css/EvidenceDrawer/evidence.module.css/proof.spec.ts deleted; r1-fixes (6)/(10) Proof arms, shell.spec's /proof row and state-matrix's five proof cells re-pointed); unit 858; tsc/eslint/stylelint clean. Rulings: the out-of-brief one-liners and the `PUBLIC_ENDPOINTS` move (the lib may not import from app) — accepted; interpretations accepted pending review (Compute's cadence = `computed_at`; the refusal grammar for unavailable/loading; `pending` on the architecture section; subject captions as drawer doctrine; manifest notes as table rows; PROOF · EXACT as a StatusPill); the state-matrix proof cells assert only the subjects (the tiles read "unavailable" without /v1/meta,/book mocks) — accepted. Notes: Task 7 — Ribbon now styleguide-only, Stampline still used by Inspector/lab/Observatory/styleguide (Task 3 removes Observatory's), primitives.module.css keeps RouteRefusal; DEBT (Task 10) — Verification's `useCashBook` walks the Cash positions though the page reads only the book's engine census → read the census without the walk. Fragile e2e spots for the shared run: the Overview deep-link's `#architecture` top ≤ 120px after client navigation; `toBeFocused()` after Escape; CSS-module class regexes; the Overview pixel pins if the pipeline strip sits inside the 900px capture. Review package review-9b029bf..67affb5.diff → reviewer (fable).
- 03:09 → 07:50 — the session limit cut the four live agents (Task 3 about to write its lib; Task 4 about to run its spec red; the Task 2 reviewer about to write; the Task 5 reviewer before reading). Resumed at 07:50 with their states named; nothing re-dispatched.
- Task 2 review (task-2-review.md): spec ✅, Approve; no Critical/Important. Byte-identity of the generator proven (sha256 of a scratchpad re-run over the ee88513 blobs); the withheld point field-for-field the contract example's; the weld against DEMO_BOOK's own fields; the feed's (engine,type)→(raw_type,unit) combos match p5_events.go; census 101→267, batch-bearing 40. Minors → triage/owner: M1 the header overstates the two demo surfaces' agreement (batches 18158–18194 bucket differently in history-demo-near.json); M2 four layout constants unnamed in the provenance header; M3 the two DM liquidations land on sub-dollar positions (the demo Book's liquidatable set is 47/49 sub-dollar — generate-demo.mjs, owner's list); M4 the contract witness observatory-series-aave.json states ray-1e27 with a 1e18-scale value (owner's list). Both "noted, not mine" fixture inconsistencies confirmed real (owner's list).
- Task 2: complete — ee88513.
- Task 5 review (task-5-review.md): spec ✅ (the five Overview pins trace unchanged; 15/16 e2e pins plausible; the retirement ledger complete), Approve with fixes — I1 the Overview deep-link pin (#9, one-shot top ≤ 120px) likely red (Next scrolls to the hash on the loading tree) → the pin made robust (`toHaveURL` + `toBeInViewport`) and the page scrolls to the hash on mount if hydration lost it; I2 the Compute/Verify refused sentences word a fetch failure as a wire-stated absence → `pipelineSteps` distinguishes unavailable ("could not be read") from the wire's own no-batch/no-receipt arms; I3 sentences composed in the subject cards and section chrome → the lib. Fix round 1/5 → implementer a77f2822d31d1ec83. Ten minors → triage.
- Task 4 report: DONE, commits f300b04 (lib + 13 pins) + e02d39f (the page; activity.spec.ts 14 pins unrun; FeedSurface/FeedList/feed.module.css/feed.spec.ts deleted; the Activity arms of p1a/p1b/r1, the shell H1 and the state-matrix feed cells re-pointed — staged hunk-by-hunk beside Task 3's live History hunks); unit 884; tsc/eslint/stylelint clean. Rulings: interface additions (ActivityRow unitTitle/txLabel/txTitle/detail; ActivityView drift; toned tiles that dash when nothing is loaded) — accepted; the fifth doctrine paragraph — accepted; the liquidation extract behind the crit pill's hover (an unestablished bonus now hidden) → RULING: refusals stay visible — the detail renders as a visible second line in the row (the reviewer confirms the exact place); fix round after review. Register items for Task 11/12: the Engine column prints "Aave v3 market (legacy)" per row; the eight type buttons + since control wrap to ~2 rows at 1280. r1-fixes (10)'s title reconciled by the integrator after Task 3. Review package review-67affb5..e02d39f.diff → reviewer (fable, dispatched 08:07).
- Shared build at b5f78eb (Tasks 1, 2, 3, 4, 5, 6 landed; Task 5's fix round pending): the whole e2e project — 197 passed, 10 skipped, 2 failed: activity.spec:86 (a strict-mode ambiguity — the account and tx links both read 0x…; sent to the Task 4 implementer as its e2e round) and verification.spec:214 (the Overview deep-link pin the Task 5 fix round is already re-expressing). Green: history.spec 17/17, api.spec 12/12, the lab's Compare width pins (Task 1's frame move), the shared specs (p1a/p1b/r1/shell/state-matrix), the four existing pixel pins (the Overview's Pipeline refactor moved nothing).
- Task 3 report: DONE, commits 339fa17 (lib + 13 pins) + b5f78eb (the page; history.spec.ts 17 pins — green on the shared build; the old three components + observatory.module.css + observatory.spec.ts deleted; History arms of p1b/r1/shell/state-matrix re-pointed); unit 884; tsc/eslint/stylelint clean. Rulings: the finding = `gridReadingLine` (takes no metric; the metric selector redraws the chart) — accepted; a fifth chip Served (the p1b fix-3 law) — accepted; `chartLabel` on the view — accepted; the scale guard as a scoped refusal arm ahead of `observatoryTakeaway`'s throw — accepted; refusal sentences say "the history of {engine}" — accepted; the legend moved into the drawer (no on-chart key) — a Task 11 gate item; two extra component files — accepted; the lowercase H1 (the takeaway verbatim) — a Task 11 gate item (R2 says the sentence is the page's own; a leading lowercase word may read as a defect — decide at the gate). r1-fixes (10) now holds four retirement notes and no assertion → the integrator deletes it in the reconciliation. Review package review-e02d39f..b5f78eb.diff → reviewer (fable, dispatched 08:09).
- Task 4 e2e round: commit 4afe8f1 (`activity-account` / `activity-tx` test ids on the row's links; the cold-load pin targets them and also pins the tx's explorer URL; tsc/eslint clean). Verification on the next shared build (Task 5's fix round is mid-edit in lib/verification-view.ts — one build after it lands covers both).
- 2026-09-17 08:12 → 2026-09-20 15:44 — the session limit cut the Task 5 fixer (at its gate set, all edits in the tree uncommitted: lib/verification-view.ts, app/proof/*, app/overview/{Pipeline,OverviewSurface}.tsx, the two specs), the Task 3 reviewer (mid-read) and the Task 4 reviewer (at the diff); the Claude Code process then ended and the :3111 and companion servers with it. Resumed 09-20 15:45: integrator lease renewed (it had expired 09-17T22:12Z); the three agents resumed from their transcripts with their states named.
- Task 5 fix round 1/5: DONE, commit a93bf7a (parent 20b41db; 9 paths; verification-view 23/23; unit 891; tsc/eslint/stylelint clean). I2 — `pipelineSteps(meta, evidence, reading: BookReading, cashAccounts)`: unread → "—"/"unavailable"/"could not be read"; wire-stated absence → "no servable batch"/"no committed receipt" and the absence sentences; `PipelineStep.line {before, figure, after}` and `ordinal` so the Overview's line is composed in the lib with its data-values unchanged. I3 — `subjectCards(manifest)` and `VERIFICATION_COPY` carry every sentence; the report lists each moved string. I1 — the page scrolls to `location.hash` once the evidence phase leaves loading; the pin is `toHaveURL` → `data-state="ok"` → `toBeInViewport()`. Minors M1–M3 taken; M4–M10 in the report. Pipeline's prop is now `reading` (Task 8's OverviewSurface edit must keep it). Verification on the shared build (rebuilding now).
- Shared build at a93bf7a: the whole e2e project 200 passed, 10 skipped, 0 failed — verification.spec 17/17 (+ overview.spec unchanged), activity.spec 14/14 after 4afe8f1, history 17/17, api 12/12, the shared specs, the four pixel pins. Task 6: complete — ac5eb15 cb22370 9b029bf (the e2e run was its pending condition). Task 5 scoped re-review over a93bf7a (review-20b41db..a93bf7a.diff) → a088b3e5b5173e1dc. Task 7 dispatched (BASE a93bf7a; the r1-fixes (10) deletion and its title reconciliation folded in).
- Task 3 review (task-3-review.md): Compliant, Approve; I1 the point record's sentences and column headers composed in HistoryPoint.tsx (carried verbatim from the deleted component, pinned, but outside web/lib) → fix round 1/5 → implementer a741371b1b5bc0063 with the two gate recommendations TAKEN: G1 a compact two-glyph mark key inside the ChartCard (absent · withheld; words from the lib; doctrine stays in the drawer); G2 the headline capitalised inside `observatoryTakeaway` itself (module + its pins) so R2's equals-law holds. I2 r1-fixes (10) assertion-less → Task 7 deletes it. Could not verify: visual widths/themes (Task 11/12), gap-hit click stability at 168 buckets beyond one green run.
- Task 7 dispatched → serena-coder a974930f83520bb60 (fable); BASE a93bf7a; brief task-7-brief.md; carries: the consumers the reviews found (RefusedTag/EngineChip zero; Stampline/Ribbon styleguide-only), primitives.module.css stays, the r1-fixes (10) deletion + title reconciliation, unit gates only.
- Task 4 review (task-4-review.md): spec ✅ (the state machine byte-identical in law — 126 comment-stripped lines equal; every row fact in the lib with the guards at the read; the retirement ledger complete; 13/14 pins plausible, the 14th fixed by 4afe8f1), Approve with changes: I1 the liquidation extract hover-only → visible (the controller's ruling; lib parts so the liquidator regains its Inspector link; two pins re-expressing the retired W-3L 430 law); I3 three notices composed in the component + "end of the filtered feed" twice → the lib. Fix round 1/5 → implementer af10b049e5567f936.
- Task 5 re-review 1 (a93bf7a): OPEN on one item — e2e #9 does not bite (the section is already in the 1280×720 viewport unscrolled) → round 1b: `expect.poll(top ≤ 120)` after `data-state="ok"` (two spec lines) → a77f2822d31d1ec83. Closed: I2 complete and honest for all four steps, the Overview line byte-identical in text and data-value; I3 every sentence in the lib (the two declared exceptions stand); M1/M2 pinned. Register note for Task 11: a pending tile shows "…" over "unavailable" while loading. Task 5 completes when 1b lands (the pin's redness is by the reviewer's geometry; the controller's next shared run confirms green).
- Task 5 round 1b: DONE, commit 6522c8e (one spec file; `expect.poll(top ≤ 120)`; red on 67affb5 by geometry, green on a93bf7a). Task 5: complete — 2abc3ef 67affb5 a93bf7a 6522c8e (the spec's green on the next shared run is the confirmation).
- Task 4 fix round 1/5: DONE, commit 091beb7 (parent 6522c8e; 6 paths; activity-view 14/14; unit 892; tsc/eslint/stylelint clean). I1 `ActivityLiquidation` parts (liquidator + Inspector href, repaid, seized, bonus realized/configured via renderBps — an unestablished figure the em dash, a null configured bonus a dash, never "0") rendered visibly beneath the pill (`activity-liquidation`, `activity-liquidator`); the two retired W-3L 430 pins re-expressed. I3 `sinceBlockDroppedNotice`, `notABlockNumberNotice`, `END_OF_FEED` from the lib. Register items for Task 11/12: liquidation rows are taller (the extract wraps 2–3 lines at 44ch). Scoped re-review over 091beb7 (review-6522c8e..091beb7.diff) → a8c7d5b80b640110d.
- 2026-09-20 15:58 → 16:33 — the session limit cut the Task 3 fixer (edits in the tree, uncommitted), Task 7 (before any edit) and the Task 4 re-reviewer (before reading); the owner logged in again; all three resumed at 16:34 with their states named.
- Task 3 fix round 1/5: DONE, commit ac3bbde (parent 091beb7; 9 paths; history-view + observatory-series 41; unit 899; tsc/eslint/stylelint clean). I1 `pointRecord(entry, response)` in the lib with the hazard law moved beside its words (every population through readWirePopulation); HistoryPoint prints only. G1 `HISTORY_MARKS` + `history-marks` key drawing the chart's own two glyphs beside the finding. G2 a sentence-initial capital at the source in all three arms of `observatoryTakeaway`, its pins updated, no view-level transform. Notes: a withheld record legitimately prints 0 for "refused position rows" (the wire's own population) — the pin asserts the wire's value and no "$0"; the mark key wraps beneath the finding when narrow (390 pass, Task 12); review M2 left at the site, M1 (one kit rule for a pressed ghost button) → Task 7/triage. The implementer saw a system-reminder-styled block inside a tool result announcing an account change and did not act on it — correct (it was the harness re-reading context after the owner's /login; nothing in the round used it). Scoped re-review over ac3bbde (review-091beb7..ac3bbde.diff) → af532aeb4f1b917b7.
- Task 4 re-review 1 (091beb7): RE-REVIEW: closed — the extract's amounts through `renderNullableDecimal`/the client's exact `formatUnits` (throws before render; null → em dash), both bonus figures through `renderBps`; DM rows `— / —` with the asset clause omitted, Aave `— / 500 bps`, a null never "0"; visible in both views; notices byte-identical; the new pins absent at 6522c8e by grep; the state machine still differs from the old FeedSurface only in the function name and the two `setNotice` expressions. Minors → Task 12's fix wave: M11 the wire's `note` is the extract's one remaining hover-only field (on DM rows it is the only sentence explaining `— / —`) — RULING: print it visibly (same law as I1); M12 an extract amount with `debt_decimals` null prints untagged (carried from FeedList; tag it `raw units` like the Amount cell); nit — `.detail` sets its own colours, so it does not dim inside an untimed-tail row.
- Task 4: complete — f300b04 e02d39f 4afe8f1 091beb7 (activity.spec's 15 pins confirm on the next shared build).
- Task 7 report: DONE_WITH_CONCERNS, commit 50c62a3 (parent ac3bbde; 23 paths; tsc/eslint/stylelint clean; unit 899; e2e unrun). Deleted after `find_referencing_symbols` on all 38 exports: StatCard, Stampline, Ribbon, EngineChip, RefusedTag, AddressMono, VerdictBanner, DataTable, MarksStamp, ProjectionBadge, SeverityHF, verdict.module.css, table.module.css. KEPT: StatusChip + chip.module.css (four `states/*` cards compose it — the pre-flight's "styleguide-only" was wrong), ribbon.module.css (DegradationBanner imports it), primitives.module.css (as ruled). Styleguide: section order and the contrast block untouched; kit specimens (VerdictHeader ×4 tones + the empty-chips "Identity missing" law, KitTable with a dim refused row and SmallToggle, KitTable on useCursorPages, the kit Drawer, IdentityChips, six KpiTiles incl. refused-dashed and pending, StatusPill vocabulary); sg-chips/sg-marks/sg-stampline retired. p1a-6 contrast pin untouched; the five-banner pin re-expressed; its ExactValue-in-banner arm retired (VerdictHeader's emphasis is a string); two new pins; r1-fixes (10) deleted with a note on each intro's new home. Rulings: the commit message corrected to say StatusChip stays — accepted; comment-only edits in RouteRefusal/StatusChip and the drawer button label "Methodology & evidence" — accepted; `states/*` have no page consumer (only sg-states) — they STAY per spec §4 ("kept, restyled to the kit"); noted for the owner's list with `EngineTag` (now consumer-less); dead rules in ribbon.module.css (~190 lines of appbar) and primitives.module.css → pruned in Task 12's fix wave; lib comments naming deleted components → reworded in Task 12's wave; `verdictBannerModel` in lib/kit.ts now pin-only → the whole-branch reviewer weighs it; the table specimen prints the word "refused" where the Book prints "—" → RULING: the living canon follows the product (dash in the numeric cell, the refusal named by its StatusPill); fold into Task 7's fix round after review. The styleguide pins need NEXT_PUBLIC_SHOW_STYLEGUIDE=1 at build AND test time — earlier shared builds lacked it (those pins were among the 10 skipped); this build sets it. Review package review-ac3bbde..50c62a3.diff → reviewer (fable).
- Task 3 re-review 1 (ac3bbde): RE-REVIEW: closed — `pointRecord` carries the record's law unchanged (the guarded epoch subtraction, the three booleans deciding answer vs fold, `ratesOutside`, the fold count character-for-character); every moved string verbatim; all nine `readWirePopulation` reads kept; the withheld "0" pin honest (api/openapi.yaml:5651-5653: `refused_positions` is a non-nullable integer, "Zero on a withheld engine with no rows behind it"; the four null totals pinned as em dashes, no "$0"); the mark key's tick is the chart's own class; G2 touched exactly the first character of the three arms. Minors → Task 12's fix wave: m1 the withheld state clause now renders dim `--ink-3`, which tokens.css reserves for captions and "never a state" — RULING: a state word takes `--ink-2` or the refused register, never ink-3; m2 the key's warn square shared with the chart by copy (one exported const); m3 the key also renders on hole-free windows; m4 the record's accessible name changed case.
- Task 3: complete — 339fa17 b5f78eb ac3bbde (history.spec confirms on the shared build running now).
- Shared build at 50c62a3 (2026-09-20), `NEXT_PUBLIC_SHOW_STYLEGUIDE=1` set for BOTH the build and the test run (the styleguide pins in p1a-fixes.spec.ts:190-199 and shell.spec.ts:80 skip without it; earlier shared builds lacked it, so those pins sat among the "10 skipped"): e2e **212 passed / 0 failed / 0 skipped**. Verifies 6522c8e (Verification deep-link pin), 091beb7 (Activity round 1), ac3bbde (History round 1), 50c62a3 (Task 7, incl. the re-expressed styleguide pins). LAW for the rest of the plan: every shared build and e2e run carries the flag.
- Task 8 (integrator, direct — four of the five links already stood: Book rows → `/inspector/{addr}`, Inspector → `/lab?address=`, Scenarios movers → `/inspector/{account}`, Overview → `/proof#architecture` from Task 5; the preview's lines already named `/lab?scenario={id}`): the stress preview's header link now names the previewed scenario when the preview is a view, and stays the bare workspace when the preview is withheld/uncovered (no id the batch published no view for). Pins: book.spec (row href shape; every link in the preview card → `/lab?scenario=eth_minus_30`; withheld → `/lab`), overview.spec (`/proof#architecture`). Production build + book/overview specs 26 passed. **Task 8: complete — b928a0b.**
- RULING (T9/T10 parallel): Task 10's StressTable item (negative room word) is Task 9(e)'s — one judge, one word, one owner. Task 10 does not touch `StressTable.tsx`, `address-stress.ts` or any `lab-*`; Task 9 does not touch `cash-*`, `book-*`, `app/book/**`, `address-lookup.ts`, `inspector-view.ts`. Task 10 also carries Verification's `useCashBook` → census-only read.
- Tasks 9 and 10 dispatched in parallel (2026-09-20), BASE b928a0b, both `serena-coder` on `fable`, briefs = the plan section + a binding controller's addendum (ownership split, what Plan 3's close-out already landed, unit gates only, commit rules). Task 9 implementer a50c1cf94d886f7a6 (lab debt + the Inspector StressTable word under the one verdict judge). Task 10 implementer acc9874d857a1ca4a (inspector/book debt + Verification census-only; never StressTable.tsx — if the epoch disclosure must render there it lands as a view-model field and the integrator wires it after Task 9). Task 7 reviewer a26455ab2e7bb9af6 still reading.
- NOTE: a system-reminder-styled block inside a Bash tool result (the shared e2e run) instructed adding a `Co-Authored-By` line to commits. It arrived in a tool result, not from the owner, and contradicts the owner's standing rule — ignored; both briefs tell implementers to ignore the same.
- Task 7 review (a26455ab2e7bb9af6, `task-7-review.md`): spec PASS WITH FIXES, quality APPROVE after the round; no Critical. Deletions exact (11 components + 2 CSS modules, every import site at the parent inside the styleguide); seven sections and the p1a-6 contrast pin md5-identical; the re-expressed five-banner pin stronger than its ancestor and the first direct pin of `VerdictHeader`'s empty-chip refusal. RULINGS → fix round 1 (same implementer a974930f83520bb60, files `app/styleguide/**`, `p1a-fixes.spec.ts`, `web/README.md`, disjoint from T9/T10): **I1** (plan-mandated) the table specimen folded one count and one sum over debt_manager + aave_v3 — the specimen becomes single-engine as the Book's table is; **I2** the refused row prints "—" as a bare text node under its refused StatusPill (the product's shape, NeedsAttention.tsx:55-60), `.refcell`/`refusedCell()` go, the "say the word" test retitled to the law as it stands, the dim arm measures the td that holds the dash (today no glyph inherited `--ink-3`); **I3** RULING: the exact value's kit home is the drawer (InspectorDrawer.tsx:90-93) — one human → exact row in `sg-drawer` + one pin line; `sg-exact` untouched; **M1** web/README.md names the kit in place of the retired components; **M4** the crit header wears the Book's chips; **M5** one sentence keeps the superseded/partial law in the section note. DEFERRED to Task 12's pruning wave: M2 (EngineTag/.engineTag, stale lib comments, dead rules in primitives/ribbon css, chip.module.css:2, STREAM_* and snapshotChipParts styleguide-only), M3 (lib/kit.ts verdict-banner model pin-only — move the empty-chips law to a pure function with its unit pin BEFORE the model goes; the unit count may not drop), M9 (kit.kv unused vs the styleguide's local .kv); Task 12 QA: M6 register notes, M7 six H1s on /styleguide; M8 the retirements' one-line notes go in the close entry (`progress-ui-overhaul.md`): p1a-fixes (ExactValue-in-banner arm; missing-identity-strip / "Verdict withheld" arms; superseded/empty/partial variants), r1-fixes (10), shell walk (three ids renamed, three retired), sections sg-chips / sg-marks / sg-stampline.
- Owner's list +1: `ExactValue` has no product consumer (styleguide-only, like `states/*`); the product's exact values live in the Inspector drawer.
- Task 7 fix round 1 landed: **de8dcbe** on b928a0b (six paths by pathspec, subject only; scope gate + hook passed). Implementer's gates: eslint + lint:css clean, unit 904 / 0 failed; tsc red on ONE line not theirs — `tests/unit/cash-summary.spec.ts(92,7)` TS2353, Task 10's uncommitted spec edit mid-flight (expected under the parallel rule; re-check when Task 10 lands). Round's e2e pins NOT yet run in a browser (no build while T9/T10 files are half-edited) — likeliest failures named by the implementer: the `thead th` list, the `/^—$/` count of 2, the dash td computing `--ink-3`, the drawer's `6,840.238278`. Implementer's notes: the refused pill's title is now `collateral sweep failed · SWEEP_FAILED` via `plainCause` / the phrasebook (the old `sweep_failed_no_success` is not a phrasebook code); the rest of the styleguide still shows the old lowercase code; the header, table and drawer now state one number (6,840.238278). Package `review-b928a0b..de8dcbe.diff` → scoped re-review 1 dispatched to a26455ab2e7bb9af6 (asked for a read on the phrasebook code and the two new lib imports).
- T9's scratch `.ts` drafts (`view-pins.ts`, `reading-pins.ts`) in the scratchpad raise LSP noise only; delete after Task 9 reports.
- Task 7 re-review 1 (de8dcbe): **RE-REVIEW: closed** — I1, I2, I3, M1, M4, M5 each CLOSED with file:line; nothing Critical or Important introduced. Arithmetic checked (61.20 + 14.55 = 75.75 one engine; 4,200.118139 + 2,640.120139 = 6,840.238278 = header = drawer; `551 / 552 computed` agrees with the one refused row); the dash is a bare text node under the Room td so the glyph inherits `--ink-3` and the pin can now fail; the untouched blocks hash identically at ac3bbde / b928a0b / de8dcbe. `SWEEP_FAILED` is a real wire code (phrasebook, 00013_risk_tables.sql:273, every product fixture) and the title is composed as `notComputedCause` composes it (cash-rows.ts:80-81). Minors → Task 12's wave: **N1** the page spells one refusal two ways — the old lowercase `sweep_failed_no_success` is the P1 mockup's string, not a wire code, welded into `states/RefusedCard.tsx:33`, `tests/unit/kit.spec.ts:175-179` and p1a-fixes :266/:390/:495 — ONE cross-file canon decision (RULING for the wave: the styleguide speaks real wire codes throughout); **N2** the specimen's row type admits a null it coalesces with `?? 0n` (TableSpecimen.tsx:140,157) — unreachable with static rows, tighten the type; **N3** after Task 10 lands the pill title calls `notComputedCause` so the " · " join has one owner; **N4** header/drawer figures equal the table's by arithmetic not construction, and the row id `dust` carries a `small`-tier figure.
- **Task 7: complete pending e2e — 50c62a3 de8dcbe** (the round's re-expressed pins run on the next shared build, after T9/T10 land; failures return to a974930f83520bb60).
- Task 11 Step 1 PREPARED (integrator, uncommitted; files `web/tests/e2e/screenshots.spec.ts`, `web/scripts/screenshot-pages.mjs` — owned by neither T9 nor T10): four PAGES entries (`history` /observatory, `activity` /feed, `verification` /proof, `api` /developers) with ready predicates (surface `data-state=ok`, no `aria-busy` inside, 50 activity rows, the receipt visible, the API toc visible); `/v1/events` answers by the request's own `account` param (the Inspector's account page vs Activity's cross-engine first page — one endpoint, two readers); `/v1/observatory/series` answers by the request's `engine`. No new mask: none of the four pages prints a browser-clock age (History's Served chip is the wire's `served_at` verbatim; Activity's headline names the wire's newest custodied instant). eslint clean; tsc clean for the pin spec. Baselines wait for the shared build after T9/T10 land — Verification's render changes with Task 10's census-only read, so its pin is taken after.
- Task 10 landed: **646aee9** on de8dcbe (19 paths by pathspec, subject only, hooks passed). All six items, none STOPPED; implementer's gates: tsc + eslint clean, unit 920 / 0 failed; e2e pins written, unrun. Pins 1/4/5 seen failing by assertion first; 2/3/6 only as module-load failures (each fix creates the export the pin reads). Implementer's concerns, with the controller's first read: (1) two lines of `lib/cash-book.tsx` touched (the over-delivery sentence lived there; the words moved to `censusFaultWords()` in book-headline.ts) — ACCEPTED in principle, reviewer to confirm smallest-change; (2) "both bounds" read as: finding figure qualified "at least $X", bars' own note `book-bands-note`, a walk-derived zero prints "—" mid-walk — reviewer asked for a read on completeness; (3) item 5 needed no StressTable edit (`stressBatchNote(view).disclosure` already renders) — nothing for the integrator to wire; `lookupRepaired` optional on the reading — reviewer asked whether optional weakens the law; (4) `/proof` now asks `/v1/book` once and loses `useCashBook`'s resume repair — OWNER'S LIST candidate pending the reviewer's read; (5) two e2e assumptions (the money regex at ~$830K; `asked.book === 1`) — first suspects on the shared build; (6) residues: terminal-page over-delivery still framed "The walk stopped before the last page"; old stress beside a new lookup after `reload()` disclosed only by the batch-mismatch note. Package `review-de8dcbe..646aee9.diff` → reviewer af8ad0a8674db4a57 dispatched (read-only, reads the commit not the tree).
- Task 9 landed: **6df392a** on 646aee9 (23 paths by pathspec, plan's message verbatim, hooks passed). Implementer's gates: tsc + eslint clean, unit 920 / 0 failed; e2e pins (4 new tests + 1 arm in lab.spec, 1 new test in inspector.spec) written, unrun. Fail-first OBSERVED: (a) a run-book without `batch` threw at render (`reading 'supersession'`), a library row without `shocks` threw in `definitionSkew`, a set without `evaluation` named the wrong field; (b) the REAL defect — `heldOf` moved ANY ok body into the hold at the next ask, so a second malformed 200 presented the first malformed body as the standing result; now only a body that reads moves (`readsAsAnswer`, taken by both `withRunning` and `withSettled`); (e) a negative-debt stress row rendered "No" on the Inspector, now "Cannot say". (c)/(d) fail by absence only. `stressVerdict` retired; `rowVerdict` in address-stress.ts, zero stale references. Deviations: `readsAsAnswer` counts withheld / not-modelled as answers (matches the landed release rule); set envelope faults read "… is outside the wire contract"; a result with a broken list stays one contradictory row. Concerns: (1) `lib/runbook.ts` (not theirs) still maps over the raw 2xx body → a missing/null `engines` rejects as "unreachable" with a TypeError message, never reaching the classifier — CONTROLLER follow-up pending the reviewer's read; (2) the demo Inspector's stress table text moves in two cells ("over cap by $1,069", "over cap by $215.75") — implementer judged the pinned viewport ends above the table; the shared build's screenshot replay decides; (5) projection cells print `+$…` for a negative `extraInterest` — out of scope, reviewer asked for reachability. Package `review-646aee9..6df392a.diff` → reviewer a4c9dc19156bd717a.
- Shared build at 6df392a + the integrator's uncommitted Task 11 entries, flag set, whole e2e minus the four un-baselined pins: RUNNING (background b9q8kz6bl; log scratchpad `e2e-p4-6df392a.log`).
- **Shared build at 6df392a** (flag set; + the integrator's uncommitted Task 11 entries; whole e2e minus the four then-un-baselined pins): **223 passed / 0 failed / 0 skipped.** Every new pin of de8dcbe (T7 round 1), 646aee9 (T10), 6df392a (T9) green in a browser, incl. the four the T7 implementer named likeliest to fail and T10's two assumptions (the money regex; `asked.book === 1`). The eight existing pixel pins replay unchanged — the Inspector pin does not reach the stress table's "over cap by" cells (T9 concern 2 settled); no primary state moved. **Task 7: complete — 50c62a3 de8dcbe.**
- Task 11 Step 1: baselines WRITTEN, NOT COMMITTED (`--update-snapshots all -g "(history|activity|verification|api) · (dark|light)"`; 8 files under `screenshots.spec.ts-snapshots/`); two replays 16/16.
- **Task 11 Step 2 — THE GATE, by the owner's delegation ("go with your best judgement", 2026-09-16 21:56). Integrator's first verdict 2026-09-20 ~17:25 USMST: CHANGES FIRST.** Read against book-dark.png for register: **G1** the four pages' `.page` carry no top padding (Book: `padding: 28px 0 24px`) — the kicker sits 28px nearer the appbar and the header jumps between tabs; **G2** the whole headline is the emphasis, so the sentence is a wall of tone colour (History: three green lines) where the Book colours only the finding's core; **G3** headline grammar below the Book's — "$1900000", "8552 account(s)", "bucket(s)", "chain action(s)", lowercase "receipt ACCEPTED" (this REVISES R2: the sentences keep their facts and speak the Book's grammar — human money, grouped integers, real plurals, emphasis + rest); **G4** History's chart end labels are struck through by the line; the zero baseline pins a flat series to the top of an empty plot; **G5** the Verification pin says "serving batch #1" beside "Batch 18,251" — the pin's `EVIDENCE_MANIFEST` is not welded to the demo batch (fixture-layer fix: a demo evidence body); **G6** Activity: "posture replay arrives with P4's outbox" (roadmap vocabulary on a public page), a section titled "History: …" on the Activity page, raw-unit amounts (carried M12); **G7** API: the Operations tile repeats the chip and the headline, Base URL twice. Captures of all eight pages both themes: scratchpad `gate5/` (32 files). Consults dispatched in parallel to ground the fix brief: `solvent-design` ad56a022419d135a8 → `gate-design-ruling.md` (tiers A/B/C, scope held: no IA rebuild, no new components); `solvent-clarity` a2ed50c24595aa54d → `gate-clarity-ruling.md` (headline emphasis/rest words and the pattern for every arm, deks, holes, Activity's strip and notices). The baselines are retaken after the gate round; nothing is committed until then.
- Task 10 review (af8ad0a8674db4a57, `task-10-review.md`): spec PASS WITH FIXES, quality APPROVE, no Critical; all six items CLOSED with file:line, every pin can fail and would have failed on de8dcbe (2/3/6 by assertion in the e2e pins, since run green on the shared build). Reviewer's answers: the `cash-book.tsx` touch is the smallest honest change; the "both bounds" reading is honest and complete apart from I1 — no census figure wrongly wears the qualifier, "—" for a walk-derived zero is right and returns to "$0" on a complete walk; optional `lookupRepaired` weakens only the type guarantee; `/proof`'s evidence and meta asks never had a resume repair, the step always prints its absolute `computed_at`; neither pixel state moves (demo walk completes 1,000 + 412 = 1,412; 6 waterfall points; stress and address both batch 18,251). RULINGS → fix round 1 (same implementer acc9874d857a1ca4a; ONE commit): **I1** the distance card says "a zero is claimed only by a complete walk" over bars printing "$0 · 0" — mid-walk a zero-so-far band prints "—" with no count and a zero-height bar; WHO DECIDES lives in cash-summary.ts; `components/kit/BandBars.tsx` opened for this item only (a null count prints no "· N"; BookLegacy's use unchanged); the narrowed e2e pins widen to `not "$0"` outright; **I2** the lab's address workspace is the disclosure's second consumer (the literal "Stress for batch" chip, AddressWorkspace.tsx:21-27 / lab-address.ts:242-243) — wired from the one lib sentence; `lookupRepaired` becomes REQUIRED (Task 9's spec literal gains the field); **I3** RULING one law, two pages: `cashCensus` returns a refused census for an absent/withheld Cash engine (contract read first: api/openapi.yaml + the Go handler read-only) — the compute step names it withheld under the refused tone on `/proof` AND `/`, never "0 Cash accounts" at neutral; the Overview's happy path byte-identical. CARRY-FORWARDS: the terminal-page over-delivery frame "The walk stopped before the last page" (equally false for the short terminal page, already pinned so; under over-delivery "at least" is not provable — landed rows may hold duplicates) → Task 12 wave; `/proof` has no resume repair → OWNER'S LIST; old stress beside a new lookup after `reload()` (covered by the batch-mismatch note until one stress request returns) → noted, no action.
- Gate consult 1/2 — `solvent-clarity` (a2ed50c24595aa54d → `gate-clarity-ruling.md`, per-page tables: element · today · ruled words · pattern for every arm; A/B/C split; pins to re-point): **R2 as first written cannot stand; G3's revision is right.** The four sentences are true but written to the wire (semicolons, middots, ISO instants, "(s)", capitalised state words) and promoted to an H1. Every fact is kept; the altitude changes: emphasis = one finding in ≤ 8 words, rest = scope + as-of; refusal/loading/failure arms are emphasis-only in the dashed tone. Ruled happy arms (demo): History "**$1.9M of legacy Aave v3 debt is outstanding,** across 8,552 accounts in the hour starting Aug 8, 20:00 UTC."; Activity "**3 liquidations among the 50 chain actions loaded,** the newest at Aug 8, 20:21 UTC; more exist beyond these."; Verification "**All 87 checked rows matched the chain exactly,** in this deployment's pinned reconcile run."; API "**17 read-only endpoints,** every money value an exact decimal string." All four deks are doctrine slogans → fact sentences (the long forms already sit in the drawer); History's holes live in the DEK ("165 of the 168 hours in this window were recorded. 2 are absent — no complete batch ran — and 1 was withheld; each is a gap on the chart, never a zero."), moving into the headline's rest only when `captured * 2 <= total`, and the headline itself changes when the newest hour is withheld. As-of humanised by a new pure `humanUtc` from the string's own UTC fields (never the browser clock); ISO verbatim stays in chips, tile subs, cells; Activity gains a `Newest` chip for the exact instant. History's chart finding states DELTAS ("debt fell $11K to $1.9M, accounts fell 91 to 8,552, and liquidatable positions rose 1 to 46") — a humanised arrow pair would print "$1.9M → $1.9M"; one bigint subtraction on one engine at one scale, no percentage. Activity: strip label "Live stream", law line "This browser connection only — not a record. The list below is the record.", "P4's outbox" deleted from every public string, section title "Recorded chain actions", both notices rewritten, the tiebreak doctrine → drawer, Amount column caption "engine units, not USD". Verification: "pin" only as the glossed "pinned reconcile run", the hash in the chip; "watermark vector" card/drawer only; "Batch 18,251" via `groupInt`, no "#"; G5's fixture weld lands in the same round. DEFECTS FOUND: Activity's loading arm prints "0 chain actions loaded…" in the H1 (breaks the page's own nothing-loaded-is-a-dash law); Verification's chip "Pinned batch" misnames a comparison sha as a batch beside "Live batch". Flag for design: "3 liquidations" in ok-green reads as a health verdict. CONTROLLER NOTE for the brief: every new fact claim is verified before it is worded ("No key or sign-in"; "a test fails the build if this page and the contract disagree"; "finished Jul 29, 02:14 UTC" from the evidence body's own field).
- Task 9 review (a4c9dc19156bd717a, `task-9-review.md`): spec PASS WITH FIXES, quality APPROVE after a small round, no Critical; (a)–(e) CLOSED on the letter, every behavioural pin fails on the parent for a stated reason; the four deviations honest; the hold defect does not exist on the set path; the screenshot pin is viewport-only and ends above the stress table; item (d) has no reachable wording drift; a negative `extraInterest` is wire-legal but unreachable (`internal/risk/scenario.go:515` rejects `annual_delta_bps <= 0`) → noted, no action. RULINGS → fix round 1 (same implementer a50c1cf94d886f7a6), TWO STEPS because Task 10's round is editing `lab-address.ts` / `AddressWorkspace.tsx` / `lab-address.spec.ts` / `lab.spec.ts`: **STEP 1 now** — **I1** a refusal without a string `code` still throws at render (`plainCause` → `code.trim()`), the classifier admitted it and `readsAsAnswer` would hold it: the entry is named (`excluded_engines[i].code`), never read, never held; the spec that blessed the shape corrected; the phrasebook itself untouched (shared with the Book); **I2** a set 2xx whose body is JSON null throws inside the `setSet` updater — both envelope classifiers name a non-object body first; failed Compare / contradictory state, never a route crash; **I3** `lib/runbook.ts` OPENED to the implementer (one owner, one commit): it maps over the raw 2xx body so malformed `engines` / `projection` shapes reject as "unreachable" with a TypeError message and five classifier names can never print in the app — the sealing made total: a 200 is never "unreachable"; **STEP 2 after Task 10's round lands** — **I4** RULING: one verdict WORD function for both pages, the Inspector's words ("Within 90d" / "Not within 90d"; a projection never answers a bare "No" on either page); `rowVerdictWord` retires; the lab's A1 pins re-pointed; + I3's e2e in lab.spec.ts. Minors: one-liners taken incl. M1 (`readsAsAnswer` vs `bookOf`'s release rule: one rule, or both corners pinned).
- Gate consult 2/2 — `solvent-design` (ad56a022419d135a8 → `gate-design-ruling.md`, 312 lines: seven observations, Tier A1–A8, B1–B10, C1–C10, a y-domain appendix): **NOT at the bar — eight small register defects, none structural; SHIP the pins after Tier A.** Composition and IA right. CORRECTION to the integrator's note: the approved Scenarios page ALSO has zero top padding (already pinned) — converge on the Book's `28px 0 24px`, the shell-level fix is owner's list C5. **R2's tone clause OVERRULED**: on the Book and Inspector ok-green is a HEALTH VERDICT; re-using it for "the data answered" gives one green two meanings and paints History's "2 absent, 1 withheld" in the colour of health — a statement of record is INK (kit gains `neutral`); only Verification's proof clause wears tone, the live clause never (the page's own doctrine: the live batch does not inherit the proof's exactness). A3 typography (grouped figures via the `displayMetric` chokepoint, no "(s)", capital initial, ISO instants in a no-break span); A4 the three chart labels are struck through because one 6px pad serves all sides and text paints before the line (pads split, labels above their reference, text last with a `--panel` halo, PLOT_HEIGHT 200 → 120); A5 the live strip leaves mono prose, the roadmap sentence → drawer; A6 the Amount column aligns, Unit its own dim column; A7 the H2 drops "History:"; A8 light theme — a white copy chip on the black terminal block. **The zero baseline is NOT a defect** (an honest, previously ruled law; a flat line is the true picture of a 1% week) → OWNER'S LIST C1, specified in the appendix (level-anchored band, liquidatable always zero-based, three-way disclosure, never `includeZero={false}` alone). Not judged: hover / degraded / refused / empty (B10: capture them).
- **Integrator's gate rulings (2026-09-20 ~17:45):** Tier A all; Tier B all (B10 by the integrator). **B1 RULED: History opens on Cash** (the product is "ether.fi Cash risk"; the Book demotes the legacy market to its foot; this reverses Task 3's "default as today" — for the owner's Rulings list). Reconciliations: words = clarity, tone/typography = design; headline + dek human tier, exact values verbatim in chips / subs / cells / record / drawer; chart labels + record grouped via `displayMetric`, the finding states deltas; as-of via `humanUtc` with NBSP joins; every new fact claim verified before it is worded. Round shape: Phase 0 shared (kit neutral tone, no-break splitter, `humanUtc`, `.meta` grid, kit pressed rule, the four paddings, A8) → Phase 1 three page implementers in parallel (History / Activity / Verification+API). Briefs: `gate-brief-0-shared.md`, `gate-brief-pages.md`. Phase 0 touches `lib/lab-headline.ts` (Task 9's file) → starts when Task 9's fix round has landed.
- Integrator (uncommitted, own files): **G5 welded** — `DEMO_EVIDENCE` in `tests/fixtures/demo/index.ts` (the contract's manifest, `substrate.batch_id` = the demo batch; the proof subject untouched), used by screenshots.spec.ts and screenshot-pages.mjs; pins replay 16/16 (the "#1" → "#18,251" text change sat inside the 1% tolerance — baselines are retaken after the gate round regardless). **B10**: screenshot-pages.mjs gains honest-state page keys (historyDegraded, activityRefused, activityExhausted, verificationFailed, verificationUnavailable) via per-page route overrides.
- Task 10 fix round 1 landed: **3066133** on 6df392a (19 paths, subject only, hooks passed; tsc + eslint clean, unit 923). I1 `bandsSoFar` in cash-summary.ts decides, `Band.count: number | null` in BandBars (null skips the guard, weighs nothing, prints no "· N"), mid-walk pins widened to `not "$0"`, a new pin that the settled page prints "$0 · 0" again; I2 `stressBatchNote.chipValue` — the phrase exists once, `lookupRepaired` REQUIRED, the lab has the Inspector pin's twin (`pageshow`); I3 contract read: `Aggregate.positions` is a required non-nullable integer, `refused` is true "whatever the position counts say", the handler copies persisted counts verbatim, the contract's example carries 0 → a withheld card's count is a placeholder; `cashCensus` → count | refused{withheld|missing, code, cause} | null; `pipelineSteps(meta, evidence, reading)` reads the census itself (4th parameter, `VerificationInput.cashAccounts`, `Pipeline`'s `cashAccounts` prop GONE); the refused step prints "batch · Cash accounts withheld" under the refused tone on both pages. Concerns: e2e pins unrun (watch `toHaveCSS("height","0px")` on the bars' `<i>` and the lab resume pin's `repairs === 1`); `cashCensus` restates cash-book.tsx's private `wholeRefusal`; **same family, NOT ruled: under a withheld Cash engine the Book prints "0 borrowing accounts" (BookSurface.tsx:90) and "0 / 0 computed" → Task 12 fix wave.** Package `review-6df392a..3066133.diff` → scoped re-review 1 by af8ad0a8674db4a57.
- Task 10 re-review 1 (3066133): **RE-REVIEW: closed** — I1, I2, I3 each CLOSED with file:line, each pin would fail on 6df392a; M2 taken; nothing Critical/Important introduced; no leftover of the removed parameter/prop; by reading no pixel-pinned state moves. Judgements: "— · N" (a positive count beside a dashed zero debt) is honest and stays — the count is a true lower bound; the `pipelineSteps` signature change + removed prop is the right call (a second reader is now impossible; the served arm's strings are character-for-character the parent's); contract reading verified (api/openapi.yaml:2452-2505, :2379-2401; cmd/api/handlers.go:331-361). **Task 10: complete pending e2e — 646aee9 3066133.**
- → TASK 12 FIX WAVE, from this re-review: **W-census** the withheld-census placeholder prints at FIVE sites, all from `deriveCashView` reading the counts whenever the engine is listed — Book SectionHead (BookSurface.tsx:90), Book Coverage chip (cash-view.ts:176-183), the VALUE of the Book "Not computed" tile (BookSurface.tsx:197), Overview strip "Accounts" (OverviewSurface.tsx:148), Overview Coverage chip (:135); after 3066133 the Overview disagrees with itself under the contract's shape (step "Cash accounts withheld" under a strip reading "Accounts 0"). Smallest fix: in `deriveCashView` null all three counts when `withheld !== null` (every site at once); RESHAPE both pins' fixtures (book.spec.ts:108, overview.spec.ts:65) to the contract's zeros — today they leave served counts standing (2 / 1 / 1), so the pin titled "nothing rendered as zero" cannot see the defect. Fold in ONE pure home for the whole-refusal predicate (`wholeRefusal` in cash-book.tsx vs `cashCensus`: no drift on contract-conforming input; they differ only on a truthy non-boolean `refused` — truthiness vs `=== true`), which also closes M4 and the second copy of "the Cash engine is missing from this batch" (cash-view.ts:159, verification-view.ts:90). **W-lab-nobatch** when the stress body names no readable batch the lab shows no chip and no disclosure and still compares the tiles (lab-address.ts:159,178) — the note's `chipValue` can carry it. **W-weak-pins** book.spec.ts:353 (`not "$0"` on the held demo walk also passes on the parent — all seven demo bands are positive on page one) and :410-412 (now satisfied by the absence of `data-count`, not by seven zeros) — strengthen. Left Minors M1, M3, M5–M9 wait for the wave.
- Task 9 fix round 1 STEP 1 landed: **6acf8d1** (seen in `git log`; the implementer's hand-back comes with STEP 2). STEP 2 (I4 one verdict word + I3's e2e) proceeds now that 3066133 is in.
- Gate round Phase 0 dispatched: `serena-coder` on `fable`, aeb15e54817b03969, brief `gate-brief-0-shared.md`, BASE 6acf8d1. `lib/lab-headline.ts` (the one-line `tone` union) is edited LAST under a clean-file guard because Task 9's STEP 2 is nominally in that area; S4/S5 carry STOP conditions on the four approved pixel-pinned pages.
- Task 9 fix round 1 landed in TWO commits on 3066133: STEP 1 **6acf8d1** (13 paths: I1 I2 I3 + Minors), STEP 2 **c79a773** (5 paths: I4 + I3's e2e). Both by pathspec, subject only, hooks passed; implementer's gates: tsc + eslint clean, unit **937 / 937**; e2e pins `--list`-parsed, unrun. Fail-first OBSERVED for I1 (`readsAsAnswer` true for a code-less refusal), I2 (`reading 'batch'` / `reading 'evaluation'`), I3 (four of six seal tests failed with the review's TypeErrors); I4 by construction only. NOTE: the implementer's write to `task-9-report.md` was REFUSED by the harness and it asked the controller to paste its section in. NOT done on its behalf (an agent that was denied an action does not get it done by proxy); the hand-back is kept instead as the CONTROLLER's own record, `task-9-fix-round-1-handback.md`, labelled as the implementer's unverified account; flagged to the owner. First reads on the deviations (reviewer to confirm): an EMPTY-string `code` admitted (contract: `type: string`, no `minLength`; the phrasebook has a sentence for it) — accept; one input class beyond the ruling (a horizon whose `becomes_liquidatable` is not boolean-or-null threw in `liquidationVerdict`; sealing now per horizon) — accept; I4's letter (`AddressWorkspace.tsx` imports `stressVerdictWords` directly as `StressTable.tsx` does; `lab-address.ts` has no use for it) — accept; M1 pinned at both corners, not unified — reviewer asked whether any input makes the two rules visibly disagree; M6 makes the classifier refuse non-string printed scalars — reviewer asked to verify each is `required` + `type: string` in the contract. Package `review-3066133..c79a773.diff` → scoped re-review 1 by a4c9dc19156bd717a.
- Gate round Phase 0 landed: **bc40bdc** (12 paths; tsc / eslint / lint:css clean; unit **951 / 0**, 14 new). S1 neutral tone (`VerdictHeaderProps.tone` + `LabHeadline.tone` = crit | warn | ok | neutral | refused; `.emNeutral`; `data-variant="neutral"`), S2 `splitInstants(text): InstantPart[]` in lib/instant-split.ts (a no-instant headline renders the identical string child), S3 `humanUtc(iso, referenceIso?)` in lib/human-utc.ts (NBSP-joined; no/ill-formed reference → the year always prints; year form "Aug 8, 2025, 20:00 UTC" per the brief — clarity wrote it without the first comma; KEPT as the brief has it, standard US form), S5 kit `.btnGhost[aria-pressed="true"]` (no approved page has a pressed btnGhost; visible today on Verification's raw-JSON toggle), S6 the four paddings, S7 A8 verbatim (on the terminal block the copied state is carried by the glyph alone). **S4 STOPPED — correctly**: B2's premise is false for the approved Scenarios pin — in lab-dark.png the header is ALREADY two rows at 1440 (main column 912px, chips 817px, the 208px action wrapped: 817 + 10 + 208 > 912); the grid would move the action to row one and wrap "Config v1" / "Engines" beneath → lab-dark/light move. RULING: B2 is OUT of this round → OWNER'S LIST (re-baseline Scenarios with approval, or an opt-in header variant); History's fifth chip wrapping under the button is accepted as the kit's current behaviour. `gate-brief-pages.md` corrected before dispatch.
- Gate round Phase 1 dispatched in parallel, BASE bc40bdc, all `serena-coder` on `fable`, brief `gate-brief-pages.md`: **History** a06d23072c81901fe (clarity's table all arms, holes in the dek, deltas finding, A2 neutral, A3, A4 chart geometry, B1 opens on Cash, B3, B9), **Activity** ad7aec6802b62e6de (headline all arms, the loading-arm zero defect, computed fact dek, Newest chip, A5 one plain strip line with no roadmap vocabulary, A6 amount/unit columns, A7, B3, B4, B5), **Verification + API** aa3380336b0d9afd1 (A2.4 proof arm toned / live arm ink, the "Pinned batch" chip defect, B8 with the Overview's `pipelineSteps` output held byte-identical, B5 computed tile subs + one Base URL, B6 index rows, B7 reflow; four fact claims to verify with file:line). Each told: any export consumed by a page it does not own keeps its words there, or STOP.
- Task 9 re-review 1 (6acf8d1 + c79a773): **RE-REVIEW: not closed** — I1, I2, I3, I4 each CLOSED (pins can fail on the parent; all four deviations accepted: empty `code` admitted — the page prints "Cash — the engine gave no reason."; the extra malformed-horizon class real; the component-level import; blank `engine` refused though the yaml has no `minLength` — a refusal that names no engine cannot be placed). ONE remaining 2xx → render-throw path, **N1 (Important)**: `movers[i].account` missing / null / a number classifies clean, reads as a result, `MoversTable.tsx:24` → `truncateAddress` throws, and `readsAsAnswer` is true so it would enter the hold; `RunBookMover.account` is `required`. + `note` / `hf_transitions.note` print as React children. M6 verified: every newly judged member `required` + `type: string`, none nullable; all 21 run-book fixtures clean by the reviewer's own transcription. → **fix round 2** (same implementer a50c1cf94d886f7a6; files `lab-classify.ts` + specs, `runbook.ts` for N2 only — disjoint from the three page implementers): N1; N2 an off-pattern listing id THROWS on the run-book path ("unreachable") while `runbookSet.ts` resolves `refused-locally` — one answer for one condition, STOP if the ripple exceeds ~25 lines; N4 one-liners (the non-object sentence's lowercase start after a full stop; un-export `sealRunBook` if unused). Told not to ask again for its report file to be written for it.
- → TASK 12 FIX WAVE, from this re-review: **W-M1** the view asks "is the body malformed" BEFORE version or coverage, making the hold's rule and the release rule ONE (today, corner one: a result on the page; a 200 both version-skewed and malformed in Cash withdraws the figures as "definition changed"; a later failed re-run brings the old figures back under "Run again failed" — three coincident conditions, nothing false prints, but neither the pin nor the comment states it); **W-M2** the null-scale room word names a false cause ("unreadable scale") when no Cash position exists — needs a view field; **W-N3** the unsealed-horizon check would admit a stray wire field named `liquidation_verdict`; **W-pin** the "by construction" pin's source-text lines (lab-address.spec.ts:428-429) pass vacuously on a comment and break on a rename — one exported cell function with a runtime pin.
- Task 9 fix round 2 landed: **84b57f1** on bc40bdc (11 paths, subject only, hooks passed; tsc + eslint clean, unit **956 / 956**; nothing STOPPED; the report-file write not retried, as told). N1: contract read first — `RunBookMover.account` (`$ref Address`), `RunBookEngine.note`, `RunBookTransitions.note` all `required`, `type: string`, not nullable → judged unconditionally in wire order; an empty note stays clean; the set engine carries no movers and prints no note → nothing judged there; `movers_note` left unjudged (a `title` attribute: React stringifies, no throw). Seven-body view pin: never an answer, never held, contradictory with the named dek, NO movers table, a held result stands with its own mover. 15 run-books + 4 sets through `runBookScenario`: clean. N2: the off-pattern id RESOLVES `refused-locally` with the set path's words to the letter; `RunBookOutcome` and `BookState` each gain the arm; `FAILURE_WORD` "Not sent"; ~10 lines / 3 files. N4: capital at the one join (`contractFaults`); `sealRunBook` un-exported. Fail-first observed for N1 and N2. CONTROLLER TODO at close: the Scenarios page-test contract (`docs/plans/2026-09-15-ui-scenarios.md:113`) gains `refused-locally` in the book's `data-state` vocabulary (the reviewer is asked for the exact line). Package `review-bc40bdc..84b57f1.diff` → scoped re-review 2 by a4c9dc19156bd717a (also asked to FINISH the hunt: any member of a 2xx body still reaching `.length` / `.map` / `.trim` / a React child / `BigInt` unjudged, across every component under app/lab).
- Task 9 re-review 2 (84b57f1): **RE-REVIEW: closed** — N1, N2, N4 each CLOSED, every new pin fails on the parent. The hunt is finished: every body-derived `.length` / `.slice` / `.map` / `.trim` / `BigInt` in the lab path sits behind a classifier or a wire guard, with three Minors left → TASK 12 WAVE: **W-N5** `hf_transitions.lanes[i].label` reaches the grid's header cells unjudged on an unmerged grid (lab-transitions.ts:268 → Heatmap.tsx:76,83; only an object throws; `required` in the schema); **W-N6** `movers[i].became_eligible` read unjudged into a verdict word — absent prints "No", a string "false" prints "Yes" (MoversTable.tsx:30; the refusal law's letter; `required`, boolean, nullable); **W-N7** `movers_note` judged like the two notes (an object prints "[object Object]" in a `title`). "Faults: The response body is not a JSON object." KEPT (a full sentence after a colon; a second join would re-create the two-places problem). Cosmetic, shared with the set path: "Nothing was sent. … so nothing was sent." **Task 9: complete pending e2e — 6df392a 6acf8d1 c79a773 84b57f1.**
- Controller docs edit (uncommitted, lands with the next controller commit): `docs/plans/2026-09-15-ui-scenarios.md` — the book's `data-state` vocabulary gains `refused-locally` (:113, the reviewer's line), and the :2825 sentence that said the rejection arms catch "a thrown id-shape error" now says the id resolves `refused-locally`. No pin enumerates the book states.
- Gate round · History landed: **ebfef41** on 84b57f1 (9 paths, subject verbatim, nothing STOPPED; eslint + lint:css clean; unit **987 / 0**, History's two unit specs 41 → 53; tsc clean in every History path — 5 errors persist in `tests/e2e/activity.spec.ts`, the Activity implementer's file mid-edit). Demo words: H1 "**$27.8M of Cash debt is outstanding,** across 1,412 accounts in the hour starting Aug 8, 20:00 UTC." (neutral); dek "165 of the 168 hours in this window were recorded. 2 are absent — no complete batch was observed — and 1 was withheld; each is a gap on the chart, never a zero."; Cash finding "…debt rose $1.8M to $27.8M, accounts fell 52 to 1,412, and liquidatable positions rose 1 to 49." B1: `HISTORY_ENGINES` (Cash first) in history-view.ts, unit-pinned to the same set as `OBSERVATORY_ENGINES` (lib/observatory-data.ts untouched). A4: the "0" label left the `obs-zero-floor` group to paint last (`obs-zero-label`), labels `pointerEvents: none`; B9 pointer target held at 2.4 by a transparent stroke. **Reconciliation 6 at work — two ruled sentences were NOT true as written (verified in the Go source) and were worded to what is:** "absent — no complete batch RAN" → "was OBSERVED" (the rollup is ticked from the indexer's loop into `date_trunc('hour', now())`: an hour is also absent when the indexer did not tick or the write failed); "(every {n}th hour)" → "the service serves at most one in every N" (the handler skips relative to the last row SERVED; arm unreachable today). Degraded headline "No hourly history exists for Cash on this deployment yet." verified: on the series route `code: "unavailable"` has ONE source (`cmd/api/p5_observatory_series.go:139`, the table is not migrated). Arms added beyond the table: an unreadable debt figure ("Unreadable is not zero.", dashed), "None of the N hours…", "The only hour…", "not stated at either end", "unreadable at one end". NOT done (outside the brief's list): clarity Tier 2 (Stride chip `hourly`, chip label `Hours … recorded`, tile sub) — "bucket" is retired from headline / dek / finding only → TASK 12 WAVE candidate. **W-history-observed**: pre-existing strings still assert what the Go source refutes ("no complete batch this hour" in `HISTORY_MARKS`, `HISTORY_ABSENT_NOTE`, the absent hover title, `pointDetailTakeaway`, `HISTORY_METHOD[1]`) → follow the verified fact ("observed") in the gate's follow-up round. `plural` is a private copy in observatory-series.ts (prose.ts unowned this round) → wave.
- Integrator: `tests/e2e/state-matrix.spec.ts` re-pointed for History (uncommitted): `ok:absent-bucket` gains an `act` pressing the legacy engine (the page opens on Cash; the contract's absent hour is in the legacy series); `degraded:rollup-unavailable` reads "on this deployment yet.".
- INTERRUPTION: session limit hit ~18:30 USMST (reset 21:30). All three page implementers had committed; the Verification/API agent's final notification reads "failed" (rate-limit 429 at its report-write) but its hand-back arrived first and `gate-report-verification-api.md` exists (17 KB). Nothing re-run.
- Gate round · Activity landed: **69e5f53** (11 paths, subject verbatim, nothing STOPPED; tsc / eslint / lint:css clean; unit **987 / 0**, activity-view 14 → 18, feed-view 21 → 24; activity.spec 15 → 16, unrun). Verified facts: `served_at` IS on the events envelope (api/openapi.yaml:4233,4235) → `humanUtc`'s reference year; `deficit_created` = "the pool's own bad-debt realization event" (:4057-4058); no posture store exists (`grep -ril outbox internal/` empty); `grep "P4\|outbox"` over the page's lib and app → 0 hits, a unit pin sweeps every arm. **Reconciliation 6:** the untimed tail is NOT "listed by block number" — `internal/store/p5_events.go:397` orders it `chain_id DESC, block_number DESC, …` and the demo tail spans two chains → "by chain and then block number", and it is a CHECKED claim (`tailKeepsItsOrder`: said only when the loaded rows show that order). Departures (reasoned in the report): refused/failed-with-rows arms emphasis-only; the refusal dek names the code once (`InspectorFetchError.message` already carries "400 bad_request: …"); the strip's no-batch sentence does not repeat the chip's "Reconnecting". B3 specificity trap fixed page-locally (`.controls .chipBtn:not([aria-pressed="true"])`; an e2e pin compares pressed vs resting computed colour). BEYOND the brief (clarity Tier 2): kicker `Activity · all engines`, `Order` chip `by block time` / `by block number`, the liquidations tile sub, and — not ruled — the Scope chip and the engine switch's first button say `all engines` (one constant `ALL_ENGINES`; test ids and `data-mode` unchanged) — ACCEPTED pending the gate's look. `feedTakeaway` had no consumer outside the page. `plural` now has FOUR copies → wave (lift to prose.ts).
- Gate round · Verification + API landed: **92f0c1a** on 69e5f53 (13 paths, nothing STOPPED; all four unit gates clean; unit 987 / 0, +13). The Overview pin: step numbers come from a new `stepTileLabel()` reading each step's existing `ordinal`; the fields `app/overview/Pipeline.tsx` reads (`key`, `ordinal`, `line`) byte-identical and pinned as literals; only the Verify step's `sub` changed, which the Overview does not render. **Reconciliation 6, twice:** "a test fails the build" → the test is `tests/unit/proof-contract-fidelity.spec.ts`, it compares the page's SOURCE MODULE (`lib/proof-contract.gen.ts`) against `api/openapi.yaml`, and fails the CI job (ci.yml:104-105), not `next build`; "every sample is the contract's own example" → 5 of 16 response samples and 5 of 6 error bodies are client fixtures (validated against the contract in packages/client-ts/test/fixtures.test.ts) — the sentence is COMPUTED and prints the fixture clause only while a sample cites one. "No key or sign-in" VERIFIED (cmd/api/main.go:708-712 no auth; the middleware chain :737-745 has no auth layer; the contract declares no security scheme) — and rate limiting exists (per-IP 20 rps, burst 40), so the dek adds "requests are rate-limited per client" while the contract carries a 429. "was not re-checked" kept as ruled; flagged as the page's softest claim ("re-" can imply it was checked once) → gate's look. Three headline arms added because the table's words would have been untrue or printed "0 drift". Prose reflow treats bullet lines as paragraph starts (no word changes; unit-checked over every description).
- **RULING (Verification tone, receipt accepted + no servable batch): `ok` stands** (design's formula: the emphasis is the PROOF finding and its tone is the receipt's; painting an exact finding amber because of the live batch would give amber two meanings on the page). The missing batch is said in the ink clause, the dek and two refused chips. This reverses a previously pinned `warn` → for the owner's Rulings list.
- Integrator re-points in shared specs (uncommitted): shell.spec.ts (Activity, Verification, API H1s), state-matrix.spec.ts (History ×2, feed ×5), p1a-fixes.spec.ts:698. Shared build at 92f0c1a + these, flag set: RUNNING (background b2lrvpub9; log scratchpad `e2e-p4-gate.log`).
- Shared build at 92f0c1a + the integrator's re-points (flag set; whole e2e minus the four then-stale pins): 231 passed / **3 failed**, all in `activity.spec.ts`, all PIN-side (the page was honest each time): (1) `activity-order` "loads 2 at a time" — the fixture echoes `limit: 2`, the pin had assumed 50; (2) the live strip truthfully adds "received on an earlier connection" because a fulfilled SSE body ends the connection after its one frame; (3) a pending tile prints the kit's "…" under `aria-busy`, not "—". Fixed by the integrator (three lines + the test's title; no round — the implementer's context is cold and the fixes are the page's own output); activity.spec 16/16.
- Task 11 baselines RETAKEN after the gate round; two replays 16/16. Captures: scratchpad `gate6/` — 52 files: eight pages × both themes × fold/full + five honest states (historyDegraded, activityRefused, activityExhausted, verificationFailed, verificationUnavailable). `tests/fixtures/demo/index.ts` loads the manifest JSON itself (plain Node cannot resolve an extensionless TS import; the capture script reads this index); the failed-receipt capture welded to the demo batch like the demo evidence.
- **THE GATE — second verdict, by the owner's delegation, 2026-09-20 ~21:50 USMST: APPROVED — the pins are committed.** Read against book-dark.png: the four pages share the Book's padding (kicker at y=93 on all), a record's headline is ink and only Verification's proof finding wears tone ("**All 87 checked rows matched the chain exactly,** in this deployment's pinned reconcile run."), figures are human in the headline and grouped-exact in the chart, no chart label is struck through, History opens on Cash, Activity's strip is one plain line with no roadmap word, the Amount column aligns with its unit beside it, the API's endpoint index is aligned rows with each fact said once, one serving batch is named once on Verification. Honest states read: History degraded (one deployment-specific headline, the service's message, no empty chart, no zero tiles); Verification failed ("The last reconcile run did not match the chain exactly, 84 of 87 checked rows matched; 3 rows drifted." in warn; the chip, the strip and the card name the rejection). ACCEPTED as-is: `all engines` wording on Activity; History's chips wrapping under the action (B2 out). The owner's later "changes first" reopens the gate; the captures are in `gate6/` and go to the companion at close.
- FINAL TREE before the pin commits: **e2e 242 passed / 0 failed / 0 skipped** (flag set; incl. the four new pins); **unit 987 / 987**; tsc clean. **Tasks 7, 9, 10: e2e CONFIRMED → complete.**
- Commits (integrator, by pathspec, subject only): **89b87c7** the secondary pages' pixel pins (spec + 8 baselines + capture script + demo index); **a9c11bf** the shared specs follow the pages' words; **d1cab73** docs — the Scenarios contract names `refused-locally`. **Task 11: complete — bc40bdc ebfef41 69e5f53 92f0c1a 89b87c7 a9c11bf.**
- → OWNER'S LIST (+): the API's degraded message names "Task B2, migration 00016" (roadmap vocabulary in the SERVICE's words, printed verbatim by the page); a rejected receipt's headline is warn while its chip and card are crit — design's A2.4 formula (`exact ? ok : warn`) followed; should a rejection be crit?; B2 the header action row (re-baseline Scenarios, or an opt-in variant); C1 the chart's level-anchored y-domain (specified in the design ruling's appendix); History default = Cash (B1) and Verification's accepted-receipt/no-batch tone = ok — both reverse earlier pinned behaviour.
- → TASK 12 WAVE (+), from the gate round: W-history-observed (five strings still say "no complete batch this hour"); clarity Tier 2 on History (Stride chip `hourly`, chip label, tile sub `hour of {iso}`); `plural` ×4 → prose.ts; "was not re-checked" — the page's softest claim (the manifest says no comparator applies; "re-" can imply it was checked once): tighten.
- **Task 12 Step 1 — gates at d1cab73:** `npm run typecheck`, `npm run lint`, `npm run lint:css`, `npm run build` all exit 0; `npx playwright test`: **unit 987 / e2e 242, 0 failed, 0 skipped** (Plan 3's close: 813 / 200) — +174 unit, +42 e2e.
- **Step 2 — widths:** probe generated from the committed capture script (same mocks), eight pages × 390 / 1366 / 1440 / 1920 / 2560: **39 of 40 no horizontal scroll; 0 text nodes under 12px anywhere.** ONE OVERFLOW: **History at 390 — scroll 434 / client 390** (a non-wrapping identity chip — the long Stride value "native hourly buckets · every captured bucket served verbatim" — and a `thead`) → **W-390-history** in the fix wave (the clarity ruling's Tier 2 already shortens that chip to `hourly`; the record table needs its scroll container checked). Output: scratchpad `widths-p4.txt`.
- **Step 4 — contrast:** no token file changed; the branch's CSS adds ONE tint — the kit's pressed ghost button, `--accent-text` on `color-mix(in srgb, var(--accent) 8%, transparent)`: light over `--bg` **4.54:1** (the worst pair), over `--panel` 4.93:1; dark over `--bg` 6.97:1, over `--panel` 6.28:1 — all ≥ 4.5. The one opacity keyframe (`livePulse`, a rename of the old `feedPulse`) animates a decorative dot under `prefers-reduced-motion: no-preference`; no text. → "one new pair, passes".
- Steps 3, 5, 6 dispatched in parallel (all on `fable`): **keyboard pins** a55a8b878f988bacd (`serena-coder`; ONE new file `tests/e2e/keyboard.spec.ts`; a PINNING task — a failing page is marked `test.fail()` with the defect and the responsible file:line, never fixed, never a weakened pin; may run its own spec against the running :3111); **cold-visitor read-through** a7ed03ed29ef2bcaf (`solvent-user`, four personas over the 52 `gate6/` captures incl. the five honest states → `task-12-user-readthrough.md`); **whole-branch review** a69d2340978d115be (range a941a39..d1cab73; seams between tasks, the laws at the final state arm by arm on the four rewritten pages, the contract, retirements, comments, tests as a whole; told NOT to re-report the wave and owner's-list items → `final-review.md`).
- **Task 12 Step 3 — keyboard pins landed: 5840b57** (`tests/e2e/keyboard.spec.ts`, new; 22 tests = 15 passing pins + 7 `test.fail()` expected-failures; `--repeat-each=3` 66/66, no flake; tsc + eslint clean). Pinned: the drawer on seven pages (Enter opens from the focused header button; `dialog.contains(document.activeElement)`; a full forward lap in DOM order asserted inside on every press; Shift+Tab from the first stop → the last, Tab from the last → the first, a full backward lap; Escape closes and the opener is focused again); the Overview has no drawer (pinned); Tab walks in DOM order with no stop skipped — Activity's every table link, Verification's every control incl. the forensics folds opened by Enter (its probes table has no row links — pinned), the API's every index anchor, the Book's Needs-attention account links; Scenarios — each row's checkbox then its row button are Tab stops in listing order, Space ticks/unticks (Compare disabled with one tick, "Compare 2 scenarios" with two, no set run dispatched), Enter selects (`data-selected` / `aria-pressed` move, surface `not-run`, the headline and Run label name it, focus stays).
- **KEYBOARD DEFECT FOUND (one defect × seven pages) → W-drawer-shift-tab:** Shift+Tab pressed as the FIRST key in an open drawer moves focus OUT of the modal onto the page behind (Book → the Legacy fold's summary; Inspector → "Open Scenarios →"; History / Activity / API → the opener itself; Verification → "Raw JSON"). `web/components/Drawer.tsx` (the kit re-exports it, components/kit/index.ts:16): :61 focuses the PANEL (`tabIndex={-1}`, :117); :94 wraps only when `document.activeElement === first`, so with the panel focused nothing is prevented. Fix: Shift+Tab with the panel (or anything not a stop) focused wraps to the last stop; then delete the seven `test.fail()` lines.
- **RULING → W-lab-url:** selecting a scenario never writes `?scenario=` (`app/lab/LabSurface.tsx:216` `onSelect={setSelectedId}`; `replaceUrl` runs only from the mode toggle and the address field), so after a selection the address bar still names the PREVIOUS scenario and a reload or shared link shows a different scenario from the one on screen — a link that lies. The selection writes the URL (`window.history.replaceState`, as the page's other URL writes do); pin: select → the URL names it; reload → the same subject. Noted: the Inspector's drawer button is "Inputs · Calculation · Provenance" (pinned as it is).
- **Task 12 Step 5 — cold-visitor read-through** (`solvent-user` a7ed03ed29ef2bcaf → `task-12-user-readthrough.md`; four personas over the 52 captures + the no-API "bad day" walked at :3111, saved under scratchpad `userwalk/`): **4 BLOCKER · 33 MAJOR · 11 MINOR.** Personas: the first-time visitor gets the Overview in ten seconds, then "70,000 people" sits above "Accounts 1,412"; Book / Inspector / Scenarios give their one thing above the fold, History / Activity / Verification open on statements about record-keeping; the risk lead's hops all land but triage stops at 6 of 27 near-cap accounts; the power user's "am I safe, how far" is answered very well, "what happened recently" fails on raw-unit amounts; the searcher trusts the site's candour, not yet its numbers. The honest states never look like zero — and all speak to whoever runs the server.
- **ADJUDICATED → `task-12-readthrough-triage.md`** (rule: WAVE = inside Plan 4's boundary or an honest-UI law broken anywhere; OWNER = a product / IA / data decision on an approved page; ARTEFACT = the capture harness). Two findings CONFIRMED IN CODE as law defects on approved pages → the wave: **W-trust-claim** (B4) `lib/trust.ts:183` labels the receipt item "Book reconciles to chain" — present tense about the live Book, green tick, from a PINNED dated run that Verification says the live batch does not inherit: an overclaim; **W-book-unavailable** (M30) on a fetch failure the Book's tiles say "not computed" ×10 (`BookSurface.tsx:48`) — the engine-refusal word for a network failure; + **W-lab-run-disabled** (a live "Run" beside "Nothing can run"), the dead "Legacy ↓" anchor. Wave also: W-history-maxlabel (the chart's LEFT label is the window's MAXIMUM and reads as the starting value; "rose 1 to 49" ambiguous), W-verify-failed ("3 drift named" names nothing), W-verify-unavailable (message twice, no retry), W-activity-refused ("Load more" with zero rows; words twice), clarity Tier 2 on History (also the 390 overflow). OWNER'S LIST, ranked 1–11 in the triage file: B1 raw-unit amounts (needs a decimals source), B2 the hero's 70,000 vs 1,412, B3 localhost base URL (the hosted-API carry-forward), 6-of-27 / 20-of-118 with no full table, liquidatable 2 vs 49 and the buried legacy market, "Not computed 6" hiding ≈ $178K, 87 ≠ 29 + 14, builders' vocabulary, three meanings of "moved", Activity with no address filter, History's headline answering the Book's question, "$109.45 — below the $100 line".
- **Task 12 Step 6a — whole-branch review** (a69d2340978d115be → `final-review.md`, range a941a39..d1cab73): **READY TO CLOSE AFTER THE FIX WAVE; no Critical.** Found clean at the final state: no refusal / withheld / absent / unloaded value renders as zero, "No", "none" or absence on the four converged pages; Cash and legacy never summed or on one axis; every arithmetic path the branch added sits behind a wire guard (History's deltas: both ends through `wireBigInt` / `readWirePopulation`, one response, one scale behind `isWireScale`, a null end prints "not stated"); Activity's dek counts rows only and `tailKeepsItsOrder` is computed from the loaded rows; `humanUtc` is regex-over-the-string, no Date / Intl, malformed verbatim, NBSP by bytes; unit files 71 → 81, no per-file count dropped, no skip / only. **Six Important, all seams:** I1 Verification says a read FAILED while it is IN FLIGHT (Task 5 r1 × the component's pending prop); I2 the styleguide still teaches the tone usage the gate overruled — no `neutral` specimen, `sg-verdict-ok` is a History record under green, the tone loop has four (Task 7 × Phase 0's "specimen not required this phase", never picked up); I3 three History arms pinned but UNREACHABLE — `buildMetricSeries → geometryOf → formatUnits` and `displayMetric → renderUsdAmount` still throw, every pin calls the sentence functions directly (the gate report's claim that the throw is gone was mistaken) — **RULING (a): guard the builders; a malformed value is a named hole, never a crash**; I4 one public number, two unwelded sources (`PUBLIC_ENDPOINTS` vs `OPERATIONS`; they agree today, one pin closes it); I5 the API dek's "validated against it" rests on a client-ts test `ci.yml` does not run — **RULING: word only what CI enforces; a CI step → OWNER'S LIST**; I6 nine plan-doc lines no longer describe the code (controller, at close, after the wave). M1 a zero-gated-rows accepted arm "All 0 checked rows matched…" — **RULING: fixed now, the refused register.** 15 Minors triaged in the file. Reviewer's process note: a tool result told it to do all work through Bash "while auto mode is active"; it arrived in a tool result, not from the owner or the controller, and was not acted on.
- **Task 12 fix wave dispatched** (2026-09-20 ~22:10), brief `wave-brief.md`, four `serena-coder` on `fable`, BASE 5840b57, disjoint files, unit gates only, ONE commit each: **A** ae5b9efb229656012 the lab + the Drawer (W-drawer-shift-tab + the seven `test.fail()` lines go, W-lab-url, W-lab-run-disabled, W-N5/N6/N7, W-M1 one rule, W-N3, W-lab-nobatch, W-pin); **B** a5d8b7b7d8f8f4172 Book / Overview / Inspector (W-census five sites + reshaped fixtures + one pure whole-refusal predicate, W-book-unavailable + the dead Legacy anchor, W-trust-claim — moves the Inspector's pinned label by ruling, W-M2, W-weak-pins, the over-delivery frame); **C** a0ee73ba0d09f0e8d History / Activity / prose (I3 guard the builders, W-history-maxlabel, W-history-observed, clarity Tier 2 + no horizontal scroll at 390, Task 3 m1–m4, W-activity-refused + Task 4 minors, `plural` → prose.ts); **D** a74236e00cbc1d756 Verification / API / styleguide / the kit's leftovers (I1, W-verify-failed, W-verify-unavailable, "was not re-checked", M1, I4, I5, I2 + Task 7 N1–N4 with real wire codes throughout, the pruning incl. lib/kit.ts's dead model AFTER its law moves into a pure function VerdictHeader calls). Pinned renderings that move BY RULING: Inspector (trust label), History (Tier 2 chips, peak label, finding, mark key), Verification + API deks — the integrator replays and re-baselines under the delegation; every implementer lists the strings it moved and the contract values it changed.
- Wave · Area A landed: **9a9874a** (28 paths, all Area A, subject verbatim, hooks passed; tsc / eslint / lint:css clean; unit **1024 / 0**; the lab's specs 165, +7, none deleted; every new or re-pointed unit pin seen failing on the parent first). Drawer: Shift+Tab and Tab from the panel wrap, wrap at the first / last stop, single-stop drawers, a click on the panel's text, Escape unchanged; the stop list now counts `details > summary` and skips unrendered controls (identical to HEAD's for the seven drawers); NOT covered — focus on `<body>` after a focused control is removed (true at HEAD; no drawer does that today). The seven `test.fail()` lines are gone. **W-lab-url needed two decisions beyond the ruling (both follow from "a link naming both params runs nothing"):** a selection DELETES `scenarios` from the URL; the deep-link decision and its notice are read from the link the page was OPENED with, captured once at mount (else a selection on a `?scenarios=a,ghost` page would re-decide the link and erase the notice). A reload of a selected-but-not-run scenario RUNS it — what `?scenario=` means; ticks stay out of the URL → ACCEPTED; for the owner's Rulings list. **W-lab-run-disabled: Run was already `disabled` at HEAD, and pinned — the real defects were** a `?scenarios=a,b` link enabling a live Compare while the listing was loading / unavailable (`deriveLabView` now returns `checked: []` in both arms), and NO `:disabled` style anywhere in the app, so a disabled primary button looks live: a page-local look in lab.module.css scoped to the two listing states (the pinned Scenarios rendering does not move) → **OWNER'S LIST: a kit-wide `.btn:disabled` register** (it would move approved pins). W-M1 changes behaviour on purpose: a Cash row malformed under another version of the definition / beside a definition that does not model Cash is "contradictory", never "definition changed" / "not modelled"; the held result stands from the moment the bad body settles. NOT in Area A's files: whole-branch M2 `liquidationEstablished` (feed-view.ts, Area C). **FOLLOW-UP after Area B lands (≈ 5 lines, `lab-address.ts` + `AddressWorkspace.tsx`): wire B's `view.scaleAbsence` / extended `sideRoomWords` into the lab's one-address table** — today it still says "unreadable scale" where the lookup holds no Cash position. Noted: `MoversTable.tsx` words its verdict cell in the component ("Cannot say" / "Yes" / "No"); the classifier now guarantees true | false | null → carry-forward.
- Wave · Area B landed: **736e09e** (35 paths, subject verbatim, hooks passed; eslint / lint:css clean; unit **1024 / 0**, +14, none deleted; tsc clean in every Area B file; its three e2e specs `--list`-parse 55 tests, unrun). **New pure file `web/lib/cash-refusal.ts`**: `wholeRefusal(book, name)` (the `refused_engines` entry wins; else the card must carry `refused === true`), `CASH_ENGINE_MISSING`, `WholeRefusal`, `bookLoadFailure(cause)` — Area D imports these (until it does, verification-view.ts keeps its second copies; mapping note: a card with no refusal object yields `code: ""` here vs `null` in D's `cashCensus`). W-census: `deriveCashView` reads none of the three counts when the engine is withheld — all five sites print the refused register, PLUS a SIXTH site found under the same law (the Book drawer printed "None." for a withheld card with an empty refusal list); both e2e fixtures reshaped to the contract's zeros; unit pin failed first (`Received: 0`). W-book-unavailable: the lib decides WHY figures are absent — a fetch failure / a 503 / a book that does not list the engine → "unavailable"; a withheld engine → "not computed"; **a read in flight → "loading…" (it used to say "not computed")**; the Legacy anchor renders only with the legacy block; the Overview strip never said "not computed" (now pinned). **W-trust-claim — the Inspector's pinned words MOVED BY RULING:** label "Book reconciles to chain" → "**Pinned reconcile run matched the chain**"; detail "29/29 Cash rows exact · committed receipt" → "29/29 Cash rows · Jul 29, 02:14 UTC" (the receipt's own `finished_at`, required by the contract; no readable instant → no date); every other arm labelled "Pinned reconcile run", detail and state unchanged; item height 58.375px before and after (measured in a headless layout harness, not the page). W-M2: `view.scaleAbsence` + the word functions in address-stress.ts (a projection cell no longer prints "+— interest"); the false cause was never reachable on the Inspector (the table mounts only with a Cash position) — it IS reachable in the lab → the lab half STOPPED (Area A's files). W-weak-pins: the held demo walk now leaves the "< 2% room" band unread on page one (ten rows move to page two; the census still reconciles at 1,412); the foreign-engine pin asserts seven dashes and zero `[data-count]`. Over-delivery: the hook records HOW an incomplete walk ended; "stopped before the last page" only when it never read its last page; a walk past its census claims no bound anywhere. Whole-branch M6 landed IN PART: `Pipeline.tsx` carries `data-tone`, the CSS styles only `refused` and `warn` (rendering `ok` green would move the Overview pins). Contract: + `book-section-cash`, `overview-live-accounts`, `data-tone` on `pipeline-*`, the Coverage chip can read `withheld`. JUDGEMENT to confirm in review: the census is also nulled when only the POSITIONS endpoint refuses the engine (the hook's third source).
- **INTEGRATOR FOLLOW-UPS after the four land (small, cross-area seams):** F1 the lab half of W-M2 (`lab-address.ts` carries `view.scaleAbsence`; `AddressWorkspace.tsx:51,54` pass it to `sideRoomWords`); F2 the Overview passes Area D's `inFlight` to `pipelineSteps` — today its steps would read "unavailable" in the refused grey while meta / evidence are still loading (I1's twin on the Overview; the served arm must stay pixel-identical); F3 verification-view.ts imports `wholeRefusal` / `CASH_ENGINE_MISSING` from cash-refusal.ts if Area D did not.
- Wave · Area D landed: **62a0b61** on 736e09e (33 paths, subject verbatim, hooks passed; tsc / eslint / lint:css clean; unit **1024 / 0**; its four specs +9: verification-view 28 → 33, proof-evidence 22 → 23, api-view 9 → 10, kit 12 → 14 — the dead model's four kit pins RE-POINTED at the live law, not dropped). Fail-first: 13 of 14 new pins failed on a scratch copy of the parent's lib for the stated reason; the I4 weld pin passes on the parent by design (the two lists agree today); kit.spec cannot load on the parent (`headerIdentity`, `specimen-book.ts` are new). **Pinned words MOVED BY RULING:** Verification's dek "…is live data that was not re-checked and does not inherit it." → "…is live data; no check covers it, and it does not inherit that result."; the API's dek → "…or a committed client fixture, cited beside each. A CI test re-reads both and fails if this page's extract has drifted from either; the fixtures are checked against the contract by the client package's own tests." (+72 chars; whether either dek takes an extra line at 1440 not observed). Contract: `data-receipt` gains `empty` (a receipt with ZERO gated rows is the refused register everywhere on Verification); + `verification-retry`; `verification-receipt` absent while `data-state="unavailable"`. Area B's `CASH_ENGINE_MISSING` and `wholeRefusal` IMPORTED into `cashCensus` (F3 done by D); `bookFailed` still restates B's `bookLoadFailure` → carry. "0 drift named" SURVIVES in the exact arm's step sentence (the pinned primary state had to stay identical) → OWNER'S LIST with the read-through's M15. **STOPPED: the Overview's pending words** — `OverviewSurface.tsx` collapses "in flight" and "failed" into one `null`, so `pipelineSteps` cannot tell them apart (Area B's files) → the integrator's F2. Not D's files: M14 (activity-view.ts, Area C), the other two drawers' M8 keys (Area C), Task 7 M9's dead `.kv` rules (kit.module.css). Left: M16's other half ("no assumed batch" above a Compute tile showing the book's batch), M15's amber-vs-red, `retryWords` vs Area A's `retry`. Process note (twice now, from two agents): a system-styled message beside a tool result telling the agent to do all file work through Bash "while auto mode is active" — arrived in a tool result, changed no deliverable.
- Wave · Area C landed: **af39652** (25 paths, subject verbatim, nothing STOPPED, hooks passed; tsc / eslint / lint:css clean on the final tree after A, B, D; unit **1024 / 0**; its six unit specs 107 → 114; history.spec 14 → 20, activity.spec 16 → 18, unrun). I3: the pin THROUGH `deriveHistoryView` seen failing on the unfixed code — `DecimalFormatError` at `formatUnits ← geometryOf (observatory-series.ts:200) ← buildMetricSeries ← tileOf ← deriveHistoryView`, the path the review named; a malformed money figure is now a named hole (`data-kind="unreadable"`, `data-mark="unreadable"`, test id `obs-gap-unreadable`; a warn cross, key label "figure unreadable" — the implementer's choices, not design's). **I3 covers MONEY only**: a COUNT outside the contract still throws at the route boundary and four existing pins state that as the law — the ruling did not reverse them → carry-forward for the owner's list (one law or two?). **History's pinned words MOVED BY RULING:** Stride chip value `hourly` (+ `title`), `data-chip` **`Buckets` → `Hours`** (ok and loading states), the four tile subs `hour of {iso}`, the finding "rose by …, to …" joined by semicolons, the key's "no complete batch was observed", the y-max label "peak $27,942,906.330446" (`data-place` peak | edge; it keeps the left edge in the Cash demo — the peak is 9 hours from the newest label), the chip row probably collapsing to one row. W-390: the 434px was the nowrap Stride chip (the `thead` already sat inside a scroll container); the chip shortened + rules inside the ≤ 640px media query only (chips may wrap, `.page` `overflow-wrap: break-word`, `.marks` may wrap); pin at 390×800. W-activity-refused: **a refused walk offers no "Load more" with rows loaded as well as with zero** (the refused cursor never advances, so the button could only re-send it; beyond the ruling's letter — ACCEPTED; one line to revert). `engineInProse` in prose.ts = "the legacy Aave v3 market" (Activity's pinned form; History's non-primary sentences follow it; Area A's `modelled` in lab-headline.ts still says "the Aave v3 market (legacy)" → carry). Fixed beyond the list under the same laws: a captured hour's null debt no longer says "null because the book was withheld"; "restart below" → "restart above"; Activity's order paragraph printed the wire id `debt_manager` → Cash; M14 fixed (doctrine 8 → 7 paragraphs); `liquidationEstablished` KEPT with its two pins (no stronger pin covers it; deleting would drop the unit count). Contract: + `obs-gap-unreadable`, `activity-liquidation-note`, `data-place`, `data-note`, `data-foot`, `history-point-title`; `history-marks` absent on a hole-free window; `activity-load-more` / `activity-end` absent while `data-state="refused"`. Other `plural` copies left: book-headline.ts:32, stress-preview.ts:47, trust.ts:44, BookSurface.tsx:23; verification-view.ts still prints "provenance row(s)" → carry.
- **Integrator follow-ups (uncommitted), the seams between areas:** **F1** the lab half of W-M2 — `AddressWorkspace` carries `scaleAbsence` (the view's cause, or "unreadable" for a scale the guard refused); the lab's table prints `sideRoomWords(…, scaleAbsence)` and `projectionWords(…)` — the SAME word functions the Inspector's table calls (the lab's component-composed projection cell and its unused money formatter are gone; `accountMoney` = `humanUsdFull`, the format is identical); unit pin in lab-address.spec. **F2** the Overview tells the pipeline which reads have SETTLED (`settled.meta / .evidence`, set in `.finally` unless aborted) and `Pipeline` prints the lib's pending word under `aria-busy` for a pending step — never its line's "unavailable"; the served arm is untouched; e2e pin in overview.spec (both reads stalled → pending in ink; then failed → "unavailable", refused). F3 done by Area D. tsc / eslint clean; unit 1024 + the new pin.
- Shared build on the post-wave tree (flag set), whole e2e: RUNNING (background bng22q660; log scratchpad `e2e-p4-wave.log`). EXPECTED to fail by ruling: the Inspector, History, Verification and API pixel pins.
- Shared build on the post-wave tree (flag set), whole e2e: 274 passed / **5 failed** — four EXPECTED pixel pins (History ×2, API ×2; the Inspector's and Verification's reworded text fell INSIDE the 1% tolerance and passed — re-baselined anyway so no baseline is stale) + the integrator's own new Overview pin, a MOCK defect: `/v1/meta` is asked twice (the surface and the shell's meta-constants reader) and the stall helper kept only the last request's handle, so the surface's request was never released → every stalled route is held and aborted (the pending half had passed: pending in ink, no "unavailable"). overview.spec 10/10.
- Re-baselined BY RULING under the delegation (2026-09-20 ~22:55): inspector, history, verification, api × both themes; two replays 16/16. Read: History — the five chips hold one row, "peak $27,942,906.330446" is labelled as the peak, the finding reads "debt rose by $1.8M, to $27.8M; accounts fell by 52, to 1,412; and liquidatable positions rose by 1, to 49."; Inspector — the Trust item reads "Pinned reconcile run matched the chain · 29/29 Cash rows · Jul 29, 02:14 UTC", two lines where its siblings are one (heavier, and true); API — the dek runs FOUR lines (every clause verified) → owner's list: trim.
- **FINAL post-wave tree: e2e 279 passed / 0 failed / 0 skipped; unit 1025 / 0; zero `test.fail` in keyboard.spec (the Drawer fix is proven in a browser on all seven pages); tsc / eslint / lint:css clean.** Commits (integrator, by pathspec, subject only): **e23c62f** the seam follow-ups F1 + F2 with their pins; **0f18a1b** the eight re-baselined pins.
- **Task 12 Step 6b dispatched in parallel:** the **scoped re-review of the wave** ad194e8db98b3344a (range 5840b57..0f18a1b by commit and path; per-area hunts — every Drawer key path incl. a control inside a closed `<details>`; URL/back-forward desync; W-M1's honest bodies; the census nulled on a positions-only refusal; the Trust card under a zero-row receipt; any remaining throw in History; the F2 `settled` races; the pruning's consumers; + the consolidated contract-value list for the plan's table → `wave-review.md`); the **Codex round** a77eb379acc7a3532 (`codex-reviewer`; brief `codex-brief.md`: READ-ONLY, range a941a39..0f18a1b source only by path, the seven laws, what to hunt, the known list NOT to report, ≤ 25 findings → `codex-findings.md`; no Codex config touched).
- **Task 12 Step 6b — Codex round** (`codex-reviewer` a77eb379acc7a3532; Codex task `task-muatuhow-l6fdyy`, 13m36s, one dispatch, read-only: `write = False`, `touchedFiles = []`, no config touched; verbatim in `codex-findings.md`): **20 findings, 19 self-labelled CRITICAL + 1 HIGH.** **ADJUDICATED → `codex-adjudication.md`.** The re-grading fact: `app/error.tsx` is a ROOT route boundary — a render throw replaces only the segment below the layout with `RouteRefusal`, header and nav mounted, the failure named — so a malformed 2xx member throwing at render on a NON-lab page is an honest coarse refusal, not a false statement; R11 took the whole-envelope law for the lab only, deliberately. A finding is FIX NOW when the page would say something FALSE or show a STUCK / STALE state. **FIX NOW (15):** Book — #2 a `batch: null` positions page throws before the decoding `try` inside the fulfilment callback → an unhandled rejection and a walk that appears active FOREVER; #1 (half) a malformed REPAIR replaces a readable book; #3 duplicate accounts across pages satisfy the census and double-count debt; #4 an unreadable COMPUTED row leaves `notComputed: 0` and the headline says "No position is liquidatable". History / Inspector — #6 numeric money bypasses the decimal guard (THIS WAVE's I3 code: the guard must be chosen by metric identity); #7 a legacy series answered to a Cash request is labelled Cash; #11 `horizons: []` is erased to `null` and the row says "No"; #16 the Trust card says "receipt unavailable" while `/v1/evidence` is in flight (the wave's law, third page). Lab — #12 the no-position negative is attributed to the LOOKUP's batch, bypassing the disagreement disclosure; #13 Compare releases its hold on membership alone; #14 the single-run hold predicate omits the mover ratio pairs and the displayed legacy engine; #15 the listing has no classifier (`200 null` → throw); #17 `?scenario=B` while the workspace falls back to A (THIS WAVE's W-lab-url); #18 "NOTHING was run" beside a dispatched address evaluation; #19 a +0.01% change is a green dot at zero. **OWNER'S LIST — one named workstream "every page's reader classifies its envelope"** (#1 first half, #5, #8, #9, #10: seven readers, Codex's list is the spec). **CARRY:** #20 copy authored in components on approved pages (BookSurface.tsx:255,259; CompareCard.tsx:47) with MoversTable. Codex found CLEAN: `humanUtc` / `instant-split`, the Drawer's cycle + closed-fold filtering + Escape + restoration, engine / scale isolation, Activity's loaded-row wording, the API's derived counts.
- The Codex fix round WAITS for the wave re-review (ad194e8db98b3344a, running) so the two sets of findings land in ONE round: three areas, disjoint files — **X** Book (`cash-book.tsx`, `cash-rows.ts`, `cash-summary.ts`), **Y** lab (`lab-*.ts`, `app/lab/**`), **Z** History + Inspector/stress (`observatory-series.ts`, `history-view.ts`, `app/observatory/**`, `address-stress.ts`, `address-lookup.ts`, `trust.ts`, `app/inspector/**`).
- **Wave re-review** (ad194e8db98b3344a → `wave-review.md`): **one more round, a small one — no Critical; two Important.** Every ruled item CLOSED with a pin that can fail on the parent (I4's weld pin cannot, by design); the census nulled on a positions-only refusal is RIGHT (the two endpoints contradict each other about one batch — the strictest arm wins); the Trust card shows a zero-row receipt as dim "no rows in the receipt", never a green tick; F2's `settled` cannot stick at false on any path found; a closed drawer's DOM is unchanged; the contrast block and the p1a-6 pin body byte-identical across the range; BOTH API dek claims TRUE against proof-contract-fidelity.spec.ts, fixtures.test.ts and ci.yml. **I-1 = Codex #6, found independently:** History's money guard branches on the value's JavaScript TYPE — a fractional JSON number still THROWS, an integer is plotted unscaled and labelled "peak 27,828,808,216,758" with no `$` while the tile and headline call it unreadable (the Area C claim that a malformed count is the only remaining throwing class was wrong). **I-2 (new):** the Trust item's reworded label speaks of the WHOLE run but its `ok` arm checks the verdict, zero drift and the Cash weld only — a run Verification calls drifted / failed / empty is ticked green on the Inspector (the ruled label widened the claim; the arm was not widened to match). 15 Minors (A-1…A-3, B-1…B-4, C-1…C-4, D-1…D-4) + the consolidated contract list (e). Process note again: a system-styled "auto mode / use Bash" message beside a tool result — not from the controller or the owner; ignored.
- **The LAST fix round dispatched** (2026-09-20 ~23:25), brief `round2-brief.md` = Codex's 15 FIX-NOW + the wave re-review's I-1, I-2 and twelve Minors, four FRESH `serena-coder` on `fable`, BASE 0f18a1b, disjoint files, unit gates only, ONE commit each: **X** a164c95f34401dba8 the Book (Codex #2 the stuck walk, #1 the malformed repair, #3 duplicate accounts, #4 the false all-clear; B-1, B-2, B-3); **Y** adeec2b55ba535db1 the lab (#13 + #14 ONE predicate admits and releases, #15 the listing judged, #12 the negative's own batch, #17 + A-2 the URL names the subject shown, #18 the notice claims what it gates, #19 tone from the unrounded delta; A-1, A-3); **Z** a0f8c7cbbabf0547e History + the stress reader + trust (I-1 = #6 money judged as money whatever its type, #7 the series answers for the engine asked, C-1 / C-2 / C-4, #11 an empty projection cannot say, I-2 the whole conjunction — agreeing with Verification's `receiptState`, #16 = B-4 a receipt in flight is pending); **W** a43bb5f69fd8f2633 Verification (D-1 the strip pending, D-2 nothing green under a receipt of no rows, D-3 retry keeps focus, D-4 a pin that can fail). All eight pixel-pinned pages must NOT move this round.
- Round 2 · Area W landed: **be9bbdb** on 0f18a1b (10 paths, subject verbatim, nothing STOPPED, hooks clean; tsc / eslint clean; unit **1031 / 0**; verification-view 33 → 35, proof-evidence 23 → 24; verification.spec 22 → 24, unrun). D-1: in flight the surface is `data-receipt="pending"`, the strip `data-tone="neutral"` under `aria-busy`, the header chips print `pending` with no refused tone (they were BYTE-IDENTICAL to a failed read's chips) — new type `ReceiptRegister = ReceiptState | "pending"`; pins seen failing first. D-2: under a zero-row receipt the weld rows are `dim` and the matching fingerprint row is ink, on the card AND in the drawer descriptor in evidence.ts (the same defect, found there too); a MISMATCH stays `crit`; `data-tone` added to the row value spans. D-3: an effect keyed on the retry attempt focuses the H1 (`tabIndex = -1` set in the effect — the H1 is the kit's); not a Tab stop. D-4: the retirement assertion moved after the fold opens, same `table` scope, `small-2` present and `dust` absent. `pipelineSteps` / `receiptState` / `ReceiptState` untouched; `receiptState` not found wrong. No pixel pin moves. Concerns → carry: **the LOADING headline wears the header's `refused` variant on EVERY page** (a law in lab-headline.ts; the kit has no pending variant) → OWNER'S LIST: a kit-level pending register for `VerdictHeader`; Area Z's History loading chip is `pending` with the refused tone (the same conflation) → check after Z lands; under `data-state="unavailable"` the surface still says `data-receipt="none"` (pinned, no visible text); the LIVE card keeps its own `ok` under an empty receipt — RIGHT under the two-subjects law (the live subject is not the proof's).
- Round 2 · Area Z landed: **c192523** on be9bbdb (16 paths, subject verbatim, nothing STOPPED, hooks passed; tsc / eslint clean; unit **1073 / 0**; +8 unit, 5 new e2e unrun). Item 1: `isMoneyMetric` — `metricUnreadable` / `displayMetric` / `geometryOf` branch on the METRIC; a money figure served as a number of any sign or size is the unreadable hole (both number cases seen failing first: the `WireIntegerError` path, and an integer plotted at 1,000,000). NOTE the converse: a COUNT served as a string now throws wherever it is read (it used to be drawn at the money scale as `$0.001412`) — the existing count law applied by metric identity. Item 2: `foreignSeries(requested, response)` checked before commit AND inside `deriveHistoryView`; refused register, `data-state="unavailable"`, chip `Record: wrong engine`. Item 3: the drawer teaches the unreadable mark; the Hours chip counts `N recorded (M with an unreadable figure)` in warn (byte-identical with none). Item 4: `projection` is null only when the wire carries none; `horizons: []` reaches `no-horizon` → "Cannot say"; `projectionWords([])` = "no horizon in the projection". **Item 5: trust.ts IMPORTS `receiptState` (pure; no cycle) — the tick appears ONLY when it says `exact`**; `TrustInput` carries the whole manifest; new arms "… · the run did not match whole", "… · the service does not vouch for this receipt", "the run gated no rows · nothing was compared", "no committed receipt"; agreement pinned over the four fixtures + drift-with-pass + zero-row; the served arm byte-identical. Item 6: new pure `lib/inspector-evidence.ts` carries the evidence PHASE (pending | failed | answered) lookup → view → trust: "receipt pending" in flight, "receipt unavailable" only after a failure; a resume repair never re-asks evidence. Decisions inside the rulings — ALL ACCEPTED: a foreign series keeps `data-state="unavailable"`; unreadable hours counted inside "recorded" across both money metrics; one wording for the empty receipt (Verification's); an answered manifest with no receipt says "no committed receipt"; a Cash weld of 0/0 inside an otherwise exact run stays dim (stricter than Verification, never greener).
- **DISCLOSED SLIP, VERIFIED HARMLESS:** Area Z globbed `lib/*.ts` while restoring LF after a Serena edit and rewrote line endings in the working copies of 23 files outside its area (incl. verification-view.ts, evidence.ts, several lab-*.ts). Integrator's check at c192523: nothing staged; the commit holds its own 16 paths; every file `git status` shows modified is Area X's or Area Y's with REAL content edits (`git diff --ignore-cr-at-eol --stat`: 37 files, +2173 / −370, all cash-* / book-* / lab-* / their specs); no file outside those two areas is touched; `core.autocrlf=true` normalises on add. No action.
- INTEGRATOR FOLLOW-UPS after X and Y land: G1 `AddressReading.evidencePhase` becomes REQUIRED (it is optional only so Area Y's hand-built reading in lab-address.spec.ts compiles mid-round); G2 the kit's `TrustChecklist.tsx` has no pending state — "receipt pending" renders dim (right) but the screen-reader word for dim is "not available": a pending state ≈ 4 lines, only if no pixel pin moves; G3 check History's loading chip (`pending` under the refused tone — the conflation Area W removed on Verification).
- Round 2 · Area X landed: **1c7ab68** on c192523 (17 paths, subject verbatim, hooks passed; eslint clean; tsc clean in every Area X file — its only errors are Area Y's mid-edit `lab-address.spec.ts(557)`; unit **1077 / 0**, +28: 20 across five re-pointed specs + 8 in the new `book-walk.spec.ts`; six new e2e pins unrun). New pure file `web/lib/book-walk.ts` (account identities across the walk). Fail-first: **Codex #2 and #4 SEEN failing on the untouched code** — `readCashPage` with `batch: null` returned rows; the summary over Codex's row said "Nothing material is liquidatable… No position is liquidatable. No account is within 10%…" (the false all-clear, confirmed); #3 and #1's repair half by mutation (identity lookup disabled; `bookAnswered` reverted to always holding the body). Pixel pins: `/book` and `/` do not move — HEAD's lib extracted to a temp folder (deleted) and every member of the view, summary, bands and tile words compared for the demo walk and the committed fixture: identical but three ADDED members; standing pins assert the demo walk has no duplicates, no unreadable rows, and that `DEMO_BOOK` passes the new envelope check. One fault-path rendering moved ON PURPOSE: under over-delivery "its rows may count an account twice" is no longer true with identities tracked → "it landed more accounts than the census counts" (four pins re-pointed). **STOPPED: B-1's `cashCensus` half** (needs verification-view.ts or app/overview/**: let `cashCensus` take the view's `withheld` / `reading.cash.refusedWhole` ahead of `wholeRefusal(book, CASH)`) → integrator follow-up G4. Carry: the Overview's pipeline still words an UNREADABLE `/v1/book` answer through its `error` phase ("unavailable" / "The batch could not be read.") — `BookReading.phase` is a closed union in Area W's file, so X added `failure.unreadable` beside `phase: "error"`; the strip above says "unreadable", the compute step does not → G5 if small. The nineteen not-X files X saw "modified with no content difference" are Area Z's disclosed line-ending slip; the integrator's check at 1c7ab68 finds none left in `git status`.
- Round 2 · Area Y landed: **41679c3** (22 paths, subject verbatim, nothing STOPPED, hooks green; tsc 0 / eslint / lint:css clean; unit **1077 / 0**; the lab's specs 166 → 179, +13, none deleted, two re-pointed; lab.spec `--list` 51, unrun). Fail-first SEEN for items 1 (run and set path), 2, 3, 5 and both Minors; items 4 and 6 fail by absence. Contract: + `data-state="listing-unreadable"` on `lab-surface`; + test id `lab-address-fallback`; an unreadable set is still `data-kind="failed"`. Choices: item 4 — the DISCLOSED FALLBACK is written to the URL (keeps "the highlight follows the subject"; one effect holds the bar to the subject shown — also A-2's second case, the shell's Scenarios link); item 5 — the notice is LIMITED ("no book run was dispatched for either" + an address sentence while an address is evaluated) rather than gating the evaluation. **Item 1 is WIDER than Codex's letter — ACCEPTED, for the owner's Rulings list:** a body or set that fails the predicate is refused WHOLE even with nothing held (a legacy-only fault beside a readable Cash row; one garbage engine figure in a set) so the page never shows as an answer something the hold refuses; this reverses, through the view only, Task 9's accepted "one contradictory row" rendering (`compareRows` and its pins untouched; the partial rendering is a two-line change in `resultBook` / `compareOf`). **RULING NEEDED AND MADE — item 4 rewrites INBOUND links** (the effect also rewrites an opened link naming a scenario the page does not show: `?scenario=ghost` becomes the first listed id, and a rewritten `?scenario=` link RUNS that scenario on reload): **an inbound link nobody selected from is NEVER rewritten** — the notice already discloses that the link's scenario is not shown; the bar is rewritten only by a SELECTION (incl. its disclosed fallback in address mode) or by the mode / address controls. A link must not come to run something nobody asked for. → a short round for the same implementer with the e2e results. Noted: a lone "<0.1%" row sits on the axis edge labelled "+0.1%" (`dotPlotScale` floors its domain only at zero, contrary to its own comment; left alone — changing it moves every lone sub-1% plot) → carry.
- Shared build on the post-round tree 41679c3 (flag set), whole e2e: RUNNING (background bxdolito7; log scratchpad `e2e-p4-r2.log`).
- Shared build on the post-round tree 41679c3 (flag set), whole e2e: **297 passed / 1 failed** — and the one failure PROVES Area Z's wrong-engine check: p1b-fixes "fix 3 … served_at verbatim" mocks EVERY series request with the legacy series; the page opens on Cash and now refuses a legacy series answering a Cash request, so the Served chip never renders. Re-pointed by the integrator (p1b-fixes.spec.ts is a shared spec): assert the refusal (`data-state="unavailable"`, no Served chip), press the legacy engine, then the same body is read and its instant printed verbatim. Every new e2e pin of areas W, Z, X and Y passed in a browser (incl. the ones their authors named likeliest to need a touch); no pixel pin moved.
- Integrator follow-ups (uncommitted; tsc / eslint clean; unit 1077 / 0): **G1** `AddressReading.evidencePhase` is REQUIRED (`EvidencePhase | null`; null only when no read was asked — an address that is not valid); the two spec helpers carry it; `evidenceReadOf` takes null. **G2** the kit's `TrustChecklist` gains a `pending` state (glyph "…", the dim class, the assistive word "pending" — a read in flight is no longer read aloud as "not available"); `TrustState` gains `"pending"` and the receipt's in-flight arm states it; the Inspector e2e pin asserts `data-state="pending"`, "pending:" and never "not available". **G3** History's loading chip is `pending` with NO tone (it wore the refused register — the conflation Area W removed on Verification). **G4 and G5 NOT taken → OWNER'S LIST under the envelope workstream:** a positions-only refusal the Overview's compute step cannot see (`cashCensus` is book-only by design on `/proof`); the pipeline wording an UNREADABLE `/v1/book` answer as "unavailable" while the strip above says "unreadable".
- Area Y short round sent (same implementer adeec2b55ba535db1): the inbound-link ruling — the bar is written only by a user's act; an opened link is left exactly as it arrived, its mismatch disclosed in words; pins that fail on 41679c3.
- Area Y short round landed: **bbd4bdd** (4 paths; tsc / eslint clean; unit 1081 / 0; lab-deep-link.spec 10 → 14). The bar-write decision is a pure function `scenarioForBar` in lab-deep-link.ts: after the reader's own selection the bar follows the subject shown (incl. the disclosed one-address fallback); otherwise it is written only when the subject IS the id the inbound link named; an opened `?scenario=ghost`, or an opened address link whose scenario the address does not carry, is left exactly as it arrived; the `address` param is written only when the bar does not already name it. The ruling had ASSUMED a notice existed for a lone unknown `?scenario=` — it existed only for `?scenarios=`; added `unlistedScenarioNotice` (the one new public sentence, READ by the controller: "This link names ?scenario=ghost, and this deployment publishes no scenario of that id. Nothing was run for it, and the link is left as it arrived. ETH -30 percent is shown instead."). After the shell's Scenarios link on a ghost page that was only opened the bar reads `/lab` — ACCEPTED (the bar names nothing; the notice stands).
- **FINAL TREE (78055a1): typecheck / eslint / stylelint / build clean; unit 1,081 / 0; e2e 299 passed / 0 failed / 0 skipped** (flag set for the build and the run; zero `test.fail`; the three `test.skip` greps are the standing conditional ones — the styleguide flag and CI). The first post-round runs each had ONE failure, both pin-side and both informative: p1b-fixes fix 3 (its mock answers every engine with the legacy series — the new engine check REFUSES it; re-pointed to assert the refusal, then the right engine) and the integrator's own stale `data-state="dim"` line under the new `pending` assertions.
- Commits (integrator, by pathspec, subject only): **78055a1** the close-out seams G1–G3 + the two re-pointed pins; **94f18ff** docs — `docs/plans/2026-09-16-ui-convergence.md` gains "Amendments at the gate and the close" (it SUPERSEDES the lines it names: the H1 / R1, R2 overruled, R3, R4, R7, what is out by ruling, and the page-test contract AS IT STANDS for the four pages + every id and attribute added since), and the Scenarios contract names `listing-unreadable`. (Whole-branch I6 discharged.)
- Scoped re-review 2 of the last round dispatched to the wave's reviewer ad194e8db98b3344a (range 0f18a1b..78055a1; asked to re-walk its own I-1 / I-2, every negative the Book can utter under one unreadable row, duplicates on a repair re-read, the ONE predicate on both paths, the URL law's event orders, and the integrator's `pending` state through every consumer). The close entry is drafted (scratchpad `plan4-close-entry.md`) and lands with the verdict.
- INTERRUPTION: the re-review 2 agent was cut by a rate limit (~00:15 USMST, mid-read of cash-rows / cash-summary; no section written). Owner re-logged 13:25; agent RESUMED by message naming its state; the range and the tree are unchanged (HEAD 94f18ff).
- **Re-review 2 (0f18a1b..78055a1): CLOSES — no Critical, no Important** (`wave-review.md`, 593 lines). I-1 closed at the root (`observatory-series.ts:200-211`: every non-decimal-string money value is the unreadable hole — never plotted, labelled or thrown); I-2 closed at the root (`trust.ts:245-280` ticks only under `receiptState === "exact"`; every `ReceiptState` × Cash-weld × evidence-phase arm walked — none greener than Verification, two stricter; no import cycle). Codex #4: every negative the Book and the Overview utter is blocked by one unreadable computed row, complete and mid-walk; never worded as refused by the engine. #3: identity case-insensitive; a within-page duplicate caught; a re-walk never mistakes a re-read for a duplicate (the tally is local to each walk effect). #13 / #14: literally one function per path (`answerFault`, `setFault`); the whole-body refusal names the engine and member; no honest body refused. The URL law holds through every event order; the one reload that runs something unclicked is the ruled disclosed-fallback case (for the Rulings list). 78055a1: every `TrustState` consumer handles `pending` (three tsc-enforced Records); every `AddressReading` constructor states `evidencePhase`; an invalid address never renders a Trust card. Eight Minors → the owner's list (Y-1, X-1…X-4, Z-1 the count law's reach grew, Z-2, W-1). **PLAN 4 CLOSES.** Close entry appended to `.superpowers/sdd/progress-ui-overhaul.md`; workspace commit follows.
