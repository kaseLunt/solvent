
######## Overview hero "70,000 people borrow against crypto to spend on a Visa card." vs 

--- P1 [trivial] P1 (recommended quick win): keep the 70,000 hook but scope it to card users, and hand the borrowing unit to the strip. Replace HERO_H1_LEAD and HERO_H1_TAIL in copy.ts; update the three e2e literals; re-baseline the two overview PNGs; add a one-line amendment under spec §5.1 (and a note on front-door.html:119) recording the owner ruling.
    WORDS: HERO_H1_LEAD = "70,000 people spend crypto on a Visa card. Some borrow against it."  HERO_H1_TAIL = " This is how close each borrowing account is to liquidation — right now."  (fallback tail if it wraps to 4 lines: " This is how close each borrower is to liquidation — right now.")
    FILES: web/app/overview/copy.ts (lines 3-4), web/tests/e2e/overview.spec.ts (lines 24-26, 56), web/tests/e2e/shell.spec.ts (line 13), web/tests/e2e/screenshots.spec.ts-snapshots/overview-dark.png, web/tests/e2e/screenshots.spec.ts-snapshots/overview-light.png, docs/specs/2026-09-15-ui-product-register-desi
    PIXEL: Yes, both overview pins move: the H1 glyphs change across a ≈900×140px region at y≈145–285, far more than the 1% (≈12,960px) diff budget, so overview-dark.png and overview-light.png must be re-baselined. At 138 characters the new H1 is at the top of the 3-line budget; if it wraps to 4 lines everythi

--- P2 [trivial] P2 (quick win, zero unsourced figures): drop the number entirely; the strip supplies the only count on the page. Same file set as P1.
    WORDS: HERO_H1_LEAD = "People borrow against their crypto to spend on a Visa card."  HERO_H1_TAIL = " This is how close each borrowing account is to liquidation — right now."
    FILES: web/app/overview/copy.ts (lines 3-4), web/tests/e2e/overview.spec.ts (lines 24-26, 56), web/tests/e2e/shell.spec.ts (line 13), web/tests/e2e/screenshots.spec.ts-snapshots/overview-dark.png, web/tests/e2e/screenshots.spec.ts-snapshots/overview-light.png, docs/specs/2026-09-15-ui-product-register-desi
    PIXEL: Yes, both overview pins move (H1 region changes). At 131 characters the H1 very likely stays at 3 lines (current is 121 across 3 lines with the third line only 27 chars), so the strip and entries do not shift; only the H1 glyphs differ. Re-baseline the two PNGs.

--- P3 [medium] P3 (structural, for plan B not the quick win): make the hero's number the system's own census so hero and strip agree by construction. Add a pure `heroLead(positions: number | null): string` to web/lib (cash-view.ts or a new lib/overview-view.ts) with a unit spec; OverviewSurface reads it. When positions is null (unread / withheld / absent) the lead is figure-free — never '0 accounts' or '— accounts'. Tail unchanged so 'each of them' now refers to the counted accounts. Requires the fallback string to be what shell.spec.ts:13 (no API) and overview.spec.ts:56 assert, and the demo/fixture strings for the loaded case.
    WORDS: loaded: `${positions.toLocaleString("en-US")} accounts borrow against crypto to spend on a Visa card.`  fallback (positions === null): "Cash accounts borrow against crypto to spend on a Visa card."  tail unchanged: " This is how close each of them is to liquidation — right now."
    FILES: web/lib/cash-view.ts (or new web/lib/overview-view.ts) + web/tests/unit/cash-view.spec.ts (or new unit spec), web/app/overview/copy.ts (lines 3-4: keep the fallback lead; drop the numeric one), web/app/overview/OverviewSurface.tsx (lines 86-89), web/tests/e2e/overview.spec.ts (lines 24-26 → fixture-
    PIXEL: Yes, both overview pins move (H1 glyphs; the demo renders '1,412 accounts borrow against crypto to spend on a Visa card. This is how close each of them is to liquidation — right now.' = ~119 chars, 3 lines, so nothing below the H1 shifts). Runtime caveat: the H1 reflows once /v1/book lands (fallback

--- P4 [small] Companion (optional, any of P1–P3): relabel the strip stat "Accounts" → "Borrowing accounts" so the strip carries the Book's unit (cash-view.ts:256). Either edit the literal in place (parity with the sibling literals at :148, :152) or, to honour the copy-in-lib law, move the three stat labels into copy.ts / cash-view. NOT recommended for the quick win because of the layout risk below.
    WORDS: "Borrowing accounts" (strip stat label)
    FILES: web/app/overview/OverviewSurface.tsx (line 156; optionally 148, 152), web/app/overview/copy.ts (if the labels move), web/tests/e2e/overview.spec.ts:181 ('Cash debt outstanding' assertion unaffected unless labels move), web/tests/e2e/screenshots.spec.ts-snapshots/overview-dark.png, overview-light.png
    PIXEL: Yes: the strip region (y≈480–675) changes. Layout risk: .liveStats is `grid-template-columns: repeat(3, auto)` with 28px gaps inside a `1fr auto` strip (overview.module.css:14, 18); a wider third label widens the right column, narrows the headline column, and the two-line strip dek (y≈570–610) could
  X NOT-NOW: A hero figure for CARD USERS sourced by the system: Solvent has no endpoint for ether.fi's card or user counts (api/openapi.yaml serves only the risk book; the design spec :44-45 notes ether.fi has no public REST API), so any cardholder number is an external quote, never a system fact — P1 keeps it 
  X NOT-NOW: A strict BORROWER count (accounts with debt > 0) on the hero or strip: /v1/book's Aggregate.positions includes collateral-only accounts (assemble.go:986-995) and no aggregate field counts debt-holding rows; deriving it client-side would need the /v1/positions walk read whole (cash-summary.ts:52-56 '
  X NOT-NOW: Correcting the demo's 1,412 to the real Cash census: spec §8 (:321-322) defers the per-engine split until the hosted API is up; the Go comment's '~9,700 DM accounts' (assemble.go:955-956) is a 2026-07-31 census note, not a served figure, so regenerating web/tests/fixtures/demo/ now would be guessing
  X NOT-NOW: Amending roadmap/VISION.md:8 ('~70k people borrow…'): a protected, owner-ratified governance file; VISION says changes are reviewed at phase boundaries through a proposed Decision — flag it, do not edit it inside a UI copy fix.
  X NOT-NOW: Re-baselining the screenshot pins is local-only (screenshots.spec.ts:25 skips on CI), so the pin update must be done and eyeballed by the implementer on the dev machine; I did not run any build or test (read-only).
  ? Q: Owner ruling required: the sentence is in the owner-approved spec §5.1 (:202-204), the option-A mockup (front-door.html:119) and the one-sentence pitch of the 2026-07-20 design spec (:15-17). Does the owner accept P1 (keep 70,000 as an explicitly card-user figure) or P2 (drop the number)? Where shou
  ? Q: Should the mockup front-door.html:119 be amended to match, given the spec's rule that the mockup wins on appearance (:10-12)? Plan 4 set the precedent of the plan carrying its amendments (commit 94f18ff).
  ? Q: Is 'borrowing account' the right unit at all? assembleDM counts an account with swept collateral and zero debt as a position (assemble.go:986-995), so both the Book's 'N borrowing accounts' (cash-view.ts:256) and any hero that says 'borrow' over-include collateral-only Cash accounts. Is that accepta
  ? Q: Which census will the live strip actually show at deploy — ~1,412 (demo assumption) or ~9,700 (assemble.go:955-956 live census 2026-07-31)? If the latter, the demo fixture and mockup numbers are off by ~7×; worth a measurement task once the hosted API is up (spec §8 :321-322).
  ? Q: Should roadmap/VISION.md:8 ('~70k people borrow against crypto collateral') be corrected at the next phase boundary via a proposed Decision, so the governance file stops asserting the same conflation the hero is being fixed for?
  ? Q: For P1: 'people' vs 'cards' — the public figure is ~70,000 active cards and ~300,000 accounts (Crypto Briefing 2026-09-04). Is '70,000 people' close enough for a hook, or does the owner want '70,000 active cards' verbatim?

######## Two kit gaps: (a) no pending variant for VerdictHeader (every in-flight headline

--- P1 [medium] (a) Add a sixth VerdictHeader tone "pending" for reads in flight. Kit: extend the union at VerdictHeader.tsx:13, add `pending: styles.emPending` to EM_CLASS, emit `aria-busy={tone === "pending" ? "true" : undefined}` on the <header>; kit.module.css after :40: `.emPending { color: var(--ink-2); }` (same ink-2 as refused, because --ink-3 may not carry a state; the register differs by the absence of any dashed chip, the header's aria-busy, and data-variant="pending"). Lib: extend LabHeadline.tone (lab-headline.ts:16), add `export const pending = (emphasis, dek): LabHeadline => ({ emphasis, rest: "", tone: "pending", dek })` beside `refused` (:39), and switch the eleven in-flight arms: LISTING_L
    WORDS: None on the pages: every loading sentence and dek stays verbatim ("Loading the history of Cash…", "Loading recorded chain actions…", "Loading this deployment's verification record…", "Loading the Cash book…", "Walking the Cash book…", "Looking up this address…", "Loading the committed scenarios…", "Running ETH -30 percent…"). New public-facing tokens are attribute values only: data-variant="pending" and aria-busy="true" on the header. Styleguide copy at page.tsx:204 changes "five tones" to "six tones" and its note gains one clause: "pending is a read in flight: ink-2, no dashed chip, the header busy — it has neither answered nor refused."
    FILES: web/components/kit/VerdictHeader.tsx, web/components/kit/kit.module.css, web/lib/lab-headline.ts, web/lib/history-view.ts, web/lib/activity-view.ts, web/lib/verification-view.ts, web/lib/cash-view.ts, web/lib/book-headline.ts, web/lib/inspector-headline.ts, web/lib/lab-address.ts, web/app/overview/O
    PIXEL: None. Every pin's ready() waits for a settled state (screenshots.spec.ts:59-116), so no loading headline is inside any pinned viewport; .emRefused is untouched, so settled refused headers (none appear in the demo primary states anyway) render as before; the header's aria-busy only appears while pend

--- P2 [small] (a, companion) Take the three in-flight identity chips out of the dashed refusal register, so a pending header carries no dashed chip at all: cash-view.ts:244 give the loading branch `{ label: "Identity", value: "pending" }` with no tone (keep tone "refused" for the unreadable/unavailable branches); inspector-view.ts:165 same; lab-view.ts:334 pass `[{ label: "Identity", value: "pending" }]` as the listing-loading chips so the header stops printing the kit's dashed "Identity missing" while the listing is merely loading (headerIdentity at kit.ts:34 accepts any present chip, so the §4 law is still satisfied). Update cash-view.spec.ts:217 to the untoned chip and add the chip to lab-view.spec.ts:
    WORDS: Chip "Identity pending" (existing words, now also on the Scenarios listing-loading header in place of the kit's "Identity missing").
    FILES: web/lib/cash-view.ts, web/lib/inspector-view.ts, web/lib/lab-view.ts, web/tests/unit/cash-view.spec.ts, web/tests/unit/lab-view.spec.ts
    PIXEL: None: these chips render only while a read is in flight, and no pin captures that state.

--- P3 [trivial] (b) Kit-level disabled register, tokens only, appended to kit.module.css after :57 (after the aria-pressed rule so `.btnGhost:disabled` wins the specificity tie): `/* A control that cannot act does not wear a live control's look: ink-2 on the panel-2 ground, the line for a border, no pointer. */ .btn:disabled { color: var(--ink-2); background: var(--panel-2); border-color: var(--line); cursor: not-allowed; } .btnGhost:disabled { background: none; }`. `.btn:disabled` (0,2,0) out-specifies `.btnPrimary` (0,1,0), so a disabled primary drops the accent fill and the white text for ink-2 on panel-2. Retire lab.module.css:4-7 (the opacity look would otherwise stack on the kit rule and pull ink-2 on
    WORDS: None: button labels are unchanged ("Compare…", "Run ETH -30 percent", "Run again", "Load more" / "Loading…").
    FILES: web/components/kit/kit.module.css, web/app/lab/lab.module.css, web/tests/e2e/lab.spec.ts
    PIXEL: None. The rule only fires on a disabled button, and no disabled button is inside any pinned 1440x900 viewport in the demo primary state: /lab's Compare is disabled but its .libFoot sits below y=900 (both lab PNGs end mid-fourth-row); /feed's Load more is enabled once settled and below 50 rows; the I
  X NOT-NOW: Giving pending a colour of its own: the palette has no third non-verdict text grey that may carry a state (--ink-3 is barred from states and findings by tokens.css:27, and every verdict colour means a verdict), so pending and refused must share --ink-2; the distinction has to be structural (no dashe
  X NOT-NOW: An animated in-flight indicator on the header: no motion tokens exist and the only motion in the kit is the toggle's 120ms left transition (kit.module.css:121); a pulsing ellipsis would need a reduced-motion contract that does not exist yet.
  X NOT-NOW: Pixel-pinning a loading state as a regression gate: the pins are settled states by design (screenshots.spec.ts:59) and a loading capture would race the stalled route; the e2e data-variant/aria-busy assertions are the honest gate for the pending register.
  X NOT-NOW: Restyling Overview's own .live header to match (it already emits data-variant loading/pending with no CSS bound): it is not a VerdictHeader and not in this item; noted as adjacent work only.
  X NOT-NOW: Styling the Scenarios library checkboxes or mode buttons as disabled: none of them ever receives a disabled prop (ScenarioLibrary.tsx:69-90,107-115), so there is nothing to style.
  ? Q: Should the pending emphasis differ typographically from refused, e.g. `.emPending { color: var(--ink-2); font-weight: var(--w-medium); }` (500 vs the h1's 600)? Colour alone cannot distinguish them (tokens.css:27 forbids --ink-3 for a state), so without a weight change a viewer sees the same grey H1
  ? Q: Is "Identity pending" acceptable as the Scenarios listing-loading chip in place of the kit's "Identity missing"? headerIdentity (kit.ts:33-35) treats any present chip as identity, so the §4 law holds, but the kit's comment at kit.ts:10-15 frames the missing chip as the answer to an omission; a read 
  ? Q: Should the Book walk-in-flight headline ("Walking the Cash book…", book-headline.ts:199-204, already variant "pending") join the pending tone, given the kicker already appends " · walking" (BookSurface.tsx:64) and the tiles are already aria-busy? The proposal assumes yes; the Overview strip already 
  ? Q: Retire lab.module.css:4-7 outright, or keep a page-local dim on top of the kit rule? Stacking opacity 0.45 over ink-2-on-panel-2 drops the light-theme ratio below 3:1, so the proposal retires it; lab.spec.ts:520-521 pins the opacity today and needs the owner's say on the replacement assertion.
  ? Q: Should the disabled primary keep a faint accent (e.g. `color-mix(in srgb, var(--accent) 12%, var(--panel-2))`, the grammar the pressed-toggle rule already uses at kit.module.css:57) so Run still reads as the primary action while disabled? The plain panel-2 fill proposed is the safer legibility choic

######## A — the repo's front door: verified fact sheet for a rewritten root README.md (R

--- P1 [medium] Rewrite root README.md from this fact sheet: pitch + honest status line (built, tested, not yet publicly deployed); real architecture tree (cmd/indexer → Postgres → cmd/riskd → cmd/api → packages/client-ts → web; two engines named with chain IDs 10 and 1; alerter/P4 explicitly 'planned'); 'What is proven' with the sourced claims (verified-ancestor rewind + named tests, opt-in anvil fork/pipeline replay, committed reconcile artifact 30838/30838 exact 0 drift with sha256 a34d7a53…, exact-integer money law, contract v1.8.0 with 17 paths + generated client + three fidelity gates, SSE, purity tests, honest-refusal laws, test counts, D-006 Codex gate); exact local-run block (cp .env.example .env; 
    WORDS: Draft sentences, each TRUE today: 'Solvent is a read-only risk observatory for ether.fi Cash: it ingests OP Mainnet (chain 10) and Ethereum mainnet (chain 1) logs into Postgres, derives every borrower position, recomputes health factors per batch, and serves them over an OpenAPI-contracted REST + SSE API and an eight-page Next.js app.' / 'Two lending engines, never summed: the Cash Debt Manager (OP) and the legacy Aave v3 ether.fi market (Ethereum).' / 'Reorg safety is verified-ancestor rewind, not a fixed rewind distance (internal/ingest/walker.go, D-003).' / 'The committed reconcile receipt compares 30,838 gated rows against chain reads at hash-pinned blocks: 30,838 exact, 0 drift (roadmap
    FILES: README.md
    PIXEL: none — README.md is outside web/; no web source, CSS or fixture changes, so no pinned 1440x900 viewport moves

--- P2 [small] Produce clean README screenshots with the existing script instead of the masked pins: `cd web && npm run build && npm run start` (port 3111), then `node scripts/screenshot-pages.mjs ../docs/readme overview book inspector lab` (add history/activity/api as wanted); commit the `-<theme>-fold.png` (1440x900 viewport) and/or `-full.png` files under docs/readme/ and reference them from README.md (design spec §9 asked for three). Use dark and light or pick one. Do NOT include the `verification` page shot: its receipt shows the fixture's 87/87, which contradicts the committed artifact (30838).
    WORDS: Alt text / captions must stay literal, e.g. 'Overview at 1440x900, demo fixtures (tests/fixtures/demo), dark theme' — the images are demo-fixture renders, not live mainnet, and the README should say so in one clause.
    FILES: docs/readme/*.png (new), README.md
    PIXEL: none — the script only reads the running build; no web source or fixture changes; new PNGs live outside web/tests/e2e/screenshots.spec.ts-snapshots/ so the pins are untouched
  X NOT-NOW: A 'live at <public URL>' link or uptime badge — no deployment exists yet (roadmap/STATUS.md blockers: VPS provisioning + domain purchase are owner actions gating W3).
  X NOT-NOW: An npm badge or 'npm install @solvent/client' — packages/client-ts/package.json is "private": true; README says '**Not published.** … nothing is on a registry'.
  X NOT-NOW: Any alerts / Telegram / watch-mode claim — no cmd/alerter; P4 'Watch & alerts' is Planned (ROADMAP.md).
  X NOT-NOW: The design spec §9 'recorded GIF/video' of the 90-second demo path — none exists in the repo (no .gif/.mp4 found under docs/ or web/).
  X NOT-NOW: Reusing web/tests/e2e/screenshots.spec.ts-snapshots/*.png as README images — they carry #FF00FF masks over the live pill and the Snapshot/Computed chips.
  X NOT-NOW: Citing the reconcile figure as '87 gated rows' — 87 is the OpenAPI example and a web test fixture; the committed artifact reports 30838 gated rows, 30838 exact, 0 drift.
  X NOT-NOW: Describing remote CI as uniformly green — only ci.yml passes; the control-plane workflow's scope-review job 'fails by construction' on direct pushes (STATUS.md).
  X NOT-NOW: A no-keys demo mode for a fresh clone — the served web has no fixture/mock mode; demo data exists only as Playwright route mocks in tests and scripts/screenshot-pages.mjs.
  X NOT-NOW: Any change to web/ files (e.g. regenerating evidence-manifest.json to 30838) inside this item — it is README scope, and that fixture feeds the pinned Verification screenshots.
  ? Q: Should the README quote the reconcile receipt totals from the committed artifact (30,838 gated rows exact, 0 drift, run 2026-08-02, pins eth 25,664,030 / op 155,018,419) — and separately, should web/tests/fixtures/evidence-manifest.json (87/87) be regenerated to match so the Verification page and it
  ? Q: Which pages become the README's screenshots (spec §9 says three): Overview, Book, Inspector, Lab are the obvious set; one theme or both?
  ? Q: Should the README name the eight pages (web/README.md says Overview + 'seven pages behind it') or the STATUS.md phrasing 'six surfaces'? The page list is the verifiable one.
  ? Q: Is 'Solvent' still the working name only ('Final name is decided before public deploy', design spec 2026-07-20 header)? The README title depends on it.
  ? Q: Should the README mention the Codex adversarial-review policy (D-006) as an engineering-process claim, given D-006 clause 5 says 'Trusted means … cited in README/portfolio claims' — i.e. only Codex-approved components may be cited? A list of which components carry a Codex-approved closing verdict wa
  ? Q: cmd/api/main.go:17 cites `TestAPIMakesNoChainCalls` but the function is `TestAPILinksNoChainClient` (cmd/api/purity_test.go:49) — fix the comment when the README lands, or leave for the API-side item?
  ? Q: Should the design-spec deployment row (Fly.io + Vercel) be mentioned at all, given STATUS.md now speaks of a VPS + domain? The README should state only what is decided.

######## Activity (/feed) and Inspector activity card: raw integer amounts ("252733333" n

--- P1 [small] A. Give the Activity page a second, already-served scale source: fetch /v1/book once on mount (getSolventClient().book(signal), as cash-book.tsx and VerificationSurface do) and merge engines[].value_decimals into the scale map beneath the stream's aggregates (stream wins when present; identical constants). Cash rows then print the placed decimal ("252.733333", unit "normalized debt · USDC") instead of "252733333 · normalized debt · raw units · USDC"; Aave rows stay raw and tagged (correct: a different unit). Requires rewriting the ActivitySurface.tsx:82-86 comment that states the page fetches no second endpoint, and mocking /v1/book in activity.spec.ts / r1-fixes.spec.ts where the raw state 
    WORDS: No new sentence; the existing "normalized debt" chip and its scaled unitTitle (feed-view.ts:182) take over from the raw arm.
    FILES: web/app/feed/ActivitySurface.tsx, web/lib/activity-view.ts (only if the scale merge is lifted into the view model), web/tests/e2e/activity.spec.ts, web/tests/e2e/r1-fixes.spec.ts, web/tests/e2e/screenshots.spec.ts-snapshots (activity-dark.png, activity-light.png)
    PIXEL: Yes: rows 0, 3, 13 and every other Cash row in the first viewport change from a 9-digit raw integer to "252.733333" etc., and their unit cell drops "raw units"; the header, tiles and strip are unchanged.

--- P2 [small] A2. Same book-sourced fallback for the Inspector's ActivityScale so an address with Cash history but no current Cash position (view.cashWire null) still scales its Cash rows instead of showing the refused-tone "raw units" pill; a shared per-tab book-aggregates context (layout level) would serve both pages and avoid two fetches.
    FILES: web/app/inspector/[addr]/InspectorSurface.tsx, web/lib/activity-rows.ts (no change to the law; only the source of valueDecimalsByEngine), possibly a new web/lib hook or the PostureProvider extended with a book read
    PIXEL: None expected: the Inspector's Activity section sits below y=900 in the pinned near state (toolbar + header + 5 tiles + backing/trust grid + 140px history card ≈ 920-980px before the section head); the pinned address has a Cash position so its rows are already scaled. Verify with the pin run.

--- P3 [small] B. Name the repaid unit on the Activity liquidation extract: for Cash rows print "debt repaid 358.12 USD" (as the Inspector already does at activity-rows.ts:72), for Aave rows print the row's symbol ("debt repaid 2,500 USDC") instead of the debt_asset address prefix "0xA0b86991…" (fall back to the prefix only when symbol is absent). Optionally add the Cash-only pre-liquidation size "of 716.24 USD before" from before_debt_usd — see open question on its scale.
    WORDS: "USD" after the Cash repaid figure; the row symbol after the Aave repaid figure.
    FILES: web/lib/activity-view.ts (liquidationDetail, ActivityLiquidation.repaidAsset → repaidUnit), web/app/feed/ActivityTable.tsx (LiquidationLine only if the field name changes), web/tests/unit/activity-view.spec.ts, web/tests/e2e/activity.spec.ts (extract assertions :225-240)
    PIXEL: Yes: row 5 (the Aave liquidation, inside the first viewport) changes "0xA0b86991…" to "USDC" in its extract; the Cash liquidations (rows 19, 37) are below the fold.

--- P4 [trivial] C. Inspector Amount column head: import the lib's ACTIVITY_AMOUNT_HEADER ("Amount · engine units, not USD") instead of the literal "Amount", so the caveat sits where the number is read on both surfaces; and stop appending the bare symbol directly to a normalized figure ("622 USDC") — order the cell as the Activity page does ("622 · normalized debt · USDC") so a normalized value is never read as a token amount.
    WORDS: Reuses "Amount · engine units, not USD" (activity-view.ts:184).
    FILES: web/app/inspector/[addr]/ActivityTable.tsx, web/lib/activity-rows.ts (amount/unit composition), web/tests/unit/activity-rows.spec.ts, web/tests/e2e/inspector.spec.ts (if any activity cell text is asserted)
    PIXEL: None expected (Inspector Activity section below the fold in the pinned near state, see A2); verify with the pin run.

--- P5 [trivial] D. Correct the Inspector demo fixture to the wire's real shape: the two `supply` rows become raw_type "supplied", amount_unit "none", amount null (record-only), matching internal/store/p5_events.go:158 and the Activity generator's own CLASSIFICATION (generate-demo-secondary.mjs:327). This removes the invented "600000000000000000 weETH raw units (opaque units)" and "1250000000000000000000 ETHFI" rows from the demo Inspector. The Inspector then shows "—" for those rows; if a raw-units demo row is still wanted, none is honest for Cash (no Cash type is opaque).
    FILES: web/tests/fixtures/demo/generate-demo-inspector.mjs (ev() raw_type mapping, :287, :302, :304), web/tests/fixtures/demo/events-demo-near.json (regenerated), web/tests/unit/demo-inspector-weld.spec.ts (row count/order/repay pins unaffected; add a vocabulary weld)
    PIXEL: None expected (below the fold in the pinned Inspector state); the 6-row ready check in screenshots.spec.ts:70 still holds.

--- P6 [small] E. Move the record-only word out of the numeric column on Activity: amount cell "—" (as the Inspector prints), unit cell "record-only" with the existing statement as its title, so the right-aligned column holds only figures or dashes and the two surfaces agree. Optional; honest either way.
    WORDS: "—" in the amount cell; "record-only" in the unit cell.
    FILES: web/lib/activity-view.ts (activityRow amount/unit for record-only), web/app/feed/ActivityTable.tsx (class choice), web/tests/unit/activity-view.spec.ts:145-147, web/tests/e2e/activity.spec.ts:558-563
    PIXEL: Yes: rows 2, 7, 8, 9, 10, 11, 12 (record-only rows inside the first viewport) change from the word to a dash and gain a unit-cell word.

--- P7 [medium] F. (Optional, medium) Liquidations tile / dek: state per-engine repaid sizes in each engine's own unit, grouped by debt symbol on Aave and never summed across engines — e.g. "Cash 2 · 767.88 USD repaid · legacy 1 · 2,500 USDC repaid". Every figure computed from debt_repaid at debt_decimals with BigInt; a row with null debt_repaid makes its engine's figure "—", never a partial sum.
    WORDS: "{n} · {sum} USD repaid" (Cash) / "{n} · {sum} {symbol} repaid" (legacy), per engine.
    FILES: web/lib/activity-view.ts (tilesFor / factDek), web/tests/unit/activity-view.spec.ts, web/tests/e2e/activity.spec.ts (tile assertions)
    PIXEL: Yes: the Liquidations tile (top of the viewport) gains a longer sub line; if the tile grows in height the whole table shifts down.
  X NOT-NOW: Human token amounts for aave_scaled rows (aave_borrow, aave_repay, aave_liquidation_call, aave_deficit_created): nominal = rayMul(scaled, variable borrow index at the event) needs the token's decimals and the event-time index; neither is on /v1/events (api/openapi.yaml:4078-4079, :4201-4207), and /v
  X NOT-NOW: USD for Cash borrow/repay rows: needs the same-block interest index (USD-6 = normalized × index ÷ 1e18); the ChainEvent carries no index (openapi.yaml:4155-4210); the deriver payload has "usd" and "index" (internal/derive/debtmanager.go:505-509) but the API serves payload only through the liquidatio
  X NOT-NOW: Token amounts on record-only supply/withdraw rows (aave_supply, aave_withdraw, supplied, withdraw_borrow_token): the payload "amount" exists (debtmanager.go:414-423; aave.go:697-703) but amount is null by contract for unit none (openapi.yaml:4080-4081). Backend: a served payload amount with registry
  X NOT-NOW: realized_bonus_bps on either engine (needs event-time prices the service does not re-read) — openapi.yaml:4139-4141; the extract's "—" stays.
  X NOT-NOW: Grouping raw integers with thousands separators: contradicts the page's own law (feed-view.ts:10-12, :47-49; ActivityTable.tsx:46-47) and the contract (openapi.yaml:4082-4084), and is asserted against by r1-fixes.spec.ts:92-94 and activity.spec.ts:166. Grouping asserts no scale, so the law could be 
  ? Q: Proposal A overturns a stated design choice (ActivitySurface.tsx:82-86: "The page does not fetch a second endpoint and does not hardcode a scale"). Is a one-shot /v1/book read acceptable on /feed, or should the book's aggregates be lifted into a shared per-tab context (the PostureProvider already ho
  ? Q: before_debt_usd's scale: the contract documents it as "USD 6-dec" (openapi.yaml:4121) and the server sets debt_decimals from the same engineValueDecimals[DM] constant (cmd/api/p5_events.go:393-396), but debt_decimals is documented as debt_repaid's scale only. Using debt_decimals for before_debt_usd 
  ? Q: Should the Inspector keep appending the bare symbol to a normalized figure ("622 USDC" at activity-rows.ts:94)? At the fixture's 1.05e18 index the USD borrowed is ~5% higher than the normalized value; the chip "normalized debt" is dim beside it. Proposal C reorders it; a stricter ruling would drop t
  ? Q: Record-only presentation: the two surfaces disagree today (Activity "record-only" in the amount cell; Inspector "—" with a title). Which register does the settle-for-deploy plan want, and is the contract's jargon "record-only" acceptable to a three-minute evaluator or should the unit cell say "no am
  ? Q: Fixture D removes the Inspector's only raw-units demo rows. Is a demo of the raw-units arm wanted anywhere in the pinned states? No honest Cash row can carry `opaque` today; the Aave rows are the honest raw-units example and already appear on Activity.
  ? Q: Every Activity proposal moves the activity pins (both themes); the pin is a local gate skipped on CI (screenshots.spec.ts:25). Confirm the owner re-baselines them in the same change rather than treating the diff as drift.

######## A — numbers a visitor cannot reconcile: (a) Book "2 accounts · 47 more under $10

--- P1 [small] (a) Move the Liquidatable tile's sub into web/lib (e.g. `liquidatableTileSub(summary)` in cash-summary.ts) and, over a book read WHOLE, append the partition's own total so the Book states the 49 History prints: material.count + belowLine.count (the same partition, cash-summary.ts:68-71). Never appended while the walk is running/stopped/unreadable (a total is claimed only whole); the bound note keeps its place. History keeps the wire's noun 'Liquidatable positions' (its label mirrors the contract field).
    WORDS: 2 accounts · 47 more under $100 · 49 in all
    FILES: web/lib/cash-summary.ts, web/app/book/BookSurface.tsx
    PIXEL: Yes: Book pins. The sub is inside the pinned KPI row (y≈350-490) and already wraps to 2 lines; '· 49 in all' likely adds a third line, growing the KPI row ~18-20px and shifting the Distance/Needs-attention cards down. Re-baseline both Book PNGs. History pins untouched.

--- P2 [trivial] (d) Reword belowLineSentence so the $100 line reads as per position and the sum is not placed 'below' it.
    WORDS: 47 more positions are technically liquidatable, each under the $100 line — $109.45 together — and not headlined.  /  singular: 1 more position is technically liquidatable, under the $100 line — $4.62 — and not headlined.
    FILES: web/lib/materiality.ts
    PIXEL: Yes: Book pins — the dek is at y≈160-220 in the viewport. The new sentence is ~8 characters longer; the dek should stay 3 lines at its ~880px measure, so no layout shift below, but the pixels change. Re-baseline both Book PNGs.

--- P3 [trivial] (c) Replace 'drift named' in the exact arm's step sentence with the counted register the rest of the page uses.
    WORDS: 87 gated rows reconciled exact against the chain; none drifted.
    FILES: web/lib/verification-view.ts
    PIXEL: Yes: Verification pins — the sentence sits at y≈510-560. It gets shorter and stays 2 lines under the 3-line Compute sentence beside it, so nothing below moves; pixels change. Re-baseline both Verification PNGs.

--- P4 [small] (b, disclosure) Add one dim row to the proof card after the two welds saying the welds are not a breakdown of the gated tally, so 29 + 14 ≠ 87 stops reading as 44 unsaid checks. Mirror in the drawer's receipt section if desired (web/lib/evidence.ts).
    WORDS: welds vs gated rows   the two per-engine weld tallies are not a breakdown of the gated rows; this manifest carries no per-check split
    FILES: web/lib/verification-view.ts, web/lib/evidence.ts
    PIXEL: Yes: Verification pins — the proof card's rows end at y≈850 and the fold summary sits at y≈880; one more row (~35px) pushes the fold summary to the viewport edge. Re-baseline both Verification PNGs.

--- P5 [trivial] (e) Reword moversCaption to name the ranking and the wire's cap, and move the section title/qualifier literals from MoversTable.tsx:35 into lab-movers.ts. Branches: shown < total → 'the 20 most affected of 118 accounts moved · the wire returns at most 20'; shown == total → 'all 5 accounts moved'; total null → 'showing 20 accounts moved · total not stated' (unchanged); unreadable scale → unchanged.
    WORDS: the 20 most affected of 118 accounts moved · the wire returns at most 20
    FILES: web/lib/lab-movers.ts, web/app/lab/MoversTable.tsx
    PIXEL: None: the caption renders below y=900 in the pinned Lab viewport (the section head's title is at y≈890 and is not changed). Lab PNGs stay.

--- P6 [trivial] (e, adjacent) Stop using 'moved' for lane_changed_rows on the Lab tile so one word maps to one wire field: relabel 'Accounts moved' → 'Accounts changing lane' (sub unchanged: 'of 1,406 measured · 0 improved').
    WORDS: Accounts changing lane
    FILES: web/app/lab/LabTiles.tsx
    PIXEL: Yes: Lab pins — the tile label is at y≈330 inside the viewport; same line count, pixels change. Re-baseline both Lab PNGs.

--- P7 [trivial] (f, optional dedupe) Drop the number from the Verify step sentence, since the tile directly above it prints 87/87 and the strip directly below prints it again; the sentence then carries the finding's scope instead of the tally. This folds in (c).
    WORDS: Every gated row of the pinned run reconciled exact against the chain; none drifted.
    FILES: web/lib/verification-view.ts
    PIXEL: Yes: Verification pins — same sentence slot at y≈510-560, still ≤ 2 lines; no layout shift. Re-baseline both Verification PNGs (shared with (c)).

--- P8 [large NEEDS-DATA] (b, data) Regenerate web/tests/fixtures/evidence-manifest.json's `reconcile` (and proof_subject.pin) from the committed drift-report.json by the loader's own rules — totals from summary.totals, welds from len(aave_rows)/len(dm_rows) with verdict=='exact' counts, finished_at from run.finished_at, comparison_sha256 — so the demo page prints what the deployed API prints (30,838/30,838/0/699; welds 29/29, 14/14; pin a34d7a53; finished 2026-08-02T02:42:31Z). Add a generator with provenance header like the other demo generators.
    WORDS: All 30,838 checked rows matched the chain exactly, in this deployment's pinned reconcile run.  /  Receipt exact · 30,838/30,838  /  Reconcile receipt: 30,838 gated rows exact, 0 drift  /  finished Aug 2, 02:42 UTC
    FILES: web/tests/fixtures/evidence-manifest.json, web/tests/fixtures/demo/generate-demo-evidence.mjs (new), web/lib/proof-contract.gen.ts (if it embeds the example)
    PIXEL: Yes: Verification (headline 'All 30,838 checked rows…' may wrap to 3 lines and push everything below), Overview (pipeline line), Inspector (receipt date). Re-baseline those six PNGs; Book/History/Lab/Activity/API untouched.
  X NOT-NOW: Saying 'the other 44 gated checks' (87 − 29 − 14) on the Verification page: false on the fixture's own numbers and on the artifact — the Aave weld's 14 rows include 10 advisory (gated:false, supplementary) rows, so the welds are not a subset of gated_rows (tally over drift-report.json; p5_evidence.g
  X NOT-NOW: Making 'accounts' and 'positions' one noun for Cash by law: the wire's field is liquidatable_positions (openapi.yaml:2466, 5654) and 'one row IS one account' is a per-batch server check stated on the run-book (openapi.yaml:1400), not a contract invariant; the Book may say '49 in all' over its own ro
  X NOT-NOW: Reconciling the page's 87 with the committed artifact the fold points at (roadmap/evidence/artifacts/w1-reconcile/drift-report.json) without regenerating the fixture: the artifact says 30,838 / sha a34d7a53 / finished 2026-08-02; the fixture's 87 / 5f0b3e2a / 2026-07-29 is the openapi example (opena
  X NOT-NOW: Collapsing the Lab's three 'moved' counts (941 lane_changed_rows, 425 web band changes, 118 movers_total) into one figure: the contract defines them as three different populations (openapi.yaml:1400 'It is NOT movers_total … and NOT newly_eligible_accounts'); only the words can be made distinct.
  X NOT-NOW: Showing the per-family gated breakdown (dm_rows 29, dm_weld 8, golden 9, freshness 29, p3 30,751 …) on the web: available only in the artifact; EvidenceResponse does not carry it and the web fetches only /v1/evidence (proof-data.ts:48-55).
  ? Q: Should the demo evidence fixture be regenerated from the committed artifact (30,838 gated / 699 advisory / pin a34d7a53 / finished Aug 2) before the deploy pins are cut? The deployed API will serve those numbers (p5_evidence.go:338-349; p5_runbook_evidence_db_test.go:262-265), so every '87' pinned t
  ? Q: For (a), should the Book also read engines[debt_manager].liquidatable_positions (the engine's own 49, currently unread by cash-view.ts) and print a disagreement when the walk's material + belowLine differs from it, rather than only the walk's total? On live data the two can differ (sweep-cut aggrega
  ? Q: Should the contract gain a description on ReconcileWeld (openapi.yaml:5811-5818) stating that a weld tally is the per-account row family (dm_rows / aave_rows) counted by verdict regardless of gate? Today the web has no contract sentence to quote for what '29/29' compares, and the Inspector's trust i
  ? Q: Is the Lab's 'Accounts moved 941' vs '425 accounts change band' vs '118 accounts moved' pair in scope for item A? It is the same class of irreconcilable numbers, on one pinned viewport, adjacent to (e).
  ? Q: Should the two component-composed strings (BookSurface.tsx:116 KPI sub; MoversTable.tsx:35 section title/qualifier) be folded into web/lib as part of (a) and (e), per the 'components compose nothing' law, even though only the words are the item's brief?
  ? Q: For (f): keep all seven restatements or accept the numberless step sentence? The receipt strip (verification-receipt) is the screenshot spec's ready signal (screenshots.spec.ts:105) and keyboard.spec.ts:98's target, so it cannot be dropped; the step sentence is the only restatement that can lose the

######## Book page (/book) — three visitor complaints: (a) 27 near-cap accounts headlined

--- P1 [small] (a) Add a second SmallToggle under the Needs-attention table that appends the near-cap rows the default view hides, following the dust-toggle pattern. Lib: in web/lib/cash-summary.ts add `nearCapToggleLabel(hidden: readonly SizedCashRow[], decimals: number): string` returning `Show ${n} more near-cap account${n === 1 ? "" : "s"} (${humanUsd(sumDebt(hidden), decimals)})`. Component: `const [showNear, setShowNear] = useState(false)`; `const nearShown = near.slice(0, Math.max(0, DEFAULT_ROWS - material.length))`, `const nearHidden = summary.nearCapRows.slice(nearShown.length)`; `base = [...material, ...nearShown, ...(showNear ? nearHidden.map(toRow near) : []), ...refused]` so the order stays "
    WORDS: Show 21 more near-cap accounts ($622K)   [alt: Show all 27 near-cap accounts ($830K)]
    FILES: web/lib/cash-summary.ts, web/app/book/NeedsAttention.tsx, web/app/book/book.module.css, web/tests/e2e/book.spec.ts, web/tests/unit/cash-summary.spec.ts
    PIXEL: None. The collapsed table renders the same 14 rows; the new toggle sits under the table foot (≈y 1235 in the demo, above the dust toggle or beside it) and the 1440×900 pin ends at row 7 (book-dark.png). Both themes unchanged.

--- P2 [trivial] (b) Lead the legacy fold's one line with the market's own finding, in the lib. cash-view.ts:429 → `Legacy · Aave v3 market — ${String(liquidatable)} of ${n(computed)} computed ${computed === 1 ? "position is" : "positions are"} liquidatable · ${debtWord} · ${String(refused)} refused`. The denominator is computed_positions (never total positions) so no negative is said over a refused row; the refused count stays as a separate clause (a served count on a non-withheld engine, never a placeholder). Do NOT add eligible debt: the demo's bad_debt[aave].eligible_debt_usd "4600" at 8 decimals prints "<$0.01". The withheld line (cash-view.ts:388) is unchanged. The anchor "Legacy Aave v3 market ↓" (Boo
    WORDS: Legacy · Aave v3 market — 46 of 8,552 computed positions are liquidatable · $1.9M debt · 0 refused
    FILES: web/lib/cash-view.ts, web/tests/unit/cash-view.spec.ts, web/tests/e2e/book.spec.ts
    PIXEL: None. The fold is below the 14-row attention table and the StressPreview / Bad-debt card row, well past y=900 in the demo (BookSurface.tsx:232-257 order; book-dark.png shows row 7 cut at the viewport edge).

--- P3 [trivial] (c-i) Make the phrasebook entry true of both wire states SWEEP_NEVER covers: refusal-phrasebook.ts:5 `["SWEEP_NEVER", "never successfully swept"]` (24 chars — fits the tile's one line where "collateral sweep never ran" fits today; shares "swept" with trust.ts:157 "never swept · collateral clock absent"; avoids "read", which the lib reserves for page-side unreadability). Every consumer inherits it: tile sub, row hover "never successfully swept · SWEEP_NEVER", the Inspector's refused headline via asSentence ("Never successfully swept."), stress-withheld text. The longer alternative "collateral never successfully swept" (34 chars) would wrap in the ~163px tile content box and grow the KPI row.
    WORDS: never successfully swept
    FILES: web/lib/refusal-phrasebook.ts, web/tests/e2e/book.spec.ts, web/tests/unit/cash-rows.spec.ts, web/tests/unit/cash-view.spec.ts, web/tests/unit/refusal-phrasebook.spec.ts, web/tests/unit/trust.spec.ts, web/tests/unit/inspector-headline.spec.ts, web/tests/unit/inspector-view.spec.ts, web/tests/unit/add
    PIXEL: Book pin: the Not-computed tile's sub at y≈446 changes text in place ("collateral sweep never ran" → "never successfully swept", one line either way), so pixels differ inside the tile but no layout shifts — re-baseline both Book PNGs. Inspector/Overview pins: none (the pinned near account is compute

--- P4 [small] (c-ii, recommended over stating $178K) Bring the demo fixture in line with what the engine serves: in generate-demo.mjs `refusedRow()` set `total_debt: null` (keep total_collateral null) and regenerate positions-dm-demo-page-1/2.json; the six refused rows then print "—" in the Debt column exactly as production would (cash-rows.ts:57). Update the comment at book.spec.ts:109 ("with its debt"). Leave the Inspector's refused fixture and the "Its last readable debt is …" path alone: the contract's field is nullable, so the web must keep tolerating a served value.
    WORDS: — (the Debt cell of a refused row prints the dash the lib already uses)
    FILES: web/tests/fixtures/demo/generate-demo.mjs, web/tests/fixtures/demo/positions-dm-demo-page-1.json, web/tests/fixtures/demo/positions-dm-demo-page-2.json, web/tests/e2e/book.spec.ts
    PIXEL: None. The refused rows are rows 9-14 of the attention table, below y=900 in the demo; the tiles above are census-based and do not read row debts.

--- P5 [small] (d) Make the sweep trust item say the consequence for THIS account, in web/lib/trust.ts sweepItem, after the existing rows/failed guards: read `const stale = position.flags.includes("collateral_sweep_stale")` and `const own = position.as_of.sweep_block` (already guarded non-zero at trust.ts:156). When failed > 0: if stale → detail `this account's last sweep failed · collateral is from its last successful sweep at block ${groupInt(own)} · gen ${G}`, state "warn", title `collateral_sweep_stale · ${failed} of ${rows} rows failed`; else → detail `${failed} of ${rows} swept accounts failed their last sweep · not this one, swept at block ${groupInt(own)} · gen ${G}`, state "warn" (the engine-wide 
    WORDS: 1 of 3 swept accounts failed their last sweep · not this one, swept at block 155,323,390 · gen 4   [stale case: this account's last sweep failed · collateral is from its last successful sweep at block N · gen 4]
    FILES: web/lib/trust.ts, web/tests/unit/trust.spec.ts, web/tests/e2e/inspector.spec.ts, api/openapi.yaml (optional: enumerate Position.flags values stale_price, collateral_sweep_stale — docs only)
    PIXEL: Inspector pin moves: the item sits at y≈636 inside the 1440×900 viewport and its detail is a ~150px `small` column, so the longer detail wraps to 2-3 lines, growing the item and pushing "Price provenance", the reconcile item and the History sparkline (y≈775-845) down — all within the pin. Book pin: 
  X NOT-NOW: Stating "$178K of debt whose collateral could not be read" on the tile or in the dek as a fact about the system: the engine never persists debt on a refused Debt Manager row (assemble.go:1170-1181: "a refusal is the absence of a number"), so in production the sum runs over zero readable rows and the
  X NOT-NOW: Making the trust item say a failed sweep EXCLUDED this account's collateral: the code proves the opposite (collateral stays at the last successful sweep, flagged stale), so no such sentence may be written.
  X NOT-NOW: Opening the legacy <details> when the section-head anchor is followed (hash → open): a component behaviour change, not a copy fix; today the anchor lands on the closed fold and the summary line is what the reader sees.
  X NOT-NOW: Fixing the demo sweep stamp (rows 3 / failed 1 on a 1,412-account book) inside this item: regenerating it changes the Inspector's pinned trust text and the History/Observatory welds (demo-secondary-weld.spec.ts:111, 364).
  ? Q: Toggle wording: "Show 21 more near-cap accounts ($622K)" (mirrors the dust toggle: hidden count + hidden Σ) or "Show all 27 near-cap accounts ($830K)" (echoes the headline figure)?
  ? Q: Should the dust-toggle label, composed in the component today (NeedsAttention.tsx:116), move into the same lib helper while the near toggle is added — the "components compose nothing" law is already bent there.
  ? Q: Demo fixture: regenerate refused rows with total_debt: null to match the engine (proposal c-ii), or keep the readable debts on the grounds that the contract's field is nullable and the API DB fixture (fixture_test.go:425-431) also seeds one? The two disagree about what a hiring evaluator sees in the
  ? Q: Demo sweep stamp rows 3 / failed 1 is inconsistent with 1,406 computed DM positions (each needs its own success row, assemble.go:1000-1008) — regenerate to e.g. rows 1,406 / failed k? It would re-pin inspector.spec.ts:88 and trust.spec.ts:46.
  ? Q: Legacy bad_debt fixture: eligible_debt_usd "4600" and collateral_at_risk_usd "9100" at 8 decimals print "<$0.01" in the fold's Liquidatable tile ("<$0.01 eligible debt") for 46 liquidatable positions — regenerate with plausible figures before an evaluator opens the fold?
  ? Q: "The ether.fi Aave v3 market is being wound down" (BookLegacy.tsx:29) is sourced only to recon/p3-probes.md:140 (caps slashed to 1 at block 22,124,166, 2025-03-25). Acceptable as public copy, or should the fold cite the cap-slash event / drop the sentence?
  ? Q: Should the contract enumerate Position.flags values ("stale_price", "collateral_sweep_stale") so the trust item reads a documented word rather than a Go constant?
  ? Q: The Not-computed tile prints only engine.refusals[0].key (cash-view.ts:292); a batch with two refusal codes names one. Out of scope here, but the same tile.
  ? Q: Should the phrasebook entry be the shorter "never successfully swept" (fits one tile line) or the clearer "collateral never successfully swept" (wraps, grows the KPI row, moves the Book pin's layout)?

######## A — builders' vocabulary on public pages (Verification, Activity, header identit

--- P1 [small] Verification: one vocabulary for the receipt — 'checked rows' (the H1's word) replaces 'gated', engine names replace wire ids in the weld rows, 'feeds registry' replaces 'fingerprint weld', plural() replaces '(s)', 'batch key' leads the materialization-key row, and the Index/Compute sentences drop 'Chain heights' and 'the wire's own integers'.
    WORDS: INDEX_SENTENCE: 'Latest block indexed for each engine, ahead of every batch.' · Compute: 'Batch 18,251 computed at 2026-08-08T20:22:08Z; every position's health from exact integers, never floats.' · Verify tile sub: 'checked rows exact (must match to pass) · 0 drifted' (empty arm: 'checked rows · none compared') · Verify sentence: '87 checked rows matched the chain exactly; 0 drifted.' · receipt strip: 'Reconcile receipt: 87 checked rows matched exactly, 0 drifted' · status row: 'ACCEPTED · every checked row matched the chain exactly' · row: 'checked rows' / '87/87 exact · 0 drifted' · rows: 'Cash rows' / '29/29 exact', 'Aave v3 market (legacy) rows' / '14/14 exact' (label `${engineName(weld
    FILES: web/lib/verification-view.ts (274, 402, 430, 442, 445, 310-316, 507, 527, 534-535, 540, 548-551, 628, 778-789; optionally the Overview line strings 348,359,421,434,449), web/lib/evidence.ts (drawer twins 902, 922-925, 936, 877-880 — optional, LOW), web/tests/unit/verification-view.spec.ts, web/tests
    PIXEL: verification-dark/light change pixels well past 1% (tile sub, two step sentences, the receipt strip, six card rows, two fold summaries all re-rasterize) but nothing moves: each replacement is one line of equal or shorter width at the 610px card ('Aave v3 market (legacy) rows' ≈190px still sits besid

--- P2 [trivial] Verification probes qualifier: plural() instead of '(s)'.
    WORDS: '1 committed probe record · 1 manifest note' / '0 committed probe records'
    FILES: web/lib/verification-view.ts (679-680), web/tests/unit/verification-view.spec.ts, web/tests/e2e/verification.spec.ts
    PIXEL: none — the probes section is below y=900 in both verification PNGs.

--- P3 [small] Activity type words: add a lib display map for the three raw enum values (buttons and Type cells print the plain word, the wire word rides the title); keep ActivityRow.type for tone and tests.
    WORDS: 'collateral enabled', 'collateral disabled', 'bad debt realised' (borrow, repay, supply, withdraw, liquidation unchanged)
    FILES: web/lib/activity-view.ts (new TYPE_WORDS + typeLabel; ActivityRow gains typeLabel near 342), web/app/feed/ActivityControls.tsx (131: print label, title=candidate), web/app/feed/ActivityTable.tsx (61: print row.typeLabel), web/tests/unit/activity-view.spec.ts (add cases), optionally web/lib/activity-
    PIXEL: activity-dark/light: only the TYPE row pixels change — 'collateral enabled' is the same length as 'collateral_enabled', 'bad debt realised' is +2 characters, and the row has ≈280px spare after 'Since block · choose one engine' (x≈1050 of 1336), so nothing wraps and the table below does not move; the

--- P4 [trivial] Activity 'Filter echo' chip: say the applied filter in the page's words, with 'any' for a null constraint instead of the em dash (the dash means refused/absent elsewhere on the page).
    WORDS: label 'Filter applied' · value 'any engine · all types · any block · 50 per page' (engine-scoped: 'Cash · borrow, repay · from block 25,635,600 · 50 per page' via engineName, joinAnd, groupInt on guarded integers)
    FILES: web/lib/activity-view.ts (276-287 filterEcho/echoTypes, 661 label), web/tests/unit/activity-view.spec.ts, web/tests/e2e/activity.spec.ts, web/tests/e2e/p1b-fixes.spec.ts
    PIXEL: activity-dark/light: the chip's value is 48 vs 52 characters, so the chips row stays one line and nothing below moves.

--- P5 [small] Header identity chips on Inspector, Book, History, Scenarios: plain labels and values for 'both engines', 'not projected', 'Stride', 'unbounded', 'Engines', 'Config'.
    WORDS: Inspector: 'Lookup complete · Cash and the legacy market' (alt 'complete · both books') · Book/Inspector: 'Current as computed, not projected' (optional; keep if width is tight) · History: 'Step hourly'; 'Range 2026-08-01T21:00:00Z → no end bound' (from-null arm 'no start bound') · Scenarios: 'Covers Aave v3 market (legacy) and Cash'; 'Scenario set v1'
    FILES: web/lib/inspector-view.ts (253 lookup chip; 261 Current chip), web/lib/cash-view.ts (253 Current chip), web/lib/history-view.ts (265 Stride label), web/lib/observatory-series.ts (339-341 describeRange), web/lib/lab-view.ts (137,145,173 chip labels), web/tests/e2e/inspector.spec.ts, web/tests/e2e/his
    PIXEL: inspector: the chips row has ≈200px spare (ends x≈893, button at x≈1100) — 'complete · Cash and the legacy market' adds ≈100px and 'as computed, not projected' ≈75px; both together fit but tightly, and a wrap would push the tiles down ≈36px and move the whole viewport, so lengthen only the Lookup ch

--- P6 [trivial] History bucket record: 'Hour record' as the title and 'balances as of block N' in the takeaway.
    WORDS: 'Hour record 2026-08-08T20:00:00Z' · 'captured at 2026-08-08T20:00:00Z · balances as of block 155,323,444.'
    FILES: web/lib/history-view.ts (479), web/lib/observatory-series.ts (786), web/tests/e2e/history.spec.ts, web/tests/e2e/state-matrix.spec.ts, web/tests/unit/history-view.spec.ts, web/tests/unit/observatory-series.spec.ts
    PIXEL: history-dark/light: the title is shorter and the takeaway stays one line; nothing moves. (Both strings sit inside the 900px viewport, so the PNGs must be re-baselined.)

--- P7 [trivial] Inspector Trust card: 'pass N' for the sweep generation and 'Cash's own oracle' for engine-exact provenance.
    WORDS: '1 of 3 rows failed · pass 4' · 'pass 4 open · sweep in progress' · 'pass 4 · 20m ago' · 'Cash's own oracle' (title stays 'engine-exact')
    FILES: web/lib/trust.ts (166-168, 177), web/tests/unit/trust.spec.ts, web/tests/e2e/inspector.spec.ts
    PIXEL: inspector-dark/light: '1 of 3 rows failed · pass 4' is one character longer and 'Cash's own oracle' is shorter than 'the engine's own inputs', both within the ≈140px detail column; the card height does not change.

--- P8 [small] Book: phrasebook SWEEP_NEVER → 'collateral never read'; section qualifier names the contract; the tile label states the $100 line instead of 'material' and moves into the lib.
    WORDS: 'collateral never read' · 'Cash's Debt Manager contract · OP Mainnet · 1,412 borrowing accounts' · 'Liquidatable · ≥ $100' (composed from MATERIAL_LINE_USD)
    FILES: web/lib/refusal-phrasebook.ts (5), web/lib/cash-view.ts (324 sectionQualifier; new tile-label constant), web/app/book/BookSurface.tsx (105: read the lib constant), web/tests/e2e/book.spec.ts, web/tests/unit/cash-view.spec.ts, web/tests/unit/cash-rows.spec.ts, web/tests/unit/address-stress.spec.ts, w
    PIXEL: book-dark/light: 'collateral never read' (21 chars) is shorter than today's 26 in the ≈194px 'Not computed' tile, so the tile row keeps its height; the qualifier gains ≈75px on a line with ≈600px spare; 'Liquidatable · ≥ $100' is about the width of 'Liquidatable · material'. Nothing moves. Keep the 

--- P9 [small] Scenarios: move the two component-composed strings into lab-view and gloss them — the assumptions button and the movers qualifier.
    WORDS: button 'Assumptions · What the model leaves out' · drawer title 'Assumptions & what the model leaves out', h3 'Left out of the model' · qualifier 'room today → after the shock · ranked by the service'
    FILES: web/lib/lab-view.ts (new copy constants), web/app/lab/LabSurface.tsx (384), web/app/lab/AssumptionsDrawer.tsx (19, 52), web/app/lab/MoversTable.tsx (35), web/tests/e2e/keyboard.spec.ts
    PIXEL: lab-dark/light: the button is alone on its row, so a longer label widens it without wrapping; the 'Most affected accounts' head only changes pixels in the last few rows of the viewport. Nothing moves.

--- P10 [small] API: a pure inline renderer for the two Markdown markers the contract uses — `**…**` and backticks — so descriptions render bold and code without touching the contract text; uppercase headings stay (they are the yaml's words).
    WORDS: none — the words are the contract's; only the markers become formatting
    FILES: web/lib/api-view.ts (new inlineParts(paragraph): {text, code, strong}[] — bold pairs first, backtick pairs inside; unbalanced markers stay literal), web/app/developers/EndpointCard.tsx (32-34, 48-50 map parts to <strong>/<code>), web/tests/unit/api-view.spec.ts (new cases incl. the nested '**409 `ba
    PIXEL: none — no endpoint card is inside the API first viewport.

--- P11 [trivial] Inspector explain drawer: replace the stale 'Proof Center' with the page's name.
    WORDS: 'not served on this surface · published by /v1/evidence (Verification)'
    FILES: web/lib/evidence.ts (124), web/tests/unit/inspector-evidence.spec.ts (37 — check the asserted text)
    PIXEL: none.

--- P12 [medium] LOW second wave (defer or do after the HIGH tier): gloss the drawer doctrine and fold rows — 'watermark vector', 'reconcile-welded', 'custodied header time', 'chain-aware tiebreak (chain, height, tx, log, seq)', 'sweep stamp (the count's collateral clock)', 'observatory_points rollup', and the Overview pipeline's 'Reorg-safe indexer / Verified-ancestor rewind / OP block'.
    WORDS: to be drafted per sentence; every gloss must be re-verified against the contract notes it paraphrases
    FILES: web/lib/verification-view.ts (133-141, 801), web/lib/evidence.ts (105, 183-185, 818-837), web/lib/activity-view.ts (186-243), web/lib/history-view.ts (114, 484-500, 617, 525), web/app/overview/Pipeline.tsx (17-38 → move to a lib)
    PIXEL: none.
  X NOT-NOW: Rewriting the SERVICE's own words: probe record path 'recon/p3-probes.md' and note, the 'deploy-bound' manifest note, scenario labels/descriptions in the Scenarios rail ('HORIZON PROJECTION', 'Factor shock on ETH/USD…'), wire identifiers in identity chips ('eth_minus_30 · v1', the 64-hex keys), the 
  X NOT-NOW: Replacing the product-wide word 'engine' (kicker 'ACTIVITY · ALL ENGINES', chips 'Scope all engines' / 'Engine Cash' / 'both engines' / 'Engines …', control label 'engine', column 'Engine', Book qualifier) — a cross-page vocabulary ruling touching six pages and ~40 text pins (activity.spec.ts:133,13
  X NOT-NOW: Renaming the canon pills 'PROOF · EXACT @ 5f0b3e2a' and 'SERVING · WATERMARKED' — spec markers (evidence.ts:45) that ~14 pins assert (verification.spec.ts:115,121,187,189,219,273,274,297; state-matrix.spec.ts:667,668,684; unit verification-view.spec.ts:450,657,722; proof-evidence.spec.ts:130,336); t
  X NOT-NOW: Removing 'raw units' from the pinned Activity rows: it is data, not copy — the pinned state aborts /v1/stream so no value_decimals is licensed; fixing it means mocking the stream in screenshots.spec.ts or having the Activity page read value_decimals from /v1/book (a second fetch), both outside this 
  X NOT-NOW: Rendering Markdown beyond bold/code (lists are already handled by contractParagraphs keeping the '* ' marker; headings, links and tables do not occur in the extract) — unnecessary and would widen the renderer's surface.
  ? Q: One word for the receipt's rows on Verification: 'checked rows' (the H1's word, proposed) or 'must-match rows' (the tile's current gloss)? Either way retire 'gated' from the public strings and keep it in the evidence drawer.
  ? Q: Is it acceptable that the Activity echo chip says 'any' for a null constraint rather than the em dash (the dash reads as refused/absent everywhere else on the page)? The contract calls the object 'The filter actually applied', so 'any engine · all types · any block' is a true statement of scope.
  ? Q: Inspector chips row has ≈200px of slack: lengthen 'Lookup complete · both engines' (to 'complete · Cash and the legacy market', or the shorter 'complete · both books') and leave 'Current not projected', or accept a Current reword and risk a wrap that moves the whole viewport?
  ? Q: Scenarios: 'What the model leaves out' vs 'Not modelled' — the tiles already print 'not modelled' for an engine the scenario does not cover (LabTiles.tsx:14), so the button should not reuse that phrase; confirm the wording.
  ? Q: Should the Inspector's ACTION_LABEL 'Deficit created' change to 'Bad debt realised' together with the Activity map so both pages use one word (pin activity-rows.spec.ts:73)?
  ? Q: Does the owner read the Endpoints qualifier 'api/openapi.yaml, verbatim' (api-view.ts:148) as permitting bold/code rendering of the contract's Markdown markers? The words stay verbatim; only the markers become formatting, and the DOM text changes (api.spec.ts:169-172 adjusts).
  ? Q: Keep or drop the 'Filter applied' chip altogether? Its facts duplicate Scope/View chips and the list qualifier 'loads 50 at a time' (activity-view.ts:502-510), but it is the only place the wire's own echo is shown.
