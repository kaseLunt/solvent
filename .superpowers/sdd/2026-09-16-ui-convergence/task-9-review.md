# Task 9 review — lab debt (R11, first set): the envelope classifier, the hold, the tile's tone, the rerun sentences, one verdict judge

Package: `646aee9..6df392a` (one commit, 23 paths, the plan's message verbatim, author Kase Lunt, no attribution line). Read-only review: nothing was built, no suite was run, no server touched, no file other than this one written. The working tree equals the commit for all 23 paths, so file:line below is the commit. "Parent" = `646aee9`, read with `git show`.

## Verdicts

- **Spec compliance: PASS WITH FIXES.** All five items are closed on the plan's letter and the addendum's. Item (a)'s stated purpose — "a version-skewed 2xx is a refusal by the field's name, never a throw at render" — is not yet total: the hunt found two remaining 2xx-to-throw paths inside the implementer's own files (I1, I2) and the mislabelling the implementer flagged in `runbook.ts` (I3), which also makes several of the classifiers' names unreachable in the running app.
- **Code quality: APPROVE after a small fix round.** The (b) finding is a real defect, correctly diagnosed and fixed at the right place (the ask, not the settle); the pins are real and fail on the parent; the moves left no stale reference. No Critical.

## Spec compliance

| Item | | Trace, and whether the pin can fail on the parent |
|---|---|---|
| (a) `classifyRunBookEnvelope` / `classifySetEnvelope` name a missing/non-object `batch`, `evaluation`, `coverage` and every non-array list | CLOSED | `lab-classify.ts:401-414` (run-book: `batch` + `batch.id` / `age_seconds` / `supersession.superseded`, `shocks`, `out_of_model`, `applied_shocks`, `held_flat`, `engines`, `excluded_engines`, `coverage`, `notes`), `:424-442` (set: `batch.id`, `evaluation` + its three fields, `requested_scenario_ids`, `results`, `excluded_engines`, `coverage`, `notes`), `:449-457` (`classifySetResult`: the four per-result lists). Every member the classifier requires is `required` in `api/openapi.yaml` (`RunBookResponse` :4936, `RunBookSetResponse` :5519, `Batch` :2406, `Supersession` :2371, `SetRunEvaluation` :5457, incl. the four-value `freshness` enum and the nullable `newest_servable_batch_id`), so no conforming body is refused. Every `batch.*` / `evaluation.*` dereference in `lab-view.ts`, `lab-compare.ts`, `LabSurface.tsx:107,309`, `AssumptionsDrawer.tsx:60` is a field the classifier checks; nothing in `web/` reads `coverage.*`. |
| (a) readers return a named unreadable state instead of throwing | CLOSED | `readEngine` envelope first, before the definition (`lab-engine.ts:53-58`); `resultBook` envelope first with `run: null`, `identity: null`, the definition's chips (`lab-view.ts:155-162`) — so the drawer button (`LabSurface.tsx:293`), the age (`:105`) and the banner (`:309`) never touch the body; `cashOutcome` before `definitionSkew` (`lab-library.ts:95`); `setMembership` envelope first (`lab-compare.ts:169-170`), which guards every `compareRows` call (`lab-view.ts:268,273-275`; `lab-reading.ts:62,74`). `definitionSkew` itself is unguarded but has exactly two callers, both behind the classifier (`lab-view.ts:163`, `lab-library.ts:97`). |
| (a) pins | CLOSED — can fail | Run-book without `batch`: the parent's `resultBook` throws `TypeError … reading 'supersession'`. Set without `evaluation`: the parent answers "evaluation.scenarios_evaluated is not a count" — fails on the words. Set without `batch`: the parent's `setMembership` passes and `compareRows` throws on `set.batch.id` — fails honestly. `lab-classify-envelope.spec.ts` fails on the parent by absence only. |
| (a) the hunt — any remaining 2xx → throw | **NOT TOTAL** | I1 (a refusal without a string `code`), I2 (a set body of JSON `null`), I3 (`runbook.ts` rejects before the classifier), M6 (object-typed string scalars as React children). |
| (b) only a body that READS moves into the hold | CLOSED — can fail | The parent's `heldOf` moved any `ok` body (`git show 646aee9:web/lib/lab-reading.ts`, lines 29-33) — the defect is real. Now `heldOf(prev, reads)` (`lab-reading.ts:38-42`), taken by `withRunning` (:44) and `withSettled` (:50); the hook passes `readsAsAnswer` on all three calls (:131, :136, :141). Unit pin: on the parent `withRunning(once, …).held` is the malformed body — fails. E2E pin: see M7. |
| (b) the banner names the HELD result's batch | CLOSED | `overHeld` returns `{...held, banner: "rerun-failed"}` where `held = resultBook(heldResult.response)` (`lab-view.ts:239-244`), so `book.run` is the held body and `LabSurface.tsx:309` passes its `batch.id`. With the hold now always a body that reads, `:241`'s `heldResult.response.batch.id` is safe too. |
| (b) `readsAsAnswer` vs `bookOf`'s release rule | CONSISTENT on the four named classes; two corners differ | `bookOf` releases for result · withheld · not modelled · definition changed (`lab-view.ts:221-225`). `readsAsAnswer` (`lab-engine.ts:106-112`) is true for a result, a withheld book (listed or by name) and a body with no Cash row. It is never true where the view says contradictory (checked arm by arm). It is false while the view releases in two corners — M1. The deviation (withheld / not modelled read) is right: the literal ruling would resurrect a released result under "Run again failed" and break the landed pin "an honest answer releases the hold". |
| (b) the set path | DEFECT NOT PRESENT | `heldSetOf` moves only a set that passes `setMembership` (`lab-reading.ts:60-66`) and always did; with the envelope classified inside `setMembership`, a malformed set can neither enter the hold nor release it. The existing pin "a set that answered its request is held…; a set that does not answer is never held" walks two consecutive non-answers. |
| (c) tile tone = headline tone for a net ≤ 0 | CLOSED — E2E can fail, unrun | `newlyTone` (`lab-headline.ts:96-101`) is the only source of `resultHeadline`'s tone (every arm, :104) and of the tile's (`LabTiles.tsx:52`); both receive `r.newly`, `r.heat` of the same `EngineResult` (`lab-view.ts:195-205`), a held result included. They cannot disagree. Parent tile: `newly > 0 ? "crit" : "ok"` → `data-tone="ok"` for −3 beside 118 crossings; the added assertion `data-tone="warn"` fails there. The unit pin fails on the parent by absence only; its cross-check against `resultHeadline().tone` is tautological for agreement but still pins each tone to a literal. |
| (d) sentences in the lib; components compose nothing | CLOSED for the two sentences | `compareRerunFailedLine` (`lab-headline.ts:209`), `staleBannerLine` (:228), one private shape `rerunFailedLine` (:206). `StaleBanner.tsx` now composes nothing. `CompareCard.tsx` still composes its other strings (M3) — pre-existing, outside (d)'s letter. Character-for-character comparison with the parent: M3. |
| (e) one judge | CLOSED | `rowVerdict` at `address-stress.ts:188`; `lab-address.ts:4` imports it (three uses); `StressTable.tsx:42` prints `stressVerdictWords(rowVerdict(r))`. `stressVerdict` / `StressVerdict`: zero hits in `web/` (code, comments, specs); the only hit in the repo is the plan's own text. |
| (e) the negative-figure gate on both pages | CLOSED — can fail | The gate is `computableSide` inside `rowVerdict` (`address-stress.ts:191-196`). On the parent's Inspector a negative debt left `flips === false` and a not-liquidatable `after` → "No". Unit and E2E pins assert "Cannot say" / "the shocked figures are not a position". |
| (e) negative room in the lab's words; no room beside an unknowable verdict | CLOSED — can fail | `StressTable.tsx:33` → `sideRoomWords` (`address-stress.ts:167`), the same function `AddressWorkspace.tsx:50,53` and the lab's dek and tiles use. The parent printed `money(side?.room)` → "−$1,069", and printed it beside "Cannot say". The rule is per SIDE: a row whose `after` is unknowable still prints its `before` room ("$190.50") beside the row's "Cannot say" — the side that stands keeps its figure. The lab does the same; I judge it honest, but it is a reading of the addendum's sentence the controller should know was made. |
| (e) IDENTICAL words on both pages | Rooms: YES (one function). Verdict cell: **NO for projection rows** | I4. |
| Ownership / process | OK | 23 paths, all inside the addendum's list; `inspector.spec.ts` gains one stress-table test only; no fixture, no baseline, no `cash-*` / `book-*` / `inspector-view`. No new comment names a task, round or review (the two process-flavoured comments in the diff — "(plan R2)" in `LabTiles.tsx`, "moved verbatim from the old Lab's…" in `lab-classify.ts` — are the parent's text, re-wrapped). |

## The implementer's deviations

1. **`readsAsAnswer` wider than the ruling's example — honest and necessary.** See (b) above and M1.
2. **`withRunning` takes the predicate — necessary.** The defect lived at the ask; `withSettled` alone cannot see it.
3. **Set faults read "… is outside the wire contract" — honest.** True of a missing member and of a wrong-typed one, uniform with the run-book path, and more specific than before where it matters (`results[1]`, `evaluation.freshness`). The pins were moved to the new words, not loosened.
4. **"A result with a broken list remains one contradictory row" — honest.** A dashed track with its word and the field's name in brackets (`compareRowWords`), never a dot at zero; the rows beside it stand; a `results[i]` that is not an object has no id to hang a row on and fails the whole Compare instead. One asymmetry to know: a set whose every row is contradictory still "answers its request" and so replaces a held comparison, where a run-book whose Cash row is contradictory does not replace a held result. It is not silent (each row says why), so I do not call it a violation.

## The implementer's concerns — my read

1. **`runbook.ts` — confirmed, Important (I3).** The exact inputs are listed there.
2. **Pixel pin — confirmed viewport-only.** `screenshots.spec.ts` calls `toHaveScreenshot` with no `fullPage` (default false); `playwright.config.ts` sets no screenshot default; the committed baselines `inspector-{dark,light}.png` are 1440×900 (PNG header); I opened `inspector-dark.png`: the frame ends inside the "What backs this debt" / Trust cards, and the stress table is not in it. The uncommitted edit to `screenshots.spec.ts` adds pages and routes, no `fullPage`. Text changing below the fold cannot move layout above it, and the spec's `ready` gate only counts the table's rows (3) — unchanged. `/lab?scenario=eth_minus_30`: newly 118 → crit as before; it does not move.
3. **`+$…` for a negative `extraInterest` — real, wire-legal, not reachable today. Minor (M4).** `additional_interest_usd` is a plain `Decimal` (`^-?[0-9]+$`, `openapi.yaml:3081`, `:2300`) under `basis: delta-only`: it is the rate change's OWN contribution to debt over the horizon, so a negative would mean a rate cut — debt growing by that much less than the unobserved base. The server cannot produce one: the scenario loader rejects `annual_delta_bps <= 0` (`internal/risk/scenario.go:515`). If one arrived, both tables would print "+−$12 interest".

## Strengths

- The (b) diagnosis. The brief asked to "prove by pin whether the hold already survives"; the implementer proved it did not, found that the leak was at the ASK, and fixed it there with a definition-free predicate rather than storing a verdict on every record.
- An envelope fault carries NOTHING of the body: `run: null`, `identity: null`, the definition's chips — so the drawer, the age and the banner are unreachable by construction rather than guarded one by one. The E2E pin asserts the absences (no batch chip, no Computed, no Engines, no drawer button).
- `readEngine` classifies before consulting the definition, so an unreadable body is never "not modelled" — pinned.
- The classifier was probed against every committed and demo body (clean), and its required members match the schema's `required` lists exactly.
- The moves are clean: `computable` / `readable` / `roomWords` / `sideRoomWords` / `rowVerdict` left no copy behind; `cannotSayTitle` merged the two pages' hovers and carried the Inspector's unreadable-duration guard to the lab's cell (the parent's lab cell printed "the — horizon carries no verdict").
- `newlyTone` is the single source of both tones rather than a second copy of the rule in the component.
- The Inspector E2E pin reasons about the false figure a negative debt would produce (`$9,428` = cap + |debt|) and asserts its absence.

## Issues

### Critical

None.

### Important

**I1 · A refusal without a string `code` still throws at render, and the classifier admits it.** `lab-engine.ts:61` calls `plainCause(refusal.code, refusal.detail)`; `refusal-phrasebook.ts:19` does `code.trim()` once the phrasebook misses and `detail` is not a non-blank string. A 2xx run-book with `excluded_engines: [{ "engine": "debt_manager" }]` (or `code: null`, or a number) passes `classifyRunBookEnvelope` — `lab-classify.ts:410` admits any object — and `deriveLabView` throws `TypeError … reading 'trim'`. The new spec blesses exactly this input as clean (`lab-classify-envelope.spec.ts:48` expects only `excluded_engines[1]`). `readsAsAnswer` returns true for it (`lab-engine.ts:108`), so it would also move into the hold. Pre-existing at the parent, but it is the class (a) exists to close and the hunt was asked for. Fix inside the owned files: admit an `excluded_engines` element only when it is an object whose `engine` and `code` are strings (both envelopes), and flip the spec line. *Within (a)'s purpose; not in its enumerated list.*

**I2 · A set 2xx whose body is JSON `null` throws inside the state updater.** `runbookSet.ts:270` passes the parsed body verbatim; `classifySetEnvelope` dereferences it (`lab-classify.ts:425-426`, `s.evaluation` on null). It is reached first from `withSetSettled` inside `setSet` (`lab-reading.ts:74`) and again from `compareOf` (`lab-view.ts:273`) — a route crash, not a failed Compare. Primitives and arrays are fine (every member is named); only `null` throws. Pre-existing (the parent threw on `set.requested_scenario_ids`). Fix: both envelope classifiers begin by naming a body that is not an object. *Within (a)'s purpose.*

**I3 · `runbook.ts` rejects before the classifier: the failure is mislabelled "unreachable" with a raw TypeError, and five classifier names are dead in the running app.** Controller-owned follow-up; the implementer's concern 1. `runbook.ts:144-153` destructures and maps the raw body, so the promise rejects and `lab-reading.ts:141` records `{ kind: "unreachable", message: <TypeError text> }` — the page says "The service could not be reached. Cannot read properties of undefined (reading 'map')." of a service that answered 200. The hold stands (it is a failure). Inputs mislabelled, exactly:

- the body is `null` (the destructuring throws), or any non-object or array (`engines` is undefined);
- `engines` missing, `null`, or any non-array (object, string, number);
- an `engines[i]` that is `null`, or a primitive (its `projection` is `undefined`, so `refineProjection(undefined)` throws);
- an `engines[i].projection` that is absent, a non-null non-object, an object without an array `horizons`, or one with a `horizons[j]` that is `null` (`packages/client-ts/src/refine.ts:204-212`).

Consequence beyond the label: `classifyRunBookEnvelope`'s `engines` / `engines[i]` and `classifyRunBookEngine`'s `projection`, `projection.horizons`, `projection.horizons[i]` (`lab-classify.ts:264-276`) can never be printed by the app; their pins pass only because they call `deriveLabView` directly. Smallest fix, in `runbook.ts` only — make the sealing total: a body that is not a JSON object is `{ kind: "failed", status, message: "2xx response body is not a JSON object" }`; an `engines` that is not an array passes through verbatim; an element is refined only when it is an object whose `projection` is an object with an array `horizons` of objects, and otherwise passes through verbatim. The classifiers already name every one of those. One unit pin per input class, plus one E2E (`engines: undefined` → contradictory naming `engines`, the held result standing). `runbookSet.ts` does not map its body — the implementer is right — and needs only the `null` case (I2, or a one-line "not a JSON object" refusal there).

**I4 · Same row, same column header, two pages, two answers — for projection rows. A ruling, not a defect of this commit.** The judge is one; the word functions are two: `stressVerdictWords` (`address-stress.ts:234-251`) and `rowVerdictWord` (`lab-address.ts:232-245`). Under "Becomes liquidatable?" the demo near address's rate row reads **"Not within 90d"** on the Inspector and **"No"** (hover: "stays inside its cap through 90d…") on the Scenarios page; a flipping projection reads **"Within 90d"** vs **"Yes · within 90d"**. The doc comment this commit wrote on the Inspector's function states the law — "a projection answers only in its horizons' terms, never a bare 'No'" — and the lab's cell prints the bare "No". Spot, cannot-say and inapplicable words are identical (the literals "Already liquidatable" and its hover, "Yes", "No", "Cannot say" are duplicated across the two functions; only the cannot-say hover is shared). Both wordings are pinned from earlier plans (`inspector.spec.ts:396`, `lab.spec.ts:906`, `lab-address.spec.ts:337,341`), so unifying moves pins on one page; neither pixel pin is affected. *Not plan-mandated; the plan asked for one judge and got it.*

### Minor

**M1 · Two predicates answer "does this body read".** The view's is `book.state !== "contradictory"` (`lab-view.ts:225`); the record's is `readsAsAnswer`, which re-walks `readEngine`'s Cash arms (`lab-engine.ts:106-112` beside `:60-70`). They differ in two corners, both where the state is decided before the Cash row is judged: (i) a version-skewed body with a malformed Cash row → the view says "definition changed" and releases; (ii) the implementer's own — a definition that does not model Cash beside a malformed Cash row → "not modelled". In both the record keeps the OLDER hold, so a later failure brings the older result back under "Run again failed" after the page had moved on from it. Nothing false prints. The report names (ii) only; neither is pinned.

**M2 · Two wording changes on the Inspector the report does not list.** An inapplicable row with no reason: "not applicable" → "the engine gave no reason" (`address-stress.ts:189`; `reason` is optional in `ScenarioResult`) — pinned in the unit spec, deliberate, fine. A null scale: the room cell printed "—" (`moneyFor(null)`) and now prints "unreadable scale" (`StressTable.tsx:33` → `address-stress.ts:170`). `view.decimals` is also null when there is no Cash wire at all, where "unreadable scale" names a cause that is not the cause; and the projection cell of the same table still goes through `moneyFor(null)` ("+— interest"). Unpinned on the Inspector.

**M3 · (d) — drift against the parent's strings, character for character.** Every reachable sentence is identical. Three differences, none visible: (1) an empty failure dek (an envelope `message: ""`) — the parent left a double space before "The result below…", the lib leaves one; HTML collapses both; (2) the parent's Compare sentence dropped `headline.rest`, the lib includes it — every failure headline is `refused(...)` with `rest: ""`, so unreachable; (3) a null batch — "Batch ? …" / "stands for batch ?." became "Its batch …" / "stands for the batch it names." — unreachable (a rerun-failed book always carries its run). `CompareCard.tsx` still composes copy: `finding()`'s idle, running and failed-with-nothing-held arms (:45-51; the last drops `rest` where the held arm now keeps it), the freshness notice (:91-106) and the three "No plot…" strings. Pre-existing; (d) named one sentence.

**M4 · The horizon-interest line exists in three copies, two of them composed in components without the negative gate.** `StressTable.tsx:62` and `AddressWorkspace.tsx:54` are the same template string; `lab-address.ts:106-108` is the lib's, with `< 0n → "not computed"`. One function in `address-stress.ts` would close concern 3 and both compositions together. Beside it, the lab's dek still says "The — horizon carries no verdict." for an unreadable duration (`lab-address.ts:120`) while its own cell's hover now says "a horizon with an unreadable duration…".

**M5 · Layering and leftovers.** `address-stress.ts:10` imports `UNREADABLE_SCALE` from `lab-headline` — the Inspector's row module now depends on the Scenarios page's headline module for one word (a third definition of that word sits in `app/inspector/[addr]/money.ts:7`). `lab-headline.ts:10` type-imports `Banner` / `HeldCondition` / `Retained` from `lab-view`, which value-imports `lab-headline` — type-only, so no runtime cycle, but the sentence module now depends on the view's types. `readableSide` is exported with no consumer outside its file; `isRecord` in `lab-classify.ts` lost its last external consumer.

**M6 · Object-typed string scalars still throw as React children.** `scenario_config_version` is the Config chip's value verbatim (`lab-view.ts:176`); `path_assumption`, `served_at`, `applied_shocks[i].asset` / `.source`, `held_flat[i].asset` / `.source` print in the drawer; a set result's `label` is the plot row's label. An object in any of them throws at render. A MISSING `scenario_config_version` does not throw — it prints an empty Config chip in the warn tone. The least likely skew class and outside the brief's list; named for completeness.

**M7 · The (b) E2E pin fails on the parent for (a)'s reason.** Its second body (`coverage: undefined`) reads as an ordinary result on the parent, so the parent fails with no banner at all. Against (a)-without-(b) it does fail for (b)'s reason (the banner would name 18,252). The unit pin isolates (b) cleanly (both bodies are malformed under either revision). Two bodies malformed in the Cash row would make the E2E pin independent of (a).

## Assessment

The commit does what the plan asked, item by item, and the one place the brief left open — whether the hold already survived — was answered with evidence and fixed at the root. The pins are honest: each behavioural one fails on the parent for a stated reason. What remains is the edge of (a): the classifier judges containers and the fields the page reads from `batch` and `evaluation`, and stops at "is an object" for the elements of `excluded_engines` — one of which is dereferenced into a `.trim()` — and at the body itself. I1 and I2 are a few lines each in owned files with a pin apiece; I3 is the controller's and is what makes (a) true in the running app rather than only in `deriveLabView`. I4 is the answer to a question the controller asked: one judge landed; one vocabulary did not, and was not asked for.

## Not verified

- Nothing was executed: no `tsc`, no eslint, no unit run, no build, no E2E. The implementer's "920 passed" is taken as reported. The six new or changed E2E tests (4 new + 1 assertion in `lab.spec.ts`, 1 new in `inspector.spec.ts`) have never run; I checked only that their helpers exist (`mockLab`, `setVerbatim`, `shapeSet`, `preflight`, `POST_CORS`, `withCash`, `chip`, `expectNoCashZero`), that the demo definition is `eth_minus_30 · v1` covering both engines, and that a later-registered route takes precedence.
- The throw paths of I1, I2 and I3 are established by reading (`refusal-phrasebook.ts:14-20`, `lab-classify.ts:425-426`, `runbook.ts:144-153`, `refine.ts:204-212`), not by running them.
- The pixel pins were not rendered. That the Inspector pin does not move rests on: no `fullPage`, 1440×900 baselines, and the baseline image ending above the stress table.
- That `liquidatable: null` on a stress side becomes the `unknowable` verdict rests on the client's `lookup()` refine, which I did not open.
- That `KpiTile` emits `data-tone` for every tone (the (c) E2E assertion) is inferred from the existing contradictory-tile assertions in the same spec.
- `lookupRepaired` / Task 10 interplay: out of scope, not looked at.


---

# Re-review 1 (6acf8d1 + c79a773)

Package: `3066133..c79a773` — STEP 1 `6acf8d1` (13 paths), STEP 2 `c79a773` (5 paths), both subject-only messages, author Kase Lunt, no attribution line. Read-only, as before: nothing built, no suite run, no server touched; this section is the only write. Everything below was read from the COMMITS (`git show c79a773:<path>`), never the working tree; "parent" = `3066133`. The implementer's account (`task-9-fix-round-1-handback.md`) was treated as claims and checked.

## Verdict

**The four rulings are met — I1, I2, I3, I4 all CLOSED — and the taken Minors landed as described. RE-REVIEW: not closed, on one count: the hunt I3's ruling ordered found one remaining 2xx → render-throw path (N1, `movers[i].account`), in the engine classifier this round did not touch.** It is pre-existing, one line and one pin in an owned file. Nothing this round introduced is wrong.

## The rulings

| Ruling | | Trace, and whether the pin can fail on the parent |
|---|---|---|
| I1 — a refusal whose `engine` / `code` is not an acceptable string is NAMED, never read, never held | CLOSED — can fail | `aRefusal` (`lab-classify.ts:396-402`): a non-object element is `excluded_engines[i]`; an object is checked member by member → `.engine` (non-blank string), `.code` (string). Used by both envelopes (:460, :490). `readEngine` classifies the envelope before `plainCause` is reached (`lab-engine.ts:53-62`), so past that line `refusal.code` is a string; `refusal.detail` is left to the phrasebook's own `typeof` gate. `readsAsAnswer` classifies first (:114-115) → never held. `refusal-phrasebook.ts`: empty diff `3066133..c79a773`. The blessing line is flipped: `{ engine: "debt_manager" }` now expects `excluded_engines[0].code` (envelope spec, the 13-case table, both envelopes). On the parent: the envelope cases fail (any object was admitted), `readsAsAnswer(noCode)` is `true`, and `readEngine(noCode, …)` throws in `plainCause`. |
| I1 deviation — an EMPTY `code` is admitted | ACCEPT | Confirmed against the yaml: `EngineRefusal` requires `[engine, code, detail, note]`, `code` is `type: string` with no `minLength` or pattern. Confirmed against the phrasebook (`refusal-phrasebook.ts:14-20`): `code: ""` misses the map; a non-blank `detail` is printed; otherwise "the engine gave no reason". The page prints **"Cannot say — the Cash book is withheld under ETH -30 percent."** / **"Cash — the engine gave no reason. A withheld book is not a computed book…"** — no empty parentheses, no "undefined" (pinned: `lab-view.spec.ts`, the `emptyCode` leg). Refusing it would refuse a body the contract allows. One asymmetry to know: `engine` is ALSO `type: string` with no `minLength`, and an empty or blank `engine` now refuses the whole body. I accept that too — a refusal that names no engine cannot be placed, and admitting it would print a bare " withheld" on the Engines chip — but it is a judgement beyond the yaml's letter, not a reading of it. |
| I2 — a non-object body is named first, by one shared sentence; no updater can throw | CLOSED — can fail | `BODY_NOT_OBJECT` (`lab-classify.ts:370`), returned first by both classifiers (:446, :477) for null, a primitive and a list (`isObject` excludes arrays); worded through `contractFaults` (:373), the one place a name becomes a reason (`lab-view.ts:151,185`, `lab-compare.ts:170`). Re-walk of my I2 path at the new commit: `withSetSettled` → `setMembership` (`lab-reading.ts:74`) → `classifySetEnvelope(null)` → `[BODY_NOT_OBJECT]`, no dereference; `heldSetOf` (:60) the same; `compareOf` (`lab-view.ts:266`) the same → a failed Compare, `held` standing. Run path: `heldOf` → `readsAsAnswer(body)` → `false` for any non-object; `resultBook` → the envelope arm → `emptyBook` (`run: null`, `identity: null`) and both readings `unreadable`. Every member check past the object gate is a `typeof` / `Array.isArray` / wire-guard test on `unknown` — I found no body shape that reaches a dereference. On the parent both classifiers throw on `null` (`reading 'batch'` / `reading 'evaluation'`). |
| I3 — `sealRunBook` is total | CLOSED — can fail | `runbook.ts:79-101`, called at :175. My list, re-derived: body `null` / primitive / array → returned verbatim → `ok` → `BODY_NOT_OBJECT`. `engines` missing / null / non-array → verbatim → `engines`. `engines[i]` null / primitive / array → verbatim → `engines[i]`. `projection` absent (`"projection" in engine` false) or non-object → `projection`; an object without an array `horizons` → `projection.horizons`; a null or primitive `horizons[j]` → `projection.horizons[j]`. A transport failure is still `unreachable` (:160-166, untouched); a 2xx that is not JSON is still `failed` (:172-174). The implementer's extra class is real: `liquidationVerdict` throws a `ContractInvariantError` for anything but true / false / null (`packages/client-ts/src/refine.ts:80-89`); sealing is per horizon (`sealable`, :79) and `classifyRunBookEngine` names the unsealed horizon `projection.horizons[i].becomes_liquidatable` (`lab-classify.ts:291`). `sealRunBook` itself cannot throw: every dereference is behind `isObject` / `Array.isArray`, and `refineProjectionHorizon` is only handed a horizon whose verdict is one of the three. Pins go through `runBookScenario` with a stub `fetchImpl`, so they pin the seam, not the helper; on the parent the null / non-list / null-element / bad-projection cases reject the promise. The first and last seal tests pass on both revisions — they are regression pins and say so. |
| I3 — the set path (`runbookSet.ts` unchanged: empty diff) | NO THROW FOUND | A null `results[i]` → `anObjectWithStrings` names `results[i]` → failed Compare. A null `results[i].engines[j]` → `classifySetResult` names `engines[j]` → one contradictory row, the rows beside it standing. A projection inside a set result is never sealed and never read for a verdict: `classifySetRunEngine` walks it behind `isRecord` / `Array.isArray` and `rowOf` reads none of it. `CompareView`'s unclassified scalars (`configVersion`, `servedAt`, `evaluated`, a row's `version`) are carried and never rendered (grep over `app/lab`). |
| I3 — the hunt: any path still left | **ONE — N1** | Below. |
| I4 — one word function, the Inspector's words | CLOSED — can fail | Both tables call the identical expression, `const verdict = stressVerdictWords(rowVerdict(r));` — `AddressWorkspace.tsx:38`, `StressTable.tsx:42` — and render it through the same three-arm cell. `rowVerdictWord` / `RowVerdictWord` / "Yes · within" / "longest horizon projected": zero hits in `web/` at `c79a773` (code, comments, specs). Tones unchanged (within → warn, inside → none, yes / already → crit, cannot-say → refused). On the parent: the lab e2e's "Within 90d" / "Not within 90d" fail ("Yes · within 90d" / "No"), and the unit pin's export and source assertions fail. |
| I4 deviation — the component imports the word function, not `lab-address.ts` | ACCEPT | The law is one function reference on both pages; `lab-address.ts` has no use for it and an unused import fails lint. |

## I4's "by construction" pin

`lab-address.spec.ts:416-432` has three kinds of assertion, of unequal strength:

1. The OUTPUT pins (holding → "Not within 90d" + hover; flipping → "Within 90d", warn; neither is "Yes" / "No") — real, and what the law is.
2. `Object.keys(labAddress).filter(/verdictword/i)` is empty — a real runtime pin of the retired NAME; it fails on the parent. It does not catch a second word function under another name.
3. The SOURCE-TEXT assertions (:428-429) — weak in both directions. They pass vacuously if the expression survives in a comment or an unused `const` while the cell prints from something else (`toContain` reads comments too; today neither file's comment happens to hold the expression). They rot on an innocent edit: rename the loop variable `r`, or let a formatter split the import, and the pin fails with nothing wrong. They also do not show that the CELL prints `verdict.text`.

A sturdier form: make divergence impossible instead of detecting it in text — export ONE cell function from `address-stress.ts` (`verdictCell(row)` = `stressVerdictWords(rowVerdict(row))`) or one shared cell component, have both tables take it, and pin the module's export surface at runtime (assertion 2's style). The behavioural guard already exists and is the one that matters: the same demo fixture is asserted on both pages (`inspector.spec.ts` "Not within 90d"; `lab.spec.ts` "Not within 90d" + hover, "Within 90d"). I would drop the two source-text lines rather than keep them; not blocking.

## M1 — two rules, pinned at both corners: my read

Honest to leave for now. I re-checked the direction that would matter: **no input makes the record hold a body the view refuses** — both rules run the same `classifyRunBookEnvelope` and `classifyRunBookEngine` (this round's new checks included) and the same merged `laneReading`, so what reads is never contradictory. The rules part only the other way (the view releases, the record declines to hold), and nothing false prints in either corner.

One consequence IS reader-noticeable, in corner one only, and neither the pin nor the doc comment states it: result R1 on the page → a 200 that is version-skewed AND malformed in Cash → the page withdraws the figures and says "ETH -30 percent changed since this result was computed" → the reader presses Run again → it fails (503, say) → **R1's figures come back** under "Run again failed — … The result below stands for batch N." A failed re-run making figures reappear that the page had just withdrawn is visible. It needs three coincident conditions, and the alternative the parent had was no better ("A result for batch N is retained but not shown" of a body that does not read). Corner two resurrects only a "not modelled" book — nothing to notice.

The ruling I would recommend when it is taken up: the view asks "is the body malformed" BEFORE "which version" and "does the definition model Cash" — a malformed body is a failed answer whatever else it says. Then the two rules are one, R1 stands from the moment the bad body settles, and `readsAsAnswer`'s second paragraph can go. Task 12 is soon enough.

## M6 — the classifier refuses more: checked

Every newly judged member is `required` and `type: string` in `api/openapi.yaml`, none nullable: `RunBookResponse` `served_at`, `scenario_config_version`, `scenario_id`, `scenario_version`, `label`, `path_assumption` (:4936-4948); `AppliedShock` `asset` (`Address`, a string), `source` (:3017-3025); `HeldFlat` `asset`, `source` (:2563-2571); `SetRunScenarioResult` `scenario_id`, `label` (:5314-5328); `SetRunEngineAbsence` `engine`, `reason` (string enum; required `[engine, reason, counts, note]`); `EngineRefusal` `engine`, `code`. The optional one nearby — `Shock.asset` — is NOT judged (`shocks` is `anObject` only, and `shockKey` reads it as `s.asset ?? ""`). Nothing optional or nullable is refused.

Fixtures, by reading: I ran the new member checks (a Python transcription, not the suite) over every `web/tests/fixtures/**/run-book*.json` at `c79a773` — 15 run-books, 4 sets, the two non-body files — all clean, including that every projection horizon carries a boolean-or-null `becomes_liquidatable` and so seals. The unit helper `runBookOf` supplies all six strings and seals with `refineProjection`, which gives the same `liquidation_verdict` the per-horizon seal gives.

## Issues

### Important

**N1 · A mover without a string `account` still throws at render; the engine classifier does not judge it.** `MoversTable.tsx:24` calls `truncateAddress(m.account)`; `format.ts:103` reads `address.length`, then `.slice`. A 2xx run-book whose Cash row carries `movers: [{ …, account: null }]` — or no `account`, or a number — classifies clean: `classifyRunBookEngine` judges a mover's four decimals and nothing else (`lab-classify.ts:231-242`), `moversTable` passes `account` through (`lab-movers.ts:100`), the reading is a `result`, and the table throws `TypeError … reading 'length'` while rendering. `readsAsAnswer` is true for the body, so it would also enter the hold. `RunBookMover.account` is `required` (`openapi.yaml:4790`), so no conforming body is affected by judging it. Unlike M6's object-for-string class, this one throws for the ordinary skews too (missing, null). Pre-existing and outside the four rulings, but it is the answer to the hunt's question. Fix, in the owned classifier: `movers[i].account` must be a string; while there, `note` and `hf_transitions.note` (printed as React children in the drawer, `AssumptionsDrawer.tsx:71-72` — an object in either throws when the drawer renders them; missing or null renders nothing). One pin per member.

### Minor

**N2 · A listing id outside the id pattern is a rejected promise labelled "unreachable".** `runbook.ts:144-149` THROWS for an off-pattern id where `runbookSet.ts` resolves `refused-locally`; the hook's rejection arm (`lab-reading.ts:141`) then says "The service could not be reached." for a request that was never sent. The id comes from a 2xx LISTING body, not a run-book body, so it is beside I3's letter; controller-owned file; pre-existing. The rejection arm is otherwise unreachable now, which is the point of I3.

**N3 · The unsealed-horizon check reads the sealed field's name off the wire object.** `lab-classify.ts:291` admits a horizon when `liquidation_verdict` is one of the three. A horizon left unsealed (its `becomes_liquidatable` is not boolean-or-null) that ALSO carried a stray wire member named `liquidation_verdict` would pass and be read from a field the contract does not have. It needs a server that emits the client's internal field name, and nothing in book mode reads a run-book horizon's verdict today. Sturdier: also require `!("becomes_liquidatable" in horizon)`.

**N4 · Leftovers.** `sealRunBook` is exported and has no consumer outside `runbook.ts` (the pins rightly go through `runBookScenario`). The `Banner` / `HeldCondition` / `Retained` re-export (`lab-view.ts:39`) is still needed — by exactly one file, `StaleBanner.tsx:3`, which already imports from `lab-headline`; pointing that import there retires the re-export. The non-object sentence starts lowercase after a full stop in the banner ("…contradicts itself. the response body is not a JSON object.") — natural for a field name, a little odd for a sentence; pinned as is.

### What the round introduced — looked for, not found

A pin that cannot fail (other than the source-text lines above, which can pass vacuously); copy composed in a component (one string moved INTO the lib — `compareFailedLine`, `CompareCard.tsx:51`; nothing moved out; `AddressWorkspace.tsx` only swapped the function); an unguarded wire read (`sealable` and the new classifier check both sit behind an object gate); a comment naming the round, the review or an issue id (grep over the added lines: none).

## Left Minors — can any not wait for Task 12?

None blocks. M2 (the Inspector's "unreadable scale" where there is no Cash wire at all) names a cause that is not the cause, so it needs its ruling in that wave rather than later, but it is reachable only when stress rows exist for an account whose lookup has no Cash position. M3's remainder, M4's three copies (a negative `extraInterest` stays unreachable: `scenario.go:515`) and `UNREADABLE_SCALE`'s home are tidiness. N1 is the one I would not let wait.

## Not verified

- Nothing was executed: no `tsc`, no eslint, no unit run, no build, no E2E. "937 / 937" is the implementer's report. The new and changed E2E tests (`lab.spec.ts`: two new tests, the re-pointed one-address assertions, the re-bodied (b) pin) have never run; I read them against the helpers (`mockLab`, `json`, `preflight`, `POST_CORS`, later-route precedence) only.
- N1's throw is established by reading (`MoversTable.tsx:24`, `format.ts:102-105`, `lab-movers.ts:100`, `lab-classify.ts:231-242`), not by running it. Whether the drawer's children are reconciled while it is closed (the `note` half) I did not determine.
- "No body shape throws in the updaters" is an argument from reading every dereference in both envelope classifiers, `readsAsAnswer`, `setMembership`, `heldOf` / `heldSetOf`; it is not a fuzz run. I did not re-read `classifyRunBookEngine`'s and `classifySetRunEngine`'s unchanged bodies line by line this round beyond the movers and projection blocks.
- The fixture check covered the members THIS round added, over `run-book*.json` only; the earlier envelope checks over those files rest on the implementer's run.
- The pixel pins: nothing in this package renders inside either pinned viewport state differently by reading (`/lab?scenario=eth_minus_30` is book mode; the Inspector's words did not change), but nothing was rendered.
- The uncommitted Phase 0 / integrator edits in the tree were not read and are not judged.


---

# Re-review 2 (84b57f1)

Package: `bc40bdc..84b57f1` — one commit, 11 paths (four `web/lib`, seven specs; no component), subject-only message, author Kase Lunt, no attribution line. Read-only, as before: nothing built, no suite run, no server touched; this section is the only write. Everything was read from the COMMIT (`git show 84b57f1:<path>`), the parent from `84b57f1~1`; the uncommitted page work in the tree was not read. The implementer's account was relayed by the controller and checked as claims.

## Verdict

**RE-REVIEW: closed.** N1, N2 and N4 are CLOSED, each with pins that fail on the parent. The throw-hunt is finished: no unjudged member of a run-book or set body now reaches a `.length` / `.slice` / `.map` / `.trim` / `BigInt(...)`; ONE unjudged member still reaches a React child — a lane's `label` on an unmerged grid — and it throws only for an object, the class round 1 graded Minor (N5). Two neighbours of it are named with it (N6, N7). None needs another round; all three are one line each in the mover / lane blocks the next visit to `lab-classify.ts` will pass through.

## The three items

| Item | | Trace, and whether the pin can fail on the parent |
|---|---|---|
| N1 — a mover's `account`, the engine's `note`, the matrix's `note` | CLOSED — can fail | `lab-classify.ts:242` (`movers[i].account`, first inside the mover, before its four decimals), `:205` (`hf_transitions.note`, closing the matrix), `:306` (`note`, closing the engine after the projection). Contract re-checked: `RunBookMover` requires `account` (`$ref Address`, a patterned string), `RunBookEngine` requires `note` (`type: string`, :4931), `RunBookTransitions` requires `note` (`type: string`, :4746); none nullable; an empty note stays clean (`typeof`, no length floor). Because `readEngine` and `readsAsAnswer` both delegate to this classifier, the body is `unreadable` by the member's name, never a `result` — so `LabSurface` never mounts `MoversTable` for it and the drawer never receives its notes — and it never enters the hold. On the parent: `corrupted(...)` returns `[]` for all fifteen shapes and `readsAsAnswer` is `true` for all seven bodies — every new assertion fails. The claim about the set path holds: `SetRunEngineSummary` carries no movers, and nothing `rowOf` / `compareRowWords` / `CompareCard` prints comes from a note (`classifySetRunEngine`, `:318-345`, judges every member `rowOf` reads, `flipped_to_eligible` included). Fixtures by reading: all 15 committed run-books carry string notes and string accounts. |
| N2 — an off-pattern id resolves `refused-locally` | CLOSED — can fail | `runbook.ts:148-150` returns the arm (`:75`) with the set path's message to the letter — I compared it with `runbookSet.ts`'s own line; the seal spec also asserts the two outcomes are `toEqual` for the same id, with a counting `fetchImpl` that must stay at zero. `lab-view.ts:64` (the state), `:253-254` (`failureOf` → the existing `failureHeadline("refused-locally")`, "Nothing was sent."); `lab-library.ts:70` ("Not sent"). `FAILURE_WORD` is an exhaustive `Record` over the outcome kinds and `failureOf` an exhaustive switch, so a missed arm would not have compiled. `LabSurface.tsx:200` emits `book.state` generically — no component needed the word. On the parent the five ids REJECT (`await` throws in the spec) and `failureOf` has no arm (`failure.state` on `undefined`). With this in, the run hook's rejection arm (`lab-reading.ts:141`) catches nothing the caller itself throws. |
| N4 — the capital at the one join; `sealRunBook` un-exported | CLOSED — can fail | `contractFaults` (`lab-classify.ts:388-389`) capitalises the body's sentence only; a field's name keeps the wire's case; `BODY_NOT_OBJECT` and the `fields` a reading carries stay lowercase (`lab-view.spec.ts` still expects the constant on `book.cash.fields` — consistent: the capital belongs to the sentence a reader sees, not to the name). The banner pin (`lab-headline.spec.ts:292-295`) is built from `contractFaults([BODY_NOT_OBJECT])`, not a hand-typed string, so it pins the join. The moved strings in lab-view, lab-compare, the envelope spec and the two `lab.spec.ts` lines all fail on the parent's lowercase. `sealRunBook` is module-private (`runbook.ts:99`); no spec imported it. |

## (1) The hunt, finished

I walked every component under `web/app/lab/**` that receives something derived from a run-book or set body — `LabSurface`, `LabTiles`, `TransitionCard` (+ the kit's `Heatmap`), `MoversTable`, `AssumptionsDrawer`, `CompareCard` (+ `DotPlot`), `LegacyCompare`, `LegacyResult`, `StaleBanner`, the library rows — and the lib functions under them (`lab-view`, `lab-engine`, `lab-library`, `lab-compare`, `lab-movers`, `lab-transitions`, `lab-headline`, `resultIdentity`). For each body member I asked: judged by a classifier or a wire guard before it reaches a string method, an array method, `BigInt`, arithmetic, or a React child?

**Judged (nothing found):** every `BigInt(...)` in `readEngine`, `rowOf`, `lab-movers` and `lab-transitions` sits behind `isWireDecimal` / `wireBigInt`; every count behind a population / occupancy / signed-count guard; every `.map` / `.find` / `.some` / `.forEach` over a body list behind `Array.isArray` with its elements judged objects; `plainCause` receives a string; `truncateAddress` a string; the drawer's printed members (`path_assumption`, the two shock lists' `asset` / `source`, `out_of_model`, `notes`, both engine notes, the identity line) are strings, and its decimals go through `exact()` (guard, else `JSON.stringify`); the chips are strings or grouped populations; the set's rows print a judged `label`, a judged `reason`, lib-computed words. Members the lib READS unjudged but only compares or interpolates — `lanes[i].kind`, `lanes[i].lower_wad` and a bucket's `lower_wad` (through `wireBigInt`, which is total), the two `comparator`s, `engines[i].engine`, a refusal's `detail` — end in a contradiction sentence or a template string, never a throw.

**What is left — one React child:**

**N5 (Minor) · `hf_transitions.lanes[i].label` reaches the grid's header cells unjudged on an unmerged grid.** `verbatimBands` passes `label: lane.label` through (`lab-transitions.ts:268`) and the kit prints it as a child (`Heatmap.tsx:76,83`). Unmerged is ALWAYS the legacy engine's grid, and the Cash grid whenever its edges are not the contract's. An object there throws at render (the legacy fold's `<details>` is closed, but its children are rendered); a missing or null label prints an empty header — an absence, not a crash. `label` is `required` in `RunBookTransitionLane` (`openapi.yaml:4498`), and all 15 fixtures carry strings, so judging it refuses nothing served. Same class as round 1's M6 (object-for-string only), hence Minor.

Two neighbours, not throws, in the same two blocks:

**N6 (Minor, a law's letter) · `movers[i].became_eligible` is read unjudged into a verdict word.** `lab-movers.ts:108` → `MoversTable.tsx:30`: `null` is "Cannot say" (right); an ABSENT member is `undefined`, which is not `null` and is falsy — the cell prints **"No"**; a string `"false"` prints "Yes". The schema makes it `required`, `boolean, nullable`. A missing verdict rendering as "No" is what the refusal law forbids, even though it takes a contract-breaking body to get there. One check beside the account: boolean or null.

**N7 (Minor) · `movers_note` as a `title`.** Confirmed by reading, not by running: a DOM string attribute is set as `'' + value`; a plain JSON object coerces to "[object Object]" and does not throw (React's coercion guard fires only for values whose `toString` would throw — symbols, Temporal-like objects — which JSON cannot carry). So the implementer is right that it is not a crash. My read: Minor, but judge it like the two notes when the file is next open — the classifier's own comment now says "text that is not text never reaches the page", `movers_note` is `required` `type: string`, a tooltip reading "[object Object]" is on the page, and the check refuses no conforming body. Leaving one of three sibling notes unjudged is the kind of exception a later reader has to rediscover.

Outside the question as asked, and not walked: the one-address workspace (its bodies are the Inspector's lookups, through the client's `lookup()` refine) and the scenarios LISTING (`GET /v1/scenarios`, read by `libraryRows`, `notRunHeadline`, the deep-link decision).

## (2) The page-test contract line

`docs/plans/2026-09-15-ui-scenarios.md:113` should now read (one insertion after `unreachable`, one clause added to the parenthetical; everything else verbatim):

```
| Surface root | `lab-surface` | `data-mode` ∈ `book · address`; `data-state` ∈ `listing-loading · listing-unavailable · not-run · running · result · not-covered · withheld · contradictory · definition-changed · not-served · no-batch · rate-limited · unreachable · refused-locally · failed` (book; `busy` is a set-run outcome, never a book state; `refused-locally` is a run whose id is outside the contract's pattern — nothing was sent) or `idle · invalid · loading · unavailable · no-position · withheld · rows` (address); `data-banner` ∈ `stale-input · superseded · rerun-failed · retained-refused` when present (a computed result is never replaced by a failed re-run: it is held under `rerun-failed`, or disclosed as `retained-refused` when its definition changed) |
```

No pin enumerates the book states: a grep for the vocabulary's distinctive run (`rate-limited · unreachable`, `unreachable · failed`) hits this line alone, and every `data-state` / `book.state` assertion in `lab.spec.ts` and `lab-view.spec.ts` names one state. The implementer's grep is right. Two other places in that plan mention the old behaviour and are narrative, not contract: `:2825` ("the rejection arms only catch a thrown id-shape error") is now false — the caller throws nothing — and the `BookState` / `FAILURE_WORD` code blocks (`:3094-3107`, `:1755`) are the plan's historical listings.

## (3) "Faults: The response body is not a JSON object."

Keep. A full sentence after a colon may take the capital; the body's sentence is the only reason in that list that IS a sentence (the others open with a field name or an id, which must keep the wire's case), and it always stands alone — both classifiers return it as the single name — so there is never a lowercase fragment beside it to look inconsistent. A second join for this one arm would re-create what `contractFaults` exists to prevent: two places where a name becomes a reason. If the colon-capital ever grates, the smaller change is on the other side — the set headline's "Faults:" prefix — not a second join.

## (4) What the round introduced — looked for, not found

- A pin that cannot fail: none. The one guarded expression (`ok.book.cash?.kind === "result" && …rows.map(...)`) evaluates to `false` against an array when the kind is wrong, so it fails rather than skips.
- Copy composed in a component: no component is in the package.
- An unguarded wire read: the three new checks are `typeof` tests behind the classifier's existing object gates; `runbook.ts` reads only its own argument.
- A comment naming the round, the review or an issue id: grep over the added lines — none; the new comments state what the member is and why it is judged.
- One cosmetic, shared with the set path and therefore not new: under a held result the banner reads "Nothing was sent. "ETH-30" is not a committed-scenario id (…), so nothing was sent." — the fact twice.

## Not verified

- Nothing was executed: no `tsc`, no eslint, no unit run, no build, no E2E. The two re-worded `lab.spec.ts` strings have not run. The implementer's fail-first observations and its "15 run-books + 4 sets through `runBookScenario`" are taken as reported; my own fixture check was a Python transcription of the new member checks (plus the three candidates N5–N7) over the 15 committed run-books — all clean — not the classifier itself.
- N7 rests on my knowledge of React DOM's attribute path (`'' + value`), not on a render.
- N5's throw is established by reading (`lab-transitions.ts:268`, `Heatmap.tsx:76,83`); I did not render a closed `<details>` to confirm its children are reconciled.
- The hunt is a reading of every body-derived dereference I could find in the files listed above, not a fuzz run; the kit components were read only where a body-derived prop reaches them (`Heatmap`, `DotPlot`, `IdentityChips`); `KitTable` was not opened — its cells arrive as nodes the lab components build.
- The one-address workspace and the scenarios listing were not walked (outside the question).
- The uncommitted page implementers' and integrator's edits in the tree were not read and are not judged.
