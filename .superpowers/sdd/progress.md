# Solvent Phase 1 — SDD Progress Ledger

> SHA REMAP (2026-07-22): history rewritten to scrub work-email identity; every SHA below refers to
> the OLD chain. Anchors on the new chain: tag v0.1.0-foundation = old 1cddde7; new HEAD after
> fix-wave = 3ef9117 (old 7467e17); control-plane install = a3b8a00 (old cea1541). Content is
> commit-for-commit identical; map any other SHA by matching commit subject via `git log --oneline`.
> Owner ratified D-001/D-002 + VISION/H0 and activated W1 on 2026-07-22 — Phase 2 is governed by
> roadmap/ from here; this file remains the Phase 1 historical record.

Plan: docs/plans/2026-07-20-solvent-phase1-recon-foundation.md (inside the Solvent repo)
Repo: C:\Users\kasel\source\repos\etherfi\Solvent — SELF-CONTAINED git repo, branch main. Parent /etherfi is NOT ours to touch (no writes, no git ops outside Solvent/). Plan paths retargeted 2026-07-21; recon agent redirected mid-flight via SendMessage.

- Task 0: complete — docker 28.4/git/curl OK; cast v1.7.1 at ~/.foundry/bin; go1.26.5 + jq-1.8.2 via winget. NOTE: shells spawned this session predate the install — prefix Go/jq commands with `export PATH="$PATH:/c/Program Files/Go/bin:$LOCALAPPDATA/Microsoft/WinGet/Links"` (tell implementer subagents this too)
- Task 1: recon complete (commit dc9597c, no code review needed — investigation artifact). Decision: GO. Key facts for later tasks: Debt Manager = HOT engine, OP proxy 0x0078C5a459132e279056B2371fE8A8eC973A9553 from block 149,521,228 (~6.4k logs/day); Aave v3 EtherFi = DORMANT, ETH mainnet (not OP!) Pool 0x0AA97c284e98396202b6A04024F5E2c65026F3c0 from block 20,625,519; Aave V4 whitelabel NOT deployed (ARFC 2026-07-14) → Observatory ships "cutover pending". Golden vector: 0x70daaac436465a0d03e45916fa68ddee6086e5fe HF=0.7329 @ ETH block 25,584,990. USER CONFIRMED GO (2026-07-21).
- Task 2: complete (commits dc9597c..30e7069 — scaffold 52f7c1e, deps prefetch 0d7a731 by controller, go-directive fix 30e7069 by controller). Review: spec ✅, 1 Important (go.mod 1.26.5 vs CI 1.24 pin) fixed inline w/ gate evidence (gofmt/vet/build clean). Minor recorded for final review: implementer report omitted gate evidence (process note only). GitHub handle: kaseLunt (module path lowercase kaselunt). USER DIRECTIVE: codex-reviewer on heaviest pieces (Task 4 store, Task 6 walker, final whole-branch review).
- Deps prefetched by controller: testify, go-ethereum, pgx/v5, goose/v3 — Tasks 3/4/5 implementers must NOT run go get and must NOT stage go.mod/go.sum.
- Wave dispatch: Tasks 3/4/5 parallel (haiku), BASE 30e7069, disjoint paths, one commit each; per-task review packages per-commit (sha^..sha).
- SANDBOX NOTE: sandbox blocks `go test` for subagents — all go build/vet/test commands in implementer dispatches need dangerouslyDisableSandbox: true (all three wave agents were unblocked this way; bake into future dispatches).
- Task 4 (store): commit 62409b0, 3/3 live-db tests. Standard review: Approved, 0 Critical/Important. Minors for final review: int32(LogIndex) cast vs INT column (plan-mandated, theoretical); no cursor-monotonicity guard; no rollback-path test; row-at-a-time inserts.
- Codex adversarial pass on store: needs-attention, 3 high. ADJUDICATED: fix now = chain-global cursor rewind (real latent data loss once Plan 2 adds 2nd OP stream) + chain-id validation + single-writer contract docs (fixer dispatched, sonnet). Documented-by-architecture = SaveBatch/Rewind fencing (single process, sequential walkers). DEFERRED TO PLAN 2 ingest rework = verify-on-conflict for divergent payloads + pgx.Batch/CopyFrom batching (Codex session resumable: 019f87a6-8eaa-7c40-8551-1f7eae76dca3).
- Task 4: complete (commits 62409b0 + fix 1b36aae, review clean — standard Approved, Codex highs adjudicated: 2 fixed + verified on re-review, fencing documented-by-architecture, verify-on-conflict + batching DEFERRED TO PLAN 2 with Codex session 019f87a6-8eaa-7c40-8551-1f7eae76dca3).
- Task 6 (walker): commit 99479d8, 6/6 tests. Standard review: Approved, 1 Important (caught-up reorg-check not pinned by any test — need next>safe + hash-mismatch test) + design catch: rewind is NOT iterative-deepening (target hash taken live ⇒ next Step always matches; forks deeper than 2×Confirmations leave stale rows below target — plan's own claim was wrong). Codex verdict: NO-SHIP (2 critical: deep-fork masking, mid-Step reorg race; 1 high: no log validation; 1 low: window overflow). USER DECISION (2026-07-22): FULL UPGRADE NOW — verified-ancestor rewind (replaces 2×Confirmations policy; plan superseded), coherent-window checks (tip before+after getLogs, cursor recheck pre-save), log validation (range/address/removed), overflow-safe window math, Data deep-copy, new Store.HighestLogAtOrBelow, adversarial test suite. Upgrade landed: 2b8fdf4 (Store.HighestLogAtOrBelow) + 603a880 (walker redesign), 14/14 ingest + 6/6 store, full suite green. Also 5e5bfc2: restored go.mod requires (early `go mod tidy` on pre-code module had stripped them — HEAD wouldn't have built in CI). Standard re-review: Approved, all prior findings closed. New Minors for final review: empty-Addresses set would reject all logs (wildcard streams unsupported — fine for this system, needs comment); validation-abort-with-existing-cursor untested (code-inspected safe); ancestor-probe RPC cost note. Codex re-review round 2 (session 019f87cf-a61f-7202-9200-7505d719ed31): still no-ship, adjudicated: ACCEPTED → batch fork-consistency validation (same-height hash agreement + tip-anchor match = ABA mitigation without RPC cost), conflicting-duplicate rejection + identical-coalesce, index>MaxInt32 guard, caught-up-before-increment (MaxUint64 wrap), startBlock=0 config rejection — hardening fixer dispatched (fable). RECOVERY NOTE: first hardening agent died on session limit; continuation agent completed it.
- Task 6: COMPLETE (commits 99479d8 + 2b8fdf4 + 603a880 + hardening ba087be/bfca9bd; final verification Approved — all four hardening changes traced correct, MaxUint64 test proven non-vacuous, refused-recommendation doc comments judged accurate, cross-batch duplicate residual adjudicated acceptable: only legitimate same-identity-different-payload path goes through verified-ancestor Rewind which deletes-then-reinserts). 21 ingest + 6 store + 4 config + 3 chain tests green, full suite green. Minor (cosmetic): mixed-hash error fires before conflicting-duplicate when both apply.
- Task 7: COMPLETE (commit 17b3a8f). Live smoke verified by controller directly (implementer agent kept parking on background-notification waits; controller finalized): 11,226 raw logs; op:debt-manager 149,521,228 → 150,103,227 (+582k blocks); eth:aave-etherfi 20,625,519 → 21,293,518 (+668k blocks); both cursors advancing across samples; indexer killed cleanly; .env untracked. Task-scoped review CONSOLIDATED into final whole-branch review (main.go is 89-line plan-transcribed glue, proven by 20-min live run) — final review must still eyeball it.
- Task 8: in progress — USER CONSENTED; repo live at github.com/kaseLunt/solvent (private); CI run 29890365450 GREEN first try (Minor: Node-20 deprecation annotations on checkout@v4/setup-go@v5); README badge + verified-ancestor architecture paragraph (1cddde7); tag v0.1.0-foundation pushed. FINAL WHOLE-BRANCH REVIEW: standard (fable) verdict = Needs fixes first, nothing architectural. FIX-NOW: (1) README dev flow broken — nothing loads .env, TEST_DATABASE_URL missing from .env.example (fresh cloner silently skips store tests / indexer errors); (2) empty-addresses config guard (runtime wedge → startup error) + wildcard comment on addrSet; (3) CI actions bump checkout@v5/setup-go@v6 (Node-20 deprecation). PLAN 2 LIST: TrimSpace RPC URLs, config branch tests, cursor-monotonicity, rollback test, batching+verify-on-conflict, stream attribution in step-failure log, ctx.Canceled shutdown log guard, Engine field validation at first consumption, contracts.json parse test, redacted-host rotation logging. WON'T-FIX (reasoned): combined error msg, endpoint URL in logs (secret leak), HeaderHash/Logs failover tests, validation-abort cursor test, error precedence. main.go: CLEAN (no starvation, ctx placement correct, sequential drain deliberate). README "forks of any depth" claim verified TRUE post-upgrade. Codex whole-system verdict (session 019f8809-8f78-7fe0-a28c-f562c15479b1): needs-attention, 4 high ALL ACCEPTED (eth_chainId startup verification; pg advisory writer lock — upgrades documented contract to enforced; duplicate/empty stream name rejection; per-attempt RPC timeout vs hanging endpoint) + 3 medium ACCEPTED (round-robin drain w/ stream-attributed logs + ctx.Canceled shutdown guard; Migrate ctx/goose.UpContext; Makefile/.env — converged with standard review). CONSOLIDATED FIX WAVE: commits 4a15333/0bfe4a6/e97f555/99111ee/7467e17, 41 tests green live-db, dual-daemon lock rejection + 60s boot verified live. Closing-gate re-review: ALL 8 fixes correct, 0 new Critical/Important, verdict "Phase: Ready to close". Pushed; CI GREEN at 7467e17.
- Task 8: COMPLETE. PHASE 1 CLOSED at 7467e17 (tag v0.1.0-foundation at 1cddde7; tip advanced past tag by fix wave — retag not needed, tag marks foundation milestone).
- NEW Plan 2 items from closing gate: advisory-lock session-death liveness check; "next tick"→"next round" log wording + per-walker error backoff; empty-stream-name test. Known tradeoff on record: 0bfe4a6/e97f555 don't build standalone (tip green).
- PLAN 2 SEED (full list): verify-on-conflict + pgx.Batch/CopyFrom (Codex 019f87a6...); per-block header verification / receipt membership / distinct-hash-per-height invariant scan → riskd health checks; TrimSpace RPC URLs; config branch tests (invalid-address, zero-window, empty-name); cursor-monotonicity guard; rollback-path test; contracts.json parse test; redacted-host rotation logging; Engine field validation at first consumption; Aave V4 stream when AIP executes (recon: ARFC 2026-07-14). REFUSED with traced reasoning → reject-below-StartBlock clamp (unsound: anchors siblings to unverified hashes; verified sub-StartBlock matches are chain-canonical proofs — pinned by new test) and legacy-DB migration (N/A: no production data predates the redesign). DEFERRED to Plan 2 riskd health checks → per-block header verification, receipt membership, distinct-hash-per-height invariant scan. Trust-model notes for docs: single-endpoint-per-Step affinity via sticky failover (documented, not enforced); empty-but-successful getLogs responses are trusted (inherent to RPC model); chain-id-vs-endpoint startup validation → Task 7.
- Task 5: complete (commits ad67121 + fix 8a37c07, review clean — both Importants resolved on re-review, fix verdict Approved). Minors for final review: endpoint URL not in rotation log; HeaderHash/Logs lack dedicated tests; report line-count inaccuracies.
- Task 3: complete (commit 732182f, review clean — Approved, 0 Critical/Important). Minors for final review: no TrimSpace on comma-split RPC URLs (keep .env URLs space-free until fixed); combined window/confirmations error message; untested invalid-address + zero-window branches.
- Briefs extracted: task-6-brief.md, task-7-brief.md.
