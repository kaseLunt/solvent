# Understand workflow — the vocabulary skeptic's verdict (landed after the implementation launch)

VOCAB verdict: 49 checks, 6 not confirmed
- REFUTED | Census: 134 description strings, 2 with bold, 27 with backticks (322 characters, all balanced), 0 markers in summaries and in the 6 error-response descriptions | CORR: 128 strings, not 134. Three of the 27 are per-operation response descriptions (getAddress 200, runBookSet 400, runBookSet 503 with 18 backticks). EndpointCard renders those only as title tooltips on the response chips (EndpointCard.tsx:62-70), so inlineParts cannot format them and their raw backticks stay.
- UNVERIFIABLE | OpenAPI description fields are CommonMark, so rendering markers is the contract's intent | CORR: 
- REFUTED | P1 Verification pixelPinMoves: nothing moves, because each replacement is one line of equal or shorter width | CORR: The tile sub must stay under 265px. For example 'checked rows exact · 0 drifted' (163px), 'checked rows exact (must match) · 0 drifted' (239px) or 'must-match rows exact · 0 drifted' (184px). The Compute sentence stays at 3 lines (sim: 221/295/131px in a 299px column), the Verify sentence at 2, and the receipt strip and card rows fit.
- REFUTED | P1 newWords: 'feeds registry — fingerprint matches the build's, by construction' | CORR: Say 'matches the service's, by construction' (mismatch arm: 'DOES NOT match the service's, which the contract says are identical by construction').
- REFUTED | P4 assertsFacts/newWords: null engine prints as 'any engine'; engine-scoped example 'Cash · borrow, repay · from block 25,635,600 · 50 per page' | CORR: The example pairs Cash with an Ethereum-range block, so it should read 'Aave v3 market (legacy) · … from block 25,635,600'. Also, 'any engine' sits beside 'Scope all engines' (activity-view.ts:657), giving two words for one scope on the same chip row. Use 'all engines'.
- REFUTED | P5 pinsAffected: lab-view.spec.ts:100,128; inspector pins e2e 64,66 only | CORR: Add lab-view.spec.ts:90,111,112,139,361,374,720 and inspector-view.spec.ts:150,199. Note that lab.spec.ts:1235 (Engines chip toHaveCount(0)) would pass vacuously after the rename.
! P1 moves a pinned viewport that the reader said would not move. The Verify tile sub 'checked rows exact (must match to pass) · 0 drifted' measures about 282px against a 265px tile inner width, so it wraps to two lines. The 4-tile KPI row then grows and shifts the step sentences, the receipt strip and both subject cards on verification-dark/light.png. Use a sub under 265px, such as 'checked rows exact · 0 drifted' (163px).
! P1 says something false: 'fingerprint matches the build's, by construction'. The registry is loaded at runtime from the --feeds file (cmd/api/main.go:138,289), and both fingerprints are s.registry.Fingerprint() (cmd/api/p5_evidence.go:200,205). Say 'the service's', which is the contract's `service.registry_fingerprint`.
! P4's engine-scoped example is inconsistent: 'Cash · … from block 25,635,600' pairs Cash (OP Mainnet, about 155M) with an Ethereum-range block that the e2e applies to the legacy Aave market. Also, 'any engine' would sit beside 'Scope all engines' as a second word for the same scope.
! P1 leaves one vocabulary only half-done: after 'gated' is retired from the pinned strings, it still appears publicly in VERIFY_EMPTY (verification-view.ts:282), RECEIPT_EMPTY_TITLE (:716), verificationDek (:859), the receiptLine empty/drift/failed arms (:780-787), RECEIPT_EMPTY_STATUS (evidence.ts:888, the card status row in the empty state), the rejected detail 'gated N/M exact, drift D' (evidence.ts deriveProofSubjectStatus, printed as 'REJECTED · …') and trust.ts:225 on the Inspector (pinned by inspector.spec.ts:577).
! P1 caution, not a law break: 'Cash rows 29/29' and 'Aave v3 market (legacy) rows 14/14' sit directly under 'checked rows 87/87', and 29 + 14 = 43, not 87. The weld rows are per-account samples (p5_evidence.go:350-352), so a reader may try to add them. Nothing is summed by the UI.
+ The API quickstart comment '// The deploy-bound evidence manifest (no client method yet — plain fetch).' (web/app/developers/ApiSurface.tsx:39) is false. @solvent/client has evidence() (packages/client-ts/src/client.ts:588-590, added in 529642d, contract 1.2.0), and web depends on it (web/package.json:23). It is below y=900, so no pin moves.
+ Verification e2e text pins missed for P1: verification.spec.ts:590 ('87/87 exact · drift 0'), :593 and :596 ('identical to service fingerprint, by construction'; :596 passes vacuously after the rename).
+ Unit pins missed for P5: inspector-view.spec.ts:150,199 ('complete · both engines'). The lab-view pins are :90,111,112,139,361,374,720, not :100,128.
+ Activity row 5 is deficit_created, a crit StatusPill whose top 3px (y 897-899) are inside the pinned viewport. The Type relabel changes those pixels.
+ The Activity dek parenthetical '(deficit_created)' is our composition (activity-view.ts:438-444 factDek), not service text. notFeasibleNow wrongly treats it as wire text. After P3 it would point at a word the page no longer prints.
+ More '(s)' and 'custodied' on public pages: the Inspector activity takeaway 'N custodied action(s) loaded for this account… untimed row(s) follow' (activity-rows.ts:115-134, below the Inspector fold); lab-deep-link.ts:155,161 ('published one(s)', 'scenario(s)'); evidence.ts:95 'unacked epoch(s)' (drawer); proof-data.ts:115 'fragment(s)'.
+ AssumptionsDrawer composes more headings and sentences in the component than lines 19 and 52 ('Path assumption', 'Applied shocks', 'Held flat', 'Identity', 'No mark moved: this scenario carries no price shock.', 'Nothing held flat.'). MoversTable also composes its COLUMNS headers.
+ The better source for 'exact integers, never floats' is internal/risk/types.go:12 plus internal/risk/floatcheck_test.go, not internal/derive/debtmanager.go.
+ The repo state has drifted from the brief. HEAD is now 3f9836d (a docs-only commit on top of f28d89d), and the working tree is not clean: 30+ web files are modified, apparently an implementer applying this item. lab-view.ts already exports ASSUMPTIONS_BUTTON, and verification-view.ts already uses the fitting sub 'checked rows exact · 0 drifted'. All checks above were run against f28d89d via git show; the PNG pins are unmodified.