# Task 6 review: `lab-address` (cf124d3..a444953)

### Spec Compliance

- ✅ Exactly two new files; no existing `web/lib/**` file touched (diff header: `web/lib/lab-address.ts` +131, `web/tests/unit/lab-address.spec.ts` +107).
- ✅ Module is the brief's Step 3 code line for line, minus the path comment; the spec is the brief's Step 1 minus the path comment plus exactly the two sanctioned freezes (`lab-address.spec.ts:243` dek, `:250` `capAfter`). No other pin changed — I diffed both against the brief by eye.
- ✅ Both frozen literals recompute from `tests/fixtures/demo/stress-demo-near.json` (`eth_minus_30`, 6 decimals): after `max_borrow_lt` 3,752,500,000 → `$3,752` (`humanUsdFull` groups whole dollars at ≥ $1,000, human-price.ts:31); after room 3,752,500,000 − 4,822,000,000 = −1,069,500,000 → `over cap by $1,069`; before room 190,500,000 → `$190.50` (cents under $1,000, human-price.ts:33). Dek `"Room today $190.50; after the shock, over cap by $1,069."` is right.
- ✅ Exports: `AddressWorkspaceState` (lab-address.ts:29), `AddressTile` (:31), `AddressTiles` (:35), `AddressWorkspace` (:45), `addressWorkspace` (:105); `TileTone` (:30) is an extra the brief's own code exports.
- ✅ State ladder in the brief's order: idle :107 → invalid :111 → lookup loading :112 → lookup unavailable :113 → stress loading :116 → stress error :117 → stress null (→ loading) :119 → no-position :121 → withheld :124 → rows :127.
- ✅ Every pinned sentence verbatim (:108, :111, :112, :114, :116, :117, :122, :125, :100). The unavailable dek traced: `unavailableLookupHeadline` builds `paragraph(sentence(message), "This is neither “no position”…")` (inspector-headline.ts:159-165), so `split(". ")[0]` yields the message; re-`sentence()`d → `"Rate limited (429), retry after 30s."`.
- ✅ Definitive "No Cash position…" only from a complete not-found: `stressReading` returns `no-position` only for `outcome === "not-found"` (address-stress.ts:106), and the client's `lookup()` throws on `found: false` without `lookup_complete: true` (packages/client-ts/src/lookup.ts:184-199). "Cannot say…" for withheld (:125). Idle/invalid/loading/unavailable all `tone: "refused"` via `refused()` (:57).
- ✅ Tiles and tones: `roomBefore` warn for `near`, crit for `liquidatable` (:135); any negative room crit and worded "over cap by" (:85, :91); `roomAfter` crit when over cap or verdict liquidatable (:144); status words `Liquidatable/Near cap/Healthy/Not computed` (:70-76, :78-81); null side → `—` (:58, :142-145).
- ✅ R8: pure function over `InspectorView` + address + `selectedId`; `rows` are `stress.rows` (:127, Plan 2's `StressRow`); `decimals` is `view.decimals` through `isWireScale` (:129); "today" is `view.cash` (:134). No second reader.
- ✅ R15: `rows.find(id === selectedId) ?? rows[0] ?? null` (:128); pinned for `"ghost"` and `null` (spec :255-256). No checkboxes/Compare.
- ✅ R11 register: every money figure via `humanUsdFull`; `truncateAddress` for the address form (:110).
- ✅ No null `decimals` reaches `humanUsdFull`: narrowed once at :129-130, then passed as `number` to `roomTile`/`roomWords`/`rowHeadline` and captured by `money` (:133).
- ✅ Spec's `reading()` (spec :167-180) covers all nine `AddressReading` fields (address-lookup.ts:26-36); differs from Plan 2's helper only in `evidence: null` and `age.seconds: null`, neither read by the workspace.
- ✅ Report accurate on the literals, the flip (fixture :111 → :124), the scope gate, and honest that the third regex was left unfrozen.
- ⚠️ Cannot verify from diff: the 4-passed run, typecheck and lint (not re-run per instructions); `@solvent/client`'s `refineBody` sealing `liquidatable` into `liquidation_verdict` (report claim, consistent with address-stress.ts:59 and the weld spec the report names).

### Strengths

- Every refusal arm returns through `empty()` (:66-68), so no refusal can carry rows, tiles, a batch or a scale past it — the shape enforces the "nothing about this address is known" sentences.
- Negative room is negated before formatting in both the tile and the words (:85, :91); `humanUsdFull`'s own `−$` path (human-price.ts:26) is never reached for room.
- The `no-position`/`withheld` arms sit *behind* the stress-loading and stress-error arms, so an in-flight or failed stress lookup can never read as a negative.
- The report's Step 4 check is the right one (the Plan 2 weld pins the flip) and it names the fixture arithmetic.

### Issues

#### Critical (Must Fix)

None. On the demo and on any well-formed wire no figure is manufactured and no verdict word lands on an unknown.

#### Important (Should Fix)

1. **Plan-mandated — the projection row's headline ignores the horizons.** `rowHeadline` (lab-address.ts:99-102) judges by `row.flips` and `row.after.verdict` only. A projection's `after` is the spot (fixture :302-312 equals :266-276), so `flips` is `false` by construction for every projection, and a horizon whose verdict is `liquidatable` would still print `"0x… stays inside its cap under Debt Manager borrow APY +200bps (PROJECTION)."` with tone `ok` — a verdict word the wire contradicts. The Inspector's own table reads `flipsAt = r.projection?.find(h => h.verdict === "liquidatable")` and prints `Within 30d` warn (app/inspector/[addr]/StressTable.tsx:37, :67-69), so under R8 the workspace diverges from the Inspector's reading. The dek `"Room today $190.50; after the shock, $190.50."` also describes a shock that is not one; the projection's words are the horizons' extra interest (StressTable.tsx:53). The pin's comment says "no flip within its horizons" (spec :257) but the assertion (:260) accepts `ok|warn|crit` and never reads a horizon — it passes for the spot reason. This is the brief's code verbatim, so it is not the implementer's deviation. Fix for the follow-up: an arm before :99 when `row.projection !== null` — an `unknowable` horizon → `refused`; a `liquidatable` horizon → `becomes liquidatable within ${horizonLabel(h.seconds)} under ${label}`, tone `warn` (the Inspector's tone); else `stays inside its cap through ${horizonLabel(longest)} under ${label}`, tone `ok`; dek from `extraInterest` via `humanUsdFull`. `horizonLabel` is exported (address-stress.ts:123). Then pin the demo's projection emphasis and `tone: "ok"` exactly.

2. **Plan-mandated, malformed-wire edge — `money()` prints a negative wire figure.** `isWireDecimal` admits `-` (wireGuard.ts:22 `^-?[0-9]+$`); `readCashPosition` keeps a negative `debt` on a refused row (inspector-position.ts:66-69, :77) and the Inspector refuses its tiles for it (`refusedTiles: true`, inspector-view.ts:278; its `lastDebt` guards `>= 0n`, :251; cause "a negative figure — not a position", :137). The workspace's `debtBefore` (lab-address.ts:133, :138) would print `humanUsdFull(-4822000000, 6)` = `−$4,822` beside `Not computed`; a negative `after.debt`/`after.cap` from `wireInt` (address-stress.ts:53) likewise. Related R8 drift: for a computed row with an `unknowable` verdict, `cap`/`room` are non-null (inspector-position.ts:89-100) so `capBefore`/`roomBefore` print figures with tone `neutral` where the Inspector prints none (`isComputedCash` false → not-computed → refused tiles). Cannot occur on a well-formed wire or the demo. One-token fix: `money = (v) => v === null || v < 0n ? REFUSED_TILE : …`, and gate the before tiles on `view.refusedTiles` so the workspace refuses exactly where the Inspector does.

#### Minor (Nice to Have)

3. **Controller concern 1 — unavailable dek split at `". "` (lab-address.ts:114): deferred minor.** `InspectorView` carries no lookup error message (only `historyLoad`/`stressLoad` do, inspector-view.ts:88, :91), so the module must read it out of the Inspector's dek, and the brief froze inspector-view.ts. The parse is correct for the dek's actual shape; the only failure is a message with an interior `". "` being shortened — still a true, dashed sentence, never a figure or verdict. Later: add `lookupLoad: LoadPhase` beside the other two and read `view.lookupLoad.message`. The `?? view.headline.dek` is for `noUncheckedIndexedAccess` typing, not dead logic.

4. **Controller concern 2 — `roomAfter` regex (spec :251): deferred minor.** `/^over cap by \$/` can fail (a `−$1,069` or `$1,069` fails it), so it is not toothless, but the figure is unpinned. The brief sanctioned two freezes; leaving a third was the right call. Freeze to `"over cap by $1,069"` next time this spec is opened.

5. **Controller concern 3 — the "No scenario applies…" arm (lab-address.ts:130-132): deferred minor, and it is *not* unreachable.** Three routes: (a) `rows: []` under `found: true` — sentence accurate; (b) `view.decimals === null` beside rows — a Cash `value_decimals` the scale guard refuses nulls `cash.decimals` (inspector-position.ts:65) while `stressReading` never consults `value_decimals` (address-stress.ts:55-60); (c) `view.cash === null` beside rows — the address lookup not-found/legacy-only while the stress response (a separate call, possibly a later batch) carries Cash rows. In (b) and (c) "The stress response carried no scenario for this account" is false — it carried them. Still a dashed refusal with no figure and no verdict word. Fix: split the arm — `selected === null` keeps the sentence; `decimals === null` → "The Cash position's value scale is unreadable, so no figure prints."; `view.cash === null` → "The lookup and the stress response disagree about this account's Cash position." Note for Task 7: `state: "rows"` can come with `tiles: null` and a non-null `selected`.

6. **`sentence()`/`refused()` duplicated from lab-headline.ts:30-36** (and a third `sentence` with different semantics at inspector-headline.ts:36). Forced by the brief (Task 3 exports neither; `web/lib/**` frozen), so acceptable for this task. The copies differ on `""`: lab-headline's `terminated` returns `""`, lab-address's returns `"."` (:43-46) — reachable via an empty wire `reason` (`result.reason ?? null` keeps `""`) → a lone-period dek at :95. What I would do: `export` both from lab-headline.ts in the next task allowed to touch it (two keywords, no behaviour change) and import them here, which also removes the `"."` edge.

7. **`afterStatus` has no "Near cap"** (:78-81): the before tile uses the Inspector's headroom band, the after tile only the verdict — a shock leaving 1% room reads `Healthy` ok where the Inspector's register for the same figures is `Near cap` warn. Plan-mandated; `headroomBand` is exported (headroom.ts:202).

8. **`no-position` names the lookup's batch, not the stress response's** (:120-122): the pin itself shows it — stress fixture batch 18251, sentence "in batch 1" (spec :215, :219). `StressReading` carries no batch id; plan-mandated.

9. **Pins that could bite harder:** stress-unavailable dek (spec :207-209), withheld dek (:230) and no-position dek (:219) are unpinned; `w.tiles!` (:244) is a `!` on derived data — an `expect(w.tiles).not.toBeNull()` first would be cleaner.

10. **Law comments:** the header states the law (:18-20); `rowHeadline`, `afterStatus`, `roomTile`, `STATUS_WORD` carry none (why "over cap by" never a negative figure; why unknowable is "Not computed"). The brief's code has none either.

### Assessment

**Task quality:** Approved

**Reasoning:** The implementation is the brief verbatim, the two sanctioned freezes are the only pin edits and both literals recompute from the fixture, and on every well-formed path no figure is manufactured and no verdict word lands on an unknown. The two Importants are the brief's own design (a horizon-blind projection headline that diverges from the Inspector's `flipsAt`, and an unguarded negative wire figure) and belong to a follow-up before Task 7 renders a projection row's headline; the controller's three concerns are all deferred minors, though the "unreachable" arm is reachable by a bad scale or two disagreeing responses.

## Re-review 1 (c1fd2c6, 477e513)

Judged from `git show c1fd2c6` and `git show 477e513` restricted to `web/lib/lab-address.ts` and `web/tests/unit/lab-address.spec.ts`; read-only; no pin run — every claim below is a trace through the post-fix code.

**RE-REVIEW: open** — the five items are addressed, but the two fix diffs introduce two consistency gaps (A, B below), each one arm plus one pin.

### Item verdicts

1. **Important 1 (projection headline) — ADDRESSED.** `projectionHeadline` (c1fd2c6, lab-address.ts:~107-124) is entered from `rowHeadline` before the flips arms whenever `row.projection !== null`; an `unknowable` horizon → `refused` naming it (`The 30d horizon carries no verdict.`); else the first `liquidatable` horizon by `find` → `becomes liquidatable within {h} under {label}` warn — the same first-listed rule as the Inspector's `flipsAt` (StressTable.tsx:37), so R8 holds; else `stays inside its cap through {longest}` ok with `longest` by `reduce` over seconds (order-independent). Dek from `extraInterest` via `humanUsdFull` at the view's decimals, exactly as the Inspector's table prints it (StressTable.tsx:53). Literals recomputed: `rescale` truncates (human-price.ts:9-12), so 7,926,575 → `$7.92` and 23,779,726 → `$23.77`; 2,592,000 s → `30d`, 7,776,000 s → `90d` (address-stress.ts:123-130). Demo pin is a whole-headline `toEqual` (spec :123); the `projected(1, true)` and `projected(0, null)` pins are exact and would fail on the old spot-only code. The empty-horizon `refused` is unreachable (`row()` nulls an empty projection, address-stress.ts:78-81) and is type honesty, not dead logic.

2. **Important 2 (negatives / refused positions) — ADDRESSED, with new breakage A.** `money()` refuses `v === null || v < 0n`; the before tiles follow `view.refusedTiles` (inspector-view.ts:278) with `statusBefore` kept as the Inspector's word; pinned on `DEMO_ADDRESS_REFUSED` (Cash refused `SWEEP_NEVER`, `max_borrow_lt: null`, `borrowings: "4100000000"` — pre-fix this printed `$4,100` beside `Not computed`; now `—`, and the pin asserts no `$` in any tile). **The `readable(side)` widening is the Inspector's law, not over-refusal:** it is `readCashPosition`'s own rule (inspector-position.ts:71-77 — debt and cap both present and non-negative, else `status: "refused"`, no cap, no room, no verdict word) applied to a stress side, with the room recomputed as `cap − debt` exactly as `side()` does (address-stress.ts:59). It is stricter than the Inspector's stress *table*, whose `moneyFor` would print a negative through `humanUsdFull`'s `−$` path (app/inspector/[addr]/money.ts:16) — but that table has no status tile; the workspace's four after tiles are the account register, and a room of `cap − (−debt)` is a negative figure printing in disguise. The right law for the tiles.
   **A — new: the headline still trusts the verdict of the side the tiles refuse.** In the fix's own pinned case (spec "a negative wire figure…", `eth_minus_30` with after `debt_usd: "-4822000000"`): `row.flips` is Plan 2's reader and still reads `true` (before not-liquidatable → after liquidatable, address-stress.ts:73-76), so `rowHeadline` returns `"0x7a3f…c21e becomes liquidatable under ETH -30 percent."` tone `crit`, while `statusAfter` is `Not computed` and every after tile is `—`. The pin asserts only the dek (`…after the shock, not computed.`), so it does not see the emphasis. Pre-fix both surfaces said "liquidatable" (consistently, if wrongly); c1fd2c6 refused the tile and left the headline — a verdict word beside a register that says the figures are not a position. Fix (in `rowHeadline`, after the projection arm, mirroring the `flips === null` arm): when a non-null side is unreadable — `(row.before !== null && readable(row.before) === null) || (row.after !== null && readable(row.after) === null)` — return `refused(\`Cannot say whether ${short} becomes liquidatable under ${row.label}.\`, \`${dek} The shocked figures are not a position.\`)`. Pin the `negative` case's emphasis and `tone: "refused"`.

3. **Minor 5 (rows arm split) — ADDRESSED, with new breakage B.** Three causes in the order empty rows → no Cash position → unreadable scale (lab-address.ts:~170-175), each keeping `state: "rows"`, the rows, the selection and `tiles: null`; the scale pin (`value_decimals: 1.5`) and the disagreement pin (`ADDRESS_NOT_FOUND` beside the demo stress readdressed) are exact. Asking the position before its scale is right (`decimals = cash?.decimals`, inspector-view.ts:173).
   **B — new: the disagreement arm publishes a definitive negative under a withheld Cash book.** `view.cash === null` also holds for `view.state === "cannot-compute"` — the lookup's Cash engine withheld (`lookup.outcome === "unknowable"`, or found with Cash in `withheldEngines`, inspector-view.ts:236-243) — and a stress response from a separate call can carry Cash rows (its own `withheld_engines` is empty, so `stressReading` returns `rows`, address-stress.ts:110-111). The arm then prints `"No Cash position for {short} to stress."` and a dek that says "the lookup found no Cash position" — the lookup found *cannot say*. Pre-fix the arm printed the weaker (wrong) "No scenario applies…"; c1fd2c6 sharpened it into a negative that the repo's first law forbids ("a withheld book is never 'no position'", inspector-view.ts:5-6; address-stress.ts:107-110). Reachable when the engine's withheld status differs between the two responses (a batch rolling between the calls, or a transient engine outage). Fix: before the not-found sentence, `if (view.state === "cannot-compute") return bare(refused(\`Cannot say — the Cash book is withheld for ${short}.\`, "The stress response carries scenarios while the lookup's Cash book is withheld — the two answers disagree."))`; the `not-found`/`legacy-only` states keep `"No Cash position for {short} to stress."` (both are complete lookups by the client's law, lookup.ts:184-199). Pin with the inspector fixtures' unknowable lookup (`UNKNOWABLE_ADDR`, tests/fixtures/inspector.ts:35) beside the demo stress readdressed.

4. **Minor 7 (after status by band) — ADDRESSED.** `afterStatus` calls `headroomBand(figures.cap, figures.debt)` — the Inspector's argument order (inspector-position.ts:79) — and the local `NEAR_BANDS = {0,1,2,3}` is the Inspector's actual set (inspector-position.ts:59: breached, `[0,2)`, `[2,5)`, `[5,10)`), including band 0 by the same rule ("the verdict governs, the room shows the contradiction"); the fallthrough `band !== null && NEAR_BANDS.has(band) ? near : healthy` is byte-for-byte the reader's (inspector-position.ts:85-87), so a `0/0` cap-and-debt after side reads `Healthy` there exactly as it does in the Inspector. Demo projection after (cap 5,012,500,000 / debt 4,822,000,000) → band 2 → `Near cap` warn; pinned (spec :125).

5. **Round 1b (`roomAfter` tone) — ADDRESSED.** 477e513 derives `roomToneAfter` from `statusAfter.tone` (crit → crit, warn → warn, else neutral) and `roomTile` still forces crit for a negative room; a refused after side refuses the room (`after === null`), and the dropped `side === null` check was redundant (`readable` returns null for a null side). Pinned `{ value: "$190.50", tone: "warn" }` on the demo projection (spec :126).

### Residual (not new, deferred minor)

- An `unknowable` verdict beside *readable* after figures prints `debtAfter`/`capAfter`/`roomAfter` (neutral) beside `Not computed`, where the before side under the same condition is refused through `refusedTiles` (`isComputedCash` excludes `unknowable`, inspector-position.ts:104). Pre-existing asymmetry, not introduced here; closing it is a `side.verdict !== "unknowable"` gate on the three after money tiles.
- Deferred minors 3, 4, 6, 8, 9, 10 from the first round are untouched, as the report says.

### Scope check

Both commits touch exactly the two Task 6 files; the module's header law and every first-round pin are intact; no `any`, the only `!` was replaced by an explicit null-throw in the refused-position pin (spec "a refused Cash position…"); the spec's `withResult`/`projected` helpers edit the wire body before `lookup()` seals it, so the sealed verdicts are the client's, not hand-built.

## Re-review 2 (8441749, f338b53)

Judged from `git show 8441749` and `git show f338b53` restricted to `web/lib/lab-address.ts` and `web/tests/unit/lab-address.spec.ts`; read-only; no pin run — each verdict is a trace through the post-fix code. The typecheck failure on `app/lab/page.tsx` (`./LabSurface`) is another task's uncommitted work and is not counted here.

**RE-REVIEW: open** — A, B, C and 2b are each addressed as scoped and correctly pinned; one residual of A remains, one hoisted condition and one pin.

### Item verdicts

- **A (unreadable side → no verdict word) — ADDRESSED for spot rows.** In `rowHeadline` (8441749), after the projection arm and before `flips === null`: a non-null `before` or `after` that fails `readable()` returns `refused("Cannot say whether {short} becomes liquidatable under {label}.", "{dek} The shocked figures are not a position.")`, with `dek` the same room sentence the flips arms use, so the unreadable side reads "not computed" in it. Traced on the pinned case (`eth_minus_30`, after `debt_usd: "-4822000000"`): `readable(after)` null → the gate fires ahead of Plan 2's `flips === true`; emphasis, tone `refused`, and dek `Room today $190.50; after the shock, not computed. The shocked figures are not a position.` all asserted by a whole-headline `toEqual` (spec "a refused Cash position…"). The `cannot` emphasis is hoisted and shared with the `flips === null` arm — one string, two causes.
  **Residual — the projection arm sits above the gate.** `if (row.projection !== null) return projectionHeadline(...)` precedes the readable check, so a projection row whose spot side is unreadable (negative debt or cap) — or, after C/2b, carries an `unknowable` spot verdict — still prints `stays inside its cap through 90d under …` ok (or `becomes liquidatable within …` warn) from the horizons, while the after tiles for that same row are `—` and `statusAfter` is `Not computed` (`after = side.verdict !== "unknowable" ? readable(side) : null` → null → `afterStatus` → `NOT_COMPUTED`). That is the headline/tile disagreement A was raised for, in the third row kind; it contradicts the law the fix wrote on `rowHeadline` itself ("a side the tiles refuse yields no verdict word here either, whatever the wire's boolean says of it"). Same reachability class as A (a malformed or self-contradicting wire), not introduced by these two diffs — the projection arm is round 1's — but A's rule, as scoped, is applied to two of three row kinds. Fix: hoist the gate above the projection arm, extended with the unknowable condition the tiles use (`side !== null && (side.verdict === "unknowable" || readable(side) === null)`), so a projection over a refused spot is `Cannot say whether … under …(PROJECTION).` with the projection's own dek; pin with `withResult("dm_rate_horizon_plus_200bps", …)` setting `before`/`after` `debt_usd` negative, asserting the whole headline and `statusAfter === NOT_COMPUTED`.

- **B (withheld Cash book beside rows) — ADDRESSED.** `view.state === "cannot-compute"` is asked first in the rows arm and returns `bare(refused("Cannot say — the Cash book is withheld for {short}.", "The stress response carries scenarios while the lookup's Cash book is withheld — the two answers disagree."))`. Ordering is sound: `cannot-compute` implies `cash === null` (inspector-view.ts:236-243), so the gate never shadows a computed position, and `not-found`/`legacy-only` keep "No Cash position … to stress." — both complete lookups by the client's law (lookup.ts:184-199). The pin builds the lookup from `ADDRESS_UNKNOWABLE` (`found: null`, `lookup_complete: false`, `withheld_engines: [debt_manager]`, tests/fixtures/inspector.ts:291-302 → `outcome: "unknowable"` → `cannot-compute`) beside the demo stress re-addressed in both the body and every result's `account`, and asserts `rows.length === 3`, `selected.applicable === true` (the disagreement is over real rows, not inapplicable ones), `tiles: null`, the whole headline, and that neither emphasis nor dek contains "No Cash position".

- **C (unknowable after verdict refuses its tiles) — ADDRESSED.** `after = side !== null && side.verdict !== "unknowable" ? readable(side) : null`, so `debtAfter`/`capAfter`/`roomAfter` are `—` beside `statusAfter: Not computed` — the before side's `refusedTiles` rule (`isComputedCash` excludes `unknowable`, inspector-position.ts:104) mirrored on the after side. Pinned: after `liquidatable: null` → `selected.after.verdict === "unknowable"` (the pin fails if the client's sealing changes), `debtBefore` still `$4,822`, the three after money tiles `REFUSED`, `statusAfter` `NOT_COMPUTED`, and the headline the `flips === null` cannot-say (address-stress.ts:74 nulls `flips` for an unknowable verdict, and the A gate passes because the figures are readable).

- **2b (dek prints no figure for an unknowable side) — ADDRESSED.** f338b53 gates `sideRoomWords` on the same condition as the after tiles, so the unknowable-after pin's dek is `Room today $190.50; after the shock, not computed. One side of the comparison is withheld or unknowable.`, with explicit `toContain("not computed")` / `not.toContain("over cap by")` guards. Dek and register now use one word for one state.

### Nits (no action required this round)

- 8441749 adds a second `/** … */` block directly after the old one on `rowHeadline`; JSDoc binds the last, so the first is now a stale orphan — delete it.
- `side !== null && side.verdict !== "unknowable" ? readable(side) : null` is written twice (`sideRoomWords`, the after tiles); a `computable(side)` helper would carry the law once — and is the natural place to close A's projection residual.
- The dek's "Room today" is the stress row's `before` while `roomBefore` is the lookup's Cash; after 2b an unknowable stress-before prints "Room today not computed" beside a `$190.50` tile from the lookup. The round-1 minor 8 family (two responses, two "today"s); deferred.
- Deferred minors 3, 4, 6, 8, 9, 10 remain as reported; the `eth_minus_30` `roomAfter` regex is still a regex.

### Scope check

Both commits touch exactly the two Task 6 files; every earlier pin is intact; the new pins are whole-headline `toEqual`s that would fail on the round-1 code (A: "becomes liquidatable" crit; B: "No Cash position … to stress."; C: `$4,822`/`$3,752` after tiles; 2b: "over cap by $1,069" in the dek).

## Re-review 3 (b42074a, 3332a45)

Judged from `review-f338b53..3332a45.diff` (two commits; `web/lib/lab-address.ts` +47/−18, `web/tests/unit/lab-address.spec.ts` +25); read-only; suite not run — every claim is a trace.

**RE-REVIEW: open** — the residual as named is closed; one narrower hole in the same law remains, which my own round-2 condition carried. Two lines and one pin.

### The residual of A — closed as named

- **Gate ahead of every verdict word, all three row kinds.** `rowHeadline` now runs: not-applicable (a refusal) → `projected`, `today`, `dek`, `cannot` → the gate over the non-null sides → the projection arm → the flips arms (diff :102-124). For a side with unreadable figures or an unknowable verdict, no verdict word — horizon or flip — is reachable in a projection row, a spot row, or an inapplicable row.
- **Law carried once.** `computable(side)` (diff :33-35: non-null, verdict not `unknowable`, `readable` non-null) is the one spelling in the after tiles (:146), `sideRoomWords` (:65), the gate (:114) and `afterStatus` (:46-47 — the fourth spelling folded, with `side === null` kept only to narrow `side.verdict` for TypeScript). `readable` is still called directly in the gate's cause selection (:115), which is a different question — *which* sentence — not a second spelling of the law.
- **Round-2 pins unchanged in meaning.** Negative spot debt on `eth_minus_30`: `computable(after)` null, `readable(after)` null → "The shocked figures are not a position." with "after the shock, not computed." — round 2's sentence. Unknowable spot verdict: `computable` null, `readable` non-null → "One side of the comparison is withheld or unknowable." — the same text the `flips === null` arm produced in round 2b, now from the gate. The demo projection and both horizon pins have computable spots, so the gate passes and `projectionHeadline` is unchanged. B, C, the refused-position and scale/disagreement pins are on paths the diff does not touch.
- **The two new pins fail on the parent.** On f338b53 the projection arm precedes the gate, so `spot({ debt_usd: "-4822000000" })` and `spot({ liquidatable: null })` on `dm_rate_horizon_plus_200bps` both print `stays inside its cap through 90d under …` ok with the interest dek — both whole-headline `toEqual`s fail, as do `statusAfter`'s `NOT_COMPUTED` (which passes on the parent only for the tiles, not the headline). 3b's `under the projection` / `projected figures` / `not.toMatch(/shock/)` fail on b42074a, so 3b's pins bite too.
- **Orphaned JSDoc removed** (diff :86-101: one block on `rowHeadline`, restated to the hoisted law). **Scope:** two files only; no earlier assertion modified — the spec diff is one added test block between the unknowable-after and withheld-book tests.

### The remaining hole — a missing after side on a projection row

The gate builds `sides = [row.before, row.after].filter((s): s is StressSide => s !== null)` (diff :113), so a **null** side never trips it. For a spot row that is harmless: `flips === null` (address-stress.ts:74) refuses a null side one arm later. For a projection row it is the round-2 disagreement again, one cause narrower: `after: null` beside horizons → the gate passes (no non-null uncomputable side) → `projectionHeadline` prints `stays inside its cap through 90d under …` ok or `becomes liquidatable within …` warn, while the tiles for that row are `—` and `statusAfter` is `Not computed` (`computable(null)` → null; `afterStatus(null)` → `NOT_COMPUTED`). The module's own words name three refused causes — "not computed for a missing, unreadable or unknowable side — the tiles' own word" (`sideRoomWords`, diff :62) — and the gate covers two. The `side !== null && (…)` condition I proposed in re-review 2 is where the null guard came from; the implementer applied it faithfully.

Reachability: a wire that publishes horizons for a spot it did not publish — self-contradicting, the same class as the negative-debt case this law was written for. By the project's rubric (a verdict word on an unknown), the same class.

Fix, in the gate only: drop the null filter — `if ([row.before, row.after].some((s) => computable(s) === null))` — and pick the cause as `sides.some(s => s !== null && readable(s) === null)` → "The {projected|shocked} figures are not a position.", else "One side of the comparison is withheld or unknowable." (the sentence the spot `flips === null` arm already uses for a missing side, so no spot pin changes). The `flips === null` arm below then has no reachable input and can go, or stay as type honesty. Pin: `withResult("dm_rate_horizon_plus_200bps", (x) => ({ ...x, after: null }))` → the refused projection headline with the withheld/unknowable cause, `statusAfter` `NOT_COMPUTED`, `roomAfter` `REFUSED`; and the same with `before: null` for symmetry with the spot path.

### Nits (no action required)

- `dek` is built for every row but, for a projection that passes the gate, is discarded in favour of `projectionHeadline`'s interest dek — a spare string, not a defect.
- Deferred minors 3, 4, 6, 8, 9, 10 remain; the `eth_minus_30` `roomAfter` regex is still a regex; the "two todays" nit stays with minor 8.

## Re-review 4 (6eee7fd)

Judged from `review-3332a45..6eee7fd.diff` (the `f7f572a` claim-lease renewal in the range ignored) and `git show --stat 6eee7fd`; read-only; suite not run — every claim is a trace.

**RE-REVIEW: closed.** The hole named in re-review 3 is closed; no residual remains in `rowHeadline`.

### The hole — closed

- **All three refused causes, ahead of every verdict word, in every row kind.** `sides = [row.before, row.after]` is now unfiltered (diff :56) and the gate trips on `sides.some((s) => computable(s) === null)` (:57). `computable(null)` is null by its first clause (`side !== null && …`), so a **missing** side trips it exactly as an **unreadable** (`readable` null) or **unknowable** (`verdict === "unknowable"`) one does — the three causes `sideRoomWords`' comment names, all in one predicate. Row kinds: an inapplicable row returns at the first line (a refusal, no verdict word); a projection row meets the gate before `projectionHeadline` (:65); a spot row meets it before the flips arms (:66-69). No horizon or flip verdict word is reachable past a side the tiles refuse.
- **Cause selection.** `sides.some((s) => s !== null && readable(s) === null)` (:60) → "The {projected|shocked} figures are not a position."; else "One side of the comparison is withheld or unknowable." A missing side lands on the second — the same sentence the spot `flips === null` arm gave it before — and present-but-unreadable figures win over a missing side when both occur, which is the truer cause. The `s !== null` guard is needed: `readable(null)` is also null and would otherwise call a missing side "not a position".
- **Spot-row pins keep their meaning.** Negative spot debt: both sides non-null, `after` unreadable → "The shocked figures are not a position." — unchanged. Unknowable spot verdict: `computable` null, `readable` non-null on both → "withheld or unknowable" — unchanged. Every demo pin (the `eth_minus_30` flip, the projection's "through 90d", within-90d warn, the unknowable-horizon refusal) has two computable sides, so the gate passes as before. A spot row with a null side — not pinned — now refuses from the gate instead of the `flips === null` arm, with the identical emphasis, dek and cause, so its output is byte-for-byte what it was.
- **The new pins fail on the parent (3332a45), by trace.** There the gate filtered null sides out and saw only the present one — computable in both cases — so it passed and `projectionHeadline` printed `0x7a3f…c21e stays inside its cap through 90d under …(PROJECTION).` tone `ok` with the interest dek (`Room today $190.50; 30d: +$7.92 interest; 90d: +$23.77 interest.` for `after: null`; `Room today not computed; 30d: …` for `before: null`). Both whole-headline `toEqual`s on the refused object fail on that. The `statusAfter`/`roomAfter` assertions in the `after: null` pin pass on the parent as well (the tiles already refused a null after in round 2) — the headline assertion is what bites, which is the point of the pin.
- **`before: null` symmetry.** The stress `before` only feeds the dek's "Room today" words (the before tiles are the lookup's Cash), so refusing a projection on a missing before is a choice for symmetry with the spot path (`flips === null` on a null before) rather than a tile-driven necessity; it is the symmetry I asked for in re-review 3 and the dek says exactly what is missing (`Room today not computed; under the projection, $190.50.`).
- **Scope.** `git show --stat 6eee7fd`: `web/lib/lab-address.ts` (+5/−2) and `web/tests/unit/lab-address.spec.ts` (+33) only; the spec diff is one added test block and no earlier assertion is modified.

### Nits (no action required)

- The `flips === null` arm (:66) is now unreachable: `flips` is null only for an inapplicable row (first line), a null side (gate) or an unknowable verdict (gate). Harmless as type honesty; removable whenever the file is next opened.
- `rowHeadline`'s JSDoc (round 3) still reads "unreadable figures or an unknowable verdict"; the inline comment at :54-55 has the full "missing, unreadable or unknowable". One word in the doc comment.
- Deferred minors 3, 4, 6, 8, 9, 10 remain; the `eth_minus_30` `roomAfter` regex is still a regex; the "two todays" nit stays with minor 8.
