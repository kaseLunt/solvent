# SDD ledger — plan: docs/plans/2026-09-15-ui-inspector.md

Spec: docs/specs/2026-09-15-ui-product-register-design.md (§5.3 Inspector, §3.5 templates, §4 kit, §7, §9). Plan committed 6f13580. Predecessor plan 1 closed at d989286.
Mode (owner ruling, plan 1): integrator builds Tasks 1, 11, 13 personally; subagents (`model: fable`, `serena-coder`) build 2–10 and 12; a task reviewer gates every task; whole-branch review + Codex rounds in Task 14. Owner side-by-side gate before the Task 13 commit.

## Pre-flight conflict scan (2026-09-15)

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 `TrustCheckItem` ↔ T4 `TrustItem` | same fields; T4 narrows `id` to `TrustId` | assignable; lib never imports components — OK |
| T2 `humanUsdFull`/`humanPrice`/`humanAmount`/`percent` ↔ T3, T6, T9, T11 | signatures `(bigint, decimals)` everywhere | OK (plan self-review); `humanUsd` compact tiers are NOT used on the Inspector (ruling below) |
| T3 `CashPosition.status` ↔ T9 `InspectorState` ↔ T11 `STATUS_WORD` | `liquidatable\|near\|healthy\|refused\|unknowable` | consistent |
| T3 `boundaryOf(position, cash)` ↔ T9, T10 weld | same arity/order | OK |
| T5 `roomSeries(engine, knownIds)`, `nearCapStreak(series)` ↔ T9, T10 weld | OK; T9 keys known ids to the HISTORY vantage (R4) | OK |
| T7 `stressReading(lookup, account)` ↔ T11 StressTable, T10 weld | OK | OK |
| T7 moves `activityTakeaway` ↔ T12 deletes `lib/inspector-lines.ts` | T7 precedes T12 | OK |
| T8 `Phase<T>.value`, `useAddressActivity` → `CursorPages` ↔ T9, T11 | field names match (`rows/loading/error/hasMore/loadMore`) | OK |
| T10 fixture numbers ↔ T12 e2e pins ↔ T6 unit pins | debt 4,822 · cap 5,012.50 · room 190.50 (3.8 %) · streak 14 (span 390 s → "≈6m") · boundary $3,818.57 / 4.5 % · sweep 1 of 3 gen 4 · prices 35 s / 180 s · reconcile 29/29 | recomputed at planning; consistent |
| T10 clock law on history points | points carry `computed_at` with no `age_seconds` (contract shape) | if `checkClocks` flags stamp-without-age as unresolved, the generator throws — implementer reads `clockCandidates` first; report NEEDS_CONTEXT rather than hand-editing ages |
| T11 test ids ↔ T12 contract ↔ T13 readiness | table in plan header | consistent |
| T12 retirements ↔ other pages' imports | only Inspector files imported the deleted modules (verified at planning: `EvidenceDrawer`, `RefusedTag`, `SeverityHF`, `EngineChip`, `MarksStamp`, `AddressMono`, `history-series`, `sparkline-scale`, `params-format`, `liq-distance`, `factorPriceGuard` stay) | OK |
| Each task's tests vs its code | verbatim strings recomputed (percent tenths, humanAge, humanPrice places) | OK |

Rulings: R1–R14 are in the plan header. Added while planning: **money on the Inspector is `humanUsdFull`** (grouped whole dollars ≥ $1,000, cents below, `<$0.01`, `$0` only for a true zero) — one account's figures are read exactly; the Book's book-wide sums stay compact. Cost if wrong: a whale account's headline reads "$1,234,567" instead of "$1.2M" — legible either way.

## Commits so far
6f13580 plan · 40d7a7f Task 1 · c0f9498 Task 2 · 248d670 Task 1 fix round 1 · 239336c plan patch (R15 + percent pins) · cf8e6de Task 3 · a755bb0 Task 2 fix · 92aa73b Task 4 · 58b5017 Task 5 · c9c21be Task 6 · 099b0b6 Task 3 fix · 9418851 Task 7 · 01d28f5 Task 4 fix · b0f4f6a Task 8 · 3a5654a plan patch (newestKind, zero-cap, contradictory)

Integrator groundwork for Task 11, uncommitted and gate-clean (typecheck/lint/lint:css): `web/app/inspector/InspectorLanding.tsx`, `web/app/inspector/page.tsx` (renders it), landing classes appended to `web/app/inspector/inspector.module.css`. Companion gate page drafted: `.superpowers/brainstorm/2116-1789507948/content/gate-inspector.html` (expects `gate3-*.png`).

## Progress

- Task 1: built by the integrator; typecheck/lint/lint:css clean; recent-lookups 2/2. Committed 40d7a7f (BASE 6f13580). Deviation from the brief: the `FormEvent` type alias is deprecated in the installed React types, so the submit handler is inline (`onSubmit={(event) => …}`) and `submit()` takes no event — same behavior. Task review: dispatched after packaging.
- Task 2: implementer dispatched (fable, serena-coder) — BASE 40d7a7f; brief task-2-brief.md; report task-2-report.md.
- Task 2: DONE_WITH_CONCERNS → commit c0f9498 (modules verbatim; two `percent.spec.ts` pins corrected by the implementer: `percentTenths(4822000000n, 5012500000n)` = 961n not 962n — 4822/5012.5 = 0.961995…; `percentTenths(-1n, 3n)` = −333n toward zero, not 0n). Both corrections are right; the plan text is patched to match.
- Ruling R15 (from the Task 2 concern): "% used" = formatTenths(1000 − roomTenths), the printed complement of room — independent truncation prints 96.1 % beside 3.8 % and a reader sums 99.9. Plan patched in Tasks 3 (module, spec: liquidatable 107.8 %, healthy 41.9 %, zero-cap arm) and 6 (`?? "—"` fallbacks; dek pins 107.8 % / 41.9 %); `ComputedCash` no longer requires non-null percents (a zero cap is computed AND liquidatable). Cost if wrong: one figure is a complement rather than a direct truncation — 0.1 % of display, no verdict touched. Task 12's "96.2% used" e2e pin stands.
- Task 2: review package review-40d7a7f..c0f9498.diff; reviewer dispatched.
- Task 1 review (task-1-review.md): SPEC ✅ · QUALITY needs-fixes. Fix round 1 (integrator): #1 Important — Trust state was colour-only for AT → `.srOnly` state word ("passed/caution/failed/not available") beside the aria-hidden glyph; #2 `aria-describedby` ties hint + refusal to the input (`useId`); #3 same-tab staleness → module listener set notified from `rememberLookup`; #5 section header on the appended CSS block (position kept — no cascade effect). Rulings: #4 (field never re-syncs `initial`) — by design; the surface is mounted `key={addr}` (plan Task 11 Step 1), ledger-noted for Task 11's reviewer; #6 (`ADDRESS_PATTERN` accepts `0X`) — frozen `lib/format.ts`, carry-forward to Plan 4 (decide: reject `0X` or normalize to `0x` before the request — never silently); #7 — the scope gate DID run (`scope-gate: OK -- 6 path(s)` in the commit transcript); report amended.
- Task 1 fix round 1 committed 248d670 (typecheck/lint/lint:css clean, unit 2/2); scoped re-review requested on review-c0f9498..248d670.diff.
- Task 1: complete — re-review all-addressed (reviewer re-ran gates: unit 1,065 passed; lint:css/typecheck/lint clean). Minor (deferred): the new `/* ---- … ---- */` header in kit.module.css:130 lacks the blank line the file's other section headers have — fold into the next kit CSS touch.
- Task 2 review (task-2-review.md): SPEC ✅ · QUALITY approved; both corrected pins verified by arithmetic. Finding #1 (Important, controller decision): `humanAmount` printed a nonzero sub-precision amount as "0" — violates the never-zero law. Ruling: dust prints `<0.0001` (maxFraction-shaped; `"−<0.0001"` when negative, matching `humanUsd`'s `−<$0.01`). Fix round 1 → resumed the Task 2 implementer: dust arm, `maxFraction` clamp, `fallPercent` refuses `to < 0n`, doc brackets, boundary pins ($10.00/$9.9999, maxFraction variants, no "−0%"). Deferred minors: #6 `group`/`rescale` duplicate `human-usd.ts` internals (frozen-lib constraint; consolidate in Plan 4); #5 `decimals` is trusted metadata (as in human-usd).
- Task 2 fix round 1 committed a755bb0 (8/8 unit; typecheck/lint clean); scoped re-review requested (`git show a755bb0`).
- Task 2: complete — re-review all-addressed (dust arm verified for maxFraction 0/1/2/4/6 and beyond decimals; unreachable for a nonzero whole part). Minor (deferred): `percent.spec.ts:30` pins `formatTenths(percentTenths(-1n, 3000n) ?? 0n)`; the tighter pin is `expect(percentTenths(-1n, 3000n)).toBe(0n)` — fold into the next touch of that spec.
- Task 3: implementer dispatched (fable, serena-coder) — BASE 239336c; brief task-3-brief.md (regenerated with R15); report task-3-report.md. Note passed: the spec has 9 tests (brief says 10).
- Task 3: DONE_WITH_CONCERNS → commit cf8e6de (9/9; typecheck/lint clean). Deviation accepted: the `{ price_decimals: _dropped, ...noScale }` destructure tripped `no-unused-vars`; replaced by the repo's `delete (noScale as {…}).price_decimals` idiom — same input to the module. Implementer observations for the reviewer: `headroomBand` is 0..6 (no effect on pins); `refusal.detail ?? null` is a no-op (detail is non-null on the wire); a computed position with an unreadable `borrowings`/`max_borrow_lt` reads `status: "refused"` with `refusal: null` (consumers key on `computed`). Review package review-239336c..cf8e6de.diff; reviewer dispatched.
- Task 3 review (task-3-review.md): SPEC ✅ · QUALITY needs-fixes (2 Important, 9 Minor). Rulings: #1 empty `factor_assets` (the solver's real shape for stable-only collateral) → phrase on the axis (`AXIS_LABEL`), never an empty subject; #2 partial joint solve → `unreadable` naming `prices[i].lowest_healthy_price`, held = held − factor − floored, deduped; #3 band 0 with a not-liquidatable verdict → "near" (NEAR_BANDS gains 0; the verdict governs, the room shows the contradiction) — supersedes the brief's {1,2,3}; #4 negative decimals → not computed; #5 documented (consumers key on `computed`/`refusal`); #6 `!computed` → boundary absent; #7 rise / `already_breached` under a not-liquidatable verdict → NEW `Boundary` kind `contradictory` (Task 11's BackingTable prints "Boundary withheld: {detail}" — integrator's duty, noted for Task 11); #8 `fields` union with `prices[i].` prefixes; #9 `VERDICT_RANK ?? 2`; #10 both percents from `roomTenths`; #11 held dedupe + `symbol ||` fallback. Fix round 1 → resumed the Task 3 implementer; expected 10 unit tests.
- Task 3 fix round 1 committed 099b0b6 (10/10; whole unit project 1,093 passed; typecheck/lint clean). One existing pin changed as a consequence of #8 (`prices[0].price_decimals`). Ruling: the two contradiction checks run before `prices` is read (a breached claim under a not-liquidatable verdict is contradictory whether or not prices are served). NOTE for Tasks 9–11: `Boundary` now has six arms incl. `contradictory` — the view passes it through; BackingTable (integrator) prints "Boundary withheld: {detail}". Scoped re-review requested.
- Task 3: complete — re-review all-addressed (`contradictory` unreachable for a liquidatable verdict: `absent` at :199, `breached` at :200 precede it). Minor (deferred): the two `contradictory` details say "not liquidatable" where an `unknowable` verdict could also reach them — unreachable on the Cash wire (DM `liquidatable` is boolean).
- Task 4: implementer dispatched (fable, serena-coder) — BASE a755bb0; brief task-4-brief.md; report task-4-report.md.
- Task 4: DONE → commit 92aa73b (5/5; typecheck/lint clean; verbatim). Implementer notes for the reviewer: `byId` leans on `Object.fromEntries`' `any` overload; `sweepItem` guards `rows` only on the failed branch. Review package review-a755bb0..92aa73b.diff; reviewer dispatched.
- Task 4 review (task-4-review.md): SPEC ✅ · QUALITY needs-fixes (4 Important, 7 Minor). Rulings (all accept-and-fix): #1 the oldest price keeps ITS budget; all-null ages → dim "age unknown"; verdict words plain (missing / past its ceiling / without a timestamp / behind an unacknowledged reorg), wire word in title, every affected input named; #2 every printed count/age through `readWirePopulation` (throws at this layer, as evidence.ts); #3 `sweep_block === 0` checked before the null stamp; #4 reconcile ok only when result pass, exit 0, drift 0 AND the weld (or gated) pair is equal with compared > 0 — unequal → warn "N Cash rows drifted", compared 0 → dim, not-pass/exit≠0 → "· did not pass"; #5 rows 0 → dim, generation_open → warn "in progress", failed > rows → "contradictory stamp"; #6 uncapped-feed / ratio-reference → warn "not oracle-direct" with plain labels, unknown → dim "provenance not recognised" (title wire word), "" → "provenance not stated"; #7 no synthesized "unnamed" title; #8 all affected inputs named; #9 result word moves to title; #10 refused sweep arm carries `sweep_block: 0` title; #11 typed `byId`, pins for every arm. Fix round 1 → resumed the Task 4 implementer.
- Task 4 fix round 1 committed 01d28f5 (10/10; typecheck/lint clean). Deviations accepted: oldest INPUT by reduce (no `oldestPriceAge`), drift count pluralised, `exact > compared` → warn "contradictory receipt", `exit_code` guarded, no title on "provenance not stated". Scoped re-review requested (`git show 01d28f5`).
- Task 4 re-review (01d28f5): OPEN with 3 Minor — provenance caveat + unrecognised word left the second input unnamed; the ok arm's visible "engine-exact" is a wire word (ruling: detail "the engine's own inputs", wire word in title — supersedes R5's literal); unpinned arms. Fix round 2 → resumed the Task 4 implementer.
- Task 4 fix round 2 committed f4ff9d7 (10/10; typecheck/lint clean). Nits left as-is (comment wording; ", " vs and-list in the broken-price group). Scoped re-review 2 requested.
- Task 4: complete — re-review 2 all-addressed (no wire verdict/provenance word stands as visible prose; every arm pinned).
- Coordination: Task 5's fix made `Streak.newestKind` required → Task 6's committed spec literals fail TS2741 until Task 6's running fix round adds `newestKind: "computed"` (told both implementers who owns it).
- Task 5: implementer dispatched (fable, serena-coder) — BASE 92aa73b; brief task-5-brief.md; report task-5-report.md.
- Task 5: DONE → commit 58b5017 (4/4; typecheck/lint clean; verbatim). Implementer notes for the reviewer: duplicate `batch_id` on the wire → later point wins silently; caller-supplied `knownBatchIds` not re-guarded. Review package review-92aa73b..58b5017.diff; reviewer dispatched.
- Task 5 review (task-5-review.md): SPEC ✅ · QUALITY needs-fixes (1 Important, 9 Minor). Rulings: #1 zero cap with debt (the engine's debt-after-empty-sweep shape; the Book calls it breached) → NEW kind `zero-cap` (no geometry, display "0 cap", counted in the streak as under the line); #2 negative den/num → distinct "negative debt/cap on the wire" gaps; #3/#4 `balances_block` and caller ids through `readWirePopulation`; #5 duplicate `batch_id` → `WireIntegerError` (the page boundary renders a route refusal — Plan 1 law); #6 no-debt title carries `@ block`; #7 `Streak.newestKind` added (Task 6's `Streak` consumer is structurally unaffected); #8 negative span → null; #9 infinite shape `num: null, den: null` pinned; #10 informational. Fix round 1 → resumed the Task 5 implementer.
- Task 5 fix round 1 committed 846e4fd (5/5; lint clean; typecheck fails only in Task 6's spec — accepted transient). Scoped re-review requested.
- Task 5 re-review (846e4fd): all-addressed; 2 Minor follow-ups → round 2 resumed (0/0 → `unpublished` "no cap and no debt", not zero-cap; pins for negative num and the no-debt `@ block`). Controller reviews that diff directly (3-line change).
- Task 5: complete — round 2 committed f352d5f (5/5); controller read the module diff: 0/0 → `unpublished` "no cap and no debt · undefined ratio", `zero-cap` only for num = 0 with debt left — matches the ruling exactly. (Its typecheck saw Task 10's untracked weld spec failing mid-TDD — expected, not in HEAD.)
- Task 9: implementer dispatched (fable, serena-coder) — BASE 846e4fd; brief task-9-brief.md (regenerated: newestKind); report task-9-report.md.
- Task 9: DONE_WITH_CONCERNS → commit 4bdade3 (6/6; typecheck/lint clean). Deviations accepted: the spec's history vantage moved 1→2 (batch id −1 is refused by `readWirePopulation` — a planning slip); `nearWire()`'s discard bindings became checked fixture invariants (lint baseline is zero warnings). Module verbatim. Reviewer dispatched.
- Task 9 review (task-9-review.md): SPEC ✅ · QUALITY needs-fixes (1 Critical, 3 Important, 5 Minor). Rulings: C1 found + Cash withheld + legacy present → `cannot-compute` naming the legacy position (never a Cash negative over a withheld book); #2 every found arm says the floor; #3 boundary only for `isComputedCash`; #4 `found` with `positions: []` → `unavailable` "the response contradicts itself" (client follow-up: `lookup()` does not enforce found ⇒ ≥1 position — carry to Plan 4/client); #5 cause derived from what is null; #6 `decimals` documented; #7 `historyOutcome` added (HistoryCard uses it — integrator); #8 no-op line dropped; #9 near test narrative (lookup 3 > history 2). Fix round 1 → resumed the Task 9 implementer.
- Task 10 review (task-10-review.md): SPEC ✅ · QUALITY needs-fixes (2 Important, 7 Minor); determinism verified (regeneration byte-identical), ajv-valid against openapi. Rulings: #1 repay delta signed; #2 `SWEEP_FAILED` is not a Go refusal code → `SWEEP_NEVER` with `sweep_block: 0`, `flags: []` (supersedes the brief); #3 ETHFI asset and projection note derived by id; #4 opaque rows `amount_decimals: null`; #5 event times at the history's 2 s/block cadence; #6 computed positions carry the num/den rational; #7 batch welded whole; #8 integer wobble; #9 prose notes. Plus integrator polish: sha256-derived tx hashes. Out of scope (carry): meta.demo.json's weETH witness `source_as_of` postdates `computed_at`. Fix round 1 → resumed the Task 10 implementer.
- Task 10: implementer dispatched (fable, serena-coder) — BASE 846e4fd; brief task-10-brief.md (regenerated: newestKind); report task-10-report.md.
- Task 10: DONE → commit 58d5502 (weld 6/6; census +6 entries (4,4,4,4,2,2), CENSUS_TOTAL 77→97, batch-bearing 32→38; whole unit project 1,134 passed; typecheck/lint clean). Review package review-20f077b..58d5502.diff (146 KB, mostly JSON); reviewer dispatched with a determinism check (re-run the generator → no drift).
- Task 6: implementer dispatched (fable, serena-coder) — BASE 58b5017; brief task-6-brief.md (regenerated with R15); report task-6-report.md.
- Task 6: DONE → commit c9c21be (7/7; typecheck/lint clean; verbatim). Implementer note: `otherEngineHeadline`'s non-legacy dek is unpinned. Review package review-58b5017..c9c21be.diff; reviewer dispatched.
- Task 6 review (task-6-review.md): SPEC ✅ · QUALITY needs-fixes (2 Important, 6 Minor). Rulings: #1 zero-cap → no used clause; #2 legacy + foreign engines partitioned (legacy dek kept, foreign noted; plural "positions exist"); #3 shared `sentence()` (capitalize, one trailing period) + cause dedupe; #4 streak parenthesis only for span > 0; #5 defensive fallbacks for empty arrays instead of tuple types (the view passes plain arrays); #6 folded into #2; #7 pins added incl. INVALID_ADDRESS_COPY ⇔ kit literal via a file-text pin; #8 `charAt`. Fix round 1 → resumed the Task 6 implementer.
- Task 7: implementer dispatched (fable, serena-coder) — BASE c9c21be; brief task-7-brief.md; report task-7-report.md.
- Task 7: DONE_WITH_CONCERNS → commit 9418851 (11/11 — the three r74 `activityTakeaway` pins inside a describe block were carried verbatim; typecheck/lint clean). Deviation accepted: `debt_asset` is nullable on the wire → null-guarded. Concern #2 (raw units): a `dm_normalized_debt` amount with null decimals prints the raw integer with only a hover — ruling pending the review: pass the Cash engine's `value_decimals` as `engineValueDecimals` and surface `rawUnits` for a visible tag (fix round after review). Review package review-c9c21be..9418851.diff; reviewer dispatched.
- Task 6 fix round 1 committed 16d5710 (8/8; typecheck/lint clean tree-wide — the TS2741 transient cleared). Deviations accepted: `sentence()` also trims whitespace; engines not deduped in cannotCompute (unreachable). Scoped re-review requested.
- Task 6: complete — re-review all-addressed (every pre-existing template byte-identical). Engine dedupe ruled unnecessary (unreachable; a loud symptom is better than a hidden caller bug). Minor (deferred): the kit-copy pin at spec :147 could be tightened from `toContain` to an exact-literal match.
- Task 7 review (task-7-review.md): SPEC ✅ · QUALITY needs-fixes (4 Important, 9 Minor). Rulings (accept-and-fix): #1 Cash withheld under `found: true` (Aave present) → `withheld` reading; #2 `activityRows(events, { valueDecimalsByEngine })` + `unitChip`/`rawUnits` on the row (the page passes the wire's `value_decimals`); #3 empty `seized` stated; #4 unscaled `debt_repaid` printed raw with "(raw units)", "—" only for null; #5 two Cash results → contradictory non-applicable row; #6 flips only when applicable; #7 `projectionNote`, `marketRealization` carried, empty horizons → null; #8 causes name engines; #9 `tx.short` ellipsis only when truncated; #10 `isWireScale` gate; #11 doc comment verbatim; #12 `actionTitle` = raw_type; #13 DM repaid suffixed USD. Fix round 1 → resumed the Task 7 implementer. NOTE for Task 11: ActivityTable passes `{ debt_manager: view.decimals, aave_v3_etherfi: legacy.value_decimals }` and shows a "raw units" pill when `rawUnits`.
- Task 7 fix round 1 committed 20f077b (13/13; typecheck/lint clean). Choices accepted: found-withheld cause names every withheld engine; `projectionNote` carried even with empty horizons; `marketRealization.decimals` unguarded (Task 11 gates with `isWireScale` before formatting, if rendered); a malformed decimal prints raw bytes + "(raw units)" (re-reviewer asked to judge vs an "unreadable" title). Scoped re-review requested.
- Task 7 re-review (20f077b): all-addressed; advisory accepted → round 2 resumed (malformed decimal → "unreadable"; null decimals → "(raw units)"; "—" only for null). Controller reads that diff.
- Task 7: complete — round 2 committed 0adacdf (13/13); controller read the diff: three distinct arms (null → "—", not a wire decimal → "unreadable", no licensed scale → raw + "(raw units)") — matches the ruling.
- Task 11 first look (build served on :3111, demo routes mocked): composition matches the mockup; fixes applied — VerdictHeader inserts the space before `rest` and takes a `ReactNode` kicker (kit); the kicker's address renders mono/no-transform; Trust detail column wraps instead of forcing the label to wrap (kit CSS); both sparklines sized by `useMeasuredWidth`; History card full width without a legacy series. Demo polish for Task 10's fix round: sha256-derived tx hashes instead of zero-padded counters (the wire-honest "raw units" rendering of `opaque` supply amounts stays).
- Task 11 second look: `useMeasuredWidth` measures once at mount and returned early for frames rendered after history arrived → new `MeasuredSparkline` (frame + hook mount together); the "10% line" label moved out of the SVG (mini: absolute top-right as mocked; History: named in the finding). Both charts now fill their frames; gaps read as gaps. Pending before Task 11's commit: adopt `historyOutcome` (Task 9 fix) in HistoryCard for a withheld/not-found history.
- Rate-limit interruption 00:58–03:40: Task 9 and Task 10 fix-round agents died before editing; both resumed 03:41 with the same instructions.
- Task 9 fix round 1 committed f013695 (7/7; typecheck/lint clean; `historyOutcome` added). Deferred minor: `notComputedHeadline`'s emphasis says "was not computed" for a computed row with an unknowable verdict (unreachable on the Cash wire). Scoped re-review requested.
- Task 11 committed de9e606 — PROCESS SLIP: the commit ran without a pathspec while the Task 10 agent had its fix-round files staged; de9e606 therefore also carries nine Task 10 files (generator, seven bodies, weld spec). Recovery: the Task 10 agent was told not to commit and to verify the committed bytes (regenerate → no drift) and run its specs; once it is idle the controller splits de9e606 into two commits (page · Task 10 fix round with its intended message) with `git reset --soft` and pathspec-limited commits. Task 11 reviewer dispatched on review-f013695..de9e606.diff with the fixture hunks marked out of scope. Lesson (ledgered for the plan close): every controller commit is pathspec-limited too.
- Task 9 re-review (f013695): open with 1 Minor — the cause ladder read the reader's forced nulls; round 2 resumed (ladder reads the wire; "refused without a code"; negative → "not a position"; no last-readable clause for a negative debt). Note for Task 11's fix round: the drawer button shows for the empty-found `unavailable` state — gate it on `view.cashWire !== null`.
- Task 10: fix round 1 verified by its implementer (regeneration: no drift; weld + census 92/92; unit 1,135; typecheck/lint clean); accepted: history refused point also `sweep_block: 0`; events anchored on served_at (42 s after the history's anchor — harmless). de9e606 SPLIT (soft reset, pathspec commits) → ccc2df1 = Task 11 page (18 files) · 1d772dd = Task 10 fix round (9 files). Scoped re-review requested on 1d772dd.
- Task 10: complete — re-review all-addressed (regeneration byte-identical; ajv-valid; the near account's numbers unchanged; sha256 hashes distinct). Observation (accepted): a `sweep_block: 0` point at 18170 between swept batches implies a sweep-row reset — plausible, not re-opened.
- Task 9 round 2 committed d88c0d0 (parent 1d772dd; 8/8; typecheck/lint clean). Accepted deviation: `notComputedCause(cash, wire: RefinedPosition | null)` with a single unreachable constant. Scoped re-review 2 requested.
- Task 9: complete — re-review 2 all-addressed (the ladder reads the wire; P6/P10/P11 correct; every other arm byte-identical to f013695). Deferred minors stand: the "was not computed" emphasis for an unknowable-verdict row (Task 6's headline; unreachable on the Cash wire); the surface's drawer-button gate (Task 11's fix round).
- Task 11 review (task-11-review.md): SPEC ✅ · QUALITY needs-fixes (1 Critical, 4 Important, 10 Minor). Rulings (all accept-and-fix, integrator): C1 the API lists every engine even with zero points → legacy HF chart only when `!engineNeverPresent(engine)`; #2 legacy series on `knownBatchAxis(response)`; #3 `newestLabel` only when the newest point is plotted (computed/zero-cap); #4 "last readable" debt only when ≥ 0 (the view's rule, mirrored on the tile); #5 every wire scale through `isWireScale` before formatting → page helper `[addr]/money.ts` (`moneyFor`, `wireMoney`, `wirePrice`, `wireExact`; "unreadable scale" never a throw); #6 Trust empty words per state; #7 "batch/batches"; #8 legacy-only sub "no Cash position"; #9 `oldestPriceAge` + `humanAge`; #10 drawer verdict gated on `isComputedCash`; #11 sub-day horizons in hours; #12 pending sub undefined; #13 comment corrected; #14 Task 12 deletes; #15 ActivityTable mounted once the lookup answered (no raw→scaled flip). Plus the Task 9 note: drawer button gated on a Cash position. Fix round 1 committed d1032e4 (pathspec-limited; typecheck/lint/lint:css clean; report "Fix round 1" appended). Scoped re-review requested.
- Task 11 re-review (d1032e4): open with 2 narrow items — #3 a zero-cap point has no geometry either → label only when `newest.value !== null`; #10 wording "served no verdict for this row". Round 2 applied and committed as 3410c42 (pathspec-limited; typecheck/lint clean; report "Fix round 2" appended); scoped re-review 2 requested; reviewer's note for Task 12: `inspector-drawer` and `inspector-activity` are conditional (Cash position / lookup answered) — its pins wait on the lookup.
- Task 12: implementer dispatched (fable, serena-coder) — BASE 1d772dd; brief task-12-brief.md; report task-12-report.md. Integrator's :3111 server stopped so the e2e run builds fresh.
- Task 8: complete — re-review all-addressed (`inFlightRef` cleared by identity; a repair rides the in-flight load; `valid` gate has no state write). Pre-existing note (live-age.ts): an identical `receiptIdentity` after a successful repair leaves `blind` undischarged — served_at is per-response, so unreachable in practice; carry to Plan 4.
- Task 8: implementer dispatched (fable, serena-coder) — BASE 9418851; brief task-8-brief.md; report task-8-report.md.
- Task 8: DONE → commit b0f4f6a (lookup-error 1/1; typecheck/lint clean; no eslint disables needed; `evidence(signal)` exists). Review package review-01d28f5..b0f4f6a.diff; reviewer dispatched. Task 9 dispatch waits for Task 5's fix (its spec pins `Streak.newestKind`).
- Task 8 review (task-8-review.md): SPEC ✅ · QUALITY needs-fixes (1 Important, 6 Minor). Rulings: #1 a failed non-aborted evidence fetch → `setEvidence(null)` (foreground failure is stated; the Trust item reads "receipt unavailable"); #2 a resume repair (`keepOnFailure`) rides an in-flight foreground load (`inFlightRef`) instead of aborting it; #5 evidence fetch gated on `valid`; #3 `params` stays `loading` for a non-Cash address — by ruling, consumers gate on the Cash position (Task 11 drawer does); #4 `useAddressActivity` is double-locked by `key={addr}` on surface AND table — Task 11 reviewer verifies both mounts; #6 accepted; #7 process note. Fix round 1 → resumed the Task 8 implementer.
- Task 8 fix round 1 committed 89cf19c (1/1; typecheck/lint clean; no disables). Accepted superset: any in-flight request (not only foreground) is ridden by a repair. Scoped re-review requested.
- Task 11 (integrator) starts now against the landed interfaces (Tasks 3–10); reviews of 7/8/9/10 run in parallel — interface changes from them, if any, are folded in before Task 11's commit.
- Task 11: all page files written — `[addr]/{InspectorSurface (rewritten), InspectorTiles, BackingTable (six boundary arms), TrustCard, HistoryCard (uses Streak.newestKind), ActivityTable (scale from the wire's value_decimals; "raw units" pill; actionTitle hover), LegacyCard, StressTable (marketRealization gated by isWireScale), InspectorDrawer}.tsx`, `[addr]/page.tsx` keyed by address, `inspector.module.css` rewritten, `InspectorLanding.tsx`; gate script gains `inspector` + routes (Task 13 change pulled forward to look at the page). Typecheck clean; lint clean after five `react/no-unescaped-entities` (typographic apostrophes). Building for the first look.
- NOTE for Task 10 dispatch: after Task 5's fix lands, `Streak` gains `newestKind` → patch the plan's Task 10 weld pin to `{ batches: 14, spanSeconds: 390, newestKind: "computed" }` and regenerate task-10-brief.md before dispatching.
- Task 12 report: DONE_WITH_CONCERNS, commit a8d30a1 (parent d1032e4; 17 paths by pathspec; contract 15/15 on a fresh build of a8d30a1; whole suite 1429/10/0 on a build that predated d1032e4's landing). Review package `review-d1032e4..a8d30a1.diff`; sdd .gitignore restored.
  - Ruling: the 503 dek pin — the plan's lowercase `toContainText("no servable batch")` was a plan defect; `unavailableLookupHeadline` sentence-cases the lookup message and `inspector-headline.spec.ts` pins the capitalised string. The contract pins the page's true sentence — accepted. Same class for `error:429` → "Rate limited (429)". Costs nothing if wrong: the unit pin is the law.
  - Ruling: r3/r4/r6-fixes.spec.ts deleted rather than left as test-less shells — accepted; the ledger lines say so.
  - Ruling: deletions staged by exact path, not `git add -u web/app/inspector` — accepted (the same lesson as de9e606).
  - Ruling: retiring r1 "(10)" also dropped the Lab/Observatory/Feed/Proof/Developers intro pins and Plan 2 touches none of those pages → RE-HOME them in the fix round as one test in r1-fixes.spec.ts without the `/inspector` arm and without the Inspector-only "fed by" placement check. Cost if wrong: five copy pins a later plan retires again.
  - Ruling: `web/lib/factorPriceGuard.ts:14` stale comment naming the deleted `InspectorPositionCard` → fix in the same round (comment only).
  - Ruling: the whole-suite number on the exact HEAD is taken by the integrator after Task 12 closes (it is Task 13's precondition anyway); Task 13 stays held until then.
- Task 11 re-review 2 (3410c42): RE-REVIEW: closed (both items; closure in task-11-review.md).
Task 11: complete — commits ccc2df1, d1032e4, 3410c42 (the page + two review rounds). Deferred minors stay ledgered above.
- Task 12 review (task-12-review.md): ✅ spec compliant (brief verbatim; every R14 home verified in the named unit specs); quality: Needs fixes.
  - Important 1 (plan-mandated): the contract's resume test ("a failed background repair never replaces the rendered position") sleeps 500 ms and re-reads the state — it never observes a repair request, so r4's law is dropped, not re-expressed. Ruling: count requests inside the aborting route and `expect.poll(() => addressRequests).toBe(1)` (the repair fired and failed), keep the state pins; if a plain `pageshow` coalesces (freshness-resume predicates), use the retired r4 test's own trigger (`git show d1032e4:web/tests/e2e/r4-fixes.spec.ts`); if the page genuinely never repairs on resume, that is a Task 11 finding to report, never a weaker pin. Cost if wrong: one flaky pin, visible on the first run.
  - Important 2: the already-ruled re-homing (r1 "(10)" non-Inspector intros) and the factorPriceGuard.ts comment. Note: the comment edit is a named exception to "web/lib/** existing files are untouched" — behavior and interfaces unchanged, cost nil.
  - Minor 1: `**/v1/meta*` missing from state-matrix `mockInspector` and runbook-bsplit `mockInspectorFor` (pre-existing; the shell fetches it) → route it in the round. Minor 2: the r1 "never-touched engine" ledger line names the wrong home; it is `tests/unit/history-copy.spec.ts` (`engineNeverPresent`) → correct the line. Minor 3: the inspector-lines ledger line omits `lookupTakeaway` (re-expressed in inspector-headline.spec.ts) and `positionTakeaway`/`positionMethodLine` (died with the position card) → one clause. Minor 4: `toHaveClass(/chipWarn/)` is a CSS-module pin, brief-verbatim → pin `data-tone` only if the chip already carries it, else leave and ledger. Minor 5 (near-account history/stress bodies under the liquidatable/healthy arms; unasserted) → deferred, noted.
  Fix round 1 → resumed implementer af6fa0a1c0508b3fc.
- Task 12 fix round 1: DONE, commit 6d403ad (parent 3410c42; 6 paths by pathspec; typecheck/lint clean; contract + r1-fixes + state-matrix + runbook-bsplit e2e 78/78 on a fresh build). Resume pin: a persisted `pageshow` alone is definitive for `resumeEvidenceOf`, so the r4 clock trigger was not needed. Minor 4 stays a class pin (IdentityChips exposes no `data-tone`) — ledgered as deferred. Scoped re-review → reviewer ac8091546d76fef98.
- Task 13 (integrator): `tests/e2e/screenshots.spec.ts` gains the Inspector page with per-page `ready` callbacks (typecheck/lint clean); Step 2 had landed with Task 11. Baselines `inspector-dark.png` / `inspector-light.png` generated on a fresh build of 6d403ad; the pin spec passes 6/6 (Overview and Book baselines unchanged). Gate re-captured on the same build (`screenshot-pages.mjs inspector` + `gate3-states.mjs`: liquidatable / not-computed / no-position / cannot-compute + mockup crop), PNGs copied to the companion as `gate3-*.png`, screen `content/gate-inspector-final.html` pushed (options: A approve / B changes). Commit HELD for the owner's approval (spec §9.3). Server on :3111 left up (build of 6d403ad) for the whole-suite run and Task 14's widths probe.
- Task 12 re-review 1 (6d403ad): RE-REVIEW: closed (Important 1–2, Minor 1–3; residual noted: the hand-routed `loading` / `error:429` state-matrix cells still leave `/v1/meta` unrouted — deferred minor).
Task 12: complete — commits a8d30a1, 6d403ad.
- Whole suite on 6d403ad (fresh build; `npx playwright test`): 1432 passed / 10 skipped / 0 failed (19.7 s) — includes the two Inspector screenshot pins from the uncommitted Task 13 spec and the re-homed intros test. Task 13's hold is lifted; its commit still waits on the owner's gate.
- Task 14 opened in parallel with the gate: whole-branch review package, widths probe, Codex status.
- Task 14 · widths probe (scratchpad `widths.mjs`, server on :3111 serving 6d403ad): overview / book / inspector at 390, 1366, 1440, 1920, 2560 — no horizontal scroll anywhere; shell max 1280 / 1280 / 1280 / 1520 / 1680 (the width contract's steps); 390 renders at full width.
- Task 14 · Codex round (spec §9.5): BLOCKED-owner — Codex CLI is still 0.144.5 and `~/.codex/config.toml` still names `gpt-6-astra`, the exact environment that rejected Plan 1's round; not retried (the CLI/config is the owner's to change). Plan 1's owed round and Plan 2's round both wait on the upgrade; the slim-diff command for both is recorded in the close entry.
- Task 14 · whole-branch review dispatched (fable, owner rule) on `review-branch-d989286..6d403ad.diff` (785 KB, fixture JSON excluded, the uncommitted pin spec appended) → `final-review.md`.
- Task 14 · contrast: Plan 2 adds no raw color anywhere (`inspector.module.css` and the kit CSS additions since d989286 use only `--ink`, `--ink-2`, `--ink-3`, `--line` and type/weight tokens), so Plan 1's token contrast result (every text token ≥ 4.5:1 on bg/panel/panel-2/chip in both themes) carries unchanged.
- Task 14 · whole-branch review (final-review.md): Ready to merge? No — C1 + I1–I5 + M1–M12. Verified before ruling: `refine.ts:194,201` seals `liquidatable` (null on Aave by the openapi contract) straight into `liquidation_verdict`; `positionVerdict` is exported from `@solvent/client` (index.ts:115) with no web consumer since the old card's deletion — C1 is real. Rulings (one fix dispatch, then one scoped re-review):
  - TAKE C1: `LegacyCard` judges by `positionVerdict(position)`; "Not computed" only for a null/refused HF; contract pin: the Aave fixture's HF 1.08 card reads "Healthy" (a liquidatable pin only if a fixture exists — none is fabricated).
  - TAKE I1: a `value_decimals` failing `isWireScale` makes the Cash amounts unreadable in the reader (null amounts, the cause names `value_decimals`) so the headline prints the refused register and never a number; `collateralTable` guards `leg.decimals` / `input.decimals` → "unreadable" cells; the view's `?? 6` manufactured scale goes (`decimals: number | null`; `moneyFor(null)` prints "—"). Unit pins: negative and fractional scales.
  - TAKE I2 (two of three): `legacySeries` and `stress` move into `InspectorView`; the history sentence ladder becomes a pure lib function with per-arm pins. RULED OUT the drawer half: the drawer's provenance section IS the wire (batch, params timeline) — reading it directly is its job, not a second source of truth. Cost if wrong: one component reads the wire for display only.
  - TAKE I3: `roomSeries` gated on `!engineNeverPresent(CASH)`; an account that never held Cash gets `room = null` and the existing "No Cash history…" sentence.
  - TAKE I4: `lib/prose.ts` (`joinAnd`, `groupInt`) replaces the three joiners and four `toLocaleString` copies.
  - TAKE I5: the near-cap dek's second sentence → "…brings this account to its cap." — `debt == cap` is not liquidatable under the strict rule and a floored 3.8 % fall leaves the account above it; the old sentence asserted a verdict the law denies. Re-pinned in unit and e2e; the gate/baselines are re-captured after the round (one sentence changes on the near page).
  - TAKE M1 (drawer formula terms in the exact register so the equations balance), M2 (`oldestPriceAge` through the population guard — never a clamped 0s), M3 (provenance warn arm keeps the label "Price provenance"; the clause moves to the detail), M6 (`horizonLabel` in lib, integer days + remainder hours, pinned), M7, M8 (drop the three dead exports unless a test imports them), M9 (no `?? 0` batch label), M12 (no review-round numbers in shipped comments or test titles — the law stays, the round goes; Plan 2's own files only).
  - DEFER M4 (two age registers — exact seconds are a defensible trust detail; ledgered), M5 (`formatTenths` vs `headroomPercent` — joins the Plan 4 consolidation list with `group`/`rescale`), M10 (kit 720px breakpoint — Plan 4 kit pass), M11 (plan table drift `.tsx`→`.ts`, `stressReading` signature — note only).
  Fix dispatch → fresh serena-coder (fable); report `task-14-fix-report.md`; BASE 6d403ad. The controller's :3111 server will be killed by the round's build; baselines and the gate are re-captured after it lands.
- Task 14 · fix round: DONE_WITH_CONCERNS, commit f440ec9 (parent 6d403ad; 26 paths by pathspec; typecheck/lint/lint:css/build clean; unit 1134; whole suite 1442 passed / 10 skipped / 0 failed on its build). Deviations accepted: `horizonLabel` prints minutes under an hour (never "0h"); `InspectorView` gains `historyLoad`/`stressLoad` (`LoadPhase`) so `historyFinding`/`stressEmptyText` are pure over the view; the I1 cause reads "the engine published an unreadable value scale (value_decimals) for this account"; the drawer's balanced formula uses its existing exact register (no "$"). No Liquidatable legacy pin — no liquidatable-HF Aave fixture exists and none is fabricated (ruled). Its concern 1 misread: the Inspector baselines in the tree are the integrator's from 04:26; a one-phrase dek change sits under the pin's 1 % ratio, so they passed — regenerated on f440ec9 below anyway. Scoped re-review → the final reviewer (af06148f785e8ec3c) on `review-6d403ad..f440ec9.diff`.
- Task 13 · baselines regenerated on the f440ec9 build (`--update-snapshots all` — the default `changed` mode had left the 04:26 files in place because the dek change sits under the 1 % ratio); pin spec 6/6. Gate re-capture on the same build follows; a NEW screen file is pushed.
- Task 13 · gate re-captured on the f440ec9 build (server on :3111; `screenshot-pages.mjs inspector` + `gate3-states.mjs`), nine `gate3-*.png` refreshed in the companion, fresh screen `content/gate-inspector-v2.html` pushed (subtitle names the re-capture and the dek change). Owner click still pending (no events).
- 05:11 usage limit hit: the scoped re-review agent (af06148f785e8ec3c) was terminated while reading the package (no output); the companion server exited on its 4 h idle timer. 10:41 resumed: reviewer re-messaged with the same scope; companion restarted on the same port (`--project-dir` unchanged, no new URL).
- Companion restart made a NEW session dir `.superpowers/brainstorm/120202-1789580533/` (same port 50691, same key/URL); the nine `gate3-*.png` and the gate screen (`gate-inspector-v3.html`) were pushed there. Owner clicks now land in `120202-1789580533/state/events`.
- Task 14 · re-review 1 (f440ec9): RE-REVIEW: closed — every taken finding fixed as ruled with a pin that failed on 6d403ad; no new defect. Residuals adjudicated:
  - R1 (activity table gets the raw `value_decimals`; a refused scale ends in the client's `assertScale` route refusal, not the page's refused register) → PARKED: the law holds (never a number); the register shape joins Plan 4's notes. Cost if wrong: a refused scale on the activity route reads as a lookup failure instead of "unreadable".
  - R2 (`historyFinding` prints the definitive "No Cash history for this account in the covered window." under a lookup still loading or errored, because the `empty()` arms null `historyOutcome` while `historyLoad` is real) → FIX NOW by the integrator: an explicit arm for the lookup's own phase, pinned. A definitive negative under a refusal breaks the standing law.
  - R3 (`horizonLabel(1..59)` → "0m") → FIX NOW: a `< 60 s` arm prints "<1m", pinned.
  Both fixes: one integrator commit by pathspec; scoped 2-file check by the final reviewer.
- Task 14 · residuals R2 + R3 fixed by the integrator: commit c3ef561 (4 paths by pathspec; unit specs green; typecheck/lint clean; fresh build; inspector + state-matrix e2e 44/44). Scoped 2-file check → the final reviewer.
- OWNER GATE (spec §9.3): APPROVED — the owner clicked "A · Approve the Inspector — commit the pins" in the companion at 10:44 (events in `120202-1789580533/state/events`, screen `gate-inspector-v3.html`, captures from the f440ec9 build; c3ef561 changes nothing on the captured states). Task 13's commit proceeds after the whole-suite run on c3ef561.
- Whole suite on c3ef561 (fresh build): 1443 passed / 10 skipped / 0 failed (25.6 s), the six screenshot pins included.
Task 13: complete — commit 91fa783 lands `screenshots.spec.ts` (Inspector page + per-page `ready` callbacks) and `inspector-{dark,light}.png` baselines from the f440ec9 build, owner-approved at 10:44.
- Task 14 · re-review 2 (c3ef561): R2 and R3 closed; one adjacent item opened — `TrustCard` printed "History · no Cash history for this account" whenever `room === null` (loading, unread lookup, withheld). Ruling: same law, same shape — `historyHead(view)` in lib beside `historyFinding`, the card only places it; six arms pinned (loading, unavailable, lookup waiting, lookup not answered, withheld ≠ "no history", no Cash history, batch count). Commit b0934f5 (3 paths by pathspec; unit/typecheck/lint/build clean; inspector + state-matrix + screenshot e2e 50/50). Scoped check → the final reviewer.
- Whole suite on b0934f5 (fresh build): 1444 passed / 10 skipped / 0 failed (28.3 s).
- Task 14 · re-review 3 (b0934f5): RE-REVIEW: closed (ladder order right, pins fail on c3ef561, the card changes nothing else). Cosmetic: the `stressEmptyText` doc comment orphaned by the `historyHead` insert → moved back onto its function, comment-only commit fee9609 (integrator; lint clean; no review round for a comment move).
Task 14: complete — whole-branch review + one fix round (f440ec9) + three integrator residual commits (c3ef561, b0934f5, fee9609), three scoped re-reviews closed; widths, contrast, whole suite 1444/10/0 on b0934f5 (the comment move changes no output). Codex round BLOCKED-owner (see above).
PLAN 2 CLOSED 2026-09-16 — close entry appended to .superpowers/sdd/progress-ui-overhaul.md; workspace and the sdd .gitignore re-include committed with it (hash in git log).
